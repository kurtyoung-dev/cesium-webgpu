# Bake artifacts — regenerated by bake-stbn.mjs. The SHIPPED atlas lives in
# packages/engine/Source/Assets/Textures/Noise/, not here, and the raw .bin is
# a debugging convenience that is never bundled.
# stbn-manifest.json IS version-controlled (it is the provenance and spectral
# evidence the spec checks against) and sits outside out/.
out/
