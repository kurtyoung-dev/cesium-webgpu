# Bake intermediates and artifacts — regenerated by bake-lroc-color.mjs.
# The SHIPPED map lives in packages/engine/Source/Assets/Textures/Moon/,
# not here. Nothing under work/ or out/ is version-controlled.
# moon-albedo-manifest.json IS version-controlled (it is the alignment
# evidence the spec checks against) and sits outside both directories.
work/
out/
