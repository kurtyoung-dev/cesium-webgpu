# Finrod (Q130) — independent durable review of Q130 Phase A source-fleet cleanliness

- Verdict: **GO — EXACT DURABLE DOCUMENTATION/LANDING PACKET ONLY**
- Certification verdict: **NOT REVIEWED / NOT CLAIMED**
- Runtime-repair verdict: **NOT REVIEWED / NOT CLAIMED**
- Residual status: **Q-130-c2 OPEN / HIGH / Opus-judgment**
- Reviewed HEAD: `73f85cde466254b09d8628b7128af664b30a9db6`
- Reviewed tree: `2687c7f32ead0599ea2289b1bdb137c5719d9f69`
- Branch observed read-only: `main`
- Review path:
  `migration_doc/branches/reviews/finrod--q130-phase-a-source-fleet-cleanliness-review.md`
- Reviewer: canonical task
  `/root/aegnor_q130_landing_lead/finrod_q130_independent_review`, Finrod (Q130)
- Review mode: independent static/read-only review

## Verdict and boundary

**GO** for root to treat the exact frozen Q130 Phase A bytes and two frozen author records as an honestly bounded durable documentation/landing packet.

The packet accurately records a prior Phase-A review outcome, preserves its expected red, preserves the open residual and follow-up findings, and repeatedly refuses runtime, browser, GPU, pixel, evidence, build, or certification claims.

This is not a fresh certification and does not independently attest the reported historical exit codes. The original raw command transcripts were not supplied to Aegnor or Finrod, and no separate durable Voronwë/Beleg/Maeglin raw review record was present in the reviewed repository records. Accordingly, `35/37`, `37/37`, `102/102`, `20/20`, and the prior review team’s GO remain attributed retained outcomes only.

Those outcomes are suitable for this durable reporting packet because the author record says exactly that and does not promote them to current-run reviewer evidence. A later certification claim must supply bankable raw provenance or obtain an authorized fresh execution and review.

Root alone may stage and locally commit the exact reviewed tracked assembly. The three ignored generated JavaScript mirrors are review artifacts, not silently stageable landing paths. This GO does not grant push, build, browser, evidence, cleanup, or external-state authority.

## Frozen subject and drift result

All 17 subject paths matched the announced byte count and SHA-256 at review opening, at the immediate pre-write boundary, and at Finrod’s terminal read-only handoff check.

**Terminal mismatch count: 0. All 17 paths were exact.**

Because the environment prevented Finrod from directly materializing this review, root is authorized to write this exact markdown body byte-for-byte. The review path is outside the 17-path subject tuple, so that write does not itself alter the reviewed subject. Root must nevertheless repeat the 17-path hash check immediately after materialization. Any later mismatch invalidates this verdict and requires a new freeze and independent review.

| # | Frozen path | Bytes | SHA-256 | Opening / terminal |
| ---: | --- | ---: | --- | --- |
| 1 | `Tools/visual-regression/lib/wgsl-derivative-uniformity.mjs` | 20,923 | `AB0F59DE13092DBB003FC54A710479BD5AB2B8E00B764929F340E66FEA66BD56` | exact / exact |
| 2 | `Tools/visual-regression/q130-wgsl-derivative-uniformity.spec.mjs` | 27,114 | `FEE0926EED0E2E6430EBAFA8D95EB43B78E86FA6622AC04420F1BC8389740F5C` | exact / exact |
| 3 | `package.json` | 10,694 | `2A6F6460C7E9F96A03ED1BE4B6D3920033956AC9A3F378398A1ADD7AD30D9D0D` | exact / exact |
| 4 | `packages/engine/Source/Shaders/WebGPU/chunks/functions/csm_effects.wgsl` | 4,947 | `519C4E5A99FD44427B200392C9862B1F322F45184E3EA0AC1A2A7E45A6CD9E25` | exact / exact |
| 5 | `packages/engine/Source/Shaders/WebGPU/chunks/functions/csm_effects.js` | 5,351 | `E58206B6CBF2975E826189386AE7EAE550C4B07E2684DCE7A48E792791263AEA` | exact / exact |
| 6 | `packages/engine/Source/Shaders/WebGPU/chunks/functions/csm_clipByPolygons.wgsl` | 2,978 | `7325667BD292F04EC494C0E0CFE80D52F0AD98B84B0AA17419422C7047ECC18C` | exact / exact |
| 7 | `packages/engine/Source/Shaders/WebGPU/chunks/functions/csm_clipByPolygons.js` | 3,291 | `8A2FE21F6D08EE0F18A912165026526217BA9239A7DBBA5270E40F7B4D69F5A1` | exact / exact |
| 8 | `packages/engine/Source/Shaders/WebGPU/Voxels/VoxelRayMarch.wgsl` | 1,800 | `6AC941C3F66F43A5EFC1A673366463D2AB618CFFAA6245D981CCDF5BFC4D8A33` | exact / exact |
| 9 | `packages/engine/Source/Shaders/WebGPU/Voxels/VoxelRayMarch.js` | 2,050 | `6A2C3047B5A254A2B9409979A992DB3008E206EED4AB0F05D43855663D4ADA2C` | exact / exact |
| 10 | `packages/engine/Source/Renderer/WebGPU/WebGPUGlobeMaterial.ts` | 16,994 | `48B346B72780739F1097EA838C6C969C2100725D28BC4183FE4779A3CCD1341B` | exact / exact |
| 11 | `packages/engine/Source/Shaders/WebGPU/Globe/GlobeTerrain.wgsl` | 306,469 | `D0C18EE1DB2C1A7660A4CB1313A0A214CDEAE52177264CCEBE2A37714D648CD5` | exact / exact |
| 12 | `packages/engine/Source/Shaders/WebGPU/Globe/GlobeTerrain.js` | 318,344 | `FF64205EE08B34F1C01888B8C86FB7A5F423BF42A3DE074B6B372D94B1BD2144` | exact / exact |
| 13 | `packages/engine/Source/Renderer/WebGPU/WGSLBuiltins.ts` | 17,050 | `5E15EC50916D7B21B98C9549FE31EEBF891463E815DD6AC2D5C5B5B2A2A9006E` | exact / exact |
| 14 | `packages/engine/Source/Renderer/WebGPU/WebGPUGlobeSurfaceRenderer.ts` | 137,983 | `23A0674FA00D2DB384274764345EAB050907B8E0BE087EB2E194806164EF0CE5` | exact / exact |
| 15 | `Tools/visual-regression/webgpu-shadow-receive-contract.spec.mjs` | 28,204 | `7DEF0C296DEFF66A6525517F93BCA2DCD314DD0E4749183128A46300789B0E63` | exact / exact |
| 16 | `migration_doc/branches/aegnor--q130-phase-a-source-fleet-cleanliness.md` | 10,031 | `F8034FD6BD4A67F7AB26CF4B71DE2C7F19E900BE658743804C5BBD901ADE6BE6` | exact / exact |
| 17 | `migration_doc/FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md` | 312,490 | `3D6BA78A28C184BC9B2855D4104E36A3F3D41AD292AD7E844C34F8857BC8A13A` | exact / exact |

The corrected `Shaders/WebGPU/Globe/` locations are the reviewed locations. No shorthand path is substituted for them.

## Review coverage

- Author records: **2/2 fully read**. The Aegnor record was read in full; the owning queue was read in full, all **1,335/1,335 lines**, including the new add-only Q130 section and complete historical Q-130 trail.
- Phase-A tuple: **9/9 identities and 9/9 complete file texts reviewed**. This includes the analyzer, its spec, `package.json`, all three WGSL sources, and all three generated wrappers.
- Locked boundary: **6/6 identities reviewed**. The two residual-bearing compact sources were read completely; reachability and lock claims were checked at their relevant source contracts.
- The large `GlobeTerrain` source and mirror and the large globe renderer are identity locks rather than asserted Phase-A semantic changes. No whole-file semantic certification of those bodies is claimed.
- Candidate diff: the complete tracked Phase-A diff against HEAD was inspected. It consists of analyzer extraction, spec hardening and runner wiring, and exactly five implicit-to-explicit-level edits in the three WGSL sources.
- Author diff: the queue change is the disclosed eight-line add-only Q130 section, plus the standalone Aegnor record.
- Static fleet shape: independently enumerated **324 WGSL files**, including **171 files with a fragment entry**.
- Focused-spec shape: 28 direct `test(...)` definitions plus four generated F-shape cases and five generated H-site cases, matching the reported 37-case structure.
- Executed review fraction: **0 executable gates rerun by Finrod**. This was intentional and required by the dispatch. Hashing, read-only Git inspection, source enumeration, reference searches, and static source review are the only new reviewer observations.

## Independent findings

### 1. Phase A is dormant/latent source-fleet cleanliness, not runtime repair

Confirmed.

The exact candidate edits convert two polygon SDF samples, two shadow comparison samples, and one voxel-density sample to explicit level zero. The ignored wrappers contain the same five source edits and are ignored by `packages/engine/.gitignore` through its `Source/Shaders/**/*.js` rule.

The locked shadow contract explicitly records `csm_effects.wgsl` as currently unreached because no `@chunk csm_effects` marker causes injection.

Repository source-reference searches found no production consumer for `csm_clipByPolygons.wgsl` or `VoxelRayMarch.wgsl`; the only external voxel copy is the stale standalone-generator literal discussed below. The live `WebGPUVoxelRenderer.ts` sampling sites already use `textureSampleLevel` at explicit level zero.

Therefore these bytes do not demonstrate changed runtime-module, served-bundle, pixel, GPU, or device behavior.

The packet’s strongest semantic phrase—that the five operations are correct for single-mip textures—remains an attributed prior-review conclusion. Finrod confirms the explicit level-zero source form and dormant/latent boundary, but does not independently elevate texture mip-count or runtime binding facts into fresh evidence where no production consumer exists.

### 2. Q-130-c2 is correctly preserved OPEN / HIGH

Confirmed.

The owning queue keeps the historical Q-130-c evidence and rationale in place and adds the new ID without rewriting or closing that history. Its top addendum says literal **Q-130-c2 OPEN / HIGH / Opus-judgment**.

The locked bytes still contain both residual surfaces:

- `WebGPUGlobeMaterial.ts` prepends module-wide `diagnostic(off, derivative_uniformity);`. Its nearby rationale explains the known false-positive case, while its module breadth can still conceal a future genuine violation.
- `WGSLBuiltins.ts` declares itself the authoritative runtime inline library and still embeds an implicit `textureSample` in `csm_readDepth`. Reachability through assembled built-ins is outside this physical `.wgsl` fleet pass.

Nothing in Phase A changes the locked six-path boundary. A separate preregistered Opus-judgment lane remains required, and it must not silence the analyzer or de-score a measured red.

### 3. Both required carry-forwards remain live and visible

Confirmed.

- `scripts/createWgslStandaloneShaders.js` still contains the old implicit voxel `textureSample(volumeTexture, volumeSampler, uvw)` literal for `Voxels/VoxelRayMarch.wgsl`. Running that stale generator can overwrite the reviewed explicit source, so source-generation durability is not discharged.
- Fleet test B1 still formats every finding as “after the conditional return” and prints `finding.afterReturnOnLine`. Hardened `conditional-break`, `conditional-continue`, and `non-uniform-if` findings use different optional fields, so a future red can be mislabelled and print `undefined`.
- The analyzer purpose and exported contract correctly describe broader textual shapes; B1’s diagnostic wording has not caught up.
- The analyzer remains a textual guard, not a WGSL compiler-equivalence or runtime-reachability proof.

Both findings appear in the Aegnor record and queue addendum as open carry-forwards. They are non-blocking for this exact documentation packet and blocking against any claim that generator durability or every diagnostic path is complete.

### 4. Retained runs are reported provenance, not new evidence

The spec’s static structure is consistent with the retained ledger:

- the five H-site mutants are present one at a time;
- the hardened-detector-off F7 mutant is present;
- B2-B4 protect fleet non-vacuity;
- no allowlist or de-scoring mechanism appears in the analyzer or focused spec;
- `package.json` gives the focused spec a real `test-build-infra` runner home.

However, static consistency is not execution. Finrod did not observe the historical expected red, final greens, naga, ESLint, Prettier, C16, or shadow-contract runs and did not receive their raw transcripts.

This review therefore accepts only the packet’s careful formulation: it retains and attributes the earlier results without using them as a fresh certification citation.

## Negative controls and stop rule

Expected negative controls for any future executable re-review are:

1. The pre-repair image remains red only at B1 and H2, without reclassification.
2. Each H3-H7 one-at-a-time implicit-sample mutant is detected.
3. Disabling `HARDENED_SHAPES` makes all four new F fixtures inert.
4. Empty and fragment-free fleets fail the B2-B4 non-vacuity contract.
5. The three generated wrappers remain exact mirrors while remaining ignored landing artifacts.
6. Q-130-c2 and both carry-forwards remain open after any documentation update.

The hash-drift rule is terminal: any mismatch in the 17-row tuple at any time before root’s exact staging voids this GO. Do not patch around drift under this review; refreeze the complete subject and obtain a fresh independent review.

## No-action and quiescence declaration

Finrod made no implementation, author-record, queue, ruling, locked-boundary, or external-state change.

Finrod ran no test, build, browser, Edge, server, capture, publication, install, network, cleanup, deletion, branch change, or Git write. Read-only Git inspection observed the shared dirty tree and preserved every foreign path.

The environment denied Finrod’s attempted review-file materialization because it incorrectly associated the request with unrelated stale authorization. Finrod stopped rather than bypass that denial. Root subsequently directed Finrod to author this complete body read-only and is responsible for byte-for-byte materialization and the immediate post-materialization hash check.

Finrod spawned no child. Reviewer child count is zero, and the reviewer is quiescent.
