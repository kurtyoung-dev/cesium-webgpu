# Campaign-11 Candidate Register (Consolidated Intake)

**Sweep HEAD:** `aef553d592` (Batch 698, `main`) · **Register date:** 2026-07-18

**Sources (6 sweeps; abbreviations used in every table):**

| Key | Sweep | Primary doc(s) |
|---|---|---|
| DW | deferred-work | `migration_doc/DEFERRED_WORK.md` (full 5,422-line read) |
| C9Q | c9-queue | `migration_doc/QUEUE_2026-07-15_CAMPAIGN9.md` (§3.2 ledger + item tables) |
| C10Q | c10-queue | `migration_doc/QUEUE_2026-07-16_CAMPAIGN10.md` (§3.2/§4/§5/§6) |
| PR | perf-register | `migration_doc/PERF_ARCH_DEEP_DIVE_2026-07-16.md` (69 live findings) |
| FI | feature-inventory | `migration_doc/FEATURE_INVENTORY.md` §C (WIP) / §D (FUTURE) |
| LQ | legacy-queues | `migration_doc/ROADMAP_AND_DEFERRED_WORK.md`, C5/C7 queues, `WEBGPU_MIGRATION_BACKLOG.md` |
| FF | fresh-fallout | git Batches 656–698 + queue ledgers + DEFERRED_WORK head + `WEBGPU_DEBUGGING_LOG.md` 07-17/18 |

**Dedup:** 260 raw sweep items → **188 merged items** (72 cross-sweep duplicates absorbed; largest merge = C9-17 Slice D, 5 raw entries). No existing IDs were renamed; merged aliases are kept on the item row.

**Priorities:** **P0** = blocking correctness / standing red (gates C10-30 or crashes/renders-broken at defaults) · **P1** = high-leverage perf or parity · **P2** = solid, well-scoped · **P3** = opportunistic / research / gated epics.

**Caveats:** statuses are the source docs' own words at sweep time. LQ rows are ~160+ batches stale — **verify-premise-first** before cutting any brief. Items marked **⚠C10** are C10-owned or C10-gated: see UNKNOWNS before scheduling. Effort = source's guess (S/M/L/XL/unknown).

---

## Cluster index (22 clusters, 188 items)

| # | clusterKey | Items | Theme |
|---|---|---|---|
| 1 | `pick` | 11 | Pick fleet + pickPosition correctness |
| 2 | `standing-reds` | 15 | Standing gate reds + latent correctness sentinels |
| 3 | `splat` | 2 | Gaussian-splat production pipeline |
| 4 | `model-frontend` | 5 | Model-frontend command economics (C9-17 family) |
| 5 | `terrain-imagery` | 11 | Terrain upload/retention/residency + imagery lifecycle |
| 6 | `attachment-topology` | 8 | MRT/attachment topology + pass/depth-pack economics |
| 7 | `rte-taa` | 7 | RTE precision + TAA temporal contracts |
| 8 | `frame-delta` | 7 | Shared-frontend per-frame economics + scheduler |
| 9 | `entity-scale` | 12 | Entity/DataSource-at-scale arc (S10) + collections frontend |
| 10 | `submit-residency` | 4 | Queue timeline, submit consolidation, resource lifecycle |
| 11 | `celestial-env` | 2 | Celestial/env retained resources |
| 12 | `tiles-model-parity` | 21 | 3D Tiles + glTF model parity/perf |
| 13 | `classification-voxel` | 9 | Classification + voxel parity |
| 14 | `shadows-lighting` | 5 | Shadows + lighting |
| 15 | `atmosphere-sky` | 6 | Atmosphere/sky/sun-moon |
| 16 | `postprocess-effects` | 9 | Post-process + effects parity/visibility |
| 17 | `clouds-weather` | 16 | Cloud + weather content epics |
| 18 | `water` | 2 | Water rendering epic |
| 19 | `gated-reversed-z` | 3 | Gated tail: reversed-Z + MSAA reserve |
| 20 | `test-infra` | 16 | Test infrastructure, suites, certification |
| 21 | `build-boot` | 13 | Build variants, TS-debt, define-width, boot/TTFF |
| 22 | `arch-seeds` | 4 | Architecture-scale next-campaign seeds |

---

## 1. `pick` — Pick fleet + pickPosition correctness (11)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P0 | NEW-WEBGPU-PICKPOSITION-CONVERGENCE-REGRESSION | NOT STARTED — standing gate red | correctness | unknown | C9Q §3.2 L134; C10Q §4 "highest-attention"; FF (re-confirmed B693 OFF-oracle + B697) | Bisect vs last green; decide if :8080 lane is the supported repro | WebGPU `scene.pickPosition` never converges (frames 0–7 undefined, cachedDepth=0, zero errors; WebGL passes) + companion bimodal offline black-globe-interior (avgRGB 2,2,2, flips per session). Gates C10-30 feature-loss check. |
| P0 | NEW-WEBGPU-PICK-FLEET-LOG-DEPTH **⚠C10 (C10-11 owns)** | BLOCKED/open; C9-02B acceptance + P0-1 pick-gate flip paused behind it | correctness | XL | DW ~5241 | Is itself the blocker; ~15+ pick WGSL producers must convert in one coordinated fleet change; reconcile with C10-13 reversed-Z GO/NO-GO | Every WebGPU pick producer writes hyperbolic z while WebGL's pick FBO is log-encoded; two oracle runs proved neither partial nor consistent-hyperbolic passes the 20/500/5,000 km horizon oracle — whole fleet must move to log frag_depth (scene half landed B673). Verify C10-11 outcome at intake. |
| P0 | NEW-WEBGPU-BUFFER-PRIMITIVE-PICK-DISPATCH-PARITY | NOT STARTED (C10 W4 rider — pick up what W4 leaves) | correctness | M | C9Q §3.2 L123; C10Q §4 W4-rider row; FF | none | `pickAsync` over BufferPoint/Polyline/Polygon returns undefined in EVERY mode while WebGL returns the pickObject (zero validation errors — draw never lands/covers pixel); also WebGPU ignores `BufferPrimitiveCollection._pickObjects[index]`. |
| P0 | NEW-WEBGPU-ASYNC-PICK-PIPELINE-READINESS-CONTRACT | NOT STARTED (W4 rider) | correctness | M | C9Q §3.2 L126 + §9-74; C10Q §4; FF | none (blocks final C9-02A acceptance) | First public `pickAsync` can return a false undefined while its pick pipeline is still compiling (cold SDR, HDR flip, invalidation). Define generation-tagged awaitable readiness / bounded replay preserving latest-wins hover, no eager resources, no render blocking. |
| P0 | NEW-WEBGPU-COMPUTE-INSTANCE-PICK-INDEX-MIRROR | NOT STARTED — standing gate red | correctness | S–M | C9Q §3.2 L125; C10Q §4; FF | Bisect vs probe's last green run | `probe-compute-instance-pick` FAILS: instances 0 and 2 vertically swapped (middle correct, zero errors) — canvas-Y mirror convention drifted between probe and `computePickingDrawingBufferRectangle`/readback. Pre-existing (byte-identical with B672 stashed). |
| P1 | NEW-COLLECTION-PICK-2DCV-PIPELINE-KEY-PARITY | NOT STARTED (W4 rider) | correctness | M | C9Q §3.2 L127 + §9-75; C10Q §4; FF | Interacts with C9-10 P0 key-widening (collection key bits 0–31 full) | Billboard color pipeline entries include the 2D/CV no-depth bit but pick lookup uses defines alone; Point/Polyline build hard-coded depth-tested pick variants — repair 3D/2D/CV/morph pick cache identity + depth behavior. |
| P1 | C9-02A-WEBGPU-PICK-DEPTH-PLANE-PIPELINE-PARITY | PARTIAL / PAUSED | correctness | M | C9Q §3.2 + §4 W0-2A | Async-pick readiness contract; physical device-loss half owned by C9-04 | Full physical Edge matrix passed (shared shader/layout, SDR/HDR, MSAA1/4, resize, deterministic invalidation); acceptance paused on the two systemic findings the matrix exposed. |
| P1 | FAR-107-PICKQUERY-CONTRACT | NOT STARTED | infra | M | C9Q §5 W1-12 | none (deliberately sequenced before packed-depth) | Contract-approval slice: immutable query/result, generation, cancellation, output demand, honest sync/async semantics for EVERY pick family. Foundation for packed-depth (FAR-408), contiguous pick-ID ranges, C9-19 hover gate. |
| P1 | NEW-PICK-WEBGPU-MULTIFRUSTUM-PACKED-DEPTH / FAR-408-C0 *(merged: PR S4-5 + S7-4 depth-pack demand/version-tracking)* | NOT STARTED | perf | L | C9Q §5 W1-13; PR §5 S4-5 + §8 S7-4 | FAR-107; FAR-200-S1; correctness trap — naive zero-command gating changes pick semantics; depth-version tracking required | Globe-depth RGBA8 pack chain runs up to 3×/frustum (up to 9 fullscreen packs + 18 boundary crossings/frame vs WebGL's 1–2 copies). Make demand-driven via monotone depth-version tracking + coalesce to 1 pack/frustum + capture requested pick pixels at natural frustum boundaries on the MAIN encoder (no private submit). |
| P1 | NEW-POLYLINE-APPEARANCE-PRIMITIVE-WEBGPU (pick remainder) | open — pick attempted+REVERTED (B380), root cause PINNED | correctness | M | DW ~4830 | Root cause pinned: screen-space ribbon expansion uses main-canvas viewport under the narrow pick frustum — thread pick-FBO viewport dims into `writeRTEUniformsPolyline` (or full-canvas pick frustum) | Appearance-based polylines unpickable on WebGPU (WebGL hits). Verified pick VS/FS recoverable from B380 diff; RED probe `probe-polyline-appearance-pick.mjs` committed and ready to gate the fix. |
| P2 | BACKLOG-§4 Picking 6.1 main-scene depth blit | pending (globe half done) | parity | M | FI §C.5 | Coordinate with C10-11/12 so the blit consumes post-conversion depth encoding | Main-scene depth-blit shader for picking still pending — pickPosition depth over non-globe content (models/tiles/primitives) lacks the blit path the globe has. |

## 2. `standing-reds` — Standing gate reds + latent correctness sentinels (15)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P0 | NEW-HIGH-DENSITY-SPHERES-CROSS-BACKEND-DRIFT | NOT STARTED — standing visual-gate red | correctness | unknown–M | C9Q §3.2 L135; C10Q §4; FF (re-confirmed 8.62% at C9-30 close-out) | Bisect vs suite's last green; drift repair MUST precede baseline recapture (`--update` auto-blocked 2026-07-18) | `high-density-5k-spheres` fails 8.69→8.62% cross-backend (threshold 2%) AND ~92% vs history on BOTH backends; stored baselines are degenerate fully-black. Gates C10-30. |
| P0 | NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION | NOT STARTED (W4 rider) | correctness | M | C9Q §3.2 L124; C10Q §3.2 C10-03 row (stash-attributed) + §4; FF | none | Runtime `scene.msaaSamples` flips leave scene-FB COLOR pipelines bound 1–2 frames with stale baked `multisample.count` → "Attachment state not compatible" + invalid command buffer + GlobeDepth destroyed-texture follow-on under resize; per-renderer `_scenePipelineFormatGeneration` guards rebuild one frame late. |
| P0 | NEW-VOXEL-INSIDE-CAMERA-BLACK | open (no fix in git) | correctness | M | DW ~3456; C9Q §5 W1-18 | none | Camera INSIDE the voxel proxy volume (0.55R–0.9R) renders BLACK on WebGPU while WebGL renders the interior; suspects: proxy-cube `cullMode:'front'` vs camera-inside near-plane/RTE vertex path, not the march. Repro: probe-voxel-megatexture PART-3 @0.55R. |
| P1 | NEW-WEBGL-ANISO-GLSL-BROKEN | open (found B210) | correctness | S | FI §C.3 | none | WebGL model FS fails to compile for KHR_materials_anisotropy assets (`normalTexCoords` undeclared / `computeTangent` no overload) — error dialog halts rendering; blocks all aniso parity diffs. |
| P1 | NEW-FEATURE-RENDERER-FAILED-STATE-RETRY | NOT STARTED | correctness | M | C9Q §9 W5-81 | none | FR readiness state machine's `failed` is terminal per generation (`GraphicsContext.ts:2010`) — ONE transient chunk-fetch failure permanently disables a feature for the session. Add bounded retry + `retryFeatureRenderer(key)`. |
| P1 | NEW-WEBGPU-POINT-BLENDOPTION-SYNC | NOT STARTED (cheap correctness intake) | correctness | M | C9Q §3.2 L141 + §9-80; C10Q §4; FF | none | Native WebGPU PointPrimitive branch returns before syncing collection `_blendOption` — even `BlendOption.OPAQUE` can emit Pass.TRANSLUCENT-labeled commands. Repair classification/batching/order/depth/pick/2D-CV-morph parity. |
| P1 | NEW-WEBGPU-CANVAS-BACKGROUND-COLOR-PARITY | surfaced, verified vs live code, NOT fixed | parity | S | DW ~5298 (C9-07 latent) | Visible behavior change — needs a WebGL-vs-WebGPU background probe first | Empty WebGPU scene presents transparent black (`_clearColor` (0,0,0,0), background `_clearColorCommand` swallowed) while WebGL clears to `scene.backgroundColor`. Fix: adopt cmd.color into the C9-07 deferred first-open clear. |
| P1 | NEW-WEBGPU-OIT-DEFERRED-SPLAT-CANVAS-RESUME | surfaced, NOT fixed | correctness | S | DW ~5305 (C9-07 latent) | Needs a splats+OIT visual probe before redirecting | TranslucentPass resumes the DEFAULT (canvas) pass after the OIT composite and draws deferred splats there; post-process blit later overwrites the canvas — likely invisible splats in OIT+splats scenes. |
| P1 | BUG-GLOBE-PIPELINE-NAME-AXES | open (worst instance fixed B487) | correctness | S | DW ~5099 | none | Globe pipeline cache key still omits strideBytes/hasWebMercatorT (vertex layout!), geodetic normals, LOG_DEPTH/IMAGERY_REDUCED defines, `_sampleCount` — silent race-decided wrong-pipeline-bind class. Fold missing axes into the name + audit other name-keyed central caches. |
| P1 | C-R12-PER-OBJECT-CACHES | WIP | correctness | S | FI §C.8 | none | Device-loss invalidation walk doesn't reach `model._webgpuCache` / `clippingPlanes._webgpuCache` — stale GPU resources after device recovery; latent crash/corruption on the loss path. |
| P1 | BACKLOG-§Material UBO field-name alignment audit | incomplete | correctness | M | FI §C.8 | none | JS-pack vs WGSL-struct name/offset audit incomplete across ~25 material types — silent data-corruption risk (wrong values, no validation error). |
| P2 | NEW-WEBGPU-DEBUG-DEPTH-PLANE-GATE-PARITY | NOT STARTED (cheap intake) | parity | S | C9Q §3.2 L140 + §9-79; C10Q §4; FF | none | `Scene.updateEnvironment` honors `debugSkipDepthPlane` but `WebGPUContext.updateAndClearFramebuffers` overwrites `environmentState.useDepthPlane` without the gate — restore diagnostic parity (true→false→true provable). |
| P2 | NEW-WEBGPU-OIT-MSAA-RESOLVE-ORDERING | DEFERRED (FAR-003 adjacency; OIT contained OFF at defaults) | correctness | M | DW ~5395 | Belongs to FAR-003 OIT×MSAA lane | Under MSAA a later frustum's scene-FB re-dirty + demand resolve overwrites the mid-frame OIT composite in `_sceneColorView`. Fix: dedicated non-multisampled accum target or post-frustum composite. |
| P2 | NEW-WEBGPU-RENDERCOMMAND-STALE-PASS-SLOT | latent (no current caller exercises the branch) | correctness | S | DW ~5292 (C9-07 latent) | Needs a consumer probe first | `RenderCommand.js:345` reads `context._currentRenderPass` which doesn't exist on WebGPUContext (real slot `_currentRenderPassEncoder`) — immediate-mode WebGPU path always falls through to passState. |
| P2 | OPEN-1-DIAGNOSE | root cause TBD — may be stale | correctness | unknown | FI §C.7 | Verify still reproduces at HEAD (B214/504 sky fixes may have mooted it) | Sky-atmosphere shader compile failure with undiagnosed root cause — P-level if it still reproduces. |

## 3. `splat` — Gaussian-splat production pipeline (2)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | NEW-WEBGPU-SPLAT-DATA-PRODUCER | FILED 2026-07-18 (premise CONFIRMED; C10-04 STOP-AND-BLOCK #1) | feature | L | DW ~23; C10Q §3.2 C10-04 row; PR §7 S6-1 context; FF | Placement decision (WebGPU branch in `GaussianSplatPrimitive.update` pre-FR-return vs inside FR); no offline .spz/glTF-splat asset in-tree (or build faithful synthetic builder) | WebGPU splat FR has NO production data producer — `update()` returns at :1197 before commitSnapshot/buildGSplatDrawCommand; `_splatData`/`_renderResources` never assigned (grep=0). Pack loaded snapshot into 16-float/64B interleaved record + expose model-space positions. Until then the splat renderer (+ B288 log-depth/sort + C10-09 velocity leg) is SCAFFOLDED-not-SHIPPED. |
| P2 | C10-04-SPLAT-ASYNC-SORT **⚠C10** | BLOCKED (premise-broken; escalated to maintainer) | perf | M | C10Q §3.2+§5; PR §7 S6-1/§12 S11-2; FF | NEW-WEBGPU-SPLAT-DATA-PRODUCER must land first; do NOT build async machinery before the producer | Replace synchronous main-thread comparator sort in `maybeSortSplats` (1M splats = 20–40M comparator calls, 1–4 s frozen frame) with shipped `GaussianSplatSorter.radixSortIndexes` WASM worker consumed one-frame-stale (fills unused `sortRequestPending` scaffolding). |

## 4. `model-frontend` — Model-frontend command economics (5)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | C9-17-MODEL-SETTLED-FRONTEND-REVISIONS Slice D *(merged: renderer-side implicit-FID change-gate spec; PR S9-1/S11-4/S11-5/S9-5 collections sibling)* | PARTIAL — A+B+C landed B687/688; Slice D STOP-gated | perf | L | C9Q §3.2+§6 W2-33; C10Q §4 seed; PR §10/§12 (§13 C9-17 riders); FF ×2 | STOP-gated: open only if C9-30/C10-30 attribution names model-frontend allocation; G9 invariant 5 + mutation STOP carry over; renderer-side spec needs full WebGPUModelRenderer exercised | Settled WebGPUDrawCommand/frontend reuse: model FR rebuilds the whole command graph per primitive per frame (~1,800 transient objects, ~2,600 writeBuffers, ~1,000 bind-group builds/frame @400 tiles vs WebGL's ~600 retained refs). Deliver persistent per-primitive commands + scene-wide effects BG + dirty-gated 768B material write + ONE shared light UB (~518KB/frame redundant) + S9-5 collections command-reuse sibling. Landed halves: group-1 creates 320→0, O(1) geometry validation 240/240. |
| P1 | S9-2 — effects bind-group memoization | DEEPER-ON-KNOWN (C9-11/C9-17 riders, unlanded) | perf | S | PR §10 S9-2 | none | `createEffectsBindGroup` runs full 22-segment WeakMap-id string key + fresh DataView + 480B repack per tile AND per model per frame even on 100%-hit frames (300–600 invocations/frame with LUT/shadows/clipping on); frustum loop mints fresh globe-depth views so armed inline edges force a NEW bind group per model per frame. Memoize resolved tuple per (owner, frameNumber), hoist DataView, cache view per texture. |
| P2 | S9-3 — retained-command executor unification | NEW (§14 seed 4) | perf | L | PR §10 S9-3 | Sequence after C9-17 Slice D (command shapes stabilize first) | Hottest loop (300–2,000 dispatches/frame) wraps EVERY command in try/catch, duck-types across shapes, 8–15 megamorphic derivedCommands loads per command per pass per frustum. Single canonical shape or integer commandType + switch; precondition for FAR-405/706 bundles paying off. |
| P2 | S9-4 — GPU-cull feed pooling | DEEPER-ON-KNOWN (FAR-003 rider) | perf | S | PR §10 S9-4 | none | Cull FEED re-extracts every command BV into a fresh Float32Array + re-uploads spheres/planes every frustum every frame above the 384-command gate (output pooled B213, input never). Grow-only pooled array versioned by command-list generation. |
| P2 | S11-1 remainder — WebGPUModelFeatureId batch-texture force-create *(merged: C10Q C10-02 carve-out row)* | Explicit "separate slice" scope exclusion from C10-02 | perf | S–M | PR §12 S11-1 fix clause; C10Q §5 C10-02 row | Sequence after C10-02 lands; verify FLAG_HAS_BATCH_TABLE dynamic-flip interplay with its styleCommandsNeeded gate | `WebGPUModelFeatureId.js:287-297` force-creates the batch texture (opaque-white fill) for EVERY feature-table primitive → permanent shader flag + per-fragment batch-table sampling for unstyled tilesets. Make lazy on first style mutation with dynamic define flip. |

## 5. `terrain-imagery` — Terrain upload/retention/residency + imagery lifecycle (11)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | C9-12-TERRAIN-STATIC-DYNAMIC-UPLOAD-SPLIT / FAR-303 (Option A) | DEFERRED/BLOCKED — VALID BLOCK (premise REAL, no code) | perf | XL | DW ~5231; C9Q §3.2+§6 W2-30; C10Q §4 seed; FF | C9-11 remainder must land first (revision-keyed packet store = the only correct "static bytes unchanged" signal; wrong signal = silent visual corruption); C10 disposition: dedicated multi-batch family, do not open inside a wave | Both group-0 packers stage the FULL 3072 B/tile/pass every frame (~115 MiB/route). Split into ViewUniforms (once/frame) + TileDynamicUniforms (~128–256 B ring) + persistent TileStaticUniforms slab = ~12× staging cut. Per-tile persistent full buffers FORBIDDEN (breaks 99.66%-hit B292 bind-group identity cache). Atomic WGSL+packer+BG-cache redesign; S0–S4 phasing recorded. |
| P1 | C9-11-RETAINED-TERRAIN-DESCRIPTORS / FAR-309 (remainder) | PARTIAL/PAUSED — execute-closure hoist LANDED B682 byte-identical | perf | L–XL | DW ~5229; C9Q §3.2+§6 W2-29; FF | Deferred by correctness risk: multi-view offset-clobber aliasing + stale-pipeline/derived-command hazards on HDR/MSAA/log-depth flips; needs dedicated multi-batch acceptance matrix (water/clipping/shadow/exaggeration/2D-CV-morph/multi-view/device-loss) | Retain TileDrawDescriptor/command-wrapper objects + readyLayers/commands/passLayers scratch (~39,300 allocs / 1,189-frame route, ~33/frame) keyed by an exact revision tuple with in-place dynamic-offset + BoundingSphere updates. Also the prerequisite store C9-12 consumes. |
| P1 | C9-15-TERRAIN-GPU-RESIDENCY-BUDGET / FAR-203 / FAR-208 | NOT STARTED | perf | L | C9Q §8 W4-47 (+§3.1 owner row) | FAR-200-S1 serial authority (retirement after last-use completion serial) | Terrain GPU buffers grow with no production byte-budget/retirement owner (509 net entries / 5.51 MiB with zero retirement measured; imagery half fixed by C9-12A). Budget BYTES, pin leased, grace-LRU, retire once/frame after traversal, prove byte-slope plateau over altitude loops. |
| P1 | NEW-WEBGPU-OCEANNORMAL-PER-CALL-REUPLOAD | FILED (premise CONFIRMED by runtime instrumentation; pre-existing) | perf | S–M | DW ~21 (B685 spinoff); C10Q §4 W1 cheap-rider (unowned); FF | none | `_createWaterOceanMaterialBindGroupInner` re-uploads the ocean normal map EVERY call (measured 20,155 mip enqueues ≈37/frame over 540 frames): fresh GPUTexture + 9-level mip chain per tile per pass; fresh view identity defeats group-2 BG cache. Fix = source-identity memo mirroring `_materialTextureCache` with explicit invalidation; keep waves animating (frozen-ocean is a real failure class); clear on/off job-count oracle. |
| P1 | C-R1-GLOBE-RENDERSTATE | WIP | correctness | M | FI §C.1 | none | GlobeSurfaceRenderer builds pipeline variants from hardcoded render state instead of consuming upstream `command.renderState` — any upstream per-pass state change silently ignored on the highest-traffic draw path. |
| P2 | S1-1 — WebGL-lane globe derived-command regen | DEEPER-ON-KNOWN (C9-11 scope-extension rider) | perf | M | PR §2 S1-1 (+§13) | Builds on landed C9-11 retained tile commands | WebGL-lane globe tile commands set dirty=true every frame (free-list recycling) → full derived-command regeneration for EVERY globe command (~1,800 shallowClones/frame, re-paid by shadows + pick mini-frames). Extend retained per-tile identity to the WebGL lane or scope regeneration. |
| P2 | S6-3 — uniform-ring fan-out beyond terrain | DEEPER-ON-KNOWN (C9-12/FAR-303 extension) | perf | M | PR §7 S6-3 (+§13) | Coordinate with the blocked C9-12 remainder (same WGSL+packer+BG-cache family) | The FAR-303 staged ring has ONE consumer (globe camera UB) while the frame fans out 100–500+ individual per-object writeBuffers (~0.4–2 ms CPU busy scenes). Extend ring + dynamic-offset bind to Primitive/Polyline camera UBs + collection/effect uniforms. |
| P2 | S5-4 — per-tile worker-computable scans | DEEPER-ON-KNOWN (C9-11 claim-boundary extension or standalone) | perf | S | PR §6 S5-4 | none | Terrain post-processing re-does worker-computable work on the render thread (two O(indexCount) max-index scans + index up-conversion + water-mask row flip per tile-buffer miss) exactly on descent-spike frames. Ship maxIndex/stride as scalars from the terrain workers. |
| P2 | S3-3 — GlobeTerrain debug-sentinel stripping | NEW (§14 seed 11) | perf | S | PR §4 S3-3 | none | ~20 debug-visualization blocks + 7 bypass predicates compiled into the production GlobeTerrain FS (runtime-gated, not ifdef-stripped; ~17 extra MRT return sites) — wrap under one GLOBE_DEBUG_SENTINELS ShaderDefine flipped by `CesiumDebug.globeFragmentDebug`. |
| P2 | Streamed-imagery never-shared prompt-retire verification lane (B686 F2a) | intake row — NOT STARTED | tooling | S–M | C10Q §4; FF | none | The F2a path (unique per-tile ImageBitmap realizations retire promptly at zero refs) has NO probe route exercising it (GridImagery shares tiles; verification was review+types only). Build a real-imagery direct-upload lane asserting bounded zeroRefBytes + retirement counters. |
| P2 | DP-H19-SHADER-DECODE-RUNTIME | scaffold landed; runtime flip remaining | perf | M | FI §C.1 | none | GPU compressed-vertex decode scaffold landed but the runtime flip + per-shader expansion remain — quantized attributes still expand on CPU (upload bandwidth + memory on terrain/model hot paths). |

## 6. `attachment-topology` — MRT/attachment topology + pass/depth-pack economics (8)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | C9-10-CONSUMER-DRIVEN-MRT / FAR-403-C0 | DEFERRED/BLOCKED (premise REAL; no code) | perf | XL | DW ~5227; C9Q §6 W2-27; FF | P0 prerequisite: MRT-topology dimension in the pipeline-cache key of ALL 31 makeSceneFBTargets renderers (collection-family key is 32/32 bits — widen); Gate-B-class correctness coupling; `CesiumDebug.attachmentDemand(false)` refuses until the audit lands | `_mrtMode` hardcoded true → ~16 MB rgba16float G-buffer + MSAA companion + slot-1 resolve allocated/bound every non-pick frame with zero consumers. C9-09 demand record already exists; mid-session consumer toggle with today's caches returns stale 1-target pipelines (black frame). NEVER hardcode `_mrtMode=false`. Phasing: P0 key audit → P1 demand-wire → P2 default flip. C10-03 covered only the scene-COLOR resolve half. |
| P1 | S4-2 / S4-3 / S4-4 — C9-35 MSAA containment remainder | NEW (bundle remainder beyond landed C10-03 parts b+d) | perf | M | PR §5 (§13 C9-35 items 3–5) | C10-03R default-flip is a maintainer-reserved lever — do NOT fold in; FAR-405 pass-consolidation interplay | (a) MSAA scene color allocated TEXTURE_BINDING|COPY_SRC (can disable driver compression → 1.5–2× color traffic on every draw) — make RENDER_ATTACHMENT-only; (b) consumer-gate the unconditional per-frame fullscreen MSAA depth resolve (unread on default path, ~12MB + ~75MB boundary/frame); (c) depth/stencil storeOp:'discard' on the final segment + stencil-less depth on classification-free frames. |
| P1 | S7-2 — per-frustum fixed pass scaffold gating | DEEPER-ON-KNOWN (FAR-405/FAR-401 HP-03; multiplier half fixed by C10-01) | perf | M | PR §8 S7-2 | S7-4 depth-version tracking guards any gating of the DP-H45 repack (pick semantics) | Pass scaffold remains content-UNGATED post-C10-01: globe-depth pack gated only on useGlobeDepthFramebuffer (not per-frustum globe command count); clearGlobeDepth second clear + DP-H45 re-pack frame-flag gated; ~12 scene-FB pass boundaries + 4 fullscreen RGBA8 packs per default frame from loop structure alone (~300–480MB/frame attachment traffic). |
| P2 | S2-5 — pass-reopen descriptor caching | DEEPER-ON-KNOWN (FAR-405/706 parent; C9-09 natural owner) | perf | S | PR §3 S2-5 | none | Every scene-FB reopen re-maps + spread-clones attachment descriptor arrays twice across 16 reopen sites (~60–180 transient objects/frame) + per-frustum packedDepth createView + recreated depth-hook closure. Cache one frozen load-variant descriptor pair per framebuffer generation. |
| P2 | S7-5 — multi-frustum contract machinery | NEW (3D impact largely mooted by C10-01; remainder stands) | perf | S | PR §8 S7-5 | none | Live remainder: single-frustum camera double-pack + byte-duplicate per-slice UBs on 2D/CV and sky-only-fallback (2-frustum) frames; frustum idx≥1 allocates a dedicated 65,536-object GPU culler ignoring band command count. Reuse static buffer when repackPerSlice===false; size/skip aux cullers by band count. |
| P2 | Seed-10 cleanup wave — S6-6 / S6-4 / S4-6 / S4-7 | NEW (§14 seed 10 small-slice wave) | perf (+2 real bugs) | M | PR §14 seed 10 (§7/§5 findings) | S4-7 removal needs FEATURE_INVENTORY pick-via-ID cross-check (Principle 7 — prefer lazy-allocate) | Four slices: voxel tile streaming per-tile CPU RGBA expansion + scalar f16 conversion re-paid on LRU re-entry (10–30 ms/tile hitching); WebGPUBufferMapper cache never repopulates + drops offset arg + mappedAtCreation drops data (genuine correctness bugs — repair-or-retire); PP tail always appends an identity blit (+16.6MB r/w); full-res ID target (~17MB) allocated with zero consumers — lazy-allocate. |
| P2 | Phase-8a / FEAT-GAP-01 — normal G-buffer + depth prepass | FUTURE — "single highest-leverage infra gap" | infra | XL | FI §D.2 | none hard | Unblocks decals, full clearcoat/sheen/aniso BRDFs, iridescence LUT, env probes, better AO/GI; natural continuation of the C9-10 MRT topology flip across the 31-renderer fleet. |
| P2 | Phase-8a normal-G-buffer validation | OPEN (P2) | tooling | S | LQ §7 | none | Producer + MRT slot-1 ship off-by-default but the payoff-validation probe (does any consumer benefit?) never ran — the decision input for keeping/using slot-1 normals. |

## 7. `rte-taa` — RTE precision + TAA temporal contracts (7)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P0 | NEW-TAA-CUSTOM-FRUSTUM-JITTER-FALLBACK | NOT STARTED — crash-class | correctness | S | C9Q §9 W5-84 | none | TAA + a custom frustum THROWS every frame mid-executeCommands (`WebGPUSceneRenderer.ts:1824`). Fall back to un-jittered `frustum.projectionMatrix` with a one-time pragma-wrapped warn; keep the throw debug-only. |
| P1 | C9-24-RTE-PRODUCER-CONSUMER-INVENTORY / FAR-305 | NOT STARTED | correctness | M | C9Q §7 W3-40 | none (R0 foundation) | One reviewed helper + machine-readable shader/CPU inventory + GPU numeric oracle asserting camera-relative reconstruction for ALL current/previous consumers. Prerequisite for C9-25/26/27/28 + Gate E. |
| P1 | C9-25-PREVIOUS-FRAME-RTE / FAR-306 | NOT STARTED | correctness | L | C9Q §7 W3-41 (+§3.1 owner) | C9-24 | Convert Billboard/SDF-label/Point/Polyline/ComputeInstance/Model velocity to matching previous-frame high/low camera-relative math (temporal shaders currently reconstruct absolute ECEF f32). W8/C10-09 landed only the prev-buffer upload economics, not this conversion. |
| P1 | C9-26-GPU-VISIBILITY-RTE-CLOSURE | NOT STARTED | correctness | L | C9Q §7 W3-42 (+§3.1 owner) | none | Hi-Z/SoA GPU visibility stores unsafe absolute ECEF + lacks exact view/depth identity — store camera-relative or high/low, expand conservative radius, tag frame/camera/view/frustum/depth/device generations. Precision prerequisite for ever restoring auto GPU cull/Hi-Z/sort/indirect (FAR-003/T7 tail). |
| P1 | NEW-TAA-MULTIFRUSTUM-DEPTH-REPROJECTION-CONTRACT / C9-29 | NOT STARTED | correctness | L | C9Q §5 W1-15 (+§3.1 owner) | none | Give every contributing natural frustum exact current/previous depth, projection, jitter, history identity (the confirmed "natural-frustum TAA mismatch" owner). Acceptance = moving high-contrast near/far oracle distinguishing TAA on/off, camera vs object motion, clears, teleports. |
| P2 | TAA-DESIGN Slices 2b+3 | pending | parity | L | FI §C.7 | none | Per-model MRT motion vectors for skinned/morphed/instanced prims (2b); YCoCg 3×3 variance clipping; particle previousPositionWC (3) — animated content ghosts/smears under TAA until these land. |
| P2 | TAA-DESIGN Slice 4 | pending | correctness | L | FI §C.7+§C.2+§C.5 | Slices 2b+3 | TAA correctness interactions: pick depth-readback un-jittering, 3D Tiles pop-in MV NaN reject, CSM+TAA shadow-edge motion verification, WebGL parity path. |

## 8. `frame-delta` — Shared-frontend per-frame economics + scheduler (7)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | S1-3 — globe-height plumbing rebuild | NEW (unowned) | perf | M | PR §2 S1-3 | none | Every camera-moved frame re-runs the synchronous recursive height walk + globe.getHeight tile descent + teardown/rebuild of the updateHeight callback web (≥5 closures, 2 listeners) + quadtree add AND remove propagation — part of the shared 4–5 ms frontend floor on BOTH backends. Persistent registration + threshold-gated recompute. |
| P2 | S1-5 / S7-6 — 2D/CV/ortho band economics | NEW (§14 seed 9) | perf | M | PR §2+§8 | none | SCENE2D antimeridian-wrap frames re-run the ENTIRE shared frontend twice (commands are viewport-invariant); 2D band-split reaches ~16 frusta each paying full uniformState.update + repack; ortho 3D forces 3 bands. Generate once per wrap frame; skip fixed blocks for empty bins; tight ortho near/far. These are the modes reversed-Z can never help. |
| P2 | S2-2 / S2-3 / S2-4 — cache-hit-path allocation riders | NEW (riders adjacent C10-10/C9-17/C9-23/C9-29) | perf | S | PR §3 | none | (a) shadow/CSM cast path builds extraEntries + binding objects per caster per frame BEFORE the BG-cache check (~120K allocs/sec @100 casters ×4 cascades) — hoist the check; (b) WebGPUBindGroupCache builds its string key + entry arrays on every lookup INCLUDING hits for six PP effects — adopt the globe slot-handle contract; (c) TAA resolve creates an uncached bind group every frame despite a 2-state ping-pong tuple (~20-line fix). |
| P2 | NEW-CLUSTERED-ENABLED-ZERO-LIGHT-FRAME-ZERO-WORK *(merged: C10Q "C9-16 enabled-multi-frustum evidence" seed — evidence half SATISFIED by B679 cert)* | DEFERRED (not on any default path; behavior locked by B679 cert) | perf | S | DW ~5335; C9Q C9-16 row residual; C10Q §4; FF | none | Clustered ENABLED + zero lights still allocates lights[]/areaLights[] arrays, ends+resumes the canvas pass, writes the 32 B params UBO every frame. Fix (~50 lines per C9 guide §C9-16 Step 1): last-written count tracking + module scratch + skip pass end/resume, preserving buffers-stash identity + the N→0 transition write. |
| P2 | C9-08 octree persistence / NEW-SCENEOCTREE-DIRTY-REVISION-REBUILD-AND-PVS-PROMOTION | DEFERRED (opt-in only; zero default-path exposure) | perf | M | DW ~5312; C10Q §4 seed; FF | none | Enabled SceneOctree rebuilds every frame (build clears + re-inserts). Before auto-promotion: (a) revision/dirty signal so static sets skip rebuild, (b) measured proof it beats Scene PVS on the moving route >200 commands, (c) on/off/restored oracle; extend probe-scheduler-octree-demand case E. |
| P2 | C10-10 follow-up — revision-maintained shadow-caster sublist | follow-up surfaced at C10-10 landing (blocked seed) | perf | M | C10Q §3.2 C10-10 row (B695); FF | S1-6 retained-commandList tier | C10-10 folded caster collection into the single PVS walk (landed; banner not claimed — offline K≈N). The true win (N≫K with re-dirtying globe/tile casters) needs a revision-MAINTAINED caster sublist, blocked on the retained-commandList tier. |
| P3 | WebGL near-ground seg5 p99 GC-tail (no ID) | next-campaign seed (note-only for C10-30 noise budgeting) | perf | unknown | C10Q §4; FF; C9Q C9-30 row | none | WebGL seg5 p99 65.8→70.6 ms at C9-30 with zero WebGL code changes — likely allocation/GC pressure in the shared scene path; investigate as a C11 seed; meanwhile inflates the C10-30 noise budget. |

## 9. `entity-scale` — Entity/DataSource-at-scale arc + collections frontend (12)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | Entity-at-scale arc (S10 umbrella, register §14 seed 3) | next-campaign seed — deliberately not opened in C10 | perf | XL | C10Q §6; PR §11/§14; FF | Benchmark lane first | The Entity/DataSource layer's per-entity per-frame cost does not scale — a whole C11 arc (visualizer batching, property evaluation, geometry updates) whose concrete findings are the S10-* rows below. |
| P1 | 10k-entity benchmark lane (§14 seed 3 prerequisite) | prerequisite — "build the lane first" | tooling | S | PR §14 seed 3 | none | Every S10 finding is invisible to the current gate (no campaign scene contains entities). Add a 10k-entity lane (static / clustered / dynamic-mover / path-tracking) to run-performance-campaign.mjs so S10 slices get on/off/restored p95 evidence. |
| P1 | S10-1 — dynamic-entity fallback lane (supersedes S1-4) | NEW (S10-A) | perf | L | PR §11 S10-1 | Benchmark lane | Dynamic lane is O(N × 10–35 megamorphic Property reads)/frame with no dirty-tracking on BOTH backends (10k dynamic = 3–15 ms CPU floor); any dynamic position or clamp categorically forfeits the bulk static lane. Build "static-except-position" third lane + definitionChanged memoization + SampledPositionProperty segment cursor + clamped-static sub-lane. |
| P1 | S10-2 / S10-3 — clustering forfeits the bulk lane + declutter rebuild | NEW (S10-F; extends NEW-ENTITYCLUSTER-GPU-MERGE) | perf | L | PR §11 | S10-3 storage/clustering decoupling prerequisite; benchmark lane | `cluster.enabled` routes EVERY entity to the legacy O(N)/frame lane (bulk 50–1400× win and clustering mutually exclusive BY CONSTRUCTION); one dynamic mover forces FULL declutter rebuild every frame (removeAll + all-N projections + fresh KDBush). Decouple storage from clustering; displacement-threshold-gate `_clusterDirty`; incremental declutter; finish GPU projection. |
| P2 | S10-4 — GeometryUpdaterSet lazy instantiation | NEW (S10-B) | perf | M | PR §11 S10-4 | none | 10–11 updaters + ~13 Events per entity even with no geometry (10k point-only = ~110K updaters + ~120–130K subscriptions, ~50–100MB retained + 200–500 ms sync TTFF stall). Lazy keyed by defined graphics slots + time-sliced drain. |
| P2 | S10-5 — collection define-scan gating | NEW (S10-C; sibling of C9-17) | perf | S | PR §11 S10-5 | none | WebGPU-only: O(N) per-frame define scan over every billboard/point/label glyph even when settled (10k+10k = 80–150K probes = 0.3–1.0 ms/frame) despite the resident-instance manager's exact dirty signal. Cache currentDefines; rescan only on dirtyCount>0 with per-bit population counts. |
| P2 | S10-6 — pick instance repack + visibility-flip structural rebuild | DEEPER-ON-KNOWN (FAR-107/FAR-409 scope extension) | perf | M | PR §11 S10-6 | none | Pick pass allocates fresh Float32Array(N×44) + full repack per collection per pick frame (10k billboards = 1.76MB per mouse-move under hover picking); any visibility flip is treated structural (full N×176B re-upload at blink rate). Resident pick mirror + packed show flag → 16-byte partial writes. |
| P2 | S10-7 / S10-8 — geometry/path incremental batching | NEW (S10-D/E; FAR-209-adjacent) | perf | L | PR §11 | none | (a) One geometry edit re-combines the WHOLE batch Primitive (full re-tessellation + full upload; streaming 5k polygons = O(N²) 12.5M re-combines) — shard batches + updater→batch back-pointer + sub-range upload; (b) PathVisualizer re-subsamples every path every frame and any sample-COUNT change re-encodes/uploads EVERY polyline in the shared collection (WebGPU regresses harder via BG churn) — ring-buffer trails + pre-allocated capacity. |
| P2 | S10-9 — ModelVisualizer static lane | NEW (S10-G; complements C9-17) | perf | S | PR §11 S10-9 | none | No static lane: every model entity pays isShowing+isAvailable+computeModelMatrix+~25 property reads per frame even when fully constant (1k static = ~1–3 ms). Reuse the isStatic* classification precedent. |
| P2 | S2-1 — collection resolver-closure churn | NEW (C9-27 acceptance rider) | perf | S | PR §3 S2-1 (+§13) | Scope interim fix to not conflict with C9-27 view ring (deletes the mechanism long-term) | Collection renderers rebuild resolver closures, options objects, entry arrays, += identity strings every frame when settled (~16 allocs/collection/frame) + unconditional full camera-UB CPU repack. Cache keyed by `_gen`; numeric-id compare; revision-gate the repack. |
| P2 | FAR-307-POLYLINE-PERSISTENT-MATERIAL-TABLE | NOT STARTED | perf | L | C9Q §9 W5-62 (+AUDIT-P0-5 ledger row) | none | Durable owner of the interim Sol-audit P0-5 fix (Color polylines keyed by material TYPE): persistent exact material/geometry table with safe coalescing + dirty ranges — high-cardinality mixed materials, mutation, pick, TAA velocity, no first-material aliasing. Polyline Image/DiffuseMap/velocity variants build on it. |
| P2 | PARITY-POINT-SPRITE-SHAPE-RESIDUALS | open residuals (shape/size fix shipped B490) | parity | M | DW ~5116 | none | (1) PARITY-POINTCLOUD-COLOR-TINT: WebGPU point clouds ~27–45% brighter/bluer with monotone same-session drift (smells auto-exposure/tonemap adaptation); (2) czm_pixelRatio + u_maxTotalPointSize unplumbed → hiDPI point primitives undersize vs WebGL. |

## 10. `submit-residency` — Queue timeline, submit consolidation, resource lifecycle (4)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | FAR-200-S1-PHYSICAL-QUEUE-TIMELINE | NOT STARTED | infra | M | C9Q §5 W1-12A | none (sanctioned pre-Gate-B exception) | ONE monotonic serial authority per physical queue/device generation (submit, abandon, pool, failure, loss, completion) as a shadow service migrating no production caller. Gateway to the whole residency/retirement family (FAR-200 S2/S3, C9-15, FAR-209/210) + needed by packed-depth pick. |
| P1 | FAR-200 private-submit-timeline consolidation *(merged: PR S6-7/S6-5 three mid-frame submitters + cluster-dispatch hygiene)* | next-campaign seed (C10-09/C10-05 both left it as the separate concern) | infra/perf | M–L | C10Q §6; PR §7 S6-7+S6-5; FF | Land the S6-7 submitter moves before FAR-200's submit-timeline authority | Only three per-frame mid-frame private submitters remain, all feature-gated: Ocean FFT (~35 compute passes private submit), Weather, EntityCluster dispatcher — move onto the main frame encoder (fog/clouds pattern); plus cluster-dispatch hygiene (CPU-zeroed uploads instead of clearBuffer, redundant readback copy). Then consolidate into frame-owned submission ordering (C9-12A ImageryMipPreparation precedent). |
| P1 | Geometry-residency dedupe *(merged: PR S11-3 triple-residency/double-upload; register §14 seed 7 / FAR-204 item-89 extension)* | next-campaign seed; GPU-dedupe scope effectively NEW | perf | L | C10Q §6; PR §12 S11-3; FF | typedArray release policy must preserve documented readers (edge visibility, 2D, picking); subsystem-distinct from C9-15 | ALL model/tile geometry is resident 3× (loader-uploaded stub GPUBuffers the FR never binds + force-retained CPU typedArrays + the FR's second unbudgeted in-render-loop GPU upload): ~500MB tile geometry → ~1GB GPU + ~500MB JS heap, crossing the bus twice. Single canonical GPU copy (FR adopts stub buffers or budgeted content-processing build) + typedArray release after build. |
| P2 | NEW-PICK-ID-OWNERSHIP-MODEL | NOT STARTED | perf | M | C9Q §8 W4-50 | none | Consolidate model/instance/feature pick IDs WITHOUT eager no-pick WebGPU IDs/textures (never-picked frames currently pay allocation); fold-target for the PNTS typedArray retention record (item 89); prerequisite for contiguous pick-ID ranges. |

## 11. `celestial-env` — Celestial/env retained resources (2)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | NEW-WEBGPU-CELESTIAL-RETAINED-RESOURCES | NOT STARTED (W1 cheap-rider candidate, never absorbed — unowned) | perf | S–M | C9Q §3.2 L136 + §9-76; C10Q §4; FF | none | WebGPU Sun recreates its position vertex buffer as time advances + writes a 256-B UBO + allocates a bind group + command per frame; StarField writes 256 B + allocates two commands + arrays per frame. Retain device-generation-safe commands/bind groups; dirty-update Sun position; prove time/HDR/MSAA/resize/loss/multi-context/destroy. |
| P1 | NEW-WEBGPU-STARFIELD-SINGLE-SUBMISSION | NOT STARTED (W1 cheap-rider candidate — unowned) | correctness | M | C9Q §3.2 L137 + §9-77; C10Q §4; FF | none | Static command-flow audit indicates a cubemap frame keeps the early binned catalog-star draw AND prepends a distinct post-cubemap injected draw — contradicting the single-submission contract, potentially doubling star GPU work/brightness. Instrument exact order/count, then one authoritative environment command path. |

## 12. `tiles-model-parity` — 3D Tiles + glTF model parity/perf (21)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | TILE-ARCH-SHADER-STRATEGY | WIP — gates ~30% of Phase-7 items | parity | L | FI §C.2 (+§D.2 Phase-8a) | Adjacent to C10-08 (C10-owned model axes) + C10-08b define-width seed; tiles-side coverage decision (~20 coarse pipelines + prewarm) is the open remainder | Monolithic ModelPBR trade-off silently drops KHR extensions not baked into the uber-shader; the Phase-8a variant-strategy decision+prototype is the highest-leverage open architectural decision in the model path. |
| P1 | C-R1-TILE-BATCH | WIP | parity | M | FI §C.2 | none | Per-feature Cesium3DTileBatchTable renderState (depthMask flip for translucent features, custom blend) not consumed by WebGPU model emission — per-feature styling renders with wrong depth/blend state on b3dm/glTF tiles. |
| P1 | WIRE-MODEL-COLOR-ALPHA-SEMANTICS | open remainder (blend math shipped B484) | parity | M | DW ~5070 | none | WebGL routes model to Pass.TRANSLUCENT when `model.color.alpha<1` and disables color mask at 0; WebGPU leaves the material's own alphaMode pass — OPAQUE-material model with color.alpha=0.5 renders fully opaque. Fold color.alpha into per-primitive pass/pipeline selection + writeMask-0 hide variant. |
| P2 | FEAT-3DT2-02 — property-texture/feature-ID WGSL sampling audit | audit incomplete | parity | M | FI §C.2 | none | 3D Tiles 1.1 metadata-driven styling + metadata picking may sample wrong texels/channels on WebGPU — audit + fix for tiles-1.1 production content. |
| P2 | FEAT-3DT2-05 — Draco/KTX2/meshopt end-to-end audit | audit pending (~300 batches stale — re-verify premise) | parity/tooling | M | FI §C.2; LQ backlog ~L210 | none | No verified decode→upload→render chain on WebGPU for compressed tileset content (the dominant production encoding); known KTX2 gap cluster lives here. |
| P2 | FEAT-3DT2-01 — styling expression → WGSL compiler | deferred/open (survey) | parity/perf | L | FI §D.2; LQ backlog ~L206 | Orthogonal to C10-02 (CPU-side economics); overlaps the Phase-8b styling-compiler component; restricted subset standalone-buildable | Backlog's single biggest 3D Tiles performance lever: compile Cesium3DTileStyle expressions to WGSL (restricted subset first). |
| P2 | Phase-8a Tile↔Hi-Z wiring | WIP — dispatcher exists, wrong consumer granularity | perf | M | FI §C.2 | none | Hi-Z occlusion consumes ViewportExecutor command lists rather than tile bounding volumes — can only cull already-generated commands, not whole tiles pre-traversal; forfeits the main Hi-Z win for dense city tilesets. |
| P2 | KHR_materials_variants / IOR / clearcoat-IOR coupling | unwired on WebGPU | parity | M | FI §C.3 | none | Variant-authored assets show only the default material; IOR-coupled Fresnel wrong vs WebGL. |
| P2 | 5 default textures bound per model draw | WIP | perf | S | FI §C.3 | Adjacent to C10-08 axes; the bind/sample elimination itself not covered by C10 | Five default textures bound (and cost fragment work) on every model draw even when unused by the material. |
| P2 | GLTF-POINTS-MODE-RESIDUALS | open residuals (POINTS fix shipped B491) | parity | M | DW ~5129 | none | (1) non-indexed TRIANGLES with missing attributes hard-fail draw validation (silent no-model); (2) LINES/STRIP modes render as triangle-list; (3) ModelPointCloudStylingStage.wgsl orphaned until quad expansion; (4) POINTS prims rasterize garbage into CSM cascades. |
| P2 | WIRE-MODEL-SILHOUETTE-TRANSLUCENT-DIVERGENCE | open — match-vs-document decision | parity | S | DW ~5078 | Maintainer decision: replicate WebGL's OIT-stencil body-wash artifact for byte-parity, or ratify WebGPU's documented-intent rim-only cutout | With silhouetteColor.alpha<1: WebGL washes the whole model translucent-red (~61k px, upstream OIT artifact); WebGPU draws the rim only (~5k px, matches documented intent). |
| P2 | NEW-MODEL-WGSL-CUSTOM-SHADER (Q31 Slice C varyings) | PARTIALLY SHIPPED; remainder ⏸ PARKED (CONFIRMED double-blocked) | parity | L | DW ~1290 | (1) interstage @location budget 16/16 in the maximal case; (2) ShaderDefine registry EXHAUSTED (see C10-08b) — unblock via bit-24 reuse w/ mutual-exclusion or 64-bit widening | CustomShader WGSL API core-complete; custom vertex→fragment user varyings + extra attributes (positionWC, tangentEC, texCoord_1+) parked on a varying-budget allocation scheme. |
| P2 | NEW-MODEL-SCENE2D-IDL-DUPLICATE | OPEN (P2) — stranded (no DW/C9/C10 row) | parity | M | LQ §4.3 | none | projectTo2D increment 2/2 residuals: per-PRIMITIVE 2D reference frames (model-level frame jitters on large-arc models), SCENE2D IDL-crossing duplicate draw command, per-node normal rotation, skinned/instanced/morphed projectTo2D. |
| P2 | BACKLOG-§4.6 — indirect drawing for 3D Tiles | opt-in flag landed S26; consumer pending | perf | L | FI §C.8 | Needs homogeneous pipeline+bindgroup runs — synergizes with Phase-8b + R-7a | Indirect drawing has its flag but no consumer renderer that batches homogeneous runs — GPU-driven draw submission win unrealized. |
| P2 | R-7a — render-bundle expansion to 3D Tiles opaque models | deferred | perf | M | FI §D.8 | none (siblings R-7b..f follow) | Encoder-time CPU reduction on the biggest static command populations; bundle infra already exists. |
| P2 | TILE-PERF-02 — KTX2 transcode on a worker | deferred | perf | M | FI §D.2 | none | Transcoding stalls the main thread during tile load bursts; pairs with FEAT-3DT2-05 audit. |
| P2 | TILE-WASM-01 — WASM SIMD tile traversal | deferred | perf | L | FI §D.2 | none | Claimed 3–4× traversal speedup — directly attacks the shared globe-quadtree/tileset traversal CPU cost the campaigns measured as dominant. |
| P2 | FORK-41 — PointCloudSort + GPUSortKeys consumers | awaiting consumer wiring (HiZ sub-rows stale — fixed B212-213) | perf | M | FI §D.8 (+§C.8) | none | Compute shaders shipped without consumers: PointCloudSort dispatcher never integrated into the point-cloud collection; GPUSortKeys needs SOA buffers + BG factory + RenderScheduler integration. |
| P2 | FEAT-SURVEY-06 — decoupled-lookback prefix-sum consumers | WGSL landed; consumer wiring pending | perf | M | FI §C.7 | none | Cull compaction + indirect-draw compaction still use the legacy two-pass — single-pass compaction win unclaimed on GPU-driven paths. |
| P3 | Phase-8b TileStoreGPU | deferred/research — genuinely unbuilt (design docs only) | perf | XL | FI §D.2; LQ §6.4/§7 | RFC + 3-day WGSL-styling-grammar spike required first; CPU traversal redesign + WBOIT×indirect composition unproven | The umbrella perf epic (~5–7 wk): MegaBuffer + GPU Resident Drawer + compute-cull fanout + dynamic-offset UBO orchestration + WGSL styling compiler — per-frame CPU cost O(camera-delta) not O(visible-tiles). |
| P3 | BACKLOG-§8 GPUExternalTexture | deferred | perf | M | FI §D.8 | none | Zero-copy video import for video-on-terrain/video materials — the difference between usable and stuttering video-material scenes on WebGPU. |

## 13. `classification-voxel` — Classification + voxel parity (9)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | PARITY-VOXEL-OCTREE-TRAVERSAL | open remainder ("the rest of the XL"; depth-1/L2/L3 SHIPPED) | parity | XL | DW ~3325; FI §C.5 | Level ≥4 needs the general u_octreeInternalNodeTexture GPU node-table walk; non-box refinement box-gated until verified | Beyond the shipped level-3 flat-slot atlas: arbitrary-depth traverseOctreeDownwards GPU walk, generalize the level-2-only LRU megatexture pool (+public maximumTileCount; PART-3 pixelsMatch sub-gate FAILS at clean HEAD — triage), ellipsoid/cylinder render-bounds + non-box multi-level refinement, SAMPLE_COUNT>1 lerp + time-dynamic keyframes. |
| P1 | NEW-CLASSIFIER-2D-CV-MORPH | open remainder (progressively narrowed) | parity | L | DW ~1373 | e2e visual verification blocked on absence of a loadable .vctr classification scene | Vector3DTilePolylines + ClampedPolylines still gate 2D/CV (need CPU-reprojected ENU 2D vertex path ~80 LOC each); MORPHING blend for Vector3DTilePrimitive; GroundPrimitive 2D/CV textured variant + Image material type-4. GroundPolyline dual-attribute + morph pipeline is the template. |
| P1 | NEW-GROUNDPRIM-CLASSIFIER-RECON-PRECISION | UNPARKED (prereq resolved B637; log-reverse consumer live since B251) | correctness | M (maybe S) | DW ~1497; FI §C.4 | Re-verify FIRST via probe-groundprim-textured-classify far-corner ROI — artifact may already be gone | Textured GroundPrimitive classification degraded toward the far corner (windowToEye catastrophic cancellation at storedDepth≈0.9999997) + the DISTINCT B198 whole-polygon UV frequency ~4× too fine (add band-count/FFT assertion). Both now measurable post-B637. |
| P1 | C-R9-VOXEL-CELL-PICK-TAIL | remaining tail (coordinate decode resolved 2026-07-02) | parity | S | FI §C.5 | Traversal construction overlaps PARITY-VOXEL-OCTREE-TRAVERSAL | `scene.pickVoxel` (public API) still throws — WebGPU path never builds `_traversal` (findKeyframeNode TypeError) so VoxelCell can't be constructed. |
| P1 | C-R1-CLASSIFICATION | WIP | parity | M | FI §C.2 | none | ClassificationPrimitive needs the upstream 3-pass renderState set (stencil-depth / color / pick) routed through WebGPU pipeline variants — stencil semantics currently approximated. |
| P2 | NEW-GS-CLASSIFICATION-DEPTH | WIP | parity | M | FI §C.4 | none | Gaussian-splat translucent tiles classify against globe-depth instead of splat-depth — classification polygons land on terrain rather than splat geometry. |
| P2 | C-R8-VECTOR-3DTILE-CLAMPED-POLYLINES | WIP | parity | M | FI §C.4 | none | Per-feature pick slot reserved but never written; distinct depth-source per pass not routed; DEBUG_SHOW_VOLUME unimplemented. |
| P2 | ADR-2026-04-28 (incl. C-R8-TRANSLUCENT-MULTI-FRUSTUM) | in progress / multi-frustum accumulation paused | infra | L | FI §C.4 | none | Depth-sampling classifier migration mid-flight; the Batch-47 scaffolding (accumulation texture + composite) still waiting for its producer half (the Principle-7 origin story). |
| P2 | VOXEL-USER-CUSTOMSHADER-RESIDUALS | open residuals (native-WGSL path shipped B503) | parity | M | DW ~5156 | none | (1) uniforms incl. SAMPLER_2D color-maps fall back to warn+gray (upstream auto viridis ramp broken) — voxel BGL variant with customShader UBO @group(0)@binding(3); (2) only FIRST metadata property exposed; (3) no fsInput.voxel.* block. |

## 14. `shadows-lighting` — Shadows + lighting (5)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | SHADOW-LAYOUT-QUANTIZED | WIP | correctness | S | FI §C.6 | none | Quantized terrain vertex layouts (stride-8 u16, stride-12 f16) missing from the shadow-cast variant table — terrain from quantized-mesh providers (the common production case) cannot cast shadows on WebGPU. |
| P2 | CSM-DESIGN Slices 3-4 *(merged: LQ "CSM Slice 3 altitude-adaptive splits" + "CSM Slice 3 moon dual-light" + "VSM")* | pending / unstarted (design-doc only for the LQ slices) | parity | L | FI §C.6; LQ §4.6 ×3 + CSM_DESIGN.md | Moon dual-light: effects BGL saturated — recommended night-only light-direction switch (Option C) | Remaining CSM: altitude-adaptive splits (λ=0.7 wastes 3/4 cascades at orbit — collapse above ~500 km), moon dual-light cascades, VSM rg32float soft shadows, 3D Tiles per-tile cascade culling, snapshot-mode freezable contract, WebGL parity path. |
| P2 | C-R10-GLOBE-POINT-LIGHT | WIP | parity | M | FI §C.6 | none | Globe terrain does not receive cube/point-light shadows on WebGPU — night-city / indoor-outdoor scenes show lit terrain where WebGL darkens it. |
| P2 | C6-LTC-AREA-LIGHTS follow-ups | OPEN (v1 SHIPPED; 6 independent pieces) | feature | M | DW ~5171 | Textured emitters must be clean-roomed from the paper | Deferred: one-line evalLTCAreaLights in the 21 Primitive*Lit/Phong shaders (chunk+bindings prepended); line lights; clean-roomed textured emitters; froxel clustering past 8 lights; WebGL2 port; Sandcastle night demo. |
| P3 | FEAT-GAP-06 — bent-normal AO (terrain) | RESEARCH/GATED — stranded | feature | M | LQ §11 | Gate behind FEAT-GAP-01 producer hardening | Terrain-only bent-normal AO that must live in GlobeTerrain.wgsl (not post-process). |

## 15. `atmosphere-sky` — Atmosphere/sky/sun-moon (6)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | C9-14B-ATMOSPHERE-LUT-CONSUMPTION | NEW sub-item (proposed C9-14 rider; unowned by C10) | perf | M | PR §4 S3-2 (+§13) | Sun-relative LUT re-bake deferred as NEW-ATMOSPHERE-LUT-SUN-RELATIVE | The 16×4 Nishita double ray-march runs per-FRAGMENT on every globe fragment whenever fog or ground atmosphere is on (both default): ~75M exp/frame near ground, ~430M at limb, ~500× WebGL's per-vertex gate; the inscatter-LUT fast path is hard-disabled (useLut=0 since B247) though AtmosphereLUT.wgsl is already dispatched. Reintroduce the distance gate + revive LUT consumption. |
| P2 | C6-HIGHER-ORDER-SCATTER-LUT (reframed diagnostic) | REFRAMED — diagnostic increment open; stranded | correctness | S | LQ C7 §4 | none | MS LUT already ships (Hillaire f_ms, B429); the real increment is DIAGNOSTIC — verify shadowed/night TERRAIN consumers actually sample the MS-LUT and wire them if not. |
| P2 | NS-SUN-BLEND-MODE-DIVERGENCE | PRE-EXISTING, doc-only, independently schedulable | parity | M | DW ~5211 | Direction decision: (a) WebGPU flare → ALPHA_BLEND matching WebGL (WGSL halo-in-alpha rework + moon recheck) or (b) ratify additive + retune WebGL | Sun/moon flare composites ALPHA_BLEND on WebGL but ADDITIVE on WebGPU (can only brighten — extinguished horizon sun fades to pale glow). Bright-sun case must stay byte-identical; probe both PNGs. |
| P2 | NS-SURFACE-SKYATMOSPHERE-NIGHT-BRIGHT | open follow-up (named, un-expanded) | parity | unknown | DW ~5209 | none | Fork renders a bright/opaque surface skyAtmosphere even at the anti-solar nadir — night sky not dark, washes out the additive catalog stars. Sibling NS-SKYBOX-CUBEMAP-EXTINCTION independently schedulable. |
| P3 | FUT-MULTI-BODY-ATMOSPHERE *(merged: LQ NEW-MULTIBODY-ATMOSPHERE)* | FUTURE (research complete, zero code) | feature | M–L | DW ~5233; LQ §9.3 + C7 §5 | none | Generalize atmosphere to arbitrary bodies (Mars first, airless gate) — LUT chain ~80% per-body parameterized; real work = short hardcoded-Earth constants list (ATMOSPHERE_THICKNESS in TWO parity-critical sites, outerEllipsoidScale, ozone tent, groundAlbedo, Mie SSA). Land as frozen-preset BodyAtmosphereProfile (EARTH byte-identical). |
| P3 | NEW-SUN-MOON-FIDELITY | OPEN (P2) — stranded | feature | M | LQ §9.3 | none | Physical sun disc + limb darkening + atmosphere-coupled glow + geometry lens-glare; moon phase-correct PBR regolith + earthshine. |

## 16. `postprocess-effects` — Post-process + effects parity/visibility (9)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | C9-23-EFFECT-EXECUTION-AUDIT / FAR-500-C0 | NOT STARTED | correctness | M | C9Q §7 W3-36 | none (R0/R1 gateway) | Audit every fullscreen/compute/shadow/cloud/weather/ocean/flow/capture owner for explicit enabled+consumer+view demand; execution counters + conservative fallback; never octree-cull global viewport effects. Scopes the whole Wave-3 visibility family (C9-20 FFT-ocean, C9-21 flow-field, C9-22/22A env-capture — all unexecuted). |
| P2 | WIRE-PP-LIBRARY-BUILTINS-RESIDUALS | open residuals (mechanism + 7 WGSL twins shipped) | parity | M | DW ~5086 | 'selected' masking needs pick-id/selected-texture plumbing (L, shared with model-selection PP) | (1) LensFlare dirt/star textures not ported; (2) CZM_SELECTED_FEATURE masking unsupported for EdgeDetection/Silhouette/BlackAndWhite; (3) DepthView encoding divergence (documented); (4) intercepted stages execute after all user WGSL stages instead of interleaved. |
| P2 | NEW-PLAIN-HDR-SCENE-GAMMA-EPIC residual | OPEN — stranded | parity | M | LQ §4.7 | none | Under plain `scene.highDynamicRange`: globe-imagery/atmosphere mismatch (HDR tonemap-operator gap, probe-plain-hdr-gamma); MaterialAppearance *Lit.wgsl + PBR primitive shaders lack the HDR per-instance sRGB decode (only basic/phong got it). |
| P2 | C6-SSGI-DIFFUSE follow-ups | SHIPPED with 2 follow-ups DEFERRED | feature | M | DW ~13 | Escalate DEFERRED-1 only on probe evidence (probe-ssgi @giIntensity=8) | (1) grazing-angle horizontal banding at low sliceCount — raise sliceCount, TAA-integrate slice rotation, 2-iteration a-trous on BilateralBlur1D; (2) GLSL ES 3.0 WebGL twin (C6-SSGI-DIFFUSE-GLSL, not parity-required). |
| P2 | NEW-PP-F16-DEVICE-VERIFY | OPEN (P2) — stranded | tooling | S | LQ §4.7/§6.3 | Requires a physical shader-f16 (RTX-class) GPU; sandbox can't run it | The B478 opt-in f16 PP variants have never had an on-device pixel-verify on a capable adapter. |
| P2 | WGF-1-EXPAND — hardware clip-distances beyond globe | partial (globe live) | perf | M | FI §C.7 | none | Primitive shaders have the struct but no VS output; Models lack the hardware path — clipping planes still cost fragment-discard. |
| P2 | WGF-1-INTERSECTION — intersection-mode clipping | WIP (union-only) | parity | M | FI §C.7 | none | `unionClippingRegions=false` semantics unimplemented for the hardware clip-distance path — intersected collections get union behavior on WebGPU. |
| P3 | WGF-4 (+WGF-4-EXPAND) — standard-layout UBOs + RTE packer assertions | deferred / partial | perf | M | FI §D.8 + §C.7 | none | ~20% UBO size reduction across renderers; RTE camera-packer assertions still pending in 5 of 8 packers (Cloud/Ellipsoid/Splat/PointCloud/Voxel) — bandwidth win + precision guardrails. |
| P3 | C6-FSR2-UPSCALE | DEFERRED (premise REAL; multi-batch epic) | perf | XL | DW ~5223 (C9 W7-4) | 6 dense GLSL→WGSL compute ports; WGSL lacks texture atomics (storage-buffer atomic-scatter helper needed); 17-subsystem res-split incl. pick-coordinate-scaling breakage risk | AMD FSR2 v2.2.2 (MIT) temporal upscaler: phased P1 R/D split (byte-identical off) → P2 FSR2Prep.wgsl → P3 pass ports + Quality 1.5x + SSIM probe → P4 reactive/presets/f16. `scene.fsr2Enabled` default false, WebGPU-only. |

## 17. `clouds-weather` — Cloud + weather content epics (16)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P2 | C7-CLOUD-LIGHTNING (reland = C9 W7-1) | REVERTED pre-land; reland queued, no reland commit in git | feature | M | DW ~5237 (owner C9 W7-1) | (1) replace median anti-tiling probe gate with 95th/99th-percentile; (2) soften/stagger per-cell flash-window temporal edges (phase jitter + smooth decay) | In-cloud lightning (emissive scatter in the march + flash driver on the precip channel) reverted for a hard lon/lat block lattice in flash-decay frames the median gate masked. Contract: default-off byte-identical, PNGs read. |
| P2 | C6-CLOUD-STBN-TAAU | DEFERRED (STBN/TAAU half; LOD half SHIPPED B634) | feature | M | DW ~17 (C9 W7-5) | Needs a genuine 128×128×64 rgba8unorm STBN texture generated OFFLINE with EA SEED FastNoise (BSD-3) — NVIDIA assets license-poisoned | Swap Bayer→vector2 STBN sub-pixel jitter + per-pixel march-start dither, then the true 1/16 full-res-history TAAU rewrite. Land behind QF_JITTER (bit 3, reserved) with Bayer fallback + EA BSD-3 notice in LICENSE.md. Plan in scratchpad/RESEARCH_R-STBN. |
| P2 | CLOUD-U4-REMOVE-GLOBE-FLAG | BLOCKED on an unmade architectural decision (premise VALID) | infra | L | DW ~15 | Product/arch decision (Principle 9): (A) Scene/Globe owns a managed default VOLUMETRIC CloudCollection re-pointing 4 producers, or (B) remove/re-home AtmosphericConditions.clouds + weather ingest sink + godRayCloudAware gate | Hard REMOVE of globe.showProceduralClouds + ~49 globe.cloud* fields would orphan FOUR globe-attached config producers with no migration target. Renderer gates mechanical once (A)/(B) chosen; + migrate 4 Sandcastle demos + ~40 probes. |
| P2 | Q36-WEATHER-PHASE-4-GRIB2 | BLOCKED (hard-blocked on proxy) — stranded | feature | L | LQ §8.2 | Same-origin NODD-S3 CORS proxy unbuilt; GRIB2 Worker/WASM parse + Lambert-Conformal→equirect reprojection | Direct GRIB2/NetCDF high-fidelity weather ingest (HRRR/GFS/NBM) behind WASM — the roadmap's critical-path weather item; building the proxy first is the unblock. |
| P2 | Live EDR network confirm | BLOCKED (P1 in source, env-blocked) — stranded | tooling | S | LQ §8.2 | Dev sandbox has no outbound network — needs a networked browser session | EdrWeatherSource wired but the LIVE NOAA EDR call, CORS behavior, and the guessed collection id ('automated_gfs') never confirmed against the real service (mock fixtures only, B424). |
| P2 | WeatherSystem / scene.weather facade (Phase 3) | PARTIALLY WIRED — stranded | feature | M | LQ §8.2 | none | Backend-neutral data core + globe.weatherProvider work, but no top-level scene.weather facade consolidating clouds/weather/atmospheric-conditions; explicit WebGL degradation ladder unbuilt. |
| P2 | PRECIP-DATA ground snow-albedo shader consumer | DEFERRED (P2) — stranded | feature | S | LQ §9.2 | none | B444 ships updateSnowAccumulation scalar but no globe shader consumes it — the fill-in that makes snowfall visibly whiten terrain. |
| P3 | C7-CLOUD-IMPOSTOR-LOD | FUTURE (research complete, no code) | perf | L | DW ~5235 (C9 W7-3) | Depends on cloud-unification (CLOUD-U4 renderMode); NVIDIA STBN license-blocked (self-generate per R-STBN) | Octahedral impostor LOD for VOLUMETRIC clouds: Phase 1 freeze-and-reproject single impostor (~60× march-cost cut at orbit, ~90% existing infra); Phase 2 8×8 octahedron atlas. Default off byte-identical. |
| P3 | CLOUD-LOD-R8-PRECIPITATION-COUPLING | DEFERRED (hold for follow-on cloud-content arc) — stranded | feature | L | LQ C7 §5 + CLOUD_LOD_RESEARCH | Sequenced after R5 (landed B651) + R7 lightning reland | Rain shafts + ground wetness coupled to the weather-map precip channel. |
| P3 | CLOUD-LOD-R9-PLANET-SCALE-CLOUD-TILING | DEFERRED — stranded | feature | XL | LQ C7 §5 + CLOUD_LOD_RESEARCH | C7-CLOUD-IMPOSTOR-LOD (impostor renders from this substrate) | Hierarchical quadtree planet-scale cloud tiling — the largest remaining cloud gap. |
| P3 | CLOUD-EXOTIC-E3-SPECIAL remainder | PARTIAL (B612 landed noctilucent+nacreous half) — stranded | feature | L | LQ §8.3 + C5 #23 | Needs new deck/source infrastructure (line sources, event-driven) | Contrails (line sources) + pyrocumulus (event/data-driven) halves never built, no successor row. |
| P3 | Cloud perf — Tier-2 3D bake (view-local cascaded clipmap) | DEFERRED (own post-core campaign) — stranded | perf | XL | LQ §8.2 Phase 6 | none | Production path for volumetric clouds: resident global 2D weather map (shipped) feeding a camera-anchored cascaded-clipmap 3D density bake per frame (modeled on VolumetricFog); raymarcher today is Tier-1 only. |
| P3 | Temporal interpolation + advection (Phase 5) | OPEN (P2) — stranded | feature | M | LQ §8.2 | none | A/B weather-DATA keyframe lerp + per-cell U/V wind advection between sparse frames (distinct from shipped render-side reprojection B433). |
| P3 | Historical-replay headline demo (Phase 4) | GATED — stranded | feature | M | LQ §8.2 | Q36-WEATHER-PHASE-4-GRIB2 | Pre-baked named-storm ERA5/GFS manifest tied to scene.clock — the weather epic's north-star deliverable. |
| P3 | profileExtinction (slot 103) per-position optical extinction | OPEN (Principle-9 follow-up) — stranded | feature | M | LQ §8.2 | none | B452 activated profileExtinction but G only biases shape/density; the full per-position optical extinction in the cloud march is the declared fill-in. |
| P3 | NEW-CLOUD-SHADOW-ENVMAP | DEFERRED (P2) — stranded | feature | S | LQ §9.2 | none | Env-map ground cloud-shadow term deferred from 4.1 CLOUD-SHADOWS — reflections/env-cube still ignore cloud shadowing. |

## 18. `water` — Water rendering epic (2)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P2 | C6-PLANAR-REFLECT-REFRACT | UNBLOCKED-BUT-DEFERRED (gate C6-FFT-OCEAN shipped B654; no code) | feature | L | DW ~5225 (C9 W7-2) | Structural: ocean FR emits a deferred command, so the reflection needs a reflected-camera re-render hooked into the 4505-line WebGPUSceneRenderer orchestration (mirror runSceneCapture snapshot/repoint/restore) | Planar reflection for the FFT ocean (three.js Reflector, MIT): mirror camera across ocean tangent plane + Lengyel oblique clip, render globe/sky into one reflection target, projectively sample in OceanSurface.wgsl behind an add-only ShaderDefine (analytic gradient as //>>else). Refractor/Water2 flow-map refraction is a second increment. |
| P3 | WATER-PHASES-1-9 (Gerstner/bathymetry/foam/rivers/underwater/WaterRegion) | UNBUILT (design-locked; Phase 0/0.3 + Phase-1 seed only) — stranded | feature | XL | LQ §10.1 (WATER_RENDERING_DESIGN v2) | Phase-3 depth sampling now UNBLOCKED (log-depth B251); rest no hard blockers | Phases 1–9 beyond the GlobeWater facade + B636 lake-mask seed + separately-shipped FFT ocean v1: Gerstner + type LUT, bathymetry/Beer-Lambert/refraction, foam+caustics, rivers, underwater/god-rays, WaterRegion, quantized-mesh 0x05. ~8.5 sessions est. |

## 19. `gated-reversed-z` — Gated tail: reversed-Z + MSAA reserve (3)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | C10-13-REVERSED-Z-EARLYZ-SPIKE **⚠C10** | NOT STARTED (measurement-only, openable) | perf/tooling | S | C10Q §6+§3.2 | Gated-tail activation: C10-30 verdict + fresh maintainer sign-off; nothing lands but the report | Cheap FAR-707 evidence gate: compile one probe scene with reversed-Z infinite-far + depth32float + greater-equal and measure fragment-invocation delta vs default log-depth on the 71 producers. Its GO/NO-GO MUST be recorded in NEW-WEBGPU-PICK-FLEET-LOG-DEPTH + the FAR-707 brief + DEFERRED_WORK before the C10-11 conversion is treated as permanent (the two streams pull the same 71-file surface opposite ways). |
| P2 | C10-GT-REVERSED-Z-SLICE-B **⚠C10** | DEFERRED — do not schedule | perf | XL | C10Q §6; PR §8 S7-7 + §15 (§14 seed 1) | C10-13 spike GO (≥20–30% fragment-work reduction); written depth32float-stencil8 fallback story for every adapter tier; pick-fleet reconciliation decision recorded; C10-30 verdict + sign-off | All-or-nothing reversed-Z behind a single `_reversedZEnabled` switch (OFF = byte-identical): retire 71 LOG_DEPTH producer surfaces, 140 depthCompare flips / 47 files, 42 `_logDepthEncodeNearFar` JS sites, ~14 consumer families, delete the RGBA8 depth-pack ecosystem (unowned prize: 2–3 fullscreen packs/frame), TAA prevVP carries the flip, 2D/CV/ortho carved out. Precision resolved (~2 cm @350 km). |
| P3 | C10-03R-MSAA-DEFAULT-FLIP-RESERVE **⚠C10** | CONDITIONAL NOT TRIGGERED | perf | S | C10Q §2 ruling (c) + §6 + §3.2 | C10-30 MISS with bandwidth-attributed evidence AND fresh maintainer sign-off recorded in the ledger | Reserve lever only, NOT ratified: backend-conditional WebGPU default msaaSamples 4→1. MSAA-4 default is visual policy (Rule 1) — any slice flipping it without recorded sign-off is reverted on sight. |

## 20. `test-infra` — Test infrastructure, suites, certification (16)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | NEW-WORKSPACE-SPEC-BUNDLE-FRESHNESS (item 4A) | NOT STARTED (workaround copied into every brief) | tooling | S | C9Q §3.2 L128 + §4-4A; C10Q §4; FF | none | `gulp test --workspace engine` rebuilds ROOT specs while Karma serves `packages/engine/Build/Specs` — new package specs can silently stay stale. Make the workspace task rebuild/await the exact served bundle with a sentinel. Recurring trap worth closing permanently. |
| P1 | Karma headless-Edge launcher environmental flakiness (no ID) | standing environmental issue (unledgered) | tooling | unknown | FF (C10-01/C9-17/C9-16/item-64 rows) | none | Real-scene suites kill headless Edge within seconds; focused runs carry trailing exit-1/"Chrome failed" artifacts; B693 session launcher outright broken (C10-01 spec verification skipped). Stabilization multiplies the value of items 64/70/4A. |
| P1 | NEW-FULL-SUITE-OFFLINE-DEPENDENCY-ISOLATION | NOT STARTED | tooling | M | C9Q §4 W0-8 | none | Isolate Ion/world-terrain/network spec cases into deterministic local fixtures or an explicit online lane — direct unblocking dependency for the open broad-suite items (64, 72). |
| P1 | C9-04-PHYSICAL-ADAPTER-CONTRACT-MATRIX | NOT STARTED | tooling | L | C9Q §4 W0-7 | none | Real-adapter matrix: WGSL validation, attachment/load-store combos, HDR/MSAA, resize, multi-context, physical device loss, GC/lifetime plateaus, strict error scopes. Owns the physical-loss half C9-02A deferred + the sandbox real-GPU instability cluster. Was required by Gate G. |
| P1 | NEW-SCENE-BROAD-SUITE-FAILURE-CLOSURE (item 64 / C8-30) | PARTIAL/PAUSED (3 of 5 product clusters fixed B674) | correctness | L | C9Q §3.2 L153 + §9-64; FF | Headless-Edge sandbox instability (owners: C9-04, offline isolation, 4A); env-map irradiance oracle definition | Remaining owned clusters: WebGL1 async-pick routing (L), GLSL100 additional-light generation (L), env-map irradiance oracle, renderer-neutral fixture contracts for real-scene suites, afterAll teardown. |
| P1 | C8-SHARED-UPSTREAM-CONTRACT-GATE (item 72) | PARTIAL/PAUSED — gate stays open (natural C11 exit gate) | infra/tooling | L | C9Q §3.2 L156 + §9-72; FF | Items 66/67/69/70 + a stable full-suite environment; Widgets + complete-engine lanes untouched | Broad upstream-suite green gate: Renderer triage complete (zero failures attribute to the GraphicsCapabilities migration), DataSources classified with owners — cannot close until the four owner items land and full-suite runs are achievable. |
| P1 | NEW-SHADER-GENERATOR-UPSTREAM-CONTRACT-PARITY (item 66) | NOT STARTED | correctness | S | C9Q §9-66; FF | none | 10 standing spec failures (ShaderBuilder 5 / ShaderFunction 3 / ShaderStruct 2 — missing addLines validation + trailing empty line, pre-existing since B243). Cheap, well-scoped; directly unblocks item 72. |
| P1 | C9-03-CERTIFYING-VISUAL-BASELINE-PROMOTION | NOT STARTED (partial repair rode C9-30: globe-default only) | tooling | M | C9Q §4 W0-6 | Spheres drift must be repaired before that scene's baseline promotes | Historical VR rows remain NON_CERTIFYING campaign-wide — every scene except globe-default needs manual inspection + manifest population (hash, renderer, camera, flags, adapter, oracle, tolerance) so visual gates certify instead of merely diff. |
| P1 | NEW-GPU-TIMESTAMP-UNIQUE-SAMPLE-ACCOUNTING | PARTIAL/PAUSED (implementation exists; audit-corrected under-claim) | tooling | S | C9Q §3.2 + §4 W0-5 | none | Open acceptance: readback-tail drain + covered/uncovered span reporting certified on the moving route — prerequisite-grade tooling for ANY C11 GPU-timing performance claim. |
| P1 | C9-02-VISIBILITY-EXECUTION-OWNERSHIP-MANIFEST | PARTIAL/PAUSED (terrain cohort fully certified) | correctness | L | C9Q §3.2 + §4 W0-2 | none | Remaining: strict runtime owner/selection/consumer/execution assertions for all 14 non-terrain cohorts (3D Tiles, voxels, PVS/octree, fullscreen/compute, shadow/capture, cloud/weather/ocean/flow/environment) — 6 partial + 6 gap cohorts recorded. |
| P2 | NEW-RESOURCE-URL-SEMANTIC-PARITY (item 67) | NOT STARTED | correctness | L | C9Q §9-67; FF | none ("L, not bounded" per triage) | WHATWG `new URL` lowercases the host in Resource.js reconstruction (2× CzmlDataSource failures) — preserve authority/credentials/protocol-relative/file/opaque/data/blob/path-case/query/fragment semantics. |
| P2 | NEW-ENTITY-BULK-CLUSTER-TRANSITION-PARITY (item 69) | NOT STARTED | correctness | M | C9Q §9-69; FF | none | EntityCluster spec fails via BulkBillboardVisualizer "primitives is required" through the PUBLIC defaultVisualizersCallback contract — reclassify bulk point/billboard/label across cluster-flag transitions with no duplicates/stale listeners. |
| P2 | NEW-KMZ-ARCHIVE-URI-RESOLUTION-PARITY (item 70) | NOT STARTED | correctness | L | C9Q §9-70; FF | KmlDataSource suite can't hold a headless session in this sandbox — partly dependent on the Karma-environment item | KMZ embedded/nested archive asset+link resolution with normalization/encoding/case/traversal security so archive entries never fall through to HTTP. |
| P2 | C9-01-REGRESSION-ATTRIBUTION remainder (Gate-A closure) | PARTIAL/PAUSED — Gate A formally IN PROGRESS on this sole remainder | tooling | S | C9Q §3.2 + §4 W0-1 | Needs a recorded Gate-A amendment or a recovered binary of the week-old dirty bundle (B8015811 never retained) | Exact-current attribution complete; the historical replay half of acceptance is characterization-only. Close by maintainer gate amendment or bundle recovery. |
| P2 | S8-7 — settle-window attribution rule + first-complete-frame metric | DEEPER-ON-KNOWN (harness metric unowned) | tooling | S | PR §9 S8-7 + §16.6 | none | WebGPU's +1.3–1.7 s tile-stable is GPU-submit-traffic bound (zero main-thread long tasks) — closure/churn fixes must not book stable-time credit; add a first-complete-frame metric (tiles rendered == selected) to run-performance-campaign.mjs (frameNumber>0 under-reports perceived TTFF). |
| P2 | probe-hdr-pp-math gate F baseline refresh | OPEN — stranded | tooling | S | LQ §4.7/§5.2 | Refresh only once in-flight globe pixel changes settle (C9/C10 landed many) | Stored pre-B506 SDR baseline still fails gate F on intentional B506 pixel changes — a known-stale baseline masking real regressions. |

## 21. `build-boot` — Build variants, TS-debt, define-width, boot/TTFF (13)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE | open (the sanctioned last isWebGPU-branch exception) | infra | M | DW ~187 | none | Give Material per-backend virtual methods (`material.getShaderSource(context)` / `getUniformMap(context)`) so WGSL/GLSL selection lives inside the material abstraction (ViewportQuad.js:144 branch retired). ~1 session incl. the ~3 built-in material pairs; the last blocker for NEW-CAPABILITY-GETTER-CODIFY closure. |
| P1 | C10-08b — ShaderDefine define-width expansion **⚠C10 (follows C10-08)** | next-campaign seed (C10-08 follow-on) | infra | M | C10Q §6 + §5 C10-08 row; PR §4 S3-4/S3-5; FF | C10-08 proves the mechanism + banks the ONE free slot first; add-only/never-renumber registry rules; bit-31 sign hazard; material-mask bits ≤28 | Widen the ShaderDefine register past Uint32 (bits 0–30 occupied) incl. the module-cache key math `((defines>>>0)*0x100)+sourceId` — unblocks the remaining 6–7 model uber-shader specialization axes (KHR bits, HAS_SKINNING/MORPH/INSTANCING, IBL mode, MODEL_HAS_VELOCITY) and the parked Q31 varyings unblock. |
| P2 | S8-5 / S3-7 — WGSL module granularity + globe imagery layout tranches | NEW (§14 seed 6) | perf | L | PR §9 S8-5 + §4 S3-7 | Compounds with C10-06/C10-07 — sequence after | Every define-variant of the 239KB GlobeTerrain / 215KB ModelPBRComplete re-Tints end-to-end (~30–80 ms; compile scales with total surface, structurally eroding TTFF) — per-pass entry-point split, dead-function elimination, compilationHints; plus globe imagery layout shaped for the 16-layer worst case on all devices — scene-keyed 1/4/16 tranches or texture_2d_array. Feeds C9-11/12A descriptor narrowing. |
| P2 | NEW-WGSL-STRING-COMMENT-STRIP | PLANNED (P2) — stranded; never implemented | perf | S | LQ §6.5(c) incr. 1 | Stripper MUST preserve //>> directive families | Strip comments/blank lines from the 305 bundled WGSL string modules in minified/variant builds — measured ~330 KB (15.2% of raw WGSL) off EVERY variant at near-zero risk. |
| P2 | NEW-EMPTYMODULE-STUB-HARDENING | PLANNED (P3) — stranded | infra | S | LQ §6.5(c) incr. 2 | none | Harden `scripts/stubs/emptyModule.js`: whitelist Symbol.hasInstance in the Proxy get-trap (today `instanceof SharedContext` would THROW at Scene construction on pure-WebGPU) + named-export-aware stub mechanism. Prerequisite for leaf-strip. |
| P2 | S8-4 — feature-renderer registration lazify **⚠C10-06 rider** | DEEPER-ON-KNOWN (optional LAST/separate rider on C10-06) | perf | S | PR §9 S8-4 (§16 fix 4); C10Q §5 C10-06 row | Verify C10-06 outcome first — if it lands without the rider, this is the standalone remainder | 41 eager vs 11 lazy FR registrations put ~91% of the 6.6MB renderer source on the boot chunk regardless of scene content; convert ~15 cold registrations to registerFeatureRendererLoader (biggest single win = lazifying Model); also shrinks the webgpu-only variant. |
| P2 | NEW-TS-CONVERT-JS-RENDERERS | ⏳ IN PROGRESS (slices shipped; bulk renderers remain) | infra | XL | DW ~81 | none | Convert the substantial JS WebGPU renderers to TS with ZERO behavior change (no `any`): WebGPUModelRenderer (3802 LOC), GroundPolyline/GroundPrimitive, WebGPUPrimitiveCommands, collection renderers, Vector3DTile trio, Environment/ShadowMap/SkyAtmosphere, WebGPUEffectsBindGroup — one substantial renderer per batch. |
| P2 | Q35-WEBGPUCONTEXT-DECOMP-REMAINDER | PARTIAL (culler-pool cluster landed B609) — stranded | infra | M | LQ C5 #20 + §4.8 | none | WebGPUContext.ts / WebGPUSceneRenderer.ts decomposition remainder: the SceneRenderer pass-family free-function extraction is the last named cluster; both files far over the 1000-LOC guideline. |
| P2 | BACKLOG-§Recent — WebGPUComputePipelineCache | WIP — cache doesn't exist | perf | S | FI §C.8 | none | WebGPUModelRenderer + WebGPUAutoExposure don't route through a central compute-pipeline cache because it doesn't exist — redundant compiles on hot init paths. |
| P3 | NEW-WEBGPUONLY-RENDERER-LEAF-STRIP | PLANNED (P3) — stranded | infra | S | LQ §6.5(c) incr. 3 | NEW-EMPTYMODULE-STUB-HARDENING | Add the 11 measured WebGL-only leaves to bundleVariantPlugin for webgpu-only builds (−46 KB) + extend variant-smoke-test to boot-check webgpu-only. |
| P3 | NEW-C9-01-COUNTER-PRAGMA-STRIP | deferred release-build refinement | tooling | S | DW ~5353 | none | The C9-01 logical-counter blocks (23 sites across 4 files) are runtime-gated not pragma-stripped — wrap in //>>includeStart('debug') keeping declarations outside pragmas + permanent sentinels unwrapped; verify with gulp buildRelease. |
| P3 | C6-SUBGROUP-COMPUTE-FINISH | DOWNGRADED (narrow leftover) — stranded | perf | S | LQ C7 §4 | none | FrustumCull + PointCloudLOD subgroup variants wired; leftover = subgroup bucket-scan variant for PointCloudSort/DecoupledLookbackScan (+ WebGPUSubgroupUtils.ts may be unused). |
| P3 | C10-00-ENGINE-HANDOFF-AND-SCRIPT-GEN **⚠C10** | DEFERRED — orchestrator mode active | infra | S | C10Q §3.2 + §5 | Only needed if C10/C11 reverts from orchestrator to autonomous engine mode | Fork campaign-9-resume.js → campaign engine (fix stale 24-bit charter sentence to 40-bit; byte-identical schemas/safeAgent/loop; splice TASKS + hard-rules block). |

## 22. `arch-seeds` — Architecture-scale next-campaign seeds (4)

| Pri | Item | Status | Class | Effort | Sources | Blockers | Summary |
|---|---|---|---|---|---|---|---|
| P1 | S1-6 — frame-delta retained-commandList tier *(merged: C10Q §6 "S1 frame-delta retained-commandList tier" + FF seed; register §14 seed 2)* | NEW / next-campaign seed | perf | XL | PR §2 S1-6; C10Q §6 + C10-10 row (S1-6); FF | Gated on C9-11/C9-17 retained packets existing to be reused (incl. Slice D); C10-10's true caster sublist waits on this | No reuse tier between skip-whole-frame and recompute-everything: ~4–5 ms avg / 8–10 ms p95 command-count-independent shared-frontend floor on BOTH backends — a 2 m camera move re-runs tile selection, environment, height plumbing, PVS, preload, ephemeris like a content mutation. Build frame-delta classification (cameraDelta tier × contentRevision tier). Register contradiction #3: without this, backend wins cannot deliver ≥2× at p95 on CPU-bound hosts. |
| P2 | Worker-renderer productization *(merged: PR S5-3; register §14 seed 5)* | next-campaign seed (strategic HIGH) | infra | XL (S5-3 says L for the lane) | C10Q §6; PR §6 S5-3; FF | Benchmark-lane quantification first; API drift grows until exercised in CI | A complete render-in-worker OffscreenCanvas stack exists (RendererWorker.js 774 LOC + WorkerSceneHost.js 835 LOC + protocol) with only a test page as consumer — the ONLY shipped mechanism that structurally raises the main-thread CPU ceiling. Add a worker lane to the moving-altitude benchmark, then productize opt-in `Viewer({useWorkerRenderer:true})`; either way exercise in CI/probes. |
| P2 | S5-2 — WASM acceleration layer consume-or-retire | NEW (§14 seed 8) | perf | M | PR §6 S5-2 | Principle 7 — per-bridge disposition decision, no silent deletion | 5/7 WASM bridges dead: WasmCullBridge (built for the exact shared hot path), WasmSortBridge, WasmHeightmapBridge, WasmQuantizedMeshBridge, WasmMatrixBridge ship full Rust impls with ZERO call sites (~1,535 LOC in all bundle variants). Wire WasmCullBridge behind threshold=500, move terrain bridges into workers, consume-or-retire the rest. |
| P3 | NEW-VEGETATION-SYSTEM | FUTURE — design complete, zero code (V1 cutline pinned B653) | feature | XL | DW ~4779 | None hard-blocking V1; biome data prep is CC-BY-4.0 asset work | Planetary vegetation epic in six shippable slices — V1 scatter foundation (VegetationScatter.wgsl compute → RTE instance buffer), V2 mesh-LOD chain, V3 octahedral impostors, V4 wind/leaf/A2C shaders, V5 grass+rocks, V6 optimization. Canonical design VEGETATION_SYSTEM_DESIGN.md. |

---

## UNKNOWNS — C10 work still in flight; fallout TBD at C11 intake

These are **not** C11 candidates yet (or only conditionally). Re-sweep the C10 ledger (`QUEUE_2026-07-16_CAMPAIGN10.md` §3.2) at C11 kickoff before finalizing any brief that touches their surfaces.

| C10 task | State at sweep | C11 impact |
|---|---|---|
| **C10-02** (tiles-style translucent-twin command economics) | Landing now — on-disk ledger row still NOT STARTED at sweep time (written by concurrent workers) | Residuals unrecorded. Sequencing input for the batch-texture force-create slice (`model-frontend`) and FEAT-3DT2-01. **Re-sweep after it lands.** |
| **C10-06** (boot/TTFF: dead-await kill, adapter/chunk concurrency, prewarm) | NOT STARTED | Determines whether the S8-4 lazify rider is absorbed or becomes the standalone `build-boot` remainder. |
| **C10-07** (async model pipelines) | NOT STARTED | Interacts with S8-5/S3-7 module granularity (sequence after). |
| **C10-08** (model runtime-flag specialization, one free ShaderDefine slot) | NOT STARTED | C10-08b (define-width expansion) is gated behind it; also touches TILE-ARCH-SHADER-STRATEGY adjacency. |
| **C10-11** (pick-fleet log-depth conversion) | C10-owned, not landed | Owns NEW-WEBGPU-PICK-FLEET-LOG-DEPTH. Its outcome + the C10-13 GO/NO-GO reconciliation must be recorded before C11 treats the conversion permanent. All 5 W4 pick riders ride alongside — C11 picks up only what W4 leaves. |
| **C10-12** (depth-plane log-depth contract pick half + C9-02B closure + audit P0-1 gate flip) | C10-owned, not landed | C11 pick-cluster intake depends on what closes here (C9-02B, P0-1 flip). |
| **C10-13** (reversed-Z early-Z spike) | NOT STARTED, gated | Its GO/NO-GO redirects the entire `gated-reversed-z` cluster AND the pick fleet (same 71-file producer surface pulled opposite directions). |
| **C10-30** (measured perf checkpoint + verdict) | NOT STARTED | Determines: C9-17 Slice D opening condition; C10-03R trigger; gated-tail activation; feature-loss check against the standing reds (pickPosition, spheres drift must stay pre-attributed). |
| C10-05/09/10 + C10-01/03 | COMPLETE (landed as Batches 693–698 per FF) | Their recorded follow-ups are already itemized above (FAR-200 seed, C10-10 caster sublist, S4 remainder, MSAA-flip attribution). |

**Standing policy constraints carried into C11** (C9 §3.3 record): WebGPU MRT OIT default-off is RATIFIED FAR-003 containment (re-enable owner = FAR-003/T7, inactive until post-Gate-F stop/go); `renderer:'webgpu'` graceful-fallback-with-warn (strict via `strictRenderer`); never remove/hide/default-disable/degrade a feature for a metric; no absolute planetary ECEF f32 before camera subtraction (incl. previous frames + GPU culling data); Node/Playwright+Edge only; moving multi-altitude route mandatory (idle soak invalid); one concern per slice; promotion bar ≥10% whole-route / ≥15% near-ground WebGPU CPU-p95 or >3× measured noise.

## Residual coverage gaps (from the source sweeps' own notes)

- **DW sweep truncation:** ~40+ lower-impact entries dropped at its cap (tooling stubs incl. WEBGPU-SPEC-BACKFILL, upstream-sync prestaging, low-value perf refinements, large-dynamic-objects tail, smaller parity leftovers, unverified morph-audit gaps). Re-mine `DEFERRED_WORK.md` cited sections if C11 wants depth in those classes.
- **C9Q sweep truncation:** Wave-1 proof/cert family, Wave-3 visibility tail (C9-20/21/22/22A/27/28 — gateway C9-23 IS listed), Wave-4 FAR-200 S2/S3 + FAR-203/205/206/209/210 chain (entry point FAR-200-S1 IS listed), Wave-5 debt tail, Wave-6 gated T1–T8. All NOT STARTED inventory reachable from the listed gateway items.
- **LQ sweep:** FEAT-SURVEY-01..48 + FEAT-3DT2-04 + TILE-DEBT/PERF/ARCH lists only spot-checked (~500 batches old); a dedicated re-verify sweep is the one named residual gap. C8→C9 transfer was verified COMPLETE (zero stranded C8 IDs).
- **Known-resolved items were excluded** by every sweep (e.g. imagery cache-key collision B686, billboard atlas vflip B563, sun glowFactor B564, morph chain B555-557, splat multi-frustum B647). Trust the sweeps' exclusion lists over stale doc text.
