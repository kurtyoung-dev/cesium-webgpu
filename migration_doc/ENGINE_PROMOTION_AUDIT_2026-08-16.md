# Engine-Promotion Audit — logic in Tools that belongs in the engine (2026-08-16)

**Directive (maintainer, 2026-08-15):** while executing the tooling-library rulings M1-M5, double-check every script for code and logic that should actually live in the main Scene or Renderer code.
**Method:** 13-agent read-only workflow (wf_baba01c8-ce3): 10 hunters (6 over the 74+ shared gate/lib modules, deep-read; 4 over the heaviest in-page probe blocks) classifying four smells - A reimplemented engine logic, B runtime monkey-patching of live scene objects, C private-field polling, D product semantics living only in gate predicates - then two adversarial verifiers that killed candidates when the engine already exposes the API, the work is already queued, or test-side INDEPENDENCE is the point (Batch-767 doctrine: a discriminator must not be built from the primitive it discriminates). Two hunter slices (lib-2, probe-2) failed on structured-output retries and are NOT covered - a follow-up sweep of those slices is owed. 56 candidate verdicts synthesized.
**Status:** PROPOSALS. Nothing was changed in the engine or Tools by this audit. The PROMOTE rows below are to be filed as DEFERRED_WORK entries (row text drafted in §5) and prioritized by the maintainer; the two engine BUGS (#6 useDepthPlane on WebGPU ignores debugSkipDepthPlane; #7 SSGI uniforms never re-uploaded after config) are real defects, not tooling debt, and should be fixed regardless.

---
# Engine-Promotion Audit — Synthesis (2026-08-16, read-only)

Inputs: two verifier passes over the Tools/visual-regression library and probe fleet (56 candidate verdicts, several overlapping). Where the two passes disagreed on a candidate I recorded the more conservative reading and note the disagreement. Nothing below was executed against the tree; line refs are the verifiers'.

## 1. PROMOTE — ranked by drift-risk × payoff

Drift-risk = how many probes carry a private read/copy that a rename or layout change silently breaks. Payoff = whether the engine (not just the fleet) gains a real capability or loses a real defect.

| # | Tool file(s) | Logic being re-implemented / reached-into | Proposed engine home | Sketch | Effort |
|---|---|---|---|---|---|
| 1 | `lib/same-task-capture.mjs` (+9 probes with byte-pinned copies + acorn validator) | Same-task render→readback (the Batch-767 doctrine); engine's own `CesiumDebug.canvasPixels()` (:1203) does deferred `drawImage` = the invalid read | `CesiumDebug` (`Scene` debug surface) | `CesiumDebug.captureNow()`: `scene.initializeFrame(); scene.render(); canvas.toDataURL()` synchronously in one task, then decode; reimplement `canvasPixels()` on it. Probes KEEP their embedded canonical block as independent discriminator; the promotion is that the engine's own debug read stops lying. Validator retires only for probes whose read is not the thing under test. | S |
| 2 | `lib/weather-probe-pinning.mjs` awaitGlobeReady (frustumCommandsList GLOBE count, **47 sites**) | Private `_view.frustumCommandsList[i].indices` walk to count GLOBE-pass commands; on WebGPU the WebGL-shaped list can read EMPTY (DEFERRED :997) | `Scene.getDebugSnapshot().frustums.passCounts` | Sum `indices` per pass over frustumCommandsList → `{globe, opaque, translucent, ...}`; WebGPU renderer contributes executed counts when the shared list is empty. Probes gate on `snapshot.frustums.passCounts.globe > 0`. | S |
| 3 | `lib/determinism-kit.mjs` settleTiles / settle-attribution findFirstCompleteFrame / `_tilesToRender` complete-tile count | Globe readiness predicate: `Globe.tilesLoaded` is queue-empty only (Globe.js:604); WebGPU black-globe race documented 3× (DEFERRED :4507, :2870, :5899) | `Globe.renderReady` + snapshot fields | `renderReady = tilesLoaded && no _tilesToRender with renderedMesh!==mesh or un-uploaded imagery && WebGPU globe pipelines resolved`; feed `WebGPUGlobeSurfaceTextures` upload count + globe pipeline pending into it; expose `selectedTileCount/completeTileCount/pendingImageryUploads/pendingPipelines` in `Scene.getDebugSnapshot()`. Keep wall-clock dwell in Tools as the floor until Edge-proven. | M |
| 4 | `lib/weather-probe-pinning.mjs` `_cloudCache.uniformData[N]` slot reads (**27 files**) + `probe-cloud-density-domain.mjs` QF literal 8192 | Cloud uniform slot indices by hand; QF word not page-reachable | `WebGPUProceduralCloudRenderer` / `WebGPUCloudTierPresets` + `CesiumDebug.cloudUniforms()` | `export const CloudUniformSlot = Object.freeze({COVERAGE:35, ...})` used by the renderer's own `data[]` writes; `CesiumDebug.cloudUniforms()` returns `{slot:value}` named copy + `weatherProviderVersion`; volumetricClouds stats add `qualityFlags:{value,names}` decoded from data[74]. Force/mutation controls stay in Tools. | M |
| 5 | `lib/globe-pipeline-readiness.mjs` + `probe-globe-pipeline-readiness.mjs` | Tiles skipped at `if (!pipeline) continue;` (WebGPUGlobeSurfaceRenderer.ts:1518) — nothing counts them; MECHANISM_PINS on `_pipelineCache`/`_webgpuPipelineCache` | `WebGPUGlobeSurfaceRenderer.getStatistics()` → `getRendererStatistics().globe` | Per-frame `tilesSkippedPipelinePending` counter reset each frame; `getStatistics(): {tilesSelected, tilesDrawn, tilesSkippedPipelinePending, pipelineVariants, pipelinesPending}`; fold into `Scene.getDebugSnapshot()` / `CesiumDebug.pipelineStatus()`. Probe retargets to existing `listPipelineVariants()` (:2759) and drops the two visibility pins. | S |
| 6 | `probe-depth-plane-horizon-oracle.mjs` useDepthPlane wrap | **Engine bug**: WebGPUContext.ts:4929-4932 recomputes `useDepthPlane` without `debugSkipDepthPlane`; `CesiumDebug.skipDepthPlane` is dead on WebGPU (Scene.js:4300 comment claims parity) | `WebGPUContext.ts` | `&& scene.debugSkipDepthPlane !== true`, or consume Scene's already-computed `environmentState.useDepthPlane` instead of re-deriving. Drop the probe wrap. | S |
| 7 | `probe-ssgi.mjs` | **Engine bug**: `ao.uniforms.ssgiDebugMode/aoWeight` read once at config (WebGPUPostProcessStageCollection.ts:758 → WebGPUAmbientOcclusionEffect.ts:869-873), never re-uploaded; runtime edits silently ignored | `WebGPUAmbientOcclusionEffect` frame update | `updateAmbientOcclusionFrameData` compares against cached values, `queue.writeBuffer` on change (same layout, no bind-group rebuild). Probe sets the uniform instead of poking internals. | S |
| 8 | `lib/webgpu-error-gate.mjs` | No `uncapturederror` listener anywhere in engine (only `device.lost`, WebGPUDeviceLossRecovery.setupHandler :280); the gate's single-slot `onuncapturederror` assignment is a latent conflict | Device acquisition + recovery path → `getRendererStatistics()` / `CesiumDebug.pipelineStatus()` | `device.addEventListener('uncapturederror', e => {stats.uncapturedErrors++; stats.lastUncapturedError=...; console.error(...)})`. Gate stays in Tools (independence) but switches to `addEventListener`. | S |
| 9 | `lib/celestial-g2-gate.mjs` pbrNeutralTonemap + inverse | 2 private engine JS ports (SolarDiscModel.js, computeAtmosphereExtinction.js) + GLSL + 2 WGSL + 3 Tools copies | new `Core/PbrNeutralTonemapping.js` | Export `pbrNeutralTonemap`, `pbrNeutralTonemapInverse`, `pbrNeutralNeutralGrey`, `START_COMPRESSION=0.76`, `DESATURATION=0.15`; SolarDiscModel + computeAtmosphereExtinction import it; add SHADER_PAIRS_LOCKSTEP row pinning GLSL/WGSL literals to the JS constants; g2/eclipse probes import from engine (linearizing captures is not circular — the gate discriminates PSF ratios). | M |
| 10 | `probe-c12-29-s5-custom-ellipsoid.mjs` camera-UB indices + Lane-B `_cameraUniformData[164/229]` | Globe camera UB is an `offset++` pack with no exported layout (tile UB already has named offsets, WebGPUGlobeSurfaceTypes.ts:282-289) | `WebGPUGlobeSurfaceCameraUB.ts` | `export const GLOBE_CAMERA_UB = Object.freeze({oneOverRadii:51, maximumRadius:86, cloudShadowVP:164, cloudShadowParams:229, ...})`, asserted against the running offset under a debug pragma; gates import it. | S |
| 11 | `lib/webgpu-model-preparation-evidence.mjs` | Admission stats only on `frameState._webgpuModelPreparationStatistics` behind a private enable flag; no in-engine conservation check | `WebGPUModelPreparationAdmission.ts` + `CesiumDebug.modelPreparation(t/f)` | Export `checkWebGPUModelPreparationConservation(stats)` (candidates == view+shadow+capture+conservative+rejected == Σreasons) + field-name lists; debug command flips the enable flag and returns stats. | S |
| 12 | `lib/voxel-megatexture-reupload-gate.mjs` | Reads `prim._webgpuCache.dataUpload.*` (l2PoolSize/evictionCount/lastL2DemandCount/slotGenerations, WebGPUVoxelDataUpload.ts:309-330); C18-V2 landed `ready` not residency stats | `WebGPUVoxelRenderer.getStatistics(prim)` → `getRendererStatistics().voxels` + `CesiumDebug.voxelAtlas()` | Frozen snapshot `{slotCount, slotsUsed, l2PoolSize, l2Dynamic, evictionCount, lastL2DemandCount, slots:[{requestSerial, generation, phase}]}`; keep the `_webgpuVoxelAtlasMaxSlots` override (declared probe hook :779). | S |
| 13 | `lib/eclipse-cloud-response-gate.mjs` bucket read + `probe-model-ibl*.mjs` IBL readiness (`\|\| true` predicate) | Env-map bucket lives on two asymmetric private owners (DynamicEnvironmentMapManager `_lastEclipseEnvBucket` :133 vs WebGPU cache :210/635); no `ready`/statistics accessor | `DynamicEnvironmentMapManager.getDebugStatistics(scene)` → `Scene.getDebugSnapshot().environmentMap` | Mirror `Moon.getDebugStatistics`: `{eclipseEnvBucket, eclipseRefreshCount, ready, lastSceneCaptureResult, cubemapSize}` with the WebGPU FR supplying cache values. **Attach to open C13-41 (REOPENED, C14 critical path)** — one accessor covers both probes; curve/bucket oracle stays in Tools. | S |
| 14 | `lib/performance-campaign-utils.mjs` summarizeEclipseGlobeShadowEvidence | `params.x` gate encoded as bare 0..4 literals with the legend only in JSDoc (EclipseGlobeShadow.js:451, writes :629/:1190) | `EclipseGlobeShadow.js` | `export const EclipseGlobeShadowGate = Object.freeze({INERT:0, ACTIVE_CUSTOM:1, ACTIVE_SUN:2, CORRECTION_SUN:3, CORRECTION_CUSTOM:4})` + `isLocalGlobeShadowGate(g)`; engine writes use the enum; runner imports it (Node-importable). | S |
| 15 | `probe-moon-globe-depth-occlusion.mjs` | Six private physicalDepth route fields (:370-374, :455-483); no TAA reset counter | `Moon.getDebugStatistics` (:944) + TAA effect stats | Add `physicalDepth:{distanceDemand, prewarmRequested, requested, actual, routeChanged, uniformSlots}`; TAA exposes `historyResetCount`. Jitter pin stays a Tools control. | S |
| 16 | `probe-sky-aureole-anchor.mjs` drawWitness | No environment block in `getDebugSnapshot` although `_environmentState` (:1262) holds the flags | `Scene.getDebugSnapshot().environment` | `{skyAtmosphereVisible, sunVisible, moonVisible, starFieldVisible, skyBoxVisible, lastExecutedFrame:{skyAtmosphere, sun, moon, ...}}` stamped at the env-command execute site (fact recorder, not the primitive under test). | S |
| 17 | `probe-celestial-gates.mjs` setupPolicyProbe | `hdrDisplayPolicy` exists (:3042) but no readable detection result or override; C12-28 records "MANUAL HDR-HARDWARE CHECK OWED" | `Scene` + `CesiumDebug.hdrDisplayOverride()` | `readonly get hdrDisplayDetected()` + `CesiumDebug.hdrDisplayOverride(true\|false\|null)` consulted by `_applyHdrDisplayDefault` and re-run. Makes the owed manual leg automatable. | S |
| 18 | isVisible occluder wrap (moon + depth-plane probes) | Same per-command monkeypatch in two probes; Scene has `debugCommandFilter`/`debugSkipDepthPlane` but no occluder bypass | `Scene.debugOccluderFilter` | Consulted in `isVisible` (:4043) only when defined: `... \|\| (defined(this.debugOccluderFilter) && this.debugOccluderFilter(command)) \|\| !isOccluded`. | S |
| 19 | `lib/c11-168-direct-model-ablation.mjs` describeModel | `Model.js` has no public resource/url getter (only `_resource` :211) while `Cesium3DTileset` has `get resource()` (:1944) | `Model.js` | `@readonly get resource() { return this._resource; }`; probes read `model.resource.url`. Command-owner census stays in Tools. | S |

Explicitly **not** promoted despite one verifier saying PROMOTE-LATER: `lib/c12-29-s5-terrain-selection-gate.mjs` radius law — the S5 lane is HELD (D8 reassignment) and the gate is the discriminator of the very radius the engine computes; revisit only after S5 closes (spec cross-assert against the exported `TERRAIN_ECLIPSE_BOUND_SAFETY_*` constants is the honest interim).

## 2. QUEUED-ALREADY

These have an owning row and need no new row — they demonstrate the pipeline already routes tool-observed gaps into queued engine work.

| Tool | Existing row | What the row already owns |
|---|---|---|
| `probe-eclipse-cloud-response.mjs` Lane B telemetry (shadowActive/strength/absorption/frameValid) | **C13-41** (Campaign 13, REOPENED) | Row text records the missing `shadowActive` telemetry and the UNREADABLE `cloudShadowControl` readout as instrument residuals; `WebGPUCloudObservability.shadow` already publishes passCount/size/cascadeSize/cascadeCount (:694-699). PROMOTE #13 above should land inside this row rather than as a new one. |
| `probe-gsplat-parity.mjs` SH-degree control | **C18-S1** (Campaign 18, gated post-C15-G8) | Introduces the SH-degree budget seam via `applySphericalHarmonicsBudget` (:695) — a public SH-degree control belongs there. Quiescence half is EXISTS-ALREADY (`isStable` :2037 / `ready` :2011). |
| `probe-celestial-gates.mjs` HDR hardware leg | **C12-28** ("MANUAL HDR-HARDWARE CHECK OWED") | Records the owed check; PROMOTE #17 is the small enabler, and could be attached to C12-28 rather than a new row if the maintainer prefers. |
| `lib/solar-bloom-glow.mjs` shared model | C12 queue :904 | Records this lib as the shared-model resolution ("no duplication"); the remaining content is a test-side forward model (WEAK, no row needed). |
| `lib/cloud-coverage-response-model.mjs` literal pinning | C13 CLOUD-LOW-COVERAGE-CUTOFF | Already uses the source-text-spec pinning pattern that the coverage/morphology twins should copy (Tools change). |

Honest note: only C13-41 and C18-S1 are genuine "row already owns this engine change" cases; the other three are pattern/precedent references, not owners of new engine scope.

## 3. EXISTS-ALREADY — simplify the tool to consume the API (small cleanup rows)

Each is a Tools-only edit: import the shipped module/getter, delete the local copy or cross-assert against it.

- `lib/ocean-datum-model.mjs` DATUM_SITES + exaggerationVerdict → `Core/GeoidUndulationGrid.sampleDegrees` (already imported by ocean-tide-datum.spec.mjs:60) + `Core/VerticalExaggeration.getHeight`; replace hand table with grid-within-tolerance cross-assert.
- `lib/ocean-tide-datum-model.mjs` exaggerationCompositionVerdict → `Core/VerticalExaggeration.getHeight` (exact body). TideModel cross-assert stays.
- `lib/c11-13-voxel-pick-pipeline-name.mjs` predicate half → import `isWebGPUPickLogDepthActive` (WebGPULogDepth.ts:105). (Label string export is a one-liner if pursued; low value.)
- `lib/weather-probe-pinning.mjs` FORBIDDEN_QUALITY_BITS hand mask → import `CLOUD_QF_*` (WebGPUCloudTierPresets.ts:209-222).
- `lib/ground-fog-band-model.mjs` geodetic/fovy/ENU pieces → `Ellipsoid`, `Cartesian3.fromDegrees`, `PerspectiveFrustum.fovy`, `Transforms.eastNorthUpToFixedFrame` (module's own recorded 60-vs-35.98° fovy mis-model is the cost of not importing); Koschmieder → `WebGPUGroundFogBand.extinctionForVisibility` (:53). HG/froxel twins stay as oracle.
- `lib/c12-29-s5-svs-footprint-gate.mjs` ECEF/Vincenty → `Ellipsoid.cartographicToCartesian` / `EllipsoidGeodesic.surfaceDistance` (pure geodesy is not what S5 discriminates; removes the two-ECEF-formulas-disagree risk). Tuple wrap stays untouched (HELD lane).
- `lib/c12-29-s5-terrain-selection-gate.mjs` level-1 fixtures → `GeographicTilingScheme`, `QuadtreeTileLoadState`, `TerrainState`, `TileSelectionResult` (public, node-importable). Radius/SSE law stays oracle; fix the missing fog term in the Tools copy.
- `lib/c12-29-s4-orbital-sunrise-gate.mjs` ATMOSPHERE_THICKNESS → `Scene/AtmosphereDerivedLighting.js:285` export. (Side finding: engine-internal dedupe hygiene — computeAtmosphereExtinction.js:8, SkyBrightness.js:65 and ~8 WebGPU packer literals of 111e3 should import the same export; that is engine hygiene, not a promotion.)
- `lib/c11-90-primitive-restart-probe.mjs` frozen 216 → compute from exported `MODEL_TOPOLOGY_TABLE` / `expandTriangleFanToTriangleList` (WebGPUModelTopology.ts:122+). Draw wraps stay (see §4).
- `probe-c12-29-s5-dense-cost.mjs` timestampRestore → `WebGPUTimestampProfiler.latestFrameSamples` (:805) + `summarizeFrameCoverage(samples).frameSpanMs`; drop the `_addToRollingWindow` wrap.
- `probe-gsplat-parity.mjs` quiescence → `GaussianSplatPrimitive.isStable` / `ready`.
- `probe-cloud-empty-frustum.mjs`, `lib/cloud-probe-harness.mjs`, `probe-cloud-temporal-rte.mjs` counters → `WebGPUCloudObservability` (frames/culledFrames/passCount/maxSteps/lightSteps/marchPixels; reconstruction.historyAccepted/Rejected/Reset/ResetReasons; lifetime.generation/resetCount/acceptedFrames). Only qualityFlags is missing (PROMOTE #4 covers it); a temporal `firstFrame/frameIndex` crumb is optional.
- `probe-eclipse-sky-totality.mjs` lens overlap → `Cesium.EclipseDiscGeometry(sun, moon, observer).obscuration` (Core/EclipseDiscGeometry.js:109-158, on the barrel) for vantage/time selection; keep a private copy only where obscuration itself is asserted.
- `probe-scene-capture-cardinal.mjs` → `buildCubeFaceCamera` exported (WebGPUDynamicEnvironmentMapCapture.ts:243); LOCAL_FACE_BASIS copy may remain as face-convention oracle.
- `scene._alternateSceneRenderer` OIT/containment readers (subset of 61 files) → `getDebugSnapshot().containment.webgpuOIT` (already merged :2236-2253). Deeper reads (`_postProcess.taaEffect`, `_depthPlane`) stay test-only; a raw public getter would violate Principle 2.
- `globe._surface._tilesToRender` readers (39 files) → `QuadtreePrimitive.forEachRenderedTile` (:191) / `forEachLoadedTile` (:175) / `Globe.tilesLoaded`; the `globe._surface` hop is upstream-stable-private.
- `probe-c11-169-primitive-breakdown.mjs` phase level → `CPU_SCENE_PHASE_NAMES` (`primitiveTraversal`/`computeShadows`, ViewportExecutor.js:355-407); sub-phase split stays Tools-only per its docstring.
- `lib/globe-pipeline-readiness.mjs` materialized/pending → `listPipelineVariants()` (:2759) — the retarget half of PROMOTE #5.

## 4. KEEP-IN-TOOLS — independence rationale

- `lib/cloud-coverage-response-model.mjs` — distribution-faithful f64 twin of a GPU f32 noise field; promoting a lossy CPU oracle ships something nobody renders with. (Tools fix: use exported `CLOUD_DENSITY_DETAIL_RATIO`, source-text-pin the BILLOWY literals.)
- `lib/cloud-genus-morphology-model.mjs` — same class: shape assertions must be independent of the shader; pin the five WGSL literals (0.12/0.72/0.35/3.0/0.95) by source-text spec instead.
- `lib/c12-29-s5-replacement-device-gate.mjs` — a device-loss gate that reads the engine's own UB size to judge the engine's UB payload is circular (Batch 767); cross-assert gate literals against `ECLIPSE_UNIFORM_FLOATS`/`SceneCaptureResult.SUBMITTED` in the spec.
- `lib/cloud-reconstruction-consume.mjs` (labels; profiler zero=null rule) — pure-Node spec surface that forbids engine imports; the C11-146 rule is consumer discipline the profiler cannot assert without knowing expected passes.
- `lib/c11-90-primitive-restart-probe.mjs` draw wraps — GPUDevice/WebGL `draw*` prototype wraps observe what the browser received, independent of the pipeline cache's opinion.
- `lib/eclipse-cloud-response-gate.mjs` predictFactor — floor deliberately recomputed so a silent engine retune fails the probe; spec cross-asserts equality with exported constants (mutant-control pattern); pin the 0.35 beer-floor (GlobeTerrain.wgsl:2156) in SHADER_PAIRS_LOCKSTEP.
- `lib/daynight-terminator-law.mjs` — frozen CLT-B4 acceptance instrument; "an instrument rewritten in the same batch as the thing it measures certifies nothing."
- `lib/sun-radiance-delta.mjs` discDifferentialCodeModel — five-stage composite ORDER is the discriminated primitive for the D1/D2 bands; pin blend order via SunFS source-text assertion.
- `lib/c12-29-s5-terrain-selection-gate.mjs` SSE/radius law + `lib/c12-29-s5-svs-footprint-gate.mjs` tuple wrap — the S5 discriminator of the radius/selection the engine computes; lane is HELD (D8), do not open engine scope there now.
- `lib/ground-fog-band-model.mjs` HG/froxel twins — a TS twin would be a third copy that still cannot see the WGSL; only the Edge run closes that drift.
- `probe-cloud-empty-frustum.mjs` stage wraps — a self-reported "I ran" counter is the primitive reporting on itself; the wrap is the honest independent form.
- `probe-msaa-resolve-elision` / `pp-library-builtins` / `c10-09` GPU prototype patches, and native GPU* prototype ledgers (replacement-device, c11-193c, c11-209) — 260 direct `createBuffer` and 69 direct `beginRenderPass` sites bypass any engine ledger; an engine count would under-report the traffic these gates exist to catch. (Tools consolidation: one shared wrap lib instead of three divergent copies.)
- `probe-logdepth-pp-slicec.mjs` stash defineProperty — deliberately breaks the engine contract as a probe control; an engine "disarm" flag is pure test plumbing.
- `probe-scene-capture-cardinal.mjs` LOCAL_FACE_BASIS — oracle asserting the documented face convention.
- `context._options.webgpu` runtime pokes (14 files) — documented construction-time options re-read per frame; the poke is a stable test control, not missing engine function (WEAK; no row).
- `probe-gsplat-parity.mjs` packed-texture word swap, `probe-cloud-density-domain.mjs` execute-wrap force, TAA jitter pin, dirty-flag writes — mutation controls; test-only by nature.
- `lib/same-task-capture.mjs` embedded text pin — even after PROMOTE #1, probes keep the canonical block so the engine's capture never becomes the probe's discriminator.

## 5. Proposed DEFERRED_WORK rows (NEW-* style; text only, nothing written)

- **`NEW-DEBUG-SAME-TASK-CAPTURE-COMMAND`** — `CesiumDebug.canvasPixels()` (:1203-1215) does a deferred `drawImage(canvas)` — the exact cross-task read the Batch-767 capture doctrine forbids (WebGL black, WebGPU transparent). Nine probes carry byte-pinned copies of `Tools/visual-regression/lib/same-task-capture.mjs` plus an acorn validator solely because the page cannot import Node. Add `CesiumDebug.captureNow()` (`scene.initializeFrame(); scene.render(); canvas.toDataURL()` in one task, then decode) and reimplement `canvasPixels()` on it. Probes KEEP the embedded canonical block as their independent discriminator; the deliverable is that the engine's own debug read stops lying. Effort S.
- **`NEW-SCENE-SNAPSHOT-FRUSTUM-PASS-COUNTS`** — 47 probe sites walk private `_view.frustumCommandsList[i].indices` to count GLOBE commands as a readiness gate; DEFERRED :997 records the list can read EMPTY on WebGPU (SCENE-EXTRACTION-AUDIT-GAP class), so the walk is brittle and backend-suspect. Extend `Scene.getDebugSnapshot().frustums` (which already reads the list for `count`, Scene.js:2161) with `passCounts {globe, opaque, translucent, ...}`, with the WebGPU renderer contributing executed counts when the shared list is empty; probes gate on `passCounts.globe > 0`. Effort S.
- **`NEW-GLOBE-RENDER-READY-PREDICATE`** — `Globe.tilesLoaded` (Globe.js:604) is queue-empty only; the WebGPU black-globe race (tilesLoaded TRUE while imagery uploads/pipelines still cook) is recorded at DEFERRED :4507/:2870/:5899 and every fix so far is probe-side dwell or a private `_tilesToRender` walk. Add `Globe.renderReady` = tilesLoaded && no rendered tile with `renderedMesh!==mesh` or un-uploaded imagery && WebGPU globe pipelines resolved (feed `WebGPUGlobeSurfaceTextures` upload count + pipeline-pending into it), plus snapshot fields `selectedTileCount/completeTileCount/pendingImageryUploads/pendingPipelines`. Keep the Tools wall-clock dwell as the independent floor until the predicate is Edge-proven. Effort M.
- **`NEW-CLOUD-UNIFORM-SLOT-TABLE-AND-DEBUG-READ`** — 27 Tools files read `context._cloudCache.uniformData[N]` by hand index and `probe-cloud-density-domain.mjs` hard-codes QF literal 8192 although `CLOUD_QF_*` is exported (WebGPUCloudTierPresets.ts:207-236) but not page-reachable. Export `CloudUniformSlot = Object.freeze({COVERAGE:35, ...})` used by the renderer's own `data[]` writes; add `CesiumDebug.cloudUniforms()` returning a named `{slot:value}` copy + `weatherProviderVersion`; volumetricClouds stats publish `qualityFlags:{value, names}` decoded from data[74]. Pin harness reads names, not indices; force/mutation controls stay in Tools. Effort M.
- **`NEW-WEBGPU-GLOBE-SURFACE-STATISTICS`** — `WebGPUGlobeSurfaceRenderer.ts:1518` `if (!pipeline) continue;` increments nothing, so tiles skipped for a pending pipeline are invisible; `lib/globe-pipeline-readiness.mjs` pins `_pipelineCache`/`_webgpuPipelineCache` visibility instead (BROKEN_STALE shape). Add per-frame `tilesSkippedPipelinePending` + `getStatistics(): {tilesSelected, tilesDrawn, tilesSkippedPipelinePending, pipelineVariants, pipelinesPending}` folded into `getRendererStatistics().globe` and `CesiumDebug.pipelineStatus()`; probe retargets to `listPipelineVariants()` (:2759) and drops the pins. Effort S.
- **`NEW-WEBGPU-DEPTH-PLANE-DEBUG-SKIP-PARITY`** — `WebGPUContext.ts:4929-4932` recomputes `environmentState.useDepthPlane` without the `debugSkipDepthPlane` term Scene.js:4300-4307 applies (whose comment claims WebGL+WebGPU identical); `CesiumDebug.skipDepthPlane` is dead on WebGPU and `probe-depth-plane-horizon-oracle.mjs` monkeypatches around it. Add the term or consume Scene's already-computed value; drop the probe wrap. Effort S.
- **`NEW-WEBGPU-AO-SSGI-UNIFORMS-RUNTIME-REUPLOAD`** — `ao.uniforms.ssgiDebugMode/aoWeight` are read once at config (WebGPUPostProcessStageCollection.ts:758 → WebGPUAmbientOcclusionEffect.ts:869-873) into the composite UB and never re-uploaded; runtime edits to a documented uniform are silently ignored (`probe-ssgi.mjs` reaches into internals as a workaround). `updateAmbientOcclusionFrameData` should compare against cached values and `queue.writeBuffer` on change (same layout, no bind-group rebuild). Effort S.
- **`NEW-WEBGPU-UNCAPTURED-ERROR-COUNTER`** — no `uncapturederror` listener exists in `packages/engine/Source` (WebGPUDeviceLossRecovery.setupHandler :280 handles `device.lost` only), so production has no uncaptured-error count and `Tools/lib/webgpu-error-gate.mjs` claims the single `onuncapturederror` slot (latent conflict). Register `addEventListener('uncapturederror', ...)` at device acquisition and recovery, surface `uncapturedErrors/lastUncapturedError` in `getRendererStatistics()` and `CesiumDebug.pipelineStatus()`; the gate switches to `addEventListener` and stays in Tools. Effort S.
- **`NEW-CORE-PBR-NEUTRAL-TONEMAPPING-MODULE`** — the PBR-neutral tonemapper exists as two private JS ports (SolarDiscModel.js, computeAtmosphereExtinction.js), one GLSL, two WGSL and three Tools copies (`lib/celestial-g2-gate.mjs` et al.); C12 queue :553 describes the chain but no row owns consolidation. Add `Core/PbrNeutralTonemapping.js` exporting `pbrNeutralTonemap`, `pbrNeutralTonemapInverse`, `pbrNeutralNeutralGrey`, `START_COMPRESSION=0.76`, `DESATURATION=0.15`; both engine callers import it; add a SHADER_PAIRS_LOCKSTEP row pinning the GLSL/WGSL literals to the JS constants; g2/eclipse probes import from engine. Effort M.
- **`NEW-WEBGPU-GLOBE-CAMERA-UB-NAMED-OFFSETS`** — `WebGPUGlobeSurfaceCameraUB.ts` is an `offset++` pack with no exported layout while the sibling tile UB has named offsets (WebGPUGlobeSurfaceTypes.ts:282-289); `probe-c12-29-s5-custom-ellipsoid.mjs` and the eclipse Lane-B probe carry magic indices (51/86/164/229) into `_cameraUniformData`. Export `GLOBE_CAMERA_UB = Object.freeze({...})`, assert against the running offset under a debug pragma, import from the gates. Effort S.
- **`NEW-WEBGPU-MODEL-PREPARATION-STATISTICS-ACCESSOR`** — admission statistics live only on `frameState._webgpuModelPreparationStatistics` behind `context._webgpuModelPreparationDiagnosticsEnabled` (WebGPUModelPreparationAdmission.ts:93-99, 491-507); no Scene/CesiumDebug exposure and no in-engine conservation check. Export `checkWebGPUModelPreparationConservation(stats)` (candidates == view+shadow+capture+conservative+rejected == Σreasons) + field-name lists; add `CesiumDebug.modelPreparation(t/f)` that flips the flag and returns stats; `lib/webgpu-model-preparation-evidence.mjs` imports the check. Effort S.
- **`NEW-WEBGPU-VOXEL-ATLAS-RESIDENCY-STATISTICS`** — `WebGPUVoxelDataUpload.ts` state (l2PoolSize :309, evictionCount :324, lastL2DemandCount :330, slotGenerations/requestSerial) is reachable only via `prim._webgpuCache.dataUpload` (read by `lib/voxel-megatexture-reupload-gate.mjs`); C18-V2 landed `VoxelPrimitive.ready` but no residency stats. Export `WebGPUVoxelRenderer.getStatistics(prim)` returning a frozen snapshot, fold into `getRendererStatistics().voxels` and `CesiumDebug.voxelAtlas()`; keep the `_webgpuVoxelAtlasMaxSlots` probe hook (:779). Effort S.
- **`NEW-DYNAMIC-ENV-MAP-DEBUG-STATISTICS`** *(land inside C13-41 rather than as a standalone row if the maintainer prefers)* — the eclipse env bucket lives on two asymmetric private owners (DynamicEnvironmentMapManager `_lastEclipseEnvBucket` :133 vs WebGPU cache `lastEclipseEnvBucket` :210/635) and no `ready`/statistics exists (only position/radianceCubeMap/SH getters :504-555), so `probe-model-ibl*.mjs` carries an `|| true` readiness predicate. Add `DynamicEnvironmentMapManager.getDebugStatistics(scene)` mirroring `Moon.getDebugStatistics`: `{eclipseEnvBucket, eclipseRefreshCount, ready, lastSceneCaptureResult, cubemapSize}` with the WebGPU FR supplying cache values; surface under `Scene.getDebugSnapshot().environmentMap`. Curve/bucket oracle stays in Tools. Effort S.
- **`NEW-ECLIPSE-GLOBE-SHADOW-GATE-ENUM`** — `EclipseGlobeShadow.js` encodes the `params.x` gate as bare literals 0..4 (legend only in JSDoc :451; writes :629/:1190) and exports no gate enum or local-shadow predicate (:1433-1458), so `lib/performance-campaign-utils.mjs` re-types the legend. Export `EclipseGlobeShadowGate = Object.freeze({INERT:0, ACTIVE_CUSTOM:1, ACTIVE_SUN:2, CORRECTION_SUN:3, CORRECTION_CUSTOM:4})` + `isLocalGlobeShadowGate(g)`; engine writes use the enum; runner/gate import it. Effort S.
- **`NEW-MOON-PHYSICAL-DEPTH-DEBUG-STATISTICS`** — `Moon.getDebugStatistics` (:944) has no physicalDepth block; the six route fields (:370-374, :455-483) are private and TAA has no history-reset counter, so `probe-moon-globe-depth-occlusion.mjs` reaches in. Extend with `physicalDepth:{distanceDemand, prewarmRequested, requested, actual, routeChanged, uniformSlots}`; TAA effect stats expose `historyResetCount`. Jitter pin stays a Tools control. Effort S.
- **`NEW-SCENE-SNAPSHOT-ENVIRONMENT-BLOCK`** — `Scene.getDebugSnapshot` (:2100-2260) has no environment block although `_environmentState` (:1262) holds isSkyAtmosphereVisible/isSunVisible/isMoonVisible; `probe-sky-aureole-anchor.mjs` wraps draw calls to witness execution. Add `environment = {skyAtmosphereVisible, sunVisible, moonVisible, starFieldVisible, skyBoxVisible, lastExecutedFrame:{skyAtmosphere, sun, moon, ...}}` stamped at the env-command execute site. Effort S.
- **`NEW-SCENE-HDR-DISPLAY-DETECTED-AND-OVERRIDE`** — `Scene.hdrDisplayPolicy` (:3042) has no readable detection result and no override; detection is subscription-driven (:3137-3153) so `probe-celestial-gates.mjs` pokes `_hdrDisplayIsHdr`; C12-28 records the manual HDR-hardware check as OWED. Add `readonly get hdrDisplayDetected()` + `CesiumDebug.hdrDisplayOverride(true|false|null)` consulted by `_applyHdrDisplayDefault` and re-run, making the owed leg automatable. Effort S.
- **`NEW-SCENE-DEBUG-OCCLUDER-FILTER`** — Scene has `debugCommandFilter` (:734-744) and `debugSkipDepthPlane` but no occluder bypass; the moon and depth-plane probes carry the same `isVisible` monkeypatch on a hot per-command path. Add `Scene.debugOccluderFilter` (default undefined) consulted in `isVisible` (:4043) only when defined. Effort S.
- **`NEW-MODEL-PUBLIC-RESOURCE-GETTER`** — `Model.js` exposes no public resource/url (only `_resource` :211 / `_gltfResource`) while `Cesium3DTileset` has `@readonly get resource()` (:1944); `lib/c11-168-direct-model-ablation.mjs` reads the private field. Add the mirrored readonly getter; probes read `model.resource.url`. Effort S.

Empty categories: none — all five sections are populated. Not empty but thin: QUEUED-ALREADY has only two genuine owners (C13-41, C18-S1); the rest are precedent references, stated as such above.
