<!-- Orchestrator review record. Landed Batch 738. The C13 queue ledger is deliberately NOT updated here: QUEUE_2026-07-23_CAMPAIGN13.md is dirty with Sol's in-flight work; ledger reconciliation happens when that tree lands. -->

# SOL 5.6 / CAMPAIGN 13 REVIEW REPORT
**Reviewer:** Orchestrator audit (read-only) — 2026-07-23
**Scope:** Batches 732-736 (`f4a934e606`, `d1d1849ccc`, `98b6afead8`, `6801790b8c`, `2d3c9f8025`) + the dirty working tree (C13-37, 11 modified / 10 untracked files, protected by `snap-sol-c13-2026-07-23`).
**Authorities:** `migration_doc/CLOUD_ARCHITECTURE_AUDIT_2026-07-23.md` (evidence), `migration_doc/QUEUE_2026-07-23_CAMPAIGN13.md` (live queue).

**State delta since the review window opened:** HEAD has advanced one commit to `438fef3fc2` (Batch 737, C11+C12 docs-only reconciliation — its own body confirms "Sol's C13 dirty tree is untouched"). All "HEAD" anchors below refer to the C13 tip `2d3c9f8025`. `origin/main` now equals `main` (`438fef3fc2`) — the 403 push block recorded in the queue (lines 17-21) has been resolved and all five C13 batches are published. Branch transparency: besides `main`, the tree carries `snap-sol-c13-2026-07-23` (deliberate, keep until the dirty tree lands) and a stray `worktree-agent-a58ee463413961f0e` (not part of C13; audit/clean separately).

---

## Executive verdict

Sol 5.6's Campaign 13 output is **high quality and unusually honest by this repo's historical standards**. Every one of the 12 audit findings' primary anchors verified TRUE at the stated anchor `851ce64389` (12/12 spot-checked — the evidence authority is trustworthy). The five committed batches contain **zero fabricated quantitative claims**: every statically checkable number was corroborated verbatim against on-disk truth manifests, and the ledgers consistently disclaim what was *not* done ("primary visible shell only", "does not close C13-37/12/06/22", "characterization, not a promotion claim"). No binding project rule was broken in committed work: ShaderDefine registry untouched (all new flags are `QF_*` uniform bits, e.g. `QF_PLANET_DENSITY` bit 13 at working-tree `ProceduralClouds.wgsl:319`), RTE contract respected and strengthened, no feature removed without audit sanction, uniform growth strictly add-only-at-tail.

The recurring weaknesses are: **probes without hard process-level watchdogs** (a binding rule, and a machine with a crash history), **phantom `sharp`/`playwright` dependencies**, one **red-at-commit spec suite** (Batch 734), and a **ledger row lagging the tree** in the dirty work.

| Batch | Commit | Task(s) | Verdict |
|---|---|---|---|
| 732 | `f4a934e606` | C13-00 launch (docs-only) | **ACCEPTABLE-WITH-NOTES** — audit evidence 12/12 verified; one doc self-claim false (DEFERRED_WORK "retained below for trace") |
| 733 | `d1d1849ccc` | C13-01 Slice A (truthful evidence tooling) | **ACCEPTABLE-WITH-NOTES** — genuinely wired RED gates; `sharp` phantom dep; no watchdog; bundle-provenance can hash the wrong file in dev-server mode |
| 734 | `98b6afead8` | C13-35 empty-frame demand scheduling | **ACCEPTABLE-WITH-NOTES** — engine change sound and probe-corroborated (42/42, 7,584 cells, 6.3553%); but committed spec suite was red at this commit (forward-references `probe-cloud-planetary.mjs`, first added in 735) |
| 735 | `6801790b8c` | C13-03 + C13-04 (WGS84 primary shells) | **ACCEPTABLE-WITH-NOTES** — strongest batch; all ledger numbers reproduced exactly; scope enforcement is spec-pinned (shadow path asserted still-spherical); no overclaim found |
| 736 | `2d3c9f8025` | C13-36 IGN ray-start jitter | **ACCEPTABLE-WITH-NOTES** — all claims verified; one tautological spec assertion; probe lacks watchdog |
| Dirty tree | uncommitted | C13-37 Slices A+B + IBL-parity leg | **DEFECTS-FOUND — do not accept as complete.** Its own acceptance oracle is RED (all 4 probe runs `domainNoRegressionPassed:false`); ledger row stale; probe is a machine-safety hazard as written |

---

## Campaign progress

Reconciled against the committed ledger (queue lines 564-608 at `2d3c9f8025`) plus the single uncommitted ledger hunk (C13-37 row, verified as the only queue diff):

**COMPLETE (5 of 38 tasks):**
- `C13-00` (B732), `C13-35` (B734), `C13-03` + `C13-04` (B735 — C13-04 explicitly scoped "primary visible shell only"; density/noise remained raw ECEF at that commit), `C13-36` (B736).

**IN PROGRESS (2):**
- `C13-01` — Slice A landed across 733-735 (truthful tour/temporal/perf probes, harness, planetary evidence); further evidence slices open (six original tour scenes still lack `minCloudish` floors; no watchdogs).
- `C13-37` — the entire dirty tree. Contains MORE than the ledger claims: Slice A (planet-stable density domain: `WebGPUCloudDensityDomain.ts` 249 ln, `CloudDensityDomain.wgsl` 135 ln, `ProceduralClouds.wgsl` +854), Slice B (`CloudNoiseMipmap.wgsl` self-labeled "Slice B", mip chains in `WebGPUCloudNoiseResources.ts` +175, footprint LOD in both consumers), and an IBL-parity leg (`WebGPUDynamicEnvironmentMapManager.ts` +88, `ProceduralSkyCubemap.wgsl` +143). Gate currently RED (see defects).

**BLOCKED (1):** `C13-11` (STBN — license provenance; note the B732 doc rewrite dropped the pointer to the already-recorded license-clean EA SEED FastNoise plan at `migration_doc/RESEARCH_REGISTER_2026-07-06.md:11,33-49`).

**NOT STARTED (~30 tasks + all 5 gates):** everything else, including all of W2 temporal reconstruction (09-13 except the 36/37 slices above), all of W3 regional weather (14-20), W4 quality (21-24), W5 transfers (25-28), W6 tail (29-34), `C13-02`, `C13-05..08`, and Gates A-D + EXIT.

**Honest percentage: ~13% of tasks complete (5/38), ~18% counting in-flight partial credit.** No gate has been attempted. The campaign is early-W1 with two W2-adjacent early wins (35, 36) pulled forward.

---

## Confirmed defects to fix

Ranked. "Blocks" = blocks accepting the dirty tree.

1. **[HIGH — MACHINE SAFETY, blocks running, blocks acceptance]** `Tools/visual-regression/probe-cloud-density-domain.mjs` has no hard watchdog and no try/finally around the Playwright browser (`browser.close()` at line 743 is success-path only). Unbounded awaits inside `page.evaluate` (rAF at 459/478; `device.queue.onSubmittedWorkDone()` at 465/480/578) mean a GPU hang leaves headless Edge with `--enable-unsafe-webgpu` alive indefinitely. This machine has crashed from runaway probes before. Current-era house standard is a force-exit watchdog (`probe-backend-isolation.mjs:34-40`, `probe-ocean-waves-perf.mjs:37-43`, `probe-oit-collection-reachable.mjs:50-54`, `probe-cpu-sampling-profile.mjs:36-40`). In-page loops themselves ARE bounded (360/180-frame caps).
   **Fix:** unref'd 240-300 s `setTimeout` force-exit + try/finally browser close. **Do not run this probe (or the 733/734/735/736 probes, which share the gap at lower severity) until added; interim: external timeout wrapper only.**

2. **[HIGH — blocks acceptance of C13-37]** The work's own acceptance gate is RED: all four runs in `Tools/visual-regression/output/cloud-density-domain/characterization/*/manifest.json` (commit `2d3c9f8025`, dirty) report `domainNoRegressionPassed:false`. Latest run (`c13-37-mips-v2-43dc216397e8c2e4`): near-horizon baked periodicity **0.1147 vs legacy 0.0953 (ratio 1.20)** — the new domain is *more* periodic than legacy in the scene where the lattice artifact was strongest (above-deck-oblique improved, ratio 0.41; morphology preservation 4/4; the softer live-envelope gate passes). The work is genuinely mid-implementation, consistent with "IN PROGRESS".
   **Fix:** Sol must iterate until `domainNoRegressionPassed:true` in ALL scenes plus manual PNG review (the probe's own limitations block, lines 1126-1137, says the same).

3. **[MEDIUM — blocks landing]** Stale ledger row: the uncommitted C13-37 edit says footprint mip/LOD "remains the next bounded slice," but Slice B is fully in the tree (`CloudNoiseMipmap.wgsl:1`; `cloud-density-lod.spec.mjs:51-116`; probe run tagged `c13-37-mips-v2`).
   **Fix:** row must name Slices A+B + IBL leg as in-tree and state the actual remaining work before any landing.

4. **[MEDIUM — blocks landing as one batch]** One-concern-per-landing strain (queue §4): the tree bundles (A) density-domain contract, (B) 3D mip/LOD, (C) IBL capture parity + revision/scene-clock invalidation. The IBL leg is defensible C13-37 parity, not a covert C13-06 (mask/shadow RTE explicitly deferred at `ProceduralClouds.wgsl:1327-1330`), but the invalidation machinery is a self-contained sub-feature.
   **Fix:** land as ≥2 batches (domain+LOD, then IBL parity/invalidation) or record an explicit single-batch justification in the ledger.

5. **[MEDIUM]** Phantom `sharp` dependency, now load-bearing: `lib/cloud-image-analysis.mjs:203-215`, `probe-cloud-perf.mjs` (since 733), `probe-cloud-empty-frustum.mjs:29-30` (since 734). Resolves only transitively via `@cesium/sandcastle → @huggingface/transformers → sharp@0.34.5`; contradicts `Tools/visual-regression/README.md` "Why no external deps."
   **Fix:** declare `sharp` as a pinned root devDependency or fall back to the harness canvas-decode path; reconcile the README.

6. **[MEDIUM]** IBL refill lost its epsilon debounce: `publishCloudIblCoverage` bumps `iblRevision` on any exact float change (`!==` on coverage/density/wind) and `WebGPUDynamicEnvironmentMapManager` refills on every revision change (~:520-580 in diff) — an app animating `cloudCoverage` now triggers full cube fill + prefilter + SH projection every frame (old path was gated by `CLOUD_COVERAGE_REFRESH_EPSILON`).
   **Fix:** quantize/epsilon-gate continuous inputs in `staticStateChanged`, mirroring the 64 m advection debounce. Should be fixed before the IBL leg lands.

7. **[MEDIUM — record only, self-healed]** Batch 734 committed a red spec suite: `cloud-probe-harness.spec.mjs:12,94` at `98b6afead8` reads `probe-cloud-planetary.mjs`, first added in 735 (`git ls-tree` verified). Bisect to 734 → ENOENT.
   **Fix:** ledger annotation; instruct Sol to order file additions before references in batch splits.

8. **[MEDIUM]** Node portability: `cloud-density-domain.spec.mjs:30` imports `WebGPUCloudDensityDomain.ts` directly — requires Node type-stripping (default ≥22.18) while `package.json:174` engines is `>=22.0.0`.
   **Fix:** tighten engines or document the runner requirement in the spec header.

9. **[LOW]** `cameraGeodeticHeight` packs `?? 0` when `positionCartographic` is undefined (`WebGPUProceduralCloudRenderer.ts:1617,1723` at 735) — force-classifies the camera below-deck; 2D/CV semantics unconfirmed.

10. **[LOW]** `cloudHighPrecision=false` doc drift: JSDoc/tier-preset comments (`CloudVolumetrics.js:148-155`, `WebGPUCloudTierPresets.ts:222-223`) imply the legacy sphere path is recoverable; post-735 it is one-part f32 WGS84 (sphere removal itself is audit-sanctioned F2.1 remediation).

11. **[LOW]** `QF_PLANET_DENSITY` comment overstates: "separately overridable by the same-build visual authority" (`WebGPUProceduralCloudRenderer.ts:2281-2286`) — the only override is probe-level uniform poking; no user escape hatch analogous to `cloudHighPrecision`. Reword or plumb a `CloudVolumetrics` flag.

12. **[LOW]** Evidence-tooling gaps from 733: bundle-provenance hashes `Build/CesiumUnminified/Cesium.js` while non-production `server.js:410` serves `Build/CesiumDev` under that path (wrong-file attestation risk); six original tour scenes have no `minCloudish` floor (visibility gate degenerates to always-pass); `dirty:false` recorded when git invocation fails.

13. **[NOTES]** Tautological f32 assertion in `cloud-ray-jitter.spec.mjs:186-196` (committed 736 — can never fail); `cloud-probe-harness.spec.mjs:151` mock `Float32Array(160)` matches neither 148 nor 168; `mipDimensions()` condition-less `for(;;)` loops forever on NaN (`cloud-noise-mipmaps.spec.mjs:20-32`); probe error-filter drops SkyAtmosphere-pattern console errors from evidence (`probe-cloud-density-domain.mjs:695-703` — log them as `knownUnrelatedErrors` instead); four defined-but-uncalled WGSL helpers (`ProceduralClouds.wgsl:687,718,1312,1355` — likely Principle-7 scaffolding for C13-05/06; annotate intended consumers); mip-selection rule duplicated between the two shaders instead of living in `CloudDensityDomain.wgsl`; file sizes keep growing (`ProceduralClouds.wgsl` 2,697 ln, renderer 2,857 ln, probe 1,166 ln — queue a decomposition item under Gate C).

**Positives worth recording:** nothing in the new files is orphaned — every module is consumed at verified sites; three specs run real naga WASM validation of the exact composed runtime WGSL; uniform growth is add-only-at-tail, TS↔WGSL matched, 16-byte aligned (168 floats); legacy density path is SHA-256-frozen by spec (`cloud-density-domain.spec.mjs:685-732`); SO(3) rotations verified orthonormal/det+1/TS↔WGSL bit-identical and proven to break the 500-unit alias period; CPU-f64 origin phases with camera-relative f32 deltas strengthen the RTE contract; no `any`, no new console logging, no ShaderDefine bits.

---

## Ledger truthfulness

Overall: **honest, with a bias toward under-claiming.** Specific deviations:

- **B732:** DEFERRED_WORK rewrite claims the original CLOUD-U4 filing is "retained below for trace" — it was replaced by a ~450-char paraphrase (verbatim only in `f4a934e606^`). The C6-CLOUD-STBN rewrite drops the recorded license-clean STBN plan while C13-11 sits BLOCKED on exactly that question. Commit subject also under-describes: it silently retro-closes C9/C10 ledgers and pauses C11 (all edits verified true against git history).
- **B733:** "arm the shared console/uncaptured-error/device-loss gate" is true of WebGPU lanes only; the tour's WebGL lane arms console+pageerror only. Runtime numbers rest on gitignored local artifacts — hedged honestly as characterization.
- **B734:** "all 42 checks plus the WebGPU error gate" double-counts (the gate IS one of the 42); "disabled is exactly black" is observed, not enforced by any check; the 12.1847 ms and 36,113-billboard figures are unverifiable (artifacts overwritten by later in-flight runs).
- **B735:** no overclaim found. All numbers reproduced exactly; scope enforcement spec-pinned.
- **B736:** "20.016 s" not verbatim in any artifact; WebGL-billboard-parity artifact from the 736 window was superseded (later runs reproduce identical deterministic counts, so plausible). Evidence provenance for 735/736 is parent-commit+dirty pre-commit runs — honestly recorded in the manifests, but no artifact binds to the landed hash.
- **In-flight C13-37 row: UNDER-claims** — Slice B is in the tree while the row says it "remains the next bounded slice." Stale, not dishonest; must be corrected pre-landing. Related: Batch 735's committed "exactly 148 floats preserved" is superseded by in-flight growth to 168, and the committed 735 acceptance spec (`cloud-primary-shell.spec.mjs:546-566`) was rewritten by in-flight work — a legitimate add-only evolution, but note that a committed evidence gate was edited mid-flight.
- The one outright false code comment: the `QF_PLANET_DENSITY` "separately overridable" claim (defect 11).

---

## Dirty-tree recommendation

**RETURN TO SOL WITH BRIEF.**

Not LAND (the work's own acceptance oracle is RED and the ledger is stale); not HOLD (the work is well-engineered, rule-compliant in the large, and mid-iteration exactly as its ledger says). The snapshot branch `snap-sol-c13-2026-07-23` stays until landing. **The new specs and especially `probe-cloud-density-domain.mjs` must NOT be executed until the machine-safety finding (defect 1) is addressed** — interim runs only under an external timeout wrapper, serially, never in background.

**Draft brief to Sol:**

> C13-37 review outcome: strong engineering, not accepted yet. Before your next submission:
> 1. **Close the RED gate.** All four characterization runs report `domainNoRegressionPassed:false`; near-horizon new-domain baked periodicity (0.1147) exceeds legacy (0.0953). Iterate the domain until legacy-vs-new no-regression passes in ALL scenes; include manual PNG review per your probe's own limitations block.
> 2. **Add the standard watchdog** to `probe-cloud-density-domain.mjs` (unref'd force-exit timer + try/finally browser close, per `probe-backend-isolation.mjs:34-40`). Retrofit the 733-736 probes in the same tooling touch.
> 3. **Correct the C13-37 ledger row**: Slices A+B and the IBL leg are in-tree; name the actual remaining work.
> 4. **Split the landing**: (batch 1) density domain + mip/LOD; (batch 2) IBL parity + revision invalidation — and restore an epsilon/quantization debounce on continuous IBL inputs before batch 2.
> 5. **Declare `sharp`** as a pinned devDependency (or drop it for the canvas-decode path) and reconcile the README dep policy.
> 6. Small fixes in-batch: `Float32Array(160)`→168 in the harness spec; reword or plumb the `QF_PLANET_DENSITY` override; annotate the four uncalled WGSL helpers with their intended consumers (C13-05/06); guard `mipDimensions()`; tighten engines or document Node ≥22.18 for the TS-importing spec; record filtered console errors as `knownUnrelatedErrors`.
> 7. Constraints re-affirmed: no feature/WebGL path removal, add-only uniform tail, no new ShaderDefine bits, one concern per landing, post-commit re-run of the cheap spec suite so at least one artifact records the landed hash.

---

## Handoff notes for the orchestrator

Verify with Edge **serially** (one probe at a time, foreground, this machine has a probe-crash history), only AFTER watchdogs land:

1. **Node-only spec suites first** (no browser; need Node ≥22.18 or `--experimental-strip-types` for `cloud-density-domain.spec.mjs`): `cloud-density-domain.spec.mjs` (expect SO(3)/scale/SHA-256-frozen-legacy + naga green), `cloud-density-lod.spec.mjs`, `cloud-noise-mipmaps.spec.mjs`, `cloud-ibl-revision.spec.mjs`, `cloud-image-analysis.spec.mjs`, plus the edited committed trio (`cloud-primary-shell` — now asserts 148+20 layout, `cloud-ray-jitter` 6/6 with composed-source naga, `cloud-probe-harness`).
2. **`probe-cloud-density-domain.mjs`** (the C13-37 acceptance authority): gate = `domainNoRegressionPassed:true` in ALL scenes (currently false in all 4 runs), morphology 4/4, `uniformFloatCount===168`, complete mip chains, then read the PNGs yourself per Principle 8.
3. **Regression sweep of the landed batches** at the post-landing hash: `probe-cloud-planetary.mjs` (21/21 checkpoints, min OFF/ON delta ~135k px), `probe-cloud-empty-frustum.mjs` (42/42, 7,584 cells, disabled non-black=0 — consider adding the 43rd `disabledExactlyBlack` check), `probe-cloud-banding.mjs` (high tier 0/786,432 static), `probe-cloud-tour.mjs` (config-truth + WebGL billboard parity 59,911/36,504).
4. **`probe-cloud-perf.mjs`** only with `node server.js --production` (dev-mode serves `Build/CesiumDev` under the hashed path — provenance would attest the wrong bundle).
5. After landing, re-run the cheap spec suite once so at least one artifact records the landed commit hash (every 735/736 artifact currently records parent+dirty).
6. Watch items for later C13 tasks: limb-view interval gap in `marchDeck` above-clouds branch (`ProceduralClouds.wgsl:1467-1479` at 735 — orbit checkpoint does not certify limb; belongs to the march-redesign tasks); the four scaffolded WGSL helpers must find their C13-05/06 consumers; C13-11 unblock path exists at `RESEARCH_REGISTER_2026-07-06.md:11,33-49` despite the B732 doc rewrite; queue a decomposition item (WGSL 2,697 ln / renderer 2,857 ln) before the C13 tail; stray branch `worktree-agent-a58ee463413961f0e` needs an owner or deletion.