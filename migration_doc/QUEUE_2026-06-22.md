# WebGPU Work Queue — 2026-06-22

## Run Progress (updated 2026-06-23)

**Shipped this run (all verified + pushed):** ✅ 355 model direct-light BRDF · ✅ 356 MaterialAppearance czm_phong (19-shader Workflow) · ✅ 357 camera-track orbit→ground global sweep (tooling) · ✅ 358 TAA jitter reaches GPU · ✅ 359 glTF VEC3 COLOR_0 widen (DP-H37) · ✅ NEW-DIRTY-CONSUME-POINT (confirmed already shipped) · ✅ **360 DP-H44 globe terrain picking — OPT-IN `globe.pickable` flag** (probe-globe-pick-h44.mjs PASS; camera-track render regression 1.55% == baseline). **New bugs surfaced + tracked in DEFERRED_WORK:** NEW-WEBGPU-GRID-MATERIAL-PATTERN-MISSING, NEW-WEBGPU-KHR-MATERIALS-UNLIT-BLACK.

**DP-H44 scope correction (2026-06-23):** WebGL `scene.pick` ALSO returns `undefined` over the globe (no globe pick-ID; `updateForPick` re-pushes color commands for pickPosition DEPTH only). So returning the globe is a DIVERGENCE from WebGL — shipped as a default-`false` `Globe.pickable` opt-in (parity preserved by default). See DEFERRED_WORK DP-H44 + WEBGPU_DEBUGGING_LOG Batch 360 (3 non-obvious failure modes documented: central-cache async hang on single-target descriptors → sync create; `updateForPick` empty on WebGPU → pick-frame rebuild; depth-always foreground-occlusion regression → gate globe out of pick pass when `!pickable`).

✅ **361 NEW-WEBGPU-PICK-COLD-SYNC-STALENESS — RESOLVED via docs + guidance** (the "forced sync readback" parity option is INFEASIBLE — WebGPU has no synchronous GPU→CPU readback; `scene.pick` JSDoc now documents the one-frame-stale/cold-first-pick semantics + steers one-off callers to `pickAsync`, and `WebGPUPickFramebuffer.end()` warns once on the cold path; point-pick probe still PASS).

⏸️ **DP-H46 (pickMetadata) — RE-SCOPED + DEFERRED as an epic (2026-06-23).** Not a focused batch: the orchestration + readback + dispatcher routing + decode already exist (backend-agnostic), but the PRODUCER is missing AND blocked on a large prerequisite — the WebGPU model has NO structural-metadata-in-shader pipeline (`MetadataPipelineStage`/`MetadataStageFS.glsl` are GLSL-only; no WGSL `Metadata` struct / property-texture / property-attribute access). Tracked as a NEW-WEBGPU-MODEL-STRUCTURAL-METADATA epic with DP-H46 as its final consumer (see DEFERRED_WORK DP-H46). Multi-day; skip per charter rule 7 (document + defer + move on).

✅ **362 MORPH-EXAG-SKIRTS — SHIPPED** (CV/morph vertical exaggeration now matches WebGL; attribute-based skirt handling = no walls; probe-exaggeration-cv.mjs FLAT→matching relief, 3D didn't-break + camera-track 1.557%==baseline). Surfaced NEW-WEBGPU-EXAG-WATER-STREAKS (pre-existing water/lake color parity gap under exaggeration; baseline-isolated as NOT caused by this fix).

✅ **363 NEW-CLOUD-IMPOSTOR-FS-PARITY — SHIPPED** (volumetric raymarch port of `CloudCollectionFS` into `CLOUD_WGSL`: ellipsoid intersect + Gardner shading + worley-FBM erosion; wired `maximumSize` through the instance buffer stride 56→68; fixed scale/slice defaults. probe-cloud-volumetric-parity.mjs: WebGPU silhouette 282x184→103x78, area ratio vs WebGL 15.5x→2.41x; PNGs READ = smooth blob → lumpy cumulus. Didn't-break: property-edit + collections-regression PASS. Residual NEW-WEBGPU-CLOUD-WORLEY-TEXTURE-PARITY = inline worley vs baked texture, ~1.6x linear + graininess gap.)

✅ **364 NEW-WEBGPU-SKYBOX-HDR-FAINT-STAR-PARITY — SHIPPED** (root-caused the "shared HDR post-process" crush to **always-on auto-exposure**: WebGPU forces `addAutoExposure` + `enabled=true` while WebGL defaults the opt-in flag off, and configure never synced it → the near-black HDR night sky collapsed to pure black. Fix = honor `collection._autoExposureEnabled` so both backends expose identically by default. diag-stars-hdr-autoexposure: AE-on maxLum 0 → AE-off maxLum 761; probe-stars-hdr-autoexposure-parity: WebGPU 231 bright/756 maxLum vs WebGL 282/717 (was 0/0). Didn't-break: disc-size-orbit identical, bloom-parity PASS w/ WebGPU globe px == WebGL. Residual = NEW-WEBGPU-STARFIELD-TUNE catalog-sprite brightness.)

✅ **C-R8-SCENE2D-JITTER — VERIFIED STALE/RESOLVED (2026-06-23, no code).** Queue #9 ("collections invisible in 2D/CV — probe-collections-2dcv-morph all-zero") is obsolete: re-ran the probe and collections now render at WebGL parity in 2D AND CV — billboard 2D 0.99 / CV 0.99 / 3D 1.00, point 2D 0.88 / 3D 0.84, polyline 2D 0.95 / CV 0.87, label 2D 1.33 / CV 1.11. Resolved by the documented Slice 1-4 chain (B261 per-frustum camera-UB resolver → e83ad7524b Slice 2 projected-frame RTE per-slice repack = "THE 2D-clip fix" → B263 coplanar depth → B268/269 globe-pass 2D BV → B275 size parity). The line-4207 "clip position wrong in shifted band" text is a stale Batch-220 investigation snapshot, superseded. **Anomaly noted (NOT a WebGPU defect, NOT C-R8):** CV point GL=0 GPU=231 — WebGL renders 0 yellow points in Columbus View while rendering 284 in 2D / 316 in 3D, so WebGPU (231, consistent across modes) is the more-correct side; flagged below as NEW-WEBGL-CV-POINT-ZERO for a later look.

⏸️ **NEW-WEBGPU-EXAG-WATER-STREAKS — INVESTIGATED + DEFERRED (2026-06-23).** Decisive isolations RULED OUT the two obvious suspects: zeroing `computeEnhancedOcean`'s `oceanContribution` (rebuild) left the blue UNCHANGED → NOT the water-color function; disabling fog + ground-atmosphere + sky-atmosphere (no rebuild, diag-exag-water-streaks-source.mjs) left it UNCHANGED → NOT atmosphere/fog. Remaining cause = globe **lighting/material water-fragment** parity (turquoise lake imagery survives on WebGPU where WebGL's `czm_phong` + `materialInput.waterMask` mutes it). That's a deeper globe-lighting batch (broad blast radius), not a water-color tweak. Pre-existing, non-default (extreme EXAG over glacial-lake terrain), LOW priority. See DEFERRED §NEW-WEBGPU-EXAG-WATER-STREAKS investigation note.

⏸️ **NEW-WEBGPU-GRID-MATERIAL-PATTERN-MISSING (#369) — SYMPTOM NOT REPRODUCED (2026-06-23).** READ probe-matappearance-parity's `matparity-Grid-{webgl,webgpu}.png`: WebGPU renders the grid LINES (matching WebGL on the grazing faces), NOT "solid green." The Batch-356 informational note is stale for that symptom. Real latent nuance (LOW priority): both WGSL grid shaders use `step(uv, lineThickness)` UV-space width vs WebGL's derivative-based constant-PIXEL width — unverifiable at the probe's single fixed view (porting the derivative math into PrimitiveMatGridLit left the probe PNG byte-identical, and that shader isn't even the flat-appearance path). Needs a multi-zoom grid probe to expose + fix. See DEFERRED §NEW-WEBGPU-GRID-MATERIAL-PATTERN-MISSING.

**▶ RESUME POINT — next batch = T3 visual.** Re-verified 2026-06-23: ✅ CSM-CASCADE-GROUND-FIT (B306) + ✅ SPLAT-MULTIFRUSTUM-DEPTH-COMPOSE + ✅ MORPH-EXAG-SKIRTS (B362) + ✅ NEW-CLOUD-IMPOSTOR-FS-PARITY (B363) + ✅ NEW-WEBGPU-SKYBOX-HDR-FAINT-STAR-PARITY (B364) + ✅ C-R8-SCENE2D-JITTER (stale-resolved) + ⏸️ NEW-WEBGPU-EXAG-WATER-STREAKS (investigated+deferred) + ⏸️ NEW-WEBGPU-GRID-MATERIAL-PATTERN-MISSING (symptom not reproduced) DONE. ✅ **365 NEW-WEBGPU-CLOUD-WORLEY-TEXTURE-PARITY — GRAIN SHIPPED** (CPU-baked faithful 3-channel worley atlas + WGSL voxelToUV/trilinear sample at `NOISE_DETAIL*cloudPoint`; SLICE64/DETAIL8 makes W exactly == WebGL's `worleyFBM(cloudPoint mod 8)`; NEAREST sampler; 512 precomputed cell-points → no-trig gen. probe-cloud-volumetric-parity PNGs READ = WebGPU now finely speckled matching WebGL grain; property-edit + collections-regression PASS. **Exposed NEW-WEBGPU-CLOUD-SIZE-PARITY** — the faithful W erodes less than B363's over-eroding inline worley, revealing a pre-existing ~1.9x cloud SIZE gap that is NOT the noise (raymarch/Gardner/TR/color WGSL verified line-by-line identical to WebGL) — a non-shader cause, tracked separately.) **Open follow-up:** NEW-WEBGPU-CLOUD-SIZE-PARITY (M — ~1.9x cloud core size vs WebGL; non-shader root cause).

✅ **366 NEW-WEBGPU-CLOUD-SIZE-PARITY — SHIPPED** (root cause pinned by the backlog-triage workflow: cloud quad VB spans quadPos[-1,1] but VS scaled the FULL quadPos → quad 2x too wide vs WebGL's offset=dir-0.5 half-extent; `vOffset=quadPos*0.5` raymarch coord was already correct. Single `*0.5` on the quad offset in vertexMain+velocity. probe-cloud-volumetric-parity: filled-px 4.3x→1.08x, area 2.41x→0.99x, PNGs READ near-identical; property-edit PASS, collections-regression PASS w/ cloud gate re-baselined 100→50. **Cloud trilogy complete: B363 shape + B365 grain + B366 size = WebGL parity.**)

**▶ NEXT — backlog-triage workflow (wf_0d8d2c85) ranked the remaining T4/T5:** do-now = 370 compute-engine-wiring (S), 373 material-per-backend-source (M), 375 KTX2-IBL (M); quick-win = 371 uniformstate-viewport (S), 378 morph-tangent-deltas (S); epic-split = 379 volumetric-cloud-layers (Perlin-Worley/weather-map fidelity; basic raymarcher already shipped); defer = 374 depthFail, 377 entity-keyframe-bridge, EXAG-WATER-STREAKS; stale-resolved (doc-only) = 380 TEME-star-rotation (B313/314), 381 TAA-order (B290). NOT scoped (schema cap): 372 DP-H47, 376 polyline-appearance — re-run or scope manually. ⏸️ **NEW-WEBGPU-STARFIELD-TUNE (B364 residual) — ASSESSED LOW-VALUE / ACCEPTABLE-AS-IS (2026-06-23):** after B364 the HDR stars are near-parity (probe-stars-hdr-autoexposure-parity: WebGPU bright=231 vs WebGL 282 = 82%, WebGPU maxLum 756 *higher* than WebGL's 717), the sprites were INTENTIONALLY tuned "subtler" in Batch 352, and there is no dedicated DEFERRED entry — a blind brightness re-tune risks undoing that intentional design + regressing the near-parity, for ≤18% bright-px gain. Deferred unless the user wants the sprites brighter. Then T4 infra → T5. **Re-verify each item vs DEFERRED_WORK + git log before starting.**

### Autonomous-run charter (user-approved 2026-06-22 — run end-to-end without stopping except for the listed blockers)

1. **Order:** top-down through the tiers; fully finish each batch (implement → build → probe → READ PNGs → commit + push) before the next.
2. **Stale items:** if already done (verify in code + git log), mark resolved + move on — no stop.
3. **Test assets:** if nothing local triggers the bug, AUTHOR a synthetic asset (node-gen glTF/scene, LIT material) + keep it as a regression fixture.
4. **New bugs:** track in DEFERRED_WORK (Principle 9) + continue — don't rabbit-hole unless it blocks the current fix.
5. **Shared-infra / critical-path fixes:** proceed, but baseline-isolate AND add a "didn't-break-the-existing-scene" probe alongside the feature probe.
6. **Large (L) features:** each as its own focused batch, committed in verifiable increments.
7. **Unverifiable fixes:** document precisely + DEFER (skip to next) — never ship unverified, don't stop the workflow.
8. **ONLY STOP FOR:** a destructive/irreversible action, a true architectural fork with no sensible default, or repeated unexplained failures.
9. **Always:** stay on `main`, push as kurtyoung-dev, never `--no-verify`, baseline-isolate every rendering claim.

---

Prioritized backlog rebuilt 2026-06-22 (post-Batch-354). Source: parallel
extraction of `DEFERRED_WORK.md` + `CAMPAIGN_ROADMAP_2026-06.md` +
`FEATURE_INVENTORY.md` §C/§D (78 raw open items), then verified against the
docs + recent git log and pruned of stale/resolved entries.

**Ordering principle:** correctness bugs that bite real/default usage →
picking (functional API holes) → visual-parity holes → infrastructure
leverage → feature gaps + high-value visual. Tiers are a recommendation,
not a constraint. Size: S/M/L.

> Re-verify an item's status against `DEFERRED_WORK.md` before starting it —
> this queue is a snapshot and the tracking docs drift as batches land.

## Tier 1 — Correctness bugs that bite real/default usage

1. ~~**NEW-MODEL-DIRECT-BRDF-PARITY** (M) — glTF models mis-shaded vs WebGL (missing Smith-joint visibility + f90 in the direct-light BRDF).~~ ✅ **SHIPPED Batch 355** (Smith-joint `smithVisibilityGGX` + f90 `fresnelSchlick2` + dropped double-metallic on sun + point/spot paths; parity −3.71%→−2.76%, KHR gate unregressed 1.32%).
2. ~~**NEW-MATAPPEARANCE-DIFFUSE-PARITY** (M) — lit MaterialAppearance primitives render darker than WebGL `czm_phong` (ad-hoc Blinn-Phong term in the `*Lit.wgsl` family; surfaced Batch 326).~~ ✅ **SHIPPED Batch 356** (all 19 `PrimitiveMat*Lit.wgsl` → czm_phong: ambient 0.5 + fixed eye-space diffuse; Color parity −54%→+1.35%, all 19 compile clean. Grid-pattern-not-rendering surfaced as a separate bug — see below.) **← NEXT UP: Tier-1 #3 NEW-TAA-PROJECTION-JITTER-OVERWRITTEN**
   - **SCOPED 2026-06-22 (not yet implemented).** Reference: WebGL `Builtin/Functions/phong.glsl` `czm_phong` — diffuse comes from **fixed eye-space directions** `max(dot(N,(0,0,1)),0) + max(dot(N,(0,1,0)),0)` (top-down always + horizon in 3D), NOT the sun direction (the sun dir only drives specular); ambient = `material.diffuse * 0.5`; net `color = 0.5·diffuse·(1+lambert) + specular`. The 19 `Shaders/WebGPU/Primitive/PrimitiveMat*Lit.wgsl` shaders instead use scalar `ambient=0.15` + `diffuse = NdotL*0.7` (actual sun dir) → too-dark ambient floor + wrong diffuse direction. NB the WGSL `worldNormal` is already EYE-SPACE (vertex writes it via `normalMatrix`), so the fixed-dir dots apply directly.
   - **Per-file fix:** `ambient = 0.15` → `0.5`; `diffuse = NdotL*0.7` → `0.5*(max(dot(normal,vec3(0,0,1)),0)+max(dot(normal,vec3(0,1,0)),0))`; keep specular + shadow/CSM/clustered/atmosphere wiring + `lighting = ambient + direct` (= `0.5 + 0.5·lambert + spec`).
   - **Caveats:** the lighting blocks VARY across the 19 (only 7 share the exact `let diffuse = NdotL * 0.7;` string — needs per-file reads, not a blind replace). EXCLUDE `PrimitivePBR{Simple,Textured}.wgsl` (real PBR) + `PrimitivePickMatLit.wgsl` (no lighting). **No parity probe exists** — `probe-all-materials.mjs` only checks no-device-errors + UB layout, not brightness; must BUILD a new WebGL-vs-WebGPU material-brightness probe (lit MaterialAppearance polygon on both backends, diff mean luminance over the primitive pixels) and READ the PNGs.
3. **NEW-TAA-PROJECTION-JITTER-OVERWRITTEN** (M) — per-frame `_updateFrustumUniforms` recompute clobbers the TAA sub-pixel jitter before any draw → static-edge AA never reaches the GPU.
4. **DP-H37** (S) — glTF `COLOR_0` as VEC3 declared `float32x4` → shader reads next vertex's `.r` as alpha → flickery alpha tracking vertex order. *(quick win)*
5. ~~**NEW-DIRTY-CONSUME-POINT** (S) — `PointPrimitiveCollection` never calls `_consumeDirtyState()` → rebuild gate fires every frame.~~ ✅ **ALREADY SHIPPED** (verified 2026-06-22 — `WebGPUPointPrimitiveRenderer` uses `syncInstancesAndConsume` + `_consumeDirtyState`; landed with NEW-COLLECTION-RENDERER-BASE).

## Tier 2 — Picking (functional API holes)

6. **DP-H44** (M) — globe surface emits no pick-ID on WebGPU → terrain invisible to `scene.pick`.
7. **NEW-WEBGPU-PICK-COLD-SYNC-STALENESS** (M) — first synchronous `scene.pick()` against a fresh pick FBO returns undefined for any primitive (async readback is one frame stale; surfaced Batch 323, needs design).
8. **DP-H46** (M) — `scene.pickMetadata` entirely unwired (`frameState.pickingMetadata` never consumed) → always null.

## Tier 3 — Visual-parity holes (user-visible)

9. ~~**C-R8-SCENE2D-JITTER** (L) — collections invisible in 2D / Columbus View.~~ ✅ **VERIFIED STALE/RESOLVED (2026-06-23)** — `probe-collections-2dcv-morph` is no longer all-zero: billboard 2D 0.99 / CV 0.99, point/polyline/label all render. Fixed by the Slice 1-4 chain (B261 per-frustum camera-UB → e83ad7524b projected-frame RTE per-slice repack → B263 coplanar depth → B268/269 globe-pass 2D BV → B275 size). See resume-point note above + DEFERRED §collections.
10. **NEW-CSM-CASCADE-GROUND-FIT** (M) — coarse sawtooth globe shadow edge vs WebGL's tight-fit map; clamp cascade far at the ground before `_fitBoundingSphere`.
11. **NEW-SPLAT-MULTIFRUSTUM-DEPTH-COMPOSE** (M) — single-pack full-frustum Gaussian-splat command occluded by opaque geometry in multi-frustum scenes (UBO not re-packed per slice).
12. **MORPH-EXAG-SKIRTS** (M) — vertical exaggeration dropped during MORPHING/COLUMBUS_VIEW (gated SCENE3D-only); needs WebGL-faithful skirt handling for the planar legs.
13. **NEW-CLOUD-IMPOSTOR-FS-PARITY** (M) — WebGPU cloud FS is a 2D-noise impostor vs WebGL ellipsoid raymarch (~4–5× per-cloud divergence; `maximumSize`/`slice` ignored).
14. **NEW-WEBGPU-SKYBOX-HDR-FAINT-STAR-PARITY** (M) — under HDR+bloom the whole WebGPU night sky is far dimmer than WebGL. **Root cause corrected 2026-06-22: the shared HDR tonemap/auto-exposure/bloom path, NOT the cubemap decode.** HDR-only (SDR byte-identical). Needs broad HDR regression coverage (model + globe), not a star-renderer tweak.

## Tier 4 — Infrastructure leverage (unblocks future work)

15. **NEW-WEBGPU-COMPUTE-ENGINE-WIRING** (M) — `WebGPUContext` never instantiates a `WebGPUComputeEngine`, so general compute (atmosphere-LUT consumption, frustum cull, point-cloud sort/LOD, GPU sort keys, Hi-Z, polygon SDF) is a runtime no-op. (NB: the dynamic env-map runs its OWN `beginComputePass`, so SH IBL etc. are unaffected — this gates the LUT/cull/sort consumers.)
16. **NEW-WEBGPU-UNIFORMSTATE-VIEWPORT** (M) — `UniformState.viewport` only seeded by the WebGL path → `viewportOrthographic`/`viewportTransformation` stay identity on WebGPU; every screen-space WGSL shader hand-builds them.
17. **DP-H47** (L) — no shared `csm_atmosphere*` auto-uniform suite; SkyAtmosphere/Clouds/GlobeGroundAtmosphere/ModelAtmosphere each hand-roll their `atmosphericConditions` pull → a custom `scene.atmosphere` param applies in one renderer and is silently defaulted in another.
18. **NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE** (M) — `ViewportQuad` + Material-driven primitives still branch on `context.isWebGPU` for WGSL-vs-GLSL; needs a per-backend `getShaderSource()`/`getUniformMap()` virtual (last sanctioned backend branch in scene logic).

## Tier 5 — Feature gaps + high-value visual

19. **DP-H18** (M) — `depthFailAppearance` entirely unimplemented on WebGPU (no twin shader/pipeline/uniforms) → see-through highlighting of occluded primitives broken.
20. **NEW-MODEL-IBL-KTX2-CUBEMAP-WEBGPU** (M) — authored KTX2 specular env maps don't load on the WebGPU context → models fall back to procedural IBL.
21. **NEW-POLYLINE-APPEARANCE-PRIMITIVE-WEBGPU** (M) — color + material slices shipped (Batches 343/344); picking, 2D/CV/morph, effects/log-depth, and textured polyline materials still open.
22. **NEW-ENTITY-GPU-KEYFRAME-KERNEL** (L) — CZML `SampledPositionProperty` entities still use the per-frame CPU interpolation lane; the auto Entity→GPU-keyframe-buffer bridge over the shipped `SampledPositionKernel` substrate is unbuilt.
23. **NEW-MODEL-MORPH-TANGENT-DELTAS** (S) — WebGPU morph interleaves only position+normal deltas; a normal-mapped morphed model keeps its rest-pose tangent frame → tangent-space normal drifts as the mesh deforms.
24. **NEW-VOLUMETRIC-CLOUD-LAYERS** (L) — raymarched volumetric clouds (Perlin-Worley + weather map + Beer-powder + dual-lobe HG + multi-scatter) — Track V headline.
25. **DP-H48** (S) — `temeToPseudoFixed` never reaches a shader → star-field/skybox uses the equinox-aligned basis, stars don't rotate with the Earth day-cycle (status unverified).

## Honorable mentions (just outside the top 25, still tracked)

NEW-TAA-PIPELINE-ORDER-RECONCILE · NEW-SUN-MOON-FIDELITY · NEW-ENTITYCLUSTER-GPU-MERGE (full GPU union-find merge) · NEW-ATMOSPHERE-LUT-SUN-RELATIVE · MORPH-MODEL-PROJECT2D · MORPH-PICK · NEW-ENTITY-BULK-CZML-HINT · NEW-TS-CONVERT-JS-RENDERERS (Model 3802 LOC + bulk renderers) · FORK-41 (Hi-Z command-drop) · NEW-GPU-SORT-PIPELINE-PHASE-3 · NEW-LOG-DEPTH-REMAINING-PRODUCERS-POINTCLOUD-SPLAT · NEW-DYNAMIC-ENVMAP-FULL-SCENE · NEW-MODEL-WGSL-CUSTOM-SHADER · NEW-VR2-6-ATMOSPHERE-HALO-ORBIT (verify status) · NEW-LOG-DEPTH-REMAINING-CONSUMERS · cosmetics (NEW-GROUND-ATMOSPHERE-DRAPE-LIMB-WIDTH, NEW-ATMO-AERIAL-PERSPECTIVE-NEARFIELD-TUNE, NEW-BUFFERPOINT-WEBGL-SINGLE-VERTEX-DROPOUT).

## Pruned from the raw extraction as stale / resolved

- **C-R1-CLASSIFICATION / C-R1-TILE-BATCH / C-R1-GLOBE-RENDERSTATE** — struck-through `~~RESOLVED~~` in the doc headers (mis-extracted).
- **NEW-VR2-1-LOW-ALT-TERRAIN-LOAD** — `FIXED 2026-05-13` (Batch 41).
- **NEW-WEBGPU-POINT-COLLECTION-PICK** — ✅ SHIPPED Batch 323 (was going to rank #1 — verification caught it).
- **NEW-COLLECTION-RENDERER-BASE** — Label fold finished Batch 332 (5/5 collections).
- **April-2026 audit IDs** (`OPEN-1-DIAGNOSE`, `Phase-8a`, `FEAT-3DT2-02/03/05`, `WGF-1-EXPAND`, `WGF-1-INTERSECTION`, `FEAT-GAP-09`, `BACKLOG-§Material UBO`, `OPTION-B-BILLBOARD`, `DP-H19-SHADER-DECODE-RUNTIME`) — pre-date ~150 batches; almost certainly resolved. Excluded pending a status check, not queued.

## 25-Batch Execution Plan (Batches 357–381)

Sequenced run of the 23 remaining queue items + the grid-pattern bug found in Batch 356 + one honorable-mention backfill. Execution model: **one batch at a time** — implement → `gulp build` → Playwright probe → READ PNGs → commit/push. Use the **Workflow tool _within_ a batch** when it parallelizes (▶); otherwise solo.

Done so far: ✅ Batch 355 (#1 BRDF), ✅ Batch 356 (#2 MaterialAppearance), ✅ **Batch 357 (TOOLING, inserted first per request) — `probe-camera-track.mjs` extended to a full orbit→ground global sweep**: 9 waypoints from deep orbit (18,000 km whole-Earth disc) through continental/regional/city/near-ground down to LITERAL GROUND LEVEL (~300 m street-level SF) + rotation to the far side of the globe (Himalaya), dual-backend (WebGPU+WebGL) screenshots + per-waypoint pixel diff at every distance band. Verified: 9/9 captured both backends, 0 errors, mean diff 1.52% / worst 3.64% (ground-level), PNGs read. **Use this harness to spot-check every rendering batch below.**

> Batch numbers below are a sequence guide; actual git batch numbers continue from **358** (TAA is the next code batch).

**Tier 1 — correctness (do first)**

- ~~**357** — NEW-TAA-PROJECTION-JITTER-OVERWRITTEN (M) — frustum recompute clobbers TAA sub-pixel jitter.~~ ✅ **SHIPPED Batch 358** (re-apply jitter in `_updateFrustumUniforms` before `updateFrustum`; baseline-isolated probe 0→0.000828, gated zero-op when off). **SCOPED 2026-06-22:** jitter IS applied in `Scene.js:5429` (`taa.applyProjectionJitter` → `proj[8/9] = base + jitter`), but the WebGPU frustum loop's `WebGPUSceneRenderer._updateFrustumUniforms` (line 1437) calls `uniformState.updateFrustum(frustum)` which RECOMPUTES a fresh un-jittered projection PER FRUSTUM (it even NaN-invalidates the cached proj for the depth-range fix), so the GPU camera UB never sees the jitter → TAA's static-edge AA half is dead. **Fix:** after each `uniformState.updateFrustum(frustum)` in `_updateFrustumUniforms`, re-apply `taa.jitterX/jitterY` to uniformState's projection AND invalidate/re-derive the cached `viewProjection*` matrices the camera UB packs (not a one-line poke — UniformState caches derived matrices off the projection). Reach the TAA effect via `scene._alternateSceneRenderer?._postProcess?.taaEffect` (same path Scene.js uses), gated on `scene.taaEnabled`. **Verify:** needs a NEW TAA-edge probe — capture a high-contrast static edge over N settled frames with TAA on vs off (jitter on → sub-pixel edge softening/accumulation; off → aliased), assert WebGPU shows the AA and matches WebGL's TAA edge profile. Cross-ref NEW-CAMERA-JITTER-ACCUMULATION (the `applyProjectionJitter` base/re-capture machinery already exists).
- ~~**358** — DP-H37 (S) — glTF COLOR_0 VEC3 read as float32x4 → flickery alpha.~~ ✅ **SHIPPED Batch 359** (widen VEC3→RGBA via expandColorsToRGBA; baseline-isolated 77.95%→5.43% on a synthetic VEC3 asset). Surfaced separately: NEW-WEBGPU-KHR-MATERIALS-UNLIT-BLACK.
- ~~**359** — NEW-DIRTY-CONSUME-POINT (S) — PointPrimitiveCollection rebuild gate never settles.~~ ✅ **ALREADY SHIPPED** (verified — uses `syncInstancesAndConsume` + `_consumeDirtyState`).

**Tier 2 — picking**

- **360** — DP-H44 (M) — globe surface emits no pick-ID → terrain invisible to scene.pick.
- **361** — NEW-WEBGPU-PICK-COLD-SYNC-STALENESS (M) — first sync pick returns undefined.
- **362** — DP-H46 (M) — scene.pickMetadata unwired → always null.

**Tier 3 — visual parity**

- **363** — C-R8-SCENE2D-JITTER (L) — collections invisible in 2D / Columbus View.
- **364** — NEW-CSM-CASCADE-GROUND-FIT (M) — sawtooth globe shadow edge.
- **365** — NEW-SPLAT-MULTIFRUSTUM-DEPTH-COMPOSE (M) — Gaussian splats occluded by globe.
- **366** — MORPH-EXAG-SKIRTS (M) — vertical exaggeration dropped in 2D/morph.
- **367** — NEW-CLOUD-IMPOSTOR-FS-PARITY (M) — cloud impostor vs WebGL volume raymarch.
- **368** — NEW-WEBGPU-SKYBOX-HDR-FAINT-STAR-PARITY (M) — HDR night sky too dim (shared tonemap/bloom path).
- **369** — NEW-WEBGPU-GRID-MATERIAL-PATTERN-MISSING (S) — grid lines don't render on WebGPU ▶ *(may expand to Stripe/Checker/Dot — Workflow if so)*.

**Tier 4 — infrastructure leverage**

- **370** — NEW-WEBGPU-COMPUTE-ENGINE-WIRING (M) — dormant compute engine (LUT/cull/sort consumers).

> **DP-H44 globe terrain picking — FULLY SCOPED 2026-06-22 (execution-ready, additive + isolated so low risk to existing globe color rendering):**
> The pick-pass infra already dispatches the GLOBE pass + routes through `selectCommandVariant(isPickPass=true)`, which renders `command.derivedCommands.picking.pickCommand` (attached via `attachPickToColorCommand`). The globe just has no pick variant. Mirror the EllipsoidPrimitive pattern (`WebGPUEllipsoidPrimitiveRenderer.ts:1160-1183`):
> 1. **Pick color uniform (camera UB tail — additive, no offset shift):** add `pickColor: vec4<f32>` after `logDepth` in `GlobeTerrain.wgsl` `CameraUniforms`; bump `CAMERA_UNIFORM_FLOATS` 144→148 in `WebGPUGlobeSurfaceTypes.ts`; pack it after the logDepth write in `WebGPUGlobeSurfaceCameraUB.ts` (the buffer growing 16B is inert for the other globe shaders — WGSL allows a buffer larger than the struct, so wireframe/debug structs need NOT change).
> 2. **Pick fragment:** add `@fragment fn fragmentPickMain(...) -> @location(0) vec4<f32> { return camera.pickColor; }` to `GlobeTerrain.wgsl` (same VertexOutput; single target).
> 3. **Pick-ID:** once per globe, `context.createPickId({ primitive: <globe/tileProvider> })` → `pickId.color`; write it into the camera UB `pickColor` slot.
> 4. **Pick pipeline:** `buildPickPipelineDescriptor(globeColorDescriptor, "fragmentPickMain", {...})` (the color descriptor is the `gpuDesc` built near `WebGPUGlobeSurfaceRenderer.ts:426`; the helper strips blend + the rgba16float G-buffer target → single-target pick pass).
> 5. **Per-tile pick command:** in `createTileCommands` (line 545), for each tile build a `WebGPUDrawCommand({ pipeline: pickPipeline, bindGroups: <same>, vertexBuffers/indexBuffer: <tile's>, pass: GLOBE/OPAQUE, pickOnly: true })` and `attachPickToColorCommand(tileColorCommand, tilePickCommand)`.
> **Verify:** new probe — `scene.pick` over terrain (globe shown, primitives hidden) returns the globe (not undefined) on WebGPU; AND the camera-track / globe probe is UNCHANGED (the camera-UB tail-append + new pick entry must not alter color rendering). READ the globe render to confirm no regression.
- **371** — NEW-WEBGPU-UNIFORMSTATE-VIEWPORT (M) — viewport matrices identity on WebGPU.
- **372** — DP-H47 (L) — shared csm_atmosphere* auto-uniform suite ▶ *(multi-renderer migration — Workflow)*.
- **373** — NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE (M) — last isWebGPU branch in scene logic.

**Tier 5 — feature gaps + high-value visual**

- **374** — DP-H18 (M) — depthFailAppearance unimplemented.
- **375** — NEW-MODEL-IBL-KTX2-CUBEMAP-WEBGPU (M) — authored KTX2 env maps don't load.
- **376** — NEW-POLYLINE-APPEARANCE-PRIMITIVE-WEBGPU (M) — pick + 2D/CV + textured polyline follow-ups.
- **377** — NEW-ENTITY-GPU-KEYFRAME-KERNEL (L) — CZML SampledPosition → GPU keyframe bridge.
- **378** — NEW-MODEL-MORPH-TANGENT-DELTAS (S) — morph tangent deltas for normal-mapped morphed models. *(quick)*
- **379** — NEW-VOLUMETRIC-CLOUD-LAYERS (L) — raymarched volumetric clouds (Track V headline).
- **380** — DP-H48 (S) — TEME stars don't rotate with Earth day-cycle.
- **381** — NEW-TAA-PIPELINE-ORDER-RECONCILE (M) — TAA pre/post-tonemap order + clamp retune (backfill).

Re-verify each item's status against DEFERRED_WORK before starting (docs drift). Re-prioritize freely — tiers are a recommendation.
