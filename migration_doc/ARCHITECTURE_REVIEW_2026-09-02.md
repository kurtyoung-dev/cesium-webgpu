# Architecture Review — 2026-09-02

**Status:** live. **Date:** 2026-09-02 (written 2026-09-03). **Compiler:** Earendil.
**Tree the findings were re-derived against:** `main` @ `532463ae35` (Batch 1390), working tree as found.

## 1. Purpose and the standing rule

This file is the **architecture-review authority** for the CesiumJS WebGPU fork. It carries the live findings of ten retired reviews, re-derived at today's code by a second reader, in the corrected wording, with the current `file:line` and the queue row that owns each one.

Five authorities are unchanged and must be **linked, never duplicated**:

| Authority | Owns |
| --- | --- |
| [`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md`](FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md) | The **remediation** authority — FAR packages, waves, gates, ADR-1..ADR-8 |
| [`FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json`](FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json) | FAR package state (`findings{}`, `state`, `gate`) |
| [`ARCHITECTURE.md`](ARCHITECTURE.md) | The **reference** — how the fork is built, layer by layer |
| [`PICKING_ARCHITECTURE_STATE_2026-08-17.md`](PICKING_ARCHITECTURE_STATE_2026-08-17.md) | Picking state (the `LD-` ids) |
| [`CLOUD_ARCHITECTURE_AUDIT_2026-07-23.md`](CLOUD_ARCHITECTURE_AUDIT_2026-07-23.md) | Planetary-cloud correctness, temporal, weather, quality, performance |

**The standing rule.** This file states _what is true about the architecture and where_. It does not schedule work, does not carry status, and does not override a queue row. The campaign queues, [`DEFERRED_WORK.md`](DEFERRED_WORK.md) and [`FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md`](FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md) remain the sole status authorities for every id they own. Where a finding below says **"unowned"**, that is itself the finding: nothing in the tracked tree will schedule it until a row exists.

**Evidence bank.** Every claim traces to [`audits/2026-09-02_old_review_validity/`](audits/2026-09-02_old_review_validity/) — ten `*.verified.md` second-reader adjudications, `SUMMARY.md`, `INBOUND_REFERENCES.md`, and the ten `*.judgement.json` first-reader files. **Take text from the `.verified.md` files, never from the `.judgement.json` files:** 57 of 236 open items needed correction, so the raw judgements carry 57 known-wrong premises. The judgement files are retained permanently because they are the only surviving record of the 383 closed items no second reader re-derived (§7).

## 2. Supersession table

Nothing was deleted. The seven documents below are **archived**, not removed: their full text stays readable at the archive path. Three more came back KEEP-PART from their verifier and **stay live where they are**; they are listed under the main table.

Archive path convention: the repository's existing `migration_doc/archive/` layout — root-level documents move to `archive/<basename>`, the 2026-04-30 trio to `archive/audits-2026-04-30/`, and the renderer-deep review joins its two companion pillars in `archive/principal-review-2026-04-16/`. **The move landed 2026-09-03**; the moved files are byte-identical apart from relative links re-resolved for their new depth, so nothing inside them was rewritten. The `README.md` ARCHIVED table and this section are the pointers back.

| Old path | Archive path | What it was | Items | Live | Where the live items are now |
| --- | --- | --- | ---: | ---: | --- |
| `migration_doc/AUDIT_2026_05_02.md` | `archive/AUDIT_2026_05_02.md` | Cross-coupling audit at Batch ~129-159 (397 lines); **id-definition source** for `A.n`/`B.n`/`C.n`/`D.n` | 60 | 4 | §3.1 (C.6), §3.7 (B.11), §3.14 (D.3, D.6); id family in §5.2 |
| `migration_doc/audits/2026-04-30_ARCHITECTURE_PERFORMANCE.md` | `archive/audits-2026-04-30/2026-04-30_ARCHITECTURE_PERFORMANCE.md` | Architecture + performance leg of the 2026-04-30 trio (230 lines); defines no ids | 48 | 16 | §3.8 (A2c-4), §3.9 (A2a-2, A2a-3, A2a-6, A2a-7, A2b-3), §3.10 (D1, D3, A1a-3, A1b-4, R1, R2, R4, R7, R9), §3.13 (A1c-1) |
| `migration_doc/audits/2026-04-30_FORK_FEATURE_INVENTORY.md` | `archive/audits-2026-04-30/2026-04-30_FORK_FEATURE_INVENTORY.md` | Fork feature-maturity snapshot at Batch 116 (283 lines); defines no ids | 58 | 11 | §3.1 (R1), §3.2 (R14, R16, R17), §3.3 (R2), §3.8 (R8), §3.9 (I4, X1), §3.10 (D4, D5, B1) |
| `migration_doc/audits/2026-04-30_MAINTAINABILITY_SURVIVABILITY.md` | `archive/audits-2026-04-30/2026-04-30_MAINTAINABILITY_SURVIVABILITY.md` | Maintainability + survivability leg of the trio (202 lines); the target of `ARCHITECTURE.md:969-974` | 54 | 32 | **§3.11 in full** (F1-F9, E1, E2, P1-P5, X2-X6, T10/N8 and their top-10 / near-term duplicates), §3.10 (C4), §3.13 (R3/N11, R4, R5, C1, C2) |
| `migration_doc/PRINCIPAL_ENGINEER_REVIEW_RENDERER_DEEP_2026_04_16.md` | `archive/principal-review-2026-04-16/PRINCIPAL_ENGINEER_REVIEW_RENDERER_DEEP_2026_04_16.md` | Renderer-deep principal review (503 lines); **id-definition source** for `C-R*`/`H-R*`/`M-R*`/`L-R*` | 50 | 12 | §3.3 (C-R4, M-R13), §3.5 (M-R5), §3.7 (H-R2), §3.9 (H-R8, M-R2, M-R3), §3.10 (H-R6, L-R1, M-R10, M-R11); **id table in §5.1** |
| `migration_doc/LOCAL_CHANGE_AUDIT_2026-07-31.md` | `archive/LOCAL_CHANGE_AUDIT_2026-07-31.md` | Local-change audit of the C11 high-value window (372 lines); defines no ids | 56 | 10 | §3.4 (S4-port-FAR-generic-primitive), §3.7 (S6.2), §3.9 (S6.3, S6.5, S8.3 ×2, S9-3, S9-4), §3.10 (S6.6), §3.11 (S6.1) |
| `migration_doc/FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.md` | `archive/FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.md` | Fork-performance authority (530 lines); the `F5-`/`ACTION-`/`REJECT-`/`GATE-` register + eleven principles | 64 | 38 | §3.1 (F5-02/03/09, ACTION-4/5/7, REJECT-1, GATE-1), §3.8 (F5-10, ACTION-13), §3.9 (F5-18/19, ACTION-1/2/3/8/10, REJECT-2/7), §3.10 (F5-05/06/11, ACTION-12/14, REJECT-3/6/8), §3.11 (F5-08, ACTION-11, GATE-3), §3.13 (F5-12/15, ACTION-15, REJECT-5, DOC-7-REQ-1/4), **§3.15 (F5-PRINCIPLES, GATE-4)**; id set in §5.2 |

**KEEP-PART — these three stay live at their current paths.** Each becomes archivable once the one named block is carried across; until then it is the sole holder of that block.

| Path (unchanged) | What must survive it before it can be archived |
| --- | --- |
| `migration_doc/PRINCIPAL_ENGINEER_REVIEW_PER_FEATURE_2026_04_16.md` | The `B-<n>` / `C-P<n>` / `H-P<n>` / `M-P<n>` / `L-P<n>` id-to-title set (B-1..B-9, C-P1..C-P18, H-P1..H-P22, M-P1..M-P12, L-P1..L-P6). `ISSUES_AND_FIXED_BUGS.md:26` and `:346` name it the definition source for the first three families; **`M-P*` and `L-P*` have no taxonomy row anywhere else**, and `archive/REVIEW_FIX_PROGRESS.md` alone carries ~49 such mentions. Its 16 live items are migrated in §3; the id-to-title index is not. Secondary anchor: `ARCHITECTURE.md:412-414` cites `H-P7` (§3.12), the only live pointer to that defect. |
| `migration_doc/FORK_VS_UPSTREAM_WEBGPU_ARCHITECTURE_AUDIT_2026-07-13.md` | Audit lines 565-588, "Allocation ownership and dual-renderer investigation" → "Maximum safe sharing boundary" — the scope-by-scope sharing table that ADR-1 / FAR-206 / FAR-207 are measured against. **No judgement item covers it.** Its two re-verified claims are carried in §3.11; the table itself is not. |
| `migration_doc/audits/2026-06-11_ULTRA_REVIEW.md` (+ `_findings.json`) | The `A<n>.<m>` / `B<n>` anchor index — the §2/§3 finding tables plus the sidecar's id-to-finding mapping — for the **~175 closed** findings. `ISSUES_AND_FIXED_BUGS.md:23-24` names "the ultra-review doc + `_findings.json` sidecar" the authoritative definition source; `:76-78` defers its open MEDIUM/LOW rows to it without reproducing them; `DEFERRED_WORK.md:4948` defers five open `NEW-*` ids to it. The 60 open findings are migrated in §3; the closed-id index is not. |

**Inbound references.** [`audits/2026-09-02_old_review_validity/INBOUND_REFERENCES.md`](audits/2026-09-02_old_review_validity/INBOUND_REFERENCES.md) §1 is the merged repoint table for all citing files. Three references are load-bearing — `WEBGPU_DEBUGGING_LOG.md:16478` (a Files section that exists only by reference), `ARCHITECTURE.md:969-974` (the only live pointer that sends a reader into a retired file _for content_ — it lands on §3.11 here), and the two runtime warning strings citing the bare id `A.4` at `WebGPUVector3DTilePolylinesRenderer.js:955` and `WebGPUVector3DTileClampedPolylinesRenderer.js:1142`, which must be repointed at `NEW-CLASSIFIER-2D-CV-MORPH`. One inbound reference the first reader recorded, `CAMPAIGN_STATE.md:38`, **does not exist**.

## 3. Migrated findings, by subsystem

Every **CONFIRMED** and **CORRECTED** item from the ten verified banks, in the corrected wording, grouped by subsystem rather than by source document. 231 items: 174 CONFIRMED (still true as first judged) and 57 CORRECTED (real, but the first reader's scope, owner, severity or number was wrong).

Column key. **id** carries its source register prefix, so an id resolves through §5 and through the bank. **Verdict** is the second reader's. **Owner** is a queue/ledger row verified present on 2026-09-03; **unowned** means nothing in the tracked tree schedules it. **Source** names the verified bank file the text came from.

Five items were **REFUTED** and must not be migrated; two more were corrected to closed. They are recorded once in §3.16 so nobody re-derives them a third time.

### 3.1 Globe, terrain & imagery

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `C.6` | `WebGPUVideoTextureManager`'s `texture_external` path has no consumer. The module has zero consumers outside its own file; its header names the live alternative (`WebGPUImageUpload` per-frame `copyExternalImageToTexture`). **Caution for the reader:** the fork-vs-upstream `ADOPT-MATRIX` row calls video "RESOLVED" — true of the manager's internals, false of the gap. `G5-tiles-model-parity.md:802-804` lists the file in the Principle-7 do-not-delete set, so "schedule its removal" is not a free option; consume-or-delete needs an explicit decision either way. | CONFIRMED | `Renderer/WebGPU/WebGPUVideoTextureManager.ts:1-42` (self-declared ORPHANED docstring; `importExternalTexture` at `:171`, `:201`); `FEATURE_INVENTORY.md:567` tags it ORPHANED | **unowned** — dossier only at `campaign11_planning/guides/G5-tiles-model-parity.md:758-772` |
| `R1` (feature-inventory) | Globe renderer size **and** the FEAT-GAP-09 LUT rollout count. `WebGPUGlobeSurfaceRenderer.ts` is 3,080 lines (not 3,933) and a DX row **does** cover it. Separately, 34 of 60 `Shaders/WebGPU/Primitive/*.wgsl` live-consume the atmosphere LUT (branching on `effects.atmosphereLutControl.x > 0.5`) against the inventory's "12 of ~44 … ~32 remaining", which still lists shaders that are wired today. The genuine remainder is 20 files: 7 `Polyline*`, `PrimitiveDepthFailColor`, and 12 `Mat*Lit` variants; the 6 `PrimitivePick*` shaders are excluded by design. Refresh the inventory to 34/60 and decide whether the `Mat*Lit` tail is an accepted end state. | CORRECTED | `WebGPUGlobeSurfaceRenderer.ts` = 3,080 lines; `FEATURE_INVENTORY.md:932`, `:1082` carry the stale count | `DX-10` (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:952`, HELD, names "globe surface after the `Q120` owner") |
| `H17` | Globe material data is repacked and uploaded **per visible tile**, not once per frame per material type — see §4 R3, this is a reversal of a credited fix. `_getOrCreateMaterialPipeline` is invoked inside `createTileCommands`, which Scene calls once per tile; each invocation runs `packMaterialUBO` (a fresh `ArrayBuffer` + `Float32Array`) and `device.queue.writeBuffer`. Only the pipeline and shader-module cache entry is per `material.type`. Thousands of redundant allocations and queue writes per frame on a material-enabled globe. **Scope note:** inert unless `globe.material` is set, which is not the default. | CORRECTED (worse than judged) | `WebGPUGlobeSurfaceRenderer.ts:522` (defn), `:1625` (call), `:1099` (`createTileCommands`), `:650-654` (pack + write), `:543-588` (per-type cache); `Scene/GlobeSurfaceTileProviderRendering.js:1356`; `WebGPUGlobeMaterial.ts:396-397` | FAR-303 |
| `HP-05` | Persistent resource realization is reachable **during draw-command emission**: the globe calls `getOrCreateTileBuffersHelper` from command construction, and a miss destroys the stale entry and creates/uploads new vertex and index buffers (up-converting Uint8 indices) inside the emit path. | CONFIRMED | `WebGPUGlobeSurfaceRenderer.ts:1317`, `:2530`, `:2716`; `WebGPUGlobeSurfaceTileBuffers.ts:177-235` | FAR-209 / FAR-210 (rows live in the **closed** CAMPAIGN8/9 queues — re-home before quoting as ownership) |
| `HP-07` | Imagery reprojection and mip generation each fragment the frame's submissions with their own `device.queue.submit([encoder.finish()])`. | CONFIRMED | `WebGPUImageryReprojection.ts:245`; `WebGPUMipmapGenerator.ts:439` | FAR-200 S2 (C9 item 45) + FAR-402 (**no row**) |
| `H24` | Device pooling does not yet imply safe resource sharing — **corrected and narrowed.** Drop the sub-claim that `model._webgpuCache` can serve the wrong device: `disposeStaleWebGPUModelCache` drains any cache whose `device` or `resourceGeneration` differs and installs an `onDeviceInvalidated` subscription. What remains true: terrain residency carries **no provider or content-hash identity** — only tile coordinates plus a `sourceVertices` array-reference check — and terrain is never retired below the device lifetime because eviction runs only at teardown. | CORRECTED | key `` `${tile.level}_${tile.x}_${tile.y}` `` at `WebGPUGlobeSurfaceTileBuffers.ts:174`, identity check `:198-200`; `evictStaleResources` `WebGPUGlobeSurfaceRenderer.ts:2813`, sole non-export caller `:2971` with `new Set()`; `WebGPUModelRenderer.ts:5287-5300`, `:5302-5325` | C9-15 / FAR-203 / FAR-207 / FAR-208 |
| `F5-02` / `ACTION-5` | Terrain-buffer lifetime is decoupled from the quadtree LRU. The residency key omits payload revision, context and device generation; nothing on the quadtree free path releases a realization; the only reclaim destroys **inline, before submit**. Fix shape: lease release from `freeResources` / mesh replacement / provider swap / device loss, batched post-submit retirement, then a byte-budgeted zero-lease grace LRU — never a per-frame `evictStaleResources(visibleTileKeys)`. | CONFIRMED | `WebGPUGlobeSurfaceTileBuffers.ts:168-176` (coordinate key), `:577-590` (inline `vertexBuffer.destroy()` / `indexBuffer.destroy()` at `:585-586`), host interface `:61`; `Scene/GlobeSurfaceTile.js:158-190` frees water-mask/vector/imagery only; `WebGPUContext.ts:553-555`, `:2932`, `:3379` | `C11-34` (NOT STARTED, `QUEUE_2026-07-18_CAMPAIGN11.md:1669`, `:2456`); FAR-203 planned |
| `F5-03` / `ACTION-4` | One mutable globe renderer is shared per `GPUDevice` across contexts. Split device-immutable resources (refcounted per device), context/view state (formats, readiness, frame numbers, pick) and globe/provider residency (terrain, wireframe, imagery, water/ocean) **before** wiring terrain release. Do not clone the renderer per context. | CONFIRMED | `Scene/GlobeSurfaceTileProviderRendering.js:1110` (`new WeakMap()`), `:1312-1318`; `WebGPUGlobeSurfaceRenderer.ts:220` `_pipelineCache`, `:461` `_webgpuContext`, `:306` `_oceanNormalMapSource` | **unowned** for the fix; `Q-86` (`FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1002`) covers only the missing test; FAR-108 did effects only |
| `F5-09` / `ACTION-7` | Unbounded `reuploadWatch` map and the ocean source lifecycle. The module-global `Map` resets entries but never deletes them — no `delete`/`clear` for it exists anywhere in `Source`. **One sub-claim is explicitly unverified** and must be carried as such: "one bound function allocated per visible model per frame" was never measured when written and is not measured now (see §7). | CONFIRMED (one sub-claim unadjudicated) | `WebGPUGlobeSurfaceTextures.ts:788-791` (module-global `Map`), `:808-823`; ocean identity memo `WebGPUGlobeSurfaceRenderer.ts:2356-2367` | `DEFERRED_WORK.md:4628` (residual) + `:4630` sentinel-to-gate row; FAR-205 for leased ocean |
| `REJECT-1` | **Never** reintroduce a per-frame `evictStaleResources` sweep as the terrain reclaim. Substance stands; **citation corrected**: `WebGPUGlobeSurfaceWireframe.ts:44-46` no longer "documents its own cache outside the sweep" — it now _claims_ the buffer is destroyed in `destroy()` / `evictStaleResources()`, which the code contradicts (the sweep iterates `_tileBufferCache` only). The wireframe cache is still outside the sweep **and** the module comment misstates it — a C16-shaped comment defect worth its own one-line fix. | CORRECTED | `WebGPUGlobeSurfaceTileBuffers.ts:577-590`; callers `WebGPUGlobeSurfaceRenderer.ts:2813-2814` and `:2971`; `_wireframeIndexCache` cleared only at `WebGPUGlobeSurfaceRenderer.ts:3020-3023`; comment `WebGPUGlobeSurfaceWireframe.ts:44-46` | **unowned** (a guard on `C11-34`) |
| `GATE-1-ACCEPTANCE` | The terrain/ocean lifetime acceptance matrix has no owning row. Carry it onto `C11-34` in full: creates/destroys/live bytes plateau; no destroyed-resource reuse; warm second leg; WebGL byte-identical; device loss and two contexts on one device; fill→real, exaggeration, wireframe, shadow/pick/capture, two providers at identical coordinates, ocean swap, request-render wakeups. The runner can already measure the plateau. | CONFIRMED | `run-performance-campaign.mjs:555`, `:920`, `:956`, `:964` (`terrainCommandRefreshRecords`); `C11-34` at `QUEUE_2026-07-18_CAMPAIGN11.md:1669`, `:2300`; FAR-203, FAR-205 checked and carry no matrix | **unowned** (attach to `C11-34`) |
| `#8` / `A1.5` | `computeModifiedModelView` allocates a `Float64Array(16)` per tile. Return into a caller-provided scratch. LOW. | CONFIRMED | `WebGPUGlobeSurfaceCameraUB.ts:1319-1327` (`new Float64Array(view)` at `:1325`, `new Float64Array(16)` at `:1327`), called per tile at `:261` | **unowned** |
| `#121` / `A14.6` | Effects BGL cache builds long concatenated string keys on every `createEffectsBindGroup` call. With the globe memoised per frame (owner: `frameState`) the cost is per-consumer-per-frame rather than per-tile, so LOW. | CONFIRMED | `WebGPUEffectsBindGroup.js:1743`, `:1776` | **unowned** |

### 3.2 3D Tiles, voxels & point clouds

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `R14` | Translucent PointCloud tiles have **no classification depth-write variant**. The depth-sampling classifier shipped (Batches 80-85) and the mechanism now exists — `WebGPUDrawCommand` carries `classificationDepthPipeline` and the splat renderer consumes it — so this is a **wiring gap, not an architecture gap**. Confirm with a probe or file under C18 Wave P. | CONFIRMED | `WebGPUPointCloudRenderer.ts` contains no classification handling at all; residue survives only inside struck ledger text at `DEFERRED_WORK.md:7581`; mechanism at `WebGPUDrawCommand.ts:240`, `:394`, `:627` and `WebGPUGaussianSplatRenderer.ts:2838` | **unowned** — `C18-P2` covers colour-format decode/translucency, not classification depth-write |
| `R16` | Four unshipped `Vector3DTilePrimitive` behaviours, recorded **only in a renderer docstring**: (1) **pick containment** — the pick path uses the depth-sample `pickFS`, so a pick hits the inflated projected silhouette rather than the stencil-clipped surface∩volume region (this fourth item was missed by the first reader); (2) SCENE2D/CV stencil coverage; (3) normal-from-depth-derivative + textured appearance; (4) `debugWireframe` LINES variant. File all four. | CONFIRMED | `WebGPUVector3DTilePrimitiveRenderer.js:37-49` (`:37-44`, `:45-46`, `:47`, `:48-49`) | **unowned** (renderer docstring only) |
| `R17` | `Vector3DTileClampedPolylines` uses **one depth source per frame for both classification passes**, plus a second residual in the same docstring: the CESIUM_3D_TILE command binds `Pass.CESIUM_3D_TILE` instead of the intended `Pass.CESIUM_3D_TILE_CLASSIFICATION`. Confirm with a mixed terrain+tileset probe and file both. | CONFIRMED | `WebGPUVector3DTileClampedPolylinesRenderer.js:40-44` (docstring), single `globeDepthTex` binding at `:168`, resolver at `:1258-1261`, `:1296` | **unowned** (renderer docstring only) |
| `H-P10` | Point-cloud instance data is rebuilt with a per-frame `Float32Array` allocation plus a full CPU transform decode loop, gated on a revision that is really the point count. | CONFIRMED | `WebGPUPointCloudRenderer.ts:1789-1803` (rebuild gate), `:1276` (allocation), `:1154-1290` (decode loop) | `QUEUE_2026-08-09_CAMPAIGN18.md:246` `C18-A1`, `:250` `C18-A5` |
| `#14` | The point-cloud sort dispatcher allocates `new Uint32Array(r.capacity)` twice per `sort()` (keys + indices) and a `new DataView(new ArrayBuffer(4))` per element in `floatToSortableUint`. Hoist both to grown instance scratch. LOW, GPU-sort path only. | CONFIRMED | `WebGPUPointCloudSortDispatcher.ts:301-302`, `:79`, `:98` | **unowned** |
| `#90` / `A10.6` | `DecoupledScan` allocates a zeros array and a fresh bind group per dispatch of the opt-in deterministic LOD scan. Cache the bind group on buffer identity and clear the partitions buffer with `clearBuffer`. LOW, default-off path. | CONFIRMED | `WebGPUDecoupledScan.ts:327-337` | **unowned** |

### 3.3 glTF Models & KHR extensions

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `C-R4` | Two residual glTF rows survive the KHR closure. **(a) Outlines** — `EXT_mesh_primitive_outline` / `CESIUM_primitive_outline` reach no WebGPU code: 0 hits across `Renderer/WebGPU/**` and `Shaders/WebGPU/**`, while the loader still generates outline coordinates via `PrimitiveLoadPlan.generateOutlines`, so the cost is paid and the result discarded. **(b) Vertical exaggeration** — 0 `exagger` hits in the model renderer, model pipeline cache and `ModelPBRComplete.wgsl` while `Scene/Model/Model.js:365` still computes `_enableVerticalExaggeration`, so under `scene.verticalExaggeration != 1` models and terrain drift apart on WebGPU. Both rows need a `FEATURE_INVENTORY` §C entry and a probe before they can be scheduled. | CONFIRMED | `Scene/Model/PrimitiveOutlineGenerator.js:66`, `PrimitiveOutlinePipelineStage.js:15`, `ModelRuntimePrimitive.js:354`; `Scene/Model/Model.js:365`; `ModelPBRComplete.wgsl` (4,325 lines) | **unowned** — `FEATURE_INVENTORY.md:241` lists the outline stage as §A upstream-only; `DEFERRED_WORK.md:9863` covers only _terrain_ shadow-cast exaggeration |
| `M-R13` | Orphaned `Model*Stage.wgsl` files: **four, not five or six.** `ModelSilhouetteStage.wgsl` **is** wired (`WebGPUModelPipelineCache.ts:34` imports, `:2745` injects). `ModelPointCloudStylingStage.wgsl` is **do-not-remove scaffolding** owned by `C18-P3` — deleting it would destroy scaffolding a launched campaign row depends on, the exact inversion Principle 7 warns about. The genuinely dead four are `ModelColorStage`, `ModelCPUStylingStage`, `ModelSplitterStage`, `ModelAtmosphereStage`: zero importers, behaviour shipped inline in `ModelPBRComplete.wgsl` (aerial-perspective fog `:3509`, model-colour blend `:2339-2352`, splitter `:2372`, CPU/batch styling `:269-276`, `:3475-3478`, `:3589`). `ISSUES_AND_FIXED_BUGS.md:175` is stale in both directions and needs this three-way split. | CORRECTED | `Shaders/WebGPU/Model/` holds all six; wiring at `WebGPUModelPipelineCache.ts:34`, `:2745` | Point-cloud file only: `DEFERRED_WORK.md:9839`, `QUEUE_2026-08-09_CAMPAIGN18.md:168` (`C18-P3`). The other four: **unowned** (C16 review candidates) |
| `R2` (feature-inventory) | KHR extension bodies live inline in `ModelPBRComplete.wgsl` (clearcoat / specular / anisotropy / iridescence / sheen / volume / transmission branches), and **two records contradict each other at HEAD**: `DEFERRED_WORK.md:5210-5236` closes `C-R4-GLTF-KHR` while `FORK_OVERVIEW.md:101`/`:153`/`:248` and `FEATURE_INVENTORY.md:1245-1248` still gate them. The glTF lens must reconcile which is current. Second drift in the same sentence: `FORK_OVERVIEW.md:153` sizes `WebGPUModelRenderer` at "~2300 LOC" against **9,284** lines today. | CONFIRMED | `ModelPBRComplete.wgsl` (4,325 lines); `DEFERRED_WORK.md:5210-5236`; `FORK_OVERVIEW.md:153`; `FEATURE_INVENTORY.md:1245-1248` | `DX-07` for the split; `FEAT-SURVEY-02/03/04` for the bodies; **no row owns the drift** |
| `C-P8` | Model-path pipeline compilation is still **synchronous on the tileset-stream hot path**, and the census is roughly double what was first recorded: `device.createRenderPipeline` is called from **fourteen** sites in `WebGPUModelPipelineCache.ts`, not eight. The comment at `:1438` describes `:1463` as a deliberate direct hatch that never consults the central pipeline cache. Every model still constructs its own `WebGPUModelPipelineCache`. The async colour path behind a ready-gate is real and does not close this. | CORRECTED | `WebGPUModelPipelineCache.ts:85-86` (async gate); sync sites `:979`, `:1065`, `:1136`, `:1372`, `:1463`, `:1521`, `:1591`, `:1650`, `:1733`, `:1823`, `:1891`, `:1977`, `:3551`, `:3597`; per-model construction `WebGPUModelRenderer.ts:5470` | `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:549` `Q-134` and `:457` `Q-143` |
| `H23` | Model geometry normalization allocates **before** the cache lookup; the per-primitive key is a string built from node and primitive indices with loader-owned revision tokens alongside. | CONFIRMED | `Scene/Model/ModelPrimitiveGeometry.js:41-64`; `WebGPUModelRenderer.ts:6397` (`` `${nodeIdx}_${primIdx}` ``) | C9-17 / FAR-204 (`QUEUE_2026-07-15_CAMPAIGN9.md:148`) |
| `S6.5-C11-185` | Visibility-triggered model preparation, slices 1-3, landed: a cached five-plane culling volume computed once per frame number and invalidated when fewer than five planes are present. The C11 row carries the measured counts verbatim (53,821 / 20,384 / 1,597 / 31,840 / 21,981 over 1,088 frames); the **timing deficit** sits on `C11-168`, not here. | CONFIRMED | `WebGPUModelPreparationAdmission.ts:140-144`, `:273-295`, call site `:423`; `QUEUE_2026-07-18_CAMPAIGN11.md:2127`, `:2534`; deficit at `:2003` | `C11-185` (numbers); `C11-168` (timing) |
| `#26` / `A3.6` | Clearcoat Fresnel is formed on `VdotH` and dims the **direct** term only, so the coat never attenuates the IBL/ambient base and there is no coat IBL lobe. WebGL's `LightingStageFS` applies the NdotV-based dimming to the whole base colour. LOW, cosmetic on coated assets. | CONFIRMED | `Shaders/WebGPU/Model/ModelPBRComplete.wgsl:3012-3016` | **unowned** |
| `#79` | `extractEdgeGeometry` cost and string-keyed edge maps — **half the premise is refuted.** The single call site runs at primitive-resource creation and stores into `primCache.edgeResources`, so the cost is **load-time per primitive, not per frame**; the "per-frame cost if not cached" framing does not survive. What remains is a load-time cleanup: the adjacency maps are `Map<string, number[]>` keyed by template strings; numeric keys would cut load-time allocation. LOW. | CORRECTED | `WebGPUEdgeVisibilityEmitter.ts:670`, `:713`, `:867`, `:1036`; call site `WebGPUModelRenderer.ts:8585`, stored `:8595-8600` | **unowned** |

### 3.4 Geometry primitives, classification & materials

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `S4-port-FAR-generic-primitive` | The generic `Primitive` path retains its legacy realization under FAR-201 / FAR-209 / FAR-210: the topology mapper still falls `LINE_LOOP` through to `line-strip` and returns `triangle-list` for `TRIANGLE_FAN`, and `stripIndexFormat` does not occur in the file at all. The ledger still reads `"state": "planned"` for all three FAR packages. Migration is a pointer, not a rescue — the ledger carries the same claim at the same specificity. | CONFIRMED | `WebGPUPrimitiveCommands.ts:958-984` (`:967-973` LINE_LOOP, `:975-980` TRIANGLE_FAN); `FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json` (`checkpointDate 2026-07-15`); `DEFERRED_WORK.md:9840` item 6 | `DEFERRED_WORK.md` GLTF-PRIMITIVE-MODE-RESIDUALS item 6; FAR-201 / FAR-209 / FAR-210 |
| `#35` | `TRIANGLE_FAN` silently falls back to triangle-list — **two corrections.** (a) An owning row **does** exist; migrating this as a new finding would duplicate a live row. (b) "No caller does the conversion" is wrong for the model path: `WebGPUModelTopology.ts:72`, `:168`, `:395` expands fans around their hub. The surviving scope is the **generic Primitive path only**, and it is owned. | CORRECTED | `WebGPUPrimitiveCommands.ts:977-981`; model path `WebGPUModelTopology.ts:72`, `:168`, `:395` | `DEFERRED_WORK.md:9842` (residual 6 of the C11-90 family) |
| `#30` | GroundPolyline `geometricToleranceOverMeter` is hardcoded to `1.0e-6` where WebGL's `PolylineShadowVolumeVS` uses the per-frame `UniformState` value. Plumb the live value through the UBO. LOW, extreme near/far zoom only. (The original `:157` anchor is the layout comment; the write is `data[83]` at `:1678`.) | CONFIRMED | `WebGPUGroundPolylineRenderer.js:211` (constant), packed at `:1678` | **unowned** |
| `#31` / `A4.6` | All three ground-primitive depth-sample entries discard on **exact float equality** with `surfaceDepth == 0.0` after unpacking the RGBA globe depth. A sentinel comparison against the packed encoding's zero (or `<= epsilon`) would be robust to sampler filtering. LOW. | CONFIRMED | `WebGPUGroundPrimitiveRenderer.js:1106`, `:1176`, `:1193` | **unowned** |
| `B8` | WebGPU `ViewportQuad` is broken and cannot represent Cesium materials: the Scene-side `isWebGPU` branch builds a colour-only `materialUniforms` which the next line overwrites with `this._material._uniforms`; the renderer compiles its pipeline synchronously, orders bindings by `Object.keys(uniformMap)`, and rebuilds its bind group per execute. Only a WebGL spec exists. **Corrected on ownership:** the Scene-side half **is** owned; what is genuinely unowned is the `WebGPUViewportQuad.ts` interior — synchronous compile, key-order bindings, per-execute bind-group rebuild, the marker-only command shape and the absent WebGPU spec. Scope the new row to that. | CORRECTED | `Scene/ViewportQuad.js:10`, `:144-169`, `:187`; `WebGPUViewportQuad.ts:308`, `:361`, `:514`, `:540-556`; only `Specs/Scene/ViewportQuadSpec.js` (WebGL) | Scene half: `DEFERRED_WORK.md:4868` `NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE` + `FIX_QUEUE…:86` `Q-9`. Interior: **unowned** (FAR-301) |
| `H18` | Material matrix packing does not implement the WGSL host-shareable layout. The alignment table names only f32/vec2/vec3/vec4/mat4x4 and derives alignment from element count; any `Array` is classified `{type:"matrix", size: value.length}` and packed contiguously. **Sharpened failure modes:** a 4-element mat2 is over-aligned to 16 bytes where WGSL `mat2x2<f32>` requires align 8, so its offset and every following field diverge from the shader's; a 9-element mat3 is packed as 36 contiguous bytes where WGSL `mat3x3<f32>` is three 16-byte-strided columns (48 bytes). No typed schema generates the WGSL declaration and the CPU offsets together. | CONFIRMED | `Scene/MaterialUniformBuffer.js:60-119`, `:183-186` | FAR-302 — **no row** in any queue, in `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md` or in `DEFERRED_WORK.md` |

### 3.5 Collections (billboard, label, point, polyline, cloud)

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `#37` / `A5.1` / `C-P9` | **Billboard and Label `eyeOffset` is silently dropped on WebGPU.** The 11 × vec4 instance layout carries no eye-offset lane; the only `eyeOffset` hits under `Renderer/WebGPU/` and `Shaders/WebGPU/` are the orphan chunk `chunks/functions/csm_eyeOffset.wgsl` and its registry entry — no shader consumes it and no renderer reads `bb._eyeOffset`. WebGL applies it through the `EYE_OFFSET` attribute. Entities that use `eyeOffset` to depth-sort labels render at the wrong depth on WebGPU. **This is the only HIGH-severity parity defect in the whole migrated set with no owning row, and it needs one before its source document is retired.** | CONFIRMED | `Shaders/WebGPU/Collections/BillboardCollection.wgsl:4-24`; chunk `chunks/functions/csm_eyeOffset.wgsl:5`, registry `chunks/CsmBuiltins.js:40`, `:146`; WebGL `Scene/BillboardCollection.js:48`, `:1423`, `:1749-1779`, `Shaders/BillboardCollectionVS.glsl:209` | **unowned** — the only queue hit is a passing field-name mention at `DEFERRED_WORK.md:5034` |
| `M-R5` | `debugShowBoundingVolume` is dropped for Billboard / Label / Point / Polyline collections — see §4 R2, this is a **reversal** of a closure. WebGL sets the flag on the command; the four WebGPU renderers never do; the consumer filters on exactly that flag. Setting `billboardCollection.debugShowBoundingVolume = true` draws the red wireframe on WebGL and nothing on WebGPU. The rest of M-R5 is upheld: `pickOnly` **is** set and consumed, and "collections do not set `castShadows`/`receiveShadows`" matches upstream WebGL. Severity is debug-tooling parity, not rendering correctness. | CORRECTED (PARTIAL) | WebGL `Scene/BillboardCollection.js:1220`, `PointPrimitiveCollection.js:842`, `PolylineCollection.js:802`, `:898`; 0 hits in `WebGPUBillboardRenderer.js`, `WebGPULabelRenderer.js`, `WebGPUPointPrimitiveRenderer.js`, `WebGPUPolylineRenderer.js`; consumer `WebGPUSceneRenderer.ts:3652`; unreachable WebGL assignment `BillboardCollection.js:750` | **unowned** — `DEFERRED_WORK.md:5012` covers only the `Buffer*` renderers and `GeoJsonPrimitive` |
| `H-P8` | Polyline behind-camera clipping is missing on **two** surfaces, and the first reader's consumer claim is inverted. `Shaders/WebGPU/Collections/PolylineCollection.wgsl` performs the screen-space divide and expansion with no `w <= 0` handling; `WebGPUVector3DTilePolylinesRenderer.js` documents the same gap against itself in its own "Current limitations" block. The renderers that **do** get the clip are the Primitive polyline family, which pull `csm_polylineCommon` in through the chunk marker and clip internally. Acceptance: a probe with a polyline through the camera near plane at planetary scale, run against both surfaces. | CORRECTED | `Collections/PolylineCollection.wgsl:91-92`, `:131-136`; `WebGPUVector3DTilePolylinesRenderer.js:26-31`; working path `csm_polylineCommon.wgsl:63` via `:123-133`, pulled at `WebGPUPrimitiveShaders.js:128`, `:138` | **unowned** |
| `#44` / `A5.7` | `disableDepthTestDistance` sets `clipPos.z = 0` with no `w <= 0` guard: all three assignments run without testing `clipPos.w`, so a billboard behind the camera inside the distance is projected onto the near plane instead of clipped. **Scope corrected:** the twin is the SDF label shader, not a point-primitive shader — `Collections/PointPrimitiveColor.wgsl` has no `disableDepthTestDistance` path at all. | CORRECTED | `Collections/BillboardCollection.wgsl:417-432` (`:420`, `:424`, `:431`); twin `Collections/BillboardCollectionSDF.wgsl:308`, `:312` | **unowned** |
| `#42` / `A5.6` | `updateWebGPUPolylines` declared async without await — **the premise is refuted.** The function's single `await` is inside it and the surrounding `beginCollectionFrame` / `finally endCollectionFrame` re-entry guard is deliberate, so dropping `async` is not the fix. What survives is narrower: `Scene/PolylineCollection.js:416` calls `fr.update(...)` and **discards the returned promise**, so a rejection inside the async update becomes an unhandled rejection rather than a thrown error. Migrate the residual, not the original text. LOW. | CORRECTED | `WebGPUPolylineRenderer.js:1567-1587` (await at `:1584`); call site `Scene/PolylineCollection.js:416` | **unowned** |
| `#43` | The billboard `show` lane is written `1.0` unconditionally after a CPU pre-cull that already tests `bb.show && bb._clusterShow !== false`, so the WGSL `show < 0.5` degenerate-quad branch is unreachable. Keep the CPU pre-cull and delete the lane plus branch. LOW cleanup. | CONFIRMED | `WebGPUBillboardRenderer.js:196`, `:272` (also `:861`) | **unowned** |
| `H10` | Collection paths still contain O(N) rebuilds and lifecycle gaps: `buildSegmentDataForGroup` and `buildPickSegmentData` both allocate full segment arrays per build. | CONFIRMED | `WebGPUPolylineRenderer.js:509` (called `:1735`), `:663` (called `:2105`) | FAR-307 / C9 item 62 (rows in the closed CAMPAIGN8/9 queues + CAMPAIGN11) |
| `#194` / `B6` | `computeBoundingVolumeForFeatureRenderer` is duplicated near-identically in two collections. Hoist into a shared Scene helper when either is next touched. LOW. | CONFIRMED | `Scene/BillboardCollection.js:2179` (called `:764`, `:1266`); `Scene/PointPrimitiveCollection.js:1321` (called `:513`) | **unowned** |
| `L-P3` | `CloudCollection` has an orphan WGSL source: `Shaders/WebGPU/Collections/CloudCollection.wgsl` and its generated `.js` exist with zero importers; the live source is the `CLOUD_WGSL` string inside the renderer. | CONFIRMED | `Shaders/WebGPU/Collections/CloudCollection.wgsl`; live source `WebGPUCloudRenderer.ts:108`, used at `:959` | `WEBGPU_DEBUGGING_LOG.md:1548` — acknowledgement only, not a row |

### 3.6 Picking

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `#47` / `A6.3` | `Scene.pickFromRay` on WebGPU returns a hit object with **no `position`**: the guard `if (!context.supportsSynchronousReadback)` emits a one-time warning and the offscreen ray-depth path is never built. The `*MostDetailed` variants are unsupported too. The picking-state document is the authority (LD-04 CRITICAL, LD-14 HIGH); the `DEFERRED_WORK` entry over-claims because its probe asserts no-throw only. | CONFIRMED | `Scene/PickingRayHelpers.js:196-207` | `PICKING_ARCHITECTURE_STATE_2026-08-17.md:404` (LD-04) / `:413` (LD-14) |
| `HP-08` | Ordinary frames upload pick state and repeat redundant GPU state calls: the primitive path allocates a `Float32Array`, writes the pick camera UB, creates a pick-camera bind group, writes the pick material UB and creates a pick-material bind group **per pickable command per update**, with no `frameState.passes.pick` gate on that block. Worth adding to the original text: the cost is not only two `writeBuffer`s but **two `createBindGroup`s** per pickable command per frame. The model path is gated. | CONFIRMED | `WebGPUPrimitiveCommands.ts:3018`, `:3989-4040`; gated counterpart `WebGPUModelRenderer.ts:6809-6810` | `Q-141` (`FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md`) + FAR-303 / FAR-409 |
| `#50` | `PickId.normalizedRgba` is produced for every pick id and read by nothing — the only other occurrences are the declaration, a doc line and a comment. Wire a consumer or drop the field. LOW; four floats per pick id. | CONFIRMED | `Renderer/PickId.js:50`; `Renderer/PickId.d.ts:25`; `Renderer/GraphicsContext.ts:1633`; `Renderer/WebGPU/WebGPUContext.ts:4221` | **unowned** |
| `#52` / `A6.7` | The pick pass returns silently on a missing pick FBO: the file contains **zero** `console.error` sites and bare `return`s at every resource-missing exit. Per the CLAUDE.md null-target sentinel rule a throttled `console.error` carrying the context id belongs at those exits. LOW. | CONFIRMED | `WebGPUSceneRendererPickPass.ts:398`, `:442`, `:463`, `:475` (also `:513`, `:895`, `:931`) | **unowned** |

### 3.7 Shadows & lighting

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `B.11` | The WebGPU CSM cast list omits **volumetric casters** (Voxel, GaussianSplat). Cascaded shadow maps take casters only from renderers that set `_shadowCastLayout` — models, globe tiles and geometry primitives. The voxel and splat renderers set it nowhere; both only _receive_. Upstream WebGL does not cast from them either, and screen-space collections are deliberate non-goals on both backends. Casting from volumetric content needs a deep-shadow-map or transmittance-accumulation technique rather than a cast-layout registration, so it stays demand-gated. **Scope corrected:** the id is _not_ "defined nowhere" — it has a disposition row and a demand gate. The action is narrower than first judged: **mirror the existing disposition into `DEFERRED_WORK.md`** (grep: zero hits) so the demand gate lives in the ledger the campaigns read, and carry the mechanism sentence above. | CORRECTED | `WebGPUCSMCastPass.ts:324`, `:328`; `WebGPUCSMRenderer.ts:237`; `WebGPUGlobeSurfaceTileBuffers.ts:541`, `:557`; `WebGPUModelRenderer.ts:875`, `:7726`; `WebGPUPrimitiveCommands.ts:407`; `WebGPUPrimitiveShadowCast.ts:28`, `:248`, `:264`; `WebGPUShadowMapRenderer.js:668`; `Scene/GlobeSurfaceTileProviderRendering.js:1506`; receive-only uniforms at `WebGPUVoxelRenderer.ts:375-378`, `WebGPUGaussianSplatRenderer.ts:592-595` | `ISSUES_AND_FIXED_BUGS.md:188` (DEFER / OPEN by design) + `NEXT_QUEUE_2026-07-04.md:101` (demand gate); **absent from `DEFERRED_WORK.md`** |
| `H-R2` / `H-P20` | Shadow cast uses a dedicated cast pipeline family, but the cast fragment shader is empty — **scope narrowed.** WebGL does **not** keep an alpha test for MASK materials (`ShadowMap.js:257` sets `isOpaque = command.pass !== Pass.TRANSLUCENT`, `AlphaPipelineStage.js:22` puts MASK in `opaquePass`, `ShadowMapShader.js:134-137` omits `czm_shadow_cast_main()` for the opaque-and-unclipped case), so a masked leaf casts its full quad on **both** backends and that half is at parity. The two real residuals are **(a) TRANSLUCENT-pass casters**, where WebGL runs the colour main and discards at `out_FragColor.a == 0.0` and WebGPU's empty `shadowCastFragment` cannot; and **(b) clipping-plane discard on the cast**, which WebGL preserves via `hasClipping` while the WebGPU cast shader has no clip term, so clipped geometry still casts. Fix shape: a cast variant that samples baseColor and evaluates clipping planes, plus a probe over a translucent-textured and a clipped caster. | CORRECTED | pipelines `WebGPUShadowMapRenderer.js:160`, `:666`, `:819-823`, depth-only `:104`, `:113`; residual `Shaders/WebGPU/Shadow/ShadowMap.wgsl:47` (`shadowCastFragment()` takes no inputs, empty body; 0 `alpha`/`discard`/`clip` hits); WebGL `Scene/ShadowMap.js:257`, `:264-266`, `Scene/ShadowMapShader.js:134-147`, `Scene/Model/AlphaPipelineStage.js:22` | **unowned** for the residuals; `QUEUE_2026-07-18_CAMPAIGN11.md:2165` `C11-189` (NOT STARTED) covers the style half only |
| `S6.2-C11-184` | `C11-184` stays IN PROGRESS until the browser gates execute: the W1 row reads "FOCUSED + MOVING RUNTIME GATES OPEN", the globe-RECEIVE leg was discharged at Batch 850 and everything else is explicitly left open. `Scene.js` keeps `cascadesEnabled` at its default `true` on both backends. | CONFIRMED | `QUEUE_2026-07-18_CAMPAIGN11.md:2126`, `:2533`; `Scene/Scene.js:1138-1148` | `C11-184` |
| `#59` | WebGPU CSM ignores `ShadowMap.numberOfCascades`. `_cascadeCount` is configurable but `numberOfCascades` has **zero** hits under `Renderer/` — no site threads `ShadowMap._numberOfCascades` (validated to 1 or 4) into that config, so a WebGL-valid `numberOfCascades: 1` shadow map still runs four cascades on WebGPU. Verify with a probe and wire the count. LOW-MEDIUM. | CONFIRMED | `WebGPUCSMRenderer.ts:405`; source value `Scene/ShadowMap.js:502-504`, validation `:376-378` | **unowned** |
| `#56` / `A7.3` | The CSM cast-cull SoA `centerX/Y/Z/radius` fields are filled per cast command per cascade per frame while the file's own comment states only `interleaved` is uploaded. Either use the SoA as a debug surface or drop it. LOW, deliberate per comment. | CONFIRMED | `WebGPUCSMCastPass.ts:534-539` (alloc), `:562-570` (fill), comment `:543-545` | **unowned** |
| `#58` / `A7.5` | Per-cast-pass `Cartesian3` allocations: `enc.high ?? new Cartesian3()`, `enc.low ?? new Cartesian3()` and a third `new Cartesian3(...)` run per cast invocation. Hold a module scratch and set `x/y/z`. LOW. | CONFIRMED | `WebGPUCSMCastPass.ts:467-470` | **unowned** |
| `#62` | The cast pass has no drawn-vs-skipped sentinel: it `continue`s silently on every unresolved command shape and never counts drawn versus skipped per cascade. A cascade that draws 0 of N>0 commands should emit a throttled permanent warning. LOW, diagnostic. | CONFIRMED | `WebGPUCSMCastPass.ts` — 0 matches for `skipped` / `drawnCount` | **unowned** |
| `#57` / `A7.4` | The clustered-lighting dispatcher calls `_scratchEyeLights.slice(0, activeCount)` per dispatch on an array the class already pools. Pass a subarray plus count. LOW, opt-in feature. | CONFIRMED | `WebGPUClusteredLightingDispatcher.ts:371` (pool `:145`, grown `:396-397`) | **unowned** |
| `#63` | The cluster-bounds shader comment claims "in Cesium WebGPU eye-space, Z grows positive forward (camera looks toward +Z)". Eye space is −Z forward; the negation in `sliceZ(i)` is what makes the slice math work. Fix the wording when C16 touches the file. LOW. | CONFIRMED | `Shaders/WebGPU/Compute/ClusterBounds.wgsl:37-40` | **unowned** |

### 3.8 Post-process, effects & attachment topology

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `A2c-4` | The post-process ping-pong pair is always allocated. Both full-resolution intermediates are created unconditionally and stay resident for the default effect-free WebGPU scene, which always routes through this pipeline for the canvas blit; when `hasActiveStages` is false the pipeline runs a copy stage and returns. Lazy allocation on the first active-stage frame remains the unowned fix. | CONFIRMED | `WebGPUPostProcessPipeline.ts:611-627`, `:1510-1513` | **unowned**; adjacent `RENDERER_LANDSCAPE_AUDIT_2026-09-02.md:233` (THREE-A09) |
| `R8` / `#72` / `A8.8` | The f16 tonemap "auto-fallback" **does not exist, and the error scope wraps no work.** `_compileStage` creates the f16 shader module **before** `pushErrorScope("validation")` and pops the scope immediately with no intervening device work, so the scope can never capture the compile it was written to guard; a rejected module is only logged. `fallbackWgslCode` is captured but never compiled, and the pipeline always uses the primary module. **The first reader's own evidence was wrong** in claiming `pushErrorScope` no longer appears in the file — it does, at `:2125`. The original finding stands **in full**, not partially. Fix by compiling inside the scope, or selecting the f32 source up front on devices without `shader-f16`, then delete the stale comment. | CORRECTED | `WebGPUPostProcessPipeline.ts:2118-2143` (module `:2118-2121`, scope `:2124-2126`, log-only `:2136-2141`, pipeline `:2175`); `fallbackWgslCode` at `:2096` and `:2124` only; stale comment `:2098-2118` | **unowned** |
| `#73` / `A8.7` / `F5-10` / `ACTION-13` | The OIT injected-pipeline cache is keyed on `` `${shaderCode.length}_${fragEntry}_${config.label ?? ""}` `` — a shader-**length** key, so two different shaders of equal length sharing an entry point and label collide, and the same key serves both the pipeline and the shader-module cache. Separately, the composite bind group is rebuilt unconditionally on every update. Fix: rebuild the composite bind group only when its views change; key derived pipelines by **module identity** (the `WeakMap`-backed `webgpuObjectIdentity` the central caches use) plus target format, sample count and depth state; then re-run the intersecting-translucency, resize, MSAA, format and device-loss gates. **Fix before OIT leaves containment.** | CONFIRMED | `WebGPUOIT.ts:706` (key), `:259` (unconditional `_updateCompositeBindGroup()`), `:350-370` (fresh `createBindGroup` each call) | **unowned** — FAR-003 contains the feature; `OIT_DEFAULT_FLIP_EVIDENCE_2026-07-18.md` is the default record |
| `B7` | OIT's depth/stencil attachment contract defect is fixed (`depthReadOnly` / `stencilReadOnly` with no load/store ops), but accumulation and revealage remain single-sampled, which does **not** make the multisample path ordering-safe. The MSAA-by-OIT ordering hazard is stated in the source itself — a better anchor than the audit line the parity matrix cites. WebGPU OIT is default-off with a sorted-alpha fallback. | CONFIRMED | `WebGPUOIT.ts:150-160`, `:163-167`; `WebGPUSceneRenderer.ts:952` (`_webgpuOITEnabled = false`); fallback `WebGPUSceneRendererTranslucentPass.ts:311` | FAR-404 — **no queue row**; ratified default-off record at `DEFAULT_PARITY_MATRIX_2026-07-18.md:35` |
| `B-4` | SSR samples an uninitialized normal texture: the placeholder is allocated and the fallback path is live; the submit half is closed. | CONFIRMED | `WebGPUSSREffect.ts:154-172`, `:211-226`, `:356-358` | `DEFERRED_WORK.md:9309` `NEW-GBUFFER-CONSUMER-SSR`, open |
| `L-P5` | A 1920×1080 canvas-width fallback literal remains in the SSR effect (it is gone from the point-cloud renderer). It disappears with the B-4 consumer switch. | CONFIRMED | `WebGPUSSREffect.ts:202-203` | **unowned** |
| `#68` | `Pipeline.destroy()` leaks user WGSL stages and the ColorGrading UBO: it releases textures, tonemap/FXAA/custom-stage buffers, effect objects, TAA and library stages, but never calls `clearUserWGSLStages()` and never destroys `_colorGradingStage.uniformBuffer`. A pipeline recreate on HDR toggle leaks the user-stage intermediates. LOW-MEDIUM. | CONFIRMED | `WebGPUPostProcessPipeline.ts:2200-2253`; `clearUserWGSLStages` defined `:1239-1244`, called only from `WebGPUPostProcessStageCollection.ts:839`, `:953`; stage `:318`, buffer via `_compileStage` at `:888` | **unowned** |
| `#106` / `A12.2` | The Scene **ID render target** is allocated but never rendered into: a full-screen `rgba8unorm` + `depth24plus-stencil8` target is allocated, exposed and **cleared every frame** while no pass renders into it and no caller reads it — WebGPU picking uses `WebGPUPickFramebuffer` instead. (`Scene/FramebufferOrchestrator.js:292` reads the **WebGL** `SceneFramebuffer` API; the WebGPU getter returns a `GPURenderPassDescriptor` and has no such method, so it is not a consumer.) Add the wasted clear pass to the cost. Delete the target after a Principle-7 re-derivation, or document it as the planned ID-pass destination. LOW-MEDIUM. | CONFIRMED | `WebGPUSceneFramebuffer.ts:24`, `:81`, `:263-265`, `:332`, `:352-357`, `:401` | **unowned** |
| `H7` / `REJECT-2` | MRT / G-buffer cost is always paid even when deferred lighting is off: `forceSceneMRT = true` with the in-source comment "Demand-driven topology selection is not wired yet"; the demand registry ORs `forceSceneMRT` into `gbufferDemanded`; the scene-FB helper's `_mrtMode` is a module-global `true` whose only mutator has no caller in `Source`. **REJECT-2 is the standing rule:** a topology change must be a context-owned immutable signature (target count/formats, depth format, sample count) folded into shader, pipeline, render-target and pass-descriptor identity, published during `prepareFrame`, prewarmed asynchronously and committed atomically at a frame boundary — **never a per-frame module-global flip.** | CONFIRMED | `WebGPUContext.ts:732`, `:5014`, `:5072-5077`, `:5086-5094`; `WebGPUAttachmentDemandRegistry.ts:216-217`; `WebGPUSceneFBTargetHelpers.ts:35`, `:49` | C9-10 / FAR-403-C0; `DM-12` acceptance (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:420-427`) |
| `#80` / `A9.6` | The TAA `taaStats` debug snapshot was made an acceptance criterion and never wired: the design doc still lists `Scene.getDebugSnapshot().renderer.taaStats` reporting history ping/pong validity, `lastJitter` and `framesSinceJump`, and there are **0 matches** for `taaStats` / `framesSinceJump` across `packages/engine/Source`. Either wire it into `CesiumDebug` or strike the criterion. LOW, doc/instrument. | CONFIRMED | `TAA_DESIGN.md:219`; 0 engine matches | **unowned** |
| `#81` | The TAA disocclusion threshold is `abs(eyePosCurr.z) * 0.001` floored at `1.0`, so below ~1 km eye depth the 1 m constant dominates. A per-pixel-footprint floor would hold across altitudes. LOW. | CONFIRMED | `Shaders/WebGPU/PostProcess/TAA.wgsl:313-314` | **unowned** |

**Label note carried from the FABLE5 bank.** `F5-04` (MRT slot 1 observe-only) is recorded SUPERSEDED in its source register, and the word will read as "fixed" to anyone skimming. It is not: the defect is **live** and only ownership moved. Label it **"live, owned by `DM-12` / C9-10"** wherever it is quoted. Evidence: `WebGPUContext.ts:732`, `:5072-5077`; `WebGPUSceneFBTargetHelpers.ts:35`, `:49`; `DEFERRED_WORK.md:9942` (`C9-10-CONSUMER-DRIVEN-MRT`).

### 3.9 Performance, compute & GPU-driven paths

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `A2a-2` / `M-R2` / `M-R3` | The context's sampler and bind-group-layout caches are plain unbounded `Map`s keyed by `JSON.stringify(descriptor)`, with no cap or eviction, cleared only on destroy and device invalidation. **Severity and ownership corrected:** the "hot during pipeline builds" premise does not hold — `getOrCreateBindGroupLayout` has **zero callers** (only a twin definition in the resource manager) and `getOrCreateSampler` has exactly one. The BGL cache is **dead by adoption** and is already owned as gain-callers-or-remove; only the sampler half is a live (and cheap) hand-serialization candidate. | CORRECTED (A2a-2) / CONFIRMED (M-R2, M-R3) | `WebGPUContext.ts:698-699` (declarations), `:5599` and `:5622` (`JSON.stringify` keys), inserts `:5600-5604`, `:5623-5627`, clears `:5456-5457`, `:7568-7570`; twin `WebGPUResourceManager.ts:100`; sole sampler caller `WebGPUSampler.ts:193` | BGL half: THREE-A06 (`RENDERER_LANDSCAPE_AUDIT_2026-09-02.md:230`, `:404`). Sampler half: **unowned** |
| `H-R8` | `getPooledBuffer` is unkeyed on usage flags: the pool is looked up by `type` alone and matched on a size band, with `usage` reaching only the miss-path `createBuffer`. Zero engine callers today, so it is a **trap for the next caller** rather than a live defect. Key on `(type, usage)` or delete as dead surface when the context is decomposed. | CONFIRMED | `WebGPUContext.ts:5705-5733` (pool declared `:703`, lookup `:5714`, band match `:5715-5717`, miss path `:5730`) | **unowned** |
| `A2a-3` | Per-frame `writeBuffer` cadence: **396 sites across 116 files** (not the 227/66 originally recorded). Collections are addressed by the dirty gate and partial-write coalescing; Polyline (7 sites) and Model (16 sites) still upload on an unprofiled per-update cadence and should be measured against the FAR settled-frame acceptance. | CONFIRMED | `WebGPUPolylineRenderer.js` ×7, `WebGPUModelRenderer.ts` ×16, `WebGPUBufferPrimitiveRenderer.ts` ×1 | `DEFERRED_WORK.md:4895` (`NEW-COLLECTIONS-DIRTY-GATE`, open), `:4989` (`NEW-PARTIAL-WRITE-COALESCING`, shipped Batch 229); FAR-30x collection acceptance |
| `H4` | Per-command uploads leave substantial CPU and Dawn-wire overhead: **327** `queue.writeBuffer(` and **188** `createBindGroup(` sites engine-wide; `WebGPUPrimitiveCommands.ts` alone holds 24 `writeBuffer` sites including the per-command pick writes; buffers round to 256 bytes. | CONFIRMED | `WebGPUPrimitiveCommands.ts:4004`, `:4029`; `WebGPUBuffer.ts:309-310` | FAR-303 (C9-12 BLOCKED) |
| `A2a-6` | The indirect-draw path is opt-in and nothing auto-flips it; the research record measured no candidate pass over the 3 ms bar. **Corrected:** the run-of-one branch that swallowed a throw was **fixed in Batch 1198** (`2a1c016c28`) — the reporter now goes through the deduped channel — so `FIX_QUEUE:1348` is **stale-green** and must not be carried as live evidence. | CORRECTED | `WebGPUContext.ts:725` (`useIndirectDrawForTiles = false`); `WebGPUPerformanceManager.ts:256-258`, `:411-414`; sole consumer `WebGPUSceneRenderer3DTilePasses.ts:284`; repair `WebGPUSceneRenderer.ts:654-679` | `RESEARCH_AND_PENDING_TOPICS.md` §R-7 (evidence) |
| `B5` | Tile indirect parameters are overwritten before recorded draws execute. Contained to `never` by default (`normalizeTileIndirectMode` fails closed); the shared-buffer overwrite across passes and frustums is unchanged, and **no per-frame indirect arena exists**. | CONFIRMED | `WebGPUSceneRenderer3DTilePasses.ts:103`, `:109-113`, `:116-135`; `WebGPUContext.ts:725`; `WebGPUSceneRenderer.ts:579-581`; `WebGPUIndirectDrawManager.ts:150-154` | FAR-501; **FAR-502 has no queue row** |
| `B4` | Hi-Z visibility is **global** while depth and command lists are **per frustum**: one `_lastHiZVisibility {flags,count}` and one in-flight readback, accepted on `prev.count !== count` alone. See §4 R1 — the ledger row that closes this contradicts the code and must be reconciled before it is quoted. | CONFIRMED | `WebGPUSceneRenderer.ts:1279-1283`, `:4174-4175`, `:1189`, `:1258`, `:4194-4198` | FAR-500 (rows in the closed CAMPAIGN8/9 queues + CAMPAIGN11); FAR-003 containment |
| `B6` | GPU sort reuses and may reallocate shared buffers before submission: one context-wide `_resources` set per dispatcher, one dispatcher per context in a `WeakMap`, written per opaque frustum. Safety rests entirely on the readback ring plus the `never` consumer default. | CONFIRMED | `WebGPUGPUSortKeysDispatcher.ts:151`, `:631-660`, `:662-700`, `:828`; `WebGPUSceneRenderer.ts:1360` (`_gpuSortConsumeMode = "never"`) | FAR-003 + FAR-105 landed; FAR-501 open; **FAR-502 none** |
| `H1` | The high-density path is **not yet GPU-driven**: readback loops in the culler and sort dispatcher, all automatic consumers fail-closed, and the 50,000-object policy is unchanged. | CONFIRMED | `WebGPUGPUCuller.ts:417-480`; `WebGPUGPUSortKeysDispatcher.ts:631-700`; `WebGPUSceneRenderer.ts:1360`, `:1189`; `WebGPUSceneRenderer3DTilePasses.ts:109-113`; `WebGPUPerformanceManager.ts:245-247`, `:1470-1485` | FAR-502..505 — **none has a queue row** |
| `H2` | Visibility inputs and readbacks lack identity and bounds safety: three zero-radius fallback sites (`bv.radius ?? bv.boundingSphere?.radius ?? 0`), count-only acceptance, and a `maxObjects ?? 65536` culler cap. The third site (the sort SOA) was not in the original list. | CONFIRMED | `WebGPUSceneRenderer.ts:3896`, `:4085`, `:4419`, `:4175`, `:4185-4186`; `WebGPUGPUCuller.ts:124` | FAR-500 |
| `S6.3-C11-187` | The conservative SOA pass-through landed (skip-rather-than-poison `continue`, `Math.fround` exactness checks, `radius <= 0.0` rejection); the **focused rerun is still open** and is unblocked by C11-133 (Batch 1018). | CONFIRMED | `Scene/SOABoundingSphereLayout.js:185-205` (`:188`, `:195-198`, `:205`); `QUEUE_2026-07-18_CAMPAIGN11.md:2129`, `:2536`, scope split at `:2220` | `C11-187` |
| `#120` / `A14.4` | Both the opaque and translucent GPU-cull dispatches allocate the bounding-sphere upload `Float32Array(count * 4)` fresh per dispatch. Hold a grown scratch. LOW; GPU cull is hysteresis-gated. | CONFIRMED | `WebGPUSceneRenderer.ts:3887`, `:4076` | **unowned** |
| `#143` / `A16.7` | The translucent GPU-cull readback ends in `.catch(() => {})`. Add a pragma-guarded log plus a permanent counter with a `console.error` past a threshold, per the sentinel rule. LOW. | CONFIRMED | `WebGPUSceneRenderer.ts:4115` | **unowned** |
| `#93` | The bitonic sort opens one compute pass **per (k, j) stage** — `encoder.beginComputePass` is inside the stage loop. Fold the stages into one pass with per-stage dynamic-offset params. LOW; GPU sort is threshold-gated. | CONFIRMED | `WebGPUGPUSortKeysDispatcher.ts:601` | **unowned** |
| `H5` | Pipeline cache identities are incomplete only in one residual place. The central fold is present and canaried (`pl:`, `pr:`, `dz:`, `mx:`, `sh:`, `wrongModuleHits`; `m:` on the compute side). The live residual is the OIT cache, which keys both its pipeline **and** its shader-module cache on shader length — see §3.8. | CONFIRMED | `WebGPURenderPipelineCache.ts:1063`, `:1070`, `:1079`, `:1089`, `:1100`, canary `:355`/`:460`/`:1265`; `WebGPUComputePipelineCache.ts:413-417`; residual `WebGPUOIT.ts:706` | Batch 825/828 + `Tools/visual-regression/pipeline-key-aliasing.spec.mjs`; FAR-304 for residuals (**no queue row**) |
| `H6` / `H-P19` | Synchronous first-use pipeline compilation is widespread: **140** `.createRenderPipeline(` and **54** `.createComputePipeline(` call sites against **1** executable `createRenderPipelineAsync` and **2** executable `createComputePipelineAsync` (counts corrected from 135/55/1/3). Ocean and ViewportQuad still compile on the frame. On the compute side `_ensurePipeline` resolves through `getOrCreateSync` → `createComputePipeline` on the hot compute-command path. The WebGPU prewarm/readiness policy is unowned. | CORRECTED (H6) / CONFIRMED (H-P19) | `WebGPURenderPipelineCache.ts:760`; `AsyncResourceMonitor.ts:400`; `WebGPUComputePipelineCache.ts:360`; `WebGPUOceanRenderer.ts:638`; `WebGPUViewportQuad.ts:308`; `WebGPUComputeEngine.ts:570`, `:601`, `:610` | C11-180 (WebGL half only); FAR-600 — **no queue row**; H-P19 **unowned** |
| `A2a-7` / `I4` | Prewarm coverage is partial. Present in the five collection renderers, globe surface shaders and renderer, depth plane, the boot set and the context's idle-init window. **Zero** prewarm in `WebGPUVoxelRenderer.ts`, `WebGPUProceduralCloudRenderer.ts`, `WebGPUVolumetricFogRenderer.ts`, `WebGPUCloudRenderer.ts`, `WebGPUModelPipelineCache.ts` — Cloud, Voxel, VolumetricFog, PointCloud and Model all compile on first use. Warming at dynamic-import settle is unowned. **Correction (I4):** the premise that Model keeps its own module `Map` "outside `WebGPUShaderModuleCache`" is **wrong** — the model cache holds a per-`GPUDevice` `WebGPUShaderModuleCache` in a `WeakMap` and resolves every module through its `getOrCreate`; the local `Map` is a memo in front of it. **This is the universal pattern**: twenty renderers construct their own per-device module cache the same way, so there is no shared central instance for Model to be outside of. Migrate only the prewarm half. | CONFIRMED (A2a-7) / CORRECTED (I4) | prewarm at `WebGPUBillboardRenderer.js:628`/`:635`, `WebGPUGlobeSurfaceShaders.ts:129`, `WebGPULabelRenderer.js:442`, `WebGPUPointPrimitiveRenderer.js:691`/`:697`, `WebGPUPolylineRenderer.js:978`/`:982`, `WebGPUSceneRendererEnsureResources.ts:535`/`:554`/`:567`, `WebGPUGlobeSurfaceRenderer.ts:746`, `WebGPUDepthPlane.ts:375`, `WebGPUContext.ts:1450-1466`; model module cache `WebGPUModelPipelineCache.ts:190-202`, `:2808`, memo `:2067`, `:2801-2823`; siblings `WebGPUCollectionRendererBase.ts:477`, `WebGPUGaussianSplatRenderer.ts:1019`, `WebGPUPointCloudRenderer.ts:72` | `DEFERRED_WORK.md:10551`, `:10611`; `FEATURE_INVENTORY.md:1212`; **none for the lazy family** |
| `A2b-3` / `X1` | Model keeps a private pipeline cache (4,751 lines) with a synchronous hatch that "never consults the central pipeline cache"; only the on-screen colour pipeline resolves centrally, with **13** deliberate direct `device.createRenderPipeline` hatches for the snap/pick/velocity variants. **Correction:** the fleet3 row cited as an open correctness finding on the same cache (display keys missing the generated-chunk class hashes) was **fixed in Batch 1209** (`79c9fa2ebb`) — `_metadataVariantKey` now folds the metadata and customShader class hashes across nine call sites, so `FIX_QUEUE:1335` is **stale-green**. `DX-10` still cites 4,591 lines. | CONFIRMED (A2b-3) / CORRECTED (X1) | `WebGPUModelPipelineCache.ts` = 4,751 lines; central resolve `:3449-3460`, `:2150-2155`; hatch `:867-869`, `:964`, comment `:1438`; hatches `:979` … `:1977`; repair `:2973` | `DEFERRED_WORK.md:10611`; `DX-10` (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:169`, `:950`); `FEATURE_INVENTORY.md:1128` already says re-scope before working it |
| `H3` | Main Scene submission is coherent but feature paths fragment it: **53** `queue.submit(` sites across `Renderer/` and `Scene/` (count updated from 52), including one in `Scene/PickDepth.js:300`; `_resumeScenePass` reloads colour, depth/stencil and the MRT slot-1 G-buffer. | CONFIRMED | `Scene/PickDepth.js:300`; `WebGPUSceneRenderer.ts:2037-2075` | FAR-200 S1/S2/S3, FAR-402/405/406 (**FAR-402/406 have no rows**) |
| `#109` | Per-frustum pass resume rebuilds attachment arrays with `map` + spread carrying `loadOp: "load"`. Pre-build the clear/load descriptor pair per resize. LOW. | CONFIRMED | `WebGPUSceneRenderer.ts:2054`, `:2205` | **unowned** |
| `HP-03` | Depth packing repeats full-screen work and object creation per frustum: the depth-copy bindings are cached by **texture identity only**, with no declared depth versions and no demand gating. | CONFIRMED | `WebGPUGlobeDepth.ts:181`, `:621`, `:648`, `:670`, `:689` | FAR-408 / FAR-405 (rows in the closed CAMPAIGN8/9 queues + CAMPAIGN11) |
| `HP-06` | Main-thread preparation budgets can consume most of a frame: the job scheduler guarantees progress on all job types each frame "even if it means exceeding the total budget", defaulting to TEXTURE 10 ms + PROGRAM 10 ms + BUFFER 30 ms = 50 ms. **Scope note the original omits:** this is **inherited upstream Cesium behaviour, unmodified by the fork** — file it as an upstream-divergence proposal (stage deadlines, backpressure), not as a fork defect. | CONFIRMED | `Scene/JobScheduler.js:59-60`, `:78-90` | FAR-210 (rows in the closed CAMPAIGN8/9 queues) |
| `HP-01` | Telemetry integrity and full-frame coverage are incomplete: GPU timing gates on `hasFeature("timestamp-query")`, and `unprofiledMs` is a reported field feeding a rolling window. **Honesty note:** the "dozens of unlabelled pass-creation sites" figure is a runtime-coverage measurement the verifier did **not** re-derive read-only; only the repair-and-gating half is verified. | CONFIRMED (structural half) | `WebGPUContext.ts:7020`, `:7040-7044`; `WebGPUTimestampProfiler.ts:92`, `:697` | `Q-143` / `DM-09`; measurement authority is the `DEBUGGING_GUIDE.md` moving-altitude campaign |
| `HP-04` | Settled frontend allocation and closure churn remain unmeasured, but **the number is corrected**: the settled-frame residue is **6 bind-group creations per frame** (Sun ×2, TAA ×2, canvas-blit ×2), not 14 — 14 was the pre-slice-A figure. No settled-heap or object-plateau gate exists. Polyline repacks per H10. | CORRECTED | `QUEUE_2026-07-15_CAMPAIGN9.md:148` (C9-17 A-C landed, 560→240 over 40 frames), `:157` (C9-11 PARTIAL/PAUSED) | C9-17 slice D + C9-11 / FAR-309 |
| `#91` | The pipeline cache double-counts misses: `stats.misses++` fires in warm, sync-probe **and** async-acquire paths and no `warmed` counter exists, so one logical acquisition can count up to three misses and `CesiumDebug.cacheStats()` reads a low hit rate. Add a warmed counter and guard the sync-probe path. LOW, instrument. | CONFIRMED | `WebGPURenderPipelineCache.ts:568`, `:663`, `:718` | **unowned** |
| `#92` | `TimestampProfiler` drops passes beyond the cap — **the counter is further along than recorded.** `_droppedPassCount` is already emitted as `droppedPassCount` in **both** report objects, so "nothing reports the counter" is wrong. The gap is the last hop only: `Scene/CesiumDebug.js` has no `dropped` reference in `gpuPassCost()`, so a truncated profile stays invisible at the console. Surface the field in the `console.table` output. LOW, instrument. | CORRECTED | `WebGPUTimestampProfiler.ts:319`, `:439`, `:469` (counted), `:794`, `:847` (reported); `Scene/CesiumDebug.js` `gpuPassCost()` | **unowned** |
| `F5-18` / `ACTION-8` | The MSAA depth resolve is **unconditional**: dispatched whenever the method and encoder exist with no demand check, the `r16float` target allocated whenever `wantSampleable && isMSAA`, and `depthSamplable: true` set unconditionally. Inventory every reader first (TAA, motion blur, aerial perspective, god rays, DoF/AO, SSR/NPR/contact shadows, clouds/fog/ground fog, picking, unknown), let unknowns **force** the resolve, and assert demand separately from resolve opens (default MSAA4 with no reader = zero opens; each real reader = exactly one; TAA at MSAA1 = demand without a resolve pass). | CONFIRMED | `WebGPUSceneRendererPostFrustumChain.ts:94-105`; `WebGPURenderTarget.ts:256-274`, `:288-302`; `WebGPUSceneFramebuffer.ts:328` | `DEFERRED_WORK.md:10481` S4-4 under C10-03; `DM-12` calls it adjacent |
| `F5-19` / `ACTION-10` / `REJECT-7` | Eager pass reopening and globe-depth packing are still uninstrumented: the slot-1 and scene-colour open counters exist, but there is **no `passOwner` and no `globeDepthPack` symbol anywhere** under `Source/Renderer/WebGPU`, no lazy "open at first work" scene pass, and no gate on a post-globe pack whose frustum emitted zero globe commands. **REJECT-7 stands with its citation corrected:** the `pickPassOverhead` note is in `FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json:632`, not in the plan `.md`. Per-frustum depth/stencil clears are load-bearing (multi-frustum depth plane, translucency, classification, picking, post-process); fold or gate a boundary only after its owner and reason are named and its successor proven equivalent in clear/load/store order. | CONFIRMED (F5-19) / CORRECTED (REJECT-7) | `WebGPUContext.ts:739-760`; 0 hits for `passOwner` / `globeDepthPack`; `FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json:632` | `DEFERRED_WORK.md:10481-10482` S4-5 / FAR-405-706 under C10-03 |
| `S8.3-empty-scene-pass-transitions` | About four **residual** avoidable empty scene-pass transitions per measured frame, after C9-07 had already removed two. **Ownership corrected:** the first reader's "no queue row, no ledger entry, surviving record: none" is wrong — FAR-405 is `active` and owns this axis, with the acceptance "no empty passes" and a measured 23.34 → 14.49 instrumented passes/frame. Re-measure on the Batch 1032 campaign runner and record against FAR-405 rather than minting a new row. Passes carrying a required clear or resolve are not candidates. | CORRECTED | `WebGPUContext.ts:2263-2267` (canvas pass deferred to first demand precisely because eager opening "produces two empty passes per frame"); `WebGPUSceneRenderer.ts:3646`, `:3660`; `FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:300`, `:765`, `:1232`; `DEFAULT_PARITY_MATRIX_2026-07-18.md:49`; `QUEUE_2026-07-15_CAMPAIGN9.md:131` | **FAR-405** (`FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json`, `"state": "active"`) |
| `S8.3-52-env-pipeline-packs` | The 52 duplicated dynamic-environment pipeline packs are addressed in code — one kernel pack per device generation, keyed by `resourceGeneration` and storage format — but the fix is **unmeasured**: no artifact re-counts the packs on a post-`b20234a16b` bundle. | CONFIRMED | `WebGPUDynamicEnvironmentMapManager.ts:357-371`; `QUEUE_2026-07-18_CAMPAIGN11.md:2186` (PARTIAL; residual allocation cleanup and HQ reuse open) | `C11-193` |
| `S9-3-C11-193` | The shared scheduler / demand registry is the next `C11-193` slice: A/B/C landed at `b20234a16b` with private encoders/submits at 0/0; the OPEN list still covers moving causal/FPS attribution, HQ reuse, replacement-device / multiview / reentrant certification and residual allocations. | CONFIRMED | `QUEUE_2026-07-18_CAMPAIGN11.md:2186`; `DEFERRED_WORK.md:14225-14232` | `C11-193` |
| `S9-4-C11-194-195` | Shared immutable model defaults (`C11-194`) and the dynamic view/light uniform arena (`C11-195`) landed; their **certification remainders** are the live part. **The architectural half must not be carried:** the first reader asked the new review to determine whether this is a live ADR-3 ownership violation, and **it is not** — the mutable arena is **context-owned, not pooled**, in both the code and its comments, and the device pool holds no arena at all (Batch 819 `ad43f8c527`; Batch 822 `0105e7c42d` records the epoch guard, dynamic offsets, group-0 layout and context ownership all untouched). What survives is a doc-truth item: the `C11-195` row still carries the 2026-08-02 audit clause as open and should be reconciled. | CORRECTED | `WebGPUModelPipelineCache.ts:2274-2280`, `:2441-2459`; `WebGPUModelDeviceResources.ts:14-68`, `:80`; `WebGPUContext.ts:6341`, `:6438-6451`; `WebGPUModelCameraArena.ts:91-99`, `:303-314`; stale clause `QUEUE_2026-07-18_CAMPAIGN11.md:2188` | `C11-194`; `C11-195` |
| `ACTION-2` | Attribute **both** bottlenecks. The WebGL half is proven and reduced to four stalls; the WebGPU steady-state floor has a ledger and a certified deficit (9.2025 ms vs 4.65 ms CPU-p95) but **no named owner**. The dense-scene starvation lane is a separate defect and must not be read as the moving-globe floor. | CONFIRMED | `QUEUE_2026-07-18_CAMPAIGN11.md:2003` (`C11-168` PARTIAL / VALID CAUSAL DEFICIT, ROOT CAUSE OPEN), `:2122` (`C11-180` PARTIAL, Batch 773); `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:164-166` | `C11-168`, `C11-169`, `C11-180`; `Q-143` / `DM-09` |
| `ACTION-3` | Generic **post-submit buffer retirement** does not exist: the deferred-destroy machinery covers textures only, and there is no `scheduleBufferDestroy` and no `_pendingBufferDestroys` anywhere in `packages/engine/Source`. Generalise the batch into a resource-retirement queue for buffers **and** textures keyed by the submit that last used them — one batch per frame, never one `onSubmittedWorkDone` promise per tile — behind a failing lifecycle test that destroys a resource used by an already-recorded command. | CONFIRMED | `WebGPUContext.ts:553-555`, `:2932`, `:3379`, `:5421` | **unowned** (prerequisite of `C11-34` / FAR-203) |

### 3.10 Architecture, layering & build

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `D1` / `D4` / `H9` | **`RenderCommand` has zero importers.** The "3 Scene files adopt RenderCommand" figure is a grep artefact and must never be carried forward: the only references to the identifier are doc comments, and the three files named adopt nothing — they carry locally defined functions whose names merely contain the string. 25 Scene files construct `DrawCommand` directly. The debt is **stronger** than recorded: the abstraction is unreferenced, not half-adopted, and the decision (wire it, or retire it) is unowned. See §4 R4. | CORRECTED | `Renderer/WebGPU/RenderCommand.js` (whole file, no importer in `packages/*/Source`); references at `Renderer/GraphicsContext.ts:1903-1904`, `Stubs/WebGLStubShader.ts:16`, `:392`, `WebGLCompatibilityStub.ts:102`, `:128`, `:134`; look-alike locals `Scene/ClassificationPrimitive.js:1300`, `Scene/GroundPrimitive.js:802`, `Scene/QuadtreePrimitive.js:1365`; barrel `packages/engine/index.js:1025`; `FEATURE_INVENTORY.md:512` tags it SCAFFOLDED / UNUSED | **unowned** for the adoption decision; `C11-24` fixed only the pass slot |
| `D3` / `B1` (feature-inventory) / `A1b-4` | Build-variant compat-exemption drift and one-way enforcement. The list holds **five** entries — `WebGLCompatibilityStub`, `WebGPUShaderTranslator`, `WebGLStubPipelineExtractor`, `WebGPUNagaTranspiler`, `WebGPUModelMetadata` — and **not** `RenderCommand`; `CLAUDE.md:437` still names four. The plugin's prefix match over-exempts, and the file riding the `WebGPUModelMetadata` entry today is `WebGPUModelMetadataVariantKey.ts`. The spec proves listed entries pass through; **nothing proves a backend-neutral file is listed**, and that negative check remains unbuilt. **Corrected consequence:** because nothing imports `RenderCommand.js`, the webgl-only throwing-stub path is currently unreachable, so the queued row is a latent-on-adoption hazard rather than a live defect. **`BUILD-VAR-MEASURE` does not close:** the backlog asks for minified **and gzipped** sizes for `Cesium.js`, `index.js` and **each split chunk** across all three variants; CLAUDE.md records only the minified IIFE figures. Re-scope it to the measurements CLAUDE.md does not carry. | CORRECTED | `scripts/bundleVariantPlugin.js:276-286`, substring match `:310-314`; `scripts/__tests__/bundleVariantPlugin.spec.mjs:266-278`; `Tools/variant-smoke-test.mjs:18-24`; `CLAUDE.md:437`; `WEBGPU_MIGRATION_BACKLOG.md:319`; `FEATURE_INVENTORY.md:1129` | `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1381`, `:1404`, `:1405` (fleet1, QUEUED) |
| `D5` / `C4` | Residual `context.isWebGPU` branches in Scene: **four** control-flow branches, not three — `GlobeSurfaceShaderSet.js:1117` (forces the non-parallel shader path on WebGPU), `OceanSurfacePrimitive.js:384` **and `:502`** (WebGPU-only primitive no-ops on WebGL), `ViewportQuad.js:144`. The import boundary is clean (**0** imports from `../Renderer/WebGPU/` in `Scene/`, and exactly one sanctioned engine-wide, `Scene/Model/MetadataWGSLPipelineStage.js:60`), and the feature-renderer census is 43 Scene files. **A second family the original does not count at all:** Scene branches on backend **command shape** in eight more places reading `command.isWebGPUDrawCommand` — a different axis, arguably legitimate dispatch, but a "remaining branch points" list that omits it is misleading. **Ownership corrected:** two of the four sites are owned; the live finding is that the owning row's own verification claim is **stale** — it asserts no remaining `isWebGPU` branch in rendering logic except ViewportQuad, which HEAD contradicts. Re-verify it, and either sanction the two re-accreted sites as capability gates or convert them. Correctly excluded as non-branches: the public getter, the debug label, and seven `CesiumDebug.js` guards. | CORRECTED | `GlobeSurfaceShaderSet.js:1117`; `OceanSurfacePrimitive.js:384`, `:502`; `ViewportQuad.js:144`; command-shape family `Scene/Scene.js:3594`, `:3599`, `:3697`, `:6109`, `View.js:663`, `WebGLShaderProgramScheduler.js:49`, produced at `GlobeSurfaceTileProviderRendering.js:1459`, `:1569`; getter `Scene.js:2793-2794` | `DEFERRED_WORK.md:4868` (`NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE`, owns `ViewportQuad.js:144`) and `:4867` (`NEW-CAPABILITY-GETTER-CODIFY`) |
| `#116` / `A13` | The capability-getter convention (Scene code branches on `context.supportsX` getters, never `isWebGPU` / `rendererType`) is enforced in code since Batch 303 but is **still absent from CLAUDE.md §2** — 0 matches for `capabilit` in the file. Add the one-sentence rule when the campaign-state pointer edit lands. | CONFIRMED | `CLAUDE.md` (0 matches); residual branch `Scene/ViewportQuad.js:144` | `DEFERRED_WORK.md` `NEW-CAPABILITY-GETTER-CODIFY` + `NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE` |
| `R4` (performance) | **No automated Scene-boundary guard exists.** `eslint.config.js` carries no `no-restricted-imports` for `Renderer/WebGPU`, and the three CI guard scripts contain **0** `isWebGPU` hits. **Two supporting citations corrected:** the `Scene.js` `isWebGPU` light overwrite is **fixed in code** — the light is now published unconditionally and derived lighting deferred to `_alternateSceneRenderer.updateDerivedLighting` — so `FIX_QUEUE:1350` / `Q-1` is **stale-green**; and `GlobeSurfaceShaderSet.js:1086` has moved to `:1117` and is still live. The guard itself has never been built. | CORRECTED | `eslint.config.js`; `Tools/ci-guards.spec.mjs`, `Tools/verify-landing-compliance.mjs`, `Tools/pre-push-guard.mjs`; live branches `GlobeSurfaceShaderSet.js:1117`, `OceanSurfacePrimitive.js:384`/`:502`, `ViewportQuad.js:144`; repair `Scene/Scene.js:3790-3798` | `FAR-308` (`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:691`, an L behind FAR-103/200/300); `FIX_QUEUE:1401` |
| `A1a-3` | Every lifecycle slot on the `FeatureRenderer` tree is still typed `(...args: unknown[])`, the base owns every optional slot so no caller has to narrow, and parity across the registered renderers is enforced only by duck-typed existence checks. | CONFIRMED | `Renderer/GraphicsContext.ts:324`, `:331`, `:340-342`, `:466`, `:485-505`, `:526` | **unowned** |
| `R7` / `T10` / `N8` | `_scenePipelineFormatGeneration` is a **manual contract**: an epoch field and getter (WebGL returns 0), **31 files** reading it ad hoc, no interface, no `recompileForSceneFormat` symbol anywhere, and **no on-hit format check** in the pipeline cache — nothing at compile or lint time says which renderers are format-aware. **Two corrections.** (a) The "predicted failure has materialised" evidence is out of date: the globe material cache is now cleared alongside the production, wireframe and debug caches (Batch 1185, `3ebd7e193e`), so `FIX_QUEUE:1332` is **stale-green**. (b) The first reader's "no queue row" is wrong: a row exists and **records the predicted failure as an observed, reproduced defect** — per-renderer guards rebuild one frame late relative to the scene-pass swap, producing "Attachment state … not compatible" plus an invalid command buffer on runtime `scene.msaaSamples` flips, measured at 50 occurrences on a 2026-07-16 baseline and reproducible via `probe-hdr-pick-format-closure.mjs`. **Elevate as one finding**, not three scattered list entries: manual contract + 31 ad-hoc readers + a recorded reproduction + (per F7 in §3.11) no post-first-frame validation scope to catch the recurrence. | CORRECTED | `WebGPUContext.ts:911-922`, `:922` (field), `:933-943` / `:942` (getter), WebGL `Renderer/Context.js:818`; 31 reading files; no format assertion in `WebGPURenderPipelineCache.ts`; repair `WebGPUGlobeSurfaceRenderer.ts:1274-1277` | **`NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION`** (`QUEUE_2026-07-15_CAMPAIGN9.md:124`, **NOT STARTED**); `ROADMAP_AND_DEFERRED_WORK.md` Q14-HDR-TOGGLE-INVALIDATION |
| `R9` | `ContextRegistry` is a **per-module static**, with no `globalThis` / `Symbol.for` anchor. Two loaded copies of `@cesium/engine` yield two registries and split-screen contexts cannot discover each other; `ARCHITECTURE.md` §2 states the multi-context guarantee without the caveat, and nothing detects the condition. | CONFIRMED | `Renderer/GraphicsContext.ts:749` (`private static _registry`), register `:833`, unregister `:908` | **unowned** (the 2026-07-13 audit's `ContextRegistry` note is about an init-failure leak, not module identity) |
| `R1` / `R2` (performance) | File-size risk, re-censused: `WebGPUContext.ts` = **7,914** lines (against the 4,363 the original flagged, after eight companion extractions), `WebGPUSceneRenderer.ts` = **5,140** with thirteen companions on disk. The originally proposed context seams are all still in the file and the decomposition row should reuse the seam list; for the scene renderer, FrustumLoop and PickPass landed as companions while refraction capture, velocity pass, edge-overlay composite and the post-process handoff are still inline. Wider census: `WebGPUModelRenderer.ts` 9,284, `WebGPUGlobeSurfaceRenderer.ts` 3,080, and **56 of 277** files under `Renderer/WebGPU` exceed 1,000 lines. The 2026-04 numbers are worthless as a baseline. | CONFIRMED | `WebGPUContext.ts` 7,914 lines, inline caches `:5594-5631`; `WebGPUSceneRenderer.ts` 5,140 lines | `DX-08`, `DX-09`, `DX-07`, `DX-10` (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:166-169`, `:931-948`); `WEBGPU_CONTEXT_DECOMPOSITION_PLAN.md`; `ARCHITECTURE.md` §8 / §8.2; `FIX_QUEUE:1384-1385`, Q-10 |
| `#184` / `B5` (ultra) | `Scene/GlobeSurfaceTileProviderRendering.js` has grown from 1,818 to **2,657** lines and no decomposition row covers it — the Wave DX rows are `Renderer/WebGPU` only, and its `FIX_QUEUE` appearances are landing-conflict notes and a `console.warn` row. Add it to the Wave DX decomposition list or exempt it explicitly. LOW, DEBT. | CONFIRMED | `Scene/GlobeSurfaceTileProviderRendering.js` = 2,657 lines; `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:487`, `:626`, `:643`, `:1266` | **unowned** |
| `H-R6` / `L-R1` / `L-P1` | `WebGPUShaderCache` keys on `descriptor.name` **verbatim** — the name **is** the whole key, so two different WGSL bodies registered under one name serve the wrong module — and `isDestroyed()` returns a literal `false`. **Reachability corrected, in the safe direction:** the class has **zero engine instantiations**. `context.shaderCache` returns the **WebGL** `ShaderCache`; the WebGPU field is declared but nothing ever assigns it, and its only other reference is a device-invalidation clear callback. The ledger states this independently. The defect is **unreachable dead surface**, not a latent live path: **retire the class**, the field and its clear callback in the DX decomposition wave — hashing the key is the wrong fix for code nothing runs. Its spec file is the runner home. | CORRECTED | `WebGPUShaderCache.ts:414-418`, `:430`; `WebGPUContext.ts:655` (field), `:7603` (clear callback), `:39` + `:1196` (the WebGL cache is what `shaderCache` returns); `DEFERRED_WORK.md:6039` | **unowned** |
| `H21` | Shader preprocessing and module-cache identity are fragmented. The **module cache is fixed** (full 32-bit lo mask plus a `definesHi` tier). Residuals: the second cache above (name-as-whole-key); a bare-`#import` regex in the buffer-primitive renderer; and lockstep-marker coverage of **10 of 324** `.wgsl` files, not 20/324. | CORRECTED | `WebGPUShaderModuleCache.ts:20-49`; `WebGPUShaderCache.ts:414-418`; `WebGPUBufferPrimitiveRenderer.ts:552-570` | FAR-305 |
| `M-R10` | `WebGPUContext.clear()` ignores `clearCommand.renderState` **entirely**, where WebGL applies the whole state. **Scope widened** from `colorMask`: the WebGPU `ClearCommand` model carries no `renderState`, `clear()` never reads one, and 0 `colorMask` hits exist. `colorMask` indeed has no shipped consumer, but **`stencilMask` has two** — `Scene/Scene.js:448-453` (`_classificationStencilClearCommand`) and `Scene/Cesium3DTileset.js:3558-3564` (`_stencilClearCommand`) — and a WebGPU `loadOp:"clear"` clears the whole stencil aspect regardless. Neither fires today because the WebGPU frustum loop never dispatches list `ClearCommand`s at all (0 `clearCommand` hits in the scene renderer and its frustum loop), i.e. the WebGPU path does not run the bivariate-visibility or classification-stencil clear dance. Keep it as a **latent parity gap** naming `renderState` with `stencilMask` as the consumer to watch. | CORRECTED | `WebGPUContext.ts:318-327`, `:4414`; WebGL `Renderer/Context.js:1331-1332` | **unowned** |
| `M-R11` | `depth-clip-control` is never enabled: the pipeline cache forwards `primitive.unclippedDepth` and folds it into the key — the only two occurrences — with **no producer**, and `"depth-clip-control"` has zero occurrences under `packages/engine/Source`; `requiredFeatures` is user-supplied only. Log depth via `frag_depth` is why this has not bitten; keep it a documented non-goal until a probe shows a difference. | CONFIRMED | `WebGPURenderPipelineCache.ts:826`, `:1072`; `WebGPUContext.ts:384`, `:1318` | **unowned** |
| `M1` | WebGPU-only feature flags conflate requested and active state. The dense family has a real `UnsafePathStatus {requested, capable, active, fallbackReason}` with stats exposed on `Scene`; the other flags remain plain mirrors with doc-only semantics ("Has no effect on the WebGL path"), including `clusteredLightingEnabled` as a bare get/set. | CONFIRMED | `WebGPUSceneRenderer.ts:890-899`; `Scene/Scene.js:2249-2254`, `:3310`, `:3333`, `:3362`, `:3402-3408`, `:3413`, init `:1228` | FAR-003 + `DEFAULT_PARITY_MATRIX_2026-07-18.md` + the C11 ratified decisions |
| `LEDGER-IDS` | The finding-ID namespace has two undefined ledger keys and stale states. The plan states "the audit intentionally has no H15 identifier — preserve that numbering". The ledger's `findings{}` map holds 47 keys including **`B13`** and **`B14`**, both `{packages:["FAR-102","FAR-103"], state:"active", gate:"correctness"}` with **no audit definition anywhere** (see §5.3). States are stale: `H5` reads `"planned"` though the fold landed at Batch 825/828; `B9` and `H11` read `"active"` though closed. The ledger's `audit` field and the plan's `:9` link both break on archival and must be repointed in the same change. | CONFIRMED | `FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:1175`; `FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json:9` and `findings{}` | FAR plan §10 + the ledger |
| `ADOPT-MATRIX` | The 2026 WebGPU adoption matrix holds: the transient-attachment probe still reads through `Reflect.get(GPUTextureUsage, "TRANSIENT_ATTACHMENT") ?? 0x10` and probes both feature spellings; `featureLevel: "compatibility"` is wired; video moved to `importExternalTexture`; **no occlusion-query use anywhere** in `Renderer/WebGPU`. Read the video row with the `C.6` caution in §3.1: the manager importing an external texture is not video imagery using it. | CONFIRMED | `WebGPUFramebufferManager.ts:54-69`; `Renderer/ContextFactory.ts:153`; `WebGPUVideoTextureManager.ts` | FAR-700..707 (experimental) + `RENDERER_LANDSCAPE_AUDIT_2026-09-02.md` |
| `F5-05` / `ACTION-12` / `REJECT-8` | The `SceneOctree` plane-mask never matches the `Intersect` enum — **mechanism corrected.** `visibility === Intersect.OUTSIDE` (−1) can never match `MASK_OUTSIDE` (`0xffffffff`), so **no subtree is ever rejected**; that half stands. But "the mask short-circuit never fires" is wrong: `MASK_INSIDE` is `0` and the `else` arm passes `childMask = visibility`, so a fully-inside node hands children a zero mask and the plane test returns early; an *outside* node propagates `0xffffffff`, which also short-circuits — **while every command in every descendant is still collected**. The cost is the full traversal plus emission, not the plane arithmetic. The repair is the whole opt-in semantics (eligibility, root bounds, translucent 3D Tiles, ordering, off-camera shadow casters) — **never the one-line enum fix**, which would switch on culling whose eligibility, bounds, ordering and shadow-caster semantics are still unspecified. | CORRECTED | `Scene/OctreeNode.js:295-300`, `:321-325`; `Core/CullingVolume.js:196`, `:205`, early return `:80-84`; `Core/Intersect.js:18`, `:33` | **unowned** (adjacent: `DEFERRED_WORK.md:10183`) |
| `F5-06` / `ACTION-12` | An **enabled** `RenderScheduler` bins and sorts layers neither renderer consumes — the file says so in its own comment — while defaulting to `enabled ?? false`. Either layer enablement/order/clear-depth actually drives both renderers' command order, or the scheduler retires. An enabled scheduler that is inert is an advertised feature that does nothing. | CONFIRMED | `Scene/ViewportExecutor.js:777-797` (comment `:777-779`, `binCommand` `:791`, `sortAllLayers` `:797`); `Scene/RenderScheduler.js:31` | FAR-504 "retire/adapt the legacy RenderScheduler" (planned, plan `:845`) |
| `REJECT-3` | **Never** route globe terrain, 3D Tiles traversal or the voxel octree through the optional general `SceneOctree`. This is a standing campaign rule, not a proposal. | CONFIRMED | `QUEUE_2026-07-18_CAMPAIGN11.md:2367-2371` | C11 queue rule `:2369` |
| `F5-11` / `ACTION-14` / `REJECT-6` | The `ResourceOwnership` scaffold has **no consumer**: eight files whose only occurrences of the string are three self-references. Adopt **only** through one concrete, measured, zero-copy consumer — the terrain lease of `F5-02` is the natural first — after a zero-copy payload contract exists. **Never integrate wholesale.** | CONFIRMED | `Renderer/ResourceOwnership/` (8 files, no importer); `DecodedPayload.ts:20-26`, `:45-47` | FAR-200 ("shadow infrastructure only … 52 direct `queue.submit` sites remain", plan `:72`, `:84`); `C11-75..78` NOT STARTED |
| `S6.6-S10-generated-barrel-trap` | The generated engine barrel assumes every module has a default export: `createIndexJs` emits `export { default as <name> } from './<moduleId>.js'` for every globbed module, unconditionally. **Three corrections.** (1) "No tracked document carries it" is wrong — a handoff states the build-before-landing rule — but a handoff is not a durable procedure home, so the drift still deserves a standing home. (2) The glob is `packages/engine/Source/**/*.js` only, so the trap is **JS-leaf-specific**; a named-export-only `.ts` leaf is unaffected. (3) The generator was **not** taught to tolerate named-export-only leaves despite a commit title saying so — that commit is a one-line addition to the `!`-exclusion list, and a later batch added a second line the same way. The mitigation is an opt-out list a new file's author must remember, which is exactly why the class recurred twice (Batches 1233 `d159d7709d`, 1274 `8accef6f45`). The honest options remain a tolerant generator or a build-before-landing gate. | CORRECTED | `scripts/build.js:1520-1550` (emit at `:1549`), glob and opt-out `:352-367`; `CODEX_HANDOFF_2026-08-29.md:90-91` | **unowned** as a standing rule |
| `#99` / `A11.5` | `FRONT_AND_BACK` maps to `"back"` with a comment saying the caller should detect the case and skip the draw; **no caller performs the skip.** Unreachable in shipped scenes but a silent wrong-output path if a render state ever asks for it. LOW. | CONFIRMED | `RenderStateToPipelineVariant.ts:228-232` | **unowned** |
| `#100` / `A11.6` | `GL_CONSTANT_ALPHA` / `GL_ONE_MINUS_CONSTANT_ALPHA` are commented out as "not mapped by WebGPU" and fall through to `return "one"`, while the same function already maps the constant-**colour** factors to `"constant"` / `"one-minus-constant"` — WebGPU's blend constant covers the alpha channel too. Unreachable today. LOW. | CONFIRMED | `WebGLStateConverters.ts:33-34`, default `:94`, working pair `:88-91` | **unowned** |
| `#102` | `detectIndexFormat`'s byte-size heuristic (`bytesPerIndex = size / indexCount; if (>= 4) "uint32"`) misclassifies a 4-byte-padded uint16 buffer with `indexCount === 1`. It is a fallback (renderers pass `indexFormat` explicitly), so keep it as a debug assert only. LOW. | CONFIRMED | `WebGPUDrawCommand.ts:552-566`, used at `:480` | **unowned** |
| `#107` / `A12.3` | Per-frame render state is smuggled onto `uniformState` via casts — **both the location and the scope in the original text are wrong.** The frustum-loop line cited is a different cast. The real publisher is the globe camera UB, which writes `_logDepthEncodeNearFar` and `_logDepthEncodeFactor` through `uniformState as unknown as {…}`, and its own comment says it runs **before** the per-slice loop on the full frustum — so it is **frame-wide** state, not per-slice. The surviving finding: three consumers read an untyped, cast-declared field off the shared `UniformState`; a typed `LogDepthEncodeState` carrier would finish the cleanup. LOW. | CORRECTED | publisher `WebGPUGlobeSurfaceCameraUB.ts:1148-1161`; declaration `Renderer/WebGPU/cesium-js-types.d.ts:650`; readers `WebGPUBillboardRenderer.js:697`, `WebGPUContactShadowsEffect.ts:231`, `WebGPUDepthPlane.ts:836` | **unowned** |
| `#146` | The variant plugin's `decisionCache` is never validated against esbuild: the plugin documents the deliberate synthetic-path optimisation with no sampled `build.resolve()` cross-check. Add a non-production sampled assertion, or document the explicit-`.js`-extension invariant it relies on. LOW. | CONFIRMED | `scripts/bundleVariantPlugin.js:362-396` | **unowned** |
| `#147` / `#150` / `#153-#156` / `B1` (ultra) | The **fork-added public API surface is uncatalogued.** The class conversions were declined as reverts and the latent-conflict premise dissolved with the v1.141-1.143 and v1.144 syncs, but roughly 31 fork-added `Scene` accessors (`enableSSR`, `enableNPROutlines`, `gpuCullingHint`, `useHDRCanvasOutput`, `taaEnabled`, `snapshotMode`, …) plus the `Globe` `cloud*` / `atmosphericConditions` / water facades are tracked nowhere as a list. Add one so upstream syncs and the C16 attribution pass can see what the fork exports beyond upstream. LOW, doc. | CONFIRMED | `FEATURE_INVENTORY.md` has no fork-added-public-surface section (the only `public surface` hit, `:671`, is the weather-ingest module list) | **unowned** |
| `#111` | A local variable named `any` (`let any = false;`) reads like the banned type. Rename on next touch. Trivial. | CONFIRMED | `WebGPUSceneRenderer.ts:5064` | **unowned** |

### 3.11 Survivability, embeddability & device loss

**This section is the repoint target for `ARCHITECTURE.md:969-974`**, the only live pointer that sent a reader into a retired file _for content_. It receives the whole survivability set: F1, F2, F3, F4, F5, F7, F8, F9, E1, E2, P1-P5, X2-X6, plus T10 (elevated in §3.10) and R5 (in §3.13), both pointer rows below.

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `F1` / `T5` / `S6.1-C11-20` | Device-loss recovery is real (a state enum, three attempts, 744 lines) but the **per-object cache walk is incomplete**. The walk's own docstring scopes it to owners exposing a literal `_webgpuCache`; `LabelCollection._webgpuLabelCache` is a differently-named field the walk cannot see, cleared only by the renderer's own teardown. The owning row is PARTIAL and says so in terms ("nested/device-loss walk open"), with a single landing commit. | CONFIRMED | `WebGPUDeviceLossRecovery.ts:27-31`, `:217`, `:240`; `WebGPUSceneRendererEnsureResources.ts:233`, `:583-604`, `:652-653`; `WebGPULabelRenderer.js:819-820`, `:1348`; `Scene/PointPrimitiveCollection.js:886-893`, `Scene/LabelCollection.js:1052-1064`; `QUEUE_2026-07-18_CAMPAIGN11.md:1639`, `:2452`, narrative `:1311-1313` | `C11-20` / `C-R12-PER-OBJECT-CACHES` (`DEFERRED_WORK.md:8360`) / FAR-208 |
| `F5-08` / `ACTION-11` / `GATE-3-ACCEPTANCE` | The device-loss walker misses whole cache families. Label and polyline renderers hold device-created bind groups and buffers in per-collection caches with **no bus subscription and no device-generation field** (grep across both files returns nothing). The model-feature-pick teardown half is closed and the async invalidation **epoch** is done and verified — every invalidation entry point bumps it and the async resolve re-checks both `_sceneFormatGeneration` and `_lifecycleEpoch` before writing back. What remains is **an owner protocol for every per-object `_webgpuCache` family** on device loss, plus the two-context shared-device recovery test. Acceptance (GATE-3): after a device loss every per-object cache family (labels, polylines, billboards, clouds, voxels, translucency state) recovers on the new device **without a scene-format bump**, in a two-context shared-device scene. | CONFIRMED | `WebGPUContext.ts:1369`, `:7566`, `:7657`; `WebGPULabelRenderer.js:819-843`, `:1080`; `WebGPUPolylineRenderer.js:1573-1598`, `:475`, `:1685`, `:1727`, `:1933`, `:2094`; epoch `WebGPUModelPipelineCache.ts:2026-2044`, bumps `:3110`, `:3197`, `:3243`, `:3291`, `:3349`, guard `:3493-3529`; `WebGPUModelFeatureId.js:1433`, `:1947` | `Q-86` for the multi-context test; **no row for the owner protocol** |
| `H8` | Device-loss and teardown generation handling is incomplete — **corrected in both directions.** The **refuted** sub-claim: the Hi-Z and sort dispatchers are no longer context-keyed `WeakMap` singletons that can outlive a device generation; both revalidate through `shouldRebuildForDevice(inst, context.device)` and destroy/rebuild on mismatch. The **surviving concrete residual**: `_msaaDepthResolveTexture` is created and its views read, but `destroyTextures()` destroys colour, resolve and depth attachments and **never this texture** — and it leaks again on every `resize()`. Plus the absence of the FAR-601 teardown-to-baseline audit. | CORRECTED | `WebGPURenderTarget.ts:257` (create), `:267` (views), `:699` (re-create on resize), `:705-722` (destroy list, omits it); `WebGPUGPUSortKeysDispatcher.ts:828-850`; `WebGPUHiZOcclusionDispatcher.ts:1004-1020` | FAR-208 active; **FAR-601 has no queue row** |
| `H-P6` | The device-loss cache clear covers a short subset of resources: a registry of 21 registrations feeding `_clearAllCaches`. | CONFIRMED | `WebGPUContext.ts:7566`, `:7568-7645`, `:7657`, `:7862` | `DEFERRED_WORK.md:10721` `S6-ENV-DETERMINISTIC-DESTROY-DEVICE-RECOVERY`, OPEN |
| `H-P2` | No device-generation tracking for in-flight async work — **one of the two named stragglers is closed.** The splat async-pipeline-promise defect **landed at Batch 1192** (`WebGPUPipelineRequestGuard`, consumed and invalidated in the splat renderer); the dossier table still lists it and reads as open, and it is not. Cite instead the environment ownership contract (whose fix text explicitly names "cancel or generation-reject in-flight asynchronous work") plus the `Q-49` blind window. The generation counter and getter exist and 313 `resourceGeneration` sites reference them. | CORRECTED | `WebGPUContext.ts:928`, `:946`; `WebGPUPipelineRequestGuard.ts:18`, `:33`, `:55`; `WebGPUGaussianSplatRenderer.ts:1404-1457`, `:3138-3150`, `:2194`; stale row `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1333`, dossier `:50` | `DEFERRED_WORK.md:10721` (OPEN); `Q-49` |
| `H-P3` | Canvas reconfigure race: `resize()` and the canvas-config `configure()` sites can interleave; the guard field exists but the race is not closed. | CONFIRMED | `WebGPUContext.ts:4655-4662`, guard field `:594`; `WebGPUContextCanvasConfig.ts:96`, `:106`, `:122` | **unowned** |
| `F2` | **No out-of-memory error scope and no memory-pressure hook.** Count corrected: **ten** real `pushErrorScope` sites, not twelve — two of the original grep hits were comment lines. Substance unchanged and confirmed: all ten are `"validation"`, **zero** `out-of-memory` scopes exist under `Renderer/WebGPU`, and **zero** `uncapturederror` / `onuncapturederror` listeners exist anywhere under `packages/engine/Source/Renderer`. A failed allocation is therefore only observed once it escalates to device loss, and an application has no hook to degrade LOD or drop effects before that. | CORRECTED | `WebGPUGlobeSurfaceShaders.ts` ×2, `WebGPUModelPipelineCache.ts` ×4, `WebGPUPostProcessPipeline.ts`, `WebGPUPrimitiveIndexUtils.ts`, `WebGPURenderBundleManager.ts:402`, `WebGPUSceneRenderer.ts:2271` | **unowned** |
| `F3` / `T7` / `N4` | Shader compile failure is logged with the context id but there is **no general fallback**: the magenta substitute pipeline exists for the **model path only** (Batch 388); every other renderer logs and drops the draw. A forced-failure hook exists for testing that path (`globalThis.CesiumWebGPUForcePipelineError`) — note it, since it is the negative control any generalisation would reuse. | CONFIRMED | `WebGPUContext.ts:3500-3505`; `WebGPUModelPipelineCache.ts:3536`, `:3557`, `:3582-3594`, `:3699`, `:3771`, hook `:2791`; `Shaders/WebGPU/Model/ErrorPipeline.wgsl` | Batch 388 (model path only); residual **unowned** |
| `F4` | Pipeline-creation failure: scopes yes, substitutes almost never — the same ten validation scopes as F2, with substitutes only in the post-process f16→f32 path (which does not work, §3.8 `R8`) and the model error pipeline. | CONFIRMED | as F2, plus `WebGPUModelPipelineCache.ts:3582` | `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1394` (fleet1 PLAUSIBLE, QUEUED — `WebGPURenderBundleManager.ts:402-419` leaks its scope when the record callback throws) |
| `F7` / `N6` | Post-init GPU validation is **silent**: a single one-shot error scope popped once via `Promise.resolve()`. With F2's finding that no `uncapturederror` listener exists, a mid-session format or layout regression produces browser-console noise the engine never sees and no probe can assert. A permanent `uncapturederror` handler stamping the context id, plus a `frameState.debug`-gated per-pass scope, closes it cheaply. **This compounds T10** (§3.10): the recorded MSAA-flip validation error is exactly the class nothing catches after the first frame. | CONFIRMED | `WebGPUSceneRenderer.ts:1087`, `:2267-2286` | **unowned** |
| `F8` | SSR normals are warned about and plumbed, but **the producer is not demanded**: the depth-derived normal producer returns early unless `frameState.useDeferredLighting === true`, and `scene.deferredLighting` defaults to `false`. `scene.enableSSR` raises no demand for the producer. The compensating behaviour is documented — the MRT topology is active on every non-pick frame, so pipelines that emit normal-roughness populate the attachment and only pipelines without such an output leave the clear sentinel. Either SSR should demand the producer, or `FEATURE_INVENTORY.md:881` should stop citing FEAT-GAP-01 and say `deferredLighting` is the switch. | CONFIRMED | warning `WebGPUSSREffect.ts:205-226`, flag `:273`; feed `WebGPUSceneRendererEnvironmentalEffects.ts:195-201`; **producer gate `WebGPUSceneRenderer.ts:2720-2721`**; `Scene/Scene.js:1167`, `:3778`, `:1213`, `:3319-3324`; `WebGPUGBufferRenderer.ts:6-11`; stale wording `FEATURE_INVENTORY.md:881` | **unowned** for the residual |
| `F9` | The three permanent sentinels (re-entry guard, null-target guard, size/overflow guard) are a SHOULD with no general check — **scope corrected: one renderer family _is_ mechanically covered.** The collections base class's three sentinels are gated by a spec in the standard `gulp test` suite, with a fast no-GPU smoke duplicate whose own header says it is **not** wired into CI. The finding survives narrowed: coverage exists for the collections base class **and nowhere else**, so a new renderer among the 277 files under `Renderer/WebGPU` can still land with none of the three. A census — which renderer has which sentinel — is the cheapest next step, and the collections spec is the template to copy. | CORRECTED | `packages/engine/Specs/Renderer/WebGPU/WebGPUCollectionSentinelsSpec.js`; `Tools/collection-sentinels-check.mjs:1-36`; `package.json:159`; `Tools/lint-debug-pragmas.mjs` (console pragmas only) | **unowned** for the general case |
| `F5` (maintainability) | No `visibilitychange` handler exists: the only one in all of `packages/engine/Source` is in the frame-rate monitor, and there are zero under `Renderer/`. LOW — Chromium restores the swap chain silently and the device-loss host path covers reconfiguration. | CONFIRMED | `Scene/FrameRateMonitor.js:115-121` | **unowned** |
| `E1` | Multi-instance safety is **strong**: a static context registry, and a device pool that checks feature level, features and limits for compatibility before sharing and releases devices explicitly. (Read with `R9` in §3.10: the registry is per-module, so two loaded copies of the engine yield two registries.) | CONFIRMED | `Renderer/GraphicsContext.ts:749-751`, `:21`, `:723`; `WebGPUDevicePool.ts:76-77`, `:285`, `:296-302`, `:459-520`, `:602-607` | FAR-102 / FAR-103 (`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:66` — "complete long-running multi-context/device-loss soak" remaining) |
| `E2` | Global-state pollution is minimal but **"clean global namespace in production builds" is not accurate as stated — two production global writes exist.** The globe surface renderer unconditionally publishes `globalThis.__webgpuGlobeBindGroupCache` and `globalThis.__webgpuGlobeImagerySlotCount`, both **outside any pragma block**, on a documented last-initialized-wins convention for `CesiumDebug.globeBindGroups()` and a probe. The cache handle is unpublished on destroy when it still points at this renderer; **the slot count is never unpublished.** So a production embed does get two `__webgpu*` properties on `globalThis`, one a strong reference to a live cache, and in a split-screen or multi-context embed both name only the most recent renderer. The sky-atmosphere latches (three) and the fragment-debug registry are pragma-stripped as described. Two further globals are _read_ but never written by the engine: `CesiumWebGPUForcePipelineError` and `CesiumWebGPUMetadataDebug`. | CORRECTED | **`WebGPUGlobeSurfaceRenderer.ts:703-705`, `:709-711`** (not pragma-wrapped), unpublish `:3050-3055`; stripped `WebGPUSkyAtmosphereRenderer.js:990`, `:1009`, `:1237` (pragma opened `:984`), `WebGPUGlobeFragmentDebug.ts:212-218`; read-only `WebGPUModelPipelineCache.ts:2791`, `WebGPUModelRenderer.ts:2377` | **unowned** |
| `P1-P3` / `T8` / `N7` | Chromium is first-class and **no Firefox or Safari run is scheduled or banked.** Both Playwright tools accept `--browser firefox\|webkit`; nothing schedules such a run; Karma resolves Chrome/Edge only; the renderer carries no Safari/WebKit workaround and no UA sniffing. A weekly manual Firefox-release + Safari TP smoke leg still needs an owner row. | CONFIRMED | `Tools/visual-regression/capture-and-diff.mjs:83`, `:692-700`; `Tools/variant-smoke-test.mjs:82`, `:186`, `:655-658`; `scripts/karmaDefaultBrowsers.js:10-12` | `DEFERRED_WORK.md:4855` (`NEW-CI-COVERAGE-SUITE`, "unknown until next hosted run") |
| `P4` | Optional features stay opt-in and there is no UA sniffing: the device pool matches feature level, feature set and limits, and there are zero `userAgent` hits under `Renderer/WebGPU`. Carry as a one-line strength. | CONFIRMED | `WebGPUContext.ts:997`, `:1014`; `WebGPUDevicePool.ts:509-520` | **unowned** (posture unchanged) |
| `X2` / `X3` / `X6` | The Electron non-issues stay non-issues: zero case-insensitive `electron` hits under `packages/engine/Source/Renderer` and zero in the debugging log; the generation counter is referenced by 31 files. | CONFIRMED | `WebGPUContext.ts:922` | **unowned** |
| `X4` | OffscreenCanvas-in-worker is **unverified opt-in scaffolding**: the module documents "Strategy C" with WebGPU-in-worker marked Experimental, and there are zero references under `Tools/` or `packages/engine/Specs/`. Treat as an unverified capability, not a shipped feature, until a worker probe lands. | CONFIRMED | `Renderer/OffscreenContextSupport.ts:1-13`; `Renderer/GraphicsContext.ts:586` | **unowned** |
| `X5` | Replacement-device recovery has **no banked real-browser attestation**: a spec exists and the recovery path retries three times, but the row stays open until an Edge-tranche probe attests. | CONFIRMED | `packages/engine/Specs/Renderer/WebGPU/WebGPUDeviceLossRecoverySpec.js`; `WebGPUDeviceLossRecovery.ts:240` | `Q-135` (`FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:497`) / FAR-208 / FAR-601 |
| `B11` | Native WebGPU features still first allocate compatibility resources, then native replacements. The **buffer half is closed** (the stub records store size only). The **texture half is live**: `Scene/Material.js` retains `_imageSources[uniformId]` **and** constructs a `Texture`, whose stub calls `device.createTexture`. Legacy shells remain in `VertexArray.fromGeometry`. No ownership token has landed. | CONFIRMED | `Stubs/WebGLStubBuffer.ts:255-262`; `Scene/Material.js:475-507`; `Stubs/WebGLStubTexture.ts:349`; `Scene/PrimitiveGeometryHelpers.js:782-825` | FAR-200..210 (FAR-209/210 rows live in the closed CAMPAIGN8/9 queues) |
| `T10` / `N8` | **Pointer row.** The `_scenePipelineFormatGeneration` manual contract — 31 ad-hoc readers, a recorded and reproduced `scene.msaaSamples`-flip failure, and no post-first-frame validation scope — is elevated as one finding in **§3.10**. It belongs to survivability because F7 is why the failure is invisible when it recurs. | CORRECTED | see §3.10 | `NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION` |
| `R5` / `T9` / `N5` | **Pointer row.** Ad-hoc diagnostic latches with no shared throttle — nine distinct `_…Logged` fields and zero throttle helper — are recorded in **§3.13**. Pragma-stripped, so readability only. | CONFIRMED | see §3.13 | **unowned** |

**Two claims re-verified today that live only in the KEEP-PART sharing block** (`FORK_VS_UPSTREAM…2026-07-13.md:565-588`), reproduced here so they are not lost if that block is read out of context. The **table itself** — what two WebGPU canvases on one pooled device may share, what two device generations may not, what two views must keep local, which ADR-1 / FAR-206 / FAR-207 are measured against — is **not** reproduced and is why that document stays live.

- `SharedResourcePool.ts` **has no importer.** The only references are `@see` doc comments at `Renderer/GraphicsContext.ts:45`, `Renderer/OffscreenContextSupport.ts:47` and `Renderer/WebGPU/WebGPUDevicePool.ts:80`. The plan agrees it must not become the primary cache (`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:193`) but records **no disposition for the orphaned module itself**.
- `Scene/ResourceCacheKey.js:387`, `:479` and `:613` still append `-context-${frameState.context.id}` to buffer and texture cache keys, so even the retained CPU product is context-scoped — the exact condition FAR-206 exists to remove.

### 3.12 Precision, RTE & ellipsoid assumptions

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `H19` / `#75` / `A9.2` | Previous-frame model motion vectors abandon the RTE path: `prevPositionMC` is reassembled by adding `translationHigh + translationLow`, then transformed by `previousModelMatrix` and `camera.previousViewProjection` in **single-precision world space**, while the current frame uses `mvpRelativeToEye`. The shader comment accepts "the residual metre of precision loss". Effect: transient multi-pixel velocity noise during fast fly-to, matching the voxel TAA pattern. A `previousViewProjectionRelativeToEye` + `previousModelMatrixRelativeToEye` pair would close it. MEDIUM, accepted. | CONFIRMED | `Shaders/WebGPU/Model/ModelPBRComplete.wgsl:1021-1023`, `:1030-1031`, `:1039-1042` | `C11-53` / FAR-306 for the finding; the RTE-pair fix is **unowned** |
| `H20` | RTE helpers are duplicated and the current assertion cannot validate packing — **count corrected**: **83** `.wgsl` sources define `fn translateRelativeToEye` (the original 166 double-counts the generated `.js` twins). Treat 83 as non-comparable with the audit's "~85 files" and re-baseline rather than claiming growth. The ineffective branch survives in exactly one source. The round-trip assertion carries a **6 m** tolerance and its own docs say it validates the _source_ values you are about to pack, not the packed offsets — so it cannot see a swapped destination offset. No shared reviewed helper exists. | CORRECTED | `Shaders/WebGPU/chunks/functions/csm_translateRelativeToEye.wgsl:38`; `WebGPURTEAssertions.ts:39`, `:70-85` | `C11-52` / FAR-305 |
| `#76` / `A9.3` | The shared `CameraUniforms` chunk lacks the DP-H41 tail: it ends at `modelViewProjectionRelativeToEye` (368 bytes) with **no `previousViewProjection`**, which the CLAUDE.md 64-bit/RTE section mandates for every renderer's `CameraUniforms`. Its only renderer consumer has no velocity path, so this is **drift between rule and chunk, not a rendering defect**. Either add the tail or scope the rule to velocity-emitting renderers. LOW. | CONFIRMED | `Shaders/WebGPU/chunks/structs/CameraUniforms.wgsl:24-46`; consumer `WebGPUBufferPrimitiveRenderer.ts` | **unowned** |
| `#78` / `A9.5` | The edge emitter uses a non-RTE model-space position: the 19-float / 76-byte vertex layout carries no high/low pair, and the file contains no `positionHigh`, `RelativeToEye` or `encodedCamera` identifier at all. Downgraded to LOW because WebGL's edge stage has the same precision regime — it is a divergence from **WebGPU's own** RTE model path only. | CONFIRMED | `WebGPUEdgeVisibilityEmitter.ts:402-403` | **unowned** |
| `H-P7` | **Hardcoded WGS84 radii break non-Earth ellipsoids — six elevation shaders, not five.** The full set bakes `WGS84_ONE_OVER_RADII`: `PrimitiveMatElevBandFlat.wgsl:99` (used `:169`), `…BandLit:111` (`:290`), `…ContourFlat:84` (`:145`), `…ContourLit:116` (`:287`), `…RampFlat:85` (`:146`) and **`…RampLit:111` (`:282`)**, which the first reader omitted, plus two cloud shaders. The **globe half is genuinely fixed**: `GlobeTerrain.wgsl:81` declares the ellipsoid-radius uniform and `:1380`/`:1575` consume it, with the `EARTH_RADIUS_FALLBACK` at `:827` carrying a comment directing shader code to prefer `camera.ellipsoidRadius`. **This is the defect `ARCHITECTURE.md:412-414` points at**, and that pointer is still accurate — Mars and Moon tilesets are at risk until it lands. It has no queue row, so that sentence is currently the only live pointer to a real defect and must be repointed here, not deleted. | CORRECTED | the six `PrimitiveMatElev*.wgsl` sites above; `CloudReconstructionAttachments.wgsl:48`; `CloudTemporalResolve.wgsl:31`; fixed globe half `Shaders/WebGPU/Globe/GlobeTerrain.wgsl:81`, `:827`, `:1380`, `:1575` | **unowned** |

### 3.13 Code hygiene, typing, tooling & test coverage

| id | Finding (corrected wording) | Verdict | Current `file:line` | Owner |
| --- | --- | --- | --- | --- |
| `C1` / `#141` | The TypeScript `any` ban holds in code, with **four** real uses, not two — the original grep missed generic and return positions. Confirmed: two DevTools-annotation casts on a wrapped `GPUShaderModule`; `submitBatch(commands: ReadonlyArray<any>)`, an untyped command array on the indirect-draw entry point; and `uniformMap: Record<string, () => any>`, already queued by fleet1, which notes the correct union `ViewportQuadUniformValue` is declared 226 lines above in the same file. Two apparent hits are a local variable named `any` (§3.10 `#111`) and two are the WGSL builtin inside shader string literals. The separate note stands: the remaining `.js` renderers are outside the ban's reach entirely — **34 `.js` against 237 `.ts`** under `Renderer/WebGPU` today. | CORRECTED | `WebGPUShaderCache.ts:398`, `:400`; **`WebGPUIndirectDrawManager.ts:334`**; **`WebGPUViewportQuad.ts:353`** | fleet1 row in `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md` owns `WebGPUViewportQuad.ts:353`; the others **unowned**. `.js` residue: `NEW-TS-CONVERT-JS-RENDERERS` (`DEFERRED_WORK.md:4715`) |
| `#140` / `A16.3` | The WebGPU renderer TS ratio, and the false "all-`.ts` island" framing. Measured: **34 `.js` / 237 `.ts`** (the original said 233). The ground-polyline, ground-primitive, five collection renderers, shadow-map and environment renderers are still untyped JS. `ES6_MODERNIZATION_STATUS.md:64` — "The WebGPU renderer is the modern island — all-`.ts`, all-ES2022" — is **drift** and should be corrected to the measured ratio with a pointer to the conversion row. | CORRECTED | measured over `Renderer/WebGPU/`; drift line `migration_doc/ES6_MODERNIZATION_STATUS.md:64` | `QUEUE_2026-07-18_CAMPAIGN11.md:1897` (`C11-154`) / `DEFERRED_WORK.md` `NEW-TS-CONVERT-JS-RENDERERS` |
| `H-P21` | `getPreferredCanvasFormat` fallbacks default to a literal `bgra8unorm` — **the census is off by a factor of five: 64 sites**, not thirteen, spell the literal as a fallback or field default under `Renderer/WebGPU`. Severity is unchanged (hygiene — the live resolve and the format epoch are what actually govern), but a DX row scoped to about thirteen call sites would be badly under-budgeted. Scope it as **one context accessor plus a lint rule banning the bare literal**. | CORRECTED | live resolve `WebGPUContext.ts:1393`, epoch `:922`; a sample of the 64: `WebGPUContext.ts:3803`, `WebGPUDebugDepthOverlay.ts:192`, `WebGPUDebugFrustumOverlay.ts:199`, `WebGPUDebugGBufferOverlay.ts:99`, `WebGPUDepthOfFieldEffect.ts:47`, `WebGPUDepthPlane.ts:449`, `WebGPUEdgeFramebuffer.ts:29`, `WebGPUEdgeVisibilityEmitter.ts:1136`, `WebGPUEnvironmentalEffectsCompositor.ts:186`, `WebGPUEnvironmentRenderer.js:821`/`:1639`/`:1676`/`:1902`/`:1905`, `WebGPUFlowFieldRenderer.ts:553`, `WebGPUFramebufferManager.ts:143`, `WebGPUGlobeSurfaceRenderer.ts:352`, `WebGPUGodRayEffect.ts:95`, `WebGPUGroundPolylineRenderer.js:2647`, `WebGPUGroundPrimitiveRenderer.js:2053`, `WebGPUHeatShimmerEffect.ts:83`, `WebGPULabelRenderer.js:886`, `WebGPULibraryPostProcessStage.ts:343` | **unowned** (file as a DX hygiene row) |
| `C2` | Pragma-wrapped logging is now tool-enforced: 99 `console.log`/`console.warn` calls across 45 files, 187 debug-pragma blocks across 70 files, a linter plus its spec wired into `package.json`, and permanent errors where the rules require them. Census re-counted and identical. | CONFIRMED | `Tools/lint-debug-pragmas.mjs` + `.spec.mjs`, `package.json:149`; permanent errors `WebGPUSceneRenderer.ts:2275`, `WebGPUContext.ts:3503` | `Tools/lint-debug-pragmas.mjs` |
| `R5` / `T9` / `N5` (maintainability) | Ad-hoc diagnostic latches with **no shared throttle**: nine distinct `_…Logged` fields across the scene and globe renderers (`_debugLogged`, `_depthOverlayWarningLogged`, `_diagFogMissingLogged`, `_execDebugLogged`, `_globeCountLogged`, `_globePassRPLogged`, `_postInitDebugLogged`, `_ppDebugLogged`, `_renderPassRedirectLogged`), and **zero** `LogThrottle` / `DiagnosticThrottle` helper anywhere under `packages/engine/Source/Renderer`. Pragma-stripped, so readability only. | CONFIRMED | `WebGPUSceneRenderer.ts:1079`, `:1081`, `:1088`, used `:1707`, `:1762`, `:2292` | **unowned** |
| `R4` (maintainability) | The public underscore-prefixed field convention (`public _canvas`, `public _adapter`, `public _device`) is **deliberate and named in comments**, not leaked encapsulation. State it once so readers do not re-file it as a defect. | CONFIRMED | `WebGPUContext.ts:488-491`, `:1098`, `:3691` | **unowned** (documented convention) |
| `R3` / `N11` | No CI docstring-vs-WGSL drift check exists in `Tools/`, `scripts/`, `.husky/` or `package.json`; two shaders still carry phase wording their modules no longer match. The pragma linter polices console pragmas and the C16-00 marker guard polices banned vocabulary; **nothing compares a `*.ts` module header against the `*.wgsl` it drives.** Because the C16 sweep will eventually delete the phase wording a grep would key on, a rule that every renderer module header names its shader as ground truth beats a phrase-grep. Low severity; belongs with the C16 tooling if funded at all. | CONFIRMED | `Shaders/WebGPU/Compute/VolumetricFog.wgsl:1`; `Shaders/WebGPU/PostProcess/VolumetricFogComposite.wgsl:1` | **unowned** (C16 owns comment vocabulary, not semantic drift) |
| `A1c-1` | There is **no per-variant named-export sweep**: the smoke test's pass criteria are page loads, `window.Cesium` defined, Viewer constructs, five frames, zero console errors. A grep for `Object.keys(Cesium)` or an export walk over the WebGL-only / WebGPU-only barrels returns nothing; the plugin spec covers the resolver matrix, not the barrels. | CONFIRMED | `Tools/variant-smoke-test.mjs:4`, `:18-24` | **unowned** |
| `F5-12` / `ACTION-15` / `DOC-7-REQ-4` | Missing integration tests — **scope corrected.** The hole is **not** "no eviction test": the tile-buffers spec does cover `evictStaleResources` as a unit against a fake host (22 `it` cases in the file). The two real holes are (a) **no test that `GlobeSurfaceTile.freeResources` releases a GPU realization**, or that a freed tile's buffers survive until submit, and (b) **no two-context / shared-device isolation-and-recovery test** (`Specs/Renderer/WebGPU/` has no multi-context spec). Any future terrain-lifetime or ownership change lands with the failing lifecycle test first. | CORRECTED | `WebGPUGlobeSurfaceTileBuffersSpec.js:298-421` | `Q-86`; DX-12 spec homes |
| `DOC-7-REQ-1` | The coverage matrix has no successor: `migration_doc/` still holds only the 2026-07-15 JSON, while today `Source/Renderer/WebGPU/` has **272** files and `Specs/Renderer/WebGPU/` has **143**. A successor snapshot — modules vs spec files vs runner home, **generated by a script** rather than hand-counted — belongs beside the DX-12 spec-homes pass. Counts prove presence, not semantic coverage. | CONFIRMED | `FORK_EXTENSION_TEST_COVERAGE_MATRIX_2026-07-15.json` | **unowned** (adjacent: DX-12) |
| `ACTION-1` / `REJECT-5` | Harden the measurement baseline (Gate 0). `run-performance-campaign.mjs` contains **zero** occurrences of `maxThroughput` and the bundle is pinned to `Build/CesiumUnminified`. Three Gate-0 items remain unowned: a **max-throughput lane whose vsync state is verified rather than assumed**, a release/minified lane, and a fully local terrain + water-mask fixture. **REJECT-5 is the standing rule:** a checkbox is not proof vsync is off — presented-FPS claims stay display-paced until a max-throughput lane exists whose vsync state is verified and recorded by the driver/browser protocol. | CONFIRMED | `run-performance-campaign.mjs:5355`, `:5362`, `:5567`, `:5572` (and `:1890`, `:1917` in-page); `QUEUE_2026-07-18_CAMPAIGN11.md:2516` | `C11-173` done; `Q-143` / `DM-01` / `DM-04`; PERF-MEASUREMENT-VALIDITY-RISKS |
| `F5-15` | `counterbalancedPairs` mislabelled — **the number is not wrong.** `buildCounterbalancedSchedule` emits one entry per repetition, each entry runs *both* renderers, and pairs are built one per entry, so six repetitions really do yield six comparison pairs (three per order). What survives: the manifest field is a **static declaration nothing reconciles against `repetitions`**, and an **odd** repetition count ≥ 7 still certifies (`certificationRun = schedule.length >= 6`, five valid pairs, ≥ 2 per order) with unbalanced orders (4/3). Fix: recompute or rename the field from `repetitions`, publish the resolved schedule in the artifact, and reject odd two-renderer counts in certification mode. Tools-only, S. | CORRECTED | `performance-workloads.json:16-17`; `performance-workloads.spec.mjs:148`, `:2897`; `run-performance-campaign.mjs:5496`, `:5696`, `:5753-5791`; `lib/performance-campaign-utils.mjs:112-123` | **unowned** (adjacent: `DEFERRED_WORK.md:4654` PERF-MEASUREMENT-VALIDITY-RISKS) |
| `#87` | `WebGPUSubgroupUtils` has **no production importer** — its only importer is its own spec. The shipped subgroup variants (FrustumCull `mainSubgroups`, PointCloudLOD `computeMainSubgroups`) were written inline. Either route those kernels through the utils or retire the module. **Principle-7 check performed:** no doc claims it as scaffolding for pending work. LOW. | CONFIRMED | `Renderer/WebGPU/WebGPUSubgroupUtils.ts`; sole importer `Specs/Renderer/WebGPU/WebGPUSubgroupUtilsSpec.js` | `QUEUE_2026-07-06_CAMPAIGN7.md:110` records the inline wiring — a **stale row, not an owner** |
| `L-P6` | `getCompilationInfo().then` has no `.catch` inside the shared shader wrapper. | CONFIRMED | `WebGPUContext.ts:3500-3516` (wrapper `:3490-3518`, release logic `:3521-3538`) | **unowned** |
| `#144` | The pragma-strip `onLoad` filter is `/\.[jt]sx?$/`, which also matches `.d.ts`. Harmless today because declaration files never enter the bundle graph, but an explicit `.d.ts` early return removes the foot-gun. Trivial. | CONFIRMED | `scripts/build.js:76` | **unowned** |
| `#145` | `WGSLShaderBuilder.js` is still prototype-method style: **24** `.prototype.` sites over 695 lines. Convert to a class under the >10-line touch rule when the file is next edited. LOW. | CONFIRMED | `Renderer/WebGPU/WGSLShaderBuilder.js` | `ES6_MODERNIZATION_STATUS.md` |
| `#223` / `B9` | There is **no modernization-diff guardrail**: no written rule says a modernization-only diff must be reviewable under `git diff -w` or must ride with a real hook / `.d.ts` need — 0 matches for `mega-commit`, `diff -w` or `unreviewable` in `CLAUDE.md` and `.clinerules`, whose ES6 section only forbids touching untouched files. With C16 rewriting comments across 556 files the same codemod-scale risk recurs; add the two sentences to the ES6 rule. LOW, process. | CONFIRMED | `CLAUDE.md`, `.clinerules` | **unowned** (landing size covered indirectly by `WORKER_ISOLATION_AND_BRANCH_HANDOFF.md`) |

### 3.14 Documentation drift and stale counts

Every row here is a record that contradicts HEAD. They are listed together because a false record is the input to the next false brief (CLAUDE.md Principle 10), and because four of them are cited elsewhere in this file as evidence.

| id | Drift (corrected wording) | Verdict | Where |
| --- | --- | --- | --- |
| `D.3` | **Three stale TS-debt counts.** Measured 2026-09-03 over the 237 `.ts` files in `packages/engine/Source/Renderer/WebGPU/`: `TS-DEBT-3` `: any` annotations = **0** (recorded "268 across 40 files"; all 13 grep hits are comment prose, four `activeAnyFrustum: anyTrue(...)` calls and one object-literal property whose value is a local `let any`); `TS-DEBT-4` `as any` casts = **2**, at `WebGPUShaderCache.ts:398`, `:400`, plus one `ReadonlyArray<any>` at `WebGPUIndirectDrawManager.ts:334` (recorded "33 across 10 files"); `TS-DEBT-5` `as unknown as` = **518 across 58 files** (recorded "~10"). Close TS-DEBT-3 and TS-DEBT-4 as effectively discharged, and **re-scope TS-DEBT-5** as either sanctioned JS-interop bridging or real debt with a bar. Do not carry any of the three numbers forward unrecounted — the first reader's replacement figure (18) was wrong in the same direction as the original, and understating debt is the more dangerous direction. | CORRECTED | `FEATURE_INVENTORY.md:1150-1152` |
| `D.6` | `WorkerSceneHost` is untagged and mis-sectioned: it sits inside `## A. EXISTING — Upstream Cesium Features` under `### A.16 Widgets & UI`, with no tag, although the files are fork-added and the design doc says "Implementation deferred". Move to §B tagged SCAFFOLDED, beside the `OffscreenContextSupport.ts (SCAFFOLDED)` row and the pending WORKER-1..9 items. | CONFIRMED | `FEATURE_INVENTORY.md:488` vs `:520`, `:1164-1165`; `Services/WorkerSceneHost.js:1-12`, `WorkerSceneProtocol.js`; `OPTION_B_SCENE_IN_WORKER.md:3` |
| `#135` / `A15.1` | The inventory implies OIT-MRT / AutoExposure / AO are **fork-exclusive**. `WebGPUAutoExposure` and the WebGPU AO entry read as fork-added with no note that `Scene/AutoExposure.js` and the GLSL `AmbientOcclusionGenerate` stage exist upstream (plain `AutoExposure` is listed at `:338`); the phrase "parity port, not fork-exclusive" appears nowhere in the file. Add the note so a backport audit does not double-count them. LOW, doc. | CONFIRMED | `FEATURE_INVENTORY.md:791`, `:338` |
| Four **stale-green** queue rows | Four `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md` rows still read QUEUED or CONFIRMED while the code is repaired: `:1332` globe material cache (fixed Batch 1185, `3ebd7e193e`), `:1335` model display-pipeline key (Batch 1209, `79c9fa2ebb`), `:1348` indirect run-of-one swallow (Batch 1198, `2a1c016c28`), and `:1350` / `Q-1` the `Scene.js` light branch (fixed by the `updateDerivedLighting` hook). **Four of the sixteen ARCHITECTURE_PERFORMANCE migrate texts lean on these as live evidence** — they have been dropped or re-dated in §3.9 and §3.10 above. This is a defect in the queue, not in any archived document. | CORRECTED | `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1332`, `:1335`, `:1348`, `:1350` |
| `WebGPUSSREffect` stale warning | The SSR effect still carries a comment and a user-facing `console.warn` telling operators that "SSR will sample an uninitialized placeholder and produce noise" — text that no longer describes runtime behaviour, since the shader reconstructs the surface normal from neighbour-pixel view positions whenever the G-buffer is not forwarded. A Principle-10 comment defect worth filing; it does **not** restore the original finding. | CORRECTED | `WebGPUSSREffect.ts:205-224`; `Shaders/WebGPU/PostProcess/ScreenSpaceReflections.wgsl:204-228`; forwarding gate `WebGPUSceneRenderer.ts:3343-3346` |
| `FEATURE_INVENTORY` WIP-vs-ledger conflict | `FEATURE_INVENTORY.md:1006` and `:1018` still list `C-R1-GLOBE-RENDERSTATE` and `C-R1-TILE-BATCH` as WIP against RESOLVED ledger entries. Reconcile against `C11-36` / `C11-82` in the same commit as the archival. | CONFIRMED | `FEATURE_INVENTORY.md:1006`, `:1018` |
| `ISSUES_AND_FIXED_BUGS` stale rows | Four rows in the successor register are stale and resolve in the ledger's favour: §5 B.8 follow-up `:189` (RESOLVED Batch 175), §5 B.10 follow-up `:190` (RESOLVED Batch 183), §5 A.13 residual (RESOLVED Batches 198-204), §4 C.2 `:164` (RESOLVED Batches 209-211). Separately `:175` (the orphan-WGSL note) needs the three-way split from §3.3 `M-R13`, and `:345` must be corrected from `C-R1..C-R14` to **`C-R1..C-R13`** (§5.3). | CONFIRMED | `ISSUES_AND_FIXED_BUGS.md:164`, `:175`, `:189`, `:190`, `:345` |
| `ES6_MODERNIZATION_STATUS` | "The WebGPU renderer is the modern island — all-`.ts`, all-ES2022" is drift against 34 `.js` / 237 `.ts`. See §3.13 `#140`. | CORRECTED | `ES6_MODERNIZATION_STATUS.md:64` |
| `CLAUDE.md` renderer-default paragraphs | **Carry-forward, not a finding of any archived document.** CLAUDE.md still instructs future sessions that the variant entry barrels call `setGlobalDefaultRenderer()` at module init and that the `package.json` `sideEffects` line exists to protect that call. **Both halves are now false.** The only definition is `RendererType.ts:269`; every other mention is a re-export; variant defaults come from the build-aliased `rendererBuildCapabilities` consumed by `getDefaultRendererType(...)`, and the absence of the setter call is **mechanically enforced** — `scripts/__tests__/bundleVariantPlugin.spec.mjs:310-326` asserts the build generator does **not** emit it, with the message "build generator must not append a mutable renderer-default setter". Fix both paragraphs in the same pass as the pointer repoints. | CONFIRMED | `CLAUDE.md` "Runtime default renderer" and "Side-effects declaration (CRITICAL)"; `RendererType.ts:25`, `:129`, `:229`, `:263`, `:269`, `:277`, `:289-291`; `package.json:47-48`; owning row `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1380` |
| `§5` backport ranking (`#126-#134`, `#136` / `A15`) | Only rank 1 of the WebGPU-only → WebGL2 backport ranking shipped (the precision-stable ray-sphere port, Batch 304, attributed in the shader). Ranks 2/3/5/6/7/8 have **no owning row**: god rays / SSR / TAA (pure fragment passes; `UniformState.previousViewProjection` is already populated on WebGL), procedural volumetric clouds, Forward+ clustered lighting (a compute-to-fragment rewrite), IBL prefilter + atmosphere LUT via ComputeEngine, GPU point-cloud LOD decimation, and GTAO. Render bundles, decoupled scan, subgroups and GPU sort/cull/HiZ are **not** backportable. OIT-MRT, AutoExposure and HBAO **already exist on WebGL** and must not be counted as fork-exclusive. Record the six unowned candidates as a single GAP row if WebGL parity of the post-process stack is still a goal. | CONFIRMED | `Shaders/Builtin/Functions/raySphereIntersectionInterval.glsl:19` |
| `§8` CLAUDE.md / runbook requests | The three rule changes the ultra review asked for do not exist in CLAUDE.md (0 matches for `capabilit`, `diff -w`, `MOVE-MAP`); enforcement happened in code and process instead. Migrate via the three referenced items (§3.10 `#116`, §3.13 `#223`, `NEW-SYNC-MOVEMAP` at `DEFERRED_WORK.md:4948`) rather than as a fourth entry. | CONFIRMED | `CLAUDE.md` |

**Parity-drift roll-up (`§3`, 16 rows).** Surviving WebGL↔WebGPU parity drift owned by no row: the clearcoat basis and direct-only dimming (§3.3 `#26`); `disableDepthTestDistance` with no `w <= 0` guard on the billboard and SDF-label shaders (§3.5 `#44`); the depth-sample float-equality discard (§3.4 `#31`); the `FRONT_AND_BACK` and `CONSTANT_ALPHA` converters (§3.10 `#99`, `#100`); and the accepted single-precision previous-frame model clip (§3.12 `#75`). All LOW except the last (MEDIUM, accepted). **Add `#37` — billboard and label `eyeOffset` — to this table: it is the only HIGH parity gap in the set and it is unowned** (§3.5).

**Questionable-decisions roll-up (`§4`, 10 rows).** The surviving text is the OIT cache key (§3.8 `#73`) and the f16 error scope (§3.8 `R8`, **strengthened** — the scope is still present and still inert). The polyline half no longer survives as written: the "async without await" premise is refuted and only the discarded-promise residual survives (§3.5 `#42`).

### 3.15 Method & governance — the eleven principles (`F5-PRINCIPLES`, `GATE-4-ACCEPTANCE`)

`FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.md` carries a method register that no successor fully states. Its eleven principles already exist as one-line statements in `CODEX_DISAGREEMENTS_WITH_FABLE_REVIEW_2026-08-16.md:60-71`, which is why the file is archivable rather than KEEP-PART — **but that packet's 33 links are line anchors into the archived file** and resolve only against git history after the move. Reproduce them under stable headings there or here.

**Three clauses exist nowhere else and must be restored explicitly, or they die with the file:**

1. **Bounded visual evidence.** A route's cross-backend diff validates the subject it rendered (offline ellipsoid + NaturalEarthII) — never real terrain, water, dense 3D Tiles, OIT, or every post-process consumer.
2. **Honest incomplete test state.** A stopped suite is neither a complete green nor a complete red, and unrelated lint debt stays separately attributed.
3. **`REJECT-4` — the control-leg clause.** `CLAUDE.md:20` and SR-1 (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:40`) carry only the **prohibition** half: never remove, default-disable, bypass or visually degrade a feature to win a metric. The distinctive half is stated nowhere else in the tracked tree: **a feature toggle is a legitimate A/B attribution control, and the OFF leg is never the shipped fix.** SR-9 covers interleaved A/B for GPU timing only. The first reader dropped this clause from `GATE-4-ACCEPTANCE`'s text as well as retiring the item, so it must be restored in **both** places.

**`GATE-4-ACCEPTANCE` (corrected).** With the control-leg clause restored: disabling OIT, clustered lighting, the scheduler, the octree, shadows or post-processing is **not an optimisation** — an OFF leg is an attribution control. Then, as first judged: compare against the current `Tools/visual-regression` baselines; add temporal sequences for TAA history, RTE under motion and altitude transitions; and any pass-folding or clear-reordering change additionally needs numeric `pickPosition`, primitive/billboard pick, classification, depth-plane/horizon, multi-frustum, off-camera shadow-caster, HDR, 2D/Columbus, resize and device-recovery checks **with the feature on**.

### 3.16 Do not migrate — refuted, and corrected-to-closed

Recorded once so they are not re-derived a third time.

| id | Source | Why it must not be carried |
| --- | --- | --- |
| `B3` (feature-inventory) | REFUTED | "The pragma linter is not wired into lint-staged or any pre-commit/CI step" is false: `.github/workflows/dev.yml:80-81` runs `npm run lint-debug-pragmas` in the `guards` job (Batch 1211, `e9fdca9838`). The first reader grepped only `lint-staged.config.js` and `.husky/`. Pragma discipline **is** mechanically enforced. The one surviving nuance — `Tools/lint-debug-pragmas.mjs:27-34` scans `Renderer/WebGPU` only, so Scene-side sites are out of scope — is already held as fleet1 rows. |
| `R7` (feature-inventory) | REFUTED | Both "no row" claims are false: `FEATURE_INVENTORY.md:1021` tracks the 3D-Tiles pop-in motion-vector NaN reject and `:1078` the CSM+TAA shadow-edge motion verification, both deferred to TAA Slice 4; `:1056` tracks pick un-jitter. The closed half is confirmed in code and `Scene.js:1321` still defaults `taaEnabled` to false. Nothing in this item needs migrating. |
| `H-R11` | REFUTED | `debugShowGlobeDepth`, `debugShowPickDepth` and `debugCommandCount` have **zero** occurrences anywhere under `packages/engine/Source` — on neither backend. They have no _writer_ either, so there is no parity gap and nothing to wire or document; `CHANGES.md:1864` records the first as removed upstream (PR #9965). The shipped half is accurate: `debugShowBoundingVolume` (30 hits under `Renderer/WebGPU`), `debugShowCommands` (6), and `DEBUGGING_GUIDE.md:1303-1309` correctly marks two more as "both". The one debug-flag gap that **is** live is §3.5 `M-R5`. |
| `M-P7` | REFUTED | Material state **is** in the primitive-cache invalidation comparison: `WebGPUModelRenderer.ts:6560-6563` includes `hasMaterialGenerationChanged(...)` (defined `:3392-3401`, comparing `_materialBase` identity with `_effectiveAlphaMode`) in the predicate that destroys and deletes the entry. Runtime factor and colour edits need no invalidation at all — they reach the GPU through `packMaterialUniforms` every frame (`:2111`, called `:6874`, `:8333`). CustomShader swaps rebuild on reference change and cascade translucency. The only residue is an in-place mutation of a **retained** `MaterialInfo` identity that changes a texture reader without changing `alphaMode`, a shape no loader in the tree produces. The distinct customShader-invalidation defect (`A5-3`) is a separate row and stays where it is. |
| `GUARDS` | REFUTED | The behavioural degenerate-triangle spec the audit demanded **exists**: `Specs/Scene/Model/EdgeVisibilityPipelineStageDecodingSpec.js:662` builds a primitive with two coincident vertices, documented at `:737-742` as the upstream #13421 repro, with both artefacts recorded shipped at Batch 299 (`DEFERRED_WORK.md:4933`). The source-text regex guard is now redundant defence-in-depth beside a real spec, not the sole certification. The `git diff --check` and material-probe halves are already closed. **No new tools row is needed.** |
| `H-P11` | CORRECTED to closed — do not re-file | The WebGL-parity premise is false. `Scene/ClippingPolygonCollection.js:300-307` performs the **identical** count-only check, with an upstream comment giving the reason. The WebGPU path is if anything stricter, gating on `lastTotalPositions`, `collection.length` **and** a non-null texture. The curvature half is likewise shared: both SDFs are planar in lat/lon space with no curvature term. This is **not a WebGPU parity gap**; if it survives at all it is an upstream-shared enhancement note whose home is an upstream issue, not a fork queue row. |
| `S9-4` ADR-3 half | CORRECTED to closed — do not re-open | The first reader asked whether the model camera arena is a live ADR-3 ownership violation. It is not: the arena is **context-owned**, stated in both the context getter's comment and the arena's own header, and the device pool holds no arena at all (Batch 819 `ad43f8c527`). Carry only the certification remainder and the stale `C11-195` clause (§3.9). |

**Latent note, carried from `H-P9` and `H-P11`, not a finding.** Two caches in the tree are keyed on a **proxy for identity rather than on identity itself** and are currently safe only because nothing mutates in place: `WebGPUPointCloudRenderer.ts:1789` sets `const revision = pointCount`, sound only because each `TimeDynamicPointCloud` frame is a fresh instance (`Scene/TimeDynamicPointCloud.js:540`, `Scene/PointCloud.js:194`, `:695-710`) and tileset PNTS content is parsed once; and the clipping-polygon SDF above. Worth one sentence in any future caching lens.

## 4. Reversals

Five items a first reader had marked **resolved, superseded or credited-as-fixed** that the second readers found **still live**. They come from four documents. Each is a reason a record is wrong today, independent of what happens to the archived text, and each gets a proposed row.

### R1 — Hi-Z occlusion is DEFAULT OFF in code while the ledger says DEFAULT ON

Judged RESOLVED on the strength of `migration_doc/DEFERRED_WORK.md:5338`, which reads:

> `### FORK-41 — Hi-Z occlusion: RESOLVED (C2-21, 2026-06-24) — root cause was a depth-source bug; command-drop now DEFAULT ON, verified`

The code says otherwise:

- `packages/engine/Source/Renderer/WebGPU/WebGPUSceneRenderer.ts:1189` — `private _hiZConsumeEnabled: boolean = false;`
- `:1191-1196` — "It remains disabled until result identity is tied to the producing frame, frustum, and command list."
- `:4195-4198` — "Do not drop commands by default until each result identifies its producing frustum, frame, and command generation." followed by `if (!this._hiZConsumeEnabled) return commands;`

So the 2026-04-30 finding is **still true in its operative sense**: no command is dropped by Hi-Z on the default path. Two consequences point in different directions.

1. **The finding itself is tracked**, so it does not by itself keep a document alive: `QUEUE_2026-07-18_CAMPAIGN11.md:1770` (`C11-98`, FORK-41, W7) owns it, and `QUEUE_2026-08-29_RESEARCH_DISPATCH.md:727-729` states the ownership explicitly.
2. **A live ledger row asserts a state HEAD contradicts.** `QUEUE_2026-08-29_RESEARCH_DISPATCH.md:605` independently verified default-off on 2026-08-29 ("Three containment switches, all default-off, all verified 2026-08-29: … FORK-41 Hi-Z command drop `WebGPUSceneRenderer.ts:1181` `_hiZConsumeEnabled = false`"). The `:5338` heading is the stale one; the superseded `:5365` PARTIAL entry below it describes the actual state better. This is exactly the kind of row a future brief would cite as a premise — the failure CLAUDE.md Principle 10 was written against.

Two verifiers reached this row independently (also flagged under `B4`, §3.9).

> **Proposed row — `NEW-FORK41-LEDGER-ROW-CORRECTION` (S, docs).** Correct `DEFERRED_WORK.md:5338` to state Hi-Z command-drop is **default OFF** with the `_hiZConsumeEnabled = false` citation and the reason, demote the "RESOLVED … DEFAULT ON, verified" heading, and leave `C11-98` as the owner of the wiring. Acceptance: the row and the code agree, and `B4`'s owning record can be quoted without contradiction.

### R2 — `debugShowBoundingVolume` is still dropped for four collections

`M-R5` was judged RESOLVED with the text "Definition only: resolved to WebGL parity". That holds for the `Buffer*` / `GeoJsonPrimitive` family only. For the four collections `M-R5`'s own title names, the flag is dropped:

- **WebGL sets it on the command** — `Scene/BillboardCollection.js:1220`, `Scene/PointPrimitiveCollection.js:842`, `Scene/PolylineCollection.js:802` and `:898`.
- **The WebGPU renderers never do** — `grep -c debugShowBoundingVolume` returns **0** for `WebGPUBillboardRenderer.js`, `WebGPULabelRenderer.js`, `WebGPUPointPrimitiveRenderer.js` and `WebGPUPolylineRenderer.js`. Their commands carry `owner` and `boundingVolume` (e.g. `WebGPUBillboardRenderer.js:1355-1356`), so culling works, but not the debug flag.
- **The consumer filters on exactly that flag** — `WebGPUSceneRenderer.ts:3652` skips any command with `cmd.debugShowBoundingVolume !== true`.
- **The WebGL assignment is unreachable on WebGPU** — `BillboardCollection.js:750` takes the feature-renderer early return well before the command-build block at `:1195-1228`.
- **No row owns it** — `DEFERRED_WORK.md:5012` is scoped to the three `Buffer*` renderers and `GeoJsonPrimitive`.

Net effect: setting `billboardCollection.debugShowBoundingVolume = true` (or the label, point or polyline equivalent) draws the red wireframe on WebGL and nothing on WebGPU. Severity is debug-tooling parity, not rendering correctness — but the item migrates as **PARTIAL**, not resolved.

> **Proposed row — `NEW-WEBGPU-COLLECTION-BV-DEBUG-DRAW` (S).** Set `command.debugShowBoundingVolume` from the collection in the four WebGPU collection renderers, as the `Buffer*` lane already does. Acceptance: a probe that sets the flag on a billboard, label, point and polyline collection and captures a non-empty wireframe on both backends.

### R3 — the globe material packing fix was credited but did not land

`H17` was kept open, but credited with "packing now happens once per frame per material type rather than per tile". **False.** `_getOrCreateMaterialPipeline` (`WebGPUGlobeSurfaceRenderer.ts:522`) is invoked at `:1625` inside `createTileCommands` (`:1099`), which `Scene/GlobeSurfaceTileProviderRendering.js:1356` calls **once per tile**; `:650-654` runs `packMaterialUBO` — a fresh `ArrayBuffer` and `Float32Array` at `WebGPUGlobeMaterial.ts:396-397` — and `queue.writeBuffer` on **every** invocation. Only the pipeline and shader-module cache entry (`:543-588`) is per `material.type`. The original headline — thousands of redundant allocations and queue writes per frame on a material-enabled globe — is **unmitigated**. It is inert while `globe.material` is unset, which is the default.

> **Proposed row — `NEW-GLOBE-MATERIAL-UBO-PER-FRAME` (M, under FAR-303).** Hoist the material pack and upload to once per frame per `material.type`, keyed on the material's own generation, and reuse a retained `ArrayBuffer`. Acceptance: a `globe.material`-enabled scene shows `writeBuffer` calls for the material UBO proportional to material types, not to visible tiles, on the moving-altitude campaign; byte-identical capture with the material set.

### R4 — `RenderCommand` has zero real importers while CLAUDE.md tells new features to use it

`H9`'s "RenderCommand now has production consumers" is **false**, and the parallel `D1` finding rests on the identical grep defect.

`Renderer/WebGPU/RenderCommand.js` has **no importer anywhere in `packages/engine/Source`**. Seven files mention the string; **zero** contain `from ".../RenderCommand.js"`. The only *import* of it outside the module itself is the generated barrel `packages/engine/index.js:1025`, and the cited "consumers" are local functions coincidentally named `updateAndQueueRenderCommand` (`Scene/ClassificationPrimitive.js:1300`, `Scene/GroundPrimitive.js:802`; `Scene/QuadtreePrimitive.js:1365` is the same shape). The remaining mentions are doc comments (`Renderer/GraphicsContext.ts:1903-1904`, `Stubs/WebGLStubShader.ts:16`, `:392`, `WebGLCompatibilityStub.ts:102`, `:128`, `:134`). 25 Scene files construct `DrawCommand` directly, and `FEATURE_INVENTORY.md:512` already tags the module SCAFFOLDED / UNUSED.

Meanwhile **CLAUDE.md's architecture section instructs that new scene features SHOULD use `RenderCommand` instead of importing backend-specific commands directly.** The backend-agnostic command abstraction the fork tells its own contributors to prefer is dead on arrival. Corrected, the finding is *stronger* than recorded; migrating either text verbatim would carry the false "3 Scene files adopt RenderCommand" figure forward.

Two second-order consequences: the webgl-only throwing-stub path for this module is unreachable, so the fleet1 exemption row is a latent-on-adoption hazard rather than a live defect (§3.10 `D3`); and `RenderCommand` is **not** on the compat-exemption list, contrary to the original claim.

> **Proposed row — `NEW-RENDERCOMMAND-ADOPT-OR-RETIRE` (M, architecture decision).** Rule the abstraction in or out. If in: one concrete, measured consumer first (the pattern `REJECT-6` demands for `ResourceOwnership`), plus the CLAUDE.md guidance kept. If out: retire the module, drop the barrel export, and **replace the CLAUDE.md instruction in the same commit** so the guidance never points at a retired module. Acceptance either way: no rule in `CLAUDE.md` names a zero-importer module.

### R5 — `REJECT-4`'s control-leg clause survives nowhere

`REJECT-4` is marked SUPERSEDED against `CLAUDE.md:20` / SR-1, but those carry only the **prohibition** half — "never remove, default-disable, bypass or visually degrade a feature to win a metric". The distinctive half, **_a feature toggle is a legitimate A/B attribution control, and the OFF leg is never the shipped fix_**, is stated nowhere else in the tracked tree; the whole SR-1..SR-17 table was read (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:38-55`) and SR-9 covers interleaved A/B for GPU timing only. The first reader additionally dropped the same clause from `GATE-4-ACCEPTANCE`'s migrate text, whose own title still contains it. On the judgement as written, the rule dies with the file.

> **Proposed row — `NEW-SR-CONTROL-LEG-CLAUSE` (S, governance).** Add the clause to the SR table beside SR-1 and restore it in `GATE-4-ACCEPTANCE` (§3.15). Acceptance: the clause is stated in a live governance document, not only in an archived audit.

**Adjacent corrections that are not reversals but travel with them** — the four stale-green `FIX_QUEUE` rows, the `MAINTAINABILITY` `T10` / `N8` row that turned out to **have** an owner recording a reproduced defect, `ULTRA` `#35` which `DEFERRED_WORK.md:9842` already owns, `PER_FEATURE` `H-P11` / `M-P7` / `H-P2` held open against repaired code, `LOCAL_CHANGE`'s over-cautious ADR-3 carry, and the refuted `GUARDS` item — are recorded in §3.14 and §3.16.

## 5. ID appendix

Transcribed from [`audits/2026-09-02_old_review_validity/INBOUND_REFERENCES.md`](audits/2026-09-02_old_review_validity/INBOUND_REFERENCES.md) §2. **This appendix is what keeps the tracked tree's citations meaningful after the archival.** An id with no entry here is a blocker.

### 5.1 `C-R*` / `H-R*` / `M-R*` / `L-R*` — the renderer-deep family (44 ids)

**Definition source:** `archive/principal-review-2026-04-16/PRINCIPAL_ENGINEER_REVIEW_RENDERER_DEEP_2026_04_16.md` (archived 2026-09-03), named authoritative at `ISSUES_AND_FIXED_BUGS.md:27`, `:345` and `README.md:421`. Dispositions and open locations were re-derived on 2026-09-03.

**Citation surface measured today:** 44 distinct ids across **59 tracked files** under `migration_doc/` (excluding the bank's 12), 43 comment lines in `packages/engine/Source`, 8 probes, 2 Tools libraries, 1 spec and 1 Sandcastle demo.

| ID | Definition (renderer-deep, 2026-04-16) | Disposition today | Still open at |
| --- | --- | --- | --- |
| `C-R1` | `command.renderState` is not consumed by the WebGPU renderer. Sub-ids `-RENDERSTATE`, `-CLASSIFICATION`, `-GLOBE-RENDERSTATE`, `-TILE-BATCH`, `-PRIMITIVE-DERIVED`, `-COLLECTIONS-PER-ENCODER`, `-RENDERSTATE-PER-RENDERER`, `-PRIMITIVE-DERIVED-PANORAMA-CULLMODE` | SUPERSEDED — owned by `DEFERRED_WORK.md:5104-5204` and rows `C11-36` / `C11-82` | Not open as a finding. `FEATURE_INVENTORY.md:1006` and `:1018` still list `C-R1-GLOBE-RENDERSTATE` / `C-R1-TILE-BATCH` as WIP against RESOLVED ledger entries — fix alongside archival |
| `C-R2` | `derivedCommands.*` never consulted on the WebGPU dispatch path (`-DERIVED-COMMANDS`) | RESOLVED — `DEFERRED_WORK.md:4906` (`NEW-DERIVEDCOMMAND-VARIANT-FACTORY`), `WEBGPU_MIGRATION_BACKLOG.md:21` | — |
| `C-R3` | Translucent commands not sorted back-to-front on WebGPU (`-TRANSLUCENT-SORT`) | RESOLVED — `WEBGPU_MIGRATION_BACKLOG.md:22` | — |
| `C-R4` | glTF model path silently drops multiple features (`-GLTF-KHR`) | **PARTIAL — CONFIRMED open.** KHR half owned at `DEFERRED_WORK.md:5210`; the two residual rows are owned by nothing | §3.3. Outline: 0 hits under `Renderer/WebGPU/**` and `Shaders/WebGPU/**`; generator Scene-side only. Exaggeration: 0 `exagger` hits in the model renderer, model pipeline cache and `ModelPBRComplete.wgsl`, while `Scene/Model/Model.js:365` still computes it |
| `C-R5` | Globe imagery layer count compile-time fixed at 4 (`-IMAGERY-16`) | RESOLVED — `DEFERRED_WORK.md:8888`; `IMAGERY_PROJECTION.md` is the live authority | — |
| `C-R6` | WebGPU primitive renderers use incorrect pass values | SUPERSEDED — `NEW-WEBGPU-COLLECTION-PASS-LITERAL-DRIFT` (`DEFERRED_WORK.md:13524-13548`); spot-check upheld, 0 numeric-literal passes remain | Residual slot drift only, self-documented at `WebGPUGroundPrimitiveRenderer.js:2400-2415` |
| `C-R7` | `_webgpuPipelineCache` declared but never instantiated. Sub-ids `-CENTRAL-PIPELINE-CACHE`, `-RENDERER-MIGRATION(-REMAINING)`, `-ROUTE-RENDERERS`, `-SHADER-MODULE-DEDUP`, `-COMPUTE-PIPELINE-CACHE` | RESOLVED — the CLAUDE.md pipeline-key section plus `Tools/visual-regression/pipeline-key-aliasing.spec.mjs`; `DEFERRED_WORK.md:7511` / `:7528` | — |
| `C-R8` | Scene-to-WebGPU: multiple invisible passes missing. **32 sub-ids** (`-SCENE-PASSES`, `-SCENE2D-JITTER`, the `-EDGE-*` family, the `-INVERT-CLASS-*` family, `-GLOBE-DEPTH-*`, the `-TRANSLUCENT-*` family, `-CLASSIFICATION-*`, `-GROUND-POLYLINE-NATIVE`, `-VECTOR-3DTILE-CLAMPED-POLYLINES`) | RESOLVED — `DEFERRED_WORK.md:7553-7708`, `WEBGPU_MIGRATION_STATUS.md:892-897`, `DEV_NOTES_primitives_classification.md` | — (the id survives as the Principle-7 origin story at `CLAUDE.md:114` and in the probe / Tools citations) |
| `C-R9` | Model, GroundPrimitive, Ellipsoid, Voxel and GaussianSplat renderers emit no pick commands. Sub-ids `-MODEL-PICK(-FAMILY)`, `-TAIL-RENDERERS`, `-MODEL-FEATURE-PICK`, `-MODEL-PICK-TRANSLUCENT`, `-VOXEL-PICK`, `-VOXEL-CELL-PICK(-TAIL)`, `-PICK-PATTERN-EXTRACT` | RESOLVED — `PICKING_ARCHITECTURE_STATE_2026-08-17.md` is the live picking authority; `DEFERRED_WORK.md:7796` / `:7906` / `:7947` / `:8151` | — |
| `C-R10` | Shadow maps skip point lights on WebGPU. Sub-ids `-POINT-LIGHT-SHADOWS`, `-POINT-LIGHT-RECEIVE(-GLOBE)`, `-GLOBE-POINT-LIGHT`, `-POINT-LIGHT-PCF`, `-CAST-LINEAR-DEPTH` | RESOLVED — `DEFERRED_WORK.md:8325` | `C-R10-CAST-LINEAR-DEPTH` is **parked, not open**, at `DEFERRED_WORK.md:8340` |
| `C-R11` | Per-frame bind group and texture view allocation in the post-process / effects hot path. Sub-ids `-BIND-GROUP-CACHING`, `-REMAINING-CONSUMERS`, `-EFFECTS-BGL-COLLECTION-CACHE` | RESOLVED — `WEBGPU_MIGRATION_BACKLOG.md:27`, `DEV_NOTES_postprocess.md:336` | — |
| `C-R12` | Device-loss recovery leaves stale GPU handles across caches (`-DEVICE-LOSS-WALK`, `-PER-OBJECT-CACHES`) | SUPERSEDED — owned by `C11-20` and `DEFERRED_WORK.md:8360` | `C11-20` is PARTIAL: `QUEUE_2026-07-18_CAMPAIGN11.md:1639`, `:2452`; code at `PointPrimitiveCollection.js:886-893`, `LabelCollection.js:1052-1064` (§3.11 `F1`) |
| `C-R13` | `WebGPUContext.destroy()` tears down the device before subsystems | RESOLVED in code | — |
| `C-R14` | **Cited, never defined** — appears only inside the range `C-R1..C-R14` | UNDEFINED (see §5.3) | `ISSUES_AND_FIXED_BUGS.md:345`, the only citation in the tracked tree |
| `H-R1` | Multi-frustum stencil clear is implicit / default-driven | RESOLVED — spot-check upheld: `WebGPURenderTarget.ts:399-425` defaults both `depthLoadOp` and `stencilLoadOp` to `clear`; the load-pass re-open at `WebGPUSceneRenderer.ts:2056-2062` explicitly overrides both to `"load"` | — |
| `H-R2` | Shadow cast path uses a raw colour command instead of a dedicated cast pipeline | **PARTIAL — CORRECTED, open, no owner.** Dedicated cast pipelines exist; the MASK / alpha-cutoff half is at WebGL parity | §3.7. `Shaders/WebGPU/Shadow/ShadowMap.wgsl:47` — `shadowCastFragment()` takes no inputs and has an empty body. Residual is **translucent-pass casters** and **clipping-plane discard on the cast** |
| `H-R3` | Pick pass does not include VOXELS or `pickMetadata` | RESOLVED — `PICKING_ARCHITECTURE_STATE_2026-08-17.md` | — |
| `H-R4` | `WebGPUPassState.applyToRenderPass` is dead code | RESOLVED | — |
| `H-R5` | `WebGLStubPipelineState` accumulator is write-only | RESOLVED — `scripts/bundleVariantPlugin.js` `WEBGPU_COMPAT_EXEMPTIONS`, CLAUDE.md compat-surface section | — |
| `H-R6` | `WebGPUShaderCache` keys on `descriptor.name` only, not a source hash | **STILL-VALID — CORRECTED: unreachable dead surface, not a latent live path.** Zero engine instantiations; `context.shaderCache` is the WebGL cache. Correct disposition: retire the class in the DX decomposition wave | §3.10. `WebGPUShaderCache.ts:414-418`; field `WebGPUContext.ts:655`, clear callback `:7603`; stated independently at `DEFERRED_WORK.md:6039`. No owner |
| `H-R7` | Globe tile commands lose `castShadows` / `receiveShadows` / `clippingPolygons` / `pickId` | RESOLVED — `CSM_DESIGN.md`, `FEATURE_INVENTORY` §B shadows | — |
| `H-R8` | `getPooledBuffer` is unkeyed on usage flags | **STILL-VALID — CONFIRMED, no owner** | §3.9. `WebGPUContext.ts:5705-5733` — pool `:703`, lookup `:5714` by `type` alone, band match `:5715-5717`, `usage` only on the miss path `:5730`. Zero engine callers, so a trap for the next caller |
| `H-R9` | Readback / `mapAsync` paths other than PickFB are unguarded | RESOLVED — Batch 213, `af49776ac3`, `WebGPUGPUCuller.ts:468-495` (both `mapAsync` calls inside `Promise.all(...).then(...).catch(...)` with a nested `try`/`catch` around each `unmap`) | **Annotation debt:** the definition is still un-annotated at `PRINCIPAL_ENGINEER_REVIEW_RENDERER_DEEP_2026_04_16.md:360`, and the resolution is recorded only in `audits/2026-04-30_ARCHITECTURE_PERFORMANCE.md:153`. **Transfer the annotation before that file is archived** — this appendix row is the durable replacement |
| `H-R10` | `_destroyFeatureRenderers` partially no-ops for critical feature renderers | STALE — spot-check upheld: `Renderer/GraphicsContext.ts:2340-2357` nulls slots and advances generations **by design**, device teardown releasing the GPU resources | — |
| `H-R11` | Debug modes broadly missing on WebGPU | **REFUTED — do not migrate** (§3.16) | — |
| `H-R12` | `derivedCommands.shadows.castCommands` not written by WebGPU commands | RESOLVED — `ISSUES_AND_FIXED_BUGS.md:214`, `DEFERRED_WORK.md:4906` | — |
| `H-R13` | Double-tonemap risk in the model path | RESOLVED — `QUEUE_2026-07-03` Q13 (`Q13-PLAIN-HDR-GAMMA-CORE`) | — |
| `H-R14` | Globe translucency dispatch missing | RESOLVED — `DEFERRED_WORK.md:5146` (`NEW-GLOBE-TRANSLUCENCY-MULTI-PASS`) | — |
| `M-R1` | Transient `createView()` / `createBindGroup()` at material upload time | RESOLVED in code | — |
| `M-R2` | Unbounded `_samplerCache` and `_bindGroupLayoutCache` on `WebGPUContext` | **STILL-VALID — CONFIRMED, no owner** | §3.9. `WebGPUContext.ts:698-699`, inserts `:5600-5604` / `:5623-5627`, cleared only `:5456-5457` and `:7568-7570` |
| `M-R3` | `JSON.stringify(descriptor)` keying for sampler / BGL lookup | **STILL-VALID — CONFIRMED, no owner** | §3.9. `WebGPUContext.ts:5599`, `:5622` |
| `M-R4` | Numeric-literal pass values | RESOLVED — folded into `NEW-WEBGPU-COLLECTION-PASS-LITERAL-DRIFT` with `C-R6` | — |
| `M-R5` | `debugShowBoundingVolume` / `debugOverlappingFrustums` / `receiveShadows` / `executeInClosestFrustum` / `pickOnly` never set on collection commands | **REVERSED: judged RESOLVED, actually PARTIAL and unowned** (§4 R2). `pickOnly` and the shadow-flag halves are genuinely closed | §3.5. WebGL sets it at `BillboardCollection.js:1220`, `PointPrimitiveCollection.js:842`, `PolylineCollection.js:802`/`:898`; the four WebGPU renderers never do; consumer `WebGPUSceneRenderer.ts:3652`. `DEFERRED_WORK.md:5012` covers only the `Buffer*` / `GeoJsonPrimitive` lane |
| `M-R6` | `WebGPULabelRenderer` looks up `getFeatureRenderer(0)` by numeric literal | RESOLVED | — |
| `M-R7` | Hand-rolled specular LOD approximation in `ModelPBRComplete` | RESOLVED | — |
| `M-R8` | WebGPU IBL uses a constant ambient term (no spherical harmonics) | RESOLVED — `FEATURE_INVENTORY` §B IBL | — |
| `M-R9` | Depth `sampleCount` mismatch risk with MSAA colour | RESOLVED — spot-check upheld: `WebGPUSceneFramebuffer.ts:321-329` carries colour and depth at the same `sampleCount`; the `sampleCount: 1` target at `:332-339` is the pick-ID target | — |
| `M-R10` | `ClearCommand` `colorMask` for channel-selective clear | **STILL-VALID — CORRECTED and widened, no owner.** The gap is the whole ignored `renderState`, not just `colorMask` | §3.10. `WebGPUContext.ts:318-327`, `clear()` `:4414`; WebGL `Renderer/Context.js:1331-1332`. Consumers to watch: the two shipped `stencilMask` `ClearCommand`s at `Scene.js:448-453` and `Cesium3DTileset.js:3558-3564` |
| `M-R11` | Depth clip (`depth-clip-control`) never enabled | **STILL-VALID — CONFIRMED, no owner** | §3.10. `WebGPURenderPipelineCache.ts:826` and `:1072` — the only two occurrences, no producer; `"depth-clip-control"` has zero occurrences under `packages/engine/Source` |
| `M-R12` | `ModelPBRComplete` `positionMC` in f32 aliases for ECEF-rooted 3D Tiles | RESOLVED — spot-check upheld: `ModelPBRComplete.wgsl:936-962` keeps the per-instance translation split (`:951-952`) and differences it in the split domain (`:959-961`) | — |
| `M-R13` | Orphaned WGSL stage shaders on disk | **PARTIAL — CORRECTED: four orphans, not five or six.** `ModelSilhouetteStage.wgsl` **is** wired; `ModelPointCloudStylingStage.wgsl` is do-not-remove scaffolding owned by `C18-P3` | §3.3. `ModelColorStage`, `ModelCPUStylingStage`, `ModelSplitterStage`, `ModelAtmosphereStage` — zero importers. Owner for the point-cloud file only: `DEFERRED_WORK.md:9839`, `QUEUE_2026-08-09_CAMPAIGN18.md:168`. `ISSUES_AND_FIXED_BUGS.md:175` needs the three-way split |
| `L-R1` | `WebGPUShaderCache.isDestroyed()` hardcoded to `false` | **STILL-VALID — CONFIRMED**, same dead-surface disposition as `H-R6`: retire with the class | `WebGPUShaderCache.ts:430` |
| `L-R2` | Placeholder-fill loop in `createEffectsBindGroup` | RESOLVED | — |
| `L-R3` | Duck-typed `_webgpuShaderType` check in `PrimitiveCommandHelpers.js` can misroute across contexts | STALE | — |

**Summary of the family:** 44 ids; **11 carry live content** (`C-R4`, `H-R2`, `H-R6`, `H-R8`, `M-R2`, `M-R3`, `M-R5`, `M-R10`, `M-R11`, `M-R13`, `L-R1`), of which **nine have no owning row anywhere**; one is refuted (`H-R11`); one is undefined (`C-R14`); the rest are closed with a named surviving record. `H-R9` carries an annotation debt discharged by its row above.

### 5.2 The other id families the references name

Every family below has a complete definition set — id, title, claim, status, evidence line — in the judgement file named, so none is lost by archiving its source document. What each needs is a transcribed table plus a repointed taxonomy row.

| Family | Ids the inbound references actually name | Definition source today | Definition set after archival | Still open |
| --- | --- | --- | --- | --- |
| `A.<n>` / `B.<n>` / `C.<n>` / `D.<n>` (cross-coupling, 2026-05-02) | `A.4` (2 runtime warning strings), `A.5`, `A.6`, `A.12`, `A.13`, `A.14`, `B.2`, `B.3`, `B.6`, `B.8`, `B.9`, `B.10`, `B.14`, `B.16`, `C.2`, `C.10`, `C.11`, `C.12`, `D.1`, `D.2` | `AUDIT_2026_05_02.md`, per `ISSUES_AND_FIXED_BUGS.md:25` and `:348` | `AUDIT_2026_05_02.judgement.json` — all 55 findings plus the summary, action-order and backflow sections. A one-line-per-id table (A.1-A.15, B.1-B.20, C.1-C.12, D.1-D.8 → title, final status, owning record) is the precondition on archival: ~25 id-only citations plus 56 source comments resolve only through it | `B.11` (§3.7), `C.6` (§3.1), `D.3` (§3.14), `D.6` (§3.14) |
| `B-<n>` / `C-P<n>` / `H-P<n>` / `M-P<n>` / `L-P<n>` (per-feature, 2026-04-16) | `B-2`, `B-5`, `B-6`, `B-9`, `C-P2`, `C-P3`, `C-P7` / `C-P7-RTE`, `C-P9`, `C-P10` (alias `BUG-3`), `C-P14`, `C-P15`, `C-P17`, `C-P18`, `H-P5`, `H-P7` | `PRINCIPAL_ENGINEER_REVIEW_PER_FEATURE_2026_04_16.md` (**KEEP-PART**), per `ISSUES_AND_FIXED_BUGS.md:26` and `:346`. **`M-P*` and `L-P*` have no taxonomy row anywhere** | `PRINCIPAL_ENGINEER_REVIEW_PER_FEATURE_2026_04_16.judgement.json` — B-1..B-9, C-P1..C-P18, H-P1..H-P22, M-P1..M-P12, L-P1..L-P6 | `H-P7` (§3.12, cited at `ARCHITECTURE.md:412-414`, **no row**), `C-P9` (§3.5), `H-P3`, `H-P8`, `H-P19`, the glTF `MASK` half of `H-P20`, `H-P21`, `L-P1`, `L-P3`, `L-P6` — all unowned. `H-P11` and `M-P7` are refuted (§3.16) |
| `A<n>.<m>` (Axis A) / `B<n>` (Axis B drift) (ultra review, 2026-06-11) | Cited by taxonomy rather than individually: `ISSUES_AND_FIXED_BUGS.md:23-24`, `:76-78`, `:347`; `DEFERRED_WORK.md:4948` defers five `NEW-*` ids to "the audit doc + sidecar JSON" | `audits/2026-06-11_ULTRA_REVIEW.md` + `_findings.json` (**KEEP-PART**) | `2026-06-11_ULTRA_REVIEW.judgement.json` — 161 grouped items covering all 235 findings by number and axis id | 60 items, all migrated in §3 with a current `file:line`. **The gap that forces KEEP-PART is the id-to-title index for the ~175 closed findings**, which §3 does not carry |
| `F5-<n>` / `ACTION-<n>` / `REJECT-<n>` / `GATE-<n>-ACCEPTANCE` (Fable 5, 2026-07-22) | `F5-02`, `F5-04`, `F5-16`, `F5-17`, `F5-18`, "audit **action 9**", and the eleven principles | `FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.md` | `FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.judgement.json` — 64 items. `QUEUE_2026-08-29_RESEARCH_DISPATCH.md:348`, `:393`, `:408`, `:416`, `:424` cite `F5-02/04/16/18` and "action 9" **by id**, so the register and that label must survive verbatim | 38 open, all migrated in §3. `REJECT-4`'s control-leg clause and two principles exist **only** in the source document — restored in §3.15 and §4 R5 |
| `B<n>` / `H<n>` / `HP-0<n>` / `M1` (fork-vs-upstream, 2026-07-13) | `B7` (`DEFAULT_PARITY_MATRIX_2026-07-18.md:35`), `HP-01`..`HP-08` (notably `HP-03`), and the ledger's whole `findings{}` key set | The audit itself (**KEEP-PART**) plus `FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md` §10 (one-line titles) | `FORK_VS_UPSTREAM_WEBGPU_ARCHITECTURE_AUDIT_2026-07-13.judgement.json` — 51 items | 34 open, migrated in §3. `B13` and `B14` are ledger keys with **no definition anywhere** (§5.3). The audit intentionally has no `H15`; preserve that numbering |
| `FAR-<nnn>` | `FAR-003`, `FAR-102`, `FAR-103`, `FAR-201`, `FAR-206`, `FAR-207`, `FAR-209`, `FAR-210`, `FAR-301`, `FAR-302`, `FAR-403`, `FAR-404`, `FAR-405`, `FAR-408`, `FAR-500`, `FAR-501`, `FAR-502`, `FAR-600`, `FAR-601` | `FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md` and its ledger — **not an archived document** | Unchanged; the plan stays the remediation authority | Definition is not at risk. **Tracking is:** `FAR-302`, `FAR-404`, `FAR-502`, `FAR-600`, `FAR-601` and `FAR-301`'s `WebGPUViewportQuad.ts` interior have **no row** in any queue, in `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md` or in `DEFERRED_WORK.md`. Separately, **a row in a closed queue is a record, not an owner** — re-home FAR-500/501, FAR-209/210 and FAR-403/405/408 out of `QUEUE_2026-07-15_CAMPAIGN8.md` / `CAMPAIGN9.md` before quoting them as live ownership |
| `NEW-*` (minted by the ultra review) | ~49 ids across `DEBUGGING_GUIDE.md`, `FEATURE_INVENTORY.md`, `DEV_NOTES_*`, `CSM_DESIGN.md`, `TAA_DESIGN.md`, `IMAGERY_PROJECTION.md`, `BUILD_AND_VARIANTS.md`, the queues, probes and four `packages/engine/Source` files | `DEFERRED_WORK.md`, not the review | Unchanged | **Nothing** — every id is defined in the ledger. The five at `DEFERRED_WORK.md:4948` (`NEW-MODEL-VS-MOTION-GATE`, `NEW-RENDERBUNDLE-AGING-DECOUPLE`, `NEW-RESOURCEMANAGER-KEY-EVICTION`, `NEW-CLUSTER-MULTIFRUSTUM-BOUNDS`, `NEW-SYNC-MOVEMAP`) need per-finding **text**, not definitions; the verified bank supplies it |
| Other ids cited by the archived docs but defined elsewhere | `FEAT-GAP-01`, `FEAT-GAP-09`, `DP-H19`, `DP-H41`, `STUB-NAGA`, `BUILD-VAR-MEASURE`, `BUILD-VAR-SCENE-AUDIT`, `CSM-33-1`, `ADR-2026-04-28`, `C-R12-PER-OBJECT-CACHES`, `NEW-CSM-VOLUMETRIC-CASTERS` | `DEFERRED_WORK.md`, `WEBGPU_MIGRATION_BACKLOG.md`, `FEATURE_INVENTORY.md`, `ISSUES_AND_FIXED_BUGS.md:188`, `archive/REVIEW_FIX_PROGRESS.md` | Unchanged | **Nothing at risk.** `NEW-CSM-VOLUMETRIC-CASTERS` is defined (`ISSUES_AND_FIXED_BUGS.md:188`, `NEXT_QUEUE_2026-07-04.md:101`) but **absent from `DEFERRED_WORK.md`** — mirror it so the campaigns' ledger carries the demand gate (§3.7 `B.11`) |

### 5.3 Cited but defined nowhere — these need a maintainer decision

Three ids are cited in the tracked tree and **defined in no document, plan row or queue**. None is caused by the archival, but each becomes unresolvable prose the moment its neighbourhood is edited, so each needs a **decision** in the same commit — not a migration.

| ID | Where it is cited | Why it is undefined | Decision needed |
| --- | --- | --- | --- |
| `C-R14` | `ISSUES_AND_FIXED_BUGS.md:345`, inside the range `C-R1..C-R14` — **the only citation in the tracked tree** | The renderer-deep review defines `C-R1`..`C-R13`; the fourteenth number was never written. Its judgement records the id as STALE, "cited, never defined" | **Correct the range to `C-R1..C-R13`** when `:345` is repointed. If the range is copied forward as-is, this document inherits a dangling id and it becomes a real blocker |
| `B13` (FAR ledger) | `FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json` — `findings.B13` = `{packages: ["FAR-102", "FAR-103"], state: "active", gate: "correctness"}` | No `B13` finding exists in the 2026-07-13 audit (its judgement enumerates `B1`..`B12`), none in the plan's §10 table, and no queue row | **Define it or drop it** before the ledger's `audit` field is repointed. An "active / correctness" ledger row whose finding text exists nowhere cannot be verified, executed or closed |
| `B14` (FAR ledger) | Same file, same shape, same packages | Same as `B13` | Same as `B13` |

## 6. Pending sections — honestly marked

Two 2026-09-02 documents belong to this review and are **not** folded into §3. Their status differs sharply.

### 6.1 The phase-1 internal register — PARTIAL AND UNRELIABLE, do not cite

[`audits/2026-09-02_ARCHITECTURE_REVIEW_PHASE1.md`](audits/2026-09-02_ARCHITECTURE_REVIEW_PHASE1.md) is linked here for provenance only. **Its findings are not reproduced in this file, and must not be quoted as premises.** Two independent reasons:

1. **Two of eighteen lenses ran.** The document discloses this itself: the verified inputs come from `gltf-models` and `geometry-primitives` only. The other sixteen — globe & imagery, 3D Tiles, collections, entity/datasource, picking, shadows/lighting, post-process, performance & compute, architecture/build, ownership-lifetime, frame-graph, shader-composition, precision-parity, async-and-readiness, testability-observability, planned-work-fit — produced **no verified output**, so its §4 cross-cutting synthesis is derived from what two lenses saw. Five further candidates (`GLTF-MODELS-1..5`) carry **no recorded vote** and fail its own survival bar without having been refuted.
2. **Four of its citations are verified false at HEAD.** Read this session:
   - `GEOMETRY-PRIMITIVES-1` ("one colour uniform, hard-coded red under `PerInstanceColorAppearance`") cites `WebGPUGroundPrimitiveRenderer.js:2132`. At HEAD `:2132` is a comment about reusing the inner primitive's pick ID, and `:1798-1801` reads `packClassificationColor(data, 24, primitive)` with a comment stating that a `PerInstanceColorAppearance` keeps its colour on the geometry instance "the way WebGL's `PER_INSTANCE_COLOR` varying does". **Per-instance classification colour landed at Batch 1389 (`e337646ea4`)**, so the finding's headline no longer holds.
   - `GEOMETRY-PRIMITIVES-2` ("bakes only `color`/`depthFailColor` at command build") cites `WebGPUPrimitiveCommands.ts:3002-3014`. At HEAD that block reads `primitive._batchTable`, `_batchTableAttributeIndices.color` and `.depthFailColor` and derives `hasInstanceColors` / `hasDepthFail`, so the anchor no longer supports the quoted claim. Whether the `show` / `offset` / `distanceDisplayCondition` half survives is **undetermined** and needs its own re-derivation.
   - **§2 and Appendix A** praise "a failed pipeline swaps to an explicit magenta `ErrorPipeline`" and cite `WebGPUModelRenderer.ts:3513-3557`. There are **zero** `ErrorPipeline` hits in that range — the symbol lives in `WebGPUModelPipelineCache.ts` (9 hits), and `:3513` is `function getCustomShaderEntries(`.
   - **§2** praises "the shadow-cast path reproduces the skinned VS rather than casting from the rest pose" and cites `WebGPUPrimitiveShadowCast.ts:277-283`. That 290-line file contains **no** `skin` reference.

   The last two are claims that a path is **correct**, so their falsity means the behaviour is unverified rather than broken — but the register errs in both directions, which is why nothing in it may be briefed without re-derivation. The document now carries this list as a banner at its head.

**Disposition.** The plan it ran under is [`ARCHITECTURE_REVIEW_PLAN_2026-09-02.md`](ARCHITECTURE_REVIEW_PLAN_2026-09-02.md), which defines the eighteen lenses and is the specification for the re-run. The phase-1 register itself is a lead list, not evidence. Anything from it must be re-derived at HEAD before it is briefed, filed or cited — including its five proposed rows. Running the sixteen missing lenses is the outstanding work; until then, treat every unlisted lens as **needing a pass, not as clean**.

### 6.2 The external landscape audit — usable; its ranked HIGH gaps

[`RENDERER_LANDSCAPE_AUDIT_2026-09-02.md`](RENDERER_LANDSCAPE_AUDIT_2026-09-02.md) is usable as written: it is an external-comparison audit (three.js `WebGPURenderer` + TSL, luma.gl 9 / deck.gl 9, the WebGPU platform, CesiumJS upstream and the geospatial field, plus PlayCanvas, Babylon.js and Bevy / wgpu / naga since its 2026-09-03 revision) with a per-claim census appendix. Its per-claim census was re-derived on 2026-09-03 (lane Erchirion) from the verification re-run: all 177 collected claims carry verifier votes (171 kept, 2 refuted, 4 split, 0 unverified), and the three added engines were mapped against the fork at `file:line` beside the original four. The HIGH list below did not change under that pass. Its rows remain ranked leads with named demonstrating targets — external claims verified at their sources, fork state read at `file:line` — not adjudicated findings of this review.

Its ranked **HIGH** gaps are reproduced here so the ranking is visible from the review authority. Ordering is by (unblocks-others, correctness risk, effort). Its MEDIUM list, "what NOT to copy" section and claim census are **not** reproduced; read them there.

| Gap | Statement | Demonstrated by |
| --- | --- | --- |
| **G1** | Take the 1.145 sync and re-derive the two WGSL twins it invalidates — clipping-polygon holes in the SDF atlas, and polygon draping onto the shared classification depth target. Unlocks C12, C13, C14, C09. The draping row shares the reopened S3 depth target, so the S3 discriminator must be re-run after the sync. | CesiumJS 1.145 |
| **G2** | Guard the RTE error-free transforms against **Metal reassociation** and add an Apple-adapter lane. The fork's hard 64-bit rule is silently voided on one vendor's compiler: the RTE chunk computes `highDifference + lowDifference` as plain f32 with **zero** contraction guards, and no Apple/Safari/macOS lane exists in `Tools/`. Risk if ignored: the FAR-camera L0-tile jitter class fixed at Batch 350 reappears for Safari and Chrome-on-macOS users with no probe able to detect it. | luma.gl 9.4 (`LUMA-G3`) |
| **G3** | Populate `ShaderDefine` bits from `navigator.gpu.wgslLanguageFeatures` and route `enable` emission through the preprocessor. Today there is **one** device-derived bit, **0** hits for `wgslLanguageFeatures`, and `enable` directives are string-prepended per renderer or duplicated into 13 `*_f16.wgsl` twins — prepends under the same define mask are exactly the aliasing class Batch 825 closed at the pipeline level. Enabler for the later platform rows. | WGSL CRD; three.js per-shader `enable` |
| **G4** | Wire **dual-source blending** as the single-pass translucent composite, sidestepping the multi-frustum MSAA topology FAR-404 plans for the MRT path. The feature is requested and exposed but the variant is unimplemented (0 hits for `@blend_src`) and no WebGPU-side capability predicate exists. WebGL2 has no dual-source blending — the documented SR-2 architectural-impossibility exception. | Chrome and Firefox 155 shipping `dual-source-blending` |
| **G5** | Close the WebGPU **snap** gates and port `surfacePosition`. The snap tier exists and its row is PARTIAL with the owed gates listed; `surfacePosition` is absent from `Scene/Snapping.js`. No new design — the Edge certification the fork already owes plus a small API port after G1, sequenced after the pick-readback measurement. | CesiumJS 1.144/1.145 `Scene.snap` |
| **G6** | Finish the WebGPU **voxel** path to upstream's default shader **before** adding the additive empty-space skip. The march preserves `stepSize` and shapes/padding are live, but the metadata-typed default CustomShader and GLSL user shaders are unrenderable and traversal stops at level 2. The audit adds only an ordering constraint and a cross-runtime oracle (cesium-native 0.56 renders the same extension). | upstream `VoxelPrimitive`; 3DTILES_content_voxels |
| **G7** | **GPU-driven traversal and draw generation for tiles.** The indirect-draw manager is scaffolded, the culler's INDIRECT mode zeroes `instanceCount` with the hint at `never`, `_drawCountBuffer` is CPU-written and never consumed, tile/voxel/splat selection is CPU-side, and the culler accepts exactly six planes. No new track — this is the meshlet Phase M3 generalised; the audit's contribution is an arbitrary-plane culling-volume input so the CPU and GPU cullers accept the same shape, and re-examining the packed-32-bit visbuffer trade-off before Tier-1 rows are briefed. | luma `GPUVirtualGeometrySelection` / `GPUSceneDrawGeneration`; three.js TSL rasterizer; cesium-native culling volumes |
| **G8** | The GPU **splat** path: compute sort first, residency behind the C18-S7 ruling. The renderer consumes the shared WASM radix-sort permutation and there is no GPU sort. A counting sort over 4096 depth bins is simpler than a full radix sort and fits the storage-buffer layout; WASM remains the WebGL and fallback path. Nothing here changes the gates. | `@luma.gl/splats`; three.js r186 four-pass counting sort |
| **G9** | A **frame graph** that owns pass order and transient lifetime. No graph exists; passes are hand-ordered, the attachment-demand registry is observational with `forceSceneMRT` true, and there are 51 `queue.submit(` sites in 34 files. Already the fork's ADR-4 / FAR-40x plan; luma's "compiled graph owns transients, borrows imports" and compile-once/encode-per-frame model are the reference. Ordering: observe-only compiler before any authority switch — **and a graph cannot land on 5,140-line files**, so it follows the DX decomposition. | luma `GPUCommandGraph`; three.js `RenderPipeline` fusion |
| **G10** | **One scene executor** over the backend boundary. The fork instantiates a 5,140-line `WebGPUSceneRenderer` as `_alternateSceneRenderer` beside `SceneRenderer.js`, 12 Scene files reference `isWebGPU`, and `GraphicsContext` covers frame/draw/clear/readback but not pipeline keys, shader generation or compute. Principle 2 taken to its conclusion; depends on G9, the DX decomposition and FAR-300 semantic packets. **Not dispatchable this wave** — recorded so the gap list is honest about where the field went. | three.js's single `Renderer` over an abstract `Backend` |

## 7. What this review did not cover

Stated plainly, because the gaps are the residual risk in archiving anything.

### 7.1 383 closed items that no second reader re-derived

Each judgement marked a set of items RESOLVED / SUPERSEDED / STALE. Verifiers spot-checked **57** of those 440 (2 as bonus checks), chosen for blast radius rather than at random — so the sample rate bounds nothing. **Five of the 57 re-read items reversed** (§4). The remaining **383 rest on a single author's reading.**

| Document | Closed by judgement | Spot-checked | **Never re-derived** |
| --- | ---: | ---: | ---: |
| `AUDIT_2026_05_02` | 56 | 6 | **50** |
| `audits/2026-04-30_ARCHITECTURE_PERFORMANCE` | 32 | 5 (+1) | **26** |
| `audits/2026-04-30_FORK_FEATURE_INVENTORY` | 45 | 5 | **40** |
| `audits/2026-04-30_MAINTAINABILITY_SURVIVABILITY` | 22 | 6 | **16** |
| `PRINCIPAL_ENGINEER_REVIEW_RENDERER_DEEP` | 39 | 5 (+1) | **33** |
| `LOCAL_CHANGE_AUDIT_2026-07-31` | 46 | 5 | **41** |
| `FABLE5_PROGRESS_AND_ACTION_AUDIT` | 26 | 5 | **21** |
| `PRINCIPAL_ENGINEER_REVIEW_PER_FEATURE` | 56 | 5 | **51** |
| `FORK_VS_UPSTREAM…2026-07-13` | 17 | 7 | **10** |
| `audits/2026-06-11_ULTRA_REVIEW` | 101 | 6 | **95** |
| **Total** | **440** | **57** | **383** |

**Mitigation, and the reason archiving can still proceed:** the ten `*.judgement.json` files stay in the bank permanently, so every closed item keeps its id, title, claim, status and evidence line. Archiving moves the prose; it does not lose the record. But a wrongly-closed item nobody reads again is invisible until it bites, and five of fifty-seven is not a reassuring rate.

### 7.2 The sixteen unrun lenses

Of the eighteen architecture lenses the review plan specifies, **two ran** — `gltf-models` and `geometry-primitives` — and their output is the unreliable phase-1 register (§6.1). The sixteen with no verified output are: globe & imagery, 3D Tiles, collections, entity/datasource, picking, shadows/lighting, post-process, performance & compute, architecture/build, ownership-lifetime, frame-graph, shader-composition, precision-parity, async-and-readiness, testability-observability, and planned-work-fit.

**§3 is therefore a migration, not a survey.** It says what the ten retired reviews found and what is still true of it — not what a fresh eighteen-lens pass over today's renderer would find. Treat every unlisted lens as needing a pass.

### 7.3 One sub-claim nobody could reach

`F5-09`'s allocation-rate claim — **"one bound function allocated per visible model per frame"** — was never measured when written and is not measured now. The rest of `F5-09` is adjudicated (§3.1). It needs a measurement, not a re-read, so no amount of document work closes it. Carried forward explicitly as **unverified**.

### 7.4 One coverage figure not re-derived read-only

`HP-01`'s "dozens of unlabelled pass-creation sites" is a **runtime-coverage measurement**, not a source fact; the verifier re-derived only the repair-and-gating half (§3.9). Do not quote the figure without measuring it.

## 8. Counts

| Bucket | Count |
| --- | ---: |
| Documents adjudicated | 10 |
| Items enumerated across them | 676 |
| Open items re-derived at today's code | 236 |
| — CONFIRMED (still true as judged) | 174 |
| — CORRECTED (real; scope, owner, severity or number was wrong) | 57 |
| — REFUTED (premise no longer holds; do not migrate) | 5 |
| — UNADJUDICATED (whole items) | 0 |
| Sub-claims left explicitly unverified | 1 (`F5-09` allocation rate) |
| Findings migrated into §3 | 231 |
| Reversals (§4) | 5 |
| Ids in the appendix (§5.1) | 44 |
| Ids cited but defined nowhere (§5.3) | 3 |
| Closed items no second reader re-derived (§7.1) | 383 |
| Architecture lenses with no verified output (§7.2) | 16 |
| Documents archived | 7 |
| Documents staying live as KEEP-PART | 3 |

Verifiers: Telumehtar, Ciryandil, Atanatar, Tarannon, Earnil, Egalmoth, Belecthor, Earnur, Tarondor, Telemnar. Bank compiler: Ciryon. This review: Earendil.
