# Campaign 5 — Next Queue (assembled 2026-07-05)

HEAD at assembly: `1fdc862617` — Batch 593 (C4-WEBGPUCONTEXT-DECOMP). Campaign 4 = Batches 562-593.

Scope: everything Campaign 4 landed is EXCLUDED. Every task below was premise-verified against LIVE CODE + git log at HEAD. Each carries a VERIFY-PREMISE-FIRST instruction, a files-hint, an approach, a named probe, and an off-gate so an implementer can start with no external doc dependency.

**Verified-open work units: 31** (23 discrete + Q34 8-batch arc). **Queued: 25** (23 + 2 representative TS batches). **Likely-stale (reconcile only): 3.** **By-design/deferred-forever: 17.**

---

## Tier 1 — Real parity bugs + newly-surfaced follow-ups (do FIRST)

| # | ID | Eff | Brief |
|---|----|-----|-------|
| 1 | NS-POLYLINE-COLLECTION-MULTI-MATERIAL | M | Mixed-material PolylineCollection: every non-Color group renders as a solid line on WebGPU (Dash loses dash, Glow loses taper). Single-material collections are at parity. Suspect the per-group loop in `WebGPUPolylineRenderer.js` (~L287 grouping, L1422-1460 per-group pipeline/bind-group) reuses the first/Color pipeline for all groups. Also blocks upstream v1.143 PathGraphics.materialMode=PORTIONS. VERIFY: build a probe drawing a collection with Color+Dash+Glow lines side-by-side, diff dash runsPerRow (WebGPU=1 vs WebGL=15). Probe: new `probe-polyline-multimaterial.mjs`. Off-gate: single-material collections must stay byte-identical. |
| 2 | NEW-GROUNDPRIM-CLASSIFIER-RECON-PRECISION | M | Textured GroundPrimitive classification (Checkerboard) degrades toward far corner of large polygons — log-depth eye-space reconstruction precision. The consumer-reverse half it waited on is now live (renderer-wide log depth + GroundPolyline reverse). VERIFY: re-run probe first — may have narrowed. Apply windowToEye log-reverse in the classifier fragment reconstruction. Files: WebGPUGroundPrimitiveRenderer + its classify WGSL. Probe: `probe-classifier-textured-materials.mjs` / `probe-classifier-logdepth-flip.mjs`. Off-gate: near-corner parity unchanged. |
| 3 | NEW-GEOJSON-WEBGPU-BV-DEBUG-DRAW-PASS | S | `debugShowBoundingVolume` now plumbs through GeoJsonPrimitive + all three Buffer* WebGPUDrawCommands (Batch 583) but WebGPU has NO BV debug DRAW pass — flag is a visual no-op (overlay delta 0.000% vs WebGL red wireframe). Add a default-off debug overlay pass in `WebGPUSceneRenderer.ts` mirroring `SceneDebug.debugShowBoundingVolume` (consume per-command flag, draw sphere/box wireframe). VERIFY: confirm no pass consumes the flag yet. Probe: `probe-geojson-holes.mjs` (extend to assert overlay non-zero when flag on). Off-gate: flag off = zero delta. |
| 4 | NS-VOXEL-PICK-FOOTPRINT-SPURIOUS-ROOT | M | `scene.pickVoxel` returns a VoxelCell on WebGPU (B570) but empty/off-box pixels yield a SPURIOUS root cell — WebGPU object pick covers the whole box footprint (WebGL returns undefined there). Fix: gate the CPU VoxelCell build on an actual ray-box/occupancy hit, not object-pick footprint. Files: voxel pick path in scene + WebGPU pick renderer. VERIFY: pick an off-box pixel, assert undefined. Probe: extend voxel-pick probe. Off-gate: on-box picks unchanged. |
| 5 | NS-VOXEL-REFINED-TILE-CELL-RETENTION | M | GPU voxel pick-march reaches octree L3 (Q9 stale) but CPU VoxelCell construction for refined tiles (megatextureIndex >= 1) is missing — child content not retained CPU-side. Fix: retain child tile content CPU-side so refined-tile picks build a full VoxelCell. Files: voxel provider/traversal + pick cell builder. VERIFY: pick a refined-tile voxel, assert cell has correct metadata. Off-gate: root-tile picks unchanged. |
| 6 | NS-CLUSTER-MULTIFRUSTUM-BOUNDS | M | Clustered-lighting A7.6: cluster bounds computed for a single collapsed near/far spanning full visible depth, so multi-frustum scenes get approximate Z-slice light binning. B572 fixed bounds-dirty re-dispatch but left this. Fix: per-frustum-slice cluster grids + per-slice re-dispatch + per-slice consumer binding. File: `WebGPUSceneRendererClusteredLighting.ts:208-219`. Opt-in, off by default. VERIFY: multi-frustum scene light-binning. Off-gate: single-frustum + clustering-off unchanged. |
| 7 | NEW-SPLAT-MULTIFRUSTUM-DEPTH-COMPOSE | L | Gaussian-splat frag_depth VALUE is correct but the single-pack full-frustum-UBO splat command is occluded by ANY opaque geometry in multi-frustum scenes (3 frustums even at low altitude). Fix: per-frustum UBO re-pack (re-encode projection + log-depth near/far/factor per slice) or a per-frustum updateCommandUniforms hook in the scene-renderer frustum loop. Same class as NEW-MULTIFRUSTUM-CULL-RESULTS. VERIFY: splat-over-globe multi-frustum probe. Off-gate: single-frustum splat unchanged. |
| 8 | NS-DECOUPLEDSCAN-NO-SCENE-WIRING | L | Decoupled-lookback prefix-sum scan compiles + has a forward-progress guard (B573 fixed a latent storageBarrier compile error that left it silently dead), but its sole consumer `WebGPUPointCloudLODProcessor` has NO Scene wiring — opt-in deterministic-LOD path never reached in-product. Fix: wire the processor into the Scene point-cloud LOD path behind an opt-in flag. VERIFY: confirm scan path is actually dispatched. Off-gate: default path unchanged. |
| 9 | NS-GPU-SORT-NO-SCENE-WIRING | M | GPU front-to-back opaque sort consumer is wired end-to-end + correct (B578 fixed 3 latent Phase-2 bugs) but gated default-OFF behind `CesiumDebug.gpuSortConsume` with NO production heuristic. Fix: add an auto-enable/thresholded Scene heuristic (e.g. opaque-command-count threshold) promoting it to a live path. VERIFY: confirm current path is debug-flag-only. Off-gate: below threshold / flag-off unchanged. NOTE: distinct from the stale ROADMAP §4.7 duplicate row (reconcile separately). |
| 10 | NS-MVT-SYNC-DECODE-OFFTHREAD-WASM | L | MVT datasource renders at WebGL<->WebGPU parity (B584) but `decodeMVT` runs synchronously on main thread. Move decode off-thread / to WASM (Worker + WASM bridge with JS fallback per WASM strategy). Perf item, not visual. VERIFY: confirm decode is still sync. Probe: MVT parity probe must stay green. Off-gate: parity + fallback path unchanged. |
| 11 | NEW-WEBGL-REPROJECT-BASELINE | S | WebGL imagery reprojection was intentionally forked (64-row grid -> 4-vertex quad + per-fragment Mercator), forking WebGL pixel output from upstream with NO visual-regression tolerance baseline to catch accidental drift. Fix: add the missing baseline guard (capture WebGL reproject output as a tolerance baseline). Doc: `IMAGERY_PROJECTION.md`. VERIFY FIRST: check whether a baseline has since landed. Off-gate: pure test-infra add, no runtime change. |
| 12 | NEW-CLASSIFIER-2D-CV-MORPH | L | Vector3DTile* classifiers (Primitive/Polylines/ClampedPolylines) silently skip in SCENE2D/COLUMBUS_VIEW/MORPHING — need CPU/shader projection of RTC-relative positions to a 2D attribute path. CAUTION: the skip-gate is BETTER than upstream WebGL (which renders wandering volumes) — do NOT lift it naively. Also blocked on missing `.vctr` test data for pixel verification. Files: per-renderer gating in Vector3DTile* renderers. VERIFY: needs a classic .vctr scene first. Off-gate: 3D-mode classify unchanged; keep skip if projection unavailable. |
| 13 | MORPH-EXAG-SKIRTS | L | GroundPrimitive under CV/MORPHING with vertical exaggeration turns terrain skirts into walls (Batch 216 revert) + ~1s disappearance transient during morph. Fix: WebGL-faithful skirt handling in the planar leg. NOTE: plain SCENE2D/CV GroundPrimitive already works (Batch 156) — this is the exaggeration+morph leaf only. VERIFY: exaggeration+morph probe. Off-gate: non-exaggerated CV/2D unchanged. |
| 14 | NS-R2B-FEATURE-ID-RESIDUAL | M | R-2b unified feature-ID texture CORE shipped (shader-samplable via `resolveFeatureIdRecolorAsync` + `FeatureIdResolve.wgsl`, default-off, B587) but 3 residuals stay open: (a) standing per-frame post-process wiring, (b) a source-tag channel, (c) an imagery per-texel ID channel. Pick one increment (recommend (a) PP wiring). Doc: `RESEARCH_AND_PENDING_TOPICS.md:352`. VERIFY: confirm the 3 residuals are still distinct. Off-gate: default-off. |
| 15 | NS-R2A-CROSS-SOURCE-ATTR-JOIN | L | Cross-source attribute-unification audited to BLOCKED-on-R-2b (no shared GPU-visible attribute namespace across globe/3D-tile/model/voxel/imagery). After R-2b + imagery per-texel ID prerequisite, the join is a thin default-off PP LUT pass with a WebGL2 texelFetch twin. Doc-only audit landed (B586); implementation untouched. DEPENDS ON #14. Doc: `R-2A_CROSS_SOURCE_ATTRIBUTE_AUDIT.md`. Off-gate: default-off. |
| 16 | TAA-SLICE-2B-MODEL-MRT-MOTION | XL | TAA depth-reprojection treats skinned/morphed/instanced geometry as static -> ghosting. Add a 2nd MRT color attachment to every model pipeline emitting per-pixel velocity (currentClip-previousClip) with prev-frame joint/morph/instance UBOs + teach the TAA shader to prefer MRT samples. Touches every model pipeline + model UBO layout + TAA shader. Coupled to the @location budget (see #19) and MORPH-TAA-PREVVP. Doc: `ROADMAP_AND_DEFERRED_WORK.md:732`. Off-gate: TAA-off + static-geometry paths unchanged. |

## Tier 2 — Epic continuations (deferred later slices of C4 epics)

| # | ID | Eff | Brief |
|---|----|-----|-------|
| 17 | Q30-LOGDEPTH-PP-SLICE-B | L | Log-depth PP consumer-reverse half. Slice A (B589) threaded live camera frustum near/far + a `logActive` flag into AO/DoF/GodRay UBs but the lane is pure scaffolding — VERIFIED no WGSL reads frustum.z (`reverseLogDepth` absent from AmbientOcclusion/DepthOfField/SSR/ContactShadows/GodRay .wgsl; present only in Aerial/VolumetricFog). Apply `csm_reverseLogDepth` gated on `logActive` in AO/GTAO, DepthOfField, ScreenSpaceReflections, ContactShadows, GodRay + the HiZ occlusion projected-z test + model fragmentClassificationMain. SSR/ContactShadows already read projection live. Off-gate: all consumers default-off => no default-scene risk; logActive=0 byte-identical. |
| 18 | Q31-CUSTOMSHADER-SLICE-B-MATERIAL-FIELDS | M | WGSL CustomShader `czm_customModelMaterial` bridge exposes only diffuse/specular/roughness/emissive/alpha; add metalness/occlusion/normalEC (normalEC requires re-deriving lighting from the perturbed normal through the writeback). Unblocked by the pre-lighting MODIFY_MATERIAL seam (B590). File: `ModelPBRComplete.wgsl` (sole carrier of czm_customModelMaterial). VERIFY: confirm only 5 fields exposed. Off-gate: shaders not setting new fields unchanged. |
| 19 | Q31-CUSTOMSHADER-SLICE-C-VARYINGS | L | Custom vertex->fragment user varyings + extra attributes (positionWC, tangentEC/bitangentEC, texCoord_1+) for the WGSL CustomShader path. BLOCKED on the interstage @location budget — TAA occupies @location 9-10. Needs a varying-budget allocation scheme before user slots can be handed out. DEPENDS ON #16 (TAA MRT layout decisions). Doc: `DEFERRED_WORK.md:1263`. Off-gate: shaders without custom varyings unchanged. |
| 20 | Q35-WEBGPUCONTEXT-DECOMP-REMAINDER | L | `WebGPUContext.ts` decomposition remainder. After the HDR canvas-output cluster extraction (B593) the file is still 5432 LOC (VERIFIED) vs the ~1000-line guideline. Two clusters remain as host-interface free-function extractions: the culler-pool cluster and the SceneRenderer pass-family cluster. Follow the B593 extraction pattern (free functions taking a host interface). Off-gate: mechanical — runtime byte-unchanged; `tsc --noEmit` clean. |
| 21 | CLOUD-EXOTIC-E1-SPECIES | M | Tier E1 exotic clouds (NET-NEW fork feature, beyond WebGL parity) — species/varieties as density shaping on the baked-density-field `ProceduralClouds.wgsl` arch: lenticularis (smooth wind-aligned lens/orographic), fibratus/uncinus. Each a bounded per-genus-gated density mode, following the `mammatusFactor` precedent (B592). Doc: `CLOUD_TAXONOMY_ROADMAP.md:86`. Off-gate: per-genus gated, default genera unchanged. |
| 22 | CLOUD-EXOTIC-E2-REMAINING | M | Tier E2 remaining supplementary features after mammatus (NET-NEW): asperitas (turbulent wavy underside), Kelvin-Helmholtz/fluctus (wave displacement), arcus (shelf/roll leading edge), virga/praecipitatio (density tail below base). Each a bounded underside/wave density mode on `ProceduralClouds.wgsl`, per-genus gated (~S per form). Same arch as B592 mammatus. Doc: `CLOUD_TAXONOMY_ROADMAP.md:109`. Off-gate: per-genus gated. |
| 23 | CLOUD-EXOTIC-E3-SPECIAL | XL | Tier E3 special clouds needing NEW decks/sources (NET-NEW): noctilucent + nacreous (high iridescent shell = separate high-altitude deck), contrails (line sources), pyrocumulus (event/data-driven). Requires new infrastructure, NOT the baked-density-field foundation — likely decomposed per form. Doc: `CLOUD_TAXONOMY_ROADMAP.md:93`. Off-gate: new decks default-off. |

## Tier 3 — Held-XL TS-convert arc (2 representative batches; arc has 8 candidate batches total)

Q34 arc = bulk JS->TS of the 27 remaining untyped WebGPU `.js` renderers (~44.9K LOC at HEAD; doc's "~25 renderers/35K LOC" + "Model 3802 LOC" are STALE — re-measured 5726). One renderer per batch, largest first, `tsc --noEmit` clean per file, runtime byte-unchanged. StarField already `.ts` (B314). The other 6 tail batches (B3 ModelPipelineCache 4005, B4 GroundPolyline 3260, B5 GroundPrimitive 2860, B6 Polyline 1979, B7 Vector3DTilePrim 1684, B8 Billboard 1551, + ~18 more S/M) ride the same pattern.

| # | ID | Eff | Brief |
|---|----|-----|-------|
| 24 | Q34-B1-MODELRENDERER-JS-TS | L | Convert `WebGPUModelRenderer.js` (5726 LOC, VERIFIED) -> `.ts`. Biggest single renderer; heavy pipeline/pick/CSM surface so typing is L despite mechanical nature. Use co-located `.d.ts` interop pattern for untyped JS deps. Off-gate: runtime byte-unchanged; `tsc --noEmit` clean; no `any`. |
| 25 | Q34-B2-PRIMITIVECOMMANDS-JS-TS | L | Convert `WebGPUPrimitiveCommands.js` (5054 LOC, VERIFIED) -> `.ts`. Off-gate: runtime byte-unchanged; `tsc --noEmit` clean; no `any`. |

---

## Reconcile-only (LIKELY-STALE — 1 doc reconcile batch, no implementation)

These doc rows contradict landed batches; fold into one reconcile batch:

- **NEW-GPU-SORT-PIPELINE-PHASE-3** — ROADMAP §4.7:573 + ISSUES:165 still say OPEN, but B578 wired the CPU-side reorder consumer and `DEFERRED_WORK.md:773` marks it RESOLVED. (The genuinely-open follow-up is the Scene auto-enable heuristic = queued task #9.)
- **GEOJSON-PRIMITIVE-PARITY-PROBE-MISSING** — ISSUES §3.2 says GeoJsonPrimitive lacks a MultiPolygon/hole parity probe + has a no-op debugShowBoundingVolume, but B583 landed `probe-geojson-holes.mjs` AND plumbed the flag. (Genuine residual = the WebGPU BV draw pass = queued task #3.)
- **Q32-ATMOSPHERE-UNIFICATION-REMAINING** — premise verified STALE by B591: all 4 sky-integral consumers already share the ONE sky/MS LUT; `probe-atmosphere-unification.mjs` (18/18 PASS) locks the invariant. No unification work remains.

---

## Deferred-forever / By-design tail (17 items — NOT parity gaps)

**Two have a genuinely cheap byte-neutral entry increment worth reconsidering (both NET-NEW beyond-parity, not parity gaps):**

- **WATER-PHASE-1-FOUNDATION-SMOKE** (M) — land ONLY the Phase-1 Foundation slice of the WATER-PHASES-1-9 epic: WaterClassificationProvider skeleton (pluggable per-globe), renderer-internal RGBA mask-texture cache falling through to the existing waterMaskTexture, WaterRegion API + scene flags on the already-shipped `scene.globe.water` facade (`GlobeWater.js`). NO shader changes (Gerstner/Fresnel is Phase 2). Default-off => byte-identical; ship a validation-gate smoke probe (skeleton wires up, 0 device errors, legacy water path unchanged). Cheap because the consumer half already exists. VERIFIED: no WaterClassificationProvider/Gerstner in tree.
- **VEGETATION-V1-CORE-SCOPING** (S) — a scope-lock slice (no runtime code) of the VEGETATION-V1-V5 epic: pin the exact V1 artifact list (Scene/VegetationLayer.js + VegetationScatterCollection + FeatureRendererKey.VEGETATION_SCATTER + WebGPUVegetationScatterRenderer.ts + Shaders/WebGPU/Compute/VegetationScatter.wgsl), resolve the GPU-cull-gate decision (Pass.CESIUM_3D_TILE vs Pass.OPAQUE), confirm the ~80% reusable infra (GPU cull/indirect/instancing/metadata/RTE/stochastic dither) is live. Design complete; V1 implement (L) follows the lock. VERIFIED: zero code shipped.

**By-design, no cheap slice (survey result — do NOT queue):**

- NEW-GLOBE-FARZOOM-DRAPE-BRIGHTNESS-TUNE — root-caused B582 to WGSL ground-atmosphere drape landing ~2 lum brighter than GLSL; residual sub-perceptual (0.033% of crop) over a DOCUMENTED intentional WGSL-per-fragment vs GLSL split. Low-payoff/high-risk polish, not a defect. (Both miners surfaced this; reconciled to BY-DESIGN.)
- NS-RENDERBUNDLE-R7A-NOT-ACTIONABLE — R-7 profiled B585: no per-pass CPU recording cost clears 3 ms (peak 3dTiles 1.02 ms). R-7a shortlist not-yet-actionable; R-7b/e/f SKIP. Revisit only if a heavier scene profile changes the numbers.
- Q36-WEATHER-PHASE-4-GRIB2 — hard-blocked: real HRRR/GFS/NBM ingest needs a same-origin NODD-S3 proxy (UNBUILT in dev env) + GRIB2 Worker/WASM parse + Lambert-Conformal->equirect reproject. Build the proxy first or defer.
- The remaining 12 (voxel-octree-deep-levels, CSM-volumetric-casters, classifier-2D-CV-morph-vs-upstream, starfield-tune, voxel-customshader-residuals, RTE-ellipsoid-nonEarth, PP-f16-device-verify, custom-shader-transpile, shadow-alternatives, scene-in-worker, watch-bucket, WGSL-preprocessor-v2) each re-read at their gate — none flip to cheap in git log 562+. None are parity gaps.

---

## Recommendation

**Run Campaign 5 as the parity-tail + epic-continuation campaign (Tiers 1-2), NOT the TS-convert campaign and NOT a beyond-parity pivot.**

Reasoning:
- Tier 1 holds the last genuine cross-backend correctness bugs — the multi-material PolylineCollection solid-line regression (#1), the two voxel-pick defects (#4/#5), the GPU-sort/decoupled-scan/cluster "built-but-unwired" trio (#6/#8/#9), and the splat multi-frustum occlusion (#7). These are user-visible or leave landed infrastructure dead; they are exactly what a parity campaign exists to close.
- Tier 2 finishes epics C4 deliberately half-landed with the hard part scaffolded (log-depth Slice B consumers verified genuinely unwired; customShader Slice B unblocked by B590's seam; WebGPUContext still 5.4k LOC). Leaving these half-done is the partial-implementation debt CLAUDE.md Principle 7/9 warns against.
- The Q34 JS->TS arc (Tier 3) is mechanical, large-surface, and pure hygiene with no runtime payoff — correct to sample 2 batches to keep it moving but wrong to let it crowd out real bugs. Save the bulk for a dedicated mechanical sweep.
- Beyond-parity (water/vegetation/clouds E1-E3) is net-new feature work. The cloud E-tiers (#21-23) earn a Tier-2 slot only because the arch is live and each form is a bounded density mode; water/vegetation stay optional entry-increments until the parity tail is drained. A beyond-parity pivot now would leave real bugs standing.

Sequence: Tier 1 (#1-16) -> Tier 2 (#17-23) -> optionally 2 TS batches (#24-25) as filler between heavier items. Respect deps: #15 after #14; #19 after #16.
