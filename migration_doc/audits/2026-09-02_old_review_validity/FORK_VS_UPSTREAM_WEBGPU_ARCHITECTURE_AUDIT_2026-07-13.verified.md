# Verification — FORK_VS_UPSTREAM_WEBGPU_ARCHITECTURE_AUDIT_2026-07-13

**Document under verification:** `migration_doc/FORK_VS_UPSTREAM_WEBGPU_ARCHITECTURE_AUDIT_2026-07-13.md` (862 lines)
**Prior judgement verified:** `FORK_VS_UPSTREAM_WEBGPU_ARCHITECTURE_AUDIT_2026-07-13.judgement.json` (51 items)
**Verifier:** Tarondor
**Date:** 2026-09-03
**Tree verified against:** `532463ae35` (Batch 1390), working tree as found

## Counts

| Bucket                                        | Count |
| --------------------------------------------- | ----- |
| Items re-verified (STILL-VALID + PARTIAL)      | 34    |
| CONFIRMED                                      | 24    |
| CORRECTED                                      | 9     |
| REFUTED                                        | 1     |
| UNADJUDICATED                                  | 0     |
| Spot-checks of RESOLVED/SUPERSEDED items       | 7     |
| Spot-check reversals                           | 0     |
| Sub-item reversals found inside PARTIAL items  | 2     |

Every line number below was read in this session. The prior auditor's citations were treated as
leads only; where they had drifted, the current location is given.

## 1. Verified STILL-VALID and PARTIAL items

| ID  | Title | Verdict | Current file:line read today | Owning row | Migrate text (corrected where needed) |
| --- | ----- | ------- | ---------------------------- | ---------- | ------------------------------------- |
| B4 | HiZ visibility is global while depth and command lists are per frustum | CONFIRMED | `WebGPUSceneRenderer.ts:1279-1283` (one `_lastHiZVisibility {flags,count}` + one `_hiZReadbackInFlight`), `:4174-4175` accepts on `prev.count !== count` alone, `:1189` `_hiZConsumeEnabled=false`, `:1258` `_hiZActiveByFrustum`, `:4194-4198` the "not until each result identifies its producing frustum, frame, and command generation" comment | FAR-500 (rows in QUEUE_2026-07-15_CAMPAIGN8/9 + QUEUE_2026-07-18_CAMPAIGN11); FAR-003 containment | As judged. **Correction to the owning record, not the finding:** `DEFERRED_WORK.md:5338` still declares FORK-41 "RESOLVED (C2-21) — command-drop now DEFAULT ON, verified", which contradicts `_hiZConsumeEnabled=false`. The new review must reconcile that row before quoting it. |
| B5 | Tile indirect parameters are overwritten before recorded draws execute | CONFIRMED | `WebGPUSceneRenderer3DTilePasses.ts:103` `INDIRECT_BATCH_MIN = 32`, `:109-113` `normalizeTileIndirectMode` fails closed to `never`, `:116-135` `resolveTileIndirectPolicy`; `WebGPUContext.ts:725` `useIndirectDrawForTiles=false`; `WebGPUSceneRenderer.ts:579-581` `manager.beginFrame()` "once per pass invocation"; `WebGPUIndirectDrawManager.ts:150-154` resets `_drawCount` | FAR-501 (QUEUE_2026-07-15_CAMPAIGN8, QUEUE_2026-07-18_CAMPAIGN11); FAR-502 has **no** queue row | As judged. Contained to `never` by default; the shared-buffer overwrite across passes and frustums is unchanged, and no per-frame indirect arena exists. |
| B6 | GPU sort reuses and may reallocate shared buffers before submission | CONFIRMED | `WebGPUGPUSortKeysDispatcher.ts:151` single `_resources` set per dispatcher, `:631-660` `prepareIndicesReadback` ring, `:662-700` `_pumpReadback`, `:828` one instance per context in a `WeakMap`; `WebGPUSceneRenderer.ts:1360` `_gpuSortConsumeMode="never"` | FAR-003 + FAR-105 landed; FAR-501 open, FAR-502 none | As judged. One context-wide resource set is still written per opaque frustum; safety rests entirely on the readback ring plus the `never` consumer default. |
| B7 | OIT uses an invalid depth/stencil attachment contract | CONFIRMED | `WebGPUOIT.ts:150-160` `depthReadOnly`/`stencilReadOnly` with no load/store ops (attachment defect fixed), `:163-167` "accumulation and revealage remain single-sampled … does not make the multisample path ordering-safe"; `WebGPUSceneRenderer.ts:952` `_webgpuOITEnabled=false`; `WebGPUSceneRendererTranslucentPass.ts:311` sorted-alpha fallback | FAR-404 has **no** queue row; the ratified default-off record is `DEFAULT_PARITY_MATRIX_2026-07-18.md:35` | As judged. Note the MSAA-by-OIT ordering hazard is stated in the source itself at `WebGPUOIT.ts:163-167`, a better anchor than the audit line the parity matrix cites. |
| B8 | WebGPU ViewportQuad is broken and cannot represent Cesium materials | CORRECTED | `Scene/ViewportQuad.js:10` WGSL import, `:144-169` `isWebGPU` branch building a colour-only `materialUniforms`, `:187` `this._overlayCommand.uniformMap = this._material._uniforms` overwrites it every update; `WebGPUViewportQuad.ts:308` synchronous `createRenderPipeline`, `:361` `Object.keys(uniformMap)` binding order, `:514` `_isViewportQuadCommand`, `:540-556` bind group rebuilt per execute. Only `packages/engine/Specs/Scene/ViewportQuadSpec.js` (WebGL) exists | **Correction: rows DO exist** — `DEFERRED_WORK.md:4868` `NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE` (the sanctioned-exception blocker) and `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:86` `Q-9` (the `ViewportQuad.js:187` inversion) | The defect text stands unchanged. Correct the auditor's "FAR-301 planned; no queue row": the Scene-side half is owned by `NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE` and `Q-9`. What is genuinely unowned is the `WebGPUViewportQuad.ts` interior — synchronous compile, key-order bindings, per-execute bind-group rebuild, the marker-only command shape, and the absent WebGPU spec. Scope the new row to that. |
| B11 | Native WebGPU features first allocate compatibility resources, then allocate native replacements | CONFIRMED | Buffer half closed: `Stubs/WebGLStubBuffer.ts:255-262` records store size only. Texture half live: `Scene/Material.js:475-507` retains `_imageSources[uniformId]` **and** constructs a `Texture`; `Stubs/WebGLStubTexture.ts:349` `device.createTexture`. Legacy shells: `Scene/PrimitiveGeometryHelpers.js:782-825` (`VertexArray.fromGeometry`) | FAR-200..210 (rows in CAMPAIGN8/9 queues for FAR-209/210); no ownership token landed | As judged. |
| H1 | The high-density path is not yet GPU-driven | CONFIRMED | Readback loops `WebGPUGPUCuller.ts:417-480`, `WebGPUGPUSortKeysDispatcher.ts:631-700`; all automatic consumers fail-closed (`WebGPUSceneRenderer.ts:1360`, `:1189`, `WebGPUSceneRenderer3DTilePasses.ts:109-113`); `WebGPUPerformanceManager.ts:245-247, 1470-1485` keep the 50,000 policy | FAR-502..505 — **none** has a queue row | As judged. |
| H2 | Visibility inputs and readbacks lack identity and bounds safety | CONFIRMED | `WebGPUSceneRenderer.ts:3896`, `:4085`, `:4419` all `bv.radius ?? bv.boundingSphere?.radius ?? 0`; count-only acceptance `:4175`; `occlude === false` preserved `:4185-4186`; culler cap `WebGPUGPUCuller.ts:124` `maxObjects ?? 65536` | FAR-500 | As judged. A third zero-radius site exists (`:4419`, the sort SOA) that the auditor did not list. |
| H3 | Main Scene submission is coherent, but feature paths fragment it | CONFIRMED | **53** (not 52) `queue.submit(` sites across `Renderer/` and `Scene/`, including `Scene/PickDepth.js:300`; `_resumeScenePass` at `WebGPUSceneRenderer.ts:2037-2075` reloads colour (`loadOp:"load"`), depth/stencil and the MRT slot-1 G-buffer | FAR-200 S1/S2/S3, FAR-402/405/406 (FAR-405 has rows; FAR-402/406 do not) | As judged, with the count updated to 53. |
| H4 | Per-command uploads leave substantial CPU and Dawn-wire overhead | CONFIRMED | 327 `queue.writeBuffer(` and 188 `createBindGroup(` sites engine-wide (matches the judgement exactly); `WebGPUPrimitiveCommands.ts` holds 24 `writeBuffer` sites including the per-command pick writes at `:4004` and `:4029`; 256-byte rounding at `WebGPUBuffer.ts:309-310` | FAR-303 (C9-12 BLOCKED) | As judged. |
| H5 | Pipeline cache identities are incomplete | CONFIRMED | Fold present: `WebGPURenderPipelineCache.ts:1063` `pl:`, `:1070` `pr:`, `:1079` `dz:`, `:1089` `mx:`, `:1100` `sh:`, canary `:355`/`:460`/`:1265` `wrongModuleHits`; `WebGPUComputePipelineCache.ts:413-417` `m:` fold. Residual live: `WebGPUOIT.ts:706` `cacheKey = ${shaderCode.length}_${fragEntry}_${label}` keys both its pipeline **and** its shader-module cache | Batch 825/828 + `Tools/visual-regression/pipeline-key-aliasing.spec.mjs`; FAR-304 for residuals (no queue row) | As judged. State the concrete failure mode: the OIT key is a shader-*length* key, so two different shaders of equal length sharing an entry point and label collide. |
| H6 | Synchronous first-use pipeline compilation is widespread | CORRECTED | **140** `.createRenderPipeline(` and **54** `.createComputePipeline(` call sites against **1** executable `createRenderPipelineAsync` (`WebGPURenderPipelineCache.ts:760`) and **2** executable `createComputePipelineAsync` (`AsyncResourceMonitor.ts:400`, `WebGPUComputePipelineCache.ts:360`); `WebGPUOceanRenderer.ts:638` and `WebGPUViewportQuad.ts:308` still synchronous | C11-180 (WebGL half only); FAR-600 has **no** queue row | Counts corrected from 135/55/1/3 to **140/54/1/2**. Everything else stands: ocean and ViewportQuad compile on the frame, and the WebGPU prewarm/readiness policy is unowned. |
| H7 | MRT/G-buffer cost is always paid even when deferred lighting is off | CONFIRMED | `WebGPUContext.ts:732` `forceSceneMRT = true` with the in-source comment "Demand-driven topology selection is not wired yet"; `WebGPUAttachmentDemandRegistry.ts:216-217` `gbufferDemanded = gbufferReadersDemand \|\| forceSceneMRT`; `WebGPUSceneFBTargetHelpers.ts:35` `_mrtMode = true` | C9-10 / FAR-403-C0 (rows in CAMPAIGN8/9/11 + DEFERRED_WORK) | As judged. |
| H8 | Device-loss and teardown generation handling is incomplete | CORRECTED | `_msaaDepthResolveTexture` created at `WebGPURenderTarget.ts:257`, views read from `:267`, and `destroyTextures()` at `:705-722` destroys colour, resolve and depth attachments **but never this texture** — it also leaks on every `resize()` (`:699` re-`createTextures`). **Refuted sub-claim:** the dispatcher `WeakMap`s no longer hand out dead-device instances — `WebGPUGPUSortKeysDispatcher.ts:828-850` and `WebGPUHiZOcclusionDispatcher.ts:1004-1020` both revalidate through `shouldRebuildForDevice(inst, context.device)` and destroy/rebuild on mismatch | FAR-208 active; FAR-601 has **no** queue row | Corrected: "the HiZ/sort dispatchers are context-keyed WeakMap singletons that can outlive a device generation" is **no longer true** — both are device-revalidated. The surviving concrete residual is the `_msaaDepthResolveTexture` leak (allocated `:257`, never destroyed `:705-722`, leaked again on resize), plus the absence of the FAR-601 teardown-to-baseline audit. |
| H9 | Shared frontend abstractions remain incomplete | CORRECTED | Exactly **one** Scene-to-`Renderer/WebGPU` import remains: `Scene/Model/MetadataWGSLPipelineStage.js:60` (the other three grep hits are prose in `Moon.js:1005`, `SolarDiscModel.js:22`, `StarField.js:30`). `Scene/OceanSurfacePrimitive.js:384, 502` still branch on `isWebGPU`; `Scene/Scene.js:3594, 3599, 3697, 6109` test `isWebGPUDrawCommand`; `Scene/ViewportQuad.js:10` imports WGSL; `Scene/View.js:172-174` setter is a bare pointer swap; `Scene.js:4720` `createView`. **`RenderCommand` still has zero importers** — the module is `Renderer/WebGPU/RenderCommand.js` and the only reference outside itself is the generated barrel `packages/engine/index.js:1025`; `ClassificationPrimitive.js:1300` and `GroundPrimitive.js:802` define a *local* function named `updateAndQueueRenderCommand` and import nothing | FAR-300/308 (no queue row); Principle 2 in CLAUDE.md; `DEFERRED_WORK.md:4867` records which residuals are intentional | Two corrections. (a) The import count is **1, not 4** — that half is nearly closed. (b) **Reversal:** the auditor's "Resolved sub-item: RenderCommand now has consumers (ClassificationPrimitive, GroundPrimitive)" is wrong; the abstraction is still unused in production code and the original sub-finding is live. `DEFERRED_WORK.md:4867` also records `ViewportQuad.js:144` and the `isWebGPUDrawCommand` checks as *intentional* residuals, which the new review should adopt rather than re-file. |
| H10 | Collection paths still contain O(N) rebuilds and lifecycle gaps | CONFIRMED | `WebGPUPolylineRenderer.js:509` `buildSegmentDataForGroup` called from `:1735`; `:663` `buildPickSegmentData` called from `:2105` — both allocate full segment arrays per build | FAR-307 / C9 item 62 (rows in CAMPAIGN8/9/11) | As judged. |
| H17 | Globe material data is repacked and uploaded per visible tile | CORRECTED — **worse than judged** | `WebGPUGlobeSurfaceRenderer.ts:522` `_getOrCreateMaterialPipeline` is called at `:1625`, inside `createTileCommands` (`:1099`), and `Scene/GlobeSurfaceTileProviderRendering.js:1356` calls `createTileCommands(tile, …)` **once per tile**. `:650-654` therefore runs `packMaterialUBO` plus `device.queue.writeBuffer` per visible tile per imagery pass; `WebGPUGlobeMaterial.ts:396-397` allocates a fresh `ArrayBuffer` and `Float32Array` on every call. Only the pipeline/shader-module cache entry (`:543-588`) is per `material.type` | FAR-303 | **Reversal of the auditor's mitigation claim.** The judgement says packing "now happens once per frame per material type rather than per tile"; it does not. The original finding's headline — thousands of redundant allocations and queue writes per frame on a material-enabled globe — is intact. Scope note the new review should carry: the path is inert unless `globe.material` is set (`tpMaterial.wgslShaderSource` non-empty), so the default globe pays nothing. |
| H18 | Material matrix packing does not implement WGSL host-shareable layout | CONFIRMED | `Scene/MaterialUniformBuffer.js:60-119` — the alignment table names only f32/vec2/vec3/vec4/mat4x4 and derives alignment from element count (`info.size >= 3` gives 16 bytes); `:183-186` classifies any `Array` as `{type:"matrix", size: value.length}` and packs it contiguously | FAR-302 — **no** queue row (confirmed absent from every queue, the FIX_QUEUE and DEFERRED_WORK) | Sharpened failure modes: a 4-element mat2 is over-aligned to 16 bytes where WGSL `mat2x2<f32>` requires align 8, so its offset and every following field diverge from the shader's; a 9-element mat3 is packed as 36 contiguous bytes where WGSL `mat3x3<f32>` is three 16-byte-strided columns (48 bytes). No typed schema generates the WGSL declaration and the CPU offsets together. |
| H19 | Previous-frame model motion vectors abandon the current RTE precision path | CONFIRMED | `Shaders/WebGPU/Model/ModelPBRComplete.wgsl:1021-1023` comment accepting "the residual metre of precision loss", `:1030-1031` `prevPositionMC = prevLinear3 * prevPositionMC + prevInst.translationHigh.xyz + prevInst.translationLow.xyz`, `:1040` `material.previousModelMatrix * vec4(prevPositionMC, 1.0)` | C11-53 / FAR-306 (`QUEUE_2026-07-18_CAMPAIGN11.md`) | As judged. |
| H20 | RTE helpers are duplicated and the current assertion cannot validate packing | CORRECTED | **83** `.wgsl` sources define `fn translateRelativeToEye`, plus 83 generated `.js` twins — the judgement's "166" double-counts the build products. The ineffective branch survives in exactly one source: `Shaders/WebGPU/chunks/functions/csm_translateRelativeToEye.wgsl:38` (and its `.js`). `WebGPURTEAssertions.ts:39` `RTE_ROUND_TRIP_TOLERANCE_METERS = 6.0`; `:70-85` documents that it validates the *source* values you are about to pack, not the packed offsets | C11-52 / FAR-305 | Corrected count: **83 distinct WGSL sources**, not 166. Treat that as non-comparable with the audit's original "~85 files" figure and re-baseline rather than claiming growth. Everything else stands: no shared reviewed helper, a 6 m tolerance, and an assertion that cannot see a swapped destination offset. |
| H21 | Shader preprocessing and module cache identity are fragmented | CORRECTED | Module cache fixed: `WebGPUShaderModuleCache.ts:20-49` keeps the full 32-bit lo mask plus a `definesHi` tier. Residuals: `WebGPUShaderCache.ts:414-418` `_getCacheKey` returns `descriptor.name` **verbatim** ("For now, use the name as the cache key … Could hash the code") — the code is not part of the key at all; bare-`#import` regex at `WebGPUBufferPrimitiveRenderer.ts:552-570`; **10** of 324 `.wgsl` files mention lockstep | FAR-305 | Corrected: the second cache does not merely *include* `descriptor.name` — the name **is** the whole key, so two different WGSL bodies registered under one name serve the wrong module. Lockstep-marker coverage is 10/324, not 20/324. |
| H23 | Model geometry normalization allocates before the cache lookup | CONFIRMED | `Scene/Model/ModelPrimitiveGeometry.js:41-64` loader-owned revision tokens (`getGeometryRevision`, `revisionHitCount`/`walkHitCount`); `WebGPUModelRenderer.ts:6397` `const primKey = ${nodeIdx}_${primIdx}` | C9-17 / FAR-204 (`QUEUE_2026-07-15_CAMPAIGN9.md:148`) | As judged. |
| H24 | Device pooling does not yet imply safe resource sharing | CORRECTED | Terrain key `${tile.level}_${tile.x}_${tile.y}` at `WebGPUGlobeSurfaceTileBuffers.ts:174`, with **an added `cached.sourceVertices === mesh.vertices` identity check** (`:198-200`) the judgement omits. `evictStaleResources` (`WebGPUGlobeSurfaceRenderer.ts:2813`) has exactly one caller besides its own export: `:2971` with `new Set()` at teardown. **Refuted sub-claim:** `model._webgpuCache` can no longer serve the wrong device — `WebGPUModelRenderer.ts:5287-5300` `disposeStaleWebGPUModelCache` drains any cache whose `device` or `resourceGeneration` differs, and `:5302-5325` installs an `onDeviceInvalidated` subscription | C9-15 / FAR-203/207/208 | Corrected: drop "model `_webgpuCache` is a single unkeyed slot that can serve the wrong device" — it is a single slot but device- and generation-guarded. What remains true: terrain residency carries no provider or content-hash identity (only tile coordinates plus a source-array reference check), and terrain is never retired below the device lifetime because eviction runs only at teardown. |
| M1 | WebGPU-only feature flags conflate requested and active state | CONFIRMED | `WebGPUSceneRenderer.ts:890-899` `UnsafePathStatus {requested, capable, active, fallbackReason}` for the dense family; `Scene/Scene.js:2249-2254` `getContainmentStats`. The other flags remain plain mirrors with doc-only semantics: `Scene.js:3310`, `:3333`, `:3362`, `:3413` "Has no effect on the WebGL path"; `:3402-3408` `clusteredLightingEnabled` get/set of `_clusteredLightingEnabled` (initialized `:1228`) | FAR-003 + `DEFAULT_PARITY_MATRIX_2026-07-18.md` + C11 ratified decisions | As judged. |
| HP-01 | Telemetry integrity and full-frame coverage are incomplete | CONFIRMED (structural half) | `WebGPUContext.ts:7020`, `:7040-7044` gate on `hasFeature("timestamp-query")`; `WebGPUTimestampProfiler.ts:92` `unprofiledMs` is a reported field, `:697` feeds a rolling window | Q-143 / DM-09; measurement authority is the DEBUGGING_GUIDE moving-altitude campaign | As judged. **Honesty note:** the "dozens of unlabelled pass-creation sites" figure is a runtime-coverage measurement I did not re-derive read-only; only the repair-and-gating half is verified here. |
| HP-03 | Depth packing repeats full-screen work and object creation per frustum | CONFIRMED | `WebGPUGlobeDepth.ts:181` `_depthCopyBindings: WeakMap<GPUTexture, DepthCopyBinding>`, `:621` lookup, `:648` and `:670` inserts, `:689` reset — caching by texture identity only, with no declared depth versions or demand gating | FAR-408 / FAR-405 (rows in CAMPAIGN8/9/11) | As judged. |
| HP-04 | Settled frontend allocation and closure churn remain unmeasured | CORRECTED | `QUEUE_2026-07-15_CAMPAIGN9.md:148` records C9-17 slices A–C landed with **`settledAllBindGroupCreates` 560 to 240 over 40 frames = 6/frame**, all from non-model owners (`updateWebGPUSun` 2, `TAA_BG` 2, `PostProcess-IdentityBlit` 2); slice D is STOP-gated to C10 intake. `:157` records C9-11 PARTIAL/PAUSED (the execute-closure hoist landed; descriptor/wrapper/array retention deferred, and the row itself corrects an earlier ~1000x overstated per-frame figure). Polyline repacks per H10 | C9-17 slice D + C9-11 / FAR-309 | Corrected: the settled-frame residue is **6 bind-group creations per frame**, not 14 — 14 was the pre-slice-A figure. The finding stands (no settled heap or object-plateau gate exists) but the number the new review carries must be 6/frame from Sun/TAA/canvas-blit owners. |
| HP-05 | Persistent resource realization is reachable during draw-command emission | CONFIRMED | `WebGPUGlobeSurfaceRenderer.ts:1317`, `:2530`, `:2716` call `getOrCreateTileBuffersHelper` from command construction; `WebGPUGlobeSurfaceTileBuffers.ts:177-235` destroys the stale entry and creates/uploads new vertex and index buffers on a miss, up-converting Uint8 indices | FAR-209 / FAR-210 (rows in CAMPAIGN8/9) | As judged. |
| HP-06 | Main-thread preparation budgets can consume most of a frame | CONFIRMED | `Scene/JobScheduler.js:59-60` "Guaranteed progress on all job types each frame, even if it means exceeding the total budget"; `:78-90` defaults TEXTURE 10 ms + PROGRAM 10 ms + BUFFER 30 ms = 50 ms | FAR-210 (rows in CAMPAIGN8/9) | As judged, with a scope note the auditor omits: this is **inherited upstream Cesium behaviour**, unmodified by the fork. The new review should file it as an upstream-divergence proposal (stage deadlines, backpressure), not as a fork defect. |
| HP-07 | Imagery reprojection and mip generation fragment submissions | CONFIRMED | `WebGPUImageryReprojection.ts:245` and `WebGPUMipmapGenerator.ts:439` both `device.queue.submit([encoder.finish()])` | FAR-200 S2 (C9 item 45) + FAR-402 (no row) | As judged. |
| HP-08 | Ordinary frames may upload pick state and repeat redundant GPU state calls | CONFIRMED | `WebGPUPrimitiveCommands.ts:3018` `hasPickIds`, `:3989-4040` allocates a `Float32Array`, writes the pick camera UB, creates a pick-camera bind group, writes the pick material UB and creates a pick-material bind group per pickable command per update — with no `frameState.passes.pick` gate on that block. The model path is gated: `WebGPUModelRenderer.ts:6809-6810` `pickDemand = passes?.pick === true && …` | Q-141 (`FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md`) + FAR-303/409 | As judged. Worth adding: the cost is not only two `writeBuffer`s but two `createBindGroup`s per pickable command per frame. |
| ADOPT-MATRIX | 2026 WebGPU adoption matrix | CONFIRMED | `WebGPUFramebufferManager.ts:54-69` still `Reflect.get(GPUTextureUsage,"TRANSIENT_ATTACHMENT") ?? 0x10` and probes both `"transient-attachments"` and `"transient-attachment"`; `Renderer/ContextFactory.ts:153` `featureLevel: "compatibility"`; video moved (`WebGPUVideoTextureManager.ts` uses `importExternalTexture`); no occlusion-query use anywhere in `Renderer/WebGPU` | FAR-700..707 (experimental) + `RENDERER_LANDSCAPE_AUDIT_2026-09-02.md` | As judged. |
| GUARDS | Validation results — failed or stale guards | **REFUTED** | `Tools/upstream-regression-check.mjs:347-378` guard 7 is indeed a source-text regex pair. **But the behavioural test the audit asked for exists**: `packages/engine/Specs/Scene/Model/EdgeVisibilityPipelineStageDecodingSpec.js:662` `it("does not throw for degenerate (zero-area) triangles", …)` builds a primitive with two coincident vertices, and `:737-742` documents it as the upstream #13421 repro. `DEFERRED_WORK.md:4933` records both artefacts as shipped at Batch 299 | `NEW-UPSTREAM-EDGE-DEGENERATE-13421` (`DEFERRED_WORK.md:4933`) — closed | Refuted: the regex guard is now redundant defence-in-depth beside a real degenerate-input spec, not the sole certification. The judgement's "one surviving tools item with no row" does not survive. The `git diff --check` and material-probe halves are already closed. No new row is needed. |
| LEDGER-IDS | Finding-ID namespace | CONFIRMED | `FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:1175` "The audit intentionally has no H15 identifier. Preserve that numbering; do not reuse H15 for new work." The ledger `findings{}` map holds 47 keys including **B13** and **B14**, both `{packages:["FAR-102","FAR-103"], state:"active", gate:"correctness"}` with no audit definition; the ledger `audit` field names this file. States are stale (`H5:"planned"` though the fold landed at Batch 825/828; `B9` and `H11` `"active"` though closed) | FAR plan §10 + ledger `findings` | As judged. The ledger's `audit` field and the plan's `:9` link both break on removal and must be repointed in the same change. |

## 2. Spot-checks of items the prior auditor closed

Chosen for blast radius: renderer-default selection, RTE/clip-space and process-global state, a
compute-in-render-pass correctness claim, a public-API-on-both-backends claim, a WebGL/WebGPU
parity claim, a shader correctness claim, and a "the default is now off" claim.

| ID | Auditor's status | My verdict | Evidence read today |
| --- | --- | --- | --- |
| B1 | RESOLVED | **Upheld** | `Scene/Scene.js:281-286` — the synchronous constructor calls `getSynchronousRendererType(options.contextOptions)` unless a pre-initialized context is supplied, with the in-source contract "Synchronous construction is deliberately WebGL-only"; `Scene.js:1652` and `:1716-1737` route `createAsync` through `ContextFactory.createContextWithDiagnostics` (`Renderer/ContextFactory.ts:216-220`); `CesiumWidget.js:784, 820` and `widgets/Viewer.js:2302` share `CesiumWidget._createAsyncContext`. No exact-string `=== "webgpu"` gate remains on the creation path. |
| B2 | RESOLVED | **Upheld (citation corrected)** | `Core/ClipSpaceConvention.js` exists; `GraphicsCapabilities.js` lives at **`Renderer/`**, not `Core/` (`Renderer/GraphicsCapabilities.js` plus `.d.ts`, consumed at `Renderer/GraphicsContext.ts:734, 1183-1189`). `Core/Matrix4.js:812-989` — all three projection builders take an explicit `clipSpaceConvention` normalized to `ClipSpaceConvention.WEBGL`; `Core/PerspectiveOffCenterFrustum.js:447-450, 471-472` cache per `clipSpaceConvention.id`; `Core/loadKTX2.js:56-71` throws in release builds without an explicit `supportedTargetFormats` ("no correct process-global fallback when WebGL and WebGPU contexts coexist"). A grep for `_depthRangeType` over all of `packages/engine/Source` returns **nothing**. No `ContextLimits._` mutation. |
| B3 | RESOLVED | **Upheld — and I actively tried to break it** | Bracketing is real: `WebGPUSceneRenderer.ts:4024-4041` ends the pass, dispatches, then calls `_resumeScenePass`; the opaque siblings do the same at `:2542-2570`. I then chased the residual map-before-submit hazard: `WebGPUGPUCuller._pumpReadback` (`:452-470`) maps the slot written by the *prior* `prepareReadback`, which would be illegal if two frustums shared one culler inside one encoder. It cannot happen — `WebGPUContext.ts:6385-6392` allocates **per-frustum** culler instances (frustum 0 reuses `_gpuCuller`; 1..N get their own), plus separate translucent and per-cascade instances (`:7273-7300`), each with its own staging buffers. The ring invariant holds. No reversal. |
| B10 | RESOLVED | **Upheld** | `Scene/MaterialHelpers.js:987-989` — "Keep Cesium's public mutable values authoritative" — constructs `new MaterialUniformBuffer(result.uniforms)` and does **not** reassign `result.uniforms`; no `defineProperty(…, "uniforms")` exists in either file. `Scene/MaterialUniformBuffer.js:418-425` `_readValue` returns `this._uniforms[name]`, the stable public object, and `:408` writes through it. In-place `material.uniforms.color.alpha = 0` persists on both backends. |
| H11 | RESOLVED | **Upheld** | `WebGPUDrawCommand.ts:184-188` (optional inputs), `:332-336` (typed fields), `:512-522` (normalized in the constructor). Ordering is assigned in shared Scene code *before* the backend branch: `Scene/BillboardCollection.js:232, 1226`, `PointPrimitiveCollection.js:164, 845`, `PolylineCollection.js:150`, `LabelCollection.js:696` via `createCommandOrdering`/`applyCommandOrdering`. All three WebGPU renderers forward it (`WebGPUBillboardRenderer.js:1358-1360, 1410-1412, 1590-1591`; `WebGPUPolylineRenderer.js:1870-1872, 1950-1952`). The serializer reads `cmd.sortLayer` and `cmd.materialSortId` (`WebGPUSceneRenderer.ts:4634, 4641`), not `renderLayer`/`materialId`. |
| H12 | RESOLVED | **Upheld** | `Scene/RenderScheduler.js:15-17` documents `enabled=false` and `:31` sets `this.enabled = options.enabled ?? false`; `Scene/ViewportExecutor.js:756-760` states the default render path does not read `materialSortId` and that the linear service runs only for the pick-pass tiebreak, the WebGPU sort-keys path, or a registered consumer. |
| H16 | RESOLVED | **Upheld** | `Shaders/WebGPU/Globe/GlobeTerrain.wgsl:5426` reconstructs `cameraWC` from the RTE pair, `:5427-5429` carries the regression comment ("Adding cameraWC a second time displaces LUT sampling…"), and `:5430-5431` passes the absolute `input.v_positionMC` plus `cameraWC` separately into `sampleAtmosphereFogLut` (`:3182-3199`), which forms `viewVec = worldPos - cameraWC`. The double-add is gone. |

## 3. Reversals

**No spot-check reversal.** All seven items the prior auditor closed are genuinely closed; B3 in
particular survived a deliberate attempt to reconstruct its hazard.

**Two sub-item reversals inside items the auditor kept open**, both in the direction of the auditor
having credited a fix that did not land. Each makes the surviving finding larger than the
judgement's `migrate_text` says, so each must be corrected before that text is migrated:

1. **H17 — "packing now happens once per frame per material type rather than per tile" is false.**
   `_getOrCreateMaterialPipeline` (`WebGPUGlobeSurfaceRenderer.ts:522`) is invoked at `:1625`
   inside `createTileCommands` (`:1099`), which `GlobeSurfaceTileProviderRendering.js:1356` calls
   once per tile; `:650-654` runs `packMaterialUBO` (a fresh `ArrayBuffer` and `Float32Array`,
   `WebGPUGlobeMaterial.ts:396-397`) and `queue.writeBuffer` on **every** invocation. Only the
   pipeline and shader-module cache entry is per `material.type`. The audit's original headline —
   thousands of redundant allocations and queue writes per frame on a material-enabled globe — is
   unmitigated. It is inert while `globe.material` is unset, which is the default.
2. **H9 — "RenderCommand now has production consumers" is false.**
   `Renderer/WebGPU/RenderCommand.js` has no importer anywhere in `packages/engine/Source`; the
   only reference is the generated barrel `packages/engine/index.js:1025`. The cited "consumers"
   are local functions coincidentally named `updateAndQueueRenderCommand`
   (`Scene/ClassificationPrimitive.js:1300`, `Scene/GroundPrimitive.js:802`). The backend-agnostic
   command abstraction that CLAUDE.md tells new scene features to prefer is still dead on arrival.

One correction runs the other way and is recorded here for symmetry: **GUARDS is refuted** — the
behavioural degenerate-triangle spec the audit demanded exists
(`EdgeVisibilityPipelineStageDecodingSpec.js:662`), so the judgement's only "needs a new tools row"
item does not need one.

## 4. Inbound references

All six inbound references in the judgement were re-derived by grep over `**/*.{md,json,mjs,js,ts}`
excluding `Build/` and `node_modules/`. The set is exactly right; one line number has drifted.

| Reference | Verified location | Repointable? |
| --- | --- | --- |
| `migration_doc/README.md` index row | **`:355`** (the judgement said `:240`) | Yes — repoint to the new review |
| `migration_doc/ARCHITECTURE_REVIEW_PLAN_2026-09-02.md:4` | confirmed, markdown link in "Why now" | Yes — historical citation by name |
| `migration_doc/FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:9` "Source audit" link | confirmed; the §10 ID table at `:1175` carries the H15 rule | Yes — repoint `:9`, keep §10 as the ID source |
| `migration_doc/FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json:9` `"audit"` field | confirmed; `findings{}` has 47 keys including undefined `B13`/`B14` | Yes — repoint at the Phase-4 ledger refresh |
| `migration_doc/FORK_PERFORMANCE_WEEKLY_CHANGE_DEFENSE_2026-07-15.md:11` | confirmed, frozen checkpoint | Yes — dead-link note |
| `migration_doc/DEFAULT_PARITY_MATRIX_2026-07-18.md:35` B7 citation at `…AUDIT_2026-07-13.md:167` | confirmed; the row paraphrases all four B7 sub-defects inline | Yes — the anchor is lost but no content is |

No reference exists under `Tools/`, `CLAUDE.md`, `.claude/`, `Specs/`, `packages/` source or
`scripts/`.

## 5. Document verdict

**KEEP-PART.**

Every one of the 51 items is now adjudicated (24 CONFIRMED, 9 CORRECTED, 1 REFUTED and 0
UNADJUDICATED among the 34 open items; 7 of the 17 closed items spot-checked and all upheld), and
every inbound reference is repointable. On that arithmetic the judgement's REMOVE-AFTER-MIGRATION
would stand — except that the judgement's 51 items do not cover the whole document.

**The section to keep is `## Allocation ownership and dual-renderer investigation` → `### Maximum
safe sharing boundary` (audit lines 565-588).** No judgement item carries it: `ALLOC-PROBE` covers
only the probe numbers above it (`:553-563`), `H24` covers the concrete cache defects, and
`GATES-PHASES` covers the separate "Recommended target architecture" section at `:644`. What lives
only here is:

- the scope-by-scope sharing table — what two WebGPU canvases on one pooled device may share, what
  two device generations may not, what two views must keep local — which is the operative rule the
  FAR plan's NATIVE_CONTEXT-to-NATIVE_DEVICE promotion (ADR-1, FAR-206, FAR-207) is measured
  against; and
- two claims I re-verified as still true today:
  - `SharedResourcePool.ts` has no importer; the only references are `@see` doc comments
    (`Renderer/GraphicsContext.ts:45`, `Renderer/OffscreenContextSupport.ts:47`,
    `Renderer/WebGPU/WebGPUDevicePool.ts:80`). The plan agrees it must not become the primary cache
    (`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:193`) but records no disposition for the
    orphaned module itself.
  - `Scene/ResourceCacheKey.js:387`, `:479` and `:613` still append
    `-context-${frameState.context.id}` to buffer and texture cache keys, so even the retained CPU
    product is context-scoped — the exact condition FAR-206 exists to remove.

This is a small migration: one table and two paragraphs into the new architecture review. Once that
block is carried across — along with the nine corrected migrate texts and the two reversals in §3 —
the document becomes REMOVE-AFTER-MIGRATION with no residue.

Three further migration obligations, none of which alone would keep the file:

1. `FAR-302` (material matrix layout), `FAR-404` (MSAA-by-OIT topology), `FAR-502`, `FAR-600` and
   `FAR-601` have **no row in any queue, in `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md`, or in
   `DEFERRED_WORK.md`**. `FAR-301`'s Scene half is owned
   (`NEW-MATERIAL-PER-BACKEND-SHADER-SOURCE`, `Q-9`) but its `WebGPUViewportQuad.ts` interior is
   not. The new review must open rows for these or they are lost with the file.
2. Several owning rows live in `QUEUE_2026-07-15_CAMPAIGN8.md` and `CAMPAIGN9.md`, and Campaign 9
   closed green at C9-30. A row in a closed queue is a record, not an owner; re-home FAR-500/501,
   FAR-209/210 and FAR-403/405/408 before quoting them as live ownership.
3. `DEFERRED_WORK.md:5338` (FORK-41, "command-drop now DEFAULT ON, verified") contradicts
   `WebGPUSceneRenderer.ts:1189`. Reconcile it before B4's owning record is cited anywhere.
