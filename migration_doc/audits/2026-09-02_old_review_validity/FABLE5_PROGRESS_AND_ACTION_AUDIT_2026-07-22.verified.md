# Verification of the FABLE5 audit judgement

- **Document under review:** `migration_doc/FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.md`
- **Judgement verified:** `migration_doc/audits/2026-09-02_old_review_validity/FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.judgement.json` (64 items)
- **Verifier:** Belecthor
- **Date:** 2026-09-03 (tree at Batch 1378 + uncommitted lane work)

## Counts

| Bucket                                         | Count |
| ---------------------------------------------- | ----: |
| Still-valid / partial items verified            |    38 |
| CONFIRMED                                       |    31 |
| CORRECTED                                       |     7 |
| REFUTED                                         |     0 |
| UNADJUDICATED (whole items)                     |     0 |
| Spot-checks of RESOLVED / SUPERSEDED / STALE    |     5 |
| Reversals found                                 |     1 (partial — a rule, not code) |

One *sub-claim* inside `F5-09` is left explicitly unadjudicated (see the row). No whole item was
guessed at.

## Method

Every code citation below was read at today's lines this session; the judgement's line numbers are
from an older tree and several had moved. Queue ownership was re-derived from
`QUEUE_2026-07-18_CAMPAIGN11.md`, `QUEUE_2026-08-29_RESEARCH_DISPATCH.md`,
`FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md`, `DEFERRED_WORK.md` and
`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md` / `…_LEDGER_2026-07-13.json`. No engine or tool
file was edited; this bank file is the only write.

## 1. Still-valid and partial items

`file:line` is what I read today. "Owner" is a queue/ledger row that actually exists and covers the
item; "none" means nothing in the live queues resolves it.

| id                | title (short)                                        | verdict   | current file:line                                                                                                            | owner                                                        | migrate text (corrected where marked)                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------- | ---------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `F5-PRINCIPLES`   | eleven method principles                              | CORRECTED | `CODEX_DISAGREEMENTS_WITH_FABLE_REVIEW_2026-08-16.md:60-71` (all eleven, one line each, anchored by line links into the file) | CLAUDE.md:20 + SR-1/SR-6/SR-8; CODEX §2 table                 | The judgement's migrate text reproduces six of the eleven. **Add the two that no successor states:** (a) *bounded visual evidence* — a route's cross-backend diff validates the subject it rendered (offline ellipsoid + NaturalEarthII), never real terrain, water, dense 3D Tiles, OIT or every post-process consumer; (b) *honest incomplete test state* — a stopped suite is neither a complete green nor a complete red, and unrelated lint debt stays separately attributed. |
| `F5-02`           | terrain-buffer lifetime vs quadtree LRU               | CONFIRMED | `WebGPUGlobeSurfaceTileBuffers.ts:168-176` (`` `${level}_${x}_${y}` ``), `:577-590` (inline `vertexBuffer.destroy()` / `indexBuffer.destroy()` at `:585-586`), host interface `:61` (only `scheduleTextureDestroy`); `GlobeSurfaceTile.js:158-190` frees water-mask/vector/imagery only; `WebGPUContext.ts:553-555`, `:2932`, `:3379` | `C11-34` (NOT STARTED, C11 queue `:1669`, `:2456`); FAR-203 planned (`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:530-543`) | As judged. Key omits payload revision, context and device generation; nothing on the quadtree free path releases a realization; the only reclaim destroys inline before submit. Fix shape: lease release from `freeResources` / mesh replacement / provider swap / device loss, batched post-submit retirement, then a byte-budgeted zero-lease grace LRU — never a per-frame `evictStaleResources(visibleTileKeys)`.                                              |
| `F5-03`           | one mutable globe renderer per `GPUDevice`            | CONFIRMED | `GlobeSurfaceTileProviderRendering.js:1110` (`new WeakMap()`), `:1312-1318` (one renderer per device); `WebGPUGlobeSurfaceRenderer.ts:220` `_pipelineCache`, `:461` `_webgpuContext`, `:306` `_oceanNormalMapSource` | none for the fix; `Q-86` (`FIX_QUEUE…:1002`) covers the missing test only; FAR-108 did effects only | As judged. Split device-immutable resources (refcounted per device), context/view state (formats, readiness, frame numbers, pick) and globe/provider residency (terrain, wireframe, imagery, water/ocean) before wiring terrain release. Do not clone the renderer per context.                                                                                                                                                                                  |
| `F5-05`           | SceneOctree plane-mask vs `Intersect` enum            | CORRECTED | `OctreeNode.js:295-300` and `:321-325`; `CullingVolume.js:196` `MASK_OUTSIDE = 0xffffffff`, `:205` `MASK_INSIDE = 0`; `Intersect.js:18` `OUTSIDE = -1`, `:33` `INSIDE = 1`                                        | none (adjacent: `DEFERRED_WORK.md:10183`)                     | **Corrected mechanism.** `visibility === Intersect.OUTSIDE` can never match `MASK_OUTSIDE`, so **no subtree is ever rejected** — that half stands. But "the mask short-circuit never fires" is wrong: `MASK_INSIDE` is `0` and the `else` arm passes `childMask = visibility`, so a fully-inside node still hands children a zero mask and `computeVisibilityWithPlaneMask` returns early (`CullingVolume.js:80-84`). An *outside* node propagates `0xffffffff`, which also short-circuits the plane tests — while every command in every descendant is still collected. So the cost is the full traversal plus emission, not the plane arithmetic; the repair is still the whole opt-in semantics (eligibility, root bounds, translucent 3D Tiles, ordering, off-camera shadow casters), never the one-line enum fix. |
| `F5-06`           | enabled RenderScheduler bins/sorts unread layers      | CONFIRMED | `ViewportExecutor.js:777-797` (comment "The layer buckets are not consumed by either renderer" at `:777-779`; `binCommand` `:791`, `sortAllLayers` `:797`); `RenderScheduler.js:31` (`enabled ?? false`)          | FAR-504 "retire/adapt the legacy RenderScheduler" (planned, plan `:845`) | As judged. Either layer enablement/order/clear-depth actually drives both renderers' command order, or the scheduler retires under FAR-504. An enabled scheduler that is inert is an advertised feature that does nothing.                                                                                                                                                                                                                                       |
| `F5-08`           | device-loss walker misses cache families              | CONFIRMED | `WebGPUContext.ts:1369` (`_registerResourceCaches`), `:7566`, `:7657` (`_clearAllCaches`); `WebGPULabelRenderer.js:819-843` + `:1080` (`sdfBindGroup`) and `WebGPUPolylineRenderer.js:1573-1598` + `:475`, `:1685`, `:1727`, `:1933`, `:2094` — device-created bind groups/buffers in per-collection caches with **no** bus subscription and **no** device-generation field (grep across both files returns nothing) | `Q-86` for the multi-context test; no row for the owner protocol | As judged. The model-feature-pick teardown half is closed. Remaining: an owner protocol for every per-object `_webgpuCache` family on device loss (labels, polylines, and the rest), plus the two-context shared-device recovery test `Q-86` asks for.                                                                                                                                                                                                             |
| `F5-09`           | unbounded `reuploadWatch`; ocean source lifecycle     | CONFIRMED | `WebGPUGlobeSurfaceTextures.ts:788-791` (module-global `Map`), `:808-823` (entries reset, never deleted — no `delete`/`clear` for it anywhere in `Source`); ocean identity memo `WebGPUGlobeSurfaceRenderer.ts:2356-2367` | `DEFERRED_WORK.md:4628` (residual) + `:4630` sentinel-to-gate row; FAR-205 for leased ocean | As judged. **Sub-claim left UNADJUDICATED:** the "one bound function per visible model per frame" allocation claim — I did not measure it and no source read settles it; carry it forward as unverified rather than as a finding.                                                                                                                                                                                                                                 |
| `F5-10`           | OIT composite bind group + shader-length pipeline key | CONFIRMED | `WebGPUOIT.ts:259` (unconditional `_updateCompositeBindGroup()`), `:350-370` (fresh `createBindGroup` each call), `:706` (`` `${shaderCode.length}_${fragEntry}_${config.label ?? ""}` ``)                        | none (FAR-003 containment; `OIT_DEFAULT_FLIP_EVIDENCE_2026-07-18.md`) | As judged. Rebuild the composite bind group only when its views change; key derived pipelines by module identity (the `WeakMap`-backed `webgpuObjectIdentity` the central caches use) plus target format, sample count and depth state; then re-run the intersecting-translucency, resize, MSAA, format and device-loss gates. Fix before OIT leaves containment.                                                                                                  |
| `F5-11`           | `ResourceOwnership` scaffold has no consumer          | CONFIRMED | `packages/engine/Source/Renderer/ResourceOwnership/` (8 files); the string `ResourceOwnership` appears in `packages/**` only inside that directory (3 self-references) — no importer; `DecodedPayload.ts:20-26` `copySourceBytes` and `:45-47` `copyBytes()` slice | FAR-200 "shadow infrastructure only … 52 direct `queue.submit` sites remain" (plan `:72`, `:84`); `C11-75..78` NOT STARTED | As judged. Adopt only through one concrete, measured, zero-copy consumer — the terrain lease of `F5-02` is the natural first — after a zero-copy payload contract exists. Never integrate wholesale.                                                                                                                                                                                                                                                              |
| `F5-12`           | missing integration tests                             | CORRECTED | `WebGPUGlobeSurfaceTileBuffersSpec.js:298-421` **does** cover `evictStaleResources` as a unit against a fake host (22 `it` cases in the file); nothing covers the quadtree free path or two contexts on one device (`Specs/Renderer/WebGPU/` has no multi-context spec) | `Q-86`; DX-12 spec homes                                       | **Corrected scope.** The hole is not "no eviction test" — it is (a) no test that `GlobeSurfaceTile.freeResources` releases a GPU realization or that a freed tile's buffers survive until submit, and (b) no two-context / shared-device isolation-and-recovery test. Any future terrain-lifetime or ownership change lands with the failing lifecycle test first.                                                                                                    |
| `F5-15`           | `counterbalancedPairs` mislabelled                    | CORRECTED | `performance-workloads.json:16-17` (`repetitions: 6`, `counterbalancedPairs: 6`); `performance-workloads.spec.mjs:148`, `:2897` (`>= 6`); `run-performance-campaign.mjs:5496`, `:5696` (one pair per schedule entry), `:5753-5791`; `lib/performance-campaign-utils.mjs:112-123` | none (adjacent: `DEFERRED_WORK.md:4654` PERF-MEASUREMENT-VALIDITY-RISKS) | **The number is not wrong.** `buildCounterbalancedSchedule` emits one entry per repetition, each entry runs *both* renderers, and `pairs` is built one-per-entry — so six repetitions really do yield six comparison pairs (three per order). What survives: the manifest field is a static declaration that nothing reconciles against `repetitions`, and an **odd** repetition count ≥ 7 still certifies (`certificationRun = schedule.length >= 6`, five valid pairs, ≥ 2 per order) with unbalanced orders (4/3). Fix: recompute or rename the field from `repetitions`, publish the resolved schedule in the artifact, and reject odd two-renderer counts in certification mode. Tools-only, S. |
| `F5-18`           | unconditional MSAA depth resolve                      | CONFIRMED | `WebGPUSceneRendererPostFrustumChain.ts:94-105` (dispatched whenever the method and encoder exist — no demand check); `WebGPURenderTarget.ts:256-274` (allocates the `r16float` target whenever `wantSampleable && isMSAA`), `:288-302` (resolve runs whenever the views exist); `WebGPUSceneFramebuffer.ts:328` `depthSamplable: true` unconditional | `DEFERRED_WORK.md:10481` S4-4 note under C10-03; `DM-12` calls it adjacent | As judged. Inventory every reader first (TAA, motion blur, aerial perspective, god rays, DoF/AO, SSR/NPR/contact shadows, clouds/fog/ground fog, picking, unknown), let unknowns force the resolve, and assert demand separately from resolve opens (default MSAA4 with no reader = zero opens; each real reader = exactly one; TAA at MSAA1 = demand without a resolve pass).                                                                                    |
| `F5-19`           | eager pass reopening; globe-depth packing             | CONFIRMED | Counters exist: `WebGPUContext.ts:739-760` (`slot1AttachmentOpens` / `slot1ResolveOpens` / `sceneColorResolveOpens`). No `passOwner` and no `globeDepthPack` symbol exists anywhere under `Source/Renderer/WebGPU` (grep: no files) | `DEFERRED_WORK.md:10481-10482` S4-5 / FAR-405-706 notes under C10-03 | As judged. There is still no pass-owner/reason counter, no lazy "open at first work" scene pass and no gate on a post-globe pack whose frustum emitted zero globe commands. Reduce boundaries only after naming each removed owner/reason and proving identical clear/load/store order — per-frustum depth/stencil clears are load-bearing.                                                                                                                        |
| `ACTION-1`        | harden the baseline (Gate 0)                          | CONFIRMED | `run-performance-campaign.mjs` contains **zero** occurrences of `maxThroughput`; the bundle is pinned to `Build/CesiumUnminified` at `:5355`, `:5362`, `:5567`, `:5572` (and `:1890`, `:1917` in-page)              | `C11-173` done; `Q-143` / `DM-01` / `DM-04`; PERF-MEASUREMENT-VALIDITY-RISKS | As judged. Three Gate-0 items remain unowned: a max-throughput lane whose vsync state is verified rather than assumed, a release/minified lane, and a fully local terrain + water-mask fixture. `F5-15`'s manifest residue rides along.                                                                                                                                                                                                                            |
| `ACTION-2`        | attribute both bottlenecks                            | CONFIRMED | C11 queue `:2003` — `C11-168` **"PARTIAL / VALID CAUSAL DEFICIT, ROOT CAUSE OPEN"**, 9.2025 ms vs 4.65 ms CPU-p95; `:2122` `C11-180` PARTIAL, landed Batch 773; `FIX_QUEUE…:164-166` Q-143/Q-141 measurement record | `C11-168`, `C11-169`, `C11-180`; `Q-143` / `DM-09`             | As judged. The WebGL half is proven and reduced to four stalls; the WebGPU steady-state floor has a ledger and a certified deficit but no named owner. The dense-scene starvation lane (`Q-143`) is a separate defect and must not be read as the moving-globe floor.                                                                                                                                                                                             |
| `ACTION-3`        | generic post-submit buffer retirement                 | CONFIRMED | `WebGPUContext.ts:553-555`, `:2932`, `:3379`, `:5421` — textures only. No `scheduleBufferDestroy` and no `_pendingBufferDestroys` exists anywhere in `packages/engine/Source`                                       | none (prerequisite of `C11-34` / FAR-203)                      | As judged. Generalise the batch into a resource-retirement queue for buffers **and** textures keyed by the submit that last used them — one batch per frame, never one `onSubmittedWorkDone` promise per tile — behind a failing lifecycle test that destroys a resource used by an already-recorded command.                                                                                                                                                      |
| `ACTION-4`        | globe renderer ownership domains                      | CONFIRMED | see `F5-03`                                                                                                                                                                                                       | none; `Q-86` for the test                                      | See `F5-03`.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ACTION-5`        | terrain realizations under quadtree lifetime          | CONFIRMED | see `F5-02`; C11 queue `:2806` still warns "never run two of `C11-32`/`C11-33`/`C11-34` concurrently — same tile-buffer lifetime"                                                                                   | `C11-34` (NOT STARTED); FAR-203                                | See `F5-02`. Acceptance unchanged: creates/destroys/live bytes plateau on the out-and-back route, second leg hits materially exceed cold, WebGL ownership byte-identical.                                                                                                                                                                                                                                                                                          |
| `ACTION-7`        | ocean source ownership; sentinel bound                | CONFIRMED | see `F5-09`                                                                                                                                                                                                       | `DEFERRED_WORK.md:4628`; FAR-205                               | See `F5-09`; the sentinel bound and the multi-view/leased ocean realization remain.                                                                                                                                                                                                                                                                                                                                                                              |
| `ACTION-8`        | demand-gate the MSAA depth resolve                    | CONFIRMED | see `F5-18`                                                                                                                                                                                                       | `DEFERRED_WORK.md:10481` S4-4                                  | See `F5-18`; reader inventory first, unknown readers force the resolve, demand and resolve opens asserted separately.                                                                                                                                                                                                                                                                                                                                            |
| `ACTION-10`       | instrument pass ownership                             | CONFIRMED | see `F5-19`                                                                                                                                                                                                       | `DEFERRED_WORK.md:10482` S4-5 / FAR-405-706                    | See `F5-19`.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ACTION-11`       | async epoch + loss-family teardown                    | CONFIRMED | epoch half landed and verified: `WebGPUModelPipelineCache.ts:2026-2044`, bumps at `:3110`, `:3197`, `:3243`, `:3291`, and the async resolve re-checks both `_sceneFormatGeneration` and `_lifecycleEpoch` at `:3493-3529` before writing back. Loss half open — see `F5-08` | Batch 819; Batch 1199; `Q-86`                                  | See `F5-08` for the remaining device-loss owner protocol; the invalidation epoch is done.                                                                                                                                                                                                                                                                                                                                                                        |
| `ACTION-12`       | repair SceneOctree + RenderScheduler semantics        | CONFIRMED | see `F5-05` (with its corrected mechanism) and `F5-06`                                                                                                                                                            | FAR-504 for the scheduler; none for the octree                 | Both opt-in paths are inert-or-broken and only one is queued.                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ACTION-13`       | repair OIT cache identity                             | CONFIRMED | see `F5-10`                                                                                                                                                                                                       | none                                                           | See `F5-10`.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ACTION-14`       | no wholesale ownership integration                    | CONFIRMED | see `F5-11`                                                                                                                                                                                                       | FAR-200/201/202/203                                            | See `F5-11`; the standing rule is "one concrete, measured, zero-copy consumer first".                                                                                                                                                                                                                                                                                                                                                                            |
| `ACTION-15`       | remaining tests + optional APIs                       | CONFIRMED | see `F5-12` (corrected scope); split-screen and T5 closed — spot-check 2                                                                                                                                          | `Q-86`; DX-12; `PICKING_ARCHITECTURE_STATE_2026-08-17.md`      | See `F5-12` for the two remaining coverage holes.                                                                                                                                                                                                                                                                                                                                                                                                                |
| `REJECT-1`        | never call `evictStaleResources` per frame            | CORRECTED | `WebGPUGlobeSurfaceTileBuffers.ts:577-590` (inline destroy, coordinate keys); only two callers — `WebGPUGlobeSurfaceRenderer.ts:2813-2814` (delegator) and `:2971` (destroy, empty set); `_wireframeIndexCache` is cleared **only** in `WebGPUGlobeSurfaceRenderer.ts:3020-3023` | none (guard on `C11-34`)                                       | Substance stands. **Citation corrected:** `WebGPUGlobeSurfaceWireframe.ts:44-46` no longer "documents its own cache outside the sweep" — it now *claims* the buffer "is destroyed in `destroy()` / `evictStaleResources()`", which the code contradicts: the sweep iterates `_tileBufferCache` only. So the wireframe cache is still outside the sweep **and** the module's own comment misstates it (a C16 comment defect worth its own one-line fix).            |
| `REJECT-2`        | never flip the module-global MRT flag per frame       | CONFIRMED | `WebGPUSceneFBTargetHelpers.ts:35` (`let _mrtMode = true`), `:49` `setSceneFBMrtMode` (no caller in `Source` — only the spec calls it); `WebGPUContext.ts:5014` `updateAndClearFramebuffers`, `:5072-5077` "observe-only … nothing in the render path gates on it", `:5086-5094` `computeAttachmentDemand` | `DM-12` acceptance (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:420-427`) | As judged. A topology change must be a context-owned immutable signature (target count/formats, depth format, sample count) folded into shader, pipeline, render-target and pass-descriptor identity, published during `prepareFrame`, prewarmed asynchronously and committed atomically at a frame boundary — never a per-frame module-global flip.                                                                                                             |
| `REJECT-3`        | never route globe terrain through SceneOctree         | CONFIRMED | C11 queue `:2367-2371` — "Never replace terrain quadtree, 3D Tiles traversal, or voxel octree with the optional general `SceneOctree`"                                                                              | C11 queue rule `:2369`                                         | As judged.                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `REJECT-5`        | a checkbox is not proof vsync is off                  | CONFIRMED | no `maxThroughput` in `run-performance-campaign.mjs`; C11 queue `:2516` still lists "GPU timestamps/max-FPS … remain open" for `C11-168`                                                                            | `C11-168` open sub-item                                        | As judged. Presented-FPS claims stay display-paced until a max-throughput lane exists whose vsync state is verified and recorded by the driver/browser protocol.                                                                                                                                                                                                                                                                                                  |
| `REJECT-6`        | no scaffold integration without a measured consumer   | CONFIRMED | see `F5-11`                                                                                                                                                                                                       | FAR-200                                                        | See `F5-11`.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `REJECT-7`        | never remove/reorder per-frustum depth/stencil clears | CORRECTED | The `pickPassOverhead` note is in **`FORK_ARCHITECTURE_REMEDIATION_LEDGER_2026-07-13.json:632`**, not in the plan `.md`: "multi-frustum object queries intentionally encode one render pass per independently projected frustum; color clears once then loads, depth/stencil clear per slice…" | FAR ledger `pickPassOverhead`; `F5-19`                         | Substance stands; the citation must say the **ledger JSON**, line 632. Per-frustum depth/stencil clears are load-bearing (multi-frustum depth plane, translucency, classification, picking, post-process); fold or gate a boundary only after its owner/reason is named and its successor proven equivalent in clear/load/store order.                                                                                                                            |
| `REJECT-8`        | never add only the octree enum one-liner              | CONFIRMED | see `F5-05`                                                                                                                                                                                                       | none                                                           | See `F5-05` — and note the corrected mechanism: the one-liner would switch on culling whose eligibility, bounds, ordering and shadow-caster semantics are still unspecified.                                                                                                                                                                                                                                                                                      |
| `GATE-1-ACCEPTANCE` | terrain/ocean lifetime acceptance matrix            | CONFIRMED | no owning row carries the matrix (`C11-34` `:1669`/`:2300`, FAR-203, FAR-205 checked); the runner can measure the plateau — `run-performance-campaign.mjs:555`, `:920`, `:956`, `:964` `terrainCommandRefreshRecords` | none (attach to `C11-34`)                                      | As judged. Carry the full matrix onto `C11-34`: creates/destroys/live bytes plateau; no destroyed-resource reuse; warm second leg; WebGL byte-identical; device loss and two contexts on one device; fill→real, exaggeration, wireframe, shadow/pick/capture, two providers at identical coordinates, ocean swap, request-render wakeups.                                                                                                                          |
| `GATE-3-ACCEPTANCE` | lifecycle acceptance                                | CONFIRMED | first and third clauses covered (`WebGPUModelPipelineCache.ts:3493-3529`; `WebGPUModelFeatureId.js:1433`, `:1947`); alternate-family clause open — see `F5-08`                                                     | Batches 819/1072/1195; `Q-86`                                  | As judged. Remaining acceptance: after a device loss every per-object cache family (labels, polylines, billboards, clouds, voxels, translucency state) recovers on the new device without a scene-format bump, in a two-context shared-device scene.                                                                                                                                                                                                              |
| `GATE-4-ACCEPTANCE` | positive controls + temporal sequences              | CORRECTED | SR table `QUEUE_2026-08-29_RESEARCH_DISPATCH.md:38-55` — SR-1 carries only "never remove/default-disable to win a metric"; nothing carries the toggle-as-control rule                                              | SR-1/SR-6/SR-8/SR-9 (partial)                                  | The judgement's migrate text silently drops the clause in its own title. **Restore it:** disabling OIT, clustered lighting, the scheduler, the octree, shadows or post-processing is not an optimisation — an OFF leg is an *attribution control*, never the shipped fix. Then, as judged: compare against the current `Tools/visual-regression` baselines; add temporal sequences for TAA history, RTE under motion and altitude transitions; any pass-folding or clear-reordering change additionally needs numeric `pickPosition`, primitive/billboard pick, classification, depth-plane/horizon, multi-frustum, off-camera shadow-caster, HDR, 2D/Columbus, resize and device-recovery checks with the feature on. |
| `DOC-7-REQ-1`     | coverage-matrix successor                             | CONFIRMED | `migration_doc/` still holds only `FORK_EXTENSION_TEST_COVERAGE_MATRIX_2026-07-15.json`; today `Source/Renderer/WebGPU/` has **272** files and `Specs/Renderer/WebGPU/` has **143**                                  | none (adjacent: DX-12)                                         | As judged, with today's counts. A successor snapshot — modules vs spec files vs runner home, **generated by a script** rather than hand-counted — belongs beside the DX-12 spec-homes pass. Counts prove presence, not semantic coverage.                                                                                                                                                                                                                          |
| `DOC-7-REQ-4`     | production-integration tests                          | CONFIRMED | see `F5-12`                                                                                                                                                                                                       | `Q-86`; DX-12                                                  | See `F5-12` (corrected scope).                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## 2. Spot-checks of items the judgement retired

Five chosen for blast radius if the document is deleted on a wrong "resolved": a live-code
correctness claim retired as owned elsewhere, an asset/tooling claim contradicted by the current
worktree, a rejected-shortcut guard marked RESOLVED, an async-correctness claim resting on a single
cited batch, and a standing rule declared superseded by documents I could check word-for-word.

| # | item                       | judgement status | my verdict                       | evidence read today                                                                                                                                                                                                                                                                                                       |
| - | -------------------------- | ---------------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | `F5-04` — MRT slot 1 observe-only | SUPERSEDED | **UPHELD, with a label warning** | The defect is fully live: `WebGPUContext.ts:732` `forceSceneMRT = true`, `:5072-5077` "observe-only … nothing in the render path gates on it", `WebGPUSceneFBTargetHelpers.ts:35` `let _mrtMode = true` whose only mutator `:49` has no caller in `Source` (only `WebGPUSceneFBTargetHelpersSpec.js`). Ownership is real: `DM-12` (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:420-427`) names "owned by `F5-04` and audit **action 9**", and `DEFERRED_WORK.md:9942` C9-10-CONSUMER-DRIVEN-MRT carries the topology-parametric site list. So SUPERSEDED = *ownership moved*, not *fixed* — the migration must not let the word imply the code is clean. |
| 2 | `F5-14` — split-screen + T5 assets | RESOLVED | **UPHELD**                       | Split-screen builds both viewers on launch: `Apps/WebGPUTest/split-screen-comparison.html:483` `new Cesium.Viewer`, `:518` `Viewer.createAsync`, globals `:562-563`, wired from `#btnLaunch` `:445`. T5: `git ls-files` shows all twelve `tycho2t5_80_*.jpg` / `tycho2t5_80_diffuse_*.jpg` (2048) tracked. The untracked `tycho2t5_80_4096_*` files and modified `SkyBoxResolutionPolicy.ts` in the worktree are the *opt-in* tier, which `R-2026-09-02-7` explicitly rules stays uncommitted — expected in-flight state, not a reversal. |
| 3 | `REJECT-9` — never demand-gate pick only at ID creation | RESOLVED | **UPHELD**                       | `WebGPUModelFeatureId.js:2392-2440` gates realization on `pickPassActive` **and** promotes pick resources through `promoteFeaturePickResources`, then re-observes the pair and requires `featureResourceGenerationMatches` after any retirement boundary (`:2408-2432`, `:2512-2540`), with generation destruction at `:1433` / `:1947`. The failure the shortcut warned about — retained bind groups still pointing at placeholders — is structurally prevented by the re-observation, not merely avoided by convention. |
| 4 | `F5-07` — stale async pipeline republish | RESOLVED | **UPHELD**                       | `WebGPUModelPipelineCache.ts:2026-2044` declares `_sceneFormatGeneration` / `_lifecycleEpoch` / `_errorSwapGeneration`; every invalidation entry point bumps the epoch (`:3110`, `:3197`, `:3243`, `:3291`, `:3349`); the async kick captures both at `:3493-3494` and writes back only if **both** still match at `:3507-3508` and `:3528-3529`. The guard is live code, not a comment. |
| 5 | `REJECT-4` — a toggle is an A/B control, never a benchmark-only removal | SUPERSEDED | **PARTIAL REVERSAL** | Only half is carried. `CLAUDE.md:20` and SR-1 (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:40`) say "never remove, default-disable, bypass or visually degrade a feature to win a metric" — the *prohibition*. Neither says the *permission and its limit*: that an OFF measurement is a legitimate attribution control whose result may never be shipped as the fix. I read the whole SR-1…SR-17 table (`:38-55`); SR-9 covers interleaved A/B for GPU timing only. With this item retired **and** the clause dropped from `GATE-4-ACCEPTANCE`'s migrate text, deleting the document would lose the rule outright. |

## 3. Reversals

**One partial reversal — a governance rule, not code.**

`REJECT-4` is marked SUPERSEDED against `CLAUDE.md:20` / SR-1, but those sources carry only the
prohibition half ("never remove or default-disable a feature to win a metric"). The distinctive half
— *a feature toggle is a legitimate A/B attribution control, and the OFF leg is never the shipped
fix* — is stated nowhere else I could find, and the judgement additionally dropped the same clause
from `GATE-4-ACCEPTANCE`'s migrate text (its own title still contains it). On the judgement as
written, this rule dies with the file. Restore it in both places before deletion; the corrected text
is in the `GATE-4-ACCEPTANCE` row above.

**No code-level reversal was found.** The four other spot-checks each hold against today's source.
The nearest thing to a false retirement is `F5-04`, where the code defect is entirely live and only
ownership has moved (`DM-12`, C9-10) — the judgement says so in its own evidence, but the word
SUPERSEDED will read as "fixed" to anyone skimming the register. Label it "live, owned by DM-12"
in the migration.

## 4. Document verdict

**REMOVE-AFTER-MIGRATION**, on the judgement's two conditions **plus** the corrections above.

Reasons:

- All 38 still-valid/partial items are adjudicated; none is unreachable. Thirty-one confirm exactly
  as judged, seven need the corrected text recorded here (`F5-PRINCIPLES`, `F5-05`, `F5-12`,
  `F5-15`, `REJECT-1`, `REJECT-7`, `GATE-4-ACCEPTANCE`). Nothing in the file is a code authority
  that only it holds — each live defect is reproducible from the source lines cited above.
- Every inbound reference can be repointed, and one of them does not exist:
  - `CLAUDE.md:19` — real, needs the replacement authority pointer.
  - `migration_doc/CAMPAIGN_STATE.md:38` — **does not exist.** `CAMPAIGN_STATE.md` (295 lines)
    contains no occurrence of `FABLE5`, `Fable 5` or "fork-performance authority"; line 38 is the
    `### C11` heading. Drop this inbound ref from the migration checklist.
  - `migration_doc/README.md` — the rows have drifted to **`:28`, `:334`, `:358`** (the judgement
    said 28/221/243).
  - `CODEX_DISAGREEMENTS_WITH_FABLE_REVIEW_2026-08-16.md` — **33** line-anchor links (not ~35), plus
    the §2 table at `:60-71`, which already restates all eleven principles in one line each. That
    table is the reason the principles are not lost — but its links are line anchors into the
    removed file, so they need repointing or a one-line note that they resolve against git history.
  - `FORK_PERFORMANCE_WEEKLY_CHANGE_DEFENSE_2026-07-15.md:611` and
    `QUEUE_2026-07-23_CAMPAIGN13.md:1289` — real, as described.
  - `QUEUE_2026-08-29_RESEARCH_DISPATCH.md` cites the ids, not the filename: `F5-16` at `:348`,
    `:416`; `F5-02` at `:393`, `:408`; `F5-04` + `F5-18` + "audit action 9" at `:424`. The `F5-` id
    register and an "action 9" label must therefore survive the migration verbatim.
  - No code, tool or spec references the file (confirmed by a repo-wide grep; the only other hits
    are gitignored lane evidence under `Tools/visual-regression/output/lane-treebeard-2026-08-29/`).

Not KEEP-PART: the eleven principles already exist as one-line statements in the CODEX §2 table, and
every remaining live item has a current file:line above that reproduces it without the file.

## 5. Notes for the migration

- Carry the seven corrected texts, not the judgement's originals, for the items marked CORRECTED.
- Add the two missing principles (bounded visual evidence; honest incomplete test state) and the
  `REJECT-4` control-leg clause; otherwise three of the eleven principles do not survive deletion.
- Label `F5-04` "live, owned by DM-12 / C9-10" rather than SUPERSEDED in the successor register.
- Two small repairs surfaced while verifying, each worth its own line rather than silence:
  - `WebGPUGlobeSurfaceWireframe.ts:44-46` claims the wireframe index buffer is destroyed in
    `evictStaleResources()`; it is not — `_wireframeIndexCache` is cleared only in
    `WebGPUGlobeSurfaceRenderer.ts:3020-3023`. Comment fix, C16-shaped.
  - The uncommitted working-tree edit to `SkyBoxResolutionPolicy.ts` contradicts itself in one
    docblock — "**The 4096 tier is bundled for both t5 variants**" four lines above "so the opt-in
    tier is reproducible but not bundled". Out of scope here, but it belongs to whichever lane owns
    that file before it lands.
- Sub-claim carried forward as unverified: `F5-09`'s "one bound function per visible model per
  frame". It was not measured then and not measured now.
