# Verification — `audits/2026-04-30_ARCHITECTURE_PERFORMANCE.md`

- **Document under verification:** `migration_doc/audits/2026-04-30_ARCHITECTURE_PERFORMANCE.md`
- **Judgement verified:** `migration_doc/audits/2026-09-02_old_review_validity/2026-04-30_ARCHITECTURE_PERFORMANCE.judgement.json` (48 items; 8 STILL-VALID, 8 PARTIAL, 12 RESOLVED, 12 SUPERSEDED, 8 STALE)
- **Verifier:** Ciryandil
- **Date:** 2026-09-03
- **Tree:** `main` at `532463ae35` (working tree; `RENDERER_LANDSCAPE_AUDIT_2026-09-02.md` and `audits/2026-09-02_ARCHITECTURE_REVIEW_PHASE1.md` present but untracked)

## Counts

| Bucket                                   | Count  |
| ---------------------------------------- | ------ |
| STILL-VALID / PARTIAL items adjudicated  | 16     |
| — CONFIRMED                              | 11     |
| — CORRECTED                              | 5      |
| — REFUTED                                | 0      |
| — UNADJUDICATED                          | 0      |
| Spot-checks of RESOLVED/SUPERSEDED/STALE | 5 (+1) |
| Reversals found                          | 0      |

Every citation below was read this session at the line given. The judgement's
line anchors were treated as leads only; where they had moved, the current
location is what is recorded.

## Adjudication of the still-valid and partial items

| id    | title                                        | verdict   | current file:line                                                                                                                                                                                                                                                                                                                                                                                                                                                             | owning row                                                                                                                                             | migrate text (corrected where marked)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | -------------------------------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| D1    | RenderCommand adoption vs DrawCommand        | CORRECTED | `packages/engine/Source/Renderer/WebGPU/RenderCommand.js` (whole file); `GraphicsContext.ts:1903`                                                                                                                                                                                                                                                                                                                                                                              | none for the adoption decision                                                                                                                         | **Corrected — the "3 Scene adopters" figure is a grep artefact.** `RenderCommand.js` has **zero importers anywhere in `packages/*/Source`**: the only references to the identifier are doc comments (`GraphicsContext.ts:1903-1904`, `Stubs/WebGLStubShader.ts:16`, `:392`, `WebGLCompatibilityStub.ts:102`, `:128`, `:134`). The three files the audit named adopt nothing — they carry locally defined functions whose names merely contain the string (`ClassificationPrimitive.js:1300`, `GroundPrimitive.js:802`, `QuadtreePrimitive.js:1365`). 25 Scene files construct `DrawCommand` directly. The debt is stronger than recorded: the abstraction is unreferenced, not half-adopted, and the decision (wire it, or retire it) is unowned. |
| D3    | RenderCommand path + exemption-list claim    | CORRECTED | `scripts/bundleVariantPlugin.js:276-286` (five entries), substring match `:310-314`                                                                                                                                                                                                                                                                                                                                                                                           | `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1381` (fleet1, QUEUED)                                                                                          | Path is right; the exemption claim is false — the list holds `WebGLCompatibilityStub`, `WebGPUShaderTranslator`, `WebGLStubPipelineExtractor`, `WebGPUNagaTranspiler`, `WebGPUModelMetadata`, and not `RenderCommand`. **Corrected consequence:** because nothing imports `RenderCommand.js` (see D1), the webgl-only throwing-stub path is currently unreachable, so the fleet1 row is a latent-on-adoption hazard rather than a live defect. Note also that `CLAUDE.md`'s own exemption list still names four entries, not five.                                                                                                                                |
| A1a-3 | `unknown[]` FeatureRenderer lifecycle typing | CONFIRMED | `packages/engine/Source/Renderer/GraphicsContext.ts:324`, `:331`, `:340-342`, `:466`, `:485-505`, `:526`                                                                                                                                                                                                                                                                                                                                                                       | none                                                                                                                                                   | Carry as written. Every lifecycle slot on the `FeatureRenderer` tree is still `(...args: unknown[])`, the base still owns every optional slot so no caller has to narrow, and parity across the registered renderers is enforced only by duck-typed existence checks.                                                                                                                                                                                                                                                                                                                                                                                          |
| A1b-4 | exemption membership enforced one-way        | CONFIRMED | `scripts/bundleVariantPlugin.js:276-286`, `:310-314`; `scripts/__tests__/bundleVariantPlugin.spec.mjs:266-278`; `Tools/variant-smoke-test.mjs:18-24`                                                                                                                                                                                                                                                                                                                           | `FIX_QUEUE:1381`, `:1404`, `:1405` (fleet1, QUEUED)                                                                                                    | Carry as written, with one locator refreshed: the spec proves listed entries pass through and nothing proves a backend-neutral file is listed; the prefix match at `:310-314` still over-exempts, but the file riding the `WebGPUModelMetadata` entry today is **`WebGPUModelMetadataVariantKey.ts`** (`WebGPUModelMetadataCache.js` no longer exists). The negative check remains unbuilt.                                                                                                                                                                                                                                                                     |
| A1c-1 | no per-variant named-export sweep            | CONFIRMED | `Tools/variant-smoke-test.mjs:4`, `:18-24` (pass criteria: page loads, `window.Cesium` defined, Viewer constructs, five frames, zero console errors)                                                                                                                                                                                                                                                                                                                           | none                                                                                                                                                   | Carry as written. A grep for `Object.keys(Cesium)` or an export walk over `Source/CesiumWebGLOnly.js` / `Source/CesiumWebGPUOnly.js` returns nothing; `bundleVariantPlugin.spec.mjs` covers the resolver matrix, not the barrels.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| A2a-2 | `JSON.stringify`-keyed sampler and BGL cache | CORRECTED | `packages/engine/Source/Renderer/WebGPU/WebGPUContext.ts:5599` (sampler), `:5622` (BGL)                                                                                                                                                                                                                                                                                                                                                                                        | `RENDERER_LANDSCAPE_AUDIT_2026-09-02.md:230`, `:404` (THREE-A06) owns the BGL half                                                                      | Both keys are still `JSON.stringify(descriptor)`. **Corrected on severity and ownership:** the "hot during pipeline builds" premise does not hold — `WebGPUContext.getOrCreateBindGroupLayout` has **zero callers** (only the twin definition at `WebGPUResourceManager.ts:100`), and `getOrCreateSampler` has exactly one (`WebGPUSampler.ts:193`). The BGL cache is dead-by-adoption and is already owned by THREE-A06, whose disposition is gain-callers-or-remove. Only the sampler half is a live (and cheap) hand-serialization candidate.                                                                                                                  |
| A2a-3 | per-frame `writeBuffer` cadence              | CONFIRMED | `WebGPUPolylineRenderer.js` 7 sites, `WebGPUModelRenderer.ts` 16, `WebGPUBufferPrimitiveRenderer.ts` 1; 396 sites across 116 files                                                                                                                                                                                                                                                                                                                                             | `DEFERRED_WORK.md:4895` (`NEW-COLLECTIONS-DIRTY-GATE`, open), `:4989` (`NEW-PARTIAL-WRITE-COALESCING`, shipped Batch 229); FAR-30x collection acceptance | Carry as written with today's counts (396 sites / 116 files, not 227 / 66). Collections are addressed; Polyline and Model still upload on an unprofiled per-update cadence and should be measured against the FAR settled-frame acceptance.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A2a-6 | indirect draw opt-in, never auto-flipped     | CORRECTED | `WebGPUContext.ts:725` (`useIndirectDrawForTiles = false`); `WebGPUPerformanceManager.ts:256-258`, `:411-414`; sole consumer `WebGPUSceneRenderer3DTilePasses.ts:284`                                                                                                                                                                                                                                                                                                          | `RESEARCH_AND_PENDING_TOPICS.md` §R-7 (evidence)                                                                                                       | Core claim holds: the path is opt-in, nothing flips it, and §R-7 measured no candidate pass over the 3 ms bar. **Corrected:** the fleet1 defect the judgement calls "the only open defect on it" — the run-of-one branch swallowing a throw — was **fixed in Batch 1198 (`2a1c016c28`)**; `WebGPUSceneRenderer.ts:654-679` now reports through the deduped channel. `FIX_QUEUE:1348` is stale-green. The consumer locator is `:284`, not `:124` / `:207`.                                                                                                                                                                                                        |
| A2a-7 | prewarm coverage                             | CONFIRMED | prewarm present in `WebGPUBillboardRenderer.js`, `WebGPULabelRenderer.js`, `WebGPUPointPrimitiveRenderer.js`, `WebGPUPolylineRenderer.js`, `WebGPUGlobeSurfaceShaders.ts`, `WebGPUGlobeSurfaceRenderer.ts:746`, `WebGPUDepthPlane.ts:375`, `WebGPUSceneRendererEnsureResources.ts:535`, `:554-580`, `WebGPUContext.ts:1450-1466`; **zero** in `WebGPUVoxelRenderer.ts`, `WebGPUProceduralCloudRenderer.ts`, `WebGPUVolumetricFogRenderer.ts`, `WebGPUCloudRenderer.ts`, `WebGPUModelPipelineCache.ts` | `DEFERRED_WORK.md:10551`, `:10611`; none for the lazy family                                                                                            | Carry as written with the coverage list widened: the globe now prewarms its default pipelines, the boot set warms the depth plane, and the context warms two GlobeTerrain shader-module variants in its idle-init window. Model and the whole lazy family (Voxel, ProceduralCloud, VolumetricFog, Cloud) still have zero prewarm; warming at dynamic-import settle is still unowned.                                                                                                                                                                                                                                                                            |
| A2b-3 | Model keeps a private pipeline cache         | CONFIRMED | `WebGPUModelPipelineCache.ts` 4,751 lines; central resolve at `:3449-3460`; private synchronous hatch at `:867-869` / `:964`; `:1438` "never consults the central pipeline cache"                                                                                                                                                                                                                                                                                              | `DEFERRED_WORK.md:10611`; `DX-10` (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:169`, `:950`)                                                                 | Carry as written with locators refreshed (`:3449`, not `:2033-2037`; `:867-869`, not `:868-869`). **One correction:** the fleet3 row cited as an open correctness finding on the same cache (`FIX_QUEUE:1335`, display keys missing the generated-chunk class hashes) was **fixed in Batch 1209 (`79c9fa2ebb`)** — `_metadataVariantKey` at `:2973` now folds the metadata and customShader class hashes and is applied at nine call sites. The row is stale-green. `DX-10` still cites 4,591 lines; the file is 4,751.                                                                                                                                           |
| A2c-4 | post-process ping-pong always allocated      | CONFIRMED | `WebGPUPostProcessPipeline.ts:611-627` (both intermediates created unconditionally in `_intermediateFormat`), `:1510-1513` (`hasActiveStages` false ⇒ `_executeCopyStage`, return)                                                                                                                                                                                                                                                                                             | none directly; adjacent `RENDERER_LANDSCAPE_AUDIT_2026-09-02.md:233` (THREE-A09, "always-allocated MRT")                                                | Carry as written; only the anchors move (`:611-627` / `:1510`). Both full-resolution intermediates stay resident for the default effect-free WebGPU scene, which always routes through this pipeline for the canvas blit. Lazy allocation on the first active-stage frame remains the unowned fix.                                                                                                                                                                                                                                                                                                                                                             |
| R1    | `WebGPUContext.ts` size                      | CONFIRMED | 7,914 lines; inline sampler/BGL caches at `:5594-5631`                                                                                                                                                                                                                                                                                                                                                                                                                        | `DX-08` (`QUEUE_2026-08-29_RESEARCH_DISPATCH.md:938`); `WEBGPU_CONTEXT_DECOMPOSITION_PLAN.md`; `ARCHITECTURE.md` §8                                     | Carry as written. The file is 7,914 lines against the 4,363 the audit flagged, after eight companion extractions; the audit's proposed seams are all still in the file and `DX-08` should reuse the seam list.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| R2    | `WebGPUSceneRenderer.ts` size                | CONFIRMED | 5,140 lines; thirteen `WebGPUSceneRenderer*.ts` companions on disk                                                                                                                                                                                                                                                                                                                                                                                                            | `DX-09` (`:944`, HELD); `FIX_QUEUE` Q-10; `ARCHITECTURE.md` §8.2                                                                                        | Carry as written. FrustumLoop and PickPass landed as companions; refraction capture, velocity pass, edge-overlay composite and the post-process handoff are still inline.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| R4    | no automated Scene-boundary guard            | CONFIRMED | `eslint.config.js` has no `no-restricted-imports` for `Renderer/WebGPU`; `Tools/ci-guards.spec.mjs`, `Tools/verify-landing-compliance.mjs`, `Tools/pre-push-guard.mjs` contain **0** `isWebGPU` hits; live branches at `GlobeSurfaceShaderSet.js:1117`, `OceanSurfacePrimitive.js:384` / `:502`, `ViewportQuad.js:144`                                                                                                                                                          | `FAR-308` (`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:691`); `FIX_QUEUE:1401`                                                                    | Carry as written. **Two supporting citations corrected:** `Scene.js:3741` (the `isWebGPU` light overwrite, `FIX_QUEUE:1350` / Q-1) is **fixed in code** — `Scene.js:3790-3798` now publishes `frameState.light` unconditionally and defers derived lighting to `_alternateSceneRenderer.updateDerivedLighting`, so that row is stale-green; `GlobeSurfaceShaderSet.js:1086` has moved to `:1117` and is still live. The guard itself has never been built, and FAR-308 is an L behind FAR-103 / 200 / 300.                                                                                                                                                       |
| R7    | `_scenePipelineFormatGeneration` manual half | CORRECTED | `WebGPUContext.ts:911-922` (documented), `:933-943` (`renderTargetGeneration`; WebGL returns 0 at `Context.js:818`); 33 files read the epoch; **no** on-hit format check in `WebGPURenderPipelineCache.ts`                                                                                                                                                                                                                                                                      | `ROADMAP_AND_DEFERRED_WORK.md` Q14-HDR-TOGGLE-INVALIDATION; `QUEUE_2026-07-15_CAMPAIGN9.md:124` (`NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION`, **NOT STARTED**) | The promotion half and the central-key half hold, and the missing structural guard holds (no `sceneColorFormat` assertion on a served hit; 33 renderer-local observers). **Corrected:** the "predicted failure has materialised" evidence is out of date — `WebGPUGlobeSurfaceRenderer.ts:1274-1277` now clears `_materialPipelineCache` alongside the production, wireframe and debug-fragment caches, fixed in **Batch 1185 (`3ebd7e193e`)**; `FIX_QUEUE:1332` (lane A4) is stale-green. The MSAA-flip row is the one live instance and is still NOT STARTED.                                                                                                    |
| R9    | `ContextRegistry` per-module static          | CONFIRMED | `packages/engine/Source/Renderer/GraphicsContext.ts:749` (`private static _registry`), register `:833`, unregister `:908`; no `globalThis` / `Symbol.for` anchor                                                                                                                                                                                                                                                                                                               | none (the 2026-07-13 fork-vs-upstream audit's `ContextRegistry` note is about an init-failure leak, not module identity)                                | Carry as written. Two loaded copies of `@cesium/engine` yield two registries and split-screen contexts cannot discover each other; `ARCHITECTURE.md` §2 states the multi-context guarantee without the caveat, and nothing detects the condition.                                                                                                                                                                                                                                                                                                                                                                                                              |

## Spot-checks of the un-verified direction

Five items the judgement closed without anyone checking, chosen for
cost-if-wrong: a claimed fix on a device-loss guard, a WebGL-fallback parity
claim, a resource-growth claim, the backend-import invariant, and the
Scene/backend boundary sweep. One extra pipeline-key check was added because
`CLAUDE.md` treats key aliasing as load-bearing.

| id         | judgement status      | what was checked                                                                                                       | current file:line                                                                                                                                                                                                                                                                                                                             | verdict           |
| ---------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| D7         | RESOLVED              | Does the pipeline cache actually have an LRU cap, or was a batch cited loosely?                                         | `WebGPURenderPipelineCache.ts:428` (`DEFAULT_MAX_SIZE = 1024`), `:483`, `:501-503` (`setMaxSize`), `:526-537` (`evictIfNeeded`), `:598`, `:684`, `:1153` (`evicted` stat)                                                                                                                                                                      | UPHELD — resolved |
| A1b-2      | RESOLVED              | Does the lazy-FR readiness model really stop the one-frame WebGL fallback, and do the three named consumers honour it?  | `GraphicsContext.ts:2083-2101` (`getFeatureRendererReadiness`), `:429-435` (`resolveFeatureRendererReadiness`); consumers `VoxelPrimitive.js:474` / `:508`, `PointCloud.js:171-174` / `:194`, `PointCloudEyeDomeLighting.js:61`, `GaussianSplatPrimitive.js:1482-1488` (`if (!defined(fr) && readiness.kind !== "unsupported") return;`)         | UPHELD — resolved |
| A2e-3      | RESOLVED              | Is the culler's `mapAsync` chain actually guarded (H-R9)?                                                              | `WebGPUGPUCuller.ts:468-495` — both `mapAsync` calls inside `Promise.all(...).then(...).catch(...)` with a nested `try` / `catch` around each `unmap`                                                                                                                                                                                          | UPHELD — resolved |
| A1a-2      | STALE                 | Is the "0 imports from `Renderer/WebGPU/` in Scene" invariant still describable as clean?                              | Exactly one real import: `Scene/Model/MetadataWGSLPipelineStage.js:60` → `../../Renderer/WebGPU/WebGPUModelMetadata.js` (sanctioned by the exemption entry)                                                                                                                                                                                    | UPHELD — stale    |
| R5         | RESOLVED              | Did the 2026-05-02 sweep really remove the Model / EdgeVisibility / Vector3DTile / GroundPolyline backend branches?     | `Model/Model.js` — the sole `isWebGPU` occurrence is the comment at `:2927`; the gate at `:2932` is `if (!modelFr)`. `EdgeVisibilityPipelineStage.js`, `Vector3DTilePrimitive.js`, `Vector3DTilePolylines.js`, `GroundPolylinePrimitive.js` — **0** `isWebGPU` hits each                                                                        | UPHELD — resolved |
| A2b-1 / H6 | SUPERSEDED / RESOLVED | Extra check: is the pipeline-key module fold and the blend hand-serialization as described?                            | `WebGPURenderPipelineCache.ts:1100` (`sh:` module-identity fold), `:1265` (`wrongModuleHits`), `:978` (blendConstant deliberately excluded), `:965` / `:967` (residual stencil `JSON.stringify`)                                                                                                                                               | UPHELD            |

## Reversals

**None found.** No item marked RESOLVED, SUPERSEDED or STALE was found to be
still live. The two things worth carrying forward instead are of the opposite
shape.

1. **A premise defect inside the still-valid set, not in the closed set.** The
   auditor's D1 — one of the eight STILL-VALID items — rests on a grep that
   matched local function names (`updateAndQueueRenderCommand`,
   `createRenderCommandsForSelectedTiles`) rather than imports of
   `RenderCommand.js`. The module has no importer at all. The corrected
   statement is stronger than the original, so migrating D1 verbatim would
   understate the debt and would repeat the same false "3 adopters" figure the
   2026-04-30 document already carried.
2. **Four queue rows are stale-green.** `FIX_QUEUE:1332` (globe material cache,
   fixed Batch 1185), `:1335` (model display-pipeline key, fixed Batch 1209),
   `:1348` (indirect run-of-one swallow, fixed Batch 1198) and `:1350` / Q-1
   (`Scene.js` light branch, fixed by the `updateDerivedLighting` hook) all
   still read QUEUED or CONFIRMED while the code is repaired. That is a defect
   in the queue, not in this document; it is reported here because four of the
   sixteen migrate texts lean on those rows as live evidence.

## Document verdict

**REMOVE-AFTER-MIGRATION** — conditional on the corrections above travelling
with the text.

Reasons:

- All 16 still-valid / partial items are adjudicated; nothing is UNADJUDICATED.
- No section of the document is a live authority nothing else holds. Its live
  content is fully covered elsewhere once migrated: `ARCHITECTURE.md` §§1-8 and
  `CLAUDE.md` hold the layering, WGSL and pipeline-key contracts;
  `RESEARCH_AND_PENDING_TOPICS.md` already holds the long-tail research items
  (A2d-2, A2d-3, D7) and §R-7; `WEBGPU_CONTEXT_DECOMPOSITION_PLAN.md` and
  `DX-08` / `DX-09` / `DX-10` hold the size risks; `FAR-308` holds the boundary
  guard; and `RENDERER_LANDSCAPE_AUDIT_2026-09-02.md` (THREE-A06, THREE-A09)
  has picked up the two cache and post-process threads.
- All inbound references resolve and can be repointed. Verified this session:
  `CLAUDE.md:603` (exact); `migration_doc/README.md:343`, `:391`, `:392` (the
  judgement's `:228` / `:272` anchors are wrong — the live rows are at 343 and
  391-392); `RESEARCH_AND_PENDING_TOPICS.md:4` and `:244` (exact);
  `ROADMAP_AND_DEFERRED_WORK.md:44` (exact);
  `audits/2026-06-11_ULTRA_REVIEW.md:336` (exact);
  `audits/2026-06-11_ULTRA_REVIEW_findings.json:2383`, `:2692`, `:3049`,
  `:3106`, `:3147`, `:3166` (provenance only); `_DOC_AUDIT_PLAN.md:5`, `:55`,
  `:58-59`, `:195`, `:230` (an executed historical plan). The successor named by
  the judgement, `migration_doc/audits/2026-09-02_ARCHITECTURE_REVIEW_PHASE1.md`,
  exists on disk (untracked), so the repoint has a target.

Conditions on removal:

1. Migrate D1 and D3 with the **corrected** text above — do not carry the
   "3 Scene files adopt RenderCommand" figure or the "on the exemption list"
   claim forward in any form.
2. Migrate A2a-2 with the corrected severity: name the sampler cache as the live
   item and hand the dead BGL cache to THREE-A06 rather than restating it as a
   hot path.
3. Migrate A2a-6, A2b-3, R4 and R7 with their stale-green supporting rows
   dropped or re-dated, so the new review does not inherit four repaired defects
   as open evidence.
4. Carry the `2026-06-11_ULTRA_REVIEW.md:336` correction with item A2c-2's
   disposition (the "globe terrain hot paths deduplicated" claim was refuted; it
   must not be migrated), and change that instruction to say the annotation
   target was removed rather than sending a reader to a deleted line.
5. Annotate `H-R9` FIXED (Batch 213, `af49776ac3`;
   `WebGPUGPUCuller.ts:468-495`) in
   `PRINCIPAL_ENGINEER_REVIEW_RENDERER_DEEP_2026_04_16.md:360` — verified this
   session as still un-annotated, and this document is the only other place the
   resolution is recorded.
