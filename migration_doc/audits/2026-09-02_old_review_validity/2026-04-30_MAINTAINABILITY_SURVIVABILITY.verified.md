# Verification — `audits/2026-04-30_MAINTAINABILITY_SURVIVABILITY.md`

**Verifier:** Tarannon · **Date:** 2026-09-03 · **Tree:** `532463ae35` (main)

**Input judged:** `2026-04-30_MAINTAINABILITY_SURVIVABILITY.judgement.json` (54 items; 21 STILL-VALID, 11 PARTIAL, 11 RESOLVED, 7 SUPERSEDED, 4 STALE)

| Count | |
| --- | --- |
| Still-valid / partial items verified | 32 |
| CONFIRMED | 25 |
| CORRECTED | 7 |
| REFUTED | 0 |
| UNADJUDICATED | 0 |
| Spot-checks of RESOLVED / SUPERSEDED / STALE | 6 |
| Reversals found | 0 |

Every citation below was read at HEAD this session. The prior auditor's line numbers were re-derived against a tree one batch behind and held up almost everywhere; the seven corrections are census counts and ownership claims, not premise failures.

---

## 1. Verified still-valid and partial items

`Row` = the queue/ledger entry that already owns the finding, verified present this session.

| id | title | verdict | current file:line | owning row | migrate text (corrections marked) |
| --- | --- | --- | --- | --- | --- |
| R3 | docstring-vs-WGSL drift check | CONFIRMED | duplicate of N11 | none | See N11. |
| R4 | public underscore-prefixed field convention | CONFIRMED | `WebGPUContext.ts:488-491`, `:1098`, `:3691` | none (documented convention) | As written. `public _canvas` (:488), `public _adapter` / `public _device` (:490-491); the convention is named in comments at `:1098` and again at `:3691`. Deliberate, not leaked encapsulation — state it once so readers do not read it as a defect. |
| R5 | ad-hoc diagnostic latches, no shared throttle | CONFIRMED | `WebGPUSceneRenderer.ts:1079` / `:1081` / `:1088`, used at `:1707`, `:1762`, `:2292` | none | As written. Nine distinct `_…Logged` fields across `WebGPUSceneRenderer.ts` and `WebGPUGlobeSurfaceRenderer.ts` (`_debugLogged`, `_depthOverlayWarningLogged`, `_diagFogMissingLogged`, `_execDebugLogged`, `_globeCountLogged`, `_globePassRPLogged`, `_postInitDebugLogged`, `_ppDebugLogged`, `_renderPassRedirectLogged`); zero `LogThrottle` / `DiagnosticThrottle` helper anywhere under `packages/engine/Source/Renderer`. Pragma-stripped, so readability only. |
| F1 | device-loss recovery real; per-object cache walk incomplete | CONFIRMED | `WebGPUDeviceLossRecovery.ts:27-31` (state enum), `:217`, `:240` (3 attempts), 744 lines; `WebGPUSceneRendererEnsureResources.ts:233` (call), `:583-604` (walk), `:652-653` (clear); `WebGPULabelRenderer.js:819-820`, `:1348` | `C11-20` (`QUEUE_2026-07-18_CAMPAIGN11.md:1639`, `:2452` — PARTIAL, Batch 778) / `C-R12-PER-OBJECT-CACHES` (`DEFERRED_WORK.md:8360`) / FAR-208 | As written. The walk's own docstring (`:583-595`) scopes it to owners exposing a literal `_webgpuCache`; `LabelCollection._webgpuLabelCache` is a differently-named field the walk cannot see, cleared only by the renderer's own teardown at `WebGPULabelRenderer.js:1348`. |
| F2 | no OOM error scope, no memory-pressure hook | **CORRECTED** | `pushErrorScope` sites: `WebGPUGlobeSurfaceShaders.ts` x2, `WebGPUModelPipelineCache.ts` x4, `WebGPUPostProcessPipeline.ts`, `WebGPUPrimitiveIndexUtils.ts`, `WebGPURenderBundleManager.ts:402`, `WebGPUSceneRenderer.ts:2271` | none | **Count corrected: ten real scopes, not twelve** — the judgement's grep counted two comment lines (`WebGPUEnvironmentRenderer.js`, `WebGPUGlobeSurfaceShaders.ts`) as call sites. Substance unchanged and confirmed: all ten are `"validation"`, zero `out-of-memory` scopes exist under `Renderer/WebGPU`, and zero `uncapturederror` / `onuncapturederror` listeners exist anywhere under `packages/engine/Source/Renderer`. A failed allocation is therefore only observed once it escalates to device loss, and an application has no hook to degrade LOD or drop effects before that. No row funds either. |
| F3 | shader compile failure logged, no general fallback | CONFIRMED | `WebGPUContext.ts:3500-3505` (`getCompilationInfo` → `compilation ERROR at line …`); `WebGPUModelPipelineCache.ts:3536`, `:3557`, `:3582-3594`, `:3699`, `:3771` (`_getOrCreateErrorPipeline`, `Shaders/WebGPU/Model/ErrorPipeline.wgsl`) | Batch 388 (model path only); residual unowned | As written. Compile errors carry the context id permanently; the magenta substitute exists for model PBR only. Every other renderer logs and drops the draw. A forced-failure hook exists for testing the model path (`globalThis.CesiumWebGPUForcePipelineError`, `WebGPUModelPipelineCache.ts:2791`) — the new review should note it, since it is the negative control any generalisation would reuse. |
| F4 | pipeline-create failure: scopes yes, substitutes almost never | CONFIRMED | ten scopes as under F2; substitutes at `WebGPUPostProcessPipeline.ts` (f16→f32 `fallbackWgslCode`) and `WebGPUModelPipelineCache.ts:3582` | `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1394` (fleet1 PLAUSIBLE, QUEUED — `WebGPURenderBundleManager.ts:402-419` leaks its scope when the record callback throws) | As written, with F2's corrected count of ten. |
| F5 | no `visibilitychange` handler | CONFIRMED | only `Scene/FrameRateMonitor.js:115-121` in all of `packages/engine/Source`; zero under `Renderer/` | none | As written. Low severity — Chromium restores the swap chain silently and the device-loss host path covers reconfiguration. |
| F7 | post-init GPU validation is silent (one-shot scope) | CONFIRMED | `WebGPUSceneRenderer.ts:1087` (`_globeValidationDone`), `:2267-2286` (the single scope, popped once via `Promise.resolve()`) | none | As written. Confirmed alongside F2 that no `uncapturederror` listener exists, so a mid-session format or layout regression produces browser-console noise the engine never sees and no probe can assert. A permanent `uncapturederror` handler stamping the context id, plus a `frameState.debug`-gated per-pass scope, closes it cheaply. |
| F8 | SSR normals: warned and plumbed, but the producer is not demanded | CONFIRMED | warning `WebGPUSSREffect.ts:205-226` (`cache.warnedNoNormalGBuffer`), flag `:273`; feed `WebGPUSceneRendererEnvironmentalEffects.ts:195-201`; **producer gate `WebGPUSceneRenderer.ts:2720-2721`**; `Scene.js:1167` (`deferredLighting = false`), `:3778` | none for the residual; `FEATURE_INVENTORY.md:881` carries the stale wording | As written, with the gate line pinned: the depth-derived normal producer returns early unless `frameState.useDeferredLighting === true`, and `scene.deferredLighting` defaults to `false`. `WebGPUGBufferRenderer.ts:6-11` documents the compensating behaviour — the MRT topology is active on every non-pick frame, so pipelines that emit normal-roughness populate the attachment and only pipelines without such an output leave the clear sentinel. `scene.enableSSR` (`Scene.js:1213`, `:3319-3324`) raises no demand for the producer. Either SSR should demand it or `FEATURE_INVENTORY.md:881` should stop citing FEAT-GAP-01 and say `deferredLighting` is the switch. |
| F9 | the three permanent sentinels are a SHOULD with no general check | **CORRECTED** | `Tools/lint-debug-pragmas.mjs` (console pragmas only); `Tools/collection-sentinels-check.mjs:1-36`; `packages/engine/Specs/Renderer/WebGPU/WebGPUCollectionSentinelsSpec.js`; `package.json:159` | none for the general case | **Scope corrected: one renderer family _is_ mechanically covered.** The judgement says nothing enforces the sentinels. In fact `WebGPUCollectionRendererBase.ts`'s three sentinels (re-entry guard, null-target guard, size/overflow guard) are gated by `WebGPUCollectionSentinelsSpec.js` in the standard `gulp test` suite, with `Tools/collection-sentinels-check.mjs` (`npm run collection-sentinels-check`) as a fast no-GPU smoke duplicate that the file's own header says is **not** wired into CI. The finding survives with narrowed scope: coverage exists for the collections base class and nowhere else, so a new renderer among the 277 files under `Renderer/WebGPU` can still land with none of the three. A census — which renderer has which sentinel — remains the cheapest next step, and the collections spec is the template to copy. |
| E1 | multi-instance safety strong | CONFIRMED | `GraphicsContext.ts:749-751` (static `_registry: ContextRegistry`), `:21`, `:723`; `WebGPUDevicePool.ts:76-77`, `:285`, `:296-302`, `:459-520` (featureLevel + features + limits compat before sharing), `:602-607` (`releaseDevice`) | FAR-102 / FAR-103 (`FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:66` — "complete long-running multi-context/device-loss soak" remaining) | As written. |
| E2 | global-state pollution minimal | **CORRECTED** | `WebGPUSkyAtmosphereRenderer.js:990`, `:1009`, `:1237` (all inside a debug pragma opened at `:984`); `WebGPUGlobeFragmentDebug.ts:212-218` (pragma-wrapped); **`WebGPUGlobeSurfaceRenderer.ts:703-705` and `:709-711` (NOT pragma-wrapped)** | none | **Corrected: production global writes exist.** The judgement claims every `globalThis` write is debug-stripped. Two are not: `WebGPUGlobeSurfaceRenderer` unconditionally publishes `globalThis.__webgpuGlobeBindGroupCache = this._bindGroupCache` (`:703-705`) and `globalThis.__webgpuGlobeImagerySlotCount = this._imagerySlotCount` (`:709-711`), both outside any pragma block, on a documented last-initialized-wins convention for `CesiumDebug.globeBindGroups()` and `probe-globe-default-limits.mjs`. The cache handle is unpublished on destroy when it still points at this renderer (`:3050-3055`); the slot count is never unpublished. So a production embed does get two `__webgpu*` properties on `globalThis`, one of them a strong reference to a live cache, and in a split-screen or multi-context embed both name only the most recent renderer. The sky-atmosphere latches (three, all stripped) and the fragment-debug registry (`WebGPUGlobeFragmentDebug.ts:212-218`, stripped) are as described. Two more globals are _read_ but never written by the engine: `CesiumWebGPUForcePipelineError` (`WebGPUModelPipelineCache.ts:2791`) and `CesiumWebGPUMetadataDebug` (`WebGPUModelRenderer.ts:2377`). Still a mild finding, but "clean global namespace in production builds" is not accurate as stated. |
| P1-P3 | Chromium first-class; no Firefox/Safari run | CONFIRMED | `Tools/visual-regression/capture-and-diff.mjs:83` (default `msedge`), `:692-700`; `Tools/variant-smoke-test.mjs:82`, `:186`, `:655-658`; `scripts/karmaDefaultBrowsers.js:10-12` (ChromeDebugging / EdgeCompat / Chrome only) | `DEFERRED_WORK.md:4855` (`NEW-CI-COVERAGE-SUITE`, "unknown until next hosted run") | As written. Both Playwright tools accept `--browser firefox\|webkit`; nothing schedules or banks such a run, Karma resolves Chrome/Edge only, and the renderer carries no Safari/WebKit workaround and no UA sniffing. A weekly manual Firefox-release + Safari TP smoke leg still needs an owner row. |
| P4 | optional features stay opt-in; no UA sniffing | CONFIRMED | `WebGPUContext.ts:997`, `:1014`; `WebGPUDevicePool.ts:509-520` (`featureLevelMatches` / `features.has(f)` / `limitsAreCompatible`); zero `userAgent` hits under `Renderer/WebGPU` | none (posture unchanged) | As written — carry as a one-line strength. |
| X2-X3-X6 | Electron non-issues stay non-issues | CONFIRMED | zero case-insensitive `electron` hits under `packages/engine/Source/Renderer`; zero in `WEBGPU_DEBUGGING_LOG.md`; `WebGPUContext.ts:922` (generation counter), 31 files reference it | none | As written. |
| X4 | OffscreenCanvas-in-worker is unverified opt-in scaffolding | CONFIRMED | `packages/engine/Source/Renderer/OffscreenContextSupport.ts:1-13` ("Strategy C", `useOffscreenCanvas: true`, WebGPU-in-worker marked "Experimental"); `GraphicsContext.ts:586`; zero references under `Tools/` or `packages/engine/Specs/` | none | As written. Treat as unverified capability, not a shipped feature, until a worker probe lands. |
| X5 | replacement-device recovery has no banked real-browser attestation | CONFIRMED | `packages/engine/Specs/Renderer/WebGPU/WebGPUDeviceLossRecoverySpec.js` exists; `WebGPUDeviceLossRecovery.ts:240` (3 attempts) | `Q-135` (`FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:497` — "INSTRUMENT LANDED (Batch 1295); row stays OPEN until the S5 replacement-device probe attests on an Edge tranche") / FAR-208 / FAR-601 | As written. |
| C1 | TypeScript `any` ban holds in code | **CORRECTED** | `WebGPUShaderCache.ts:398`, `:400`; **`WebGPUIndirectDrawManager.ts:334`**; **`WebGPUViewportQuad.ts:353`** | a fleet1 PLAUSIBLE row in `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md` owns `WebGPUViewportQuad.ts:353`; the others unowned | **Count corrected: four real `any` uses, not two.** The judgement's grep missed `any` in generic and return positions. Confirmed: `(wrapped as any).wgslSource` / `.shaderName` (`WebGPUShaderCache.ts:398`, `:400`, DevTools annotation on a wrapped `GPUShaderModule`); `submitBatch(commands: ReadonlyArray<any>)` (`WebGPUIndirectDrawManager.ts:334`) — an untyped command array on the indirect-draw entry point, unowned by any row; `uniformMap: Record<string, () => any>` (`WebGPUViewportQuad.ts:353`) — already queued by fleet1, which notes the correct union `ViewportQuadUniformValue` is declared 226 lines above in the same file. The two apparent hits at `WebGPUSceneRenderer.ts:5064-5072` are a local variable named `any` used as an object-literal value, and `WebGPUVoxelRenderer.ts:993`/`:1114` are the WGSL builtin inside shader string literals. The judgement's note stands: the 34 remaining `.js` renderers (`NEW-TS-CONVERT-JS-RENDERERS`, `DEFERRED_WORK.md:4715`) are outside the ban's reach entirely — 34 `.js` against 237 `.ts` under `Renderer/WebGPU` today. |
| C2 | pragma-wrapped logging, now tool-enforced | CONFIRMED | 99 `console.log`/`console.warn` calls across 45 files; 187 debug-pragma blocks across 70 files; `Tools/lint-debug-pragmas.mjs` + `.spec.mjs`, `package.json:149`; permanent errors at `WebGPUSceneRenderer.ts:2275`, `WebGPUContext.ts:3503` | `Tools/lint-debug-pragmas.mjs` | As written; census re-counted and identical. |
| C4 | backend-agnostic Scene code | **CORRECTED** | 0 imports from `../Renderer/WebGPU/` in `Scene/`; `getFeatureRenderer(` in 43 Scene files; branches at `GlobeSurfaceShaderSet.js:1117`, `OceanSurfacePrimitive.js:384` **and `:502`**, `ViewportQuad.js:144` | census fact; `2026-06-11_ULTRA_REVIEW_findings.json:2989` cites this doc for the branch count | **Residual-branch count corrected: four `context.isWebGPU` control-flow branches, not three, and a second family the judgement does not count at all.** The import boundary (0) and the feature-renderer census (43 files) are confirmed exactly. Missed branch: `OceanSurfacePrimitive.js:502` (`defined(this._lastContext) && this._lastContext.isWebGPU`). Separately, Scene code branches on backend _command shape_ in eight more places — `Scene.js:3594`, `:3599`, `:3697`, `:6109`, `View.js:663`, `WebGLShaderProgramScheduler.js:49` reading `command.isWebGPUDrawCommand`, produced at `GlobeSurfaceTileProviderRendering.js:1459` and `:1569`. That is a different axis from `context.isWebGPU` and arguably legitimate command dispatch, but a "remaining branch points" list that omits it is misleading. Non-branches, correctly excluded: the public getter `Scene.js:2793-2794`, the debug label `SceneDebug.js:191`, and seven `isWebGPU` guards in `CesiumDebug.js` (`:184`, `:502`, `:594`, `:866`, `:956`, `:1075`, `:1137`), which is a debug tool that lives in `Scene/`. |
| T5 | per-object cache walk (top-10 #5) | CONFIRMED | duplicate of F1 | `C11-20` / `C-R12-PER-OBJECT-CACHES` | Migrate F1's text. |
| T7 | no general pipeline-failure fallback (top-10 #7) | CONFIRMED | duplicate of F3/F4 | Batch 388 | Migrate F4's text. |
| T8 | no Firefox/Safari CI (top-10 #8) | CONFIRMED | duplicate of P1-P3 | `DEFERRED_WORK.md:4855` | Migrate P1-P3's text. |
| T9 | per-instance log latches (top-10 #9) | CONFIRMED | duplicate of R5 | none | Migrate R5's text. |
| T10 | `_scenePipelineFormatGeneration` contract is manual | **CORRECTED** | `WebGPUContext.ts:922` (field), `:942` (getter); 31 files under `packages/engine/Source` reference it; zero `recompileForSceneFormat` / `FormatAwareRenderer` symbols | **`NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION` (`QUEUE_2026-07-15_CAMPAIGN9.md:124`, NOT STARTED)** | **Ownership corrected: the judgement says "no queue row; FAR Phase 6 umbrella only". A row exists, and it records the predicted failure actually happening.** `NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION` states in terms: "Per-renderer `_scenePipelineFormatGeneration` guards rebuild one frame late relative to the scene-pass swap", producing "Attachment state … not compatible" plus an invalid command buffer on runtime `scene.msaaSamples` flips, measured at 50 occurrences on a 2026-07-16 baseline and reproducible via `probe-hdr-pick-format-closure.mjs`. That upgrades this from a theoretical contract risk to an observed defect with a repro — and it compounds with F7, because the resulting validation error is not caught after the first frame. The structural half of the finding stands unchanged: 31 ad-hoc readers, no interface, nothing at compile or lint time saying which renderers are format-aware. |
| N4 | magenta error pipeline (near-term #4) | CONFIRMED | duplicate of F3/F4 | Batch 388 | Migrate F4's text. |
| N5 | `LogThrottle` helper (near-term #5) | CONFIRMED | duplicate of R5 | none | Migrate R5's text. |
| N6 | debug-gated per-pass validation scope (near-term #6) | CONFIRMED | duplicate of F7 | none | Migrate F7's text. |
| N7 | Firefox + Safari smoke run (near-term #7) | CONFIRMED | duplicate of P1-P3 | `DEFERRED_WORK.md:4855` | Migrate P1-P3's text. |
| N8 | declare `recompileForSceneFormat` on an interface | **CORRECTED** | duplicate of T10 | `NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION` | Migrate T10's corrected text — the proposal is unbuilt (zero symbol hits) but the problem it solves is a recorded, reproduced row, not an unowned theoretical risk. |
| N11 | CI docstring-vs-WGSL drift check | CONFIRMED | no such check in `Tools/`, `scripts/`, `.husky/` or `package.json`; `Shaders/WebGPU/Compute/VolumetricFog.wgsl:1` still reads "Phase 5b real kernels", `PostProcess/VolumetricFogComposite.wgsl:1` still reads "Phase 5a infrastructure pass" | none (C16 owns comment vocabulary, not semantic drift) | As written. `Tools/lint-debug-pragmas.mjs` polices console pragmas and the C16-00 marker guard polices banned vocabulary; nothing compares a `*.ts` module header against the `*.wgsl` it drives. Because the C16 sweep will eventually delete the phase wording a grep would key on, a rule that every renderer module header names its shader as ground truth beats a phrase-grep. Low severity; belongs with the C16 tooling if funded at all. |

---

## 2. Spot-checks of the RESOLVED / SUPERSEDED / STALE items

Selected for cost-if-wrong: three correctness/parity claims, one claim that inverts a rule CLAUDE.md still states as CRITICAL, and one supersession pointing at rows in another document.

### SC-1 — E3 / T6 "global default renderer premise no longer holds" (STALE) — UPHELD

The judgement claims nothing calls `setGlobalDefaultRenderer` any more. This contradicts CLAUDE.md's "Runtime default renderer" and "Side-effects declaration (CRITICAL)" paragraphs, which say the variant entry barrels call it at module init. **The judgement is right and CLAUDE.md is stale.** The only definition is `RendererType.ts:269`; the only other mentions in `packages/engine/Source`, `packages/widgets/Source`, `scripts/` and the generated barrels are re-exports (`scripts/build.js:505`, `:1566`; `Source/Cesium.js:1848`; `Source/CesiumWebGLOnly.js:1487`; `Source/CesiumWebGPUOnly.js:1515`). The singleton still exists (`RendererType.ts:263`, setter `:269`, getter `:277`) but is inert by construction: variant defaults come from build-aliased `rendererBuildCapabilities` consumed by `getDefaultRendererType(...)` (`RendererType.ts:25`, `:129`, `:229`, `:289-291`).

Stronger than the judgement knew: the absence is **mechanically enforced**. `scripts/__tests__/bundleVariantPlugin.spec.mjs:310-326` asserts that `scripts/build.js` does _not_ match `/setGlobalDefaultRenderer\(RendererType\.\$\{defaultRenderer\}\)/`, with the message "build generator must not append a mutable renderer-default setter", and requires the phrase "immutable RendererBuildCapabilities alias" to be present instead. No reversal.

### SC-2 — E4 / N10 "variant barrels + `sideEffects` rationale" (STALE) — UPHELD

Verified `package.json:47-48` still lists `./Source/Cesium*.js` under `sideEffects` while the generated barrels contain no bootstrap to protect. The owning row is real: `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1380` (fleet1, CONFIRMED, QUEUED) — "package.json sideEffects './Source/Cesium\*.js' rationale (protecting a setGlobalDefaultRenderer bootstrap) no longer matches the generated barrels". No reversal.

**Carry-forward for the new review (not a finding of this document):** CLAUDE.md itself still instructs future sessions that the barrels call `setGlobalDefaultRenderer()` and that the `sideEffects` line exists to protect that call. That is now false in both halves. Whoever executes the R-2026-09-02 CLAUDE.md pointer edits should fix those two paragraphs in the same pass as the inbound-ref repoint below.

### SC-3 — F6 / X1 "explicit `renderer: 'webgpu'` now falls back cleanly" (RESOLVED) — UPHELD

`ContextFactory.ts:232-300` iterates the attempt plan, records a `RendererAttemptDiagnostic` per attempt with a `stage` tag, and on a failed explicit-WebGPU attempt emits a **permanent** `console.warn` naming the failing stage and message before falling back to WebGL. `RendererType.ts:190-201` builds the plan and honours `strictRenderer !== true && buildCapabilities.webgl`; `strictRenderer` is declared at `:79`. Adapter failures are wrapped as `RendererInitializationError("adapter", …)` in `WebGPUDevicePool.ts:791-800`, which is exactly the distinguishable-stage signal X1 asked for. No reversal.

### SC-4 — P5 "`featureLevel: 'compatibility'` really downgrades" (RESOLVED) — UPHELD, and stronger than claimed

`WebGPUDevicePool.ts:766-775` sets `featureLevel = "compatibility"` on the `GPURequestAdapterOptions` before `navigator.gpu.requestAdapter(adapterOptions)` at `:794`, and `:784-790` refuses to satisfy a compatibility request from a prefetched core adapter. Beyond the judgement's evidence, the device request is also negotiated against the resulting adapter — `_negotiate(adapter, opts)` at `:813-821` produces the `requiredFeatures` / `requiredLimits` actually passed to `adapter.requestDevice`, so a compat adapter's reduced capability set propagates to the device rather than throwing. Sharing is refused across feature levels at `:509-520`. No reversal.

### SC-5 — T3 "GroundPolyline silent-invisible fixed" (RESOLVED) — UPHELD

`WebGPUGroundPolylineRenderer.js:1-18` states the three invariants, including the actual root cause: viewport dimensions must come from `context.drawingBufferWidth/Height` because feature-renderer update runs before `uniformState.viewportCartesian4` is populated, so its zero `.zw` would make `metersPerPixel = 2.0 / (viewport.z * proj[0][0])` infinite, push the width extrusion to NaN clip space and silently cull every triangle. The live shader code consumes `metersPerPixel(...)` at `:505`, `:608`, `:989`, `:1007`, `:1140`, guarded by `max(0.0, …)`. The file is still JavaScript at 3,242 lines, as the judgement says. No reversal.

### SC-6 — R2 / C3 / T1 / T2 "file-size findings superseded by DX-07..DX-10 + fleet1 + FAR-602" (SUPERSEDED) — UPHELD

The cited rows exist and say what the judgement says they say: `QUEUE_2026-08-29_RESEARCH_DISPATCH.md:166-169` carries `DX-07` (decompose `WebGPUModelRenderer.ts`, QUEUED), `DX-08` (`WebGPUContext.ts`, QUEUED), `DX-09` (`WebGPUPrimitiveCommands.ts` + `WebGPUSceneRenderer.ts`, QUEUED) and `DX-10` (pipeline cache + the six remaining >1,000-line renderers, HELD), with detail sections at `:931-948` and a dispatch order at `:886`; `FIX_QUEUE_2026-08-27_AUDIT_FINDINGS.md:1384-1385` carries the two fleet1 file-size rows. The staleness claim is confirmed by re-census: today `WebGPUModelRenderer.ts` 9,284, `WebGPUContext.ts` 7,914, `WebGPUSceneRenderer.ts` 5,140, `WebGPUGlobeSurfaceRenderer.ts` 3,080, and **56 of 277** files under `Renderer/WebGPU` exceed 1,000 lines (the judgement said 57 of 276 — a one-file counting difference, immaterial). The 2026-04 numbers (4,363 / 3,933 / 18 files) are worthless as a baseline. No reversal.

---

## 3. Reversals

**None found.** All six spot-checks upheld the prior auditor's disposition, and two (SC-1, SC-4) are better supported than the judgement claimed. Nothing marked RESOLVED, SUPERSEDED or STALE turned out to be live.

The nearest thing to a reversal runs the other way: **T10 / N8 were marked STILL-VALID with "no queue row", and a row does exist** — `NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION` (`QUEUE_2026-07-15_CAMPAIGN9.md:124`), which records the contract's predicted failure as an observed, reproduced defect. That strengthens the finding rather than retiring it, and gives the migration a real owner to point at.

---

## 4. Document verdict

**REMOVE-AFTER-MIGRATION** — conditional on the corrected texts, not the judgement's originals, being the ones that migrate.

Reasons:

1. **Every item is adjudicated.** 32 of 32 still-valid/partial items were read at HEAD this session; 0 UNADJUDICATED, 0 REFUTED. Six of the twenty-two RESOLVED/SUPERSEDED/STALE items were spot-checked and all held.
2. **Nothing here is a unique live authority.** Every item with an owner points at a queue row that exists and is current (`C11-20`, `C-R12-PER-OBJECT-CACHES`, `Q-135`, FAR-102/103/208/601, `DEFERRED_WORK.md:4855`, `DX-07..DX-10`, the fleet1 rows at `FIX_QUEUE:1380`/`:1384`/`:1394`, `NEW-WEBGPU-SCENE-PASS-MSAA-FLIP-TRANSITION`). Every item without an owner is a census or an observation whose full content is reproduced in the migrate text above — the document holds no evidence that dies with it.
3. **All seven inbound refs can be repointed**, re-derived at today's lines: `CLAUDE.md:603` (glob `audits/2026-04-30_*.md`; drop "maintainability" from the trio description); `migration_doc/README.md:343` (the trio row — the judgement's line 228 has drifted); `ARCHITECTURE.md:3` (historical "folds in" list, needs nothing) and **`ARCHITECTURE.md:966-968`, the only live pointer that sends a reader here for content** — it must be repointed to the new review's survivability/embeddability section, which is therefore the section that has to receive the migrate text of F1, F2, F3/F4, F5, F7, F8, F9, E1, E2, X4, X5, P1-P3, T10 and R5; `ISSUES_AND_FIXED_BUGS.md:2` (history, needs nothing) and `:355` (repoint the survey to the new review and the A16.3 lineage to `DEFERRED_WORK.md:4715`); `WEBGPU_CONTEXT_DECOMPOSITION_PLAN.md:4` (provenance string only); `audits/2026-06-11_ULTRA_REVIEW_findings.json:2989` and `:3776` (immutable evidence strings in a superseded sidecar — optionally repoint `documentedIn` to the new review's C4 census). Nothing under `Tools/` references the file, and it defines no ids of its own.

**The gate on removal.** Seven of the thirty-two migrate texts in the judgement are wrong as written — F2's scope count, F9's "nothing enforces it", E2's "clean global namespace", C1's `any` count, C4's residual-branch count, and T10/N8's "no queue row". Migrating the judgement's text verbatim would carry all seven errors into the replacement review, where they would be harder to catch than they were here. Use this file's corrected column as the migration source.

**One item deserves elevation on the way in.** T10 as corrected is no longer a readability note: `_scenePipelineFormatGeneration` is a manual contract with 31 ad-hoc readers, a recorded reproduction of it failing on `scene.msaaSamples` flips, and — per F7 — no post-first-frame validation scope to catch the failure when it recurs. In the new review those three belong together as one finding, not scattered across a top-10 list, a failure-mode section and a near-term-fix list.
