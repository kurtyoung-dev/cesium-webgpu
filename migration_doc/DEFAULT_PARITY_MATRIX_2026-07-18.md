# DEFAULT PARITY MATRIX — WebGL vs WebGPU at a default-constructed Viewer

- **HEAD commit**: `aef553d592c4ec549e2f57561d09ce941a3a3f97` (Batch 698: C10-05-MODEL-TEXTURE-MIP-CHAIN)
- **Date**: 2026-07-18
- **Method**: STATIC SWEEP ONLY — merged from four read-only source/doc audits (angles: scene-options, containment-flags, pipeline-behavior, policy-record). Every row was verified in current source with the WHY traced to its containment/policy record. **A runtime-diff pass (Edge/Playwright, per-backend snapshots) is PENDING** — see §B.
- **Scope**: deliberate default/config/containment divergences between a default-constructed WebGL Viewer and a default-constructed WebGPU Viewer, plus tracked feature-gap behaviorals that fire at defaults. Unintentional parity *bugs* are WEBGPU_PARITY_REPORT territory and are excluded, except where a silent divergence was surfaced by the sweeps themselves.
- **Row count**: 22 divergences + a verified-parity appendix (§Parity) recorded so the runtime pass does not re-derive them.
- **Severity order**: visible-visual > behavioral > perf-only > inert-feature. "Ratified-or-silent" legend: **RATIFIED** = explicit maintainer ratification (QUEUE_2026-07-15_CAMPAIGN9.md §3.3, 2026-07-16) · **PLAN** = sanctioned via the accepted FAR plan / audit record · **DOCUMENTED** = in-code/audit-comment or DEFERRED_WORK row, no policy ratification · **SILENT** = no ledger/queue/DEFERRED_WORK row found at sweep time.
- All paths relative to `f:/Dev/GH/cesium-webgpu/packages/engine/Source/` unless prefixed `migration_doc/` or `Tools/`.

## Matrix

| # | Name | WebGL default | WebGPU default | Ratified-or-silent | Rationale (WHY) | Consequence | Anchors | Recommended action |
|---|------|---------------|----------------|--------------------|-----------------|-------------|---------|--------------------|
| 1 | **Weighted-blended OIT** (`scene.orderIndependentTranslucency`) — *the anchor case* | ON — option defaults true (`Scene.js:322 _useOIT = ?? true`); `View.js:85-90` allocates OIT; `FramebufferOrchestrator.js:84-96` activates it → McGuire-Bavoil weighted-blended MRT OIT for all translucent commands | OFF (contained) — request preserved (`environmentState.useOIT`, `WebGPUContext.ts:4098`; `_lastOITRequested`) but renderer-owned gate `WebGPUSceneRenderer._webgpuOITEnabled = false` (`WebGPUSceneRenderer.ts:878-884`) contains allocation (`WebGPUSceneRendererEnsureResources.ts:162-168`) and execution (`WebGPUSceneRendererTranslucentPass.ts:128-134`); fallback = complete back-to-front sorted alpha blend. Diagnostics: `getContainmentStats().webgpuOIT` → `fallbackReason: "contained-unsafe-path"` (`WebGPUSceneRenderer.ts:4753-4768`); toggle `CesiumDebug.webgpuOIT()` (`CesiumDebug.js:754-794`) | **RATIFIED** — FAR-003, maintainer 2026-07-16, `migration_doc/QUEUE_2026-07-15_CAMPAIGN9.md:163-167` §3.3 item 1; landed Batch 661 (`374f193f8a`) | Full containment story: Sol audit P0 #6a ratified the fail-closed default. Authoritative defect record = audit finding **B7** (`migration_doc/FORK_VS_UPSTREAM_WEBGPU_ARCHITECTURE_AUDIT_2026-07-13.md:167`, "OIT uses an invalid depth/stencil attachment contract"): (1) `depthSampleableView` published as `_depthStencilView` — at sampleCount 1 a depth-only-aspect view installed as depth-stencil attachment with stencil ops; under MSAA an r16float single-sample color RESOLVE view used as depth/stencil (format + sample-count violations, GPUValidationError risk); (2) accum/revealage textures single-sample vs MSAA pipelines; (3) mixed translucent sets LOSE draws — once any command has `_oitPipeline`, remaining non-OIT translucent commands return unrendered, no alpha fallback; (4) manual OIT draw loop omits parts of the command contract (dynamic offsets, viewport/scissor/stencil/blend constants, indirect draws, some draw offsets). Fix lane FAR-401/FAR-404; restoration row FAR-003/T7 (C9 Wave 6). NOT stale: `WebGPUOIT.ts` has had no defect-fixing commit since B7; two OIT latent findings remain OPEN — NEW-WEBGPU-OIT-MSAA-RESOLVE-ORDERING (`DEFERRED_WORK.md:5395`, later-frustum MSAA re-resolve stomps the mid-frame OIT composite) and NEW-WEBGPU-OIT-DEFERRED-SPLAT-CANVAS-RESUME (`:5305`); the ratification's own re-enable acceptance (before/after translucent-intersection visual probe) does NOT yet exist in `Tools/visual-regression` | Intersecting/interpenetrating translucent geometry composites order-dependently on WebGPU (sorted alpha popping/wrong blend) where WebGL is order-independent; plus per-frame CPU back-to-front sort cost. Non-overlapping translucency looks similar | Scene.js:322; View.js:85-90; FramebufferOrchestrator.js:84-96; WebGPUContext.ts:4098; WebGPUSceneRenderer.ts:878-884,1194-1200,4753-4768; WebGPUSceneRendererTranslucentPass.ts:122-197; WebGPUSceneRendererEnsureResources.ts:162-168,351-369; CesiumDebug.js:754-794; OIT.js:277-295 | **keep-ratified** — default-flip is a maintainer GO/NO-GO gated on the Batch-700 OIT evidence package (§C); static verdict today = NO-GO |
| 2 | **Enhanced ocean shading** (`globe.enableEnhancedOcean` + `ocean*` dials) | Nonexistent — documented "opt-in and only take effect in the WebGPU renderer… no effect on the WebGL path" (`Globe.js:352-356`); WebGL renders the classic GlobeFS water effect (specular + oceanNormalMap waves) under `showWaterEffect` (default true) | ON — `this.enableEnhancedOcean = true` (`Globe.js:382`): Fresnel, GGX specular, multi-octave wave normals, foam/whitecaps, subsurface scattering, deep-water color; active dials `oceanDeepColor/oceanFresnelPower/oceanReflectivity/oceanFoamThreshold/oceanDarkening` (`Globe.js:384-421`), consumed by WebGPUGlobeSurfaceRenderer/material | **SILENT** (fork enhancement, no containment/ratification ID) — the *reverse* direction of FAR-003 (WebGPU ON, WebGL not-possible) | Shipped default-on as part of the "Enhanced rendering configuration (WebGPU)" block; never went through a visual-policy ratification. Interacts with the OPEN user-bug lane water-bugs-2026-07-06 (ocean bright / no waves) | The largest default-pixels divergence over open water: WebGPU shows stylized-physical water (imagery darkened ×0.6, foam, Fresnel) vs WebGL's classic specular water — a default Viewer's oceans simply look different per backend | Globe.js:352-421,1124-1131; WebGPUGlobeSurfaceRenderer.ts (`_createWaterOceanMaterialBindGroupInner`) | **flip-candidate** (top of shortlist §A) — default to `false` for parity or obtain explicit ratification; resolve jointly with water-bugs-2026-07-06 |
| 3 | **Sun bloom** (`scene.sunBloom`, default true) | ACTIVE — `Scene.js:556 this.sunBloom = true`; `FramebufferOrchestrator.js:51-59` allocates SunPostProcess; `EnvironmentRenderer.js:43-57` runs the multi-pass downsample/brightpass/blur glare chain whenever the sun is visible; toggling the flag works | Legacy path OFF, flag INERT — `supportsLegacySunBloom` returns false (`WebGPUContext.ts:1648-1656`) so SunPostProcess is never allocated; `scene.sunBloom` is read nowhere under `Renderer/WebGPU` (grep: zero hits). Substitute = disc + glow halo + six lens-flare bursts BAKED into the procedural sun texture (`WebGPUEnvironmentRenderer.js:96-98,224-229,398`); the documented replacement (WebGPUPostProcessPipeline Bloom/LensFlare) defaults OFF | **DOCUMENTED** (AUDIT_2026_05_02 C.12 guard + capability-getter comment) — but NO parity-gap tracking row exists despite being a default-ON visual feature | The guard was added to stop WebGL FB/shader resource leaks on every WebGPU viewer; the compensating WebGPU bloom path was never made default-on, and the capability comment's claim that PP Bloom/LensFlare "handles" it is not true at defaults | Different sun appearance in any sun-in-frame view at defaults: WebGL screen-space bloom (spill beyond the sun quad, horizon partial-occlusion glow) vs WebGPU baked-texture glow; `scene.sunBloom=false` changes WebGL output but does nothing on WebGPU (silently inert control) | Scene.js:556; FramebufferOrchestrator.js:42-63,163-165; EnvironmentRenderer.js:43-57; GraphicsContext.ts:934-939; WebGPUContext.ts:1648-1656; WebGPUEnvironmentRenderer.js:96-115,204-300 | **needs-runtime-evidence** (quantify sun-in-frame delta) + file a parity-gap row; candidate fix = wire `scene.sunBloom` to the WebGPU PP Bloom/LensFlare stages |
| 4 | **Empty-scene canvas background color** | `scene.backgroundColor` clear executes — the background `_clearColorCommand` targets the default framebuffer and runs | WebGPU `_clearColor` is (0,0,0,0) from the constructor and is never fed `scene.backgroundColor`; the background clear command targeting the default framebuffer is swallowed — an empty WebGPU scene presents transparent black | **SILENT/ACCIDENTAL** — latent finding filed during C9-07; queued as NEW-WEBGPU-CANVAS-BACKGROUND-COLOR-PARITY (`migration_doc/DEFERRED_WORK.md` ~5300); no ratification | The C9-07 demand-open-canvas-pass slice deliberately preserved the bytes (empty-scene byte-identity gate) rather than fixing it; the fix (adopt `cmd.color` into the deferred first-open clear) needs its own WebGL-vs-WebGPU background probe because it is a visible behavior change | Visible on empty scenes / anywhere the canvas background shows through (`globe.show=false`, custom `backgroundColor`): WebGPU shows transparent black instead of the configured color | WebGPUContext constructor `_clearColor`; migration_doc/DEFERRED_WORK.md ~5300; C9-07 slice notes (WebGPUSceneRendererPassRedirect.ts:17-27) | **fix-silent-divergence** — already queued; land with a dedicated background-color probe |
| 5 | **Residual hyperbolic-depth producers** under the log-depth default | Log depth ON and UNIFORM — `Scene.defaultLogDepthBuffer=true && context.fragmentDepth` (`Scene.js:141,310`; `Context.js:1079`); GLSL derived commands apply LOG_DEPTH to every producer | Master switch ON (`WebGPUContext.ts:581 _logDepthWriteEnabled = true`, Batch 251; `fragmentDepth` true `:1615`) but coverage PARTIAL: Mat* primitives, Buffer* family, EllipsoidPrimitive, Vector3DTile, GroundPolyline depth-sample read, point-cloud/Gaussian-splat producers still write hyperbolic NDC depth (`WebGPUContext.ts:573-580` comment lists the tail) | **DOCUMENTED** WIP — NEW-LOG-DEPTH-REMAINING-PRODUCERS (`migration_doc/DEFERRED_WORK.md:1595`; epic NEW-WEBGPU-GLOBE-CLASSIFY-DEPTH-PRECISION, Batches 183-267) | Log depth was deliberately shipped producer-by-producer behind a one-line kill switch; the tail conversion simply has not landed. (The pick-fleet hyperbolic encoding is row 8's blocker.) | For the listed primitive families, far-range depth precision differs at defaults: z-fighting/depth-tie resolution behaves differently (WebGL sub-meter log precision vs hyperbolic quanta at range). Only visible in scenes using those primitives at long camera distances | WebGPUContext.ts:573-581,1615; WebGPULogDepth.ts:69-80; Scene.js:141,310; Context.js:1079; migration_doc/DEFERRED_WORK.md:1563,1595 | **needs-runtime-evidence** (far-range depth-fight probes per family) + continue the tracked conversion tail |
| 6 | **Synchronous pick family** (`pick` / `drillPick` / `pickPosition` / `sampleHeight` / `clampToHeight`) | Fully synchronous, current-frame — `supportsSynchronousReadback` true (`GraphicsContext.ts:977`); pick renders and reads back in the same call; per-frustum PickDepth unproject answers `pickPosition` on the FIRST call | Async-readback emulation — `supportsSynchronousReadback` false (`WebGPUContext.ts:1658-1668`). Sync `pick` returns the PREVIOUS readback (one frame stale; cold first pick at a new location can return undefined — P0-3 fix AUDIT-P0-SYNC-PICK-REGION-DRIFT restored the documented warm-up pattern, 2026-07-16); `drillPick` one-frame-stale + one-time "unreliable on WebGPU" warning (`Picking.js:876-895`, AUDIT B.6); `pickPosition` = one-frame-stale async cache over ONE packed full-frustum LOG-encoded depth texture (`Picking.js:759-789`, Batch 252; first query undefined → ray-pick fallback, converges 1-2 frames; in 2D/ortho ALWAYS undefined → permanent ray-pick fallback); `sampleHeight`/`clampToHeight` reconstruct from live-view depth, on-screen only, 1-2 frame convergence (`Scene.js:1762-1802`, `Picking.js:1078-1177`, Batch 284). Async variants (`pickAsync` etc.) are the sanctioned path | **DOCUMENTED** — platform constraint (no sync GPU→CPU readback); mitigations = Batches 184/192/221/252/284; honest-semantics work tracked as FAR-107; P0-3 row calls the interim stricter gate "an undisclosed semantics change" (reverted) | WebGPU has no synchronous pixel readback; the full-frustum log reconstruction replaced the per-slice loop that produced ~85,000 km antipode garbage (Batch 221). Batch 693 close-out notes the pickposition probe as a standing pre-existing red | API-visible at defaults: one-off click picks can return undefined/stale until repeated; drillPick unreliable (warned); height/clamp queries only work for visible geometry and lag a frame; 2D/CV pickPosition never uses the depth buffer. Continuous hover behaves equivalently | WebGPUContext.ts:1658-1668,1702-1714; Picking.js:654-789,876-929,1078-1177; Scene.js:1762-1802,4334-4345; GraphicsContext.ts:977 | **keep-ratified** (platform constraint) + **needs-runtime-evidence** for the exact first-call/warm-up semantics; keep FAR-107 honest-semantics lane open |
| 7 | **`scene.pickVoxel` throws on WebGPU** (C-R9-VOXEL-CELL-PICK-TAIL) | Functional (upstream path) | `pickVoxelCoordinate` decode is byte-identical parity (VOXEL-CELL-PICK-RELAND, probe-voxel-cell-pick.mjs), but public `scene.pickVoxel` THROWS after the decode — the WebGPU path never builds the required follow-on object | **DOCUMENTED** WIP residual (`migration_doc/FEATURE_INVENTORY.md:822`) | Tracked tail conversion, not a containment — simply unlanded | Apps calling `scene.pickVoxel` work on WebGL and throw on WebGPU at defaults whenever voxels are present | migration_doc/FEATURE_INVENTORY.md:822; Picking.js (pickVoxelCoordinate path); Scene.js:4427-4440 (OBB ray-test footprint caveat) | **fix-silent-divergence** (hard failure at defaults — land the tail; already tracked) |
| 8 | **Depth plane omitted from the PICK pass** | Rendered — pick frames draw the depth plane (`SceneRenderer.js:480,498`; `environmentState.useDepthPlane` honored), so the globe disk occludes picks beyond the horizon | OMITTED — module-level `PICK_DEPTH_PLANE_ENABLED = false` (`WebGPUSceneRendererPickPass.ts:69`); the guarded `_renderDepthPlane(config, "pick")` at `:496-497` never runs | **DOCUMENTED** containment — Sol audit P0-1 (horizon oracle failed at 20/500/5000 km); re-enable blocked on NEW-WEBGPU-PICK-FLEET-LOG-DEPTH (`migration_doc/DEFERRED_WORK.md:5241`); C9-02A/B queued | The gated draw used a mismatched log-depth encode frustum and OVER-occluded point picks; the whole pick fleet still writes hyperbolic frag_depth, and hyperbolic depth cannot discriminate the plane from a just-beyond-horizon marker at 5,000 km (sub-ulp). Under-occlusion was chosen as the lesser evil over over-occlusion. Batch 673 landed the scene-side half of the log-depth contract | WebGPU `scene.pick` can return entities just beyond the horizon that WebGL correctly reports as globe-occluded (see-through-globe picks) | WebGPUSceneRendererPickPass.ts:52-69,496-497; WebGPUDepthPlane.ts:171; SceneRenderer.js:480,498; migration_doc/DEFERRED_WORK.md:5241 | **keep-ratified** (deliberate lesser-evil) — unblock via the pick-fleet log-depth conversion, then flip the gate with the horizon oracle |
| 9 | **PointPrimitiveCollection `blendOption` command classification** | `blendOption` (default OPAQUE_AND_TRANSLUCENT) correctly splits commands into opaque/translucent cohorts with matching Pass labels | The native WebGPU PointPrimitive branch returns before synchronizing collection `_blendOption`; even `BlendOption.OPAQUE` can emit commands labeled `Pass.TRANSLUCENT` | **DOCUMENTED** open bug row — NEW-WEBGPU-POINT-BLENDOPTION-SYNC (`migration_doc/QUEUE_2026-07-15_CAMPAIGN9.md`, NOT STARTED 2026-07-16) | Open defect, not a policy gate; repair requires exact opaque/translucent/both classification, batching, order, depth, pick, 2D/CV/morph parity | Behavioral at defaults for point-primitive scenes: commands ride the translucent pass (sorted alpha, no depth write) where WebGL runs them opaque — depth interaction and draw-order differences | WebGPUPointPrimitiveRenderer.js; migration_doc/QUEUE_2026-07-15_CAMPAIGN9.md row | **fix-silent-divergence** (already queued — schedule; fires at defaults) |
| 10 | **Renderer selection: sync constructor + non-strict fallback** (meta-divergence) | A synchronous `new Viewer()`/`new CesiumWidget()` ALWAYS resolves WebGL — `getSynchronousRendererType` returns WEBGL only (`RendererType.ts:222-233`; `Scene.js:213`) | WebGPU requires the async factories (`Viewer.createAsync` etc.) or explicit contextOptions; dual-build entry barrel sets global default to WEBGPU for AUTO (`RendererType.ts:263`). With `strictRenderer` default false (`RendererType.ts:79`; `GraphicsContext.ts:511-516`), an explicitly-requested `'webgpu'` that fails init silently falls back to WebGL with only a console.warn (`ContextFactory.ts:242-265`); invalid renderer string throws only in debug builds | **RATIFIED** — §3.3 item 2 (2026-07-16): the Sol tranche's hard-fail was REVERTED to charter graceful-fallback-with-warn; strictness preserved behind opt-in `strictRenderer`. FAR-102/FAR-103 govern normalized selection + transactional async init | Charter policy: graceful degradation by default. Original B1 concern (tests believing WebGPU is active while exercising WebGL) is mitigated by requested-vs-active diagnostics | Determines WHICH backend a "default" Viewer gets at all: legacy sync construction is WebGL; an app asking for WebGPU can silently run WebGL after fallback — every other row in this matrix then inverts. Runtime lanes MUST assert `context.rendererType` | RendererType.ts:79,190-201,222-233,263-271; ContextFactory.ts:242-265; GraphicsContext.ts:500-563; Scene.js:213; migration_doc/QUEUE_2026-07-15_CAMPAIGN9.md:168 | **keep-ratified** — runtime pass must verify per-lane backend identity to guard every other row |
| 11 | **`scene.debugSkipDepthPlane` diagnostic gate ignored** | Honored — Scene.updateEnvironment respects it when computing `useDepthPlane` | Ignored — `WebGPUContext.ts:4079-4082` recomputes `environmentState.useDepthPlane` later WITHOUT the debug gate, overwriting the Scene-level decision | **DOCUMENTED** open parity row — NEW-WEBGPU-DEBUG-DEPTH-PLANE-GATE-PARITY (NOT STARTED 2026-07-16) | Queued; explicitly scoped as not affecting the default production path | Debug-only: the depth-plane skip toggle works on WebGL, not WebGPU. Default rendering identical (both draw the depth plane in SCENE3D) | WebGPUContext.ts:4079-4082; migration_doc/QUEUE_2026-07-15_CAMPAIGN9.md row | **fix-silent-divergence** (low priority, already queued) |
| 12 | **Post-process chain always active** (`environmentState.usePostProcess`) | Demand-computed — `usePostProcess` only when HDR/stages/AO/FXAA/bloom active (`FramebufferOrchestrator.js:103-115`); with everything default-off WebGL renders direct to canvas, no PP chain | FORCED ON every non-pick frame — `requiresSceneRenderer` overridden true (`WebGPUContext.ts:1627-1634`; base false `GraphicsContext.ts:1223`); `environmentState.usePostProcess = !picking` (`WebGPUContext.ts:4108`); missing PP pipeline is a hard error (`WebGPUSceneRenderer.ts:3192-3202`). Tonemap stays SDR-passthrough at defaults | **PLAN/architecture-sanctioned** (AUDIT 2026-05-02 routed it through the `requiresSceneRenderer` capability getter; CLAUDE.md: "usePostProcess must always be true for WebGPU"). Not FAR-003 | WebGPU cannot render directly to the canvas through the multi-frustum executor; the PP blit is the ONLY canvas-composite path. C9-07 (demand-open canvas pass) and C10-03 (resolve elision) shape its cost without changing the policy | One extra fullscreen pass + offscreen FB every WebGPU frame at defaults; pixels intended-identical (passthrough) | FramebufferOrchestrator.js:98-139; GraphicsContext.ts:1223; WebGPUContext.ts:1627-1634,4101-4109; WebGPUSceneRenderer.ts:3192-3202; Scene.js:369-381 | **keep-ratified** (backend contract) |
| 13 | **Always-MRT scene framebuffer topology** (`forceSceneMRT`) | Single color attachment (+ globe-depth FBO); no G-buffer unless a feature demands it (`FramebufferOrchestrator.js:141-161` behind `useDeferredLighting=false`) | `forceSceneMRT = true` (`WebGPUContext.ts:511`) — full-MRT topology (incl. rgba16float G-buffer slot, ~16MB@1080p + MSAA companion) opened every frame regardless of demand; C9-09 demand registry currently OBSERVE-ONLY (`WebGPUContext.ts:4115-4140`; `WebGPUAttachmentDemandRegistry.ts:18-25`); `CesiumDebug.attachmentDemand(false)` REFUSED (Batch 684 — live flip proven unsafe pending the 31-renderer topology-keyed pipeline-cache audit) | **DOCUMENTED** — FAR-401-C0 / C9-09 conservative default; the acting slice C9-10 (demand-driven gating) is the named pending Campaign-9 Wave-2 item (rule 3: unknown demand keeps MRT) | Deliberate observe-before-act sequencing; flipping early risks pipeline-cache/topology mismatches across 31 renderers | Extra attachment memory + bandwidth per WebGPU frame at defaults; no pixel difference. Cannot be disabled mid-session by design | WebGPUContext.ts:511,4115-4140; WebGPUAttachmentDemandRegistry.ts:18-25,136-165,223-243; CesiumDebug.js:543-601 | **keep-ratified** — re-audit after C9-10 lands (this row changes) |
| 14 | **AutoExposure compute always-on (even SDR)** | OFF — auto-exposure participates only under HDR tonemapping; `highDynamicRange` defaults false → no exposure work | ON — `addAutoExposure` called unconditionally at pipeline init (`WebGPUSceneRendererEnsureResources.ts:492-508`); `WebGPUAutoExposure.ts:132 enabled = true`, altitude gate default on (`:143`, orbit floor 0.75); 2 compute passes + readback ring per frame (`WebGPUPostProcessPipeline.ts:1486-1528`). Tonemap consumer disabled at SDR defaults → result unconsumed | **SILENT** (in-code AUDIT_2026_05_02 B.14 comment only — "SDR day/night moon transitions need adaptive exposure"; predates the containment-policy era; no FAR/queue row) | Closest thing in the inventory to a silent default-on WebGPU-only behavior; the B.14 justification concerns transitions the SDR default path cannot even express while tonemap is off | At defaults: per-frame compute + readback with no visual effect. Once HDR/tonemap is enabled, WebGPU exposure adapts with an altitude gate WebGL does not have — a visual policy difference in non-default HDR mode | WebGPUSceneRendererEnsureResources.ts:492-508; WebGPUAutoExposure.ts:132,143; WebGPUPostProcessPipeline.ts:1486-1528; WebGPUPostProcessStageCollection.ts:531-536 | **fix-silent-divergence** — file a tracking row; demand-gate the dispatch when no consumer is enabled; ratify the HDR altitude-gate behavior |
| 15 | **Demand-driven frame-graph shaping** (C9-07 demand-open canvas pass + C10-03 MSAA resolve elision) | N/A mechanisms — canvas is the default FB (always "open"); MSAA resolves via blitFramebuffer at consumption points | Canvas render pass opens only on first demand (untouched-canvas frames get an endFrame clear/present fallback); `_sceneColorResolveElisionEnabled = true` (`WebGPUContext.ts:562`) replaces eager per-segment resolves with resolve-on-consume (scene-COLOR 9→1/frame on the default globe); `false` is the documented kill switch / identical-build A/B oracle | **DOCUMENTED/PLAN** — FAR-405 (C9-07 landed 2026-07-16, ledger `:436-446`; C10-03 landed 2026-07-18, `DEFERRED_WORK.md:5414-5422`); claim boundaries explicit. NOTE: the adjacent default `msaaSamples` 4→1 flip (option c) is **explicitly NOT RATIFIED** (reserve lever C10-03R: requires a C10-30 checkpoint miss + bandwidth-attributed evidence + fresh maintainer sign-off) | Default-path performance recovery slices; both documented to differ only in pass-open/resolve TIMING, not results | Fewer empty passes + resolves per WebGPU frame; no intended visual delta. Interacts with row 4 (byte-identity gate preserved the background-color bug) and OIT latent finding NEW-WEBGPU-OIT-MSAA-RESOLVE-ORDERING | WebGPUContext.ts:543-562; WebGPUSceneRenderer.ts:1928,2013,2085; WebGPUSceneRendererPassRedirect.ts:17-27,173; migration_doc/DEFERRED_WORK.md:5414-5422; migration_doc/QUEUE_2026-07-16_CAMPAIGN10.md:72-81 | **keep-ratified** — runtime pass should A/B the elision kill switch once as a byte-identity check |
| 16 | **Vertex typed-array retention** (loader memory) | Loaders release CPU-side vertex typed arrays after GPU upload | `requiresVertexTypedArrayRetention = true` (`WebGPUContext.ts:1636-1646`; base false `GraphicsContext.ts:929`) — GltfLoader/EdgeVisibility/b3dm consumers keep typed-array caches (no `GPUBuffer.getBufferData()` back-channel) | **DOCUMENTED** (AUDIT 2026-05-02 — capability getter replaced scattered `isWebGPU` checks) | Platform constraint | Higher steady-state CPU memory per model on WebGPU at defaults; no visual difference | WebGPUContext.ts:1636-1646; GraphicsContext.ts:929 | **keep-ratified** |
| 17 | **Night-side city lights** (`globe.enableNightLights` / `nightIntensity`) | Nonexistent — property inert on WebGL (`Globe.js:352-356`) | `enableNightLights = true`, `nightIntensity = 2.5` (`Globe.js:358-373`) — armed but gated on `globe.enableLighting` (default false), so inert at pure defaults. The moment a user enables the standard upstream `enableLighting` flag, WebGPU additionally boosts night-side imagery (nightAlpha > dayAlpha emissive city lights); WebGL does not | **SILENT** (fork enhancement, WebGPU-only, shipped default-armed; no containment ID) | Same unratified-enhancement family as row 2 | None at strict defaults; a one-flag-away visible divergence (night-hemisphere brightness) via a common user setting | Globe.js:352-373 | **flip-candidate** (§A) — default-off for parity, or ratify as a deliberate WebGPU enhancement default |
| 18 | **`useWebVR` stereo rendering** | Available — per-eye viewport split executes when enabled (default false) | Fail-loud unsupported — `supportsStereoViewport` false (`WebGPUContext.ts:1716-1726`); `Scene.useWebVR` setter THROWS DeveloperError (`Scene.js:2695-2720`) | **DOCUMENTED** (AUDIT_2026_05_02 C.11 — fail loudly rather than silently render single-eye masquerading as stereo) | WebGPU scene renderer hard-codes full-canvas viewport and ignores `passState.viewport`; per-eye plumb not landed | None at defaults (false on both); on opt-in WebGPU throws where WebGL renders stereo — deliberate loud-fail | Scene.js:2695-2720; GraphicsContext.ts:955; WebGPUContext.ts:1716-1726 | **keep-ratified** (loud-fail is the correct interim contract) |
| 19 | **`usePostProcessSelected` hardwired false** | Active when applicable — `usePostProcessSelected = usePostProcess && postProcess.hasSelected` (`FramebufferOrchestrator.js:137-138`); selected-feature stages (e.g. silhouette-on-selected) composite via the selected path | Hardwired OFF — `environmentState.usePostProcessSelected = false` every frame (`WebGPUContext.ts:4109`) | **SILENT** — no containment ID, queue row, or DEFERRED_WORK entry found (likely an unfinished port) | No recorded rationale — the definition of a silent divergence | Post-process stages operating on the selected-feature subset never take the selected path on WebGPU. Invisible at defaults (no selected stages), wrong the moment a user adds one | WebGPUContext.ts:4109; FramebufferOrchestrator.js:137-138; SceneRenderer.js:716 | **fix-silent-divergence** — file a tracking row + port or fail loudly |
| 20 | **GPU culling / Hi-Z consume / GPU sort-keys / tile-indirect** (contained WebGPU compute family) | Nonexistent — CPU frustum cull + CPU comparator sort are the only path | Present but fail-closed: Scene publishes `gpuCullingHint = "never"` before first frame (`Scene.js:467-474`); `_gpuCullingRequestedMode = "never"` (`WebGPUSceneRenderer.ts:1082`), `_hiZConsumeEnabled = false` (`:1132`), `_gpuSortConsumeMode = "never"` (`:1309`), `_tileIndirectStatus.requestedMode = "never"` (`:942-948`), `useIndirectDrawForTiles = false` (`WebGPUContext.ts:502`); translucent cull requires explicit "always". Requested-vs-active via `getContainmentStats()` (`:4682-4834`) + `CesiumDebug.highDensityCull()`; 7 focused unsafe-default Jasmine cases | **RATIFIED/PLAN** — FAR-003 family (ledger row; plan `:339-346`; audit findings B3 encoder-locking + map-before-submit, B4 global-HiZ-vs-per-frustum identity, B5 shared indirect-buffer offset stomp, B6 SOA buffer reuse across frustums, H12, M1). Boundary statement: "containment is a correctness measure and is not counted as saved work" | Current pipelines map GPU results back to CPU command arrays and are not safe auto defaults. Staleness check: Hi-Z's ORIGINAL depth-source bug was later fixed but containment now rests on the still-open B4 cross-frustum hazard (ROADMAP row self-consistent); GPU sort capability proven under forced 6,400-command characterization (exact CPU-comparator match) but normal-frame readback/identity hazards remain. Restoration = FAR-003-SAFE-AUTO-RESTORATION T7 (C9 Wave 6) | None at defaults — containment deliberately EQUALIZES behavior with WebGL (CPU cull/sort on both). Perf headroom forgone until T7. RenderScheduler bin/sort is contained off on BOTH backends (H12) — not a divergence; Sol rec #116 asks for per-flip containment-ownership ledger rows (tile-indirect + RenderScheduler not yet named in the ledger's FAR-003 row) | Scene.js:466-474,2926-2935; WebGPUSceneRenderer.ts:942-949,1082,1132,1305-1309,4682-4834; WebGPUContext.ts:502,5104; WebGPUContextCullerPool.ts:28,109,166; WebGPUSceneRenderer3DTilePasses.ts:255-277; RenderScheduler.js:31; migration_doc/FORK_ARCHITECTURE_REMEDIATION_PLAN_2026-07-13.md:339-346 | **keep-ratified** — close the ledger-row naming gap (Sol #116); restoration via T7 only |
| 21 | **WebGPU-only opt-in flags silently inert on WebGL** (taaEnabled + its MSAA-forces-1 coupling, motionBlur, aerialPerspective(+froxel), godRay, heatShimmer, coldOptics, useCascadedShadowMaps, ssr, nprOutlines, contactShadows, clustered lighting, weather particles, deferredLighting, ditherEnabled, colorGrading, volumetricFog, SSGI, FFT ocean, LTC lights, SkyAtmosphere.{multipleScattering,useScatteringLut,ozone,improvedMiePhase,dualLightInline}, contextOptions.webgpu.{hdrEnvironmentMap,iblPrefilterQuality,envMapMultiScatter,sceneCaptureReflections,envMapTemporalAccumulation,cloudsInReflections}, renderScheduler.occlusionCulling) | Flags exist on shared classes but are documented no-ops ("No effect on WebGL"); all default false/'parity'; setting them emits NO warning | All default false/off — byte-identical off path (Batch 426 Phase-1 policy: "Every field here defaults to the WebGL-parity behavior"). When enabled: WebGPU-only effects engage; `taaEnabled=true` additionally forces WebGPU MSAA to 1 (Batch 234 contract, C10 ruling d) and suppresses FXAA (`WebGPUPostProcessStageCollection.ts:490`) | **DOCUMENTED** per-flag (JSDoc + parity-default policy; FEATURE_INVENTORY §B tags) | Deliberate opt-in surface; the fork's parity-default policy keeps a default Viewer byte-neutral | None at defaults. On opt-in, WebGL silently ignores the flag (only `useWebVR` fails loudly in that direction) — API asymmetry, not default divergence | Scene.js:1083-1150,1231-1300,2878,5844-5851; SkyAtmosphere.js:55-129; GraphicsContext.ts:557-660; WebGPUPostProcessStageCollection.ts:427-493; WebGPUSceneRenderer.ts:1390-1414; RenderScheduler.js:91-103; ViewportExecutor.js:428-511 | **keep-ratified** — consider a one-time console.warn when a WebGPU-only flag is set on a WebGL context |
| 22 | **`RenderCommand._executeWebGPU` stale pass slot** | RenderCommand executes normally through the WebGL path | `_executeWebGPU` reads `context._currentRenderPass` — a property that does not exist on WebGPUContext (real slot: `_currentRenderPassEncoder`) — so the branch always falls through and never receives the active pass encoder (`Renderer/WebGPU/RenderCommand.js:345`) | **SILENT/ACCIDENTAL** — latent pre-existing defect filed during C9-07 as NEW-WEBGPU-RENDERCOMMAND-STALE-PASS-SLOT (`migration_doc/DEFERRED_WORK.md` ~5293); no containment record | No current caller exercises the broken branch on the default route, so it is inert rather than visibly broken; fix requires a consumer probe first (Principle 8) | Any future/extension consumer of the immediate-mode RenderCommand WebGPU path gets no active pass encoder. Zero default-path impact today | Renderer/WebGPU/RenderCommand.js:345; migration_doc/DEFERRED_WORK.md ~5293 | **fix-silent-divergence** (probe-first, already filed) |

## Verified-parity appendix (checked by the static sweeps — NOT divergent at defaults; do not re-derive)

| Item | Evidence |
|------|----------|
| FXAA / AO / Bloom / Tonemap / autoexposure-stage defaults | All default false on BOTH backends (`PostProcessStageCollection.js:43-63`); WebGPU SYNCS stage enabled-state from the same collection each frame (`WebGPUPostProcessStageCollection.ts:160-183,339-340,490-493`); Tonemap gated on `useHdr` on both (`:520-536`, NEW-PP-LIBRARY-TONEMAP-ORDER fix); tonemapper PBR_NEUTRAL honored. (Row 14 covers the *compute*, not the stage.) |
| `msaaSamples` default 4 | Same `Scene.js:488` default honored by both. Off-default caveats: WebGPU clamps to [1,4] (`WebGPUSceneRenderer.ts:1403-1413` — user-set 8 silently renders 4), TAA forces 1 (WebGPU-only). The 4→1 default flip is **explicitly NOT RATIFIED** (reserve lever C10-03R; W8-3 "default msaaSamples change requires maintainer ratification"). |
| Log-depth master default | ON for both (`Scene.defaultLogDepthBuffer=true`; WebGPU `fragmentDepth=true` + `_logDepthWriteEnabled=true` since Batch 251) — except the row-5 producer tail and row-8 pick fleet. |
| Default 3D frustum count | 1 on BOTH as of Batch 693 (`b156079da8`, C10-01 ENV-command frustum binning; previously a permanent 2-frustum floor on WebGPU). Sentinel: `Tools/visual-regression/probe-frustum-count-3d.mjs` (1/1/1 at 18,000km/500km/300m); 2D/CV proven no-op. |
| Sky/sun/moon/atmosphere env components | Parity components exist on WebGPU; sun/moon atmospheric extinction computed once in `Sun.js:136-173` and consumed by both GLSL and WGSL. StarField bright catalog now renders on BOTH backends (WebGLStarFieldRenderer, Batch 324, `Context.js:766-777`). |
| Translucency-derived paths | OIT excluded from pick frames symmetrically (`!picking` both sides); `pickTranslucentDepth=false` shared; classification-over-translucent uses a different architecture on WebGPU (ADR-2026-04-28, Batch-47 scaffolding inert by design) with output parity claimed via probes. |
| Scheduler-level opt-ins | RenderScheduler / SceneOctree / OcclusionCulling all default OFF on both (`RenderScheduler.js:31`; `SceneOctree.js:44`; `OcclusionCulling.js:44`); RenderScheduler bin/sort contained off on both (FAR-003/H12/FAR-504). |
| Misc parity defaults | `requestRenderMode=false`, `shadows=false`, `scene3DOnly=false`, `highDynamicRange=false`, `invertClassification=false`, `useDepthPicking=true`, `depthTestAgainstTerrain=false`, `fog.enabled=true`, `mapMode2D=INFINITE_SCROLL`, globe-depth FB effectively always-on both, `globe.pickable=false` deliberately matched to WebGL (DP-H44), powerPreference 'high-performance' both. WebGPU device pool (`useDevicePool` default true) and the ignored `contextOptions.webgl.*` block have coincidentally-matching defaults (opaque canvas, high-perf adapter); non-default `webgl.alpha:true` silently stops working on WebGPU. |

## (a) FLIP-CANDIDATE shortlist — divergences whose rationale is stale or absent

Best candidates for default-parity toggles (each needs either a flip-to-parity or an explicit maintainer ratification; none currently has a policy record):

1. **`globe.enableEnhancedOcean = true`** (row 2) — the largest default-pixels divergence, zero ratification, reverse-direction (WebGPU-ON) case, and entangled with the OPEN water-bugs-2026-07-06 lane. Strongest flip candidate: default `false`, keep as documented opt-in until the water bugs close and the maintainer ratifies a default-on look.
2. **`globe.enableNightLights = true` (armed)** (row 17) — silent default-armed enhancement, one common flag (`enableLighting`) away from a visible divergence. Flip to default `false` or ratify.
3. **AutoExposure always-on compute** (row 14) — B.14 comment-only justification, result unconsumed at SDR defaults; demand-gate the dispatch (perf parity at zero visual cost) and ratify the HDR-mode altitude gate separately.
4. **`usePostProcessSelected` hardwired false** (row 19) — no rationale on record at all; either port the selected path or fail loudly; file the missing tracking row.
5. **Empty-scene background color** (row 4) — silent, visible, already queued (NEW-WEBGPU-CANVAS-BACKGROUND-COLOR-PARITY); the only silent divergence with default-path visual exposure. Fix with a dedicated probe.
6. *(honorable mention)* **`scene.sunBloom` inert on WebGPU** (row 3) — documented guard, but the "PP Bloom/LensFlare handles it" claim is not true at defaults; wiring the flag to the WebGPU PP stages is the parity-restoring move rather than a flip.

Explicit non-candidates: OIT (row 1 — ratified, GO/NO-GO gated, see §C); GPU cull/HiZ/sort/indirect (row 20 — ratified fail-closed, restoration owned by FAR-003/T7); usePostProcess/forceSceneMRT/frame-graph shaping (rows 12/13/15 — architecture contracts with named follow-up slices); `msaaSamples` 4→1 (explicitly NOT ratified — do not flip).

## (b) Runtime-verification plan (pending Edge/Playwright pass)

The follow-up runtime pass must snapshot BOTH backends per lane (split-screen harness or paired runs), and for every lane **first assert `context.rendererType`** (row 10 — silent fallback would invert every result). Per finding:

1. **Backend identity + containment truth**: `CesiumDebug.snapshot()`, `getContainmentStats()` (webgpuOIT: requested=true/active=false/fallbackReason="contained-unsafe-path"; gpuCuller/hiZ/gpuSortKeys/tileIndirect all requested "never"), `environmentState` dump (useOIT, usePostProcess, usePostProcessSelected=false, useDepthPlane), attachment-demand record (C9-09), effective msaaSamples (expect 4/4), frustum count via `probe-frustum-count-3d.mjs` (expect 1/1/1 parity).
2. **Row 1 (OIT)**: NEW translucent-intersection probe (two interpenetrating translucent primitives + camera orbit) — capture WebGL (OIT) vs WebGPU (sorted alpha) baseline mismatch %. This probe is also the missing re-enable acceptance artifact for §C.
3. **Row 2 (ocean)**: open-ocean saved view (specular sun angle + coastline) WebGL vs WebGPU; repeat with `enableEnhancedOcean=false` to confirm the flip restores classic-water parity.
4. **Row 3 (sun bloom)**: sun-in-frame view; WebGL sunBloom true/false A/B (delta expected) vs WebGPU sunBloom true/false A/B (delta expected ZERO — proves inertness).
5. **Row 4 (background)**: `globe.show=false` + non-black `scene.backgroundColor`; assert WebGPU canvas pixels = transparent black (bug present) vs WebGL = backgroundColor.
6. **Row 5 (hyperbolic tail)**: far-camera depth-fight scene per remaining family (Buffer* primitive over terrain at >1000 km, point cloud at range) — quantify flicker/tie-break divergence.
7. **Rows 6-8 (pick family)**: cold-click `scene.pick` first-call result (undefined expected on WebGPU, hit on WebGL) then warmed result; `drillPick` staleness + console warning capture; `pickPosition` first-call undefined → 1-2 frame convergence; 2D-mode pickPosition permanent undefined; horizon occlusion oracle at 20/500/5000 km (WebGPU expected to over-return beyond-horizon picks); `pickVoxel` throw capture; `sampleHeight`/`clampToHeight` on/off-screen matrix.
8. **Row 9 (points)**: PointPrimitiveCollection with `blendOption: OPAQUE` intersecting opaque geometry — verify WebGPU commands ride TRANSLUCENT (depth-write difference visible) vs WebGL opaque.
9. **Row 14 (autoexposure)**: `CesiumDebug.gpuPassCost(true)` / pass-cost dump — count autoexposure dispatches at SDR defaults (expect >0 on WebGPU, 0 on WebGL); confirm zero pixel delta with the stage force-disabled.
10. **Row 15 (frame-graph)**: single-session A/B of `_sceneColorResolveElisionEnabled` true/false — byte-identity check on the default globe; empty-scene byte-identity for the C9-07 fallback clear.
11. **Row 17 (night lights)**: `enableLighting=true` night-hemisphere view WebGL vs WebGPU (divergence expected); repeat with `enableNightLights=false` (parity expected).
12. **General**: all captures via the split-screen harness (`Apps/WebGPUTest/split-screen-comparison.html`) + `Tools/visual-regression/capture-and-diff.mjs` conventions; Edge only (Playwright Firefox has no WebGPU); record mismatch % per lane into a companion `RUNTIME_DIFF_RESULTS.md` keyed by the row numbers above.

## (c) WebGPU OIT default-flip — maintainer GO/NO-GO gate

The row-1 OIT default (WebGPU MRT OIT contained off) is a **ratified maintainer decision** (2026-07-16, QUEUE_2026-07-15_CAMPAIGN9.md §3.3 item 1). Flipping it back on is **NOT a matrix action**: it is an explicit maintainer GO/NO-GO **gated on the Batch-700 OIT evidence package**. The static record feeding that decision, as of this sweep, is **NO-GO**: (a) audit finding B7's four defect classes (invalid depth/stencil attachment contract, single-sample accum/reveal vs MSAA pipelines, mixed-set draw loss, incomplete manual draw-loop command contract) have had no fixing commit to `WebGPUOIT.ts` since being filed; (b) two adjacent OIT latent findings remain open (NEW-WEBGPU-OIT-MSAA-RESOLVE-ORDERING, DEFERRED_WORK.md:5395, filed 2026-07-18; NEW-WEBGPU-OIT-DEFERRED-SPLAT-CANVAS-RESUME, :5305); (c) the ratification's own re-enable acceptance artifact — a before/after translucent-intersection visual probe — does not yet exist in `Tools/visual-regression` (runtime-plan item 2 creates it); (d) the fix lane (FAR-401/FAR-404) and restoration row (FAR-003/T7, C9 Wave 6) are unlanded. Until the Batch-700 package closes those four items and the maintainer signs off, the matrix disposition for row 1 remains **keep-ratified**.
