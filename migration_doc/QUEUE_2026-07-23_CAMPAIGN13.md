# Campaign 13 — Planetary Volumetric Clouds: RTE, Temporal Reconstruction, and Weather Realism

Prepared: 2026-07-23

Status: **LAUNCHED / EXECUTING (2026-07-23).**

Launch authority: explicit maintainer direction on 2026-07-23:

> "Okay confirm all of these findings are documented then lets start work on the cloud fixes and
> improvements. Create a cloud specific campaign and then lets launch it."

Anchor: committed `main` HEAD **`851ce64389` (Batch 731)**. The queue and launch truth are
`C13-00`, landed locally as **Batch 732 (`f4a934e606`)**. Runtime work begins with `C13-01` only
after the launch/document batch is committed, `git status` contains no unrelated changes, and
`npx tsc --noEmit` is green.

Publication update: the 2026-07-23 HTTP-403 attempt below is historical. As of
the 2026-07-31 audit, local `main` and `origin/main` are equal at Batch 771
(`fe990ab335`) [that audit's changeset landed as Batches 772-781 on 2026-08-01,
`origin/main` = `3900608bb9`]. No runtime gate may be bypassed merely because
later work is present in the tree, landed or not.

Campaign 13 supersedes **only** Campaign 11's `clouds-weather` execution cluster. It does not close
Campaign 11's unrelated open work. Campaign 12 launched on 2026-07-23 and is
executing independently; Campaign 13 does not close or renumber it.

Operating model: **ORCHESTRATOR**. One worker implements a bounded slice and returns a verified dirty
tree; an independent reviewer checks the actual diff and evidence; only the orchestrator lands it and
updates this ledger.

**EXECUTOR RULING (maintainer, 2026-07-24, recorded Batch 758): the orchestrator (Claude Fable/Opus
workers) owns ALL Campaign 13 execution — Option B.** Sol 5.6's dispatch lane is closed; the
maintainer will handle any future re-partitioning. If Sol returns and leaves uncommitted work in the
tree, it is taken over, independently verified, and landed under orchestrator review (the Batch
743/754 takeover protocol). The C13 dispatch freeze is LIFTED. The mechanics in
`campaign11_planning/guides/G10-charter-mechanics.md` remain the process reference.

**RE-PARTITIONED 2026-08-24 — `R-2026-08-24-3`** ([MAINTAINER_RULINGS_2026-08-24.md](MAINTAINER_RULINGS_2026-08-24.md)).
The ruling above reserved re-partitioning to the maintainer (_"the maintainer will handle any
future re-partitioning"_); this is that reserved act, and it narrows Option B rather than
replacing it. **Codex Sol MAY build bounded, spec-verifiable C13 instrument and harness work**
under an Opus lane lead, with a **separate Opus station-3 review**, the orchestrator landing.
**C13 engine-semantic changes remain Opus-authored**, and everything outside that carve-out stays
under Option B as written. The Batch 743/754 takeover protocol is unchanged.

This regularizes **Batch 1131** (`1c2161c8de`) and **Batch 1136** (`e2615ef8e2`), the SOL-4
refresh-cost rounds, which were Sol-built under cross-family station-3 review. SOL-4 is C13
territory — this queue's own `C13-41` exit condition names it — so those batches were a
**deviation from Option B, disclosed in their commit messages but never reconciled here**. Both
stand as landed; the reconciliation gap is what this stamp closes.

### 2026-08-02 Codex audit + rendered-tail overlay — ⚠ HISTORICAL, SUPERSEDED AT BATCH 866

> **HISTORICAL — read for mechanism, not for status (banner added 2026-08-09, handover audit FIX 18).**
> Everything from here to the start of §1 was written **inside a live worktree on 2026-08-02** and is
> in the present tense about that worktree ("the current worktree adds…", "the retained rebuilt Edge
> run…", "still cannot close the owed rendered parser lane"). **That worktree is long gone and its
> content landed.** `C13-08` and `C13-GATE-B` both **CLOSED at Batch 866 (2026-08-07)**, which is the
> supersession point for every status claim below; the §9 ledger rows are the authority. A successor
> reading this section as current would re-open closed work and hunt for a tree that does not exist.

`C13-08` and `C13-GATE-B` are **COMPLETE as of 2026-08-07 (Batch 866)** — see the ledger rows. The Batch-806 WebGPU regional-placement pixel lane remains green,
as does the retained regional-tail evidence described below. The current
worktree adds the CoverageJSON antimeridian parser correction (7/7 focused
mutation lane; 38/38 with the expanded bounds suite), the persistent
rendered-tail probe, and the independent promotion audit's regional
cell-registration correction: non-wrapped continuous source coordinates now
clamp before their interpolation fractions are derived, preventing west/north
extrapolation and a diagonal observation leaking into a no-data corner. The
focused bounds suite is 31/31, including mutation-sensitive W/E/N/S and no-data
edge cases. The retained rebuilt Edge run of
`probe-weather-regional-tails.mjs` passed every WebGPU cyclic-seam and WebGL
regression arm: both ±180° sides observed, seam halves continuous with no
centre wall or duplicate band, far view byte-identical to procedural fill,
non-vacuous regional pack statistics, unchanged WebGL billboard pixels,
zero WebGL volumetric publication, and zero device/console/page errors. All ten
PNGs were visually reviewed; the 100%-coverage regional fixture is intentionally
overcast on both seam sides, has no seam wall, and the far and WebGL control
pairs are byte-identical. That evidence is retained rather than erased, but it
does not replace the seven browser regressions still required by the checklist:
the five global ingest/source/channel/time probes, the seam/pole probe, and the
intended-behaviour METAR probe have no post-Batch-797 evidence. Canonical
COMPLETE promotion requires those seven green reruns after the packer change,
followed by orchestrator/maintainer review and landing.

Do not mint a duplicate task ID. The parser defect belongs to `C13-08` and is
fixed in the current worktree:
`CoverageJsonParser` derives longitude bounds with ordinary numeric min/max, so
a cyclic axis such as `170, 175, 180, -175, -170` becomes an almost-global span
instead of a wrapped 20-degree region even though `WeatherTexPacker` correctly
handles explicitly supplied wrapped bounds. It now unwraps the source axis
before deriving both orientation and bounds; forward/reverse seam encodings
canonicalize to the same interval while ordinary/global bytes are preserved.
A manually supplied `170°E → 170°W` packer fixture still cannot close the owed
rendered parser lane.

---

## 1. Canonical ID table

This table is the campaign backbone and is authored before wave prose. IDs are add-only: never
renumber, reuse, or mint a `C13-*` identifier elsewhere without adding it here first.

| ID | Canonical task | Pri | Class | Effort | Wave | Hard dependencies |
| --- | --- | --- | --- | --- | --- | --- |
| `C13-00` | Launch seal, audit-truth capture, and C11 cloud transfer | R0 | docs/gate | S | W0 | maintainer authority |
| `C13-01` | Repair the Node/Playwright cloud tour and capture current moving baselines | P0 | test/correctness | M | W0 | `C13-00` landed clean |
| `C13-02` | Cloud CPU/GPU observability and temporal-cost counters | P0 | tooling/perf | M | W0 | `C13-01` |
| `C13-03` | WGS84/RTE coordinate contract plus planetary RED probes | P0 | architecture/correctness | M | W1 | `C13-01` |
| `C13-04` | Automatic RTE and WGS84 shell math in the primary cloud march | P0 | correctness | L | W1 | `C13-03` |
| `C13-05` | RTE temporal reprojection, history origin, and teleport/reset contract | P0 | correctness | L | W1 | `C13-03` |
| `C13-06` | RTE cloud-shadow, mask, environment-capture, and atmosphere consumers | P0 | correctness | L | W1 | `C13-03`, `C13-04` |
| `C13-07` | Dateline/pole-safe global weather sampling stopgap | P0 | correctness | M | W1 | `C13-03` |
| `C13-08` | Honor `WeatherField.bounds`, missing-data semantics, and regional packing | P0 | correctness/data | M | W1 | `C13-01` |
| `C13-09` | Cloud reconstruction attachments: front depth, weighted depth, velocity, and moments | P0 | architecture/perf | L | W2 | `C13-04`, `C13-05` |
| `C13-10` | True 1/16-rate current-frame raymarch with full-resolution temporal reconstruction | P0 | perf/quality | L | W2 | `C13-02`, `C13-09` |
| `C13-11` | License-clean STBN generation/import and stochastic cloud jitter | P1 | quality/perf | M | W2 | provenance-approved asset path | **UNBLOCKED — RULING R-2026-08-10-5: generate our own STBN in-repo, ground-up (void-and-cluster/simulated-annealing per the published algorithms) as a reproducible build tool with hash-pinned output + Fourier-spectrum validation, following the moon-albedo-bake pattern. Dispatched CO-36. Fallbacks (NVIDIA textures w/ cleared license; ship without) preserved in MAINTAINER_RULINGS_2026-08-10.md.**
| `C13-12` | Disocclusion, variance clipping, reactive history, and wind-aware reprojection | P0 | correctness/quality | L | W2 | `C13-09`, `C13-10` |
| `C13-13` | Decouple lighting fidelity from spatial raymarch tier | P1 | quality/perf | M | W2 | `C13-10` |
| `C13-14` | Globe-quadtree weather tile schema, gutters, cache, atlas, and LOD | P0 | architecture/data | XL | W3 | `C13-07`, `C13-08` |
| `C13-15` | Regional and seasonal climate-prior field | P1 | weather/quality | L | W3 | `C13-14` |
| `C13-16` | Per-region cloud-type and vertical-deck mixtures | P1 | weather/quality | L | W3 | `C13-14`, `C13-15` |
| `C13-17` | Deterministic per-formation randomization | P1 | weather/quality | M | W3 | `C13-14`, `C13-16` |
| `C13-18` | Local-tangent ENU wind and spatial advection | P1 | weather/correctness | L | W3 | `C13-14` |
| `C13-19` | Temporal interpolation between weather slices | P1 | weather/quality | M | W3 | `C13-18` |
| `C13-20` | Regional-source composition, priority, and no-data fallback | P1 | weather/data | M | W3 | `C13-08`, `C13-14` |
| `C13-21` | Multi-layer morphology and energy-consistent cloud lighting | P1 | quality | L | W4 | `C13-12`, `C13-16` |
| `C13-22` | Rich Beer shadow maps and temporally stable cloud shadows | P1 | quality/perf | L | W4 | `C13-06`, `C13-12` |
| `C13-23` | Per-position optical extinction | P2 | quality | M | W4 | `C13-16`, `C13-21` |
| `C13-24` | Backend-neutral `scene.weather` / `WeatherSystem` facade | P2 | API/architecture | M | W4 | `C13-08` |
| `C13-25` | In-cloud lightning reland with upper-tail anti-grid oracle | P2 | feature/quality | M | W5 | `C13-17`, `C13-19` |
| `C13-26` | GRIB2 ingest through same-origin proxy and WASM decoder | P2 | data/feature | L | W5 | proxy, decoder, network fixture |
| `C13-27` | Live EDR network confirmation | P2 | tooling/data | S | W5 | networked browser session |
| `C13-28` | Ground snow-albedo consumer on WebGL and WebGPU | P2 | feature/parity | M | W5 | `C13-20`, terrain shader coordination |
| `C13-29` | Orbit cloud impostor/freeze-and-reproject quality tier | P3 | perf/quality | L | W6 | `C13-10`, `C13-11`, Gate C |
| `C13-30` | Precipitation coupling: rain shafts and ground wetness | P3 | feature | L | W6 | `C13-20`, `C13-25`, `C13-28` |
| `C13-31` | Contrail and pyrocumulus source infrastructure | P3 | feature | L | W6 | `C13-14`, `C13-16` |
| `C13-32` | View-local cascaded 3D density clipmap | P3 | architecture/perf | XL | W6 | `C13-10`, Gate C |
| `C13-33` | Historical weather replay demo | P3 | demo/data | M | W6 | `C13-24`, `C13-26` |
| `C13-34` | Cloud-shadow contribution to dynamic environment maps | P3 | feature/quality | S | W6 | `C13-22` |
| `C13-35` | Command-empty environment demand scheduling and cold-start cloud readiness | P0 | correctness/perf | M | W0 | `C13-01` |
| `C13-36` | Animated per-pixel IGN ray-start jitter and periodicity oracle | P0 | quality/perf | M | W2 | `C13-03` |
| `C13-37` | Planet-stable non-periodic baked density domain | P0 | quality/correctness | L | W2 | `C13-03`, `C13-36` |
| `C13-38` | Suppress cloud-IBL environment refreshes while full cloud IBL is irrelevant | P0 | perf/correctness | S | W2 | `C13-37` |
| `C13-39` | Hoist density LOD/domain transforms out of per-sample view, light, shadow, and IBL loops | P0 | perf/architecture | L | W2 | `C13-02`, `C13-37` |
| `C13-40` | Async cloud-noise prewarm plus generation-keyed reconstruction resources and retirement | P1 | perf/architecture | M | W2 | `C13-02`, `C13-37` |
| `C13-41` | C12-29 S3 rider: eclipse-driven cloud lighting, cloud shadow, and IBL dimming/refresh. ★ **ELEVATED TO THE C14 CRITICAL PATH by `R-2026-08-10-1`** ([`MAINTAINER_RULINGS_2026-08-10.md`](MAINTAINER_RULINGS_2026-08-10.md), stamped here 2026-08-09): the maximal C12 exit gate keeps C12 open until every `C12-29` slice lands including S3, which this row canonically owns — so C14 is blocked on C12 is blocked on `C13-41`. Status and next diagnostic in the §9 ledger row. | P1 ★ | correctness/quality | M | W2 | `C13-GATE-B`, `C13-06`, `C13-38`; informed by `C13-39` |
| `C13-39B-CLOUD-SHADER-VARIANT-SPLIT` | Compile-time cloud shader **variant split** (cone vs straight light march, possibly the shadow/IBL legs) via the `ShaderDefineHi` hi-word registry + the `//>>ifdef` preprocessor, so per-route code stops taxing every other route's static register budget — then re-attempt the per-route micro-optimizations INSIDE each slimmer variant. **This is the ONLY supported `C13-39` retry vector**, filed Batch 762 (`c79f575e9e`) alongside the `C13-39` CLOSED-NEGATIVE result. *(Row added 2026-08-07, close-out docs reconciliation: the item existed only in `DEFERRED_WORK.md` → "2026-07-24 - C13-39B" and was therefore invisible to every scoping pass over this table, even though the `C13-39` row names it as the retry path. Add-only; no ID renumbered. Pri/Wave assigned by this pass for schedulability and are the orchestrator's to ratify; Effort is the filing's own M–L.)* | P2 | perf/architecture | M–L | W2 | `C13-39` (CLOSED-NEGATIVE); `C11-149` hi-word registry (landed Batch 739); **mandatory interleaved-A/B protocol** per the `C13-39` closure |
| `C13-16-EROSION-STRENGTH-RANGE-CONTRACT` | Define and enforce the public `erosionStrength` range before treating `base >= full` as a proof outside the renderer's normalized/default `[0,1]` operating domain. `AtmosphericConditions.erosionStrength` and `cloudErosionStrength` currently forward an unvalidated override; values at/above the remap pole or below zero need an explicit compatibility ruling, unit coverage, and both density-route checks. **FILED during the U2 lane, lands 2026-08-21 (Batch 1108); OPEN. No clamp or behavior change belongs in the U2 morphology slice.** | P2 | API/correctness | S | W3 | `C13-16`; public compatibility ruling |
| `CLOUD-LOW-COVERAGE-CUTOFF` **(fog cheap-path arm)** | Fog cheap-path cloud-shadow coverage gate — route `Compute/VolumetricFog.wgsl::sampleCloudShadow`'s raw `1.0 - coverage` threshold onto the shared coverage response without making the sparse end worse. ⚠ **KNOWN ID COLLISION — this row is the FOG CHEAP-PATH ARM ONLY.** `DEFERRED_WORK.md` carries a **byte-identical** `### CLOUD-LOW-COVERAGE-CUTOFF` heading for a *different* item, the **visible-march arm** ("coverage <= ~0.40 renders zero cloud"), which was RESOLVED at Batch 798 (`c5bd07bf1f`); a grep for the bare ID returns that one first. Always disambiguate by subtitle, and any future "mark `CLOUD-LOW-COVERAGE-CUTOFF` resolved" edit must say which arm. *(Row added 2026-08-07, close-out docs reconciliation: §9 already carried a status row for this arm while §1 — the add-only backbone — did not, so the ID had status without an entry. Pri/Wave assigned by this pass and are the orchestrator's to ratify.)* | P2 | correctness/quality | S | W2 | 2026-08-02 Codex handoff §5 item 3; `DEFERRED_WORK.md` "fog cheap-path arm" entry |
| `C13-GATE-A` | Launch and evidence-truth gate | R0 | gate | S | W0 | `C13-01`, `C13-02`, `C13-35` |
| `C13-GATE-B` | Planetary correctness gate | R0 | gate | M | W1 | `C13-03..08` |
| `C13-GATE-C` | Temporal reconstruction and measured-performance gate | R0 | gate | M | W2 | `C13-09..13`, `C13-36..41` |
| `C13-GATE-D` | Regional weather realism gate | R0 | gate | M | W3 | `C13-14..20` |
| `C13-EXIT` | Feature-preserving cloud certification | R0 | gate | L | EXIT | Gates A-D, selected W4/W5 owners |

---

## 2. Confirmed current-state findings

These findings are the frozen launch premise, not the live completion ledger. Every implementation
brief must re-grep the named symbols and reproduce its premise at execution HEAD before changing
code. Section 9 and the dated evidence subsections are the current execution authority when later
Campaign 13 work supersedes a launch-time finding.

### 2.1 Temporal reconstruction is not yet TAAU

- The current raymarch target is one-half width and one-half height. It shades every texel in that
  target every frame: **one-quarter of full-resolution pixels**, not one-sixteenth.
- `temporalUpdateFraction` is documented like a pixel-refresh fraction, but the renderer packs it as
  the history blend weight and `CloudTemporalResolve.wgsl` samples fresh current color for every
  output texel. Renaming/migration belongs with `C13-10`; until then, evidence must call it a blend
  weight rather than a work-reduction control.
- The current jitter is a repeating 4×4 Bayer sequence, not spatiotemporal blue noise.
- The history/reconstruction surface carries color only. It has no cloud front depth, weighted depth,
  velocity, variance/moments, or explicit reactive/disocclusion data.
- `CloudUpscale.wgsl` uses scene depth for a small bilateral upscale, but it does not have cloud depth
  with which to distinguish overlapping or newly revealed cloud volumes.

Primary local anchors:

- `packages/engine/Source/Renderer/WebGPU/WebGPUProceduralCloudRenderer.ts`
- `packages/engine/Source/Renderer/WebGPU/WebGPUCloudTierPresets.ts`
- `packages/engine/Source/Shaders/WebGPU/Environment/CloudTemporalResolve.wgsl`
- `packages/engine/Source/Shaders/WebGPU/Environment/CloudUpscale.wgsl`

### 2.2 Planet-scale precision is inconsistent

- The primary march has an opt-in camera high/low path, but `CloudVolumetrics.cloudHighPrecision`
  defaults false. Planetary precision is correctness infrastructure and must not depend on an
  appearance-quality toggle.
- The temporal resolver reconstructs a world anchor from a raw `vec3<f32>` camera position and a
  previous view-projection matrix. It does not share the march's high/low origin, so enabling the
  primary high-precision path does not make cloud history RTE-correct.
- Noise/weather sampling retains raw world-space positions in parts of the march. High/low shell
  intersection alone cannot prevent swimming or precision loss if the density coordinate later
  returns to full ECEF `f32`.
- Cloud shadows, environment capture, cloud masks, and atmosphere composition need one verified
  coordinate contract. Fixing only the visible march would leave history and consumers unstable.
- The current shell assumptions must be checked against Cesium's WGS84 ellipsoid rather than treating
  the whole planet as a convenient sphere in one subsystem and an ellipsoid in another.

The required end state is one camera-relative/RTE cloud frame, with stable planet anchoring, shared by
the visible march, temporal reprojection, weather sampling, shadow passes, mask/composite passes, and
atmospheric consumers.

### 2.3 Equirectangular weather sampling cannot satisfy the globe contract

- `worldToWeatherUV` converts the sample direction to longitude/latitude. U repeats and V clamps.
- The procedural weather-map producer is not periodic at the first/last longitude columns. A repeating
  sampler does not make non-periodic source data seam-free, so the dateline can show a discontinuity.
- At the poles, all longitudes geometrically collapse while the texture row still contains distinct
  values. This creates pinching and orientation artifacts.
- `WeatherTexPacker` assumes a global texture and does not honor regional `WeatherField.bounds`.
  Regional source data can therefore be stretched across the whole globe instead of being placed in
  its real geographic extent.
- Fine cloud motion applies global Cartesian offsets rather than local east/north tangent wind.
  Direction and radial contamination vary around the globe.

`C13-07` is a bounded stopgap for current global maps. `C13-14` is the durable architecture:
Cesium-globe-quadtree weather tiles or an equivalent seam-safe globe parameterization, with overlap
gutters, bounds, no-data semantics, LOD, and stable cache ownership. The general scene octree is not
the owner for globe weather.

### 2.4 Regional variety and same-type variety are both missing

The current packed weather channels provide useful scalar controls, but the system still has one
collection-level genus/profile and a globally repeated procedural morphology. It lacks:

1. A slow climate/region prior: latitude, season, land/sea, orography, and broad tropical, maritime,
   continental, desert, and polar regimes.
2. A synoptic field: coverage/cloud fraction by level, humidity, pressure/vertical motion,
   precipitation, and wind.
3. A per-region mixture of cloud types and vertical decks rather than one global genus.
4. Mesoscale formation identity and variation.
5. Fine 3D microstructure.
6. Continuous interpolation and advection between weather times.

Formation variety must be deterministic. Seed from stable globe tile/cell identity, weather epoch,
layer/type, and an explicit user seed. Randomize physically bounded distributions—coverage
threshold, base/top altitude, density, erosion, anvil strength, phase response, and local wind—not
per-frame white noise. The same camera, time, data, and seed must reproduce the same formations.

### 2.5 Tier selection currently trades away too much appearance

The lowest/far tier reduces geometric samples but also zeros or strongly reduces powder, ambient, and
isotropic lighting terms. That makes the orbit view cheap by making it unusually flat. Spatial
sampling, lighting fidelity, shadow fidelity, and history quality need separate budgets. `C13-13`
must preserve coherent atmosphere and scattering while reducing spatial work.

### 2.6 Existing cloud regression evidence is not a trustworthy current oracle

`Tools/visual-regression/probe-cloud-tour.mjs` still checks removed Globe cloud properties before
writing the unified `globe.defaultCloudCollection.volumetric` configuration. After cloud unification,
those conditions can be false, so the intended presets are not reliably applied. Several cloud images
also predate later cloud fixes, and still images cannot certify temporal stability.

`C13-01` repairs the probe before any visual fix. It must exercise motion, altitude transitions,
teleports, time/wind changes, the antimeridian, both poles, multiple regions, multiple cloud types,
and repeated formations of the same type.

### 2.7 Takram is a reference, not a code transplant

Takram's published cloud path demonstrates the architecture to compare against:

- current-frame raymarch at one-quarter width and one-quarter height;
- full-resolution temporal history;
- stochastic sampling;
- cloud depth/velocity output;
- vectorized cloud layers;
- richer Beer shadow-map data and temporal filtering.

Their own documentation also records temporal ghosting/smearing and depth-overlap limitations. Do not
copy the transmittance-weighted-mean-depth limitation without evaluating front depth plus moments, and
do not imply their attractive published cloud package is already a production WebGPU implementation.
The current repository describes WebGPU clouds as work in progress.

Primary references:

- [three-geospatial repository](https://github.com/takram-design-engineering/three-geospatial)
- [cloud package documentation](https://github.com/takram-design-engineering/three-geospatial/tree/main/packages/clouds)
- [CloudsPass source](https://github.com/takram-design-engineering/three-geospatial/blob/main/packages/clouds/src/CloudsPass.ts)
- [cloud raymarch shader](https://github.com/takram-design-engineering/three-geospatial/blob/main/packages/clouds/src/shaders/clouds.frag)
- [temporal resolve shader](https://github.com/takram-design-engineering/three-geospatial/blob/main/packages/clouds/src/shaders/cloudsResolve.frag)
- [shadow shader](https://github.com/takram-design-engineering/three-geospatial/blob/main/packages/clouds/src/shaders/shadow.frag)

### 2.8 Command-empty frames currently suppress demanded clouds

The initial `cloudQuality=128` zero-cloud artifact was not evidence that the live-noise shader fails
at 128 steps. The probe configured the collection correctly, but its cold upward-looking view drained
to zero frustum command lists before the lazy procedural renderer executed. The WebGPU scene renderer
returned before resource setup and the post-frustum/environment chain, leaving `_cloudCache`
uninitialized, uniforms zero, no cloud pass, and a meaningless sub-millisecond timing.

This is both an evidence bug and a renderer scheduling bug:

- a loaded feature-renderer handle is not proof its `execute` ran;
- a fixed rAF count does not guarantee the environment chain ran;
- no geometry/frustum commands does not mean a demanded screen-space cloud/fog/weather frame has no
  work.

`C13-35` owns the scheduling correction and the exact readiness gate. The 128-step path remains under
`C13-13` for lighting/spatial-budget separation, but the old blank screenshot is reclassified as
invalid evidence rather than a raymarch defect.

### 2.9 The severe static lattice precedes temporal history

The repeated rows/spokes are visible in full-resolution, temporal-off captures and are much stronger
with baked noise than live noise. The baked 3D texture repeats on all axes, uses harmonically related
frequencies, and is sampled in the ECEF domain. Its approximate visible periods are `7.4 km` for the
base shape and `0.667 km` for detail; perspective projects that Cartesian lattice into converging
rows.

The planned per-pixel animated interleaved-gradient-noise ray-start offset also never shipped:
`QF_JITTER` and the tier's `jitterEnabled` exist but are not consumed, while the current Bayer value
is one frame-global UV offset. Temporal history preserves and can amplify this structured input, but
is not its original cause. `C13-36` restores the license-free ray-start jitter contract; `C13-37`
owns the durable non-periodic density domain.

---

## 3. Functionality-preservation contract

Performance work must improve an existing feature rather than remove, bypass, default-disable, or
visually weaken it. The following current capabilities are explicit regression gates:

- WebGL `CloudCollection` billboard rendering and all upstream cloud APIs.
- The unified `CloudCollection.renderMode` ownership model, with `BILLBOARD` and `VOLUMETRIC`
  remaining selectable.
- `globe.defaultCloudCollection`, `CloudVolumetrics`, `CloudType`, and
  `AtmosphericConditions.clouds` facade behavior.
- Adaptive coarse-to-fine marching, march-step growth, far cap, early termination, and existing
  quality selection.
- Existing lighting, time-of-day coloration, ambient/ground bounce, powder, aerial perspective, IBL,
  god rays, cloud shadows, and atmosphere/fog consumers.
- Existing multi-deck, cloud-type profile, weather-channel, species, exotic, and special-cloud
  controls.
- EDR, WCS CoverageJSON, METAR, and synthetic weather sources, their async time model, and offline
  fixtures.
- Environment-map cloud capture and every current default-off feature switch.
- Multi-context and device-loss behavior. Cloud/weather GPU resources are per context/device, never
  module-global mutable renderer state.

Cloud-march features may remain WebGPU-only where WebGL has no volumetric architecture. Features
placed on a backend-neutral Scene, Weather, or globe-surface API must preserve WebGL behavior and
receive a WebGL implementation or an explicit documented degradation contract. `C13-28` is a
both-backend terrain feature.

No new public flag is a substitute for correct internal RTE. Existing flags remain backward
compatible, but planet-scale precision should become automatic once Gate B verifies it.

---

## 4. Standing implementation rules

1. **Probe first.** Reproduce the exact defect before changing engine code. A visual change requires
   Node/Playwright screenshots, quantitative evidence, and human inspection of the PNGs.
2. **No Python tooling.** Browser automation and benchmark orchestration use the existing Node/Edge
   toolchain.
3. **Motion is mandatory.** Request-render idle soak is not an FPS or temporal-stability test.
4. **One concern per landing.** Do not combine RTE, weather tiling, and appearance tuning into an
   unreviewable shader rewrite.
5. **RTE is end-to-end.** Camera subtraction occurs before `f32` reconstruction. Never add encoded
   high/low ECEF values before subtracting the encoded camera.
6. **Stable coordinates.** Density/noise coordinates are planet-anchored yet locally precise.
   Camera-relative rendering must not make the weather swim.
7. **Add-only layouts.** Existing `CloudUniforms`, quality flags, and shader define identities are not
   reordered. New topology participates in exact cache keys.
8. **No hot-path conversion.** Weather decoding, regional composition, tile upload, pipeline
   creation, and representation conversion are asynchronous/prepared before the draw hot path.
9. **Resource lifetime follows submit.** Replaced weather/history/shadow resources retire only after
   their last submitted use and remain context/device owned.
10. **Determinism.** Randomization derives from stable identities and explicit seeds. It does not
    call a changing random source per frame.
11. **License provenance.** Do not vendor NVIDIA STBN assets or any unprovenanced noise texture.
    `C13-11` stays blocked until a license-clean generation/import path and notices are recorded.
12. **Honest performance claims.** Report GPU time, CPU time, resolution, quality tier, adapter,
    route, scene content, and feature state. A correct change that misses a performance target is an
    honest result, not permission to degrade output.

---

## 5. Transfer map from Campaign 11

This is an ownership transfer, not duplicated scheduling. Campaign 11 retains its historical IDs and
evidence; Campaign 13 owns the remaining cloud execution.

| Campaign 11 source | Campaign 13 owner | Intake disposition |
| --- | --- | --- |
| `C11-124` — lightning reland | `C13-25` | transferred, broadened by the new formation/time architecture |
| `C11-125` — STBN/TAAU | `C13-09..12` | **PARTIAL at transfer**: march growth/far cap shipped; true 1/16 reconstruction and STBN remain |
| `C11-126` — remove Globe cloud flag / unification | `C13-00` only | **COMPLETE before C13** in B621/B622; doc truth only, never reimplement |
| `C11-127` — GRIB2 | `C13-26` | transferred, blocked on proxy/decoder/fixture prerequisites |
| `C11-128` — live EDR confirmation | `C13-27` | transferred, environment-blocked |
| `C11-129` — `WeatherSystem` / `scene.weather` | `C13-24` | transferred |
| `C11-130` — snow albedo | `C13-28` | transferred; both-backend globe-surface contract |
| `C11-SEED-10` — cloud impostor LOD | `C13-29` | transferred as a gated quality/battery tier |
| `C11-SEED-11` — precipitation coupling | `C13-30` | transferred |
| `C11-SEED-12` — planet-scale cloud tiling | `C13-14` | promoted to P0 architecture; largest regional-weather gap |
| `C11-SEED-13` — exotic remainder | `C13-31` | transferred |
| `C11-SEED-14` — Tier-2 3D bake | `C13-32` | transferred as post-Gate-C architecture option |
| `C11-SEED-15` — interpolation/advection | `C13-18`, `C13-19` | split into spatial and temporal owners |
| `C11-SEED-16` — historical replay | `C13-33` | transferred, still GRIB2-gated |
| `C11-SEED-17` — position-dependent extinction | `C13-23` | transferred |
| `C11-SEED-18` — env-map cloud shadow | `C13-34` | transferred |

The durable legacy input remains
`campaign11_planning/guides/G12-clouds-weather.md`. Its historical status labels are not the live C13
ledger.

---

## 6. Execution waves

### W0 — Truthful oracle and measurement

#### `C13-00` — launch seal and documentation truth

Record this queue, the corrected C11 transfer state, the audit findings, and the active-campaign
registration. Do not claim a cloud runtime change. This row is complete when the launch batch lands
clean and the queue is the single source for cloud execution.

#### `C13-01` — repair and expand the cloud tour

Fix the unified-API setup in `probe-cloud-tour.mjs`, then create reproducible moving sequences for:

- low-altitude ground/horizon;
- inside a cloud deck;
- above the deck;
- orbital altitude and altitude transitions;
- dateline crossings in both directions;
- north- and south-pole approaches;
- at least four distinct climate/region fixtures;
- at least three cloud types, including multiple same-type formations;
- wind/time advancement, camera teleport, and history reset.

Record source/build hash, adapter, canvas resolution, tier, current/history target dimensions, CPU
frame distribution, available GPU timestamps, temporal-delta/ghost metrics, and screenshots. Do not
compare volumetric output to WebGL billboards as if they were visual-parity twins; use WebGL to guard
shared scene/globe functionality.

#### `C13-02` — cloud observability

Expose, without per-frame allocation:

- raymarch target dimensions and pixels dispatched;
- current/history resolve pixels;
- primary/light-march sample counts or bounded proxy counters;
- history accept/reject/reset counts;
- weather texture/tile cache hits, misses, uploads, and live bytes;
- cloud shadow pass count and dimensions;
- CPU stage timing and GPU timestamp timing when supported.

Keep clean and API-instrumented lanes separate. Instrumentation must be removable/disableable without
changing the render result.

#### `C13-35` — command-empty environment demand scheduling

An empty geometry/frustum command list is not proof that the frame has no work. Preserve the
zero-work return only when no post-frustum/environment consumer is demanded. Managed or user-owned
volumetric clouds, fog, weather, SSR, outlines, and contact shadows must still reach the canvas and
environment chain while looking into an otherwise command-empty sky.

Cloud probes must await the exported lazy feature-renderer key and then prove at least one actual
execute plus initialized uniforms/targets. A fixed rAF warm-up count, configuration round trip, or
loaded renderer handle is not execution evidence.

### W1 — Planetary correctness before appearance tuning

`C13-03` defines the shared cloud coordinate contract and authors RED probes. `C13-04` applies it to
the visible march; `C13-05` applies it to temporal history; `C13-06` applies it to shadow/mask/capture
and atmosphere consumers.

The contract must specify:

- WGS84 ellipsoid shell intersection and height;
- camera high/low or a camera-relative local frame;
- stable tile/cell origins for density and weather coordinates;
- previous-frame origin and previous camera transform;
- scene-mode and teleport/history invalidation;
- local tangent basis used for wind;
- packing precision and the exact point at which `f64` CPU values become `f32` GPU-relative values.

`C13-07` may first make the existing global equirectangular producer periodic and pole-safe as a
bounded correctness patch. It must not be presented as the final regional architecture.

`C13-08` stops stretching regional fields globally. The packer and provider contract must carry
bounds, coordinate reference, resolution, missing/no-data, revision/time, and source priority.

### W2 — Real temporal reconstruction and measured performance

`C13-09` creates an exact topology for current cloud output. At minimum evaluate:

- premultiplied color/transmittance;
- nearest/front cloud depth;
- transmittance-weighted depth;
- motion/velocity;
- first/second moments or a compact variance representation.

Do not rely on only one mean depth for separated overlapping volumes.

**LOCAL C13-09 SENTINEL HARDENING 2026-08-09; LANDING OWED.** An exact
zero-alpha/no-cloud ray now returns scalar depth `-1` from the CPU weighted-depth
twin; the WGSL attachment writer leaves `depth = (-1, -1)` and velocity invalid.
Every positive alpha,
including the small-alpha continuity band, retains the existing interval
midpoint behavior. No pass, target, format, default, or current consumer was
changed. Focused reconstruction-attachment contracts pass **42/42**. C13-09
remains infrastructure-complete; downstream C13-10/C13-12/Gate-C reconstruction
acceptance remains open.

`C13-10` makes one current-frame phase cover one-sixteenth of full-resolution pixels and reconstructs
the full-resolution output from history. It must prove the actual current target/dispatch size; a
smaller blend weight is not a work reduction.

`C13-11` replaces ordered Bayer sampling only after a provenance-clean STBN resource exists. The old
path remains the loading/fallback route. This task may proceed in parallel with attachment design but
does not block RTE or bounds correctness.

`C13-12` builds on `C13-05`'s coarse history-compatibility contract and owns attachment-aware
motion/depth rejection, variance clipping, reactive history, wind advection in reprojection,
disocclusion, and weather/density changes that require the reconstruction attachments from
`C13-09/10`. Coarse frame-gap, teleport, scene/projection, morph, temporal re-entry, deck, and
multi-deck topology resets are already owned by `C13-05`.

`C13-13` preserves scattering/atmosphere fidelity at lower spatial tiers. Sample count, lighting
octaves, shadow quality, reconstruction quality, and output resolution become explicit independent
budgets rather than one "low means flat" preset.

`C13-36` restores the existing but unwired `jitterEnabled`/`QF_JITTER` contract with a deterministic,
license-free animated interleaved-gradient-noise offset at each pixel's ray start. It remains
separate from the 16-phase reconstruction/update mask and must preserve the adaptive march's
conservative base-density oracle.

`C13-37` removes the axis-aligned periodic lattice from the baked density domain. Increasing texture
resolution is not sufficient because it retains the normalized repeat period. Use planet-stable
regional/local coordinates, seeded rotations, incommensurate scales, and footprint-aware sampling;
apply identical macro shaping to full density and the empty-space oracle. The full-resolution,
temporal-off baked-vs-live periodicity probe is the acceptance authority before temporal history can
hide or amplify the result.

`C13-38` closes the post-`C13-37` cloud-IBL invalidation leak. A cloud appearance revision must not
launch the full environment-cube fill, prefilter, and SH projection while neither the current nor the
previous environment state used the full cloud march. The prior-full-march case remains relevant so
an ON-to-OFF transition still performs its one teardown refresh, and revisions accumulated while
opted out must be consumed when the user later opts in. This is an execution suppression only: it
does not weaken visible clouds, reflection quality, coverage-only IBL, or any public feature.

`C13-39` owns the steady-state ALU regression risk exposed by the post-`C13-37` hot-path review.
`cloudDensityMipLevels()` currently repeats texture-dimension queries and three `log2` evaluations
inside density taps, while cone lighting, shadow, and IBL paths also repeat the same seeded-domain
matrix transforms per sample. Capture the current shader as the baseline, then precompute the common
footprint logarithm, per-domain bias/clamp bundle, and affine rotated ray/basis coordinates at the
widest safe scope. Defer fine/detail work until a sample is actually occupied. Do not change the LOD
curve, sample thresholds, shadow/cascade count, IBL content, LIVE escape route, or morphology to win
timing. Acceptance requires GPU-timestamp lanes for baked, LIVE, single/cascaded shadow, and IBL,
plus visual/morphology equivalence.

`C13-40` owns first-use and lifetime costs. Noise/mipmap construction currently performs synchronous
module/pipeline creation and all bake/downsample passes from the first visible render; reconstruction
passes also need generation-keyed bind-group retention and post-submit retirement. Prewarm
asynchronously after exact formats/topology are known, publish readiness explicitly, and retain the
existing fallback until the new generation is ready. `C13-05` may remove its independently proven
per-frame temporal bind-group allocation now; that bounded hot-path correction does not close this
broader task.

### W3 — Globe-native weather and regional formation variety

`C13-14` is the central weather architecture. Prefer Cesium globe-quadtree ownership with:

- geographic tile keys and level;
- overlap gutters and seam-safe filtering;
- asynchronous source decode/composition/upload;
- explicit logical weather-field ownership versus physical per-device texture realization;
- bounded CPU/GPU caches and post-submit retirement;
- parent fallback and child refinement;
- a stable tile/cell seed domain;
- no draw-time format conversion or network work.

`C13-15` adds a coarse, slowly varying prior. Offline fallback must work without live weather.
Climatology informs probability distributions; it must not pretend to be current observed weather.

`C13-16` converts region and synoptic signals into mixtures of cloud types/decks. Avoid one global
collection genus determining the entire planet.

`C13-17` adds deterministic formation identity and physically bounded parameter variation. Tests must
prove both variety and repeatability.

`C13-18` moves weather in local tangent ENU space. `C13-19` interpolates sparse time slices so hourly
data changes do not pop. `C13-20` defines composition when global, regional, live, cached, and
procedural sources overlap or contain holes.

### W4 — Quality after the architecture is stable

`C13-21` generalizes cloud layers and lighting without returning to one giant global noise field.
Target a small vectorized layer set with per-layer altitude, thickness, density, morphology, phase,
weather mixture, and wind.

`C13-22` evaluates richer Beer shadow-map data, stable cascades/coverage, PCF, and temporal filtering.
The current terrain, aerial, fog, and environment consumers must remain intact.

`C13-23` consumes local weather/density for optical extinction rather than applying only one
collection-level scalar.

`C13-24` exposes one backend-neutral weather owner and facade. Scene code must not import
`Renderer/WebGPU` or branch on `context.isWebGPU`.

### W5 — Transferred bounded features

Open these after their substrate exists:

- `C13-25`: lightning with per-cell phase jitter, a smooth decay envelope, and P95/P99 anti-grid
  analysis. The old median-only gate is forbidden.
- `C13-26`: GRIB2. Keep network/proxy and decoder prerequisites explicit; a mocked fixture is not a
  live-network certification.
- `C13-27`: live EDR validation in a networked browser session.
- `C13-28`: accumulated snow modifies terrain albedo/roughness on both backends, with default-off
  identity and regional placement.

### W6 — Gated architecture/content tail

`C13-29..34` are not automatically activated by launching C13. Each needs its predecessor gate and a
fresh maintainer/orchestrator scope decision. Retaining them here prevents loss; it does not make
them prerequisites for the core planetary/temporal/weather repair.

---

## 7. Gates and acceptance

| Gate | Required evidence | Gate fails when |
| --- | --- | --- |
| `C13-GATE-A` — launch/evidence truth | Clean launch hash; repaired Node/Playwright tour; moving captures at all named viewpoints; exact build/adapter/resolution/tier; clean vs instrumented lanes; counters prove present target sizes and work | A preset is not actually applied, the scene pauses, still images substitute for motion, build identity is ambiguous, or instrumentation changes output |
| `C13-GATE-B` — planetary correctness | WGS84/RTE probes green from ground through orbit; camera motion and teleports do not cause precision swimming; temporal/shadow/mask/capture share the coordinate contract; dateline and poles have no visible seam/pinch; regional bounds place fixtures correctly | Any path returns to raw full-ECEF `f32`, only the primary march is fixed, a regional field is globally stretched, or the seam merely moves |
| `C13-GATE-C` — temporal/performance | Counters prove true 1/16 current work; full-resolution history; depth/velocity/moment topology; disocclusion/cuts/resizes reset safely; moving GPU/CPU evidence beats the half-resolution baseline at comparable quality; no new ghost trails or sparkle | `temporalUpdateFraction` is still only a blend tweak, quality is weakened to win timing, a route segment regresses materially, history smears across cuts, or the performance claim lacks GPU evidence where available |
| `C13-GATE-D` — regional realism | At least four regions show intentionally different distributions; same-region/same-type formations vary; fixed seed is reproducible; changed seed varies; ENU wind direction is geographically correct; time slices interpolate; tile seams/gutters remain invisible | "Variety" is one global noise retune, randomness changes per frame, climatology is presented as live truth, source overlap is undefined, or movement changes direction around the globe incorrectly |
| `C13-EXIT` — certification | Gates A-D green; selected W4/W5 owners complete or explicitly deferred; WebGL billboards and shared globe/Scene APIs green; all existing cloud feature toggles preserved; TypeScript/build/lint/unit tests green; moving golden tour and performance report committed; zero unowned device/validation errors | A feature was removed/default-disabled, WebGL/shared APIs regress, a skip is treated as a pass, cloud history or resource lifetime is unbounded, or a visual/perf claim is not reproducible |

Performance promotion requires a named-stage improvement of at least 5% or more than three times the
measured noise, with no material route-segment regression. Larger architecture work may still be a
valid correctness landing when it truthfully misses that performance banner.

---

## 8. Required verification matrix

Every relevant slice selects from this matrix and names its exact subset in the brief.

| Dimension | Required cases |
| --- | --- |
| Backend | WebGPU volumetric; WebGPU billboard; WebGL billboard; shared Scene/globe API behavior |
| Altitude | ground/horizon; inside; above; regional/continental; orbit |
| Geography | antimeridian east/west crossing; north pole; south pole; equatorial maritime; tropical land; mid-latitude continental; arid; polar |
| Motion | slow pan; fast flight; altitude transition; orbit; teleport; resize; quality-tier change |
| Time/weather | static; advancing time; wind; source revision; missing data; overlapping global/regional sources |
| Cloud content | clear; sparse; overcast; low deck; high ice deck; deep convection; multiple formations of one type; multiple types |
| Consumers | terrain shadow; aerial perspective; volumetric fog; god rays; environment capture; post-process/TAA; picking/depth remains unaffected |
| Lifecycle | enable/disable; source swap; context destroy; device loss; two simultaneous contexts; cache pressure/eviction |
| Performance | clean moving lane; API-instrumented lane; GPU timestamps where available; release/minified confirmation before headline |

Visual acceptance includes temporal sequences, not only final frames. At minimum measure history
error after camera cuts, disocclusion trails, luminance flicker, dateline/pole discontinuity, regional
distribution, same-type variation, and deterministic replay.

---

## 9. Live execution ledger

Status vocabulary: **IN PROGRESS · COMPLETE · PARTIAL / PAUSED · BLOCKED · DEFERRED · CONDITIONAL NOT
TRIGGERED · NOT STARTED**.

A landed slice updates its row in the same commit with premise, files, probe names, before/after
evidence, functionality-preservation result, and rollback boundary. Missing ledger evidence is a
landing defect.

| ID(s) | Status | Evidence / next action |
| --- | --- | --- |
| `C13-00` | **COMPLETE — Batch 732 (`f4a934e606`)** | Explicit 2026-07-23 maintainer launch authority; C11 transfer map and confirmed findings recorded here; TypeScript, Markdown lint, and Prettier passed. Local `main` contains the launch commit. The original HTTP-403 publication note is historical; `origin/main` now reaches Batch 771. |
| `C13-01` | **TOUR GREEN — 12/12 fixtures and 6/6 sequences, Batches 790 (`a9c6c93376`) / 794 (`2263ca6a49`), 2026-08-01. Row stays OPEN on residual bookkeeping only.** *(Header stamped 2026-08-07, close-out docs reconciliation: it read "IN PROGRESS — Batches 733–735 evidence follow-up (2026-07-23)" while this cell's OWN 2026-08-01 addendum, two sentences later, already recorded the executed tour as green — the header and its evidence disagreed. One further reconciliation the header should carry: of the "TWO REAL GAPS pinned by knownGapId ceiling gates" recorded below, **`CLOUD-LOW-COVERAGE-CUTOFF` (the visible-march arm) was RESOLVED at Batch 798 (`c5bd07bf1f`)** — fair-weather skies render, the plains ceiling gate was converted back to a floor and passes, and `DEFERRED_WORK.md` carries the explicit downstream note that this row still lists it as open. **CIRRUS remains the live gap** (`C13-16` per-genus, PARTIAL). Do not re-derive the coverage response or re-flip the plains gate.)* | Batch 733 repaired unified API/config truth, deterministic offline/fixed-time capture, raw-frame metrics, backend-aware workload selection, and the moving WebGPU route. Batch 734 requires an actual procedural execute plus initialized cache/pipeline instead of trusting a loaded lazy handle or fixed warm-up count; the procedural tour now uses same-camera OFF/ON contribution for colored pole/dusk fixtures. The stale `cloudQuality=128` blank claim is superseded by 128/8 full-resolution work with 7,584 cloud cells; Batch 735 supersedes the pre-fix north-pole blank with green WGS84 evidence. Climate/region/type/same-type fixtures, wind/time and temporal-reset sequences, complete per-sequence metrics, and GPU timing remain. **2026-08-01: the Batch-790 fixture/sequence tour EXECUTED (calibration + confirmation runs, both lanes): 12/12 fixtures and 6/6 sequences GREEN. Calibration resolved four first-run reds: a discarded warm-up now precedes the fixture loop (the async prewarm cold-start rendered fixture 1 stable-black at meanLum 3.3 vs 50-145 warmed); the open-cell floor recalibrated 0.03->0.012 against measured 0.017; the clouds-on reset expectation corrected to FRAME_GAP (a full toggle records no deactivation frame; REACTIVATED is reserved for a temporal-tier toggle - open follow-up phase). TWO REAL GAPS pinned by knownGapId ceiling gates that fail loudly when closed: CIRRUS renders ~nothing (C13-16 per-genus unbuilt) and CLOUD-LOW-COVERAGE-CUTOFF (coverage <=0.40 renders zero cloud - isolated single-variable sweep; every fair-weather sky renders clear; DEFERRED_WORK entry with acceptance).** **2026-08-07 (CO-9), residual bookkeeping narrowed to ONE concrete item.** `C13-02` landed the broad counter surface (`getRendererStatistics().volumetricClouds` / `CesiumDebug.cloudStats()`: raymarch target size + pixels dispatched, current/history resolve pixels, bounded primary/light sample budgets, per-frame history accept/reject/reset with reasons, weather cache hits/misses/uploads/bytes, shadow pass count + dimensions, opt-in CPU stage timing, and a CLOUD-SCOPED unique-sample GPU fold riding C11-140s accounting). **The tour does not read any of it yet** — it still reports its own ad-hoc metrics — so this rows outstanding "complete per-sequence metrics, and GPU timing remain" clause is now precisely "wire the fixture/sequence tour onto `volumetricClouds` and re-run", with no missing instrument behind it. That wiring plus the re-run is the first step of the `C13-GATE-A` run; it is NOT done here and this row stays OPEN. |
| `C13-02` | **COMPLETE on the counter surface — LANDED Batch 907 (`ec2c6e9801`), CO-9 close-out lane** *(placeholder "batch number/hash stamped at landing" resolved 2026-08-09, handover audit FIX 18)*. **ONE named residual, recorded rather than folded in: no probe READS the surface yet.** | **Premise re-verified, and the prior cell was right that pass timing alone did not close it.** `C13-39` (Batch 762) landed byte-inert `timestampWrites` on all seven cloud passes plus `DynEnvMap Sky Fill`, which made GPU time *attributable* to the cloud march but said nothing about how many pixels the march dispatched, how much of the frame the cloud lane actually occupied, whether the temporal history accepted or reset, what the weather cache did, or what the CPU spent scheduling any of it. **What landed now** — new `Renderer/WebGPU/WebGPUCloudObservability.ts` (pure, allocation-bounded) plus wiring: (1) `CloudFrameCounters`, one record per context, RESET IN PLACE at the top of every execute — including the frustum-cull early return, so a culled frame reports zeros and bumps `culledFrames` instead of presenting last frame's numbers as current work; (2) `CloudCpuStageAccumulator`, fixed-slot `Float64Array` timing for `total`/`pack`/`shadow`/`temporal`/`composite`, **DISABLED by default** so the shipped path pays no `performance.now()` — that is C13-02's own removability clause, not a perf nicety, and `setEnabled(false)` CLEARS the window so a stale run cannot read as current; (3) `summarizeCloudGpuCoverage`, the **cloud-scoped UNIQUE-SAMPLE fold**. **★ (3) is the load-bearing decision.** C11-140 (Batch 903) established that summing per-pass durations double-counts every nanosecond two passes share and that clamping the ratio hides it; the cloud lane is exactly where that bites (shadow map / cascade atlas / half-res march are separate passes a driver may overlap). So the cloud total is the UNION of the cloud passes' intervals folded through the SAME `summarizeFrameCoverage` the whole-frame ledger uses, `cloudOverlapMs` surfaces the excess, and `WebGPUTimestampProfiler` now RETAINS the frame's raw `TimedPassSample[]` (`latestFrameSamples`) that its own fold already allocated — without that, a scoped consumer has only `lastMs` per pass, i.e. the sum. `DynEnvMap Sky Fill` is a SEPARATE scope, never folded into the cloud union (folding it would attribute a whole sky bake to the cloud lane — the C11-146 attribution error). **Pass accounting rides the ONE `timedCloudPass` seam** every cloud pass already routes through, so counts cannot drift from the encode sites the way seven hand-placed increments could; the spec checks the registry/encode-site correspondence in BOTH directions. **ZERO WGSL, structurally** — C13-39's negative result binds (static register allocation), so the sample figures are explicitly BOUNDED PROXIES (`marchPixels * maxSteps`, `* lightSteps`) and a spec test fails if any cloud `.wgsl` acquires an observability token. **Zero per-frame allocation** (the per-pass array is reused; the spec asserts array identity across a reset) and **byte-inert on the render path** (every edit is bookkeeping alongside numbers the renderer already computed; the temporal verdict is recorded on the SAME branches that maintain the lifetime totals, so the two cannot disagree). **★ WHAT GATE A CAN NOW READ**, exactly: `getRendererStatistics().volumetricClouds` / `CesiumDebug.cloudStats()` publish — `raymarch{width,height,pixelsDispatched,halfResActive,maxSteps,lightSteps,primarySampleBudget,lightSampleBudget}`; `reconstruction{resolveWidth,resolveHeight,resolvePixels,upscalePixels,historyAccepted,historyRejected,historyReset,historyResetReasons,acceptanceThisFrame,lifetime{generation,resetCount,acceptedFrames}}`; `weather{cacheHits,cacheMisses,uploads,uploadBytes,liveBytes}`; `shadow{passCount,size,cascadeSize,cascadeCount}`; `passCount` + the per-label `passes` breakdown; `cpu{enabled,reentries,unmatchedEnds,stages{last,avg,max,samples}}`; and `gpu{clouds,environment}` each carrying `cloudCoveredMs` (union), `cloudSummedMs`, `cloudOverlapMs`, `cloudSpanMs`, `frameSpanMs`, `cloudFrameFraction`, `matchedPassCount`, `missingPassNames`, `invertedSampleCount`. That is the whole of Gate A's "counters prove present target sizes and work" bullet, and the GPU half is falsifiable because it rides C11-140's accounting instead of a parallel one. `gpu` is `null` — never zero-filled — when the adapter lacks `timestamp-query` or no readback has landed, because a zeroed timing block is indistinguishable from a genuinely idle lane. **HONEST SCOPE LIMITS, recorded not hidden:** (a) "weather **tile** cache" has no referent at HEAD — the weather path is a single 256x128 `rgba8unorm` texture, so the counters are texture-level (hit / miss / upload / uploadBytes / liveBytes) and a tile-cache counter arrives with the regional packer (`C13-08`/`C13-14`), not before; (b) the cascade ATLAS is one render pass carrying three viewport-scoped draws, so `shadowPassCount` counts it once and `shadowCascadeCount` carries the tiles — counting three would make the number incomparable with the profiler's pass ledger. **Evidence:** new `Tools/visual-regression/cloud-observability-counters.spec.mjs` **26/26**, pure Node, no browser — the A-group builds a frame whose sum and union differ (7 vs 6 ms) and demands the difference be reported; B2 poisons every numeric field, resets, and requires the ONLY survivors to be the three with a recorded reason, so a per-frame field added later without a reset entry fails there rather than in a probe six batches downstream; the D-group carries **10 source mutants** each proven to break its own check. Full pure-Node lane **2179 pass / 4 fail**, all four the pre-existing bare-worktree `ERR_MODULE_NOT_FOUND` specs (`model-native-pipeline-stage-tax`, `moon-normal-strength-policy`, `moon-webgl-mip-policy`, `webgpu-snap-framebuffer-lifecycle`). `tsc --project packages/engine --noEmit`: 135 `TS2307`, **non-`TS2307` = 0**. Prettier clean on every touched file; ESLint clean per file (engine `.ts` files report "no matching configuration" in a bare worktree, as they do for every batch). `DEBUGGING_GUIDE.md` gains the `cloudStats(t/f)` command row in the same commit, per the sync rule. **THE RESIDUAL (does not block Gate A's INPUT, does block its EVIDENCE):** no probe reads the surface yet, so Gate A is now *satisfiable* but not yet *satisfied* by a recorded run — `probe-cloud-tour.mjs` still reports its own ad-hoc metrics. Wiring the tour (and the moving/instrumented lanes) onto `volumetricClouds` is the Gate-A run's first step and belongs to `C13-01`'s residual bookkeeping, which is where it is filed rather than being silently absorbed here. **Rollback boundary:** `WebGPUCloudObservability.ts`, the `latestFrameSamples` retention + accessor + reset-clear in `WebGPUTimestampProfiler.ts`, the counter fields/reset/record sites in `WebGPUProceduralCloudRenderer.ts`, the `volumetricClouds` block in `WebGPUContext.getRendererStatistics`, `CesiumDebug.cloudStats`, and `cloud-observability-counters.spec.mjs`. |
| `C13-03` | **COMPLETE — Batch 735** | The active WGS84/RTE coordinate contract, f32-faithful source/math suite, and provenance-bearing moving planetary OFF/ON oracle are landed. The default route is green at 21/21 antimeridian, pole, deck/altitude, regional, and orbit checkpoints with clean WebGPU gates. |
| `C13-04` | **COMPLETE — Batch 735 (primary visible shell only)** | Both one-part and high/low branches intersect WGS84 expanded ellipsoids; production precision defaults on, CPU-f64 cartographic height drives interval/deck ordering, and view/light/midpoint height fractions share the oblate boundaries without growing the then-current 148-float uniform. The density-domain work that remained open at this landing later closed under `C13-37`, and temporal RTE/coarse reset later closed under `C13-05`; shadow/mask/capture/atmosphere and regional-weather consumers remain explicitly open under `C13-06..08`. |
| `C13-05` | **COMPLETE — Batch 754 (bounded W1 RTE/reset slice)** | Perspective temporal reprojection now uses inverse-current and previous view-projection-relative-to-eye transforms, CPU-`f64` camera delta, encoded camera origin, and WGS84 configured-deck intersection without rebuilding raw ECEF `f32`. Allocation-free history classification resets on incompatible frame continuity, teleport, scene/projection mode, morph, temporal re-entry, resource resize, deck topology, and observed cull/re-entry; two temporal parity bind groups are retained across frames. The inverse current RTE transform composes the existing inverse projection with translation-free inverse view, while orthographic/morph frames preserve the live current march and remain current-only until reconstruction carries a per-pixel ray origin. Invalid reprojections return before the 3×3 clamp fetch. `cloud-temporal-rte.spec.mjs` passes `11/11`; the complete cloud spec lane passes `64/64`; `probe-cloud-temporal-rte.mjs` is GREEN with 26 finite 60-float uploads, maximum high/low reconstruction error `0.002723 m`, exact/stable source-build provenance, observed cull/re-entry, and clean WebGPU/device/console gates. This does not close color/depth/velocity/moment attachments, true 1/16 current work, or attachment-aware wind/depth/disocclusion rejection; `C13-09/10/12` remain open. |
| `C13-06` | **COMPLETE — promoted 2026-08-07 (Batch 866; closure-audit 4.A verdict, evidence unchanged)** | One frame owner (`WebGPUCloudShadowFrame.ts`) now supplies the cloud beer-shadow projection in CPU `f64`: the footprint centre is a WGS84 geodetic surface projection (was a `6378137` m sphere — up to `21,385 m` off radially at the poles, more than eight deck thicknesses), and both the forward and inverse sun-view matrices are emitted RELATIVE TO A CALLER-SUPPLIED EYE. `cloudShadowMain` reconstructs a camera-relative column, intersects the SAME `cloudShellAxes` expanded-WGS84 boundaries the visible march uses on BOTH its high-precision and explicit-`cloudHighPrecision=false` branches, resolves the height fraction with `ellipsoidShellHeightFractionRTE`, and samples density through the new `cloudDensityRelativeWithFootprint` — which reads the C13-37 CPU-`f64` origin phases, retiring the raw-ECEF `cloudDensityCoordinatesAtWorld` route from the live shadow path and making the previously-scaffolded `cloudDensityCoordinatesAtRelative` / `cloudMorphologyCoordinateAtRelative` live. All three shadow consumers (globe terrain, aerial-perspective inscatter, volumetric-fog hi-fi) now project a camera-relative operand — `v_positionRTE`, `rayDir * eyeDistance`, `froxelOffsetFromCamera(gid)` — instead of `vp * vec4(fullEcefPosition, 1.0)`. `CloudShadowUniforms` stays 20 floats and the cascade stride stays 64 floats: only the matrix SEMANTICS changed. Measured f32 projection error over the footprint: `0.0019 m` at ground / `0.0053 m` at 20 km / `0.029 m` at 800 km / `0.54 m` at 18,000 km, versus a flat `0.259 m` for the retired absolute form — better at every altitude sampled. `cloud-shadow-rte.spec.mjs` passes `17/17` (including a cross-validation of the owner's geodetic projection against `Ellipsoid.WGS84.scaleToGeodeticSurface` to `2.1e-9 m` over 280 lat/lon/altitude samples); the full cloud spec lane is `81/81`; `npx tsc --noEmit` is clean; naga validates all four touched shaders. Feature preservation: no cloud feature removed or default-disabled; the `cloudHighPrecision=false` A/B route and the planar-scene-mode absolute matrix are both retained (a new `cloudShadowCascadeParams.y` flag selects the operand, so 2D/CV/morph are byte-identical). Already-compliant consumers were verified and pinned, not rewritten: the god-ray mask inherits the primary frame through `marchDeck` + the shared per-frame bind group, and environment capture already reads the shared origin phases at a geodetic capture radius. **Not closed here:** the god-ray mask's duplicate full-resolution `marchDeck` (audit 2.9 — needs the `C13-09` reusable-transmittance attachment), and the volumetric fog's DEFAULT local-fbm cloud approximation, which is a different density field with its own sphere and remains `C13-22`/`C13-09` work. Orchestrator probe run 2026-08-01: `probe-cloud-shadows-flagon/cascades/parity` green (cascades PASS explicit, 0 console errors), and the NEW `probe-cloud-shadows-polar.mjs` at 82N shows ground-band delta 37.2 (ON 23.6 / OFF 60.8) with PNGs read — the polar cast shadow the spherical footprint erased is present. Rollback boundary: `WebGPUCloudShadowFrame.ts`, the `cloudShadowMain` body, `cloudDensityRelativeWithFootprint`, the four consumer projection sites, and `cloud-shadow-rte.spec.mjs`. **CAVEAT APPENDED 2026-08-07 — the POLAR-LANE PIXEL EVIDENCE ONLY is demoted to UNPINNED; the promotion stands.** An adversarially-verified audit of `47a940eed9..a6d4b1763a` CONFIRMED that `probe-cloud-shadows-polar.mjs`, as run on 2026-08-01, was the full Batch-855 contamination class and had never been pinned: it loaded `?renderer=webgpu` with no `offline` flag (Ion world imagery + world terrain streaming straight into the scored lower-40% ground band), issued 121 bare `s.render()` calls per capture (which `Scene.js` fills with `JulianDate.now()` — and `requestRenderMode` did NOT neutralize it, because `maximumRenderTimeChange` defaults to 0 and the pinned date differs from wall-clock now by ~46 days), left `cloudWindSpeed` at the 15.0 m/s default with `cloud.time` reaching the density field as `windDirection * windSpeed * time`, left `cloudQuality` at the 64 tier path, and captured **OFF last** — scoring an unpaired absolute `off.mean - on.mean > 0.5` with no control leg. The 37.2 figure above is therefore **not a determinism-grade number and must not be carried forward or compared against**; the probe was re-pinned at Batch 861+ (P1–P8 + P10, read-back enforcement, bracketed `onA → off → onB` determinism control, warm-up discard, exit 0/1/2/3, threshold unchanged at 0.5) and **the orchestrator owes a re-run.** Two honest qualifiers, both recorded because they bound the damage: (1) the bias is unbounded and unmodelled rather than uniformly favourable — `GlobeSurfaceTileProvider`'s base colour has mean 42.5 under this probe's own `(r+g+b)/3` metric, so arctic imagery at 82N/15E can land either side of it; (2) at 82N on 2026-06-21 near local noon the ~31.4° solar elevation gives a ~4.5 km slant path through the 1200–3500 m deck at density 1.0 / coverage 0.7, so a 61% darkening is physically consistent with a genuine cast shadow — the instrument was unsound, the historical verdict probably was not. **THE PROMOTION'S OTHER EVIDENCE IS UNAFFECTED AND STANDS ON ITS OWN:** `cloud-shadow-rte.spec.mjs` 17/17 (including the `2.1e-9 m` geodetic cross-validation over 280 samples), the 81/81 cloud spec lane, clean `tsc`, naga on all four shaders, and the `probe-cloud-shadows-flagon` / `cascades` / `parity` runs. Only the polar-lane pixel claim is demoted, and only until the pinned re-run lands. **DISCHARGED 2026-08-07 (same day): the pinned re-run is GREEN.** On tip `1c3778072d` at 82N: pins read back (imagery 0, ellipsoid terrain, wind 0, quality 32), globe readiness binned=177, control bracket onA 93.778 vs onB 93.778 delta 0.0000 (tol 0.1, time drift none), shadows-ON mean 93.78 vs OFF 99.51 — delta 5.73 against the untouched 0.5 bar over 78,848 samples, exit 0. The polar cast shadow is re-certified by a sound instrument; both bounding qualifiers above resolved as predicted (unsound instrument, sound verdict). C13-06 COMPLETE stands on full evidence. See `C13-WEATHER-PROBE-FLEET-NETWORK-GLOBE` § *UPDATE 2026-08-07* in `DEFERRED_WORK.md` for the mechanism trace and the selector lesson (the Batch-855 sweep was scoped by the `probe-weather-*` FILENAME glob, so a probe of identical shape under a different name was missed). |
| `C13-07` | **COMPLETE — promoted 2026-08-07 (Batch 866). The 855-filed susceptibility of its pixel gate was REAL and the verdict SURVIVED it: `probe-weather-seam-poles` re-ran PASS 3/3 under the pin (Batch 860)** | Bounded global-map seam/pole correction; does **not** replace `C13-14`. Convicted defect, measured on the shipped 256×128 default map: the procedural producer's fBM was aperiodic in `u`, so the two texels that `addressModeU: "repeat"` filters together across ±180° held unrelated values — antimeridian coverage step **max 1.000, mean 0.585** against an interior neighbour step of max 0.336 / mean 0.029, i.e. a full-contrast wall of cloud at the dateline; and both polar rows carried the full 0..1 coverage spread across longitude even though all of those longitudes collapse to one point, so the value at the pole depended on the azimuth of approach. Fix: one shared convention module (`Scene/Weather/WeatherMapSeam.ts`) carrying the texel-centre to lon/lat contract, a lattice-wrapping periodic value-noise fBM, and a wrap-aware latitude-dependent longitudinal low-pass; the default producer moved out of the renderer into `Scene/Weather/ProceduralWeatherMap.ts` so it and `WeatherTexPacker` cannot drift apart; the packer now resamples at texel centres and runs the same polar filter; `worldToWeatherUV` gained an `atan2(0,0)` spin-axis guard. Post-fix antimeridian step **max 0.067, mean 0.010** — below the interior *mean* — both polar rows single-valued, and the low-pass is an exact byte identity below 59° latitude so mid-latitude content and every existing weather probe are unchanged. `Tools/visual-regression/weather-map-seam.spec.mjs` 17/17; engine `tsc --noEmit` adds no error; ESLint and Prettier clean. Orchestrator pixel gate `probe-weather-seam-poles.mjs` green 2026-08-01: on-seam view at 0.7N (both seam sides cloudy in the twin) shows hemisphere halves 52.3/52.9 with the meridian column step (0.6) BELOW the frame's own p95 (1.9); pole sector means show no pinwheel (maxDev 9.4 N / 65.0 S vs means 162.8/115.4) and no atan2 garbage cluster; PNGs read. Probe authoring surfaced three instrument lessons now recorded in DEBUGGING_GUIDE (camera-local cloud patch at orbit altitudes, per-view local-noon clock, async-prewarm warmup discard). Residual, explicitly `C13-14`: equirectangular is still the parameterization, so 59–90° keeps a roughly 4× steeper ground-space gradient than the equator, the polar cap is a hard row mean rather than a real cap tile, and there are still no gutters, bounds, no-data, or LOD. |
| `C13-08` | **COMPLETE — promoted 2026-08-07 (Batch 866): the seven owed browser regressions are all green under the Batch-855 pinning (roster in the Gate B row), which is STRONGER evidence than the pre-pin runs the row originally asked for** | Bounds / no-data / regional packer contract; the LAST Gate B row. Focused cyclic parser/mutation lane 7/7 and parser+bounds lane 38/38. The retained rebuilt Edge regional-tail acceptance is green: both seam sides render continuously with no centre wall or duplicate band, the far view is byte-identical to procedural fill, WebGL billboard pixels are unchanged with zero volumetric publication, error arrays are empty, and all ten PNGs were reviewed. The independent audit also fixed a regional CELL edge-clamp defect with W/E/N/S and diagonal no-data guards. Canonical COMPLETE remains blocked until the five global ingest/source/channel/time probes, seam/poles, and METAR are rerun after that packer change; landing review alone is insufficient. **Premise (confirmed, audit 2.5):** `WeatherTexPacker` assumed every field spanned the texture, so a regional EDR/WCS/METAR field was stretched over the whole planet; `CoverageJsonParser` turned a `null` range value into `0` and `MetarWeatherSource` turned "no station within the influence radius" into `0`, i.e. both fabricated an OBSERVED CLEAR SKY out of an absence of data; and C13-07 explicitly deferred the source-grid coordinate reference to this row. **Contract (one home — `Scene/Weather/WeatherFieldGrid.ts`):** (1) a `WeatherField` grid is NODE-CENTRED (gridline-registered) by default — `bounds.west/north` are the coordinates OF column 0 / row 0 — with a declarable `"cell"` (pixel-registered) alternative; chosen because it is what the shipped packer already did (so the global path stays byte-identical), because CoverageJSON `domain.axes` carry sample COORDINATES and the GFS/GDPS model output behind them is gridline-registered, and because only a node-centred global grid actually has a sample AT the pole, which C13-07's polar low-pass assumes. The TEXTURE stays cell-centred (C13-07); the packer converts between the two registrations explicitly instead of conflating them. (2) A texel is NO-DATA when it falls outside `bounds` or when every contributing cell is no-data (`NaN` always, plus an optional declared `noDataValue`); coverage carries the validity for the whole cell. (3) No-data texels are written from a typed `WeatherNoDataFill` — `"procedural"` by DEFAULT (the same bytes the renderer already shows with no provider, the only fill that is continuous with the no-provider case and cannot be read as an observation) or an explicit `"constant"` quad; precedence packer-option > field > default. (4) Wrap-awareness engages exactly where it is REACHABLE — a cell-registered full-circle field — which is the case C13-07 correctly said did not exist under node registration. **Files:** `Scene/Weather/WeatherFieldGrid.ts` (new, the contract), `WeatherTexPacker.ts` (bounds placement + validity-weighted resample + fill + `packWeatherFieldDetailed` stats), `WeatherTypes.ts` (`registration` / `noDataValue` / `noDataFill` / `priority`, the last DECLARED for `C13-20` and explicitly not yet consumed), `WeatherProvider.ts` (`setNoDataFill` / `getNoDataFill` / `getPackStats`), `CoverageJsonParser.ts` (bounds derived from `domain.axes` with the requested bbox as fallback; `null` → no-data), `MetarWeatherSource.ts` (out-of-radius → no-data), `SyntheticWeatherSource.ts` (honours a regional `request.bounds`), `WeatherMapSeam.ts` (defers to the new module), `scripts/build.js` (public surface). **Evidence:** new `Tools/visual-regression/weather-field-bounds.spec.mjs` 31/31, which carries the pre-C13-08 packer inline as BOTH a byte-identity oracle and a defect oracle — a global field packs byte-for-byte identically to the old code across ramp/rich/uniform/coarse fixtures, while the SAME oracle is shown anchoring a CONUS ramp at lon −180 and reading its midpoint at lon 0 (the stretch), against which the fixed packer changes >50% of texels and fills lon 0 from the procedural map. Antimeridian-straddling bounds (170°E→170°W) pack identically whether east is written `-170°` or `190°`. `weather-map-seam.spec.mjs` still 17/17; the complete cloud spec lane is 132/132 and the cloud+weather lane 176/176; the full pure-Node spec suite is 691/691. Root `npx tsc --noEmit` clean; `tsc --project packages/engine/tsconfig.json` adds no error (its 134 pre-existing `TS2307`s are all missing generated `Shaders/**/*.js` in an unbuilt worktree, none in `Scene/Weather`); Prettier and ESLint clean on every touched file. **Functionality preservation:** the default global node-registered path is byte-identical by test; only the previously incorrect cell-registered outer half-cells change, the procedural producer is untouched, `weatherTexBounds` stays global, and the committed `/mock-edr` + `/mock-wcs` fixtures are asserted to still derive EXACTLY the global rectangle so those probes are unchanged. Two INTENDED behaviour changes, both the point of the row: a CoverageJSON `null` and a METAR out-of-radius cell now render as the procedural fill instead of a fabricated clear hole. **Not closed here:** the observed/fill boundary is a hard one-texel edge (feathering + multi-source priority composition is `C13-20`, which is why `priority` is declared but unread), and there are still no per-tile bounds, gutters, atlas, or LOD (`C13-14`). **Rollback boundary:** `Scene/Weather/WeatherFieldGrid.ts`, the `WeatherTexPacker` body, the four source/provider edits above, and `weather-field-bounds.spec.mjs`. **Orchestrator probe checklist is in the C13-08 section below.** |
| `C13-09` | **INFRASTRUCTURE COMPLETE — LANDED Batch 935 (`3cb301e32d`), CO-23 / Lane C W2 head** *(placeholder "batch number and hash stamped at landing" resolved 2026-08-09, handover audit FIX 18)*. **PRODUCED BUT NOT CONSUMED, BY DESIGN, and OPT-IN DEFAULT-OFF. Edge acceptance EXECUTED GREEN 2026-08-08, Batch 936 (see the stamp at the cell tail).** | **What exists.** The exact attachment topology for current cloud output, as four contract slots in the new pure module `Renderer/WebGPU/WebGPUCloudReconstructionAttachments.ts`: **slot 0 `color`** `rgba16float`, premultiplied radiance + alpha (transmittance `1 - A`) — this is the EXISTING half-resolution march target, NAMED by the contract and deliberately `ownedHere: false` so nothing is allocated twice; **slot 1 `depth`** `rg32float`, R = front (nearest) cloud distance in metres, G = transmittance-weighted mean distance in metres, `-1` = no cloud on this ray; **slot 2 `velocity`** `rgba16float`, RG = current UV minus reprojected previous UV, B = reprojection VALIDITY, A = normalized previous anchor distance; **slot 3 `moments`** `rgba16float`, R/G = mean and mean-square of normalized depth, B/A = mean and mean-square of coverage. The row's "do not rely on only one mean depth for separated overlapping volumes" is answered by carrying front AND weighted depth as separate channels, and by taking the front from the NEAREST composited shell in the multi-deck case. Format choices are decisions, not defaults, and are argued in the module: `rg32float` because a half-float metre at 100 km quantizes to ~50 m — thicker than the deck a disocclusion test has to separate; `rgba16float` velocity because two channels cannot distinguish "did not move" from "could not be reprojected"; normalized moments because raw metre-squared moments saturate half-float at ~255 m. **Owned cost 24 B/texel** (8 B/texel of the 32 B contract is the pre-existing colour target and is excluded from the row's byte figure on purpose). **Lifecycle**: creation, resize, and DEVICE-SWAP recreation keyed on `GPUDevice` IDENTITY rather than size, with a MONOTONIC generation counter that `releaseCloudAttachmentGeneration` never rewinds — that is the property C13-40's post-submit retirement depends on, since a rewound counter would let a retired bind group's key collide with a future one. The generation is committed only after every owned texture exists, so a partially built set can never advertise itself as complete. **Producer**: one new pass, `CloudReconstructionAttachments pass`, encoded BETWEEN the half-res march and `CloudTemporalResolve` (so C13-12 can read the set inside the resolve without reordering anything), writing all three owned targets as MRT from the new `Shaders/WebGPU/Environment/CloudReconstructionAttachments.wgsl`. **★ C13-39 IS ENFORCED MECHANICALLY, NOT BY CONVENTION.** The producer is a SEPARATE WGSL module compiled into a SEPARATE pipeline from that file ALONE — never concatenated with `PROCEDURAL_CLOUDS_SOURCE` — and it re-derives the WGS84 shell intersection rather than importing the march's. `ProceduralClouds.wgsl` is BYTE-UNCHANGED and is pinned by SHA-256 (`8cc74fbb…f28fa1bf`) plus line count (2962) in `cloud-reconstruction-attachments.spec.mjs` F1, which is the row that fails loudly if anyone (including a later C13 row that is not C13-10) puts reconstruction code back into the shared module. **Depth is an ESTIMATOR, and the honest limit is recorded here rather than in a commit message**: it recovers the transmittance-weighted mean the march's own resolved alpha implies over the geometric shell interval (`t0 + 1/sigma - span*(1-a)/a` under uniform extinction), which is strictly more than C13-05's midpoint proxy and strictly less than per-sample accumulation. Per-sample accumulation requires the march to emit it, which requires changing the march, which is **C13-10's** row and carries the interleaved-A/B protocol. There is deliberately no snap-to-`t0` at high alpha — the approach is logarithmic, so a snap would put a tenth-of-a-deck discontinuity exactly at full opacity; the transmittance is floored instead, and the CPU twin and the WGSL are pinned as the same expression character for character. **Counters (Gate A)**: `CLOUD_TIMED_PASS_NAMES` grows to eight (add-only); new per-frame `attachmentWidth`/`Height`/`Pixels`/`Count`/`Generation` plus RESIDENT `attachmentLiveBytes` (survives the per-frame reset for the same reason `weatherLiveBytes` does), published under `reconstruction.attachments` together with the per-slot contract, and surfaced by `CesiumDebug.cloudStats()` and the new `CesiumDebug.cloudReconstructionAttachments(t/f)`. **DEFAULT OFF is a correctness claim, not caution**: with the flag clear nothing allocates and no pass is encoded, so the shipped cloud lane is identical in pixels AND in cost; with it on the producer runs and the counters report, but the composite is byte-identical either way because NOTHING READS THE SET. **Evidence**: `cloud-reconstruction-attachments.spec.mjs` 40/40 (contract, lifecycle executed over create/resize/device-swap/release, the estimator over its whole domain, uniform packing offsets, the pass-topology and composite-unchanged pins with mutants, the C13-39 hash pin, naga on the producer AND on the untouched march); the full cloud spec lane is 303/303 (`cloud-observability-counters.spec.mjs` 26/26 with the new fields, `cloud-temporal-rte.spec.mjs` re-pinned at the extracted helper); `npx tsc --noEmit` in `packages/engine` has ZERO non-`TS2307` errors (the one new `TS2307` is the unbuilt generated shader `.js`, the same class as the other 136); prettier and eslint clean. **One shared refactor**: the `inverse(P * Vrot) = inverse(Vrot) * inverse(P)` composition C13-05 used inline is now `resolveCloudInverseCurrentVpRte`, consumed by BOTH the temporal resolve and the producer — a second copy of an RTE inversion is exactly the drift C13-03's contract exists to prevent. `cloud-temporal-rte.spec.mjs` pins it at the one site and additionally rejects a general `Matrix4.inverse`. **What this does NOT close.** No consumer exists: attachment-aware motion/depth rejection, variance clipping from the moment pair, reactive history, wind advection in reprojection, and disocclusion are `C13-12`; true 1/16-rate current work and the march-emitted accumulated depth are `C13-10`. Orthographic and morph frames still produce NO attachments, because the producer needs a usable inverse current view-projection-relative-to-eye for its per-pixel ray — the same residual C13-05 recorded, closing when reconstruction carries a per-pixel ray origin. The full-resolution tier produces nothing either (there is no intermediate march target to derive from), which is correct given C13-10 makes the reconstruction resolution the march resolution. **OWED EDGE ACCEPTANCE (orchestrator, machine lane):** two legs at `cloudVolumetricQuality` "low" or "medium" — (a) DEFAULT, attachments off: cloud output byte-identical to the pre-change build and `reconstruction.attachments.liveBytes === 0`; (b) `CesiumDebug.cloudReconstructionAttachments(true)`: cloud output STILL byte-identical to leg (a), with `produced === true`, `targetCount === 3`, `generation >= 1`, `liveBytes === width*height*24`, and `passes["CloudReconstructionAttachments pass"] === 1`. **Rollback boundary**: `WebGPUCloudReconstructionAttachments.ts`, `CloudReconstructionAttachments.wgsl`, the attachment cache fields / `ensureCloudAttachmentResources` / `releaseCloudAttachmentResources` / producer encode block / `setCloudReconstructionAttachments` in `WebGPUProceduralCloudRenderer.ts`, the attachment counters in `WebGPUCloudObservability.ts`, `CesiumDebug.cloudReconstructionAttachments`, and `cloud-reconstruction-attachments.spec.mjs`. **★ EDGE ACCEPTANCE EXECUTED GREEN — 2026-08-08, Batch 936: `Tools/visual-regression/probe-cloud-reconstruction-attachments.mjs` PASS exit 0, 17/17 predicates.** Active window: toggle → produced within 1 frame, targets 3, generation 1, liveBytes exactly 400×250×24, pass encoded once. Byte identity: same-page OFF→ON→OFF element screenshots share a single SHA-256 — WITH the march dispatching every frame, so the producer ran LIVE through all three captures, a STRONGER read than the pre-registered form. Self-healing release: freed within its bounded window on disable. Resize rider: 800×500→1024×640→800×500 gives generation 1→2→3 (monotonic, never rewound) with liveBytes exactly w×h×24 at each size. The leg-(a) 'byte-identical to the pre-change build' wording proved PHYSICALLY UNTESTABLE as written: same-build cross-page captures differ 37.26% because temporal history freezes initialization variance into per-page fixed points (discriminator pair: two same-build pages differ; in-page captures 0/1/24 frames apart are identical). The arm was discharged as a cross-build pixel diff against a CLEAN Batch-934 build — 30.69% mismatch ≤ 74.53% bound (max(2×same-build floor, 0.1%), FIRST-PASS DERIVED), i.e. BELOW the same-build noise floor: unchanged within instrument resolution — plus the F2/F3 structural pins. Instrument findings banked for the fleet: (i) in-page drawImage from a WebGPU canvas copies transparent black even in the same task — canvas pixels MUST come from Playwright element screenshots; (ii) the published cloud counters lag cache truth by 1–2 frames (resident figures publish at frame start) — the probe reads through a bounded consistency window and records the latency; (iii) readiness must gate on marchPixels>0, not fixed warm-up counts (the async noise bake races them); (iv) an IN-REPO worktree resolves `@cesium/engine` through the MAIN tree's node_modules workspace symlink — the pre-change build was contaminated until the worktree got local `node_modules/@cesium` junctions (bundle then verified 0 matches for the debug command). OPEN OBSERVATION (watch, not a defect claim): one discarded instrument iteration saw a page where 28 flag-on frames after ~170 steady frames produced nothing until a resize; the certified design did not reproduce it. If a C13-10/12 consumer ever sees a produced=false window with the flag on, start there. **★ SURVIVAL-PROBE WINDOWS RE-DERIVED — 2026-08-09 (CO-32). INSTRUMENTS ONLY; NOT RE-RUN.** The three windows that went red on the resolve-alive build — `aLiveBytesZeroBeforeToggle`, `bLiveBytesExact`, `selfHealingReleaseOnDisable` — were NOT the subject failing. `?offline=true` resolves through `Apps/CesiumViewer/CesiumViewerStartupOptions.js` to **`requestRenderMode: true`**, `maximumRenderTimeChange` keeps its `0.0` default, and this probe pins the clock; Batch 942 restored the temporal resolve, the cloud lane now converges, and a converged lane makes `scene.render(t)` execute NOTHING — no `executeProceduralClouds`, no `resetCloudFrameCounters`, no new published snapshot. This probe's windows counted RENDER CALLS, so the 4-call publish-lag window and the 8-call release window both expired against a FROZEN snapshot. THE FROZEN SNAPSHOT IS VISIBLE IN THE 2026-08-08 REPORT: `active`, `heal`, `settle`, `onSettled` and `offSettled` all return byte-identical counters (`produced:true, 400x250, gen 1, liveBytes 0, passCount 1, marchPixels 100000`) — including `offSettled`, whose flag was OFF — and only the resize rider reads live values, because a viewport change is the one thing that woke the scene. **★ AND IT MEANS `settledByteIdentityOffOn` / `OnOff` WERE GREEN VACUOUSLY**: `c1 === c2 === c3` because nothing rendered between those captures. **★ THE ROW'S OWN "OPEN OBSERVATION" ABOVE IS HEREBY EXPLAINED AND CLOSED**: "28 flag-on frames after ~170 steady frames produced nothing until a resize" is this mechanism exactly — a refresh-skipped lane plus the one event that wakes it. It was never an engine defect, and a C13-10/12 consumer seeing `produced=false` with the flag on should start at the render gate, not at the producer. **What changed (all in `Tools/visual-regression/lib/cloud-refresh-skip.mjs` + this probe):** (i) KEEP-LIVE — `scene.requestRender()` immediately before every `scene.render()`, with the premise READ BACK (`requestRenderMode`, `snapshotMode.enabled/isFrozen`) and a `KEEP-LIVE PREMISE BROKEN` structural reason if snapshot mode is ever on, because `markDirty`-on-frozen is `requestRender()`'s only effect beyond the gate flag; (ii) EXECUTE-COUNTED BOUNDS on `volumetricClouds.frames` — `produced` 60 (unchanged), `released` 8 calls → **12 executes** (the self-heal fires during pack on the first execute after the flag clears and the resident figure publishes within the measured 1-2 execute lag, so 4 suffice and 12 is 3x), `settled` 300 calls → **60 executes** (that condition is UNREACHABLE in this fixture — the half-res march dispatches 100,000 px on every execute in every run on record — so the phase is an honest bounded warm-up and the real convergence proof is the gate that follows), plus a new `resized` at 30; (iii) BOUNDED STATIONARITY ROUNDS with an **ANTI-VACUITY** check — byte-equality across zero executes is not evidence, and the fold names it; (iv) the same gate applied to the **same-build REFERENCE**, because an unconverged reference INFLATES the noise floor and the cross-build bound is `max(2 x floor, 0.1%)` — a sloppy reference made the pre-change arm EASIER to pass; (v) the resize rider's arrival moved from `produced` (which the PRE-resize state always satisfied — it passed by luck, its frozen `liveBytes === 0` failing coherence and forcing two more frames) to `resized`, which requires the dims to differ from the pre-resize dims AND the generation to exceed the pre-resize generation, with a `RESIZE BASELINE DEGENERATE` reason if that baseline is zeroed, and the re-enable window widened 1 → 4 executes so the baseline is a frame the producer actually ran. **PREDICATE MEANINGS ARE UNCHANGED** and pinned character-for-character in the spec: a counter that never publishes still fails at its bound. RECORDED, NOT CHANGED: `aLiveBytesZeroBeforeToggle` reads the post-disable HEAL window and is expression-identical to `selfHealingReleaseOnDisable` — the name misdescribes it, but a pre-registered predicate is not renamed mid-lane. **Gates:** new `cloud-refresh-skip.spec.mjs` 22/22 (pure repair + mutants + source pins on BOTH probes), `cloud-reconstruction-attachments.spec.mjs` unchanged and green, full `cloud-*.spec.mjs` lane 388/388, `probe-fleet-contract.spec.mjs` green with the probe unallowlisted, prettier + eslint clean. **PRE-REGISTERED FOR THE NEXT RUN (no PRE_BASE):** exit **3**, every predicate GREEN including the three that were red, the sole structural reason being the `PRE-CHANGE ARM SKIPPED-BY-CONFIGURATION` line and `crossBuildDiffWithinDerivedBound: null`. Deviations and what each would mean: `bLiveBytesExact` still red ⇒ a REAL resident-publish defect (the window is now real executes); `settledByteIdentityOffOn/OnOff` red ⇒ the C13-09 pixel-inertness claim genuinely broke under the restored resolve, since those greens were previously vacuous and are now producer-live; any `REFRESH-SKIP STARVED` line ⇒ keep-live itself did not take and NOTHING was observed in that window. **★ THE VISUAL ARM REDESIGNED — 2026-08-09 (CO-33). INSTRUMENTS ONLY; NOT RE-RUN. ★ TWO PREDICATE MEANINGS CHANGED — do not compare a new run's visual verdicts against an older run's without reading this.** The Batch-953 run did exactly what CO-32 built it to do and returned a verdict nobody had planned for: every counter and lifecycle predicate GREEN (all three CO-32 targets fixed, now on real executes), and `settledByteIdentityOffOn` / `OnOff` RED **on a stationarity precondition the same output declared broken** — 8 rounds of 8 real executes never produced two byte-equal captures, and the same-build cross-page floor read 29.2%. **(1) THE INSTRUMENT GAP, CLOSED.** A predicate that fires red on its own failed precondition is an instrument fault: the C13-09 subject was never given a chance to pass or fail, and that RED would have been read as a C13-09 regression. Both identity predicates are now TRI-STATE — meaning UNCHANGED where they apply (two byte-stationary captures must be byte-equal, and two settled fixed points that differ is still a real difference — both executed as tests), `null` + STRUCTURAL-BY-NAME where the precondition fails, with `null` routed to structural through a new NOT-RUN fold so it can never read green. That is the consume probe's own pattern, now shared. **(2) WHY THIS IS A REDESIGN AND NOT A GATE.** Under keep-live the frame is genuinely non-stationary: a jittered half-resolution march feeding a temporal resolve ORBITS, it does not converge, so byte identity tests a property the subject does not have. And the historical greens came from FROZEN frames — so this arm has been either vacuously green or structurally red and has **never once measured pixel-inertness on an executing frame**. **THE REPLACEMENT — PAIRED SAME-STATE FLUCTUATION BANDS** (`Tools/visual-regression/lib/cloud-refresh-skip.mjs` §3 is the authority). Each state's own consecutive captures — separated by REAL executes, the last 6 retained because a gate's early rounds are still on the convergence curve and the tail is the orbit — give its frame-to-frame diff distribution; a cross-state comparison is judged against the POOLED band of BOTH endpoint states, because the diff has one endpoint in each. **THE BAND RULE, FIRST-PASS DERIVED AND LABELLED EVERYWHERE IT PRINTS: `upper = max + (max − min) + floor`** — one observed range of headroom above the observed maximum, because at n≈5 the sample range is a biased-low estimate of the population range and one more range is the minimal non-parametric extrapolation that assumes no distribution. Floors: 0.001 for the two fraction statistics (this probe's own long-standing 0.1%-of-pixels absolute floor, reused so the two arms cannot drift) and 1/255 for magnitude (the 8-bit quantum of the captures being compared). **REJECTED AND RECORDED:** a fixed percentage (nobody derived it — the exact failure the consume probe's D3 guard exists to prevent, and worse here since it would sit on a 30% floor); mean + kσ (σ is noise at n≈5 and it imports a normality assumption with no evidence — the range rule uses only order statistics); max alone (zero headroom, so the subject's own next frame refutes it); a cross-PAGE floor as a same-page baseline (per-page fixed points differ permanently, so that band measures page identity, not the flag); restoring stationarity by dropping keep-live (that is the B952 defect deliberately reintroduced, and it is where the vacuous greens came from); averaging N frames per state (discards the spread the band needs to calibrate itself, compares an image no viewer ever sees, and with an orbit of unknown period estimates nothing stable); and a PER-PIXEL MIN/MAX ENVELOPE (genuinely stronger, and NAMED AS THE NEXT STEP if this pass's detection limit proves too coarse — rejected for this pass because an n-sample envelope under-covers the fluctuation by an unknown amount and therefore needs this same paired calibration anyway, while multiplying the retained-image budget by the number of states). **THREE STATISTICS OUT OF ONE PASS** — `mismatchFraction` (footprint of any difference; saturated at ~30% by dither), `coarseFraction` (footprint above 7/255, i.e. above the dither amplitude), `meanNormalizedDelta` (magnitude, ignoring footprint): a band on the saturated footprint alone would admit almost any change that does not restructure the image. INSIDE requires all three; OUTSIDE names which exceeded, with its value and its bound. **ANTI-VACUITY IN BOTH DIRECTIONS, both NAMED, neither green:** a zero-execute sample is not merely uninformative but ACTIVELY PERMISSIVE (its diff is 0, which drags the minimum down, which WIDENS the band), so it poisons the whole band rather than being averaged away; fewer than 3 pairwise diffs is UNDERSAMPLED; and — the second vacuity mode, which is the part that makes an INSIDE verdict mean anything — every band is calibrated IN-RUN by an injected CONTROL LADDER (5 declared rungs trading area against amplitude, ascending in nominal mean delta, applied to the SECOND image of a REAL same-state pair so the control carries the same fluctuation baseline a cross-state diff does, and signed away from the clamp so every touched pixel really moves by its declared amplitude). The lowest rung the band rejects is the run's **DETECTION LIMIT**, printed beside the verdict; a band that rejects NOTHING is `BAND NOT DISCRIMINATING` and yields `null`. That converts "within noise" into a measurement with a stated sensitivity. **(3) TIERS.** `foldPixelInertness` picks stationary > band > none — byte identity is DEMOTED to the regime where it applies, not abandoned. NEW `pixelInertOffOnWithinBand` / `pixelInertOnOffWithinBand` are the live-frame form and are RED-CAPABLE: an OUTSIDE verdict is a C13-09 finding and is deliberately NOT laundered into a structural note (executed as a test, the same rule `starvedWindowReasons` enforces one tier up). **(4) COLLATERAL, all consequences of the same redesign:** C2 and C3 are now converged-and-sampled through the same gate C1 always used (a bare screenshot endpoint has no fluctuation samples AND is a point on whatever curve it happens to be on — the defect that produced the consume probe's uninterpretable 34.5%); the cross-build arm is now judged against a CROSS-PAGE band with its own ladder, replacing `max(2 × floor, 0.1%)` over a single reading, which at the measured 29.2% floor was a **58% bound that no build difference could ever have exceeded** (the floor is still reported — it just no longer IS the bound); a non-stationary state is no longer pushed to structural on its own, because under keep-live it is the EXPECTED regime, but its reason is QUOTED inside any comparison that ends with no usable tier; and the pixel-comparison kernel is now marker-delimited and pinned BYTE-IDENTICAL across both probes, since a silent divergence would leave their bands incomparable while every printed number still looked plausible. **Gates:** `cloud-refresh-skip.spec.mjs` 22 → **39** (new sections G+H: the band rule executed against its rejected alternatives, all four anti-vacuity modes, the ladder's ordering/geometry/clamp bound, the detection-limit fold, the tier fold, the "a red band verdict is never structural" rule, and — the brief's requirement — **MUTANTS BOTH WAYS**: a within-band fluctuation must NOT fail, an injected real change beyond the band MUST, and an uncalibrated band must not manufacture a green; plus source pins on both probes for sampling, capping, pooling, the tri-state predicates and the retired doubled-single-reading bound); `cloud-reconstruction-consume-probe.spec.mjs` 30 → 31; `cloud-reconstruction-attachments.spec.mjs` unchanged and green; full `cloud-*.spec.mjs` lane 388 → **406/406**; `probe-fleet-contract.spec.mjs` 32/32 with NEITHER probe allowlisted; prettier + eslint clean; CRLF preserved. **PRE-REGISTERED FOR THE NEXT RUN (no PRE_BASE):** exit **3**, with `settledByteIdentityOffOn/OnOff` **NOT RUN** (null — the live regime), `pixelInertOffOnWithinBand` / `OnOffWithinBand` **GREEN**, every counter and lifecycle predicate green, and the structural list limited to the PRE-CHANGE-ARM-SKIPPED line plus the two NOT-RUN identity predicates. What a deviation means: a band predicate **RED** ⇒ C13-09's pixel-inertness claim genuinely breaks under the restored resolve, at the printed detection limit — the first time this arm has ever been able to say that, and it must be re-filed with the exceeding statistic and the limit quoted. **★ READ THE `MARGINAL` FLAG BEFORE FILING ANY SUCH RED.** A verdict whose every exceedance is SMALLER than the smallest perturbation the band was demonstrated to reject is printed `★ MARGINAL` (computed, not judged: the exceeded value is compared against the detection-limit rung's own value for that statistic). The verdict is NOT softened — the band said OUTSIDE and that stands — but a two-percent exceedance on the saturated footprint statistic alone, with the coarse and magnitude statistics comfortably inside, is a difference at the instrument's demonstrated resolution and is a BAND-WIDTH question, not a C13-09 regression. Treating a hairline number as a finding is the specific failure this lane already paid for at Batch 944 (the tier round-trip) and Batch 953 (these very predicates); `BAND NOT DISCRIMINATING` ⇒ the ladder is too coarse for this fixture's fluctuation and the ENVELOPE design named above is the next instrument step (NOT a subject finding); `BAND VACUOUS` or any `REFRESH-SKIP STARVED` ⇒ keep-live stopped taking (instrument); and `settledByteIdentity*` coming back GREEN ⇒ the lane converged after all, which would be a real change in the subject and must be reconciled against the Batch-953 non-convergence before anything is claimed from it. |
| `C13-10` | **FIRST SLICE LANDED — 2026-08-08, Batch 939 (CO-25). ROW STAYS OPEN: still half-resolution, not 1/16; history still half-res.** ~~Edge legs 0/A/B/C/D + the Karma `WebGPUShaderDefines` run OWED to the machine lane.~~ **⚠ OWED LIST RECONCILED 2026-08-09 (handover audit FIX 17) against this cell's own later stamps — the header listed all five legs as owed while the cell tail records three of them discharged. ACTUALLY OWED: (a) leg 0** — default byte-identity + `emission` — **not yet re-run at HEAD; (b) leg A** — C13-09's attachments-only inertness (`producerTargets === 3`, composite byte-identical) — **MARGINAL, not clean: at Batch 955 (`298af6a6d6`) the survival run exits 1 with `pixelInertOffOnWithinBand`/`OnOffWithinBand` RED at 100.8%/109.5% of bound, and the NAMED CONFOUND is time-asymmetric capture pairing, so the honest verdict is "QUESTIONED AT THE MARGIN, not established broken"; the next dispatch is the time-symmetric band and/or the CO-33 per-pixel min/max envelope, and only after that instrument does a marginal excess become a product claim; (c) the Karma `WebGPUShaderDefines` run** — still owed in full. **DISCHARGED — do not re-run as owed: legs B, C and D all green at Batch 955 (`298af6a6d6`), the CONSUME `--perf` probe's first full pass (exit 0)** — leg B core 15/15 with `producerTargets` measured 3→2 in one page, leg C's tier round-trip CLOSED-NEGATIVE, leg D reproduced (march B−A +0.9678 ms vs +0.841; producer −0.0055 ms cheaper twice; drift control clean twice) and the visual arm calibrated (same-state control inside at 96.1%, attachments→consume outside at 198.8% coarse — the first consumer's pixel change is REAL and quantified, which the row permits). | **What landed.** (1) The MARCH EMISSION VARIANT — the C13-39 resolution: two new add-only `ShaderDefineHi` bits (bit 4 `CLOUD_MARCH_EMIT_RECONSTRUCTION`, bit 5 `CLOUD_RECONSTRUCTION_CONSUME`, kept separate so C13-09's produced-but-not-consumed state stays A/B-testable). Under bit 4 `marchDeck` accumulates front distance + `Σwᵢ·tᵢ` using the march's OWN sampleWeight (`Σwᵢ = alpha` by construction, so the weighted mean is the plain quotient — an ACCUMULATION, not C13-09's uniform-extinction estimator), and the march writes contract slot 1 directly as a second colour attachment of the existing half-res pass — one pass, one traversal; the producer then READS it and drops depth from its MRT (`producerTargets` 3 → 2, the counter that proves ownership MOVED). Owned bytes stay 24 B/texel. ONLY the half-res march pipeline compiles the bit; the full-res march, beer-shadow map, cascade atlas and god-ray mask compile `PROCEDURAL_CLOUDS_SOURCE` verbatim, and every variant pipeline is a separate lazily-built object beside the historical one — a runtime flip never rebuilds a shipped pipeline. (2) The CONSUMER'S FIRST SLICE with the C13-12 boundary drawn from the ledger's own words (§6 W2: C13-12 owns every THRESHOLDED test): C13-10 owns the READS and non-parametric validity — true weighted-depth anchor (shell-midpoint proxy retired inside the variant), producer motion vector reused (no second transform, the C13-03 drift rule), history refused on malformed moments / empty 3×3 neighbourhood (proven exactly output-equivalent) / producer validity 0 / depth sentinel / off-screen motion. Spec D4 makes the boundary ENFORCEABLE: any numeric literal in the consume-only lines that is not a bound, a binding index, or rgba16float's own quantum FAILS in this row's spec — a C13-12 threshold cannot land here silently. (3) DEFAULT BYTE-IDENTICAL, STRUCTURALLY: `preprocess(march, 0, 0)` is code-identical to the pre-C13-10 module (1798 code lines; only comments differ). THE SHA PIN WAS UPDATED DELIBERATELY per F1's own note: raw `ProceduralClouds.wgsl` now `74affeac…eb30173` / 3105 lines, and F1 SPLIT into F1a (raw-file pin) + F1b (pins what actually bounds the register footprint: the default variant's non-comment text byte-equal to pre-change, plus emit-variant-not-a-no-op); producer + resolve pinned too. (4) Counters add-only under `reconstruction.emission` (`requested` RESIDENT by design) + `CesiumDebug.cloudReconstruction(t/f)` (guide row added at landing — the worker had omitted it). **Cross-validation worth keeping:** on uniform extinction the accumulation agrees with C13-09's estimator to <0.5% of the interval (the estimator's exact domain); on front- vs back-loaded decks of EQUAL alpha it separates them by >50% of the deck where the estimator reads them within 5% — C13-09's recorded limit, executed as a test. **Collateral, one shared home:** three shaders gaining `//>>ifdef` broke seven raw-text naga specs; fixed via new `lib/wgsl-variant.mjs` exposing the ENGINE'S OWN preprocess (and the hand-rolled `expandZeroDefines` in `cloud-shadow-rte.spec.mjs` retired into it). **Register-footprint expectations are DERIVED ONLY** (emitting march +~4 live f32; emitting producer expected CHEAPER — skips up to 6 `rayEllipsoidIntersectRTE` calls and nine log-bearing estimator evaluations for ten rg32float loads) — every occupancy claim is owed to the interleaved-A/B protocol (C13-39), not asserted. **Evidence:** `cloud-march-emission.spec.mjs` 30/30 (variant/C13-39 pins, emission math + CPU twin, shader text + estimator-reinstatement mutant, consumer + boundary scan, wiring/lifetime/counters, naga ×6 preprocessed forms + bounded loops); `cloud-reconstruction-attachments.spec.mjs` 41→42 green; cloud/weather/eclipse lane + pipeline-key-aliasing 608/608; tsc 0 non-TS2307. **OWED EDGE LEGS (machine lane):** Leg 0 default byte-identical + `emission.requested === false`; Leg A attachments-only — C13-09's leg (b) MUST SURVIVE C13-10 (`producerTargets === 3`, composite byte-identical); Leg B `cloudReconstruction(true)` — `requested/emitted/consumed true`, `producerTargets === 2`, liveBytes UNCHANGED, pass count unchanged, composite MAY differ (read the PNGs — first consumer); Leg C lifecycle (resize / tier flip / device loss; generation monotonic; consumed back within a frame); Leg D perf, INTERLEAVED A/B MANDATORY, reporting the march and producer passes separately. Plus `gulp test --includeName WebGPUShaderDefines` (spec grew 4 → 6 pins). **★ CONSUME FIRST RUN — 2026-08-09, Batch 944 (`probe-cloud-reconstruction-consume.mjs --perf`, exit 1):** LEG B CORE GREEN 15/15 — requested/emitted/consumed true within 1 frame, producerTargets measured 3→2 IN ONE PAGE (a move, not an assertion), liveBytes 2,400,000 in BOTH flag states, generation unchanged across the flip, pass topology unchanged (4→4; march/producer/resolve/upscale each 1→1), zeroErrors green (the Batch-942 raw-compile fix holding). VISUAL NUMBERS NOT INTERPRETABLE THIS RUN: off→attachments read 34.5% with a 0.0000% same-state floor — the off capture was taken ~21 frames in (mid-convergence) while the floor pair was taken converged; each phase needs its own stationarity gate before capture (probe fix). LEG C: 2 reds are a probe defect (the resize never took — dims stayed 400×250, gen stayed 1); 1 red is a LIKELY-REAL FINDING: after a low→high→low tier round-trip, emitted/consumed did NOT re-engage within 90 frames — suspect the tier flip recreates the cloud cache and drops attachmentsEnabled/reconstructionEnabled, against B935's stash-the-request intent. FILED: C13-10-TIER-ROUNDTRIP-REENGAGEMENT (needs an engine-side lifecycle check before any fix). LEG D STRUCTURAL BY NAME (correct behavior, no false green): all windows produced zero usable pass timings AND only 5/10 windows held their declared arm across verify frames — both consistent with the refresh-skip idling the lane during measure spans (and/or the timestamp surface not armed); the perf leg needs a keep-live mechanism (per-frame invalidation) plus explicit gpuPassCost arming before any occupancy number exists. THE C13-39 REGISTER QUESTION REMAINS OPEN — no timing was produced, none is quoted. Probe-fix queue: per-phase stationarity gates, resize plumbing, leg-D keep-live + timing arming; plus the survival probe's 3 window reds (same refresh-skip class, recorded at Batch 942). **★ ALL FOUR PROBE FIXES LANDED — 2026-08-09 (CO-32). INSTRUMENTS ONLY; NOT RE-RUN (no Edge in that lane).** They are ONE mechanism, now given a shared enforceable home in `Tools/visual-regression/lib/cloud-refresh-skip.mjs`: `?offline=true` resolves through `Apps/CesiumViewer/CesiumViewerStartupOptions.js` to **`requestRenderMode: true`**, `Scene#maximumRenderTimeChange` keeps its `0.0` default, and the probe pins the clock — so once Batch 942 restored the temporal resolve and the cloud lane converged, `scene.render(t)` executed NOTHING: `shouldRender` false, no `executeProceduralClouds`, no `resetCloudFrameCounters`, no new published snapshot. Every window counted RENDER CALLS, and render calls had stopped being frames. The B944 report proves it from inside: all ten leg-D windows report `attemptedFrameCount: 0` while `support.enabled` is true (the profiler was armed and never saw a frame), and exactly the five A-windows "held their arm" because the FROZEN snapshot was itself an A-state snapshot — `emission.requested` reads `false` in the B-windows, i.e. `cloudReconstruction(true)` had been called and never reached an execute. Neither debug toggle calls `scene.requestRender()`, which is why a flag flip alone could not wake the lane. **(1) KEEP-LIVE.** `scene.requestRender()` immediately before every `scene.render()`, in every span of both probes. DERIVATION: it sets `_renderRequested` and — only when snapshot mode is FROZEN — marks that snapshot dirty; `SnapshotModeService` is disabled by default and both probes now READ THE PREMISE BACK (`requestRenderMode`, `snapshotMode.enabled/isFrozen`) and emit `KEEP-LIVE PREMISE BROKEN` if it ever stops holding. The flag reaches no uniform, no pipeline key and no pass-encode decision, so the SET of GPU passes encoded on a rendered frame is unchanged — keep-live changes only WHETHER the frame renders, never WHAT it renders. It is gate-identical to the private `scene.forceRender(t)` (`_renderRequested = true; this.render(time)`) and to what the shipped `CesiumWidget` loop does at 60 Hz outside `requestRenderMode`. REJECTED AND RECORDED: a camera nudge (changes the view matrix, the motion vectors and the history reprojection — precisely what is being timed); a clock advance (`cloud.time` enters the density field as `windDirection * windSpeed * time`, and it breaks the pinned-clock rule); `scene.requestRenderMode = false` (gate-equivalent, but a global fixture change that outlives the span that needed it). **(2) EXECUTE-COUNTED BOUNDS.** Every budget is denominated in `volumetricClouds.frames` — the counter `resetCloudFrameCounters` bumps at the top of every execute, ahead of the frustum-cull return. The render-call budget survives only as a loop bound (`max(8, 4x)`; the 4x is margin for the ONE observed call that yields no execute, the frame after a viewport change, where `Viewer.prototype.resize` runs on `scene.postUpdate` OUTSIDE the `shouldRender` block and requests the NEXT frame). Two outcomes are kept DISTINCT: **EXPIRED** (the budget of real executes was spent and the condition never held — a genuine negative that belongs to a predicate) versus **STARVED** (render calls spent without earning executes — structural, nothing was observed either way). Collapsing them is how a finding gets manufactured. **(3) PER-PHASE STATIONARITY GATES.** Each captured state (off / attachments-only / consume / consume+8) converges under its OWN bounded gate — 8 executes per round, ≤8 rounds, byte-equality OBSERVED not assumed — before its screenshot counts, and each recorded diff now carries an `interpretable` flag that is true only when BOTH endpoints converged. That is exactly the B944 34.5%: a converged attachments capture minus an `off` capture taken ~21 executes into convergence. Non-convergence is NAMED per state, never silently averaged. **(4) RESIZE — THE B944 DIAGNOSIS IS REFUTED BY ITS OWN REPORT.** "The resize never took" is wrong: `canvasSize` read `1024x640` while the attachments stayed `400x250` at generation 1. The viewport change DID reach the canvas. What failed is that `until: "consumed"` was ALREADY TRUE in the pre-resize state AND its coherence check was ALREADY SATISFIED by the pre-resize numbers (`400x250`, `liveBytes 2,400,000`, self-consistent), so the phase returned after ONE render call carrying a stale snapshot. The survival probe's rider "worked" only by luck — its frozen state had `liveBytes === 0`, which failed coherence and forced two more frames. REPAIR: wait (bounded, kept live) for the canvas backing store to CHANGE, then open a `until: "resized"` window whose arrival requires the attachment dims to DIFFER from the pre-resize dims AND the generation to EXCEED the pre-resize generation — a condition the stale state cannot satisfy — with `RESIZE PLUMBING` named if the canvas never moves and `RESIZE BASELINE DEGENERATE` named if the pre-resize baseline reads zeroed (which would restore the defect silently). **(5) LEG D KEEP-LIVE + TIMING ARM.** Warm, verify, measure and the same-task final read are all kept live; `CesiumDebug.gpuPassCost(true)` is issued at window START (so query sets and readback buffers allocate during WARM, outside the measured span) and AGAIN immediately before MEASURE (it resets the profiler and is the only call that also re-arms), and the arm is RE-VERIFIED afterwards. A window is attributable only with `enabled === true`, `reArmed === true`, `attemptedFrameCount >= measureFrames`, `sampleLedgerBalanced === true` and `frameCount > 0`; `attemptedFrameCount` is the exact discriminator the B944 run needed, since it counts ARMED frames and read 0. A window failing any of those is **DISCARDED-AND-NAMED** (`PERF WINDOW DISCARDED — window i (iteration j, arm): reason`), never enters a median, and a run where none survive prints `PERF ARM BLIND` and quotes no number. **★ ONE B944 FINDING IS WITHDRAWN AS NOT-YET-ESTABLISHED — `C13-10-TIER-ROUNDTRIP-REENGAGEMENT`.** Its evidence was "emitted/consumed did NOT re-engage within 90 frames"; those were 90 RENDER CALLS whose execute count is unknown and plausibly zero, i.e. the same starved-window class as everything else on this list. The filing must NOT be actioned as an engine defect until the fixed probe re-runs it; `p.cReturnToLowReengages` is unchanged and will answer it honestly either way. **Gates:** NEW `cloud-refresh-skip.spec.mjs` 22/22 (the pure repair, each rule against its own mutant, plus source pins on BOTH probes — keep-live at every render site, execute-denominated loop bounds, the resize condition, the discard rule); `cloud-reconstruction-consume-probe.spec.mjs` 29 → 30 green (D1/D6/D7 re-pinned to the new units, D9 added so removing the shared import fails here too); full `cloud-*.spec.mjs` lane 388/388; `probe-fleet-contract.spec.mjs` green with NEITHER probe allowlisted; prettier + eslint clean. **PRE-REGISTERED FOR THE NEXT `--perf` RUN:** leg B stays green 15/15 (unchanged code path, now on real executes); leg C's `cResizeReengagesBothWays` / `cGenerationMonotonic` / `cLiveBytesTrackSize` go GREEN if the engine resizes the attachment set as the survival probe's rider already proves it does; leg D produces real per-pass medians for the march and the producer with paired B−A deltas — which is the FIRST time the C13-39 register question has any data. **THE EXIT CODE IS THE TIER-ROUNDTRIP ANSWER: exit 1 with `cReturnToLowReengages` RED means the re-engagement defect is REAL and now established on real executes (re-file it with the execute counts); exit 3 with every predicate green means the B944 filing was a starved window and should be CLOSED-NEGATIVE.** Watch items that are pre-declared as instrument, not subject: any `REFRESH-SKIP STARVED` line (keep-live did not take); `PERF WINDOW DISCARDED … armed only N of 60` with N = 59 (the profiler's arm boundary — move the bound to `measureFrames - 1` and record it, do not widen it silently); a non-interpretable diff (a state never converged and its pixel number must not be quoted). **★ POST-CO-32 VERIFICATION RUNS — 2026-08-09, Batch 953.** CONSUME `--perf`: **exit 3, ZERO red predicates — `C13-10-TIER-ROUNDTRIP-REENGAGEMENT` CLOSES NEGATIVE** (re-engagement certified on real executes; the B944 filing was a starved window). Leg C resize green. **LEG D: the FIRST real C13-39 timing data** — march A 1.5298ms → B(emit) 2.3696ms, paired B−A median **+0.841ms** (A-spread wide [1.53..3.76], B tight; NOISY FIRST DATA, no occupancy claim quoted — the +55% exceeds the ~4-live-f32 derivation and needs more iterations before it is a finding); producer B−A **−0.0046ms** (emitting producer CHEAPER, as derived); resolve +0.0013ms; upscale drift control +0.0002ms (clean). All four visual states failed their anti-vacuity stationarity gates (8×8 executes, never byte-equal): under LIVE execution the frame is genuinely non-stationary — pixel diffs stay uninterpretable and the visual arm needs a TOLERANCE-BAND redesign, not byte identity. SURVIVAL probe: **all counter/lifecycle predicates green (CO-32's three targets fixed)**; the two settled-byte-identity predicates fired red ON A FAILED STATIONARITY PRECONDITION — per the consume probe's own pattern they must gate STRUCTURAL-by-name when the precondition fails (survival-probe instrument gap, follow-up CO-33), and the underlying C13-09 pixel-inertness question moves to the same tolerance-band design. The prior identity greens are retroactively named vacuous in the B952 record. **★ THE VISUAL ARM REDESIGNED — 2026-08-09 (CO-33). INSTRUMENTS ONLY; NOT RE-RUN. ★ THE VISUAL ARM'S MEANING CHANGED; the counter, lifecycle and perf arms are untouched.** The Batch-953 `--perf` run's four `STATIONARITY NOT REACHED` lines were the CO-32 gates working correctly and reporting that the PREMISE of the whole visual arm is false: with the lane really executing, no state reaches a byte-stationary fixed point, so every recorded diff came back `interpretable=false` and the numbers — 33.4% off→attachments, 38.6% attachments→consume, 37.8% off→consume, against a SAME-STATE floor of 30.4% — had no baseline to be read against. The full design, its derivation and every rejected alternative are recorded in the C13-09 stamp above and in `Tools/visual-regression/lib/cloud-refresh-skip.mjs` §3; what changed HERE: (i) each of the four captured states now yields fluctuation samples from the stationarity gate it already ran (last 6 captures, each pair separated by real executes); (ii) the four comparisons are judged against POOLED bands of their two endpoint states — `upper = max + range + floor`, FIRST-PASS DERIVED and printed with its terms — on THREE statistics (footprint, coarse footprint above the 7/255 dither amplitude, and magnitude), because a band on the saturated 30% footprint alone would admit almost anything; (iii) each band is calibrated IN-RUN by an injected control ladder riding a real same-state pair, so every verdict is printed with the DETECTION LIMIT it is scoped to, and a band that rejects nothing reports NOT MEASURED instead of a comfortable "inside"; (iv) the consume self-floor is retained as the same-state control it always was, now with a band of its own. **STILL NO PASS/FAIL BAR, DELIBERATELY.** The row says the composite MAY differ under its first consumer, so an OUTSIDE verdict here is INFORMATION — the first quantified statement this lane has ever been able to make about the consumer's visual effect — and not a failure. `cloud-reconstruction-consume-probe.spec.mjs` D3's forbidden list GREW to cover the band/visual/inertness identifiers as well as the raw diff fields, with a second mutant, precisely because a band verdict is the kind of respectable-looking number a later hand would promote into the fold; new D10 pins the band arm's existence, the pooling, the structural routing and the pixel-free report view. What IS structural here is a comparison with NO usable tier; correspondingly, a non-stationary state is no longer pushed to structural on its own (it is the expected regime under keep-live) and its reason is quoted into the fold that needed it. `interpretable` keeps its old meaning and is still computed and printed — it is simply no longer the gate on whether a number can be read. **Gates:** `cloud-refresh-skip.spec.mjs` 22 → 39 (band rule, all four anti-vacuity modes, ladder, detection limit, tiers, and MUTANTS BOTH WAYS — a within-band fluctuation must not fail, an injected real change beyond the band must, an uncalibrated band must not manufacture a green — plus a pin that the pixel kernel is BYTE-IDENTICAL in both probes); `cloud-reconstruction-consume-probe.spec.mjs` 30 → 31; full `cloud-*.spec.mjs` lane 388 → 406/406; `probe-fleet-contract.spec.mjs` 32/32, neither probe allowlisted; prettier + eslint clean; CRLF preserved. **PRE-REGISTERED FOR THE NEXT `--perf` RUN:** leg B stays green 15/15 and leg C stays green (the tier round-trip stays CLOSED-NEGATIVE); leg D produces a SECOND round of paired march/producer deltas — the +0.841 ms march figure needs corroboration before it is a finding and the −0.0046 ms producer figure is the derived direction; and the visual arm, for the first time, either reports every comparison INSIDE its band with a printed detection limit, or reports OUTSIDE with the exceeding statistic named — which IS the C13-10 composite-change question, asked in a form that can be answered. ★ THE ACHIEVABLE EXIT CODE CHANGED, which is itself a meaning change to record: this probe previously could not exit 0 while any state failed stationarity; with `--perf` and derivable bands it now can. Exit **3** should therefore be read as "`--perf` omitted, or a band could not be derived/calibrated", and `BAND NOT DISCRIMINATING` specifically means the ladder is too coarse for this fixture (instrument — the per-pixel ENVELOPE design named in the C13-09 stamp is the next step), NOT that the consumer is invisible. **★ BAND-DESIGN VERIFICATION — 2026-08-09/10, Batch 955.** CONSUME `--perf`: **EXIT 0 — the probe's first full pass** (legs B/C/D all green). Leg D SECOND round REPRODUCES the first: march B−A **+0.9678ms** (vs +0.841; B-windows tight both rounds — the emitting-march cost is approaching finding status, A-spread still wide), producer **−0.0055ms** (cheaper, consistent), resolve +0.0008, upscale +0.0001 (drift control clean twice). Visual arm now speaks with its calibration: same-state control INSIDE (96.1%); **attachments→consume clearly OUTSIDE (coarse 198.8% of bound, mean 171.5%) — the first consumer's pixel change is REAL and now QUANTIFIED (legitimate: the row says the composite MAY differ)**; off→attachments MARGINALLY outside (100.8%/109.5%). SURVIVAL: exit 1 — `pixelInertOffOnWithinBand`/`OnOffWithinBand` RED with the ladder proving the band discriminates (5/5 rungs rejected at r1). **HONEST VERDICT: C13-09 pixel-inertness is QUESTIONED AT THE MARGIN, not established broken** — both probes read the off↔attachments delta at 100–116% of bound while consume reads ~200%, and the NAMED CONFOUND is that cross-state capture pairs sit further apart in execute-time than the same-state band pairs (temporal drift inflates the numerator only). FOLLOW-UP (next dispatch): a TIME-SYMMETRIC band (band pairs separated by the SAME execute gap as the comparison endpoints) and/or the per-pixel min/max envelope CO-33 named as the next step; only after that instrument does a marginal excess become a product claim. |
| `C13-11` | **PART 1 COMPLETE — Batch 961 (`b288bfc7bb`); PART 2 OPEN.** ~~BLOCKED~~ | *(Restamped 2026-08-09, handover audit FIX 15 — this cell read "BLOCKED" 54 batches after the blocker was discharged; the dispatch spec is copied in below so part 2 is dispatchable from the queue without opening `DEFERRED_WORK.md`.)* **PART 1 (shipped):** `Tools/stbn-bake/` generates a 128×128×64 scalar spatiotemporal blue-noise volume from PUBLISHED algorithms only (Ulichney 1993 void-and-cluster; Georgiev–Fajardo 2016 pairwise energy; Wolfe et al. 2022 separable spatiotemporal criterion) — **no NVIDIA STBN SDK code or textures, which remain non-commercial-licensed and out of bounds per `R-STBN` / `R-2026-08-10-5`**. Installed asset `packages/engine/Source/Assets/Textures/Noise/stbn_scalar_128x128x64.png` (1,049,988 B, SHA-256 `8dd44e0b07…6cb2d579`), reproducible (`--repro` byte-identical across processes), gated by `Tools/visual-regression/stbn-asset.spec.mjs` 16/16 incl. three exact mutants; measured spatial low 0.0432 / high 1.1936, temporal low 0.1047 / high 1.5008 against a white-noise null of 1.000 on every band. `packages/engine/Source/Scene/StbnNoiseVolume.js` is the asset seam and has **no consumer, deliberately**, so no unmeasured shader change rode the generation batch. **PART 2 = `C13-11-PART2-CLOUD-STBN-CONSUMPTION`** (`DEFERRED_WORK.md:1533`, filed CO-36; effort **M**; **depends on nothing** — part 1 discharged the only blocker). **Three WGSL sites in `Shaders/WebGPU/Environment/ProceduralClouds.wgsl`:** (1) the `BAYER4` half-res sub-pixel UV jitter in `fragmentMain` — highest value, an ordered 4×4 matrix on a visible lattice; (2) `cloudRaySamplePhase` → `interleavedGradientNoise`, already gated on `QF_JITTER` bit 3 + `QF_TEMPORAL`, so only the tap changes; (3) `coneJitter`'s `hash33`, which wants a **vec3** the scalar asset cannot supply — decide on evidence between keeping `hash33`, baking a second scalar volume (`--seed` exists), or extending the generator's value term to vectors. **Already true, no work needed:** `cloud.frameCounter` is a 6-bit 0…63 sequence (uniform slot 76, widened by C13-36), so `slice = u32(cloud.frameCounter) & 63u`; **`@group(0) @binding(14)`** is the next free bind slot (13 is last used); **`qualityFlags` bit 14** is free (`QF_PLANET_DENSITY` is bit 13). **Constraints:** sample with `textureLoad` and NO sampler (bilinear across a tile boundary would blend two unrelated temporal slices); upload `r8unorm`, never `-srgb`, never premultiplied. **Default OFF** behind the new quality bit with the Bayer/IGN branches kept verbatim as the else/loading fallback, so `defines=0` stays byte-identical. **Acceptance:** off-path byte-identity FIRST, then a Playwright probe at a cloud-filling saved view comparing half-res temporal residual against the Bayer baseline (Principle 8 — never a user reload). Full seam design: `Tools/stbn-bake/README.md` §8. |
| `C13-12` | NOT STARTED | Opens after reconstruction topology and current-work layout. `C13-05` now supplies the coarse compatibility/reset generation; this task retains attachment-aware wind/weather/depth rejection, disocclusion, variance clipping, and reactive history. |
| `C13-13` | NOT STARTED | Lighting/spatial-tier separation still follows reconstruction topology. The old 128-step blank was command-empty scheduling/readiness, not a raymarch-tier failure; fresh visible evidence is clean but has no comparable adaptive/fixed companion and therefore proves no speedup. |
| `C13-14` | NOT STARTED | Promoted C11 planet-scale tiling seed; core W3 architecture. |
| `C13-15` | NOT STARTED | Climate prior; distinguish climatology from current observations. |
| `C13-16` | **PARTIAL — per-genus MORPHOLOGY slice IMPLEMENTED and LANDED Batch 826 (2026-08-06). RULING R7 — **RULED `R-2026-08-21-20` (2026-08-21): the landed U2 morphology STAYS ON with its measured regression disclosed (U2 A/B campaign COMPLETE / REGRESSED, Batch 1108); `C13-39B` is the containment; the fork's never-default-disable-for-a-metric principle re-affirmed.** (option 3, the genus-conditioned base-field variance budget) was EXECUTED IN THE MODEL on 2026-08-07 and returned STOP-WITH-FRONTIER: the mechanism reaches gate C without touching an aspect, a strength or a bar (CIRRUS 1.232 → 1.959 at +1.2% mean column opacity, gate C ratio 2.34, gate E 1.878/1.109, gate D argmax on the wind axis at a 1.59× margin vs the baseline's 1.02× near-tie), but the SAME budget costs the `northatlantic-cirrus-fibratus` fixture −47.5% column opacity and −43.1% of the TAIL statistic its `minChangedFraction` floor counts. The blocker is NOT the fibre carve: it is the subtractive erosion's convex ZERO CLAMP, which turns base-field variance into mass wherever the deck is thin — attribution executed, `erosionScale` 0 flips the same budget to +5.7%. The trade-off is a monotone curve with a ~1:−2 exchange rate and NO point holds both surfaces. NO shader, NO `FIBRE_MORPHOLOGY` value, NO uniform slot and NO gate bar changed; the levers, the frontier and the refutations live in `Tools/visual-regression/lib/cloud-march-transfer-model.mjs` + `cloud-march-transfer.spec.mjs` (24 → 49 tests, green), which also asserts NEGATIVELY that no budget symbol exists in `ProceduralClouds.wgsl`. Frontier table, rejected variants and the two concrete unblockers (a shipped `cloudGateMean(cEff)` response; a way to spend variance without spending tail mass at low coverage) are in `DEFERRED_WORK.md` §`C13-16-SCREEN-ANISOTROPY-ATTENUATION` UPDATE 2026-08-07. Checklist item 6's premise is superseded there and in the checklist itself. **U2 FOLLOW-UP 2026-08-07 — THE STOP LIFTS FOR THE PAIR.** Both Batch-896 unblockers were evaluated in the model. Moving `genusFibreFactor` AHEAD of the subtractive erosion (`max(gate*gradient*fibre - erosion, 0)`) is worth +0.31 elongation at ZERO budget (CIRRUS 1.232 -> 1.541) and can only remove mass; a genus-conditioned erosion-DEPTH compensation restores it, and the two nearly cancel at BOTH coverages at once (the reorder's loss ratio 1.77 against the erosion's gain ratio 2.2 — at zero budget, compensation 0.667 lands -0.2% at the gate configuration and +0.2% at the tour fixture). With a small budget on top the RECOMMENDED point (carve-first, budget 0.55, downWeight 0.25, compensation 0.6) clears every bar: CIRRUS 1.733, ratio 2.072, gate E 1.644 / 1.121, gate D argmax 90 deg at a 1.456x rival margin, +4.4% opacity at the gate configuration, and the `northatlantic-cirrus-fibratus` ground `changedFraction` predicted at 0.00258 against its 0.002 floor — where Batch 896's best gate-C point predicted 0.00159, UNDER it. CUMULUS stays byte-identical by a STRUCTURAL gate (genusFibreFactor early-returns exactly 1.0, and `x*1` is exact in f32), the uniform tail does NOT grow, and R7 unblocker U1 (`cloudGateMean(cEff)`) comes OFF the critical path — at the pair's smaller budget weight the exact pivot is worth ~3.6 points of fixture tail and the plain derived `BASE_FIELD_MEAN` still passes. A third lever surfaced a separate finding: the LIVE route erodes SUBTRACTIVELY while the BAKED route uses a `remap` the renderer calls "mean-preserving", so **the two shipped routes disagree about the composition** — aligning them is not byte-neutral and does not reach the gates. STILL NO SHADER CHANGE; the pre-registered Edge acceptance (gates A-E per lane plus the item-6 floor, with failure bands) and the full sign-off scope are in `DEFERRED_WORK.md` UPDATE 2026-08-07 (later); spec 49 -> 73 tests, green. Residual: no point is within a STRICT 3% at BOTH configurations (the trade still runs ~1:1.7), which the spec asserts so the candidate is not sold as tighter than it is.  Edge acceptance is ENTIRELY OWED: an acceptance run was ATTEMPTED on 2026-08-06 and produced NO product verdict in either direction — read it as ZERO credit, not partial credit (detail at the tail of this row). Regional MIXTURES remain NOT STARTED (blocked on `C13-14`/`C13-15`, both NOT STARTED).** | **Premise (confirmed):** `Scene/CloudTypeProfile.js` has carried five axes per WMO genus since Weather Phase 0, and TWO of them reached no renderer at all — the FIBROUS/PUFFY `erosion` style and the Henyey-Greenstein `phaseG`. A genus therefore differed only in deck, height-gradient shape (`profileShape`@101), density scale (`profileDensityScale`@102), and extinction (`profileExtinction`@103), so CIRRUS rendered as a faint SCALED-DOWN CUMULUS. That is exactly what the C13-01 tour recorded: after `CLOUD-LOW-COVERAGE-CUTOFF` restored cirrus VISIBILITY on 2026-08-01, `northatlantic-cirrus-fibratus`'s gate note still read "Genus MORPHOLOGY (fibrous streaks vs generic puffs) remains C13-16". **Morphology model:** cirriform cloud is ice precipitating from small generating cells near the tropopause; crystals fall at ~0.3-1 m/s through a layer whose horizontal wind changes strongly with height (jet-stream shear of order 5-20 m/s per km), so each crystal is advected downstream as it descends and the deck is drawn into long streaks trailing beneath and downwind of the head. Three consequences are now modelled: (1) ANISOTROPY — `genusFibreFactor` divides ONLY the along-wind axis of its Worley domain by the genus aspect ratio, so isotropic cells read as filaments that many times longer along the wind than across (observed cirrus runs 5:1 to 20:1); (2) FALLSTREAK TILT — the along-wind coordinate is displaced by `(1 - h) * shear`, so the streak's lower end lags its generating head at the deck top; (3) PHASE — `genusForwardG` offsets the HG forward lobe by `profile.phaseG - CUMULUS.phaseG`, because hexagonal ice plates/columns scatter far more forward-peaked (g ~ 0.88-0.9) than liquid droplets (~0.76-0.78), clamped at 0.95 so the HG denominator never approaches its singularity. A fourth lever, `genusErosionHeightWeight`, blends the subtractive detail erosion's `1 - h` base weighting toward uniform: cumuliform base-weighting models a convective water cloud's ragged bottom and crisp top, and ice has no such buoyant asymmetry. **Architecture:** the fibre factor is a FOURTH link in the existing `mammatus × species × feature` `[0,1]` chain, not a second chain — those three are OPT-IN user selections of a supplementary feature/species on top of a genus, this one is the genus's own baseline character read from the profile table, and the WMO hierarchy (genus → species) is exactly that composition. It is applied IDENTICALLY in `legacyCloudDensity`, `legacyCloudBaseDensity`, and `cloudMacroSampleAt`'s shared `factor`, so the W5 `base >= full` empty-space-skip invariant holds structurally and the beer-shadow producer (which reads `cloudDensityRelativeWithFootprint` → `cloudMacroSampleAt`) tracks the visible march. A domain WARP was rejected as the lever: the C13-37 shape/warp/detail coordinates are seeded-SO(3)-rotated and `fract`-wrapped, so an anisotropic map on them would break both the wrap and the frozen domain contract — which is precisely why every existing morphology feature is a mask. **Files:** `Scene/CloudTypeProfile.js` (+`FIBRE_MORPHOLOGY` table + `getFibreMorphology`; `.d.ts` updated), `Shaders/WebGPU/Environment/ProceduralClouds.wgsl` (`genusFibreFactor` / `genusErosionHeightWeight` / `genusForwardG` + 4 uniform slots + 6 call sites), `Renderer/WebGPU/WebGPUProceduralCloudRenderer.ts` (`CLOUD_GENUS_MORPHOLOGY_FLOATS = 4`, `CloudUniforms` 168→**172** ADD-ONLY with every earlier offset frozen). **No new public dial:** `cloudType` remains the single selector and the values live in the JS-authoritative table. **Evidence:** new `Tools/visual-regression/cloud-genus-morphology.spec.mjs` 21/21 with a CPU twin (`lib/cloud-genus-morphology-model.mjs`) that imports the shipped table rather than restating it. Metrics are STRUCTURAL, never band means (the campaign's recorded trap): correlation length along wind ÷ across wind recovers the authored aspect (cirrus 8.10 vs authored 9, cirrostratus 4.65 vs 5, cirrocumulus 1.95 vs 2) and a top-vs-base cross-correlation recovers the authored shear (0.900/0.360/0.140 vs 0.9/0.35/0.15). Falsifiability confirmed by running the suite against deliberately broken trees: flattening the shipped cirrus row's anisotropy/shear to `(0.6, 1.0, 0.0)` turns 5 tests RED, and deleting `/ aspect` from the WGSL turns the wiring test RED. Byte-neutrality is asserted with `assert.equal` and no tolerances at every level (identity row exactly `0/1/0`, phase delta exactly `0`, factor exactly `1`, erosion weight exactly `fround(1 - h)`, `genusForwardG(g, 0) === g`) and each WGSL guard is an explicit early return, mutation-checked. Full pure-Node lane **1357 pass / 4 fail**, where all four failures are the pre-existing bare-worktree `ERR_MODULE_NOT_FOUND` on generated `Shaders/**/*.js` (`model-native-pipeline-stage-tax`, `moon-normal-strength-policy`, `moon-webgl-mip-policy`, `webgpu-snap-framebuffer-lifecycle`) and are unrelated. Naga validates the combined `CloudDensityDomain + ProceduralClouds` source. `tsc --project packages/engine` adds no error (134 pre-existing `TS2307`, non-`TS2307` = 0); root `npx tsc --noEmit` clean; Prettier and ESLint clean per file. `cloud-density-domain.spec.mjs`'s frozen legacy hashes were re-frozen with the reason recorded, and `legacyBakedBase` deliberately keeps its original hash, which is what proves the re-freeze was scoped to the morphology chain. **Functionality preservation:** no feature removed or default-disabled; WebGL is untouched (`ProceduralClouds.wgsl` is fork-only per `SHADER_PAIRS_LOCKSTEP.md`, and the billboard `CloudCollection` renders genus-agnostic puffs by documented design, so no lockstep row is owed). **NOT closed here:** the row's canonical scope — converting region and synoptic signals into per-REGION mixtures of types and vertical decks — needs the `C13-14` weather quadtree and the `C13-15` climate prior, both NOT STARTED; today the genus is still one collection-level value for the whole planet, which is the failure the wave prose names. Also open: a per-genus SPECIES default (cirrus still needs an explicit `cloudSpecies: "fibratus"` for the finer filament tier), and `C13-21`'s multi-layer morphology. **Do NOT self-promote:** Edge acceptance owed, checklist below. **Rollback boundary:** the `FIBRE_MORPHOLOGY` table, the three WGSL functions and their six call sites, the 168-171 uniform row, and `cloud-genus-morphology.spec.mjs`. **ACCEPTANCE ATTEMPT 2026-08-06 — NO VERDICT, ZERO CREDIT.** `probe-cloud-genus-morphology.mjs` (6 gates: UNIFORMS / NEUTRAL / DIRECTION / WIND / GRAIN / CLEAN) landed at Batch 830 against the checklist below. Its first run failed at init reporting `executeCalls=0`, `initialized=false`, `pipelineReady=false` — which reads exactly like a BROKEN renderer and is a good disguise; the actual cause was the probe defining a `configure()` helper and never CALLING it before `awaitProceduralReady`. `configure()` is what enables the volumetric renderer, and every working cloud probe (`probe-cloud-banding`, `probe-cloud-tour`) configures FIRST. Fixed at Batch 836 with the reason pinned in a comment at the call site. The renderer now initializes, but the probe exceeds its own 420 s watchdog (exit 2): six legs with paired clouds-on/off captures at `cloudQuality: 96` is too expensive. **That is a probe-tuning problem, not a product finding — nothing in Batches 830/836 is evidence for OR against the cirrus morphology, and no checklist item below is discharged.** Recorded tuning direction: the structural legs measure autocorrelation DIRECTION and do not need 96 march steps, and/or the legs can be split across runs. **2026-08-06 — the DIRECTION phase RAN and produced real product data, and maintainer ruling R3 has now been EXECUTED against it.** The phase-split probe (uniforms / neutrality / direction, derived watchdog) cleared the watchdog problem; `manifest-direction.json` (22:46 UTC) records five lanes, `acceptedLines` 16/16 and `saturatedFraction` 0 on every one, `uniformRow` `[0.6, 9, 0.9, 0.12]` for CIRRUS and `[0, 1, 0, 0]` for CUMULUS. Gates C (cirrus 1.178 vs floor 1.6; ratio 1.214 vs 1.4), D (argmax moved 60 deg vs 90 +/- 30) and E (cirrostratus->cirrocumulus margin 1.081 vs 1.1) all MISS. **R3 required the march transfer to be MEASURED before any authored aspect or gate threshold is touched, and it now has been:** `Tools/visual-regression/lib/cloud-march-transfer-model.mjs` + `cloud-march-transfer.spec.mjs` (22/22, five-way mutation group) reproduce all five lanes UNFITTED — worst elongation error 0.054 on the three optically-thin genera and 0.135 on the optically-thick CUMULUS lane, worst half-length error 13.0%. **Three findings change the row.** (i) The attenuation is NOT the shell integration: elongation is step-count invariant (8/16/32/96 -> 1.240/1.231/1.232/1.232), so the mechanism recorded in `DEFERRED_WORK` before today was wrong. It is the multiplicative composition with the ISOTROPIC BASE field — strip `fbmNoise` to its mean and the SAME march, steps, saturation and estimator return 9.06 from the 9:1 domain. (ii) **Gate C is UNREACHABLE by authored aspect.** The transfer curve tops out at 1.379 near aspect 32 and then turns over (1.317 at aspect 256), so option (a) of the recorded decision — raise the aspects — does not exist as stated. (iii) The lever is fibre STRENGTH: at the SHIPPED aspect 9, elongation crosses 1.6 between strength 0.70 and 0.75, at a cost of about **-17% mean column opacity**. **That cost lands directly on checklist item 6 below, whose reasoning must be re-read:** item 6 justifies the conservative carve depths on the ground that "elongation is provably independent of carve depth" — TRUE of the FIELD, and FALSE of the integrated image, where depth is the only effective lever. Item 6's instruction to LOWER `FIBRE_MORPHOLOGY[CIRRUS].strength` if the `northatlantic-cirrus-fibratus` ground floor is missed, and gate C's demand to RAISE it, now point in opposite directions on the same knob. **No authored value was changed and no gate threshold was re-derived** — R3 forbade both as first moves. The decision (spend the opacity, re-derive C/D/E from this model, or accept and document the attenuation) is the maintainer's; full numbers, curves and stated limitations are in `DEFERRED_WORK.md` under `C13-16-SCREEN-ANISOTROPY-ATTENUATION` UPDATE 2026-08-06. ⛔ **THE MODEL'S ONLY ADVERSARIAL VALIDATION WAS VACUOUS — all five MUTATION entries. Found by audit, REBUILT 2026-08-07. The model itself, the five-lane validation and all three findings above are UNAFFECTED; only the mutation group was broken.** The predicate was `worstErrors(overrides).elongation > THIN_ELONGATION_TOLERANCE` where `worstErrors` maxes over `ALL_GENERA` — CUMULUS included — and `THIN_ELONGATION_TOLERANCE = 0.1`, while CUMULUS's **un-mutated** residual is 0.135 (the number this row already records, and the reason `THICK_ELONGATION_TOLERANCE = 0.2` and `toleranceFor()` exist at all). The assertion was therefore satisfied **before any mutation was applied**: each of the five subtests passed on the baseline residual, not on the mutation's effect. Independently reproduced: renaming each of the five override keys — which `buildModelParameters` silently ignores, reading every field as `overrides.X ?? default` with no unknown-key validation — leaves the model at `e=0.135, h=13.0%` and **all five subtests still green**. Only `includeBaseField` had an incidental backstop elsewhere in the file; the other four were wholly unguarded. **REBUILD:** each subtest now scores TWO things. (a) **Displacement from the un-mutated model** (`displacementFromBaseline`) — relative discrimination, immune by construction because the baseline's displacement from itself is exactly 0; measured margins `dispE` 0.173 / 0.181 / 0.232 / 0.247 / 7.82 against a 0.1 floor and `dispH` 19.8% / 26.0% / 80.1% / 106% / 2331% against 15%. (b) **Ground-truth loss at each lane's OWN regime tolerance** via `toleranceFor()`, which is the claim the group's docstring always made and which discriminates all five (naively raising the shared bar to the THICK 0.2 would NOT have — `includeFibre` lands at 0.193). A new **meta-mutant** feeds both predicates a mutation the model cannot read (the empty override, and all five keys renamed at once) and requires both to return FALSE — the guard on the guard. Falsification executed: with `includeErosion`→`includeErrosion` and `saturate`→`saturateTransfer` renamed, the rebuilt subtests go **RED** with `the mutation did not move the model (elongation 0.000 …) — either the override key is no longer read`, where the old group stayed green. Model evaluations are memoised so the group costs less than before (spec 22→**24 tests**, 49 s→44 s, all green). **Second fix, same lane (one-home rule):** `lib/cloud-march-transfer-model.mjs` hard-copied `WORLD_TO_NOISE`, `COVERAGE_ANCHOR`, `COVERAGE_EXPONENT` and a whole re-statement of `cloudEffectiveCoverage` as local literals, while the engine ships all four as an importable CPU twin (`WebGPUCloudDensityDomain.ts`) that a sibling model in the same fleet (`fog-cheap-coverage-model.mjs`) already imports — and the copy had ALREADY drifted in rounding (the shipped twin f32-rounds `anchor`, `c / anchor` and the `pow` result; the copy did not). Since `CLOUD_COVERAGE_ANCHOR`/`EXPONENT` have been re-derived once already (`CLOUD-LOW-COVERAGE-CUTOFF`), this was a live drift risk with nothing in the spec able to notice. All four are now IMPORTED and re-exported under the historical names, and a new shipped-artifact test asserts **function identity** (`cloudEffectiveCoverage === shippedCloudEffectiveCoverage`), not merely equal values, plus the response's shape. **Output pin: every elongation in the validation table is unchanged to six decimals** (CUMULUS 0.836341, CIRRUS 1.232340, CIRROSTRATUS 0.992401, CIRROCUMULUS 0.936739); two half-lengths move by 1e-6 px in the sixth decimal (CUMULUS across 4.180764→4.180763, CIRRUS across 6.075257→6.075256), i.e. a relative 2.4e-7 against a 15% tolerance. The recorded margins (worst elongation 0.135, worst half-length 13.0%) stand. `cloud-genus-morphology.spec.mjs` 21/21 and `cloud-density-domain.spec.mjs` 14/14 remain green. |
| `C13-17` | NOT STARTED | Stable formation seeds and parameter distributions. |
| `C13-18` | NOT STARTED | Local ENU spatial advection. |
| `C13-19` | NOT STARTED | Weather time-slice interpolation. |
| `C13-20` | NOT STARTED | Source overlap and missing-data composition. |
| `C13-21` | NOT STARTED | Opens after temporal and regional substrate. |
| `C13-22` | NOT STARTED | Opens after RTE consumers and temporal substrate. |
| `C13-23` | NOT STARTED | Transferred extinction fill-in. |
| `C13-24` | NOT STARTED | Transferred backend-neutral facade. |
| `C13-25` | NOT STARTED | Transferred lightning reland; upper-tail oracle mandatory. |
| `C13-26` | **BLOCKED** | Requires a same-origin proxy, GRIB2 WASM decoder, and deterministic fixture before live validation. |
| `C13-27` | **BLOCKED** | Requires a networked Edge session; offline/mock success is not live EDR certification. |
| `C13-28` | NOT STARTED | Transferred snow-albedo consumer; both backends. |
| `C13-29` | DEFERRED | Gated tail; reconsider after Gate C proves the cheaper temporal path. |
| `C13-30` | DEFERRED | Gated tail after regional precipitation and terrain consumers. |
| `C13-31` | DEFERRED | Needs source-infrastructure design. |
| `C13-32` | DEFERRED | XL architecture option; activate only after measured Gate-C attribution. |
| `C13-33` | CONDITIONAL NOT TRIGGERED | Blocked by `C13-26`; mock demo does not satisfy the real-data headline. |
| `C13-34` | DEFERRED | Opens after rich/stable cloud shadows. |
| `C13-35` | **COMPLETE — Batch 734** | A non-consuming per-frame cloud-demand signal and shared environment-demand predicate keep a zero-frustum frame alive only when an effect needs it. The black-sky acceptance records eight managed and eight real user-owned frames with `numFrustums=0`, exact request/demand observation, and all resource/post/environment/cloud/canvas stages reached; the managed default is off during the user-owned phase. Eight disabled control frames retain zero frustums and skip all expensive stages. Each active output has 7,584 cloud cells, disabled is exactly black, and all 42 checks plus the WebGPU error gate pass. |
| `C13-36` | **COMPLETE — Batch 736** | The existing tier-owned `jitterEnabled` contract now drives a license-clean analytic IGN phase once per fragment. T1/T2 animate only with realized temporal history; full-resolution T3 holds a deterministic spatial phase; the power-user escape path remains exact midpoint. Six source/math/Naga checks, low/high browser artifacts, the moving altitude route, temporal smoke, and WebGL billboard parity are green. This did not close periodic baked density (`C13-37`), mask/history alignment (`C13-06`/`C13-22`), or advanced attachment-aware rejection (`C13-12`); coarse temporal RTE/reset subsequently closed under `C13-05`. |
| `C13-37` | **COMPLETE — Batch 743 (gate GREEN, confirmed twice bit-identically)** | Landed: Slice A (planet-stable density domain: `WebGPUCloudDensityDomain.ts` + `CloudDensityDomain.wgsl` + `ProceduralClouds.wgsl` integration, 148→168-float add-only uniform tail) + Slice B (3D noise mip chains `CloudNoiseMipmap.wgsl`/`WebGPUCloudNoiseResources.ts` + footprint-aware LOD in both consumers) + the IBL-parity leg (`WebGPUDynamicEnvironmentMapManager` capture parity + revision/scene-clock invalidation, now quantization-debounced at the publish side so animated `cloudCoverage` cannot refill every frame). **RED→GREEN root cause:** the three "independent" SO(3) domain rotations shared m22≈−0.427 (Shoemake m22=2·u1−1; adjacent xorshift32 seeds → correlated first draws); the correlated WARP draw aligned the ~1 km warp-texel lattice 3.0° from screen-horizontal at the near-horizon camera — the audit's visible lattice artifact. Fix: warp-only re-draw, penalty-optimized seed 45296 (constants + provenance docs only; shape/detail draws unchanged, layout unchanged, frozen legacy path SHA-intact, RTE untouched). Final gate: near-horizon 0.0918 vs legacy 0.0953 (ratio 0.963, PASS — thin margin, oracle deterministic), above-deck 0.0858 vs envelope 0.0994 (PASS; legacy-ratio 0.496), morphology 4/4 both phases, 52/52 Node specs, PNGs reviewed by worker AND orchestrator. Residual honesty: warp screened against the two acceptance cameras (other geometries have their own projections); shape/detail m22 correlation remains, documented in the module docstring. **Single-batch justification (review defect 4):** the acceptance oracle certified the COMBINED tree; splitting the renderer's interleaved domain/LOD/IBL hunks would create untested intermediate commits (renderer publishing IBL fields no consumer reads). Also lands: watchdogs on all six cloud probes (machine-safety defect 1), `sharp` pinned as a root devDependency + README exception (defect 5), and the review's small-fix list (defects 4a-f). Follow-ups spun out: limb-view `marchDeck` interval gap → the march-redesign tasks; the temporal helper is now consumed by `C13-05`, while shadow/mask/capture integration remains under `C13-06`; WGSL/renderer decomposition is queued under Gate C. |
| `C13-38` | **COMPLETE — Batch 754** | Cloud revisions now request the full environment rebuild only while the current or previous environment state uses the full cloud march: `(wantMarch || lastUsedCloudMarch) && revisionChanged`. Animated visible-only clouds therefore cannot trigger the 256²×6 fill, prefilter, and SH projection while full cloud IBL is opted out. First opt-in, active full-march updates, and ON-to-OFF teardown remain refresh-capable. `cloud-ibl-revision.spec.mjs` passes `4/4`, including 100 inert opted-out revisions; `probe-cloud-ibl-optout-revision.mjs` proves both opt-in cycles and teardown with clean WebGPU/device/console gates. This is eliminated irrelevant work, not a measured FPS claim. |
| `C13-39` | **CLOSED - NEGATIVE RESULT, Batch 762 (2026-07-24).** Instrumentation + baseline LANDED; the optimization itself is REJECTED with the mechanism on record. Two independently-implemented drafts (full hoist set; then a register-light rescope) were each rejected by a drift-controlled interleaved A/B (bundle-swap within one session, reversed-order rounds, byte-identical occupancy fingerprints): the primary straight-route view march regressed +36-54% in BOTH orderings on both drafts, and even the hash-frozen LIVE legacy density route regressed - because WGSL register allocation is STATIC: code compiled into the module inflates every pipeline's register footprint regardless of runtime branches (`lightConeEnabled()` gates execution, not allocation), and the pass is occupancy-bound, not ALU-bound. What LANDED (762): per-pass GPU timestamp wiring for all 7 cloud render passes + the env Sky Fill (`timedCloudPass`/`withComputePassTimestamps`, byte-inert unarmed), `probe-cloud-lod-hoist-perf.mjs` with six workload-verified lanes (bake-readiness render-measure-check, env-map OWNER model - the manager has NO scene-level owner, only Model/Tileset call it - fill attribution, fixed frame-budget clocks) and the MANDATORY interleaved-A/B protocol in its header, plus the banked pre-change baseline manifests. Any future attempt goes through `C13-39B-CLOUD-SHADER-VARIANT-SPLIT` (DEFERRED_WORK): compile cone/straight/shadow/IBL as separate shader variants via the C11-149 hi-word registry so per-route code stops taxing every pipeline - the only vector the evidence supports. |
| `C13-40` | NOT STARTED | First-use bake/prewarm and broader generation/retirement work; the C13-05 temporal parity bind-group cache is a separately bounded partial. |
| `C13-41` | ★ **C14 CRITICAL PATH — `R-2026-08-10-1`.** The maintainer ruled the **maximal C12 exit gate**: C12 stays open until every `C12-29` slice lands **including S3** (clouds + IBL eclipse response), and **S3 is canonically owned by this row**. C14 (Dynamic Ocean & Wind) is therefore blocked on C12, which is transitively blocked on `C13-41` — this is the single most schedule-load-bearing row in the campaign. Ruling text, consequence and documented fallback (Option A: narrow the gate to S1/S2/S4/S6 and transfer S3 formally here) in [`MAINTAINER_RULINGS_2026-08-10.md`](MAINTAINER_RULINGS_2026-08-10.md); mirrored in [`CAMPAIGN_STATE.md`](CAMPAIGN_STATE.md) §1a. **REOPENED — `R-2026-08-14-1` (the 2026-08-12 COMPLETE/EDGE-VERIFIED reconciliation below is VACATED; gate rework landed Batch 1105; exit = SOL-4 banked refresh cost + the 1.0496 shadowContrastInvariant mechanism).** Historical verdict this cell previously led with: LANDED — Batch 871 (`2cb4090b16`); EDGE ACCEPTANCE OWED. ~~Its probe was AUTHORED 2026-08-07 (CO-9) and has NEVER RUN — see the CO-9 addendum at the end of this cell.~~ **⚠ CORRECTED 2026-08-09 (handover audit FIX 16): the "NEVER RUN" clause was written when the probe was authored and was already false by the next batch; it has been the first thing a reader saw ever since, on the campaign's most important row.** The probe EXISTS (authored at Batch 907, `ec2c6e9801`, which is what made Gate A satisfiable) and has **SIX recorded Edge runs, Batches 908–931**, every one of them read below in this cell: run 1 (`ec2c6e9801`) STRUCTURAL → run 2 (`1970806a59`) exit 1, failure named → run 3 / CO-11 (Batch 912) → run 4 → run 5 (`3e8f265659`, CO-19) → run 6 (`016ed5d5dc`) plus the CO-21 attribution pass (Batch 927). **STANDING at HEAD:** the deck response is **IN BAND** (`deckPureRatioInBand` TRUE at 0.63587 against the pre-registered 0.635 ± 0.01, and 0.5139 at run 6 — the CO-17 `e = 1.01` subtraction is confirmed at pixels); the gating FAIL is `shadowContrastInvariant` (shadow-contrast excess 1.055 as a ratio-of-ratios); run 5's identity-violation headline is **WITHDRAWN** and no engine finding stands from the deck-free lane. **NEXT DIAGNOSTIC — `C13-41-DECKFREE-CONTROL-REDESIGN`, and it is INVESTIGATE, not widen:** the deck-free ground control has produced three distinct behaviours across three runs (frozen-dark, sun-varying, frozen-at-baseColor), so it is state-order-dependent and was never a stable measurement; it needs a state-isolated capture design (its own configure epoch or its own session) before "does the globe dim by F" can be answered. Iterating the in-lane control a fourth time is explicitly not worth a run. **CLOSURE CRITERION after run 6:** one run on the redesigned control in which all three lanes score with `deckFreeGroundCapturesSettled` TRUE and `shadowContrastInvariant` either passes or is converted to a named engine defect with its own row — the band does not move to get there. **FIRST RUN 2026-08-07 (tip `ec2c6e9801`): STRUCTURAL — and the vacuity design did its job.** The shadow lane refused to score: un-eclipsed ground contrast 0.9969 vs the <=0.98 floor — the cloud cast shadow is not darkening the ground in the probe scene AT ALL (diagnosis owed: scene config vs a real cast-shadow gap). Measured while unscored: deck ratio at deepest **2.937 vs the DERIVED band [0.44, 0.70], non-monotone** — the deck's contrast against the S2-dimmed sky ROSE ~3x at totality; candidate mechanisms: the filed `C13-41-CLOUD-AMBIENT-IS-A-CONSTANT` class (a barely-dimming deck over a fully-dimming sky raises the difference image), a deck-isolation assumption failure, or the eclipse factor not reaching the deck in this scene — INVESTIGATE, do NOT widen the band. Parity leg GREEN (`sweepFactorSeriesParity` true). **Refresh-cost leg INVALID, NOT discharged** (negative per-refresh: control leg 5.97 s vs sweep 0.77 s — first-leg warm-up contamination; re-run with warm-up parity or interleaving). **Two instrument fixes owed:** the probe exits 2 on a STRUCTURAL verdict (contract says 3), and per-lane structural scoping must quarantine only the blind lane rather than demoting deck fails to non-gating. **SECOND RUN (post-B909 instrument overhaul, tip `1970806a59`): EXIT 1, the failure NAMED — and the pre-registration converts it to a REAL product finding.** Deck lane on a PROVABLY clean isolation (`deckBackgroundMax` 0, `deckBackgroundIsDark` true): ratio series [1, 0.983, 0.962, 0.894] — MONOTONE and responding, but at deepest eclipse the deck dims only ~10.6% where the pure-deck model predicts ~42% (band [0.44, 0.70]). Under the B909 rule this is a REAL FAIL (`deckRatioInBand`), not band-widenable: **the volumetric deck UNDER-DIMS during an eclipse**. Suspect #1 is the filed `C13-41-CLOUD-AERIAL-TINT-UNDIMMED` addend, which now has a quantitative target (it must account for ~0.31 of displayed ratio at this camera, far more than the "few percent" first estimated — measure it by zeroing the addend in a diagnosis build). Shadow lane STILL blind at the corrected 9000 m / −38° geometry (clear-sky ground contrast 0.9987 vs ≤0.98) — escalated from probe-config toward a real cast-shadow-on-ground question; NOTE the promised `shadowActive` telemetry did not appear in the report (instrument residual). IBL + parity lanes GREEN; `predictedRefreshCountExact` true (275). **`C13-41-ECLIPSE-REFRESH-COST-UNMEASURED` DISCHARGED: WebGPU 7.749 ms/refresh, WebGL 1.607 ms/refresh** (ABBA-interleaved, warm-up parity, 8 segments/leg, preconditions verified). **THIRD PASS (CO-11, Batch 912) — ONE engine line, TWO diagnoses, ZERO bands moved, BOTH prior hypotheses REFUTED BY ARITHMETIC.** HALF 1: the aerial addend is REAL and now DIMMED (CPU-only `dimAerialTint` at the pack site, `WebGPUProceduralCloudRenderer.ts:2280-2302`; zero WGSL; byte-identical at F=1; physically right — the tint models camera-to-cloud inscattered skylight, is todT-keyed and therefore eclipse-INVARIANT today) — but its quantitative target is REFUTED: measured share a in [0.086, 0.170] (mean 0.120), luma 0.708, addend = 9.9% of the deck band; max possible ratio effect 0.894 -> 0.791; the proposed zero-the-addend build would read 0.876-0.886, never <= 0.70. DERIVED PRE-REGISTRATION for the re-run: deck series [1.000, 0.972, 0.939, 0.841], deepest 0.841 envelope [0.818, 0.857] — `deckRatioInBand` WILL STILL FAIL, said in advance. THE RESIDUAL IS THE DECK'S OWN REINHARD: the fit gives e = 0.22*L ~ 7.7 (L ~ 35, matching the shader's own "peaks ~20-30" note and the literal sunIntensity 10.0), where a 2.15x radiance dim displays as 1.13x; the [0.44,0.70] band was derived for e <= 1 (ceiling needs e <= 1.693). The eclipse dim is arithmetically correct and the display transform eats it — filed as `C13-41-CLOUD-DECK-TONEMAP-SWALLOWS-THE-DIM` (maintainer ruling needed; options that restore headroom change the un-eclipsed look). HALF 2: THE CAST SHADOW WAS WORKING ALL ALONG — lane B had a SECOND vacuity under the geometric one: the offline globe renders baseColor (0,0,0.5), display luma 0.036, so the ground share of the band was 1.76% and the beer floor's max reach 1.14% — the 0.98 ceiling was UNREACHABLE BY CONSTRUCTION; the 0.126% actually measured back-solves to ~11% of the visible ground fully shadowed. The fit reproduces BOTH readings (0.513 vs 0.512511 clear; 0.3786 vs 0.378175 eclipsed). No engine gap; producer + consumer + RTE pair verified consistent; `shadowActive` WAS in the second run's report (rungs[i].shadow) — CO-10's telemetry claim corrected: nothing READ it; now `verdicts.shadowTelemetry` + a SHADOW console line. PROBE-SCENE FIX: lane B flies 1400 m / -8 deg (below the deck floor — structurally deck-free for downward rays), bright globe Color.fromBytes(200,200,200), deck-free ground control per rung; TWO new STRUCTURAL detectors (gates 23 -> 25): `shadowGroundIsBright` [0.15,1] and `shadowGroundNotOccluded` [0.85,1.18]. Specs 59/59 + 18/18 (E2c replays the second run's exact shape and requires exit 3 naming BOTH detectors); adjacent suites green. RE-RUN: gulp build (provenance now requires the dimAerialTint marker) + the probe; EXPECTED EXIT 1 with `deckRatioInBand` the only failure and ALL THREE lanes scoring; a still-blind shadow lane WITH both detectors true would be the FIRST real engine evidence — file then, not before. **FIFTH RUN (tip `3e8f265659`, the CO-19 discriminator): SHAPE B — THE GLOBE PATH IS INDICTED, and harder than predicted.** `deckPureRatioInBand` TRUE at **0.63587** (pre-registered 0.635 ± 0.01 — the CO-17 e=1.01 subtraction is CONFIRMED AT PIXELS, nearly dead-centre). `offNoCloudVariesWithSun` TRUE (spread 0.0549 — the fourth run's frozen values resolved). All lanes scored, structuralReasons []. But `deckFreeGroundDimsByFactor` FAILED with an **IDENTITY VIOLATION**: at obscuration ZERO — where on/off = 1.0 by construction — the deck-free ground measured **0.449** (off 0.4627, on 0.2078), and the ratio-over-F stays ~0.43-0.45 at every rung. Merely ENABLING the eclipse machinery dims the deck-free ground path x0.44 INDEPENDENT of obscuration. Candidates: (a) an engine eclipse-enable side effect on the globe light path (a real, serious identity violation), or (b) a leg-state divergence between the probe's on/off configure paths (instrument). The F^0.708 residue analysis must be RE-READ after this is attributed — a constant enable-side dim contaminates every on-vs-off ground comparison. `shadowContrastInvariant` still failing (1.0555 this run). Diagnosis dispatched (CO-21): static diff of the two configure paths first, then the engine enable chain. **SIXTH RUN (tip `016ed5d5dc`): SHAPE I — RUN 5'S IDENTITY-VIOLATION HEADLINE IS WITHDRAWN.** `deckFreeGroundCapturesSettled` FALSE (the CO-21 settled-twin gate fired exactly as designed, quarantining the attribution while `shadowContrastInvariant` stays the gating FAIL), and the tell is decisive: the off-leg deck-free control read **exactly 200/255 = the RAW baseColor, unlit, at ALL FOUR rungs** (spread 0, variesWithSun false) while the on-leg varied (0.354→0.157). Across three runs this control has produced THREE distinct behaviors (frozen-dark, sun-varying, frozen-at-baseColor) — it is STATE-ORDER-DEPENDENT and was never a stable measurement; the ×0.44 was an artifact of it. NO ENGINE FINDING STANDS from the deck-free lane. The surviving eclipse findings are unaffected (they never depended on this control): e=1.01 pixel-confirmed, deck 0.5139 in band, shadow-contrast excess 1.055 (ratio-of-ratios), F^0.708 shape. FILED: `C13-41-DECKFREE-CONTROL-REDESIGN` — the deck-free ground control needs a state-isolated capture design (own configure epoch or own session) before the does-the-globe-dim-by-F question can be answered; iterating the in-lane control a fourth time is not worth the run. **CO-21 ATTRIBUTION PASS (Batch 927) — THE x0.44 IS *NOT ATTRIBUTED*, AND SAYING SO IS THE FINDING. THE FIFTH RUN CANNOT DECIDE ITS OWN HEADLINE.** Static diff of the two legs plus a full audit of the enable chain leaves TWO live causes and REFUTES the shortcut that would have picked one. **WHAT THE RUN'S OWN NUMBERS PROVE.** At rung 0 (F = 1) the deck-PRESENT captures are **bit-for-bit identical across the eclipse toggle** — `onNoShadow` = `offNoShadow` = 0.45784608750450445 and `onShadow` = `offShadow` = 0.27773434657205215 — and so is lane A (`onClouds` = `offClouds` = 0.6273689572513024). Enabling the eclipse at zero obscuration is therefore provably byte-neutral for those two configurations. Only the deck-FREE read moves: `offNoCloud` 0.46274509803954333 (which is EXACTLY 118/255 — the band is spatially uniform to f64, i.e. flat unshadowed ground) against `onNoCloud` 0.2077768453355468, ratio **0.4490**; over F the series is 0.4490 / 0.4341 / 0.4330 / 0.4374 while the deck-present twin's is 1.0000 / 1.0098 / 1.0288 / 1.0842. So the ~x0.44 is confined to the ONE configuration `enableVolumetric: false`, and the two readings cannot both be of the same surface: the OFF-leg retention `offNoShadow/offNoCloud` is 0.9894 but its previously-uncomputed ON twin `onNoShadow/onNoCloud` is **2.2035**. Either the deck-free read is the truth and the deck-present pair MASKS a real engine violation, or the deck-free read is a transient. **THE ENGINE SIDE IS CLEAN BY CONSTRUCTION AT F = 1 — every site audited, none violating.** `resolveEclipseCloudFactor` returns 1.0 (`Scene/EclipseCloudResponse.js:139-145`), `eclipseCloudDirectionalFraction` returns 1.0 because obscuration is not `> 0` (:162-174), `applyEclipseCloudDimming(v, 1.0)` is a bit-exact multiply (:189-191), and `quantizeEclipseEnvironmentRefreshInput(1.0) = 256` IS the identity bucket (:206-210), so **the eclipse-enable does not even produce a refresh EDGE at obscuration 0** (`WebGPUDynamicEnvironmentMapManager.ts:787-790`, `Scene/DynamicEnvironmentMapManager.js:354-365`) — the half-built-IBL hypothesis has no trigger to fire on. All five dimming consumers are identities at F = 1 (`WebGPUProceduralCloudRenderer.ts:1947/2167/2296` incl. B912's `dimAerialTint`, `WebGPUDynamicEnvironmentMapManager.ts:1776`, `DynamicEnvironmentMapManager.js:839`) and so is the strength publish (`WebGPUProceduralCloudRenderer.ts:2715`). The globe's own chain is identity too: `Globe.js:1236-1238` (`* (eclipseSceneLightFactor ?? 1.0)`), `Scene.js:3875-3882`, and `UniformState.js:942-946` which is *gated on `!== 1.0`* and never runs. S5 cannot be the carrier either — `EclipseGlobeShadow.js:1376-1379` requires `enableEclipseGlobeShadow !== false` and the probe sets it false at `probe-eclipse-cloud-response.mjs:576-578`, so `WebGPUGlobeEclipseUniforms.prepare` returns the inert slice on its first gate (`:193-199`). **THE INSTRUMENT SIDE HAS ONE REAL DEFECT, AND IT IS SMALLER THAN IT LOOKS.** The legs are NOT matched: the eclipse-OFF leg's `setEclipse(false)` at probe line 983 is a **no-op** (the flag has been false since the `publishedOff` read at line 916), so `bOnNoCloud` is the only scored capture in the probe taken one settle after a genuine state TRANSITION. But the shortcut that wanted to end there is refuted by the probe's own timing: BOTH legs get a full 500 ms `settleMs` after their last transition (off: line 916 -> renderAt 917 -> aimCamera 981 -> settle 988; on: 1015 -> settle 1026), so "the ON leg simply had less time" is FALSE as stated, and the 0.449/0.434/0.433/0.437 series is too tight for a free-running race. **NEITHER CAUSE IS ESTABLISHED, SO THE SIXTH RUN MEASURES INSTEAD OF ASSUMING.** Instrument fix landed: lane B now captures the deck-free control TWICE on EACH leg — first position and last position, same dials, same settle, same reducer (`offNoCloudSettled` / `onNoCloudSettled`). New gate `deckFreeGroundCapturesSettled` (predicates 28 -> 29) requires both legs to reproduce within `BAND_MEAN_CAPTURE_DELTA`; a drift is STRUCTURAL, because an attribution taken off a capture that was still moving is not an attribution. New band `deckFreeGroundSettleDelta`. New blindness domain **`deck-free`, a CHILD of `shadow`** (`ECLIPSE_CLOUD_LANE_PARENTS`), which is the per-lane scoping the third pass asked for in the direction that matters: a blind lane B still quarantines the attribution, but an unsettled deck-free CONTROL no longer demotes `shadowContrastInvariant` — verified by replaying the fifth run's own JSON through the new gate (exit 1, attribution quarantined, `shadowContrastInvariant` still the gating FAIL, retention pair printed 0.9894 vs 2.2035). **PRE-REGISTERED SIXTH-RUN VERDICT SHAPES, both already spec-pinned:** (SHAPE E) `deckFreeGroundCapturesSettled` TRUE and `deckFreeGroundDimsByFactor` FALSE at ~0.449 -> the reads CONVERGED, the x0.44 is the measurement, and this is a REAL engine identity violation to be filed with the deck-present masking as its second half (spec K3); (SHAPE I) `deckFreeGroundCapturesSettled` FALSE -> the first-position capture had not converged, exit 3, the settled twin is the measurement and the fifth run's headline is withdrawn (spec K2). **CONTAMINATION RE-READ — three findings SURVIVE, one is withdrawn, one needs re-derivation.** SURVIVES: `deckPureRatioInBand` 0.63587 and the whole CO-17 `e = 1.01` subtraction, plus `deckRatioInBand` 0.5139 and the `deckTonemapFit` — lane A's rung-0 clouds-on frames are BIT-IDENTICAL across the toggle, which proves the enable-dim does not reach the deck lane at all. SURVIVES: `C13-41-SHADOW-CONTRAST-ECLIPSE-EXCESS` (1.0554 this run, 1.0496 the fourth) — it is a ratio OF ratios in which any constant ON-leg multiplier cancels exactly, and all four of its terms come from the deck-present configuration that rung 0 proves byte-neutral. WITHDRAWN pending the sixth run: `deckFreeGroundExcessAtDeepest` and every statement of the form "the globe path is indicted" — those are ratios *against F*, where a constant multiplier does NOT cancel. NEEDS RE-DERIVATION: the **F^0.708 residue law** is fitted to deck-free on/off ground brightness against F, so it inherits the multiplier one-for-one and cannot be quoted until the shape is known — note it survives as a *shape* either way (the residue exponent is fitted to the series' curvature, which a constant multiplier leaves invariant) but its AMPLITUDE is void. **GATES:** node --test 85/85 (was 79) on the response gate + 18/18 on the IBL suite; adjacent eclipse suites green (dimming 48, state 34, sky-totality 51, globe-shadow-visual 4, svs-oracle 8, umbra-rte 18); prettier + eslint clean; NO engine files touched, so no tsc leg is owed. New specs K1 (the ENABLE-IDENTITY mutant — a dim at F = 1 — fails, and the delta 0.551 is 10.3x the tolerance CAP), K2 (unsettled = STRUCTURAL, both legs' deltas named, nothing else quarantined), K3 (a SETTLED 0.449 still FAILs — the detector cannot launder a real defect), K4 (a MISSING twin reads as unconverged, never as agreement), K5 (the ON-leg retention twin is computed, reported, never gating), K6 (the probe captures the twin on BOTH legs, matched in position). **FOURTH RUN (post-B912/913/914, tip `6e9c997287`): every lane SCORED for the first time — and both pre-registrations were beaten by reality.** (1) DECK IN BAND: 0.5139 at deepest (series ends in-band, `deckRatioInBand` TRUE) — far past CO-11's pre-registered 0.841 envelope [0.818, 0.857]. The aerial addend's real share was therefore ~4x the geometric estimate, and the `C13-41-CLOUD-DECK-TONEMAP-SWALLOWS-THE-DIM` fit (e~7.7) is CONTRADICTED by this run — that entry needs re-derivation before any maintainer ruling is asked for (the e-fit inherited the un-dimmed addend's contamination). The eclipse deck response is now IN SPEC. (2) SHADOW LANE SCORES AND FAILS FOR REAL: vacuity fully cleared (`shadowGroundIsBright` + `shadowGroundNotOccluded` + `shadowNonVacuous` all true; clear contrast 0.6633; texelSpan 8.3; producer active both positions) and `shadowContrastRatioAtDeepest` = **1.0496 vs the [0.97, 1.03] invariant** (model predicted +0.08%; the discriminating rung reads 1.0214, in band) — the cast shadow's CONTRAST WEAKENS ~5% at deepest eclipse, a real, modest, named product finding (mechanism candidates: the shadowed floor is ambient-lit and ambient dims by a different law than the direct term; the 0.35 beer floor interacting with the dimmed scene). Filed as `C13-41-SHADOW-CONTRAST-ECLIPSE-EXCESS` — do not widen the invariant. (3) Cost re-measured consistently (WebGPU 7.959, WebGL 1.05 ms/refresh). Parity green. Instrument residual: the consumer-side `cloudShadowControl` slot read back UNREADABLE in the telemetry line — fix the readout, it does not gate. **FIFTH PASS (CO-17, 2026-08-07) — BOTH FOURTH-RUN DEFERRALS DISCHARGED BY DERIVATION. ZERO ENGINE CODE, ZERO BANDS MOVED, ONE GATE ADDED, ONE DEFERRED ENTRY CLOSED-NEGATIVE AND ONE UPGRADED.** **HALF 1 — THE SHADOW MODEL: the ambient/direct split is derived, and it REFUTES its own hypothesis.** The cast shadow is `color = color * sampleCloudGroundShadow(...)` (`GlobeTerrain.wgsl:4838`), a MULTIPLY on the whole surface colour, so only what is ADDED after that line is un-shadowable: `U(F) = D*d(F) + A*a(F)`, `S(F) = T_F*D*d(F) + A*a(F)`. Under the publication `d` and `a` are the SAME function — C13-41's `applyEclipseCloudDimming` and C12-29 S2 both multiply by one scalar and there is no second law anywhere — so **F cancels out of both bands** and the closed form is `ratio = 1 + (s_1 - s_F)*(1 - c1)/(s_1*c1)` with the split `x` and the beer transmittance `tau` BOTH cancelled against the MEASURED clear contrast. **No free parameter to tune.** At the deepest rung (`s_F = 0.9995501`, `c1 = 0.679870`) that is `1 + 0.00044989*0.470868 = 1.00021184`, i.e. **3.9x CLOSER to 1** than the directional-only 1.00083551, because the residue DILUTES the move. **The directional-only model is the SUPREMUM of the entire split family** (attained at `x = 0` with `tau` at the beer floor, where the closed form reduces to `(1 - 0.65*s_F)/0.35` verbatim), so **no admissible split reaches 1.05** — the measured 1.049596 is **59x past the family cap** (316x / 192x at the shallower rungs). **THEREFORE `shadowContrastRatio` STAYS AT [0.97, 1.03] BY DERIVATION** and `shadowContrastRatioAtDeepest` = 1.0496 is a REAL product finding; headroom over the model went 36x -> 142x. New gate 26 `shadowContrastModelIsBoundedByDirectional` (domain `gate-arithmetic`, never quarantined) enforces the inequality. **THE FINDING IS REFRAMED AND ENLARGED:** because F cancels, the published laws also require BOTH ground bands to dim by exactly F, and they do not — unshadowed reads 1.0177 / 1.0409 / **1.1261** of F and shadowed 1.0305 / 1.0631 / **1.1820**, whose quotient IS the contrast ratio (1.181983/1.126131 = 1.049596 exactly). Inverting the four reads per rung for the shadowable term's own law (`extractShadowableDimming`) gives `d/F` = 1.0000 / 0.9924 / 0.9954 / 1.0075 — **the shadow path is exactly correct to under 1% and the entire under-dim sits in the residue the shadow cannot touch.** Mechanism candidate (b) from the filed row (beer floor x dimmed tone curve) is REFUTED BY SIGN: every compressive display transform moves the contrast the WRONG way (Reinhard's `T(1+x)/(1+Tx)` is increasing in `x`, so `Rc < 1`; pure gamma is invariant). What fits is a sub-linearly-dimming ADDEND worth 11-50% of the ground band (`tau = 0.35, p = 0.708, x ~ 0.50`, RMS 8.0e-4 on U, reproducing the contrast ratios to 0.4%). **HALF 2 — THE DECK's `e`, RE-DERIVED: `C13-41-CLOUD-DECK-TONEMAP-SWALLOWS-THE-DIM` is REFUTED ENTIRELY and CLOSED-NEGATIVE, no maintainer ruling owed.** The deck's displayed value is `mix(toneMapped, cloud.aerialColor, aerial)`, and Batch 912 made the tint linear in F, so the model is two-term: `R(F) = (1-s)*F(1+e)/(1+F*e) + s*F`. The third pass inverted the SINGLE-term form against an addend-contaminated 0.882 — reproducible today as `fitDeckTonemapEntry(0.4642, 0.882, 0) = 7.70`, so the arithmetic was right and the omitted term was the error. **`s` falls out by SUBTRACTION of the two runs with no fitting** (they differ only in the tint's law, so `R_undimmed - R_dimmed = s*(1-F)`): 0.7118 / 0.7091 / 0.7095 from the three rungs independently, agreeing to **0.4%**. With `s = 0.7101`, **e = 1.0033 / 1.0045 / 1.0127** — one number, spread 0.009, across F = 0.46..0.89; an unconstrained two-parameter fit of the fourth run ALONE agrees (`s = 0.7175, e = 1.0525`, RMS 1.05e-5) though it is ill-conditioned on its own. **`e = 1.01` is INSIDE the band's own design envelope** ([0.44, 0.70] is `F(1+e)/(1+F*e)` for `e` in [0,1]; the 0.70 ceiling holds to `e = 1.693`), and the implied pure-deck ratio at the deepest rung is **0.6355** against the `e = 1` edge value 0.6341. An UNFITTED parameter checks out: the decomposition puts the tint luma at 0.6879, inside `cloud.aerialColor`'s own Rec.709 range [0.6496, 0.7081] from the literals at `WebGPUProceduralCloudRenderer.ts:2297-2299`, at todT ~ 0.66. **CO-11's 9.9% reconciled honestly: the true share is 7.2x it, NOT the "~4x" this cell recorded** — two compounding errors, (1) ALGEBRA, `s = a*A0/((1-a)*r(e) + a*A0)` and `A0 = 0.688 > r(e) = 0.513`, so even at CO-11's own `a = 0.12` the share is 0.155 not 0.099 (1.56x, no measurement involved), and (2) GEOMETRY, the measured `a = 0.654` is a ~39 km march midpoint because lane A's scored SKY band is horizon-weighted and a ray grazing a 1500-4000 m layer stays inside it for tens of km, against CO-11's 5.2-10.2 km near/overhead march (4.6x). **GATES:** `eclipse-cloud-response-gate.spec.mjs` **70/70** (was 59/59; +11 across two new groups J and K), `eclipse-cloud-ibl-response.spec.mjs` still 18/18, prettier + eslint clean on both touched files, CRLF preserved. J1 pins that the closed form has no free parameters (three different `(x, tau)` pairs on one `c1` predict identically, checked against a SECOND implementation that carries both); J2 pins the supremum property including equality at the corner; J3 pins the fourth run's numbers and asserts the band is UNCHANGED; **J4 is the mutant** — a residue dimming as `F^0.708` reproduces 1.0496 and leaves the band, so only a law the publication does not contain can reach the measurement, while `p = 1` lands inside it; J5 round-trips `extractShadowableDimming` and pins `d/F ~ 1` on the real numbers; J7 injects the fourth run's shape into the reference fixture and requires `shadowContrastInvariant` to be the ONLY failure. K1-K4 pin the subtraction, the re-fit, the reproduction of `e ~ 7.7` as the single-term artefact, and the fold's provenance string. **PRE-REGISTERED FOR THE FIFTH EDGE RUN (two probe legs, ~25 lines, both in lane B/A):** (1) `onNoCloud` — the eclipse-ON twin of the existing deck-free ground control, at the same instant per rung. `onNoCloud/offNoCloud == F` to the determinism bracket means the globe's own light path dims correctly and the residue is CLOUD-DRIVEN (which the current numbers already point at: turning the deck on ADDS 13.7% to the ground band at rung 0, `offNoShadow/offNoCloud = 1.1589`, even though the lane flies at 1400 m below a 1500 m deck floor and no downward ray reaches the deck — suspects are the cloud-driven post-process addends and the filed `C13-41-ENV-GROUND-INSCATTER-ADDEND-UNDIMMED` class); `> F` exonerates the cloud subsystem and indicts the globe path. (2) a `cloudAerialStrength = 0` diagnostic leg on lane A — the dial already exists (float 91) and sets `a = 0` exactly, so the deck ratio IS `rho` and `e` reads off ONE measurement with no cross-run input; **pre-registered: the deepest rung reads 0.635 +/- 0.01**, and `deckTonemapFit` already accepts the lane-supplied `deckAerialShare` for it. **INSTRUMENT RESIDUAL FOUND WHILE DERIVING, does not gate and does not affect the arithmetic:** `offNoCloud` reads **bit-identical (0.2750603921572111) at all four rungs** across instants 54 minutes apart while `offNoShadow` at the same instants moves +3.3% — the lane's camera is sun-locked in HEADING only, so the local sun elevation does change and a deck-free band should move with it. Either it is genuinely invariant for a reason worth knowing or it is not being re-captured; it IS what `shadowGroundIsBright` and `shadowGroundNotOccluded` read, and both are scored off rung 0 alone. Print the four values in the SHADOW console line — four identical f64s is the tell. **★ ALL FOUR PAID — Batch 909 (CO-10). THREE instrument defects fixed, FOUR diagnoses filed, ZERO bands moved, ZERO engine code changed.** Gate spec **56/56** (was 36/36; +20 across three new groups), `eclipse-cloud-ibl-response.spec.mjs` still 17/17, prettier + eslint clean on all three touched files. **(1) EXIT CONTRACT.** The mapping is now the pure function `eclipseCloudExitCode` in the gate module — **0 PASS / 1 FAIL / 2 HARNESS FAULT (watchdog or escaped throw, i.e. no verdict formed) / 3 STRUCTURAL (the probe finished and refused to certify)** — and **FAIL now OUTRANKS STRUCTURAL**, because with per-lane scoping a run can carry both and the failure is the actionable half. The provenance and schedule-derivation refusals moved 2 → 3 (a pin on the BUILD is the same class as a pin on the scene); only the watchdog and the top-level catch keep 2, and the catch's message was relabelled from "STRUCTURAL" to "HARNESS FAULT". Spec group G pins it, including a MUTANT that reproduces the shipped `structural ? 2` mapping and is required to differ. **(2) PER-LANE SCOPING.** Every gating predicate now declares a nesting blindness DOMAIN (`ECLIPSE_CLOUD_PREDICATE_LANES`): `gate-arithmetic` (never blind) / `cloud-page` → `deck`, `shadow` / `ibl-page` → `ibl-model`. A blind lane quarantines only its own domain and children; the rest still gate and their failures are FAILURES, listed in `failedPredicates` alongside `unscoredPredicates` and the structural reasons. **The CPU-published reads deliberately sit in `cloud-page`, not in `deck`/`shadow`** — `eclipseSceneLightFactor` and `_cloudCache.shadowStrength` need the page to have run, not the deck to be visible, so a vacuous pixel lane no longer suppresses them either. Each vacuity DETECTOR lives in the domain it blinds, so a vacuous lane emits one structural reason instead of a reason plus a derived FAIL restating it. Spec group H pins it, and **H1 replays the first run's exact shape** (shadow contrast 0.9969 + deck ratio 2.937) and requires exit **1** with `deckRatioInBand` in `failedPredicates` — the assertion the old fold could not make. **(3) COST LEG — warm-up parity + interleaving, arithmetic moved into `computeRefreshCost` and spec'd.** See the `C13-41-ECLIPSE-REFRESH-COST-UNMEASURED` update in `DEFERRED_WORK.md`; in short, both legs now render the whole schedule before anything is timed, the timed legs interleave ABBA across 8 segments with one untimed toggle-absorbing frame each, the estimate is non-negative or **`INVALID` with a named reason** (never a negative number printed as a cost), and the per-segment table is emitted so the still-unexplained 7.7x inversion can be localized on the next run. **(4a) DIAGNOSIS — THE CAST SHADOW: PROBE-SCENE CONFIG, and it is a GEOMETRY error, now FIXED.** No engine gate suppresses it. The chain is intact at HEAD (`config.cloudCastShadows === true` → `ensureShadowResources` → `computeCloudShadowFrame` → `cache.shadowActive`, `WebGPUProceduralCloudRenderer.ts:2685-2802`; consumer `WebGPUGlobeSurfaceCameraUB.ts:846,897,913`; FS gate `camera.cloudShadowControl.x > 0.5` at `GlobeTerrain.wgsl:4815` with `sampleCloudGroundShadow` at `:2180-2221`), the coverage cutoff passes on BOTH arms at coverage 0.6 (`cloudEffectiveCoverage(0.6) = 0.6` exactly, `CloudDensityDomain.wgsl:186-195`; the fog cheap-path arm at `VolumetricFog.wgsl:718-722` is a different consumer entirely and cannot touch terrain), `enableEclipseGlobeShadow=false` is provably orthogonal (its only consumers are `EclipseGlobeShadow.js:1379` and a WebGL-only GLSL define at `GlobeSurfaceShaderSet.js:513`), and `enableLighting`/`viewer.shadows` are not required. **The defect is that the shadow map is 512 texels across a ±60 km footprint** (`CLOUD_SHADOW_SIZE = 512`, `CLOUD_SHADOW_FOOTPRINT_M = 60000.0`, `WebGPUProceduralCloudRenderer.ts:918,926`) — **234 m of ground per texel** — while lane B flew the lane-A vantage at **300 m** altitude. At −35° with ~17.5° vertical half-FOV, the scored 60%..95% band covered ground ranges 300/tan(50.8°) = **245 m** to 300/tan(38.6°) = **376 m**: a **~131 m strip, smaller than ONE texel.** The map is constant over the entire measured band by construction, so the lane could only ever read "the one texel under the camera", and a ~63%-clear field reads clear. **0.9969 is that reading, not a missing feature.** Lane B now flies its own camera at **9000 m / −38°** with `cloudDensity` 0.85 — the geometry `probe-cloud-shadows-flagon` (9000 m/−38°) and `probe-cloud-shadow-cascades` (8000 m/−32°) both use to resolve a cast shadow — which puts the same band at 6.6–10.1 km, a ~3.5 km strip spanning **~15 × 35 texels**; density matters because the ground term is `max(exp(-opticalDepth * 0.04), 0.35)` (`GlobeTerrain.wgsl:2219`). The lane also now reports the engine's own `shadowActive` in both eclipse positions, so a future vacuity can distinguish "the producer never ran" from "the footprint had no cloud in it". Spec F3 pins the geometry, including that the 300 m band is sub-texel and the 9000 m band is not. **(4b) DIAGNOSIS — THE 2.937 IS AN ISOLATION ARTEFACT, and the filed ambient mechanism is QUANTITATIVELY REFUTED.** The deck ratio is bounded above by **1** for any deck model whatsoever: C13-41 multiplies BOTH packed scalars by F — `sunIntensity` (`WebGPUProceduralCloudRenderer.ts:1947-1950`) and `ambientIntensity` (`:2167-2170`) — so `weightedColor` is exactly linear in F, and the deck's own Reinhard (`ProceduralClouds.wgsl:2543-2544`, `exposure` 0.22) is monotone, giving `H(F) ≤ H(1)`. Even the counterfactual `C13-41-CLOUD-AMBIENT-IS-A-CONSTANT` describes — an ambient that refuses to dim — has supremum exactly 1, and lands at **0.741** for the deck's realistic pre-exposure split (direct ≈ 1.8 from `(msLight + silverLining) × 10.0`, ambient ≈ 0.9 from `mix((0.35,0.34,0.30),(0.5,0.65,0.95), h) × 1.5`): `r(0.22(0.4642·1.8 + 0.9)) / r(0.22·2.7) = 0.2763/0.3727`. **2.937 is outside the achievable range of every deck model, dimmed or not**, so the measurement was not of a deck. **The isolation was wrong, and its own documented rationale was false.** The probe's comment read "the sky cancels exactly"; the deck composites as `mix(sceneColor, deckColor, cloudAlpha)` (`WebGPUSceneRendererEnvironmentalEffects` stage, post-tonemap), so `cloudsOn − cloudsOff = α·(H − S)` — **the background survives with a MINUS sign**, and it does not cancel across eclipse positions either, because the sky is tonemapped by the scene chain (Reinhard + `inverseGamma`, `Tonemapping.wgsl:172-178`) while the deck carries its own private Reinhard and composites AFTER the gamma stage. The sky's display ratio at F = 0.4642 is ≈ `F^(1/2.2)` = **0.7055**, while the deck's is `F(1+e)/(1+F·e)` ∈ [0.4642, 1); when H and S land close the denominator `H(1) − S(1)` collapses and the ratio diverges upward without bound. A bright deck over a nearly-as-bright sky reproduces the reading directly: display **0.80 over 0.75 gives 2.42**, **0.85 over 0.80 gives 3.20**, and 2.937 sits between them at an off-contribution of ≈ **0.05** — comfortably ABOVE the 0.01 vacuity floor, which is exactly why the deck lane did *not* report itself blind and the number reached the verdict looking like a measurement. (An earlier draft of this note asserted the excursion required a negative denominator; a scan over the whole `(H, S)` plane refutes that — the positive-denominator branch reaches 275 — and the corrected bracket above is what the gate's `deckBackgroundIsDark` precondition now forecloses.) **The band was never the wrong band:** [0.44, 0.70] is exactly `F(1+e)/(1+F·e)` for `e ∈ [0,1]` (0.4642 at e=0, **0.6341** at e=1 — the row's "0.46 faint / ~0.63 bright core" verbatim), i.e. it was DERIVED for the pure deck ratio and the instrument was measuring something else. **FIX:** the cloud page now pins `sky: false` through the existing shared harness option (sky shell, skybox, sun, moon and clear colour all black), so `S ≈ 0` and the difference IS `α·H`; and the assumption is no longer trusted — new gating predicate **`deckBackgroundIsDark`** (23 gates now, was 22) reads the clouds-off band back against a `deckBackgroundCeiling` of 0.02 and blinds the DECK lane STRUCTURALLY if a background term survives, which is the honest verdict for a run in that regime. Spec H6/H7 pin both the impossibility argument and the band's identity with the pure-deck formula. **TWO NEW DEFERRED ENTRIES filed from the same trace, engine untouched:** `C13-41-CLOUD-AERIAL-TINT-UNDIMMED` (`ProceduralClouds.wgsl:2555-2557` — `cloud.aerialColor` is an undimmed ADDEND, same class as the bake's inscatter term; biases the deck ratio upward by ~a few percent for a near deck) and `C13-41-CLOUD-SUNINTENSITY-HAS-NO-USER-INPUT` (`config.atmosphereLightIntensity` has no referent on `CloudVolumetrics` at HEAD, so the deck's direct term is the literal `10.0` fallback and `globe.atmosphereLightIntensity` no longer moves it — a wiring gap this row's premise text half-records). **RE-RUN:** `node Tools/visual-regression/probe-eclipse-cloud-response.mjs`. **PRE-REGISTERED VERDICT SHAPE:** all three lanes should now score — deck (background verifiably black, ratio ≤ 1 by construction, expected in [0.44, 0.70] plus a small upward aerial-tint bias), shadow (ground band now ~15×35 shadow texels, expected contrast well under the 0.98 vacuity ceiling and its eclipse double-ratio inside [0.97, 1.03]), IBL (unchanged). **Expected EXIT 0** if the model holds; **EXIT 1** naming the specific predicate if it does not — a deck ratio outside the band with `deckBackgroundIsDark` true is now a REAL product failure and must not be band-widened; **EXIT 3** only if a lane is still blind, and the structural reason will now name which lane and leave the others' verdicts intact. *(Stamped 2026-08-07, close-out docs reconciliation: this cell read "IMPLEMENTATION DONE (worker, 2026-08-07) — pending orchestrator landing + Edge run" — wording written **by** commit `2cb4090b16` itself, which is the commit that landed the row. That commit carries `Scene/EclipseCloudResponse.js` (+ its `.d.ts`), `Scene/DynamicEnvironmentMapManager.js`, `Renderer/WebGPU/WebGPUProceduralCloudRenderer.ts`, `WebGPUDynamicEnvironmentMapManager.ts`, `WebGPUAerialPerspectiveEffect.ts`, `WebGPUVolumetricFogRenderer.ts`, `WebGPUPostProcessStageCollection.ts` and `WebGPUGlobeSurfaceCameraUB.ts` on `main`, so the "pending orchestrator landing" clause was stale the moment it was written.)* **What is genuinely owed:** the batch pre-registered three falsifiable Edge predictions — at obscuration 0.9 the cloud-deck pre-tonemap ratio is EXACTLY 0.4642 (displayed 0.46-0.63 post-Reinhard); ground-shadow contrast moves +0.08% (0.350000 → 0.350293), where a large measured change REFUTES the model; a 0 → 0.9 → 0 sweep produces EXACTLY 275 environment refreshes (1 + 2 x 137 edges) — and **no probe exists to run them.** `Tools/visual-regression/` carries the Node contract `eclipse-cloud-ibl-response.spec.mjs` (17/17) but no `probe-eclipse-cloud-*.mjs`; the existing eclipse probes (`probe-eclipse-globe-shadow`, `probe-eclipse-scene-dimming`, `probe-eclipse-sky-totality`, `probe-eclipse-sun-fade`) do not measure any of the three. **Authoring that probe is the first step of this acceptance, not a re-run.** | **Premise CONFIRMED, and the hole was total.** C12-29's own research filed "clouds + IBL" as C13-owned, and the S2 inventory entry records the WebGL IBL radiance bake as deliberately left undimmed "whose sun-direction debounce would latch a dark IBL past totality". At HEAD nothing in the cloud/IBL subsystem responded to an eclipse on EITHER backend: the volumetric deck's direct term is `(msLight + silverLining) * cloud.sunIntensity` and `sunIntensity` is packed from `config.atmosphereLightIntensity` — the globe's UNDIMMED user field, **not** the per-frame `tileProvider.atmosphereLightIntensity` mirror `Globe.js:1226` dims for the ground atmosphere; the deck's ambient colours (floats 80-82 / 84-86) are hard-coded constants that track no scene light at all; the beer-shadow `strength` was a literal `1.0` at four consumer sites; and neither environment manager's radiance bake carried an eclipse term. So at totality the deck and every IBL-lit model stayed at full midday brightness over a world already at the ~5-lux twilight floor — the C12 exit-gate divergence class, here cross-SUBSYSTEM rather than cross-backend. **Publication verified at HEAD (names re-derived, not trusted):** `Scene/EclipseState.js` `updateEclipseState` publishes `frameState.eclipseState` from `Scene.js:6010` (`prepareLogicalViewEclipse`), and `getEclipseSceneLightFactor` publishes `frameState.eclipseSceneLightFactor` at `Scene.js:6016-6019`; `Scene/computeSolarObscuration.js` supplies the limb-darkened overlap. Nothing about the eclipse is recomputed by this row. **Design — TWO derived scalars, one new backend-neutral module** `Scene/EclipseCloudResponse.js` (+ co-located `.d.ts`, `export default` present): (a) `resolveEclipseCloudFactor` returns S2's `eclipseSceneLightFactor` and drives cloud DIRECT light, cloud AMBIENT, and both environment bakes; (b) `eclipseCloudDirectionalFraction` returns `visible / (visible + ECLIPSE_RADIOMETRIC_FLOOR*(1-visible))` — the DIRECTIONAL share of the surviving flux — and drives the cast-shadow strength alone. **REFUTATION that forced (b):** the obvious implementation, scaling shadow strength by S2's factor, is arithmetically wrong. Shadowed ground is `F * mix(1, 0.35, s*F)`; at `s = 1` that is `F*(1 - 0.65F)`, which **rises** from its un-eclipsed `0.350` to a peak `0.3846` at `F = 0.769` — a shadowed patch of ground would get ~10% BRIGHTER through the whole early partial phase, with the shadow lifting and the world dimming in opposition. Measured first rise at obscuration `0.000005`, i.e. immediately. The directional fraction is instead exactly `1.0` at zero obscuration, `0.99955` at 90% obscured, `0.9524` at 99.9%, and exactly `0` at totality; monotone (`d/dvisible = FLOOR/(FLOOR + visible(1-FLOOR))^2 > 0`); and it reuses S2's constant with no tuned number of its own. Physically: an eclipse dims the direct beam and the skylight together, so the shadow's contrast RATIO is invariant until the nonlocal umbral floor takes over and the shadow genuinely vanishes. It deliberately omits S2's adaptation exponent — that is a display transform, not a change in the directional/nonlocal split. **ONE OWNER preserved.** `WebGPUCloudShadowFrame.ts` (C13-06) still owns the geometry and is untouched; the new `strength` travels the SAME `context._cloudCache` seam `shadowAbsorption` already uses, published once per frame in `executeProceduralClouds` next to `shadowActive`, and read by all four consumers (`WebGPUGlobeSurfaceCameraUB` cascade + single branches, `WebGPUAerialPerspectiveEffect` via `WebGPUPostProcessStageCollection`, `WebGPUVolumetricFogRenderer` slot 98). No second path; no consumer re-derives. C13-38's `(wantMarch \|\| lastUsedCloudMarch) && revisionChanged` gate is untouched. Not folded into `C13-34`. **IBL leg ships BOTH halves on BOTH backends,** because either half alone is a defect: dim-without-trigger latches, trigger-without-dim burns a cube fill to reproduce the same bytes. The dim lands on the documented lockstep twin pair — WebGPU `SkyUniforms.scatteringIntensity` (slot 34, `data[34]`) ↔ WebGL `u_brightnessSaturationGammaIntensity.w` (`adjustments.w`) — the manager-level multiplier on the FINAL sky/ground/reflected-cloud radiance on both sides, so **no shader was edited on either backend**. The SH step-3 multiply is deliberately NOT dimmed on either backend (it projects the already-dimmed cube; a second multiply would square the factor) and the spec pins that. The refresh input EXTENDS the existing debounce rather than paralleling it: `quantizeEclipseEnvironmentRefreshInput` snaps to the SAME `1/256` unit grid as `IBL_REVISION_UNIT_STEP` / `CLOUD_COVERAGE_REFRESH_EPSILON`, returns an exact INTEGER bucket, and enters each manager's existing dirty predicate as a LEVEL comparison (`eclipseEnvBucket !== cache.lastEclipseEnvBucket` on WebGPU, grouped with the sky-bake terms; `eclipseEnvBucket !== this._lastEclipseEnvBucket` inside `regenerateEnvironmentMap` on WebGL). **That symmetry IS the anti-latch mechanism** — recovery needs no second code path, because a factor returning to 1.0 walks the bucket back to the identity bucket and fires exactly one final fill. The WebGPU commit lives ONLY in the `refreshGranted` branch after the complete fill, so C11-193's lossless deferral is preserved; `NaN` sentinels make the first frame always run. **WebGL parity decision, made deliberately and recorded:** the WebGL cloud path is the billboard `CloudCollection`, which gets **NOTHING**. `CloudCollectionFS.glsl` has no sun direction, no scene light and no N-dot-L; its only brightness input is `v_brightness`, a per-`CumulusCloud` AUTHORED appearance property. It renders identically at noon and midnight and was already exempt from every S2 site, so an eclipse-only multiply would be the ONLY light response it has — an inconsistency rather than parity, and it would silently reinterpret user data as a light. The premise is re-verified by the gate (E2), not assumed. The WebGL IBL, by contrast, DOES have a real counterpart and got the full symmetric treatment. **OCCUPANCY (C13-39 mandate): zero, structurally.** All seven sites are CPU-side uniform multiplies; `git diff` touches no `.wgsl` and no `.glsl`, so there is no new static register pressure and no variant split is owed. Gate E1 enforces it by requiring no `C13-41` marker in any of the seven shaders this row's levers live in, plus no `eclipse` token at all in the six that have none (`GlobeTerrain.wgsl` is excluded only because C12-29 S5's per-fragment umbra predates this row, and its `sampleCloudGroundShadow` body is separately asserted eclipse-free). **Evidence:** new `Tools/visual-regression/eclipse-cloud-ibl-response.spec.mjs` **17/17**, pure Node, no browser — exact-identity on every accessor (`applyEclipseCloudDimming(v, 1.0) === v` asserted with `assert.equal`, no tolerance), the rejected-design refutation with its measured peak, monotonicity over a 20,000-sample sweep, exact quantization edges (bucket `k` covers `[(k-0.5)/256, (k+0.5)/256)`; identity bucket 256 covers factor ≥ `0.998046875`), the one-way stale-dark latch mutant reproduced and rejected, deferral losslessness, sub-step jitter inertness, and **10 source mutants** each proven to break its own check via `pinWithMutant`. Full pure-Node lane **1697 pass / 4 fail**, where all four failures are the pre-existing bare-worktree `ERR_MODULE_NOT_FOUND` specs (`model-native-pipeline-stage-tax`, `moon-normal-strength-policy`, `moon-webgl-mip-policy`, `webgpu-snap-framebuffer-lifecycle`). `tsc --project packages/engine --noEmit`: 135 `TS2307`, **non-`TS2307` = 0**; root `npx tsc --noEmit` clean; Prettier clean on all changed + new files; ESLint clean per file. **One collision found and resolved honestly:** the first placement put `eclipseEnvChanged` between `cloudMarchPathChanged` and `captureModeChanged`, which broke `webgpu-dynamic-environment-recovery.spec.mjs`'s pinned adjacency of the capture run. The term was MOVED to the sky-bake group (after `lutPathChanged`), which is also where it belongs semantically since it is deliberately not march-gated — no existing assertion was widened or edited. **Feature preservation:** no cloud or IBL feature removed, default-disabled or degraded; every site is `* 1.0` bit-exact outside an enabled solar eclipse. **Edge predictions (falsifiable, pre-registered):** at obscuration `0.9` the eclipse factor is `0.464228` and the directional fraction `0.999550`, so (i) the cloud deck's PRE-tonemap radiance ratio vs no-eclipse is exactly `0.4642`, and post-Reinhard the DISPLAYED ratio lands between `0.46` (faint deck) and `~0.63` (a bright core at exposed radiance ~1) — never above ~0.7; (ii) the ground cloud-shadow contrast changes by **+0.08%** (`mix(1,0.35,s)` goes `0.350000 -> 0.350293`), i.e. visually unchanged — a probe measuring a large shadow-contrast change at 0.9 REFUTES the model; (iii) a `0 -> 0.9 -> 0` obscuration sweep produces **exactly 275** environment refreshes (one first-frame baseline + `2 x 137` bucket edges, buckets `256 -> 119`), which is quiescent on roughly two thirds of an 801-frame sweep. **STILL OWED:** Edge acceptance (an eclipse-lighting cloud probe with clouds-on/off and eclipse-on/off legs, plus an IBL recovery leg that steps a clock through totality and out the other side and asserts the environment brightens back); the wall-clock cost of the 275 fills is unmeasured (`C13-41-ECLIPSE-REFRESH-COST-UNMEASURED`). **NOT closed here** and filed in `DEFERRED_WORK.md`: the deck's ambient MAGNITUDE is still a hard-coded constant at all times of day (`C13-41-CLOUD-AMBIENT-IS-A-CONSTANT`), and both bakes' ground-facing texels keep an undimmed inscatter ADDEND, symmetrically on both backends (`C13-41-ENV-GROUND-INSCATTER-ADDEND-UNDIMMED`). A measured, bounded terminal transient in shadowed ground at second contact is documented rather than smoothed away (`C13-41-SHADOW-TERMINAL-TRANSIENT`). **Rollback boundary:** `Scene/EclipseCloudResponse.{js,d.ts}`, the two cloud-uniform multiplies and the `shadowStrength` publication in `WebGPUProceduralCloudRenderer.ts`, the four consumer strength reads, the dim + bucket in both environment managers, and `eclipse-cloud-ibl-response.spec.mjs`. **★ CO-9 ADDENDUM (2026-08-07) — THE PROBE NOW EXISTS AND HAS NEVER RUN.** `Tools/visual-regression/probe-eclipse-cloud-response.mjs` + `lib/eclipse-cloud-response-gate.mjs` (the pure predicate/band half) + `eclipse-cloud-response-gate.spec.mjs` (`node --test`, **36/36**). This closes the "authoring is the first step of this acceptance, not a re-run" clause above; **the Edge run itself is still owed and no band below has ever been measured — every one carries `status: "DERIVED"`.** **THE THREE PREDICTIONS ARE EACH BOUND TO A NAMED PREDICATE.** (i) deck lighting -> `deckRatioInBand`, band **[0.44, 0.70]** (the row's own [0.46, ~0.63], never above ~0.7, with the lower bound relaxed 5% relative below 0.4642 for 8-bit quantization on a DIFFERENCE image, since the deck is isolated as `cloudsOn − cloudsOff` within each eclipse position so the S2-dimmed sky cancels exactly). (ii) cloud shadow -> `shadowContrastInvariant`, band **[0.97, 1.03]** on the contrast ratio `(shadowOn/shadowOff)|eclipse ÷ (shadowOn/shadowOff)|clear` — two orders of magnitude wider than the predicted **+0.084%** and set by capture noise, not by the effect. **That band is also the refutation the row asked for:** a dedicated 0.5452-obscuration rung is added precisely because the REJECTED design (strength = S2's factor) peaks there, and it puts the ratio at **1.429** — 14x outside — so `shadowContrastRejectsAlternativeDesign` fails if the substitution ever lands. `shadowStrengthMatchesDirectional` additionally pins the published `_cloudCache.shadowStrength` to 1e-9 against a second implementation of `visible/(visible + FLOOR*(1−visible))`. (iii) the 275-refresh cadence -> `predictedRefreshCountExact` (**=== 275**, DERIVED in the gate module from the published constants rather than written down, so a retune of the 5-lux floor, the 1/3 exponent or the 1/256 grid moves it and the gate notices), the two engine-count bands **[234, 275]** (275 is the arithmetic CEILING: both managers commit the bucket only inside the branch that re-fills, so a transition IS a fill, and a C11-193 deferral can only MERGE adjacent edges, never create one), and `sweepQuiescenceInBand` **[0.60, 0.75]** against the row's "roughly two thirds" (527/801 = 0.658). **THE SWEEP'S SHAPE IS A DELIBERATE CHOICE, RECORDED:** 401 real instants aimed at the linear obscuration ramp by bisecting the engine's OWN `updateEclipseState` (AIMING — every gate compares against a second implementation, never the engine's accessor), then a REVERSE REPLAY for the descending branch, so the factor series is an exact mirror and "exactly 275" is determinate instead of a coin flip on where a real falling branch's samples land. `rampNeverSkipsABucket` gates the margin that makes the count *edges* rather than *jumps* — 401 frames give 0.89 buckets/frame at the steep end, and the spec proves a 201-frame ramp DOES skip and DOES under-count. **THE TWO STILL-OWED ITEMS THE ROW NAMES ARE BOTH IN THE RUN.** The IBL recovery leg is `iblRecovers` (**[0.98, 1.02]**), which steps the clock through the deep phase and out the other side and requires the model's environment-lit brightness to return to its own baseline — the stale-dark latch is reproduced as a spec mutant and must fail it. `C13-41-ECLIPSE-REFRESH-COST-UNMEASURED` is discharged by an identical 801-frame CONTROL leg with `enableEclipse` OFF (which produces exactly one fill), making the wall-clock differential attributable to the eclipse-driven fills; `refreshCostMeasured` gates only that a real non-negative number came back, because the row registers no budget to score it against — **that row does not discharge on a run where the differential cannot be formed.** **WEBGL GETS NO CLOUD LANE, deliberately:** its cloud path is the billboard `CloudCollection` and the E2 premise is a SOURCE claim already pinned by `eclipse-cloud-ibl-response.spec.mjs`; re-litigating it in pixels would only add a lane that cannot fail. WebGL DOES run lane C, because the IBL leg ships symmetrically and the anti-latch mechanism is the same quantized level comparison. **INSTRUMENT HONESTY:** vacuity is STRUCTURAL, not a verdict — a deck below the contribution floor, a cast shadow that does not darken the ground, an unlit model band, a wrong `rendererType` or a missing lane all exit **3**/**2** and emit NO `failedPredicates`; the spec's E-group proves each of those five. `engineRefreshCountExactReportedOnly` and `deckRatioMatchesLinearReportedOnly` are computed and printed but explicitly NOT gating, with the reason each may legitimately read false, so a reader cannot mistake a diagnostic for the cause (the failure mode `probe-eclipse-scene-dimming` had to name after being misdiagnosed twice in one day). **WHAT THE ORCHESTRATOR OWES:** `node Tools/visual-regression/probe-eclipse-cloud-response.mjs` on Edge against a current `gulp build` (the probe hard-gates provenance on four verbatim markers and refuses a stale bundle), then TIGHTEN the bands against the observed margins and flip `status` off `DERIVED`. Widening a band to make the first run pass is the failure mode every `why` string in the gate module exists to make visible. |
| `CLOUD-LOW-COVERAGE-CUTOFF` (fog cheap-path arm) | **IMPLEMENTED — 2026-08-06, Edge acceptance owed. DO NOT promote to COMPLETE.** | **Row provenance:** this campaign never carried a numbered row for it; the work item is the 2026-08-02 Codex handoff §5 item 3 ("Fog cheap-path coverage gate — the queue row's remaining arm") plus the `CLOUD-LOW-COVERAGE-CUTOFF` entry in `DEFERRED_WORK.md`. Recorded here so the arm is traceable from the campaign that owns the defect family. **Premise correction (the brief's framing was half wrong):** `Compute/VolumetricFog.wgsl:608` WAS the last raw `1.0 - coverage` gate in the cloud-density family — the 2026-08-01 fix converted `ProceduralClouds.wgsl` (x3) and `ProceduralSkyCubemap.wgsl` (x1) and missed the fog, and `cloud-coverage-response.spec.mjs`'s "every coverage gate routes through the shared response" test never read the fog file. But the fog gate was NOT structurally dead below 0.40 like the visible march, and routing it through `cloudEffectiveCoverage` alone would have made the sparse end WORSE (28.2 pp error, vs 23.9 pp for doing nothing). **Real defect:** the fog's cheap cloud shadow samples its own local 3-octave `fbm3d`, not the baked textures, and a coverage threshold only transfers between density fields that share a distribution. Measured in f32 with the shipped arithmetic: baked shape channel mean 0.4307 / sigma 0.0896 / max 0.716 (85,184 samples over its full period) vs fog field mean 0.5000 / sigma 0.1206 / max 0.936 (41,472 samples at the real ECEF magnitudes, so the GPU's f32 hash quantisation is inside the measurement) — symmetric about 0.5 by construction and 35% wider. Against the post-fix visible deck the fog gate admitted 0.07% at coverage 0.15 (deck 2.21%) and 65.0% at 0.55 (deck 41.3%): fair-weather skies cast almost no fog shadow, scattered skies cast a near-overcast one. Worst error **23.9 pp**. **Fix — a re-derivation, not a rescale** (the acceptance recorded for the cloud arm forbids a shifted cutoff): standardise the fog SAMPLE onto the baked field's first two moments via `normalizeFogCheapCloudField` (`0.4307 / 0.5 / 1.3459`, all measured; the fog mean exact because the symmetry is structural), then apply the shared response unmodified. Normalising the sample rather than moving the threshold also matches the `smoothstep` RAMP — at coverage 0.55 the gate at the field's 90/95/98/99.9th percentiles goes `0.323/0.431/0.550/0.798` -> `0.085/0.139/0.208/0.389` against the deck's `0.081/0.127/0.196/0.396`. Worst support-tracking error **23.9 pp -> 1.5 pp** over coverage 0.05..1.0. `CloudDensityDomain.wgsl` is now prepended to the fog compute module by `WebGPUVolumetricFogResources` (module scope — `WebGPUShaderModuleCache` keys on `(sourceId, defines)`, never on source text), so the engine still holds ONE definition of the response. **Files:** `Shaders/WebGPU/Compute/VolumetricFog.wgsl`, `Renderer/WebGPU/WebGPUCloudDensityDomain.ts`, `Renderer/WebGPU/WebGPUVolumetricFogResources.ts`, new `Tools/visual-regression/fog-cheap-coverage-gate.spec.mjs` + `lib/fog-cheap-coverage-model.mjs`, the naga case in `cloud-shadow-rte.spec.mjs`, `SHADER_PAIRS_LOCKSTEP.md`. **Evidence:** new spec **15/15**, executing the real `WebGPUCloudDensityDomain.ts` and the real shader text, with a four-way in-file MUTATION group (historical gate / normalisation deleted / response reverted / sigma ratio flattened to 1) that requires the passing measurement to REJECT each mutant; the tree was additionally mutated three ways and confirmed red (WGSL gate reverted -> 3 red, twin ratio 1.0 -> 6 red, composition dropped -> 1 red). Metrics are banded (threshold exceedance) or point (the gate at one order statistic), never field means — the campaign's recorded trap. Full pure-Node lane **1404 pass / 4 fail**, the four being the pre-existing bare-worktree `ERR_MODULE_NOT_FOUND` specs. Naga validates `CloudDensityDomain + VolumetricFog`. `tsc --project packages/engine`: 135 `TS2307`, non-`TS2307` **0** (134 -> 135 is the new generated-shader import, same class as the two existing `CloudDensityDomain.js` importers); root `npx tsc --noEmit` clean; Prettier and ESLint clean per file. **Functionality preservation:** the change lives entirely below the `cloudShadowHiFi` / `cloudShadowEnable < 0.5` / `coverage <= 1e-3` early returns, so a scene without volumetric clouds is byte-identical and the opt-in hi-fi beer-shadow path is untouched; nothing removed or default-disabled. `VolumetricFog.wgsl` is fork-only, so no lockstep row is owed. **OWED (do not self-promote):** an Edge run with volumetric clouds ON at coverage 0.15 / 0.35 / 0.55 showing the shadowed ground fraction tracking the visible deck, plus a clouds-OFF byte-identical control. **Rollback boundary:** the `sampleCloudShadow` gate + `normalizeFogCheapCloudField` in the WGSL, the three constants and the twin in `WebGPUCloudDensityDomain.ts`, the `VOLUMETRIC_FOG_COMPUTE_SOURCE` composition, and the two new spec files. |
| `C13-GATE-A` | DEPS SATISFIED, DECLARATION OWED (2026-08-21 audit): C13-01 tour-green, C13-02 COMPLETE B907, C13-35 COMPLETE B734 - the gate is undeclared, not un-started; executing its evidence checklist is the remaining work | Follows `C13-01/02`. |
| `C13-GATE-B` | **COMPLETE — CLOSED 2026-08-07 (Batch 866)** | `C13-03`, `C13-04`, and `C13-05` are complete. `C13-06` and `C13-07` are implemented and probe-verified. `C13-08` now has a 31/31 CPU twin, the cyclic-axis parser/mutation lane (7/7; 38/38 combined), the regional CELL edge-clamp correction, and the Batch-806 regional-placement pixel pass. The final persistent Edge tail now passes: WebGPU east/west/seam/far cyclic CoverageJSON lanes satisfy continuity, no-wall/no-duplicate, procedural-fill, regional-stat, and clean-device gates; WebGL preserves a non-vacuous billboard control byte-for-byte while issuing zero volumetric publications. Companion tail contracts are 10/10 and all ten PNGs were reviewed. Evidence: `output/weather-regional-tails/manifest.json`. The retained tail evidence remains valid for its node-registered fixture, but promotion to COMPLETE and the formal `C13-41` unblock require post-change green reruns of the five global ingest/source/channel/time probes, seam/poles, and METAR, followed by review and landing. **Batch 860 acceptance:** four of the five pinned probes certified 3/3 (`edr-mock`, `wcs`, `ingest`, `seam-poles`); `metar` went RED 3/3 on gate 4. **Batch 861 — DISCRIMINATOR + CHANNELS PIN AUTHORED, EDGE RUNS OWED.** `metar` gate 4 is re-aimed at CALIBRATED partial coverage with an in-probe headroom precondition (`collectHeadroomStructural`, exit 3 if the aim misses) after reading B was confirmed arithmetically — the old band's effective coverage was 0.90..1.00 at every sampled point, so the gate had never been able to move — and reading A was refuted at source (`decodeWeatherChannels` is called from all three density evaluators and its outputs are consumed). `probe-weather-channels.mjs` is migrated off its private pin copy onto `lib/weather-probe-pinning.mjs` and its determinism control now BRACKETS the source and strength changes gates 3 and 5 read across (`richA -> neutral -> richOff -> richB`). No assertion was widened or lowered; new browser-free guard `weather-probe-headroom.spec.mjs` 29/29 with 9 adversarial mutants. **Owed before Gate B can close:** one Edge run of `probe-weather-metar.mjs` (the discriminator) and ten consecutive Edge runs of `probe-weather-channels.mjs` on one build. **PAID 2026-08-07 (Batch 866), on one build (tip 193393790c, post-864 rebuild):** the metar discriminator ran GREEN with the aim structurally proven — 22/30 calibration candidates in-band, selected band [-98,-90,-84,84,90,98,104], scored-leg headroom summed |delta| 0.0000 against its 0.015 tolerance — and gate 4 measured **sum|ch1-ch0| = 0.1469 against the unchanged 0.04 bar** (3.7x clear), settling reading B end-to-end: the channel path was always live, the original gate was blind. `probe-weather-channels` ran **10/10 GREEN with the scored rich-fraction vector BYTE-IDENTICAL across all ten runs** (rich mean 0.8758, stddev 0.2889, spread 0.0000 — the pre-registered prediction). Full instrument roster at closure: `edr-mock` 3/3, `wcs` 3/3, `ingest` 3/3, `seam-poles` 3/3 (Batch 860), `time` exempt-by-proof (Batch 855), `metar` GREEN via discriminator, `channels` 10/10. Exit-gate criteria (§ gate table) all have green evidence; no assertion was widened at any point in the chain. See `C13-GATE-B-METAR-CHANNELS-NO-EFFECT`, `C13-GATE-B-CHANNELS-PROBE-NONDETERMINISM` and the new `NEW-PROBE-SATURATED-DIFFERENCE-METRIC` class in DEFERRED_WORK.md. |
| `C13-GATE-C` | NOT STARTED | Follows `C13-09..13` and `C13-36..41`; STBN may remain an explicit blocker if its provenance is unresolved. |
| `C13-GATE-D` | NOT STARTED | Follows `C13-14..20`. |
| `C13-EXIT` | NOT STARTED | Dead last. |

**C13-41 STATUS — REOPENED 2026-08-14 by maintainer ruling `R-2026-08-14-1`
([`MAINTAINER_RULINGS_2026-08-14.md`](MAINTAINER_RULINGS_2026-08-14.md);
audit findings S2/S3 in [`SOL_WEEK_AUDIT_2026-08-14.md`](SOL_WEEK_AUDIT_2026-08-14.md)).
This stamp is AUTHORITATIVE over both reconciliation blocks below — they are
retained verbatim as the record of what was believed on 2026-08-12, not as the
current status.** The 2026-08-12 `COMPLETE / EDGE VERIFIED` verdict rested on
two gate demotions that the ruling REVERSES:

- `shadowContrastInvariant` — the criterion measured failing at **1.0496** — had
  its subject swapped for a newly-authored decrement model and the original
  reading moved to reported-only. The raw display band **[0.97, 1.03] has not
  moved** and now gates again; the decrement model is retained beside it as
  `shadowDecrementMatchesGroundDim` / `shadowDecrementRejectsAlternativeDesign`.
- `refreshCostMeasured` was demoted on the authority of a **7.749 / 1.607
  ms/refresh** measurement for which **no artifact is recoverable** (audit S3;
  recomputation from the only two retained `refreshCost` blocks gives 7.951 and
  14.06). The protective `ECLIPSE_CLOUD_GATE_PREDICATES.includes(...)` guard
  deleted in the same commit is restored, and the quarantine assertion that had
  been inverted — letting an unsettled deck-free CONTROL silence lane B's own
  red — is un-inverted: the prediction-(ii) checks are back in the `shadow`
  domain.

**EXIT CRITERIA for the reopened row (both required, ruling rider):**

1. **A fresh, banked refresh-cost measurement** (SOL-4): an Edge run whose **RULED `R-2026-08-21-24` (2026-08-21 evening): BOTH — the 2026-08-21 runs are BANKED as the artifact of record (WebGL 3.342 / 2.714 ms per refresh; WebGPU unmeasurable by wall clock at this fixture's frame cost, the negative-differential record retained), which satisfies this prerequisite on honest evidence; AND the cost lane is being re-instrumented on GPU timestamp queries (Sol package dispatched 2026-08-21 evening) as the durable instrument. Until that runs, the WebGPU cost is a bound, not a figure.** **RE-INSTRUMENTATION LANDED (Batch 1131, 2026-08-21 evening; Sol build, Opus station-3 review, two severity-1 findings closed at landing):** the refresh-cost lane now carries a protocol-v2 GPU-timestamp segment ledger (per-segment samples bound to fills, eight counters closed over frames, capture-hook identity restored, WebGL declared `unavailable` by exact witness with wall clock kept as its figure of record). Two honesty constraints were added by the review: the timed pass is `DynEnvMap Sky Fill` ALONE — one of 44 compute passes a refresh encodes (6 irradiance + 36 radiance prefilter dispatches and the SH projection carry no `timestampWrites`) — so the protocol declares `scope: sky-cube-bake-only` and the figure is a LOWER BOUND on the refresh, printed as such; and an all-zero GPU differential at the undeclared timestamp resolution is INVALID (a bound, not a figure), never a certified 0 ms. Consequence: this instrument cannot yet turn the WebGPU cost from a bound into a figure — round 2 (label the remaining refresh passes through `withComputePassTimestamps` and sum a declared pass set) is dispatched; the first Edge run of the lane is instrument commissioning, not acceptance.** **ROUND 2 LANDED (Batch 1136, same evening; Sol build, Opus station-3 review, no code defect): every compute pass the refresh encodes now carries a label through the engine's timestamp wrapper (sky fill, IBL irradiance ×6, IBL radiance prefilter ×36, SH projection, plus the optional temporal blend; the HQ source-mip chain folds under the prefilter label), zero cost when profiling is off; the lane is protocol v3 with `scope: "whole-refresh"` over a declared `passNames` set, four mandatory labels per fill, per-label samples re-summed by the gate, v2 rejected. At this fixture's defaults the refresh is exactly 44 passes under 4 labels, so the figure is no longer a lower bound BY CONSTRUCTION — but it remains a BOUND IN THE LEDGER until the Edge commissioning run executes (owed to the machine lane after a bundle refresh; the profiler's 128-pass-per-frame query budget may surface as a loud `droppedPassCount` INVALID on a fill frame, fixable by raising the budget, not the lane). **SOL-4 FIRST COMMISSIONING RUN (2026-08-24, run `348ab43d`, report sha256 `dc6c74b9...50e6d57`, protocol v3) — THE INSTRUMENT WORKED AND REFUTED ITS OWN "PER FILL" PREMISE.** Exit 1: `shadowContrastInvariant` FAIL, refresh cost INVALID and quarantined. `droppedPassCount` was **0 in all sixteen segments** — the anticipated failure did not occur; a different one did. **"Four mandatory labels per fill" is wrong, because a fill is not a refresh.** `eclipseEnvChanged` is one of ten disjuncts in `WebGPUDynamicEnvironmentMapManager.ts:1194-1206`, and `:652` re-commits `lastEclipseEnvBucket` on *every* refresh, so a refresh fired for another reason leaves no bucket transition and the probe never counted it. **The proof is in the banked data itself: the eclipse-OFF control legs record 0 fills and 82 retained per-pass samples (16/9/8/8/8/8/9/16 across the eight segments)** — zero eclipse-driven fills, eighty-two environment refreshes. A first-principles sun-motion model (`SUN_REFRESH_EPSILON_SQ = 0.005^2`, 71.1 s of clock per refresh at this fixture's latitude and date) predicts 91.4 against the 82 measured, segment for segment. **READ THE FOURTEEN NUMBERS CORRECTLY:** 25/16/9/25/33/8/8/8/8/34/25/9/16/25 are **per-pass RETAINED GPU SAMPLE COUNTS under protocol v3 on counter-clean segments** (`readbackSkipCount` 0, `frameCount` = frames, `droppedPassCount` 0) — they are **not** v4 `refreshSubmissions`; **no banked report carries any v4 field, because v4 had not yet run when those were banked (v4’s first reports arrived 2026-08-25 — see the PROTOCOL v4 stamp at the end of this row).** Counter-clean is also not the same as valid: **only four segments have `gpuTime.valid === true`** (eclipse pairs 1/2/5/6), since a counter-clean segment whose sample count still disagrees with its fill count is refused. **Cost, derived from those four valid segments only: 30.907 / 28.878 / 28.649 / 28.453 ms per refresh, sample-weighted mean 29.154 ms over 117 samples, at <= 34 refreshes per 100 frames.** The two steepest segments (57 and 56 fills per 100 frames) are excluded because the profiler's 3-deep readback ring saturates there (`_bufferCount = 3`, `WebGPUTimestampProfiler.ts:198`; 14 and 37 skipped frames) and **the direction of their bias was, at depth 3, both KNOWN and pointing LOW** — superseded as of the depth-8 re-run, which removed the loss that created it. The depth-3 per-refresh cost was bimodal (36.652 / 72.711 ms, ratio 1.984, every timed pass scaling together — a device-wide rate signature), and in the five segments carrying 80 of the 84 lost samples the transition into the expensive regime *preceded* the loss run, so the dropped samples were the expensive ones and the retained mean UNDER-stated; the true eclipse mean was bounded [44.116, 54.934] ms. The cross-segment correlation (Spearman ρ 0.643) is real but does not bear on this, because the bias question is *within* a segment. **TWO RIDERS THAT STILL GOVERN ANY COMPARISON:** (i) **depth-8 numbers are NOT directly comparable to the depth-3 run** — a skipped frame ran with no `timestampWrites` at all, so at 56% skips the profiler was alternating instrumented and uninstrumented frames and timing only the instrumented ones; the deeper ring measures a DIFFERENT POPULATION, not the same one more completely. (ii) **Depth cannot cover an unbounded latency** — in the worst depth-3 pair the timed passes accounted for ~4032 ms of GPU work in a 4164 ms segment (~97% occupancy), and when submissions outrun completion no finite depth suffices; depth 8 is the right fix for the measured regime and only that regime, and the gate must keep refusing a mean over survivors rather than reporting one. This is still not an all-density figure. It does retire the "GPU power-state down-clock on the idler leg" hypothesis: ~91% of the timed legs' wall time is animation-frame yield, so wall clock structurally cannot see asynchronous refresh work. **Instrument repaired, instrument-only, no engine change (protocol v4):** refreshes are counted per frame from `WebGPUContext.getEnvironmentRefreshStats().submissions` under a strict `frameId + 1` chain, WebGL from `_lastTime` value transitions, and **the gate re-derives the count from the retained per-frame witness — the probe's declared field is cross-checked and never operative.** Cardinality is no less strict, only correctly keyed; every invalid segment is named; a ledger that does not close names the counters that tripped it. Spec 145 -> **161** -> **168**; fleet 62/62 -> **68/68**; purpose-header 18/18; prettier/eslint clean; root tsc 0; 13 mutants, all caught. **`shadowContrastInvariant` stays RED at 1.0341102079879674 and the band stays [0.97, 1.03].** Ten recorded reports in three value clusters, relative spread **1.4e-5**, outside the band in **all ten** — and one of them, `run-53c67362` (2026-08-12T16:59:58), scored that same reading as **`shadowContrastInvariant: true`, status PASS, exit 0**: the READING was outside the band in all ten reports; the VERDICT was green in one pre-restore report. That is a genuine PASS of a gate `R-2026-08-14-1` then superseded, and it corroborates the re-open. The refuted clause is prediction (ii)'s *"an eclipse dims the direct beam and the skylight together"*: the shadow lane's band also contains ProceduralClouds' independently tone-mapped over-composite. Decrement-cancelled, `terrainDim = 0.46887558379804545` (1.0101 x F — the shadow path is right to 1%) against `compositeDim = 0.49013073670762686` (1.0559 x F); substituting `terrainDim` for the residue's law reconstructs a raw ratio of exactly 1. **The beer floor then bounds the residue: `clearContrast = T*(1-share) + share` with `T >= 0.35` gives `share <= 0.32266890343992366`, and since `requiredResidueDim` is decreasing in share that ceiling is a FLOOR of 0.5347485349345928 on the residue's own dimming — ABOVE the independently measured `deckRatioAtDeepest` 0.513868583346416, whose implied share 0.4724102220997878 is OUT OF RANGE. The visible cloud deck alone therefore does NOT account for the excess**; the mechanism class (an un-shadowable residue dimming more slowly than the terrain) stands, and something in it dims more slowly still. Carried in the instrument as reported-only `shadowResidueDimLocus`, `shadowResidueShareAtDeckRatio`, `shadowResidueShareCeiling`. **No verdict is claimed; the machine-lane Edge run earns those.** Recorded Batch 1154. Spec 168/168; fleet 68/68; purpose-header 18/18; engine-project tsc green in main.** **PROTOCOL v4 HAS PRODUCED ITS FIRST REPORTS, AND THE DEPTH-8 RE-RUN IS THE ARTIFACT OF RECORD — run `8b806b09-004c-48ab-b902-f4fce64cd109`, 2026-08-25T18:15:11Z→18:19:45Z, `incomplete: false`, GATE FAIL / exit 1.** The first v4 attempt that day died at the first cost segment of the first IBL sweep with `ReferenceError: describeRefreshCostLedgerClosure is not defined`: the describer landed at Batch 1154 as a Node import (`probe :179`) called inside `runCostSegment` ⊂ `RUN_IBL_SWEEP` ⊂ `page.evaluate(:3466/:3473)`, and `page.evaluate` drops the callback’s closure. The 161-test spec was GREEN over that unreachable path because its call-site coverage was substring counting — the Principle 10 failure mode, in a probe whose own header at `:137-138` states the rule it broke. **THE RUN CARRIES EXACTLY ONE ENGINE-RELEVANT RED: `shadowContrastInvariant` false at `shadowContrastRatioAtDeepest` 1.0341102079879674 against the ruled [0.97, 1.03] band — bit-identical to the value already recorded, so it is NOT re-scored and the band does NOT move.** (`shadowContrastRatioAtDiscriminating` 1.0144533824422224, `shadowContrastClear` 0.5632631754241546, `shadowContrastModelIsBoundedByDirectional` true.) **CARDINALITY IS GREEN AND EXACT:** `predictedSweepRefreshCount` 275, `engineRefreshCountWebGPU` **275**, `engineRefreshCountWebGL` **275**, both `InBand`, `predictedRefreshCountExact` true — v4’s per-frame witness re-derivation hit the pre-registered 275 on both backends, which is the thing v4 was built to do. **READING THE ARTIFACT:** its top-level `schema` string still reads `c13-41-eclipse-cloud-response-v3` while `refreshCost.protocol.version` reads `4`; these are different axes (report schema vs refresh-cost protocol) and the `-v3` tail must not be read as “not a v4 run”. Repair landed instrument-only: the table and describer are embedded in the callback as canonical TEXT **generated from** `REFRESH_COST_LEDGER_EXPECTATIONS`, pinned byte-for-byte. The class now has an **enforceable home** — `lib/page-scope-closure.mjs` plus fleet-contract teeth **G1–G6**, scoped to probes AND `lib/` on the F4 precedent, which turn RED on the pre-fix probe and on a fabricated new violator. Spec **161 → 168**; fleet **62 → 68**; purpose-header 18/18; nine mutants across two families, one inertness form each, zero survivors. **RING DEPTH 8 ELIMINATED THE SATURATION, AND THE “EMPTY DRAIN” READING IS REFUTED.** The depth-3 ring was the measured cause of the earlier loss: WebGPU lost 84 of 280 samples against 1 of 82 on its control, with every post-capture counter at zero and the sample ledger balanced in all sixteen segments — the frames were **never armed**. The ring advances one slot per profiled frame and a capture holds its slot until `mapAsync` resolves, so the depth a workload needs is its readback latency in frames; the worst segment sampled 44 of 100 attempts through three slots, bounding that latency at **6.82 frames** against a depth of 3. The probe now calls `profiler.setBufferCount(8)` and **reads the depth back off the profiler rather than assuming it**, publishing it on the report’s observed `refreshCost.protocol.gpuTime.ringDepth` (never inside the frozen `REFRESH_COST_GPU_TIME_PROTOCOL` contract). **Run `8b806b09` records `ringDepth: 8`, `ringSaturated` on ZERO of sixteen segments, and `gpuTime.status "valid"` on FIFTEEN of sixteen — against four of sixteen under depth 3.** **WHAT ACTUALLY REMAINS IS ONE SEGMENT, NOT SATURATION:** `verdicts.refreshCostMeasured` is still **false** and `cost.webgpu.gpuMsPerRefresh` still **null**, because pair 0 eclipse is refused with `invalidReason` *“the pre-segment GPU readback drain did not close”* — a **pre-segment** drain that failed to quiesce, which is a different defect from a ring too shallow to arm, and which refutes reading the drain as returning empty. One refused segment is enough for the gate to withhold the figure, by design. The provenance block pins `WebGPUTimestampProfiler.ts` at `fa82456c651df12c040c2dad8a5775596001c546064ee80a09229fb3685297ac` at both start and end, so this run measured the landed ring change and no other. **WebGL cannot close this row — it is a different backend AND a different measurand:** `cost.webgl.msPerRefresh` 3.667279411874273 ms is `wall-clock` with `gpuTime.status "unavailable"` (*“WebGL renderer has no GPU timestamp accounting path”*), while on WebGPU the wall clock is a BOUND (51.321717171656964 ms/refresh) and the GPU time is the figure of record. **No WebGPU per-refresh cost figure is claimed.** **`offNoCloudVariesWithSun = false` IS NOT A SECOND RED — MIS-SPECIFIED PREDICATE, FILED 2026-08-25 (code deliberately UNTOUCHED).** Run `8b806b09` lists it in `verdicts.reportedOnlyPredicates` alongside five other reported-only readings, excludes it from the 32-entry `verdicts.gatePredicates`, and `verdicts.failedPredicates` is `["shadowContrastInvariant"]` alone — so it never affected an exit code, a gate label or a verdict. It is **mis-specified**: its subject, the deck-free eclipse-OFF ground band, is a flat 200/255 pinned base colour that the shipped `clamp(lambertDiffuse * 5.0 + 0.3, 0.0, 1.0)` (`GlobeTerrain.wgsl:2202`) saturates at every rung of the Iceland ladder (NdotL 0.532→0.454, i.e. 2.57×–2.96× above the clamp ceiling — the sun would have to fall below about 8° elevation to move it, while the ladder bottoms out at 27°), so it is STRUCTURALLY INCAPABLE of varying with the sun and its `false` reading carries zero information. **The deck-free lane is emphatically NOT vacuous** — the opposite of the S6 precedent it superficially resembles: four state-isolated sessions with distinct tokens, per-rung distinct `moonObscuration` readbacks (0 / 0.30006 / 0.54527 / 0.90008) proving four live renders, `maximumRawDistance` 5.55e-13, an eclipse-ON twin off the identical capture path varying monotonically 200→178→154→93 bytes in step with the predicted factors, and `deckFreeGroundIsLit` / `deckFreeGroundDimsByFactor` both GREEN at ≈1.002 × F. The predicate’s polarity is backwards — under the shipped law a NON-ZERO OFF spread is the anomaly — and the CO-19 comment block at `lib/eclipse-cloud-response-gate.mjs:1222-1230` assumes the opposite of what `lib/c13-41-deckfree-control.mjs:31-37` and `:956-959` state in the same fleet. **Remedy, stated as observables and NOT implemented in this batch:** assert the eclipse-ON leg is strictly decreasing with each rung’s ON/OFF ratio reproducing the independently predicted eclipse factor at that rung’s own published obscuration; assert per-capture identity (a distinct `moonObscuration` and a per-rung recomputed camera distance — both already in the artifact, neither asserted today); invert the OFF-leg assertion to “invariant AND equal to the pinned deck-free base colour at every rung, within the 8-bit capture delta”, which the fold already computes as `maximumRawDistance` but does not surface as a named reading; and keep `deckFreeGroundIsLit` as the diffuse-path liveness gate, which already does that job correctly and is green. If any of that lands, the CO-19 comment block must be rewritten in the same round. **No band is moved, widened or relaxed by this finding.**
   **SOL-4 RUN LEDGER 2026-08-21 evening (orchestrator machine lane, `--serve-built`,
   `probe-eclipse-cloud-response`): NOT YET BANKED, four runs recorded honestly.** Runs 1-2
   STRUCTURAL on provenance - the served bundle was the U2 A/B-swapped pre-bundle and then
   a missing artifact (the Batch-1114 engine-tsc break aborted every build; fixed forward in
   Batch 1121, bundle rebuilt with `buildCesiumDual` + `build`). Run 3 (`2925d5b4`) on an
   attestable bundle: provenance PASS, parity PASS 2/2, `shadowContrastInvariant` FAIL (the
   open 1.0496 mechanism, item 2 below, unchanged), refresh-cost lane quarantined by an
   INSTRUMENT defect - the protocol check compared the live factor against the scheduled
   ramp under the 1e-9 same-input band while the clock-solved realized obscuration carries a
   ~1e-7 residual; gate repaired in Batch 1123 (factor vs realized obscuration at 1e-9,
   realized vs ramp at the schedule band; spec 127 -> 131). Run 4 (`afff9782`) with the
   repaired gate: factor schedule passes on both backends; **WebGL VALID at 3.342 ms per
   refresh**; **WebGPU INVALID - the control leg outran the eclipse leg (5982 ms vs 5418 ms
   over 801 frames), a negative differential the probe refuses to attribute** - measured
   while two Codex worker sessions ran tsc/eslint/node-test on the same machine, i.e. the
   CPU-contention class the U2 sentinels established. Run 5 (`b7d2971e`) with no
   orchestration load: WebGL VALID again at 2.714 ms per refresh (eclipse 1495 ms vs control
   757 ms over 801 frames, 272 fills); WebGPU INVALID again with the SAME SIGN - control 5092 ms
   vs eclipse 4958 ms. **Two runs agreeing in sign is not contention: on WebGPU both legs run
   at ~6.5 ms per frame (WebGL ~1.2) and the no-refresh control leg is consistently the slower
   one, so a wall-clock ABBA sweep cannot attribute the 272 fills - the historical 1.607 ms
   per refresh would be ~440 ms, inside the observed 134-564 ms leg-to-leg drift. The sign
   pattern (busy leg faster) is what a GPU power-state down-clock on the idler leg produces.
   NEXT STEP IS A RULING (RULING_REQUESTS item 12): the banked cost is an operative exit
   prerequisite under R-2026-08-14-1, so its instrument cannot be changed by the orchestrator -
   either re-instrument the cost lane on GPU timestamp queries (the repository already carries
   `gpuPassCost`) or rule the WebGPU refresh cost unmeasurable-by-wall-clock and below the
   sweep's resolution.** Filed as `NEW-SOL4-REFRESH-COST-BELOW-WALLCLOCK-RESOLUTION`.
   Reports retained under `Tools/visual-regression/output/eclipse-cloud-response-report.run-*.json`.
   interleaved ABBA/warm-up-parity accounting is archived as a retained
   artifact, so the number is reproducible from evidence rather than cited from
   memory. Until it is banked, the run's own estimate gates.
2. **A mechanism investigation of the 1.0496 contrast reading**: the raw
   post-cloud-composite ratio is a real finding and a red on the critical path
   is information. The ProceduralClouds over-composite is a known confound and
   is why the decrement leg exists — naming the confound is not the same as
   explaining the 1.0496, and the row does not close on the confound alone.

No band moved in either direction as part of this reversal; the change is
re-scoring only.

**C13-41 ACCEPTANCE + LANDING RECONCILIATION (2026-08-12; HISTORICAL — superseded
on status by the R-2026-08-14-1 stamp above; authoritative over the stale
`EDGE ACCEPTANCE OWED`, standing-failure, and next-diagnostic clauses inside the
long historical row):** the landing-equivalent post-comment Edge run
`b5e3f63c-94c6-4204-8706-dd30eabd2eaf` is **PASS / exit 0**, with all **30/30**
gating predicates true, no structural, unscored, parity, page, GPU, cleanup, or
provenance failures, and canonical/immutable report SHA-256
`DD9BA7DCAC0434B91C0F88EFFA4804F3F1CB99747D888B5449B2A483236E70EE`
(346,189 bytes). The served build entry/map are
`E49370212931733D12A6813DBED22550FDA44E0E55792E0E3019DA9BAC2D27DA` /
`D20374BCD894349CBDFFFA1AE0B09818ADA430F4207BE9C5899D8092C67125A8`.
The state-isolated deck-free control is lit, settled, and dims by the published
factor; the terrain shadow-decrement model passes inside its propagated
quantization interval, while the old raw post-cloud composite ratio `1.034120`
is retained as reported-only because it mixes the later cloud composite into a
terrain-shadow invariant. This **discharges the C13-41 / C12-29 S3 machine
criterion**. The exact reviewed 16-file selective packet landed as
`9c043987a5`; therefore **C13-41 is COMPLETE / EDGE VERIFIED and C12-29 S3 is
complete**. The separate CLT-B3
terminator-specific WebGL/WebGPU browser acceptance is not discharged by this
run.

**C13-41 COST-STATUS RECONCILIATION (2026-08-12; ⚠ SUPERSEDED 2026-08-14 by
`R-2026-08-14-1` — retained as the record of the reasoning the ruling reversed.
Its central premise is the 7.749 / 1.607 pair, for which audit S3 found NO
recoverable artifact; `refreshCostEstimateValidReportedOnly` no longer exists
and `refreshCostMeasured` gates again):** the
second Edge run already discharged `C13-41-ECLIPSE-REFRESH-COST-UNMEASURED`
with banked ABBA/warm-up-parity measurements of **7.749 ms/refresh WebGPU** and
**1.607 ms/refresh WebGL**. The post-run-6 closure criterion is the later and
controlling criterion: one redesigned-control run with all three lanes scoring,
`deckFreeGroundCapturesSettled` true, and `shadowContrastInvariant` passing or
converted to its named own-row defect. It does not require the historical cost
obligation to be re-discharged. Closure reruns therefore preserve cost segment
accounting and its exact INVALID reason as
`refreshCostEstimateValidReportedOnly`; a noisy negative re-estimate is neither
a speedup nor a gating product failure. Publishing a replacement performance
number requires a separately pre-registered completed-work paired estimator.

> **C13-16 U2 EVIDENCE CORRECTION — LOCAL/UNCOMMITTED; authored during the U2 lane, restamped 2026-08-21 at landing (Batch 1108).** **U2 INTERLEAVED A/B CAMPAIGN COMPLETE 2026-08-21 (pair b1108c, four immutable rounds banked in cesium-webgpu-visual-evidence): VERDICT REGRESSED.** Usable drift-clean rounds in both orders (r0 pre-first, r3 post-first; r1/r2 honestly discarded - their control sentinels caught real orchestrator-side CPU contention, named per round). Confirmed across both usable rounds: CloudShadow map ~+5% on all four shadow gates both genera; cumulus full-res ProceduralClouds +2..+12% by route; cascade atlas +9..+19%; cirrus full-res march CHEAPER by 5..15%; cumulus pixel identity byte-exact in every usable lane-round, so the cost is pure compute - consistent with the probe header's static register-pressure prediction (C13-39B's first live trigger). Known instrument limitation recorded: the half-res cone lane's timing is noise-dominated (sign-flips across usable rounds) and its rows are diagnostic, not gating. The row's no-regression acceptance is RED until register-pressure containment lands; the control-band materiality floor (0.21 ms, derived from same-bundle cross-round noise) is implemented and spec-pinned. Instrument-finding first-red pairs b1108/b1108b remain banked as the occupancy-blindness and pre-floor evidence. This
> supersedes the U2 headline scores embedded in the long `C13-16` row above;
> those `1.733 / 2.072 / 1.644 / 1.121 / 1.456 / 0.00258` values came from the
> research-only dynamic `gateMeanQuantile(cEff)` pivot, while the local WGSL uses
> the constant `GENUS_BASE_FIELD_MEAN`. The canonical model now explicitly uses
> that exact constant-pivot law. Its fresh full scores are CIRRUS **1.702**,
> CIRRUS/CUMULUS **2.035**, gate-E steps **1.619 / 1.119**, gate-D argmax 90 deg
> with **1.432x** rival margin, mean-opacity deltas **+3.2%** at the gate and
> **-13.2%** at the tour fixture, fixture-tail delta **-12.7%**, and predicted
> ground `changedFraction` **~0.00244** against the 0.002 floor (about 22% model
> margin). The constant-pivot choice adds no response LUT, uniform, resource,
> pass, allocation, or extra hot-path lookup; the dynamic pivot remains a future
> refinement. Three evaluators and visible/light/shadow paths are source- and
> mutation-pinned to budget -> height -> carve -> erosion with exact CUMULUS
> identity. **This is not landed or browser-certified:** visual acceptance and a
> mandatory interleaved performance A/B are owed. The `base >= full` proof is
> scoped to normalized `erosionLo` in `[0,1)`; the unvalidated public override is
> queued as `C13-16-EROSION-STRENGTH-RANGE-CONTRACT` rather than silently clamped.

### C13-01 Slice A launch evidence

This is an oracle/tooling slice only; it changes no engine renderer or cloud appearance.

- The shared harness writes `globe.defaultCloudCollection.volumetric`, rejects unknown fields, forces
  live rendering, and records exact configuration/render-mode/backend truth. The tour and temporal
  probes boot `offline=true`, pass their authored `JulianDate` into every manual render, capture the
  raw Cesium canvas, and arm the shared console/uncaptured-error/device-loss gate.
- A 2026-07-23 Edge/WebGPU route run on NVIDIA Pascal completed the canonical eight-segment,
  18,000,000 m to 302 m flight in 20.007 s with 1,118 measured frames and no page, external-network,
  or device errors. CPU `Scene.render` was p50 `2.90 ms`, p95 `6.00 ms`, p99 `8.60 ms`; diagnostic
  display pacing was `55.88 FPS` with a `46.77` 1%-low. This single run is characterization, not a
  promotion claim and not a cloud-on/off delta.
- The same route proves renderer realization in addition to API round-trip: the `1280x720` canvas
  owned a `640x360` raymarch target, a `640x360` temporal-history target, two live history textures,
  and ready raymarch, temporal-resolve, and upscale pipelines. Exact per-frame pass/sample counters
  remain `C13-02`.
- The repaired static tour produced `199,937` cloudish pixels at the east antimeridian and preserved
  billboard output on WebGPU/WebGL (`36,113`/`36,504` cloudish pixels), with clean error gates. The
  north-pole fixture produced `0` cloudish pixels and a uniform-blue raw canvas. Its visibility gate
  is intentionally RED.
- The medium temporal sequence applied its exact tier/time and captured static, moving, and settled
  frames without device errors. Visual inspection confirms severe ordered/repeating spatial structure;
  this is baseline defect evidence only because numeric ghost/flicker/history metrics are not yet
  implemented.
- The repaired adaptive-march maximum-throughput probe requires an explicit pair ID, distinct runtime
  bundles, matching adapter/browser/resolution/configuration, clean error truth, and visible clouds.
  Batch 733's `cloudQuality=128` escape-hatch run was RED with `0` cloud pixels and its
  sub-millisecond queue-drain timing was invalid. C13-35 later proved that this was a skipped
  command-empty environment frame, not a 128-step shader failure.
- Functionality preservation: WebGL volumetrics remain unsupported by design, while WebGL billboard
  and shared collection/API coverage remain active. Unsupported implicit workloads are recorded as
  skips; an explicitly requested incompatible workload fails. No feature was removed or disabled to
  obtain these measurements.
- Rollback boundary: Batch 733 Slice A is confined to Node/Playwright tools, workload metadata/tests,
  and documentation. The first runtime correction is isolated in C13-35/Batch 734 below.

### C13-35 Batch 734 scheduling/readiness evidence

- `WebGPUSceneRenderer` now keeps a command-empty frame alive only when a shared, side-effect-free
  environmental-demand predicate reports managed/user volumetric clouds, SSR, NPR outlines, contact
  shadows, weather, volumetric fog, or ground fog. With no frustums and no demand, the existing
  zero-work return remains.
- The predicate consumes no cloud request. The eventual environmental renderer remains the sole
  request consumer, and the same predicate governs the scene-color snapshot-copy decision.
- The black-sky Node/Edge acceptance holds `numFrustums=0` for all three eight-frame phases. Managed
  clouds active: resource setup, post-processing, environmental effects, procedural clouds, and
  canvas writes each execute `8/8`; the exact `128` primary/`8` light-step full-resolution path
  produces `7,584` cloud cells. A real user-owned VOLUMETRIC collection with the managed default off
  publishes and exposes demand `8/8`, reaches the same stages `8/8`, and also produces `7,584` cloud
  cells. All clouds disabled: those stages execute `0/8`, the image is exactly black, and the
  true-empty fast path is preserved.
- Managed/disabled images differ at `49,980` pixels (`6.3553%`); the user-owned phase has the same
  contribution. All 42 acceptance checks pass and the WebGPU error/device-loss gate is clean. The
  separate maximum-throughput characterization is green at `12.1847 ms/frame`; it is not a speedup
  claim because its fixed companion is stale and noncomparable.
- Functionality preservation: no effect or renderer was disabled to obtain the active result;
  WebGL billboard parity remains `36,113`/`36,504` evidence pixels. The procedural-tour pole/dusk
  gate now measures same-camera OFF/ON contribution instead of assuming all valid clouds are bright
  and neutral.
- Rollback boundary: the engine change is limited to non-consuming demand observation and the
  zero-frustum early-return/snapshot gates. Readiness/probe changes are independently removable and
  do not alter production configuration or rendering.

### C13-03/04 Batch 735 WGS84 primary-shell evidence

- Root cause: the old equatorial-radius sphere placed the modeled surface about `21.4 km` above
  WGS84 at the poles. A camera authored at geodetic `20,000 m` was classified below ordinary cloud
  decks, selected a far-side interval, and could be entirely erased by terrain depth. Weather was
  disabled and explicit high/low alone did not fix the wrong surface.
- Cloud deck boundaries now use axes `(a+h, a+h, b+h)` for WGS84 `a=6,378,137 m` and
  `b=6,356,752.314245179 m`. The high/low branch intersects those axes camera-relatively; explicit
  `cloudHighPrecision=false` retains a one-part f32 precision A/B while still using correct WGS84
  geometry. Production/default precision is on.
- The renderer reuses uniform slots `62–63` for the polar radius and CPU-f64 cartographic camera
  height; the layout remains exactly `148` floats. Camera/deck classification and multi-deck sorting
  no longer reconstruct altitude from a large f32 ECEF magnitude. View, light, and aerial-midpoint
  height fractions share precomputed oblate inverse axes, keeping division out of density/light
  inner loops.
- Nine source/math tests cover the old polar misclassification, equator, antimeridian, both poles,
  below/inside/above deck, both WGS84 boundaries, defaults/layout, and WGSL source ownership. The
  orbit oracle rounds every modeled operation to f32: worst root errors are `0.596 m` nadir,
  `38.547 m` near horizon, and `212.424 m` in a bounded near-grazing case; exact tangency is
  deliberately not claimed.
- The provenance-bearing default Node/Edge route is green at `21/21` checkpoints with 12
  deterministic transition frames per segment, minimum same-camera OFF/ON delta `135,330` pixels,
  default high precision `true`, and no page/GPU/device errors. It spans the antimeridian, both
  poles, `800 m` below-deck, inside/above deck, `200 km`, and `18,000 km`. The fixed-view explicit
  false/true precision A/B differs at `154/786,432` pixels (`0.020%`).
- The post-change moving cloud route remains clean at 1,117 frames over `20.004 s`: diagnostic
  `55.84 FPS`, CPU p50 `2.9 ms`, p95 `6.4 ms`, p99 `9.1 ms`, all eight altitude segments, and no
  device/page failures. An alternating steady-state explicit false/true queue-drain check measured
  `12.42–12.53 ms/frame` versus `12.45–12.52 ms/frame`; this is noise-level parity, not a claim
  comparing the new WGS84 shader with the removed sphere.
- Functionality preservation: WebGL billboard behavior and inert volumetric configuration remain
  intact; no cloud feature or renderer path was removed. This landing closed only the primary
  visible-shell geometry/height owner. Its then-open density-domain and temporal consumers later
  closed under `C13-37` and `C13-05`; standalone shadows/masks/capture/atmosphere and regional
  weather remain under `C13-06..08`. Gate B remains open.
- Rollback boundary: WGS84 uniform packing/default selection, the primary WGSL shell/height helpers,
  and their contract/probes/tests form one removable slice. No temporal attachment, weather
  resource, shadow topology, or WebGL shader is changed.

### C13-05 Batch 754 temporal RTE/coarse-reset evidence

- Premise/root cause: the half-resolution temporal resolver reconstructed a full-ECEF `f32` anchor,
  projected it through the absolute previous view-projection matrix, approximated the deck with an
  equatorial midpoint sphere, retained color history across incompatible frame gaps/tier/cull/deck
  transitions, and allocated a temporal bind group every frame.
- The current frame now unprojects with the inverse current VP-relative-to-eye transform and
  intersects configured WGS84 deck shells in camera-relative space. CPU `f64` computes
  `currentCameraWC - previousCameraWC`; the shader adds that delta to the current eye-relative
  anchor and projects through `previousViewProjectionRelativeToEye`. Encoded camera high/low data is
  reused from the primary march, so no full-ECEF `vec3<f32>` is reconstructed.
- `WebGPUCloudTemporalHistory.ts` owns an allocation-free coarse classifier/commit contract.
  Initial history, missing transforms, frame gaps, teleports over 50 km, scene/projection changes,
  morph, temporal re-entry, primary-deck changes, multi-deck topology changes, resize, and observed
  cull/re-entry all reject incompatible history. A persistent missing transform or morph remains
  current-only for safety, while the diagnostic generation/count advances only once per newly
  observed cause until the reset episode clears. Ordinary bounded camera, clock, and wind evolution
  retain history. Two parity bind groups are created with the history resources and selected
  without a hot-path allocation.
- `cloud-temporal-rte.spec.mjs` passes `11/11`, including WGS84 equator/dateline/both-pole/orbit
  f64 oracles, a RED legacy-polar fixture, reset truth, layout/allocation guards, source ownership,
  and Naga. `probe-cloud-temporal-rte.mjs` passes `22/22` in Edge/WebGPU with 26 finite 60-float
  temporal uploads, observed look-away cull/re-entry, tier/deck/multi-deck/resize transitions,
  maximum camera high/low reconstruction error `0.002723 m`, exact/stable source/build
  fingerprints, and no validation, device-loss, console, or page errors.
- Independent final review moved the 3×3 neighborhood clamp after shell/behind-camera/offscreen
  rejection, replaced general perspective VP inversion with inverse-view/projection composition,
  made orthographic/morph history explicitly current-only pending per-pixel ray-origin support, gave
  resize an explicit reset bit/generation, distinguished adjacent new reset causes from persistent
  causes, and made the never-active temporal-off path skip redundant bookkeeping. The complete cloud
  spec lane is `64/64` GREEN after those corrections.
- Functionality preservation: the primary march, high direct tier, medium temporal tier, public
  cloud controls, WebGL billboard path, and graceful WebGL volumetric no-op remain present. The
  repaired U8 preservation probe is byte-identical for billboard mode on both renderers.
- Scope/rollback: this closes planetary temporal coordinates, representative configured-deck
  reprojection, coarse compatibility resets, and parity bind-group retention only. Color-only
  half-resolution history remains; `C13-09/10/12` still own attachments, true 1/16 current work,
  wind/depth/disocclusion/variance/reactive rejection. The helper, temporal uniform packing/WGSL,
  retained bind groups, and focused tests/probe form the rollback boundary.

### C13-38 Batch 754 irrelevant cloud-IBL refresh suppression

- Premise/root cause: after `C13-37`, every published cloud appearance revision was a dynamic
  environment refresh reason even while the full reflected-cloud march was opted out. Visible-only
  animation could therefore schedule an irrelevant 256²×6 cube fill, IBL prefilter, and SH-L2
  projection.
- The revision gate is now
  `(wantMarch || cache.lastUsedCloudMarch) && liveCloudRevision !== cache.lastCloudRevision`.
  Current full-march animation still refreshes; revisions published while opted out remain
  unconsumed for the next opt-in; and a previous full-march environment still receives its one
  ON-to-OFF teardown refresh.
- `cloud-ibl-revision.spec.mjs` passes `4/4`, including 100 consecutive opted-out revisions.
  `probe-cloud-ibl-optout-revision.mjs` passes `12/12` in Edge/WebGPU: two off-animation/opt-in
  cycles, the teardown edge, exact/stable source/build provenance, and clean validation,
  device-loss, console, and page gates.
- Functionality preservation/rollback: no reflection, cloud, coverage-only IBL, public option,
  WebGL path, or quality setting was removed or weakened. This is proven irrelevant-work
  suppression, not a quantitative FPS claim. The one relevance predicate, table-driven test, and
  runtime scheduling probe are the rollback boundary.

### C13-36 Batch 736 ray-sample phase evidence

- The pre-existing tier contract is now real: `jitterEnabled` contributes `QF_JITTER` to uniform
  slot 74. The low/medium temporal tiers use a per-pixel Jimenez-2014 analytic
  interleaved-gradient-noise phase with a 64-frame sequence. Full-resolution T3 uses the same
  spatial field at frame zero because it has no temporal filter. The explicit numeric
  `cloudQuality` escape route retains `jitterEnabled=false` and the exact old midpoint expression.
- IGN is evaluated once per fragment outside the ray loop and requires no texture, sampler, bind
  group, upload, or external asset. It is blue-noise-like screen noise, not an STBN claim. The
  frame counter widened from 16 to 64 exact integer phases; Bayer UV jitter and cone-light jitter
  continue to mask the low four bits, preserving their existing 16-phase cycles.
- Only `t + phase * curStep` changes. `t`, `tProcessed`, conservative base-density testing, coarse
  backtracking, interval bounds, and loop progression are unchanged; base and full density sample
  the same position. The cloud-aware god-ray mask deliberately remains at midpoint because it has
  no temporal filter. Replacing that duplicate march with resolved/upscaled cloud alpha or a
  suitable MRT is retained under `C13-06`/`C13-22` rather than accepting mask shimmer here.
- `cloud-ray-jitter.spec.mjs` passes `6/6`: f32 spatial/temporal distribution, tier/flag wiring,
  midpoint fallback, 64/16 counter behavior, f32 interval containment from near range through
  orbit-scale distances, shared density position, and full-shader Naga validation. The engine
  build and TypeScript check are green.
- `probe-cloud-banding.mjs` produced valid provenance-bearing low and high artifacts with clean
  WebGPU/device gates. Low realized `24` primary steps at `512x384` with matching temporal history
  and `QF_JITTER`; high realized `96` steps on the direct full-resolution path with no temporal
  allocation. Its fixed-camera single and frame-32 high-tier PNGs are exactly identical
  (`0/786,432` changed pixels), proving this bounded temporal-off route does not sparkle.
- The low characterization records `547,928` single-frame and `556,378` settled interior cloud
  pixels. Its coherent-jump densities are `0.002843` and `0.010453`; without a separately built
  provenance-compatible pre-change companion these values do not establish an improvement.
  Manual inspection still shows the large-scale ordered baked-density lattice, which keeps
  `C13-37` open and next.
- The post-change moving altitude route covered all eight segments and 1,117 active frames over
  `20.016 s` with no page, device, or route failures. Diagnostic pacing was `55.80 FPS`; CPU was
  p50 `2.9 ms`, p95 `5.7 ms`, p99 `7.6 ms`. Compared with Batch 735's single-run
  `55.84 FPS` / `2.9` / `6.4` / `9.1 ms`, this is a no-regression characterization, not a promoted
  speedup. The medium temporal static/pan/settle smoke also completed without new errors.
- Functionality preservation: no cloud feature, quality tier, atmospheric consumer, or renderer
  path was disabled. WebGPU and WebGL billboard smoke remained visible (`59,911` and `36,504`
  neutral-cloud evidence pixels). Coarse temporal coordinate/reset handling later closed under
  `C13-05`; attachment-aware wind/weather/depth/disocclusion/variance/reactive rejection remains
  owned by `C13-12`.
- Rollback boundary: the tier flag packing, 64-phase counter, per-fragment IGN helper/phase
  parameter, and their two focused tools form one removable slice. Baked density resources,
  weather, shell geometry, reconstruction attachments, WebGL shaders, and public APIs are
  unchanged.

### C13-16 orchestrator Edge-acceptance checklist (per-genus morphology slice)

`cloud-genus-morphology.spec.mjs` (21 tests, counted in the shipped file) proves the coordinate
transform and the identity guards on the CPU. It cannot prove the GPU samples the new uniform row,
that the pipeline compiles the added functions, or that a viewer READS cirrus as ice. These browser
checks are the slice's acceptance and the row must not be promoted without them.

> **STATUS 2026-08-06 — EVERY item below is still OWED.** The probe that executes this checklist
> exists (`probe-cloud-genus-morphology.mjs`, Batch 830; items 1/2/3/5/9 scored, items 4 and 8
> captured for human verdict without inventing a scalar, items 6/7 other probes' vehicles, item 10
> deliberately omitted because a timing claim would require the mandatory interleaved-A/B protocol).
> It has been RUN and has produced NO verdict: Batch 836 fixed a missing `configure()` call that had
> the volumetric renderer reporting `executeCalls=0 / initialized=false / pipelineReady=false` — a
> convincing impersonation of a broken renderer — and the run then exceeded the probe's 420 s
> watchdog at `cloudQuality: 96`. Probe tuning is owed; **no item below may be ticked on the
> strength of that run.**
>
> **UPDATE 2026-08-06 (later) — the probe was phase-split and the `direction` phase RAN.** Items 2,
> 3 and 5 now have REAL verdicts, and all three MISS: cirrus elongation 1.178 against the 1.6 floor
> and 1.214 against the 1.4 ratio (item 2), a 60 deg argmax movement against 90 +/- 30 (item 3), and
> a cirrostratus->cirrocumulus margin of 1.081 against x1.1 (item 5). Items 1 (`neutrality` phase),
> 6, 7, 9-in-full and the two human-verdict PNGs remain owed. **These three misses are ONE
> phenomenon and it is now MEASURED**, per maintainer ruling R3: see
> `Tools/visual-regression/cloud-march-transfer.spec.mjs` and the `C13-16-SCREEN-ANISOTROPY-ATTENUATION`
> UPDATE in `DEFERRED_WORK.md`. **Item 6 below now carries a KNOWN-STALE justification** — it argues
> the carve depths were kept conservative because "elongation is provably independent of carve
> depth", which holds for the noise FIELD and is false for the integrated IMAGE, where carve depth
> (fibre `strength`) is the only lever that moves the on-screen number at all. Do not act on item 6's
> depth reasoning without reading the transfer model first; the two are in direct conflict and the
> resolution is a maintainer decision, not a probe run.
>
> **UPDATE 2026-08-07 — RULING R7 (option 3, the genus-conditioned base-field variance budget) has
> been EXECUTED IN THE MODEL, and the answer is STOP-WITH-FRONTIER. No shader, table or uniform
> changed.** The budget reaches gate C without touching an aspect, a strength or a bar — CIRRUS
> elongation **1.232 → 1.959 at +1.2% mean column opacity**, gate C's ratio 2.34, both gate E steps
> clear (1.878 / 1.109), gate D's argmax on the true wind axis at a **1.59×** margin against the
> baseline's 1.02× near-tie — but the SAME budget costs the `northatlantic-cirrus-fibratus` fixture
> **−47.5% of its column opacity and −43.1% of the tail statistic its `minChangedFraction` floor
> actually counts**. That is item 6's collision, arriving through a mechanism item 6 never named:
> not the fibre carve's depth, but the subtractive erosion's ZERO CLAMP, which is convex and
> therefore turns the base field's variance into mass wherever the deck is thin. Attribution
> executed: `erosionScale` 0 flips the same budget from −47.5% to +5.7%. **Item 6's premise is
> superseded twice over** — its "elongation is provably independent of carve depth" was already
> false of the image, and its instruction to reach for `FIBRE_MORPHOLOGY[CIRRUS].strength` when the
> floor is missed is now the wrong knob as well. Full frontier table, the refuted variants, and the
> two concrete unblockers are in `DEFERRED_WORK.md` §`C13-16-SCREEN-ANISOTROPY-ATTENUATION`
> UPDATE 2026-08-07; the numbers are pinned by `cloud-march-transfer.spec.mjs` (49/49).
> **Do NOT run a browser acceptance for this ruling — there is nothing new to accept.**
>
> **UPDATE 2026-08-09 — U2 is implemented LOCALLY and remains UNCOMMITTED.** The exact constant-pivot WGSL/model twin clears the model gates and predicts this item's floor at **~0.00244** against 0.002 (about 22% model margin), not the superseded dynamic-pivot study's 0.00258. Visual acceptance and mandatory interleaved performance A/B remain owed; this is not a browser verdict. Item 6 below is the gate that decides it, and its own reasoning is still superseded — the knob is the COMPOSITION, not `FIBRE_MORPHOLOGY[CIRRUS].strength`.

1. **Default byte-neutrality — the first gate, and a hard equality.** Capture the DEFAULT scene
   (no `cloudType`, i.e. CUMULUS) before and after this change at a pinned `JulianDate` and camera
   and require **exactly 0 changed pixels**, not a small diff. The whole design rests on three
   explicit early returns; a nonzero diff means one of them is not being taken and the finding is
   more important than the feature. Use the existing off-identity vehicle
   (`probe-cloud-u8-offident.mjs`) plus one cloud-bearing view.
2. **Cirrus-vs-cumulus DISCRIMINATION, structurally — not by brightness.** The recorded trap is that
   a faint wide-band cloud change is invisible to a band mean, and the inverse trap is that a mean
   difference proves nothing about SHAPE. Capture the same camera/clock/coverage twice, once with
   `cloudType: CloudType.CUMULUS` and once with `CloudType.CIRRUS`, and compute a DIRECTIONAL
   statistic on the cloud pixels: the ratio of luminance autocorrelation length along the projected
   wind azimuth to the length across it. Cirrus must be markedly anisotropic and cumulus must be
   near-isotropic. A whole-frame or band mean is NOT acceptable evidence here.
3. **The wind vector actually steers the streaks.** Re-run the cirrus lane with the wind rotated 90°
   and require the measured elongation axis to rotate with it. This is the check that separates a
   real wind-aligned domain from a fixed diagonal texture artifact, and it is cheap.
4. **Fallstreak tilt is visible.** From a near-horizontal view of a cirrus deck, the filament's lower
   edge must sit downwind of its upper edge. Read the PNGs; a scalar cannot settle this one.
5. **Cirrostratus stays a SHEET and cirrocumulus stays GRANULAR.** The table deliberately gives the
   veil a shallower carve (it is the halo genus) and the mackerel field a near-round aspect. Capture
   all three ice genera at one camera and confirm the grain ordering
   cirrus > cirrostratus > cirrocumulus, matching the spec's measured 8.10 / 4.65 / 1.95.
6. **The tour fixture that filed this row — and the one place this can regress.** Re-run
   `northatlantic-cirrus-fibratus` in `probe-cloud-tour.mjs`. Its floor gate
   (`minChangedFraction` 0.002) is the risk: the recorded post-CLOUD-LOW-COVERAGE-CUTOFF values are
   ground **0.0028** and above-deck **0.0148**, and the fibre carve retains roughly **0.59** of the
   deck's mean mass, so the ground station lands near **0.0017** in the worst (purely multiplicative)
   case. ~~That is the whole reason the carve depths were kept conservative — elongation is provably
   independent of carve depth (asserted in the spec), so depth buys morphology nothing. If ground
   comes in under the floor, the correct response is to lower `FIBRE_MORPHOLOGY[CIRRUS].strength`,
   NOT to lower the fixture floor, and NOT to raise the anisotropy.~~ **SUPERSEDED by R7's execution
   (2026-08-07) — both halves of that reasoning are now known false.** Elongation is independent of
   carve depth in the noise FIELD only; in the integrated image, depth is the only lever that moves
   it. And the fixture's real fragility is not the carve at all: at its coverage **0.45** the mean
   subtractive erosion EXCEEDS the mean gated density, so this deck exists only as the base field's
   upper tail, and the tour's `changedFraction` gate is a TAIL count, not a mean. Any reduction of
   the base field's variance — the mechanism R7 chose — therefore lands here first: the budget that
   clears gate C at +1.2% opacity at the direction probe costs this fixture **−47.5%** of its column
   opacity and **−43.1%** of the tail. `FIBRE_MORPHOLOGY[CIRRUS].strength` is no longer the knob this
   item should reach for; see `DEFERRED_WORK.md` §`C13-16-SCREEN-ANISOTROPY-ATTENUATION` UPDATE
   2026-08-07 for the frontier and the two concrete unblockers. **Both unblockers have since been
   evaluated (UPDATE 2026-08-07 later): the carve-before-erosion composition plus a genus-conditioned
   erosion-depth compensation predicts this station at **~0.00244 — about 22% model margin** — while
   clearing gates C/D/E under the exact constant-pivot WGSL law. That is the local pre-registered
   number to score against.** Still report the measured number.
   Its `gate.why` note — "Genus MORPHOLOGY (fibrous streaks vs generic puffs) remains C13-16" —
   should be rewritten in the same landing once the pixels support it. Do NOT rewrite it before the
   capture.
7. **Shadow/visible agreement.** The fibre factor rides in `cloudMacroSampleAt`'s shared factor, so
   the beer-shadow map sees the same filaments. With `cloudCastShadows` on, the cast ground shadow
   under a cirrus deck must be streaked, not blobby — `probe-cloud-shadows-flagon.mjs` is the vehicle.
8. **Phase sanity — the most likely tuning casualty.** `genusForwardG` clamps the cirrus forward lobe
   to 0.95 against the default 0.85, roughly a 10x forward peak. Capture a near-sun cirrus view and
   confirm the forward glow reads as a bright gradient rather than a clipped white disc. If it blows
   out, the fix is `GENUS_PHASE_G_LIMIT`, and the number should be reported rather than quietly
   retuned.
9. **Gate.** `scene.context.rendererType === "webgpu"` on every lane, zero new device/console errors,
   canvas-element PNGs captured same-task, per-view local-noon clock, and a discarded warm-up render
   before the fixture loop (the async-prewarm cold start renders fixture 1 stable-black).
10. **Occupancy note.** `C13-39` closed NEGATIVE on the finding that WGSL register allocation is
    STATIC — code compiled into the module taxes every pipeline regardless of runtime branches, and
    this pass is occupancy-bound. This slice adds two runtime-guarded helper functions plus reordered
    arithmetic in all three density evaluators. If a timing lane is run at all it must use the mandatory
    interleaved-A/B protocol in
    `probe-cloud-lod-hoist-perf.mjs`'s header; a single-ordering comparison is not evidence.

### C13-08 orchestrator probe checklist

> ⛔ **STALE — DO NOT ACT ON THE ACCEPTANCE VERDICT IN THIS SECTION (stamped 2026-08-07, close-out
> docs reconciliation). The prose is retained deliberately; only its verdict is superseded.**
> This section still opens "these browser checks remain OPEN and are the row's acceptance" and
> closes "the seven checklist probes in items 1-2 have no post-Batch-797 evidence. The row and
> Gate B therefore remain IN PROGRESS / NO-GO". **Both statements were overtaken by Batch 866
> (`58af0d1819`), which CLOSED Gate B on one build (tip `193393790c`).** The authority is the
> Batch-866 instrument roster recorded in the `C13-GATE-B` row of §9, not this checklist:
> `edr-mock` **3/3**, `wcs` **3/3**, `ingest` **3/3**, `seam-poles` **3/3** (Batch 860);
> `time` **exempt-by-proof** (Batch 855); `metar` **GREEN** via the Batch-861 discriminator, with
> checklist item 2's own instruction honoured exactly — the separation was reported, not re-tuned:
> gate 4 measured **sum|ch1−ch0| = 0.1469 against the unchanged 0.04 bar** (3.7x clear), 22/30
> calibration candidates in band, scored-leg headroom summed |delta| 0.0000 against a 0.015
> tolerance; `channels` **10/10 GREEN** with the scored rich-fraction vector byte-identical across
> all ten runs (mean 0.8758, stddev 0.2889, spread 0.0000). No assertion was widened or lowered at
> any point in that chain.
>
> **Why the prose below is kept rather than deleted:** its per-item procedure is still the correct
> method for any future re-run of this fleet — which probe drives which field, why item 2's
> separation must be reported rather than re-tuned, the item-3 regional placement pixel gate
> design (inside/outside the rectangle, compared against the no-provider procedural fallback), the
> item-4 antimeridian seam conditions, and the item-5 `getPackStats()` cross-check asserted in the
> same page evaluation as the capture. Read it as a runbook, not as a status.

The CPU twin (`weather-field-bounds.spec.mjs`, 31/31) proves the packer's bytes. It cannot prove the
GPU consumes them, so these browser checks remain OPEN and are the row's acceptance:

1. **Regression floor.** Re-run `probe-weather-ingest.mjs`, `probe-weather-edr-mock.mjs`,
   `probe-weather-wcs.mjs`, `probe-weather-channels.mjs`, `probe-weather-time.mjs`, and
   `probe-weather-seam-poles.mjs`. All six drive GLOBAL fields and the spec asserts their bytes are
   unchanged, so any red here is a real regression, not a threshold artifact.
2. **`probe-weather-metar.mjs` — the intended behaviour change.** Its far-field is now the procedural
   map instead of a fabricated clear hole. The clear-station (`-120°`) and cloudy-station (`0°`)
   views are both inside the 40° influence radius and must still separate; if the separation shrank,
   report the numbers rather than re-tuning the source.
3. **NEW probe — regional placement (the row's own pixel gate).** Drive a REGIONAL field
   (`provider.setRequest({ bounds })` with `SyntheticWeatherSource("uniform", 1.0)` over a CONUS-ish
   rectangle) and capture two nadir views at a pinned clock: one INSIDE the rectangle and one far
   OUTSIDE it (e.g. mid-Indian-Ocean). Inside must be overcast; outside must match the SAME view with
   NO provider attached (the procedural fallback), because that is exactly what the fill writes.
   Pre-fix, the stretched field made both views overcast. Read the PNGs; a diff number alone is not
   the claim. Follow the C13-07 probe lessons: per-view local-noon clock, a discarded warm-up capture
   before the fixture loop, camera-local altitude (~250 km, not orbit), canvas-element PNGs.
4. **Antimeridian parser + rendered region — COMPLETE IN THE CURRENT WORKTREE;
   LANDING REVIEW OWED.** Ingest CoverageJSON whose longitude
   axis crosses `+180/-180`; the 7/7 focused lane now asserts the parser derives
   the same wrapped ~20° `WeatherField.bounds` and bytes for forward/reverse
   encodings. Still render cameras on both sides of the seam.
   The observed band must be continuous, must not duplicate, and far-away
   views must remain procedural fill. A manually supplied `170°E → 170°W`
   bounds lane remains only the packer control. The rebuilt Edge lane satisfies
   both-side continuity, no-duplicate/no-wall, far-fill, same-evaluation stats,
   and clean-error gates; all PNGs were reviewed.
5. **Pixel-free cross-check.** `provider.getPackStats()` must report `global: false` with both
   `observedTexels > 0` and `filledTexels > 0` for every regional lane, and `filledTexels === 0` for
   every global lane. Assert it in the same page evaluation as the capture (same-task capture rule).
6. **Gate.** `scene.context.rendererType === "webgpu"` on every lane, zero new device/console errors.

**2026-08-02 Codex rendered-tail acceptance + promotion-audit update:**
`probe-weather-regional-tails.mjs` now persists the two previously one-off/open
tails. Its WebGPU arm intercepts a real cyclic-axis CoverageJSON EDR response,
hard-gates the parser-derived `170..190°` field, captures both sides of the seam
plus a seam-centred and far-away control, and couples every capture to regional
`getPackStats()`. Its WebGL arm explicitly packs and attaches the same provider
to a volumetric-mode billboard collection, then requires unchanged billboard
pixels and zero volumetric publications. The companion fixture/policy/capture
contract has 10/10 pure-Node tests, including mutations for a duplicated band,
a missing seam side, a centre wall, changed WebGL pixels, and WebGL volumetric
publication. **Rebuilt Edge acceptance is GREEN:** every listed arm passed,
device/console/page errors were empty, and all ten PNGs were visually reviewed.
The WebGPU regional east/west/seam images show the intended continuous 100%
overcast fixture; the far pair and WebGL billboard pair are byte-identical.
An independent promotion audit retained that evidence but found the regional
cell-registration edge extrapolation described in the dated overlay; it is now
fixed and pinned by the 31/31 bounds suite. The same audit confirmed that the
seven checklist probes in items 1-2 have no post-Batch-797 evidence. The row and
Gate B therefore remain IN PROGRESS / NO-GO until those browser regressions are
rerun after this packer change; landing review alone cannot promote them.

---

## 10. Source pointers

### Current implementation

- `packages/engine/Source/Renderer/WebGPU/WebGPUProceduralCloudRenderer.ts`
- `packages/engine/Source/Renderer/WebGPU/WebGPUCloudTemporalHistory.ts`
- `packages/engine/Source/Renderer/WebGPU/WebGPUCloudShadowFrame.ts`
- `packages/engine/Source/Renderer/WebGPU/WebGPUCloudTierPresets.ts`
- `packages/engine/Source/Renderer/WebGPU/WebGPUCloudNoiseResources.ts`
- `packages/engine/Source/Renderer/WebGPU/WebGPUDynamicEnvironmentMapManager.ts`
- `packages/engine/Source/Shaders/WebGPU/Environment/ProceduralClouds.wgsl`
- `packages/engine/Source/Shaders/WebGPU/Environment/CloudTemporalResolve.wgsl`
- `packages/engine/Source/Shaders/WebGPU/Environment/CloudUpscale.wgsl`
- `packages/engine/Source/Scene/CloudVolumetrics.js`
- `packages/engine/Source/Scene/CloudCollection.js`
- `packages/engine/Source/Scene/Weather/WeatherProvider.ts`
- `packages/engine/Source/Scene/Weather/WeatherTexPacker.ts`
- `packages/engine/Source/Renderer/WebGPU/WebGPUSceneRendererEnvironmentDemand.ts`
- `Tools/visual-regression/cloud-primary-shell.spec.mjs`
- `Tools/visual-regression/cloud-ray-jitter.spec.mjs`
- `Tools/visual-regression/cloud-temporal-rte.spec.mjs`
- `Tools/visual-regression/cloud-shadow-rte.spec.mjs`
- `Tools/visual-regression/cloud-ibl-revision.spec.mjs`
- `Tools/visual-regression/probe-cloud-banding.mjs`
- `Tools/visual-regression/probe-cloud-temporal-rte.mjs`
- `Tools/visual-regression/probe-cloud-ibl-optout-revision.mjs`
- `Tools/visual-regression/probe-cloud-empty-frustum.mjs`
- `Tools/visual-regression/probe-cloud-planetary.mjs`
- `Tools/visual-regression/probe-cloud-tour.mjs`

### Durable project context

- `FABLE5_PROGRESS_AND_ACTION_AUDIT_2026-07-22.md`
- `QUEUE_2026-07-18_CAMPAIGN11.md`
- `campaign11_planning/guides/G12-clouds-weather.md`
- `CLOUD_LOD_RESEARCH_2026-07-05.md`
- `CLOUD_UNIFICATION_DESIGN.md`
- `ATMOSPHERE_CLOUD_IMPROVEMENT_PLAN.md`
- `WEATHER_DATA_INGEST_ROADMAP.md`
- `WEATHER_RECREATION_ROADMAP.md`
- `DEFERRED_WORK.md`
- `FEATURE_INVENTORY.md`
- `DEBUGGING_GUIDE.md`
- `CLOUD_COORDINATE_CONTRACT_2026-07-23.md`

Line numbers in older reports are hints. Re-grep symbols at the start of every brief.
